class Vector {
  float x;
  float y;

  init(float x, float y) {
    this.x = x;
    this.y = y;
  }

  float lengthSquared() {
    return this.x * this.x + this.y * this.y;
  }
}

Vector vector = new Vector(3.0, 4.0);
print(vector.lengthSquared());
