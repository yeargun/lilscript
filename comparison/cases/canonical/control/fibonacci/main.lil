int fibonacci(int n) {
  int prev = 0;
  int current = 1;
  for (int i = 0; i < n; i++) {
    int next = prev + current;
    prev = current;
    current = next;
  }
  return prev;
}
print(fibonacci(12));
