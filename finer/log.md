# Hypothesis log

One row per hypothesis, newest first. The verdict cell is the folder's summary: open the folder only
when the row is the idea you are about to have. Lanes are `lang` · `port` · `compiler` ·
`measure` ([README](README.md#lanes)). `node finer/tools/new.mjs <slug> --lane <lane>` opens the
next folder and prints its row; `node finer/tools/new.mjs check` keeps rows and folders in step.

| # | lane | question | verdict |
|---|---|---|---|
| [047](hypotheses/047-declarations-are-spelled-one-per-binding/README.md) | compiler | Is one `var` per module binding, each initialised `void 0`, a generic loss Terser's `join_vars`/`unused` class takes, and do three peephole folds recover it? | **OPEN.** Hand rewrites of katexlil's ESM: −88 / −240 / −388 Brotli for initializer-off / + joins / + assignments folded; folds landed with node-oracle tests; portfolio build pending. |
| [046](hypotheses/046-katexlil-is-untyped-so-closed-equals-open/README.md) | port | Is katexlil's +5800 the port's shape, and why is its closed lane bigger than its open one? | **Confirmed.** Closed (`extern_fields=false`) is byte-identical to open: the port owns no fields, every object is a `JsValue` bag and every class a prototype table; the site's closed lane was level 8. Loss sits in the glue and hand-built classes (`entry` +358, `symbols` +331, `Settings` +155, `buildHTML` +143), 60/88 modules beat Terser. The 1960 inlined string constants are −8207 raw but +129 Brotli as names, so the compiler's rule stays. Shipped: build cache key carries the binary (−1255 unmeasured until now), the pre-expanded unicode table → generator loop, a Playwright benchmark and a results generator replacing a 12× perf claim that did not reproduce. Defects: `mangle.exports=true` ABI mismatch; level 0 unresolved export. |
| [045](hypotheses/045-single-use-declarators-are-left-standing/README.md) | compiler | Can a canonical declarator-substitution fold with Terser's stop rules take the 42 micromark sites we leave? | **OPEN.** Harvest: Terser collapses right to left across statements with a dozen stop rules (`tighten-body.js:278-970`), Oxc substitutes single-use declarators (`minimize_statements.rs:1149`), ours are beam-only and refuse effectful initializers; 243 of 244 micromark sites have nothing observable in between. −119 / −151 priced; ≥ −100 confirms |
| [044](hypotheses/044-a-ternary-arm-loses-its-parentheses/README.md) | compiler | Why does 041's narrowed build fail `node --check`? | **Confirmed and fixed.** `fold_common_conditional_arms` re-emitted a stripped arm bare; `conditional_arm_text` now re-parenthesises an arm holding a top-level comma and admission refuses `?a,b:c`. Seven shapes node-identical; the 041 narrowed artifact is refused before the fix and admitted after. Terser, Oxc and Closure own precedence in the printer; ours lives per fold, as 040's adjacency lives at the splice |
| [043](hypotheses/043-statement-fusion-is-mostly-absent/README.md) | compiler | Is the `return E,V` fold a silent non-runner at level 13, and is boundary absorption worth ≥ 100 on micromarklil? | **Falsified.** Terser's own `sequences`, removed from its defaults, costs at most +3 Brotli on four ports, and micromarklil's shipped artifact already carries the fusion through its esbuild `minifySyntax` step (+20 when the compiler's file arrives fused). The fold does land 0 of 162 sites — the late cleanup finds a zero ledger on 15 of 19 entries, still 0 at 8192 probes — and is worth −19 by hand. Terser's band is `collapse_vars` (+280) and `unused` (+296) on micromark: single-use assignment collapsing, 013's `var ` gap |
| [042](hypotheses/042-owned-bags-are-still-jsvalue/README.md) | port | Can jquerylil's five compiler-owned bags be re-typed so their reads stop blocking fusion? | **Falsified at the claimed size, confirmed in direction: −57 Brotli** (support alone −68), 25 of 013's 405 stores. A typed read stops blocking but stays non-deferrable, and the level-15 search's source-dependent `function-spelling` flip (+99 / +141 on two states) outweighs every per-bag effect. Found on the way: **shipped jquerylil's `scrollTop` TypeError is a `this`-using method spelled as an arrow** by that candidate family, which admission must refuse |
| [041](hypotheses/041-the-local-rename-starves/README.md) | compiler | Does `converge_local_names` never run on the jquerylil tree build, and is that the 1045 Brotli? | **Falsified as stated, then landed.** The ledger never starves it; it rewrote nothing because a second `var t` in one function made the resolver's answer non-total. Narrowed to unsound scopes (behind 044): **jquerylil −663, remarklil −2094, rehypelil −766, remark-gfmlil −277, markedlil −62**, no port worse, 1663 tests, every changed port's suite green. The 1045 dates to 2d2268a's `reserve_enclosing_js_bindings`; 40 header spellings remain against Terser's 24 |
| [040](hypotheses/040-splices-have-no-adjacency-rule/README.md) | compiler | Why does shipped jquerylil call an undeclared `returnHr`? | **Fixed at the class level.** `fold_assigned_truthy_ternaries` rewrote `return(n=Ir(…))?n:X` to `returnIr(…)||X`; the fused name hid a use, the callee was inlined and deleted, and the repair fold refused. Terser cannot do this because its printer owns adjacency (`output.js:595`); the same rule now sits in `apply_token_rewrites`, the one splice every fold uses |
| [039](hypotheses/039-terser-spells-names-by-frequency/README.md) | compiler | Is the −1136 Terser's mangle finds on jquerylil the spelling of names, not their length? | **Falsified: −27.** We already spell from frequency alphabets. The gain is Terser's *local* rename (−765) and it exists only on the working-tree artifact: the committed one is **1045 Brotli smaller** and Terser *loses* 274 on it, because `converge_local_names` starves under the budget in the tree build (90 distinct parameter-header spellings against 25). Found on the way: **shipped jquerylil throws `ReferenceError` on `animate`** — `returnHr(...)`, a keyword fused into its operand |
| [038](hypotheses/038-the-loop-runs-on-one-host/README.md) | measure | Can the fleet be built across the Azure pool instead of this host? | **OPEN.** The owner's brief names a pool of Azure machines for parallel compiles; nothing in the repo knows which machines or how to reach them, and every tool serializes on this host |
| [037](hypotheses/037-react-markdown-miscompile/README.md) | compiler | Why is react-markdownlil 14166 behind? | **Fixed.** Its `terminal_codec_probe_limit = 0` hid a miscompile: two prototype-alias folds asked "read *after*?" and a hoisted function 61 KB *before* the class reads the alias. `binding_is_observed_outside_span` asks the whole-program question with exact shadowing. The port drops the line for **−3351 Brotli**, 120/120; unified and remark, which bundle the same VFile, stop throwing on import; mobx −130, rehype +99 from the search re-deciding |
| [036](hypotheses/036-the-fix-exists-and-starves/README.md) | compiler | Do we need to build scope-aware shadowing? | **No, and we do not want it.** `precise_cross_scope_shadowing` already exists and starves — but forcing it on gives module bindings their one-char names exactly as 035 predicted and costs **+432 Brotli**, because those names come out of the locals' pool. 035's premise falsified: the 62-of-63 two-char naming is the *better* trade. Every budget knob was byte-identical for the same reason |
| [035](hypotheses/035-where-the-compiler-headroom-is/README.md) | compiler | What is left for the compiler, measured? | **Not the printer** — a pure reprint of our output saves 0 bytes. It is **name allocation**: we give **62 of 63 top-level bindings two-char names** where Terser gives 53 of 58 one-char ones, because module and local names come from **disjoint pools** while Terser lets locals shadow. Terser extracts **−884 / −856 / −1136** from finished micromark / mobx / jquery artifacts. `local_name_reserve` swept and already optimal |
| [034](hypotheses/034-escaped-non-ascii/README.md) | port | Why does a *reprint* of our output save 7 KB? | **The bundler escaped 2304 non-ASCII characters** into `\uXXXX` — six ASCII bytes where the literal is two or three UTF-8 ones. Our own output has 48 bytes of slack; the bundle has 7021. `charset: "utf8"` fixes it: **−27689 raw and −1093 Brotli** across micromarklil and rehypelil. Fourth defect living between compiler and artifact |
| [033](hypotheses/033-member-bodies-are-scopes/README.md) | compiler | Why could the resolver not find that binding? | **A member body that is not recognised is not a scope**, so its `var`s leak to the module and poison the name. Accessors, `static`, generators, computed names and **keyword-named members** (`delete(e){...}`) were all unrecognised. Fixed: **mobxlil emits 018's ten classes at last**, −6786 raw / −235 Brotli, no other port moves |
| [032](hypotheses/032-export-resolver-false-negative/README.md) | compiler | Why does mobxlil still emit 0 classes? | **Our export resolver cannot find a binding node runs fine.** The pipeline turns mobx's shipped artifact into 10 classes and **−769 Brotli**; admission refuses it for an "unresolved export binding" that `node --check`, esbuild, and an actual `import` all resolve. Second admission false-negative in two hypotheses, both refusing the class rewrite |
| [031](hypotheses/031-admission-blocks-the-class-rewrite/README.md) | compiler | Why does the canonical peephole never land? | **Admission rejects it for having a `constructor`** — the class rewrite's own keyword, counted as a new static property, at a gate 018 never counted. Exempting it globally measures **+826 worse** (it changes the whole portfolio); exempting it only where an already-won artifact is re-checked is **−194 Brotli** on micromarklil and −29 on markedlil |
| [030](hypotheses/030-the-build-undoes-the-compiler/README.md) | port | Is the port, not the compiler, why we do not get smaller? | **For micromarklil, yes — its build script.** esbuild with `minifySyntax:false` re-prints `!0` as `true`, discarding all 87 compact booleans the compiler chose. **−4538 raw, −229 Brotli**, 1963/1963 tests pass. Third time a tool *between* compiler and artifact has been the whole story |
| [029](hypotheses/029-specialisation-is-not-the-lever/README.md) | compiler | Is cloning what costs us the repetition class? | **No — every anti-cloning switch makes it bigger.** call-site specialisation off is +45, all three off +48, the folding switches are already on. On jquerylil `region-outlining` changes **nothing** and the documented phi lever is worth 5 bytes, not 87. Every config surface is now exhausted for this class |
| [028](hypotheses/028-unminified-lil-lane/README.md) | measure | Why does `unified` emit 87 `var` against Terser's 12? | **It does not — the harness never minified our lane.** For the three `lilBundlePorts` the Lil side is a raw esbuild bundle measured against a Terser-minified official. **10634 Brotli of reported loss was the harness**: remark +10395 -> +5988, react-markdown +18552 -> +12923, unified +808 -> +264 |
| [027](hypotheses/027-tuning-is-exhausted/README.md) | port | Can config tuning close the two nearest losses? | **No.** 20 builds over remark-math (+137) and unified (+234) buy 10 bytes, and those 10 fail 5 tests. Beam width is worth 47 bytes on posthog and **zero** on unified; level 13 beats 15 on unified and loses to it on remark-math. What is left is volume: unified emits **47% more functions, each 29% bigger** |
| [026](hypotheses/026-the-missing-classes/README.md) | compiler | Where did 018's ten `class` declarations go? | **Generated, then corrupted — but that is not why they are missing.** The fold does destroy a valid ten-class artifact 36 times a build, and candidates *are* refused before admission counters see them. But disabling it changes nothing (0 classes either way, Brotli identical), so my causal claim was false |
| [025](hypotheses/025-brotli-repetition-gap/README.md) | measure | Do we lose Brotli because our output compresses worse? | **No — because we emit more code.** Repeat-coverage gap predicts nothing (r=+0.13); raw excess predicts almost everything (**r=+0.94** over 15 ports). Every win emits less JS than Terser, every loss more, by 9.5–85%. Our compression ratios are already competitive |
| [024](hypotheses/024-optional-chain-floor/README.md) | compiler | Does the ES syntax floor actually stop forbidden syntax? | **Not for `?.`.** The check matched a `"?."` token the lexer never emits (it emits `?` then `.`), so the arm was unreachable at every edition. Fixed by adjacency. Latent only: every port targets ES2022 |
| [023](hypotheses/023-unparseable-class-expressions/README.md) | compiler | Why did `--measure` fail once all 16 ports verified? | **The compiler emitted unparseable JavaScript.** `emit_class` keyed its terminator off the keyword, not the shape, so `var u=class VFile{...}` came out bare and ran into the next statement. remark and unified had no working build. Fixed; 80/80 artifacts parse; **rehype and remark-gfm flip to WIN** |
| [022](hypotheses/022-harness-refresh/README.md) | measure | Can the project's own scoreboard be regenerated? | **For 11 of 16.** Five ports' `package.json` drifted from the manifest and the harness fails closed — that is why REPORT.md is stale. The eleven that run: **every one improved or held, −3202 total, and remark-gfm flips LOSS → WIN** |
| [021](hypotheses/021-reflective-ffi-predicts-loss/README.md) | port | Why are the receivers dynamic in the first place? | **The losing ports are JavaScript transliterated through a host FFI.** Reflective calls per kloc order the scoreboard: markedlil **0.8 → −579 WIN**, micromarklil **178 → +4154**. But the obvious remedy was tested twice on a real file and **regressed both times** — partial typing cannot pay because the helper layer is `JsValue`-typed end to end |
| [020](hypotheses/020-unstable-transitivity/README.md) | compiler | The `unstable` closure propagates from every operand; narrowing it to fusible ones should cut the named-temporary excess | **Sound, and reverted.** Cuts unstable 12% but nets **+69 Brotli** (−56 micromark, −38 mobx, **+29 marked, +81 jQuery**). Fewer unstable values is not fewer bytes |
| [019](hypotheses/019-one-mechanism/README.md) | lang | Are the eleven scoreboard losses eleven problems? | **No — one mechanism, confirmed on two unrelated families.** `JsValue` receivers escape untyped, so values are `unstable` and each takes its own named statement: +7.6%/+19.4% identifiers, +113%/+42% semicolons. Source typing prices at ~10%, SSA destruction at ~90% |
| [018](hypotheses/018-mobx-admission-regression/README.md) | compiler | mobxlil is +3577 with a frozen source — is that a compiler regression too? | **Reframed.** −7546 raw / −253 Brotli across `42c1ad0..edbdf3a`, but the admission gate is **load-bearing**: removing it emits unparseable JavaScript. The bytes are the cost of correctness, not a revertible mistake. Seven mechanisms falsified; 59s repro |
| [017](hypotheses/017-oxc-declaration-merge/README.md) | compiler | oxc merges `var a; a=b()` into one declaration and LilScript does not — is that the statement-density win? | **No. Implemented, tested, measured at zero, reverted.** LilScript's SSA destruction already emits initialized declarators; the target shape occurs 0–3 times per artifact |
| [016](hypotheses/016-marked-size-regression/README.md) | compiler | markedlil's committed artifact is smaller than what any current compiler produces — is there a regression? | **Yes: `593f048`, `src/compiler.rs`, +1568 raw (4.7%).** Statement merging is lost (+479 `;`, −388 `,`); six fewer candidates reach evaluation. Three mechanisms falsified; reported not patched |
| [015](hypotheses/015-does-this-work-help/README.md) | measure | Do these compiler changes help the *shipped* sibling libraries, not just the in-repo benchmark ports? | **Caught a regression of my own.** One change cost **+282 Brotli** on jQueryLil; reverted. Everything else is byte-identical to HEAD there |
| [014](hypotheses/014-dirty-tree-scoreboard/README.md) | measure | Are the reported losses even measured against committed artifacts? | **No.** 18 ports have uncommitted `dist` changes. Re-measured, the markdown stack is **8 W / 6 L, −23588 Brotli**, not 6 W / 10 L, +28435 |
| [013](hypotheses/013-statement-density/README.md) | lang | jQueryLil's remaining +780 is one mechanism: surplus assignment statements | **Confirmed and priced at −540 Brotli (69% of the gap).** Cause is the getter-hook assumption making 1681 values unstable. The flag that fixes it is Terser's `pure_getters`, which the baseline also has off — so the honest fix is a plain-data *type*, not a flag |
| [012](hypotheses/012-port-scoreboard/README.md) | measure | Where does every `*Lil` port actually stand against upstream? | **Measured.** 21 comparable ports: 10 W / 11 L. Two `REPORT.md` verdicts were stale and flip to wins; jQueryLil's real gap is **+780**, not +4038 |
| [011](hypotheses/011-string-pooling-under-compression/README.md) | compiler | The pooling *benefit* model credits every repeat at full width, which is only true for a raw objective | **Confirmed.** Threshold tuning is non-monotone and was rejected; jQueryLil drops pooling for **−35 Brotli** |
| [010](hypotheses/010-string-pool-alias-pricing/README.md) | compiler | The string pool admits aliases that lose bytes because it prices every alias as one character | **Confirmed, landed.** Small win (−3 Brotli on jQuery); the cost model was simply wrong |
| [009](hypotheses/009-search-starvation/README.md) | compiler | The search is starved, not too narrow — higher levels dilute rather than deepen | **Confirmed and landed.** Probe ladder retuned; jQuery −58 Brotli, acorn −8, level 13 now beats level 15 |
| [008](hypotheses/008-jquery-compressibility-gap/README.md) | port | jQueryLil's Brotli loss is a compressibility problem, not a volume problem | **Confirmed.** raw +2.7% but Brotli +11.7%; property mangling is a total no-op on this port |
| [007](hypotheses/007-level-13-sweet-spot/README.md) | compiler | Level 13 is the effort plateau and should be the default | **Confirmed.** Level 15 is 20x the CPU for 1.4% of the bytes. Default changed |
| [006](hypotheses/006-markdown-stack-loss-diagnosis/README.md) | measure | The 10 markdown-stack losses are compiler-quality gaps | **Mostly falsified.** The three worst are harness/build bugs, not codegen |
| [005](hypotheses/005-idle-fold-guards/README.md) | compiler | 82% of fold invocations do nothing; skip them | **Split.** Decline-memo landed; entry guards **falsified** by a token census |
| [004](hypotheses/004-peephole-relex-tax/README.md) | compiler | The jQuery-scale bottleneck is not the codec | **Confirmed.** 95% is emission + the 135-fold peephole; the "uncapped fixed point" lead was real but not the cause |
| [003](hypotheses/003-cheap-codec-screening/README.md) | compiler | A cheaper Brotli quality can *rank* candidates the way q11 does, so exact encodes can be reserved for a verified beam | **Measured, parked.** Technique works (q9 ranks the q11 winner first, 20x cheaper) but 004 showed the codec is only 7% of the artifact that actually hurts |
| [002](hypotheses/002-content-addressed-memoization/README.md) | compiler | Both primitives are pure functions of their bytes and can be memoized on a content digest | **Landed.** −91% analyses, byte-identical output |
| [001](hypotheses/001-where-does-compile-time-go/README.md) | compiler | Compile time is concentrated in two whole-artifact primitives (canonical codec, generated-JS re-analysis) | **Confirmed** for small artifacts; 004 corrected it for large ones |
