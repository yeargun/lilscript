# Owner brief — 2026-09-02

Verbatim, as transcribed from speech (a run of blank lines collapsed). Distilled into
[objective.md](../objective.md) §9 and [038](../hypotheses/038-the-loop-runs-on-one-host/README.md);
this file is provenance and is never edited.

> listen builds are taking extremely long. you know.. normaly we must use our vms.. azure cli is
> auth already. listen.
>
> lilscript-workers
>
> we have it.. any build/compilation must take place in those machines, so everything will be
> faster. you must create proper infra for such things to work.
>
> and lets discuss about vm types.. i want much more performance, almost best vms we want
> Standard_F8s_v2 (6 instances) this is not the best for sure
>
> tell me usd pricings and tradeoffs.. mainly for compilation reasons obviousl... which vms could
> fasten

## Readings

| transcribed | read as |
|---|---|
| lilscript-workers | the VM scale set `lilscript-workers` in resource group `lilscript-build-farm`, West Europe, six Standard_F8s_v2, on this host's own subnet (10.1.0.x), admin `lilfarm` with this host's SSH key |
| any build/compilation must take place in those machines | every port build, fleet pass and A/B is dispatched to the pool; this host measures (`finer/tools/workers.mjs`) |
| proper infra | the dispatcher, provisioning, sync and dist return in `finer/tools/workers.mjs`; the pool's SKU is the owner's decision, priced in 038 |
| which vms could fasten | per-core speed first (one Rust process per port, Rayon-parallel, ~400 MB): AMD Genoa (Dalsv6 / Fasv6) over Cascade Lake (Fsv2); Spot for batch builds |

---

## Second brief (evening, on cnlil)

Verbatim:

> our implementation the build must be both smaller or same size,
> and also either more performant or exactly same performance.
>
> its not okay to compromiseo n performance
>
> ==
>
> investigate.. porper lilscirpt ..

Distilled into [objective.md](../objective.md) §3 (the runtime constraint is "not slower than
upstream on the port's own benchmark", not a percentage) and
[048](../hypotheses/048-the-singleton-engine-is-a-positional-handle/README.md).

### Readings

| transcribed | read as |
|---|---|
| our implementation the build | the cnlil artifact (`yeargun/cnlil`, `@itslil/cn`), the port whose page shows losses on both axes |
| smaller or same size, and also either more performant or exactly same performance | both gates at once: ≤ upstream bytes under the declared objective and ≤ upstream ns/op on every lane of the upstream harness |
| not okay to compromise on performance | the runtime guard rail for a port is 0% regression against upstream, not `max_regression_percent` |
| investigate.. porper lilscirpt | a proper LilScript investigation of the loss (§6 order, §10 reading first), and the port written as proper LilScript rather than a transliteration |

## Third brief (evening, mid-investigation)

Verbatim:

> config must be compression first but idk.. you know it has arraybuffer usage this that.. we need
> lilscript versino to compile those also.. so we cane xplicitly call int32? array? idk how syntax
> must be but i want such thing for sure

### Readings

| transcribed | read as |
|---|---|
| config must be compression first but idk | the port's `lilscript.toml` should put compression first (`priority = "size-first"`, the default, level 13, Brotli) as long as the runtime gate above holds; "idk" leaves the choice to the measurement |
| arraybuffer usage this that | upstream cn's engine state is typed arrays (`Int32Array`, `Uint8Array`, `Float64Array`) |
| we need lilscript version to compile those also.. explicitly call int32 array | the language must let a port declare typed-array state explicitly. It already does: `ArrayBuffer`, `SharedArrayBuffer` and the nine core views are types (`docs/language-v0.1.md:329-349`), and cnlil declares every engine table as `Int32Array`/`Uint8Array`/`Float64Array`. Not yet spelled: `DataView`, `BigInt64Array`/`BigUint64Array`, `Atomics` (`:342`) |

## Fourth brief (evening, mid-fix)

Verbatim:

> exact same perfomance or even better is needed for sure ..
>
> why its not working like that?
>
> also you know extra pure funciton this that, google closure compiler advanced mode like
> everything we have.. in lilscript or we must have ..
>
> so yeah.. investigate and fix

and, on being told the fix would touch the compiler:

> wait btw.. when you change compiler behaviour its a little sus thing.. you know lots of
> libraries we have.. we shouldnt overfit to this library optimization. its genericaly
> heuristicaly very proper.. it needs to be.. so whenever compiler is changed, you must somewhat
> verify some things

### Readings

| transcribed | read as |
|---|---|
| exact same performance or even better | the runtime gate stands: ≤ 1.00 on every lane of the port's benchmark, no exceptions |
| pure function this that, closure compiler advanced mode, everything we have | the compiler is expected to carry every Closure ADVANCED technique class already (refs/competitor-techniques.md is the ledger); the cnlil residual is not a missing Closure pass but V8 emission shapes (module cells, `arguments`, absent-value spelling), which Closure does not address either |
| we shouldn't overfit to this library | objective §7: no library-shaped fold; a compiler change is a generic claim with a proof for any program, never a pattern from one port |
| whenever compiler is changed you must verify some things | objective §8 gains the rule: a compiler change lands only with the compiler suite green, every port's own tests green on a pool build with the branch binary, codec sizes per port against the main binary, and the perf harnesses that exist; shape changes ship as explicit knobs first and flip their default only on a fleet measure |
