# LilScript objective

The objective and target architecture live in [docs/compiler-design.md](../docs/compiler-design.md#objective). The single [migration plan](../docs/migration/index.md) contains all implementation steps, acceptance gates and progress.

This page is a pointer, not another objective or plan. Earlier objectives and migration proposals are preserved in the external archive identified by the plan's source-disposition table. Their implementation sequences and numeric policy proposals are retired.
