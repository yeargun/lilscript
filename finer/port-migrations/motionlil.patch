diff -ru a/src/framer-motion/animation/animate/consumer.lil b/src/framer-motion/animation/animate/consumer.lil
--- a/src/framer-motion/animation/animate/consumer.lil
+++ b/src/framer-motion/animation/animate/consumer.lil
@@ -40,7 +40,7 @@
   return null;
 }
 
-export class ScopedAnimateOptions {
+export class ScopedAnimateOptionsConsumer {
   AnimationScope? scope;
   bool? reduceMotion;
   bool? skipAnimations;
@@ -57,7 +57,7 @@
 }
 
 export func(JsValue, JsValue?, JsValue?)->GroupAnimationWithThen createScopedAnimate(
-  ScopedAnimateOptions? options = null
+  ScopedAnimateOptionsConsumer? options = null
 ) {
   AnimationScope? scope = null;
   bool? reduceMotion = null;
diff -ru a/src/framer-motion/animation/animate/element-spring.lil b/src/framer-motion/animation/animate/element-spring.lil
--- a/src/framer-motion/animation/animate/element-spring.lil
+++ b/src/framer-motion/animation/animate/element-spring.lil
@@ -26,7 +26,7 @@
   return null;
 }
 
-export class ScopedAnimateOptions {
+export class ScopedAnimateOptionsElementSpring {
   AnimationScope? scope;
   bool? reduceMotion;
   bool? skipAnimations;
@@ -43,7 +43,7 @@
 }
 
 export func(JsValue, JsValue?, JsValue?)->GroupAnimationWithThen createScopedAnimate(
-  ScopedAnimateOptions? options = null
+  ScopedAnimateOptionsElementSpring? options = null
 ) {
   AnimationScope? scope = null;
   bool? reduceMotion = null;
diff -ru a/src/framer-motion/animation/animate/element.lil b/src/framer-motion/animation/animate/element.lil
--- a/src/framer-motion/animation/animate/element.lil
+++ b/src/framer-motion/animation/animate/element.lil
@@ -26,7 +26,7 @@
   return null;
 }
 
-export class ScopedAnimateOptions {
+export class ScopedAnimateOptionsElement {
   AnimationScope? scope;
   bool? reduceMotion;
   bool? skipAnimations;
@@ -43,7 +43,7 @@
 }
 
 export func(JsValue, JsValue?, JsValue?)->GroupAnimationWithThen createScopedAnimate(
-  ScopedAnimateOptions? options = null
+  ScopedAnimateOptionsElement? options = null
 ) {
   AnimationScope? scope = null;
   bool? reduceMotion = null;
diff -ru a/src/framer-motion/animation/animators/waapi/animate-elements-mini.lil b/src/framer-motion/animation/animators/waapi/animate-elements-mini.lil
--- a/src/framer-motion/animation/animators/waapi/animate-elements-mini.lil
+++ b/src/framer-motion/animation/animators/waapi/animate-elements-mini.lil
@@ -6,8 +6,8 @@
 import { getComputedStyle } from "../../../../motion-dom/render/dom/style-computed";
 import { getValueTransition } from "../../../../motion-dom/animation/utils/get-value-transition-mini";
 import { pxValues } from "../../../../motion-dom/animation/waapi/utils/px-values-mini";
-import { NativeAnimation } from "../../../../motion-dom/animation/NativeAnimationMini";
-import { resolveElements, AnimationScope } from "../../../../motion-dom/utils/resolve-elements-mini";
+import { NativeAnimationMini } from "../../../../motion-dom/animation/NativeAnimationMini";
+import { resolveElements, AnimationScopeMini } from "../../../../motion-dom/utils/resolve-elements-mini";
 import { secondsToMilliseconds } from "../../../../motion-utils/time-conversion";
 import {
   asArray,
@@ -19,9 +19,9 @@
 } from "../../../../motion-dom/cast-host.ts";
 
 extern func(int, int)->float delayFunctionOf(JsValue value);
-extern NativeAnimation nativeAnimationOf(JsValue value);
+extern NativeAnimationMini nativeAnimationOf(JsValue value);
 
-class AnimationDefinition {
+class AnimationDefinitionMini {
   Map<string, JsValue> map;
   string key;
   JsValue[] unresolvedKeyframes;
@@ -40,11 +40,11 @@
   }
 }
 
-export NativeAnimation[] animateElements(
+export NativeAnimationMini[] animateElements(
   JsValue? elementOrSelector = null,
   JsValue? keyframes = null,
   JsValue? options = null,
-  AnimationScope? scope = null
+  AnimationScopeMini? scope = null
 ) {
   if (elementOrSelector == null) {
     return [];
@@ -56,7 +56,7 @@
     return [];
   }
 
-  AnimationDefinition[] animationDefinitions = [];
+  AnimationDefinitionMini[] animationDefinitions = [];
 
   for (int i = 0; i < numElements; i++) {
     JsValue element = elements[i];
@@ -118,14 +118,14 @@
           elementTransition["type"] == null && elementTransition["ease"] == null;
 
         animationDefinitions.push(
-          new AnimationDefinition(map, key, unresolved, valueOptions)
+          new AnimationDefinitionMini(map, key, unresolved, valueOptions)
         );
       }
     }
   }
 
   for (int i = 0; i < animationDefinitions.length; i++) {
-    AnimationDefinition def = animationDefinitions[i];
+    AnimationDefinitionMini def = animationDefinitions[i];
     JsValue[] unresolvedKeyframes = def.unresolvedKeyframes;
     JsValue animationOptions = def.options;
     JsValue element = animationOptions["element"];
@@ -158,10 +158,10 @@
     animationOptions["keyframes"] = unresolvedKeyframes;
   }
 
-  NativeAnimation[] animations = [];
+  NativeAnimationMini[] animations = [];
   for (int i = 0; i < animationDefinitions.length; i++) {
-    AnimationDefinition def = animationDefinitions[i];
-    NativeAnimation animation = new NativeAnimation(def.options, def.map, def.key);
+    AnimationDefinitionMini def = animationDefinitions[i];
+    NativeAnimationMini animation = new NativeAnimationMini(def.options, def.map, def.key);
     def.map.set(def.key, animation);
     animations.push(animation);
   }
diff -ru a/src/framer-motion/animation/animators/waapi/animate-style-mini.lil b/src/framer-motion/animation/animators/waapi/animate-style-mini.lil
--- a/src/framer-motion/animation/animators/waapi/animate-style-mini.lil
+++ b/src/framer-motion/animation/animators/waapi/animate-style-mini.lil
@@ -1,16 +1,16 @@
-import { GroupAnimationWithThen } from "../../../../motion-dom/animation/GroupAnimationWithThenMini";
-import { AnimationScope } from "../../../../motion-dom/utils/resolve-elements-mini";
+import { GroupAnimationWithThenMini } from "../../../../motion-dom/animation/GroupAnimationWithThenMini";
+import { AnimationScopeMini } from "../../../../motion-dom/utils/resolve-elements-mini";
 import { animateElements } from "./animate-elements-mini";
 
-export func(JsValue, JsValue, JsValue?)->GroupAnimationWithThen createScopedWaapiAnimate(
-  AnimationScope? scope = null
+export func(JsValue, JsValue, JsValue?)->GroupAnimationWithThenMini createScopedWaapiAnimate(
+  AnimationScopeMini? scope = null
 ) {
   return (JsValue elementOrSelector, JsValue keyframes, JsValue? options = null) => {
-    return new GroupAnimationWithThen(
+    return new GroupAnimationWithThenMini(
       animateElements(elementOrSelector, keyframes, options, scope)
     );
   };
 }
 
-export func(JsValue, JsValue, JsValue?)->GroupAnimationWithThen animateMini =
+export func(JsValue, JsValue, JsValue?)->GroupAnimationWithThenMini animateMini =
   createScopedWaapiAnimate();
diff -ru a/src/framer-motion/animation/sequence/create-full.lil b/src/framer-motion/animation/sequence/create-full.lil
--- a/src/framer-motion/animation/sequence/create-full.lil
+++ b/src/framer-motion/animation/sequence/create-full.lil
@@ -35,7 +35,7 @@
   }
 }
 
-class DurationAccum {
+class DurationAccumFull {
   float maxDuration;
   float totalDuration;
 
@@ -156,7 +156,7 @@
 
   float prevTime = 0.0;
   float currentTime = 0.0;
-  DurationAccum accum = new DurationAccum();
+  DurationAccumFull accum = new DurationAccumFull();
 
   int sequenceLen = 0;
   if (sequence.isArray()) {
@@ -203,7 +203,7 @@
       currentTime = calcNextTime(currentTime, atOpt, prevTime, timeLabels);
     }
 
-    DurationAccum segmentAccum = new DurationAccum();
+    DurationAccumFull segmentAccum = new DurationAccumFull();
     segmentAccum.totalDuration = accum.totalDuration;
 
     if (isMotionValue(subject)) {
diff -ru a/src/framer-motion/render/dom/scroll/offsets/presets.lil b/src/framer-motion/render/dom/scroll/offsets/presets.lil
--- a/src/framer-motion/render/dom/scroll/offsets/presets.lil
+++ b/src/framer-motion/render/dom/scroll/offsets/presets.lil
@@ -3,7 +3,7 @@
 export float[][] scrollOffsetAny = [[1.0, 0.0], [0.0, 1.0]];
 export float[][] scrollOffsetAll = [[0.0, 0.0], [1.0, 1.0]];
 
-export class ScrollOffset {
+export class ScrollOffsetPresets {
   float[][] Enter;
   float[][] Exit;
   float[][] Any;
@@ -17,4 +17,4 @@
   }
 }
 
-export ScrollOffset ScrollOffsetValues = new ScrollOffset();
+export ScrollOffsetPresets ScrollOffsetValues = new ScrollOffsetPresets();
diff -ru a/src/motion-dom/animation/AsyncMotionValueAnimationJs.lil b/src/motion-dom/animation/AsyncMotionValueAnimationJs.lil
--- a/src/motion-dom/animation/AsyncMotionValueAnimationJs.lil
+++ b/src/motion-dom/animation/AsyncMotionValueAnimationJs.lil
@@ -1,5 +1,5 @@
 import { WithPromise } from "./utils/WithPromise";
-import { JSAnimation, JSAnimationOptions } from "./JSAnimationFull";
+import { JSAnimationFull, JSAnimationOptionsFull } from "./JSAnimationFull";
 import { KeyframeResolver, flushKeyframeResolvers } from "./keyframes/KeyframesResolver";
 import { createDOMKeyframesResolver } from "./keyframes/DOMKeyframesResolver";
 import { getFinalKeyframe } from "./keyframes/get-final";
@@ -17,7 +17,7 @@
 
 float MAX_RESOLVE_DELAY = 40.0;
 
-export class AsyncMotionValueAnimation extends WithPromise {
+export class AsyncMotionValueAnimationJs extends WithPromise {
   JsValue options;
   JsValue? animation;
   KeyframeResolver? resolver;
@@ -40,7 +40,7 @@
     this.stopActive = null;
     this.isStopped = false;
     this.createAnimation();
-    AsyncMotionValueAnimation selfControls = this;
+    AsyncMotionValueAnimationJs selfControls = this;
     setProp(this, "attachTimeline", (JsValue timeline) => {
       return selfControls.attachTimeline(timeline);
     });
@@ -93,7 +93,7 @@
     }
     JsValue motionValue = options["motionValue"];
     JsValue element = options["element"];
-    AsyncMotionValueAnimation self = this;
+    AsyncMotionValueAnimationJs self = this;
 
     func(JsValue[], JsValue?, bool)->void onKeyframesResolved = (
       JsValue[] resolvedKeyframes,
@@ -220,7 +220,7 @@
         callFn1(onUpdate, v);
       };
     }
-    JSAnimationOptions jsOpts = new JSAnimationOptions(
+    JSAnimationOptionsFull jsOpts = new JSAnimationOptionsFull(
       floatKeys,
       velocity,
       typeStr,
@@ -229,13 +229,13 @@
       onUpdateFn
     );
     jsOpts.extra = options;
-    JSAnimation jsAnim = new JSAnimation(jsOpts);
+    JSAnimationFull jsAnim = new JSAnimationFull(jsOpts);
     animation = jsAnim;
     stopActive = () => {
       jsAnim.stop();
     };
 
-    AsyncMotionValueAnimation self = this;
+    AsyncMotionValueAnimationJs self = this;
     asWithPromise(animation).finished().then((bool _v) => {
       self.notifyFinished();
       return true;
@@ -284,7 +284,7 @@
     } else {
       this.pendingTimeline = timeline;
     }
-    AsyncMotionValueAnimation self = this;
+    AsyncMotionValueAnimationJs self = this;
     return () => {
       self.stop();
     };
diff -ru a/src/motion-dom/animation/AsyncMotionValueAnimationSpring.lil b/src/motion-dom/animation/AsyncMotionValueAnimationSpring.lil
--- a/src/motion-dom/animation/AsyncMotionValueAnimationSpring.lil
+++ b/src/motion-dom/animation/AsyncMotionValueAnimationSpring.lil
@@ -1,5 +1,5 @@
 import { WithPromise } from "./utils/WithPromise";
-import { JSAnimation, JSAnimationOptions } from "./JSAnimationFull";
+import { JSAnimationFull, JSAnimationOptionsFull } from "./JSAnimationFull";
 import { NativeAnimationExtended } from "./NativeAnimationExtended";
 import { KeyframeResolver, flushKeyframeResolvers } from "./keyframes/KeyframesResolver";
 import { createDOMKeyframesResolver } from "./keyframes/DOMKeyframesResolver";
@@ -17,7 +17,7 @@
 
 float MAX_RESOLVE_DELAY = 40.0;
 
-export class AsyncMotionValueAnimation extends WithPromise {
+export class AsyncMotionValueAnimationSpring extends WithPromise {
   JsValue options;
   JsValue? animation;
   KeyframeResolver? resolver;
@@ -40,7 +40,7 @@
     this.stopActive = null;
     this.isStopped = false;
     this.createAnimation();
-    AsyncMotionValueAnimation selfControls = this;
+    AsyncMotionValueAnimationSpring selfControls = this;
     setProp(this, "attachTimeline", (JsValue timeline) => {
       return selfControls.attachTimeline(timeline);
     });
@@ -93,7 +93,7 @@
     }
     JsValue motionValue = options["motionValue"];
     JsValue element = options["element"];
-    AsyncMotionValueAnimation self = this;
+    AsyncMotionValueAnimationSpring self = this;
 
     func(JsValue[], JsValue?, bool)->void onKeyframesResolved = (
       JsValue[] resolvedKeyframes,
@@ -238,7 +238,7 @@
           callFn1(onUpdate, v);
         };
       }
-      JSAnimationOptions jsOpts = new JSAnimationOptions(
+      JSAnimationOptionsFull jsOpts = new JSAnimationOptionsFull(
         floatKeys,
         velocity,
         typeStr,
@@ -247,14 +247,14 @@
         onUpdateFn
       );
       jsOpts.extra = options;
-      JSAnimation jsAnim = new JSAnimation(jsOpts);
+      JSAnimationFull jsAnim = new JSAnimationFull(jsOpts);
       animation = jsAnim;
       stopActive = () => {
         jsAnim.stop();
       };
     }
 
-    AsyncMotionValueAnimation self = this;
+    AsyncMotionValueAnimationSpring self = this;
     asWithPromise(animation).finished().then((bool _v) => {
       self.notifyFinished();
       return true;
@@ -303,7 +303,7 @@
     } else {
       this.pendingTimeline = timeline;
     }
-    AsyncMotionValueAnimation self = this;
+    AsyncMotionValueAnimationSpring self = this;
     return () => {
       self.stop();
     };
diff -ru a/src/motion-dom/animation/AsyncMotionValueAnimationSpringJs.lil b/src/motion-dom/animation/AsyncMotionValueAnimationSpringJs.lil
--- a/src/motion-dom/animation/AsyncMotionValueAnimationSpringJs.lil
+++ b/src/motion-dom/animation/AsyncMotionValueAnimationSpringJs.lil
@@ -1,5 +1,5 @@
 import { WithPromise } from "./utils/WithPromise";
-import { JSAnimation, JSAnimationOptions } from "./JSAnimationFull";
+import { JSAnimationFull, JSAnimationOptionsFull } from "./JSAnimationFull";
 import { KeyframeResolver, flushKeyframeResolvers } from "./keyframes/KeyframesResolver";
 import { createDOMKeyframesResolver } from "./keyframes/DOMKeyframesResolver";
 import { getFinalKeyframe } from "./keyframes/get-final";
@@ -15,7 +15,7 @@
 
 float MAX_RESOLVE_DELAY = 40.0;
 
-export class AsyncMotionValueAnimation extends WithPromise {
+export class AsyncMotionValueAnimationSpringjs extends WithPromise {
   JsValue options;
   JsValue? animation;
   KeyframeResolver? resolver;
@@ -38,7 +38,7 @@
     this.stopActive = null;
     this.isStopped = false;
     this.createAnimation();
-    AsyncMotionValueAnimation selfControls = this;
+    AsyncMotionValueAnimationSpringjs selfControls = this;
     setProp(this, "attachTimeline", (JsValue timeline) => {
       return selfControls.attachTimeline(timeline);
     });
@@ -91,7 +91,7 @@
     }
     JsValue motionValue = options["motionValue"];
     JsValue element = options["element"];
-    AsyncMotionValueAnimation self = this;
+    AsyncMotionValueAnimationSpringjs self = this;
 
     func(JsValue[], JsValue?, bool)->void onKeyframesResolved = (
       JsValue[] resolvedKeyframes,
@@ -216,7 +216,7 @@
         callFn1(onUpdate, v);
       };
     }
-    JSAnimationOptions jsOpts = new JSAnimationOptions(
+    JSAnimationOptionsFull jsOpts = new JSAnimationOptionsFull(
       floatKeys,
       velocity,
       typeStr,
@@ -225,13 +225,13 @@
       onUpdateFn
     );
     jsOpts.extra = options;
-    JSAnimation jsAnim = new JSAnimation(jsOpts);
+    JSAnimationFull jsAnim = new JSAnimationFull(jsOpts);
     animation = jsAnim;
     stopActive = () => {
       jsAnim.stop();
     };
 
-    AsyncMotionValueAnimation self = this;
+    AsyncMotionValueAnimationSpringjs self = this;
     asWithPromise(animation).finished().then((bool _v) => {
       self.notifyFinished();
       return true;
@@ -280,7 +280,7 @@
     } else {
       this.pendingTimeline = timeline;
     }
-    AsyncMotionValueAnimation self = this;
+    AsyncMotionValueAnimationSpringjs self = this;
     return () => {
       self.stop();
     };
diff -ru a/src/motion-dom/animation/GroupAnimationMini.lil b/src/motion-dom/animation/GroupAnimationMini.lil
--- a/src/motion-dom/animation/GroupAnimationMini.lil
+++ b/src/motion-dom/animation/GroupAnimationMini.lil
@@ -1,15 +1,15 @@
 import { setProp } from "../dom-host";
-import { NativeAnimation } from "./NativeAnimationMini";
+import { NativeAnimationMini } from "./NativeAnimationMini";
 
-export class GroupAnimation {
-  NativeAnimation[] animations;
+export class GroupAnimationMini {
+  NativeAnimationMini[] animations;
 
-  init(NativeAnimation[] input) {
+  init(NativeAnimationMini[] input) {
     this.animations = [];
     for (int i = 0; i < input.length; i++) {
       this.animations.push(input[i]);
     }
-    GroupAnimation self = this;
+    GroupAnimationMini self = this;
     setProp(this, "attachTimeline", (JsValue timeline) => {
       return self.attachTimeline(timeline);
     });
diff -ru a/src/motion-dom/animation/GroupAnimationWithThenMini.lil b/src/motion-dom/animation/GroupAnimationWithThenMini.lil
--- a/src/motion-dom/animation/GroupAnimationWithThenMini.lil
+++ b/src/motion-dom/animation/GroupAnimationWithThenMini.lil
@@ -1,8 +1,8 @@
-import { GroupAnimation } from "./GroupAnimationMini";
-import { NativeAnimation } from "./NativeAnimationMini";
+import { GroupAnimationMini } from "./GroupAnimationMini";
+import { NativeAnimationMini } from "./NativeAnimationMini";
 
-export class GroupAnimationWithThen extends GroupAnimation {
-  init(NativeAnimation[] input) {
+export class GroupAnimationWithThenMini extends GroupAnimationMini {
+  init(NativeAnimationMini[] input) {
     super(input);
   }
 
diff -ru a/src/motion-dom/animation/JSAnimationFull.lil b/src/motion-dom/animation/JSAnimationFull.lil
--- a/src/motion-dom/animation/JSAnimationFull.lil
+++ b/src/motion-dom/animation/JSAnimationFull.lil
@@ -31,7 +31,7 @@
   return percent / 100.0;
 }
 
-export class JSAnimationOptions {
+export class JSAnimationOptionsFull {
   float[] keyframes;
   float velocity;
   string? type;
@@ -58,7 +58,7 @@
   }
 }
 
-export class SampleResult {
+export class SampleResultFull {
   float value;
   bool done;
   init(float value, bool done = false) {
@@ -67,8 +67,8 @@
   }
 }
 
-export class JSAnimation extends WithPromise {
-  JSAnimationOptions options;
+export class JSAnimationFull extends WithPromise {
+  JSAnimationOptionsFull options;
   bool stopped;
   string state;
   float? startTime;
@@ -83,7 +83,7 @@
   DriverControls? driver;
   AnimationState delayState;
 
-  init(JSAnimationOptions options) {
+  init(JSAnimationOptionsFull options) {
     super();
     this.options = options;
     this.stopped = false;
@@ -111,7 +111,7 @@
         }
       }
     }
-    JSAnimation self = this;
+    JSAnimationFull self = this;
     setProp(this, "attachTimeline", (JsValue timeline) => {
       return self.attachTimeline(timeline);
     });
@@ -266,7 +266,7 @@
     }
   }
 
-  SampleResult sample(float sampleTime) {
+  SampleResultFull sample(float sampleTime) {
     SpringGenerator? gen = this.springGen;
     if (gen == null) {
       float[] keys = this.options.keyframes;
@@ -274,10 +274,10 @@
       if (keys.length > 0) {
         v = keys[keys.length - 1];
       }
-      return new SampleResult(v, true);
+      return new SampleResultFull(v, true);
     }
     AnimationState state = gen.next(sampleTime);
-    return new SampleResult(state.value, state.done);
+    return new SampleResultFull(state.value, state.done);
   }
 
   void tick(float timestamp) {
@@ -365,10 +365,10 @@
       elapsed = clamp(0.0, 1.0, iterationProgress) * this.resolvedDuration;
     }
 
-    SampleResult result = this.sample(elapsed);
+    SampleResultFull result = this.sample(elapsed);
     if (activeGenerator != null && activeGenerator != this.springGen) {
       AnimationState mirroredState = activeGenerator.next(elapsed);
-      result = new SampleResult(mirroredState.value, mirroredState.done);
+      result = new SampleResultFull(mirroredState.value, mirroredState.done);
     }
     if (isInDelayPhase) {
       float[] keys = this.options.keyframes;
@@ -376,7 +376,7 @@
       if (keys.length > 0) {
         first = keys[0];
       }
-      result = new SampleResult(first, false);
+      result = new SampleResultFull(first, false);
     }
     if (!isInDelayPhase) {
       if (this.playbackSpeed >= 0.0) {
@@ -431,7 +431,7 @@
     } else if (this.startTime == null) {
       this.startTime = time.now();
     }
-    JSAnimation self = this;
+    JSAnimationFull self = this;
     this.driver = frameloopDriver((float timestamp) => {
       self.tick(timestamp);
     });
diff -ru a/src/motion-dom/animation/NativeAnimationMini.lil b/src/motion-dom/animation/NativeAnimationMini.lil
--- a/src/motion-dom/animation/NativeAnimationMini.lil
+++ b/src/motion-dom/animation/NativeAnimationMini.lil
@@ -10,7 +10,7 @@
   callMethod1
 } from "../dom-host";
 
-export class NativeAnimation extends WithPromise {
+export class NativeAnimationMini extends WithPromise {
   JsValue options;
   JsValue animation;
   bool isStopped;
@@ -63,7 +63,7 @@
       if (autoplay is bool && autoplay == false) {
         this.pause();
       }
-      NativeAnimation selfFinish = this;
+      NativeAnimationMini selfFinish = this;
       setProp(this.animation, "onfinish", () => {
         selfFinish.onAnimationFinish();
       });
@@ -191,7 +191,7 @@
           iterationDuration = iterationDuration + delay / 1000.0;
         }
       }
-      NativeAnimation selfTimeline = this;
+      NativeAnimationMini selfTimeline = this;
       JsValue valueAnimation = record{ iterationDuration: iterationDuration };
       setProp(valueAnimation, "pause", () => {
         selfTimeline.pause();
diff -ru a/src/motion-dom/animation/interfaces/motion-value-spring.lil b/src/motion-dom/animation/interfaces/motion-value-spring.lil
--- a/src/motion-dom/animation/interfaces/motion-value-spring.lil
+++ b/src/motion-dom/animation/interfaces/motion-value-spring.lil
@@ -1,6 +1,6 @@
 import { MotionGlobalConfig } from "../../../motion-utils/global-config";
 import { secondsToMilliseconds } from "../../../motion-utils/time-conversion";
-import { AsyncMotionValueAnimation } from "../AsyncMotionValueAnimationSpringJs";
+import { AsyncMotionValueAnimationSpringjs } from "../AsyncMotionValueAnimationSpringJs";
 import { makeAnimationInstant } from "../utils/make-animation-instant";
 import { getValueTransition } from "../utils/get-value-transition";
 import { getDefaultTransition } from "../utils/default-transitions";
@@ -203,6 +203,6 @@
         }
       }
     }
-    return new AsyncMotionValueAnimation(options);
+    return new AsyncMotionValueAnimationSpringjs(options);
   };
 }
diff -ru a/src/motion-dom/animation/interfaces/motion-value.lil b/src/motion-dom/animation/interfaces/motion-value.lil
--- a/src/motion-dom/animation/interfaces/motion-value.lil
+++ b/src/motion-dom/animation/interfaces/motion-value.lil
@@ -1,6 +1,6 @@
 import { MotionGlobalConfig } from "../../../motion-utils/global-config";
 import { secondsToMilliseconds } from "../../../motion-utils/time-conversion";
-import { AsyncMotionValueAnimation } from "../AsyncMotionValueAnimationJs";
+import { AsyncMotionValueAnimationJs } from "../AsyncMotionValueAnimationJs";
 import { makeAnimationInstant } from "../utils/make-animation-instant";
 import { getValueTransition } from "../utils/get-value-transition";
 import { getDefaultTransition } from "../utils/default-transitions";
@@ -203,6 +203,6 @@
         }
       }
     }
-    return new AsyncMotionValueAnimation(options);
+    return new AsyncMotionValueAnimationJs(options);
   };
 }
diff -ru a/src/motion-dom/animation/utils/arc.lil b/src/motion-dom/animation/utils/arc.lil
--- a/src/motion-dom/animation/utils/arc.lil
+++ b/src/motion-dom/animation/utils/arc.lil
@@ -1,3 +1,4 @@
+import { MathHost, Math } from "../../render/dom/parse-transform";
 import { wrap } from "../../../motion-utils/wrap";
 import { motionValue } from "../../value/index";
 import { animateMotionValue } from "../interfaces/motion-value";
diff -ru a/src/motion-dom/projection/geometry/conversion.lil b/src/motion-dom/projection/geometry/conversion.lil
--- a/src/motion-dom/projection/geometry/conversion.lil
+++ b/src/motion-dom/projection/geometry/conversion.lil
@@ -6,11 +6,11 @@
 } from "../../../motion-utils/types/geometry";
 
 export Box convertBoundingBoxToBox(BoundingBox bb) {
-  return Box{Axis{bb.left, bb.right}, Axis{bb.top, bb.bottom}};
+  return new Box(new Axis(bb.left, bb.right), new Axis(bb.top, bb.bottom));
 }
 
 export BoundingBox convertBoxToBoundingBox(Box box) {
-  return BoundingBox{box.y.min, box.x.max, box.y.max, box.x.min};
+  return new BoundingBox(box.y.min, box.x.max, box.y.max, box.x.min);
 }
 
 export BoundingBox transformBoxPoints(
@@ -20,7 +20,7 @@
   if (transformPoint == null) {
     return point;
   }
-  Point topLeft = transformPoint(Point{point.left, point.top});
-  Point bottomRight = transformPoint(Point{point.right, point.bottom});
-  return BoundingBox{topLeft.y, bottomRight.x, bottomRight.y, topLeft.x};
+  Point topLeft = transformPoint(new Point(point.left, point.top));
+  Point bottomRight = transformPoint(new Point(point.right, point.bottom));
+  return new BoundingBox(topLeft.y, bottomRight.x, bottomRight.y, topLeft.x);
 }
diff -ru a/src/motion-dom/projection/geometry/models.lil b/src/motion-dom/projection/geometry/models.lil
--- a/src/motion-dom/projection/geometry/models.lil
+++ b/src/motion-dom/projection/geometry/models.lil
@@ -6,17 +6,17 @@
 } from "../../../motion-utils/types/geometry";
 
 export AxisDelta createAxisDelta() {
-  return AxisDelta{0.0, 1.0, 0.0, 0.0};
+  return new AxisDelta(0.0, 1.0, 0.0, 0.0);
 }
 
 export Delta createDelta() {
-  return Delta{createAxisDelta(), createAxisDelta()};
+  return new Delta(createAxisDelta(), createAxisDelta());
 }
 
 export Axis createAxis() {
-  return Axis{0.0, 0.0};
+  return new Axis(0.0, 0.0);
 }
 
 export Box createBox() {
-  return Box{createAxis(), createAxis()};
+  return new Box(createAxis(), createAxis());
 }
diff -ru a/src/motion-dom/projection/node/create-projection-node.lil b/src/motion-dom/projection/node/create-projection-node.lil
--- a/src/motion-dom/projection/node/create-projection-node.lil
+++ b/src/motion-dom/projection/node/create-projection-node.lil
@@ -238,7 +238,7 @@
     return null;
   }
   if (value.isObject()) {
-    return Point{parseFloatValue(value["x"]), parseFloatValue(value["y"])};
+    return new Point(parseFloatValue(value["x"]), parseFloatValue(value["y"]));
   }
   return null;
 }
@@ -377,7 +377,7 @@
     this.animationProgress = 0.0;
     this.resolvedRelativeTargetAt = 0.0;
     this.linkedParentVersion = 0;
-    this.treeScale = Point{1.0, 1.0};
+    this.treeScale = new Point(1.0, 1.0);
     this.resumeFrom = null;
     this.resumingFrom = null;
     this.animationValues = null;
diff -ru a/src/motion-dom/projection/utils/measure.lil b/src/motion-dom/projection/utils/measure.lil
--- a/src/motion-dom/projection/utils/measure.lil
+++ b/src/motion-dom/projection/utils/measure.lil
@@ -9,7 +9,7 @@
 export class ScrollOffset {
   Point offset;
   init(float x = 0.0, float y = 0.0) {
-    this.offset = Point{x, y};
+    this.offset = new Point(x, y);
   }
 }
 
@@ -33,12 +33,12 @@
   JsValue? transformPoint = null
 ) {
   JsValue rect = callMethod0(instance, "getBoundingClientRect");
-  BoundingBox box = BoundingBox{
+  BoundingBox box = new BoundingBox(
     rectField(rect, "top"),
     rectField(rect, "right"),
     rectField(rect, "bottom"),
     rectField(rect, "left")
-  };
+  );
   (func(Point)->Point)? typedTransform = null;
   if (transformPoint != null && isFunction(transformPoint)) {
     typedTransform = (Point point) => {
@@ -55,7 +55,7 @@
           y = ry;
         }
       }
-      return Point{x, y};
+      return new Point(x, y);
     };
   }
   return convertBoundingBoxToBox(transformBoxPoints(box, typedTransform));
diff -ru a/src/motion-dom/render/dom/parse-transform.lil b/src/motion-dom/render/dom/parse-transform.lil
--- a/src/motion-dom/render/dom/parse-transform.lil
+++ b/src/motion-dom/render/dom/parse-transform.lil
@@ -39,13 +39,13 @@
   return substring(s, start, end);
 }
 
-extern class MathHost {
+export extern class MathHost {
   float atan2(float y, float x);
   float atan(float x);
   float PI;
 }
 
-extern MathHost Math;
+export extern MathHost Math;
 extern float parseFloat(string value);
 
 float radToDeg(float rad) {
diff -ru a/src/motion-dom/utils/resolve-elements-mini.lil b/src/motion-dom/utils/resolve-elements-mini.lil
--- a/src/motion-dom/utils/resolve-elements-mini.lil
+++ b/src/motion-dom/utils/resolve-elements-mini.lil
@@ -4,7 +4,7 @@
   isEventTarget
 } from "../dom-host";
 
-export class AnimationScope {
+export class AnimationScopeMini {
   JsValue current;
   JsValue[] animations;
 
@@ -16,7 +16,7 @@
 
 export JsValue[] resolveElements(
   JsValue? elementOrSelector = null,
-  AnimationScope? _scope = null
+  AnimationScopeMini? _scope = null
 ) {
   if (elementOrSelector == null) {
     return [];
diff -ru a/src/motion-dom-geometry-smoke.lil b/src/motion-dom-geometry-smoke.lil
--- a/src/motion-dom-geometry-smoke.lil
+++ b/src/motion-dom-geometry-smoke.lil
@@ -16,8 +16,8 @@
 a.min = 0.0;
 a.max = 100.0;
 digest = digest + calcLength(a);
-AxisDelta d = AxisDelta{0.0, 1.0, 0.0, 0.0};
-Axis target = Axis{10.0, 210.0};
+AxisDelta d = new AxisDelta(0.0, 1.0, 0.0, 0.0);
+Axis target = new Axis(10.0, 210.0);
 calcAxisDelta(d, a, target, 0.5);
 digest = digest + d.translate + d.scale;
 Box b1 = createBox();
@@ -33,7 +33,7 @@
 digest = digest + aspectRatio(b1);
 digest = digest + scalePoint(10.0, 2.0, 0.0);
 Delta delta = createDelta();
-calcBoxDelta(delta, b1, Box{Axis{0.0, 200.0}, Axis{0.0, 100.0}});
+calcBoxDelta(delta, b1, new Box(new Axis(0.0, 200.0), new Axis(0.0, 100.0)));
 applyBoxDelta(b2, delta);
 digest = digest + b2.x.max;
 Record<JsValue> values = record{};
diff -ru a/src/motion-dom-small-smoke.lil b/src/motion-dom-small-smoke.lil
--- a/src/motion-dom-small-smoke.lil
+++ b/src/motion-dom-small-smoke.lil
@@ -46,7 +46,7 @@
 Delta d = createDelta();
 d.x.translate = 10.0;
 d.y.translate = 5.0;
-Point tree = Point{1.0, 1.0};
+Point tree = new Point(1.0, 1.0);
 string t = buildProjectionTransform(d, tree, null);
 digest = digest + t.length;
 MotionValueStateLatest state = new MotionValueStateLatest();
diff -ru a/src/motion-utils/types/geometry.lil b/src/motion-utils/types/geometry.lil
--- a/src/motion-utils/types/geometry.lil
+++ b/src/motion-utils/types/geometry.lil
@@ -1,33 +1,61 @@
-export struct Point {
+export class Point {
   float x;
   float y;
+  init(float x, float y) {
+    this.x = x;
+    this.y = y;
+  }
 }
 
-export struct Axis {
+export class Axis {
   float min;
   float max;
+  init(float min, float max) {
+    this.min = min;
+    this.max = max;
+  }
 }
 
-export struct Box {
+export class Box {
   Axis x;
   Axis y;
+  init(Axis x, Axis y) {
+    this.x = x;
+    this.y = y;
+  }
 }
 
-export struct BoundingBox {
+export class BoundingBox {
   float top;
   float right;
   float bottom;
   float left;
+  init(float top, float right, float bottom, float left) {
+    this.top = top;
+    this.right = right;
+    this.bottom = bottom;
+    this.left = left;
+  }
 }
 
-export struct AxisDelta {
+export class AxisDelta {
   float translate;
   float scale;
   float origin;
   float originPoint;
+  init(float translate, float scale, float origin, float originPoint) {
+    this.translate = translate;
+    this.scale = scale;
+    this.origin = origin;
+    this.originPoint = originPoint;
+  }
 }
 
-export struct Delta {
+export class Delta {
   AxisDelta x;
   AxisDelta y;
+  init(AxisDelta x, AxisDelta y) {
+    this.x = x;
+    this.y = y;
+  }
 }
diff -ru a/src/lilscript.toml b/src/lilscript.toml
--- a/src/lilscript.toml
+++ b/src/lilscript.toml
@@ -20,3 +20,8 @@
 properties = true
 exports = false
 pool_strings = true
+
+[bundle]
+# The semantic route lowers these host modules into the program, where
+# pruning, inlining and naming reach them.
+host_modules = "embed"
