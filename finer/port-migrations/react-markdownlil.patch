diff -ruN a/lilscript.toml b/lilscript.toml
--- a/lilscript.toml	2026-09-23 20:40:45.476694475 +0000
+++ b/lilscript.toml	2026-09-23 20:40:45.492942328 +0000
@@ -7,14 +7,20 @@
 # can be acted on.
 rematerialize_member_reads = true
 assume_pure_property_reads = true
+# micromark's state functions and this layer's callbacks are called, never
+# constructed; React reads a component's prototype only to test for a class,
+# which an arrow answers the same way. Measured (b79): -146 Brotli.
+assume_unconstructed_callbacks = true
 priority = "size-first"
 optimization_level = 13
 cost_model = "brotli"
-assume_pristine_builtins = false
+# The graph and this layer call standard builtins the way upstream does; an
+# environment that replaces them breaks upstream too. Measured on the browser
+# build (b79): 29,681 -> 27,394 Brotli.
+assume_pristine_builtins = true
 strip_console = true
 local_name_reserve = 48
 candidate_search = "production"
-function_spelling = "function"
 public_aggregate_abi = "named"
 
 [mangle]
diff -ruN a/package.json b/package.json
--- a/package.json	2026-09-23 20:40:45.418537207 +0000
+++ b/package.json	2026-09-23 20:40:45.438078652 +0000
@@ -34,6 +34,7 @@
         "require": "./dist/react-markdown.development.cjs",
         "default": "./dist/react-markdown.esm.js"
       },
+      "browser": "./dist/react-markdown.browser.js",
       "import": "./dist/react-markdown.esm.js",
       "require": "./dist/react-markdown.cjs",
       "default": "./dist/react-markdown.esm.js"
diff -ruN a/scripts/build.mjs b/scripts/build.mjs
--- a/scripts/build.mjs	2026-09-23 20:40:45.388693701 +0000
+++ b/scripts/build.mjs	2026-09-23 20:40:45.404086110 +0000
@@ -1,9 +1,11 @@
 import {
   accessSync,
   constants,
+  cpSync,
   existsSync,
   mkdirSync,
   readFileSync,
+  rmSync,
   unlinkSync,
   writeFileSync,
 } from "node:fs"
@@ -46,9 +48,9 @@
   if (result.status !== 0) process.exit(result.status ?? 1)
 }
 
-function compileLil(compiler, configName, outputName) {
+function compileLil(compiler, configName, outputName, source = resolve(root, "src")) {
   run(compiler, [
-    resolve(root, "src", "index.lil"),
+    resolve(source, "index.lil"),
     "--target",
     "js-module",
     "--config",
@@ -70,6 +72,20 @@
   mkdirSync(dist, { recursive: true })
   compileLil(compiler, "lilscript.toml", `${file}.raw.js`)
   compileLil(compiler, "lilscript.closed.toml", `${file}.closed.raw.js`)
+  // The browser build (the `browser` export condition) decodes named
+  // character references through the document, as upstream's browser graph
+  // does (decode-named-character-reference's `index.dom.js`), so the entity
+  // table stays out. The locked graph is untouched: its decoder module is
+  // swapped in a staging copy of the source.
+  const staging = resolve(root, ".tmp", "browser-src")
+  rmSync(staging, { recursive: true, force: true })
+  cpSync(resolve(root, "src"), staging, { recursive: true })
+  cpSync(
+    resolve(root, "src", "browser", "decode-named.lil"),
+    resolve(staging, "graph", "remark-parse", "micromark", "decode-named.lil"),
+  )
+  compileLil(compiler, "lilscript.toml", `${file}.browser.raw.js`, staging)
+  rmSync(staging, { recursive: true, force: true })
 }
 
 compileIfRequested()
@@ -83,6 +99,12 @@
 const raw = readFileSync(rawPath, "utf8").trimEnd()
 writeFileSync(resolve(dist, `${file}.esm.js`), `${banner}${imports}const development=false;\n${raw}\n`)
 writeFileSync(resolve(dist, `${file}.development.js`), `${banner}${imports}const development=true;\n${raw}\n`)
+const browserRawPath = resolve(dist, `${file}.browser.raw.js`)
+if (existsSync(browserRawPath)) {
+  const browser = readFileSync(browserRawPath, "utf8").trimEnd()
+  writeFileSync(resolve(dist, `${file}.browser.js`), `${banner}${imports}const development=false;\n${browser}\n`)
+  unlinkSync(browserRawPath)
+}
 const closedRawPath = resolve(dist, `${file}.closed.raw.js`)
 if (existsSync(closedRawPath)) {
   const closed = readFileSync(closedRawPath, "utf8")
@@ -109,5 +131,5 @@
 await buildCommonJs(`${file}.development.js`, `${file}.development.cjs`)
 
 console.log(
-  `wrote dist/${file}.esm.js, dist/${file}.development.js, dist/${file}.cjs, dist/${file}.development.cjs, dist/${file}.closed.js`,
+  `wrote dist/${file}.esm.js, dist/${file}.browser.js, dist/${file}.development.js, dist/${file}.cjs, dist/${file}.development.cjs, dist/${file}.closed.js`,
 )
diff -ruN a/scripts/generate-schema.mjs b/scripts/generate-schema.mjs
--- a/scripts/generate-schema.mjs	2026-09-23 20:40:45.393451615 +0000
+++ b/scripts/generate-schema.mjs	2026-09-23 20:40:45.413768963 +0000
@@ -1,54 +1,64 @@
-import {writeFileSync} from "node:fs"
-import {dirname, resolve} from "node:path"
-import {fileURLToPath} from "node:url"
-import {html, svg} from "property-information"
-
-const root = resolve(dirname(fileURLToPath(import.meta.url)), "..")
-
-function serialize(info, includeSpace) {
-  const dashed = info.property.replace(/[A-Z]/g, (letter) => `-${letter.toLowerCase()}`)
-  const attribute = info.attribute === info.property
-    ? ""
-    : info.property.startsWith("aria") && info.attribute === `aria-${info.property.slice(4).toLowerCase()}`
-      ? "@"
-      : info.attribute === info.property.toLowerCase()
-        ? "~"
-        : info.attribute === dashed
-          ? "-"
-          : info.attribute
-  const fields = [
-    info.property,
-    attribute,
-    `${includeSpace && info.space ? "s" : ""}${info.commaSeparated ? "c" : ""}`,
-  ]
-  while (fields.at(-1) === "") fields.pop()
-  return fields.join("|")
+// property-information@7.2.0's html and svg schemas as the row groups that src/property-information/lib/find.lil
+// loads (writes src/property-information/lib/schema-data.lil).
+//
+// Every property of the two merged schemas is one row of one group. A group is a space-separated list of
+// names; the property is the group's prefix + the name, and its attribute follows the group's rule unless the
+// row spells it out as `name|attribute`. Rules: 0 the property itself, 1 its lowercase, 2 its dash-case,
+// 3 `aria-` + the lowercased name. Groups also fix the info's `space` (truthy for every property of a
+// schema module that has a space, i.e. all but aria's) and `commaSeparated`, and which schema(s) they
+// define into (rows identical in both schemas are defined into both).
+import {writeFileSync} from 'node:fs'
+import {dirname, resolve} from 'node:path'
+import {fileURLToPath} from 'node:url'
+import {html, svg} from 'property-information'
+
+const root = resolve(dirname(fileURLToPath(import.meta.url)), '..')
+
+const rows = (schema) => Object.values(schema.property).map((i) => ({p: i.property, a: i.attribute, s: Boolean(i.space), c: Boolean(i.commaSeparated)}))
+const key = (r) => [r.p, r.a, r.s, r.c].join('\0')
+const h = rows(html), s = rows(svg)
+const inSvg = new Set(s.map(key)), inHtml = new Set(h.map(key))
+const both = h.filter((r) => inSvg.has(key(r)))
+const htmlOnly = h.filter((r) => !inSvg.has(key(r)))
+const svgOnly = s.filter((r) => !inHtml.has(key(r)))
+const dash = (p) => p.replace(/[A-Z]/g, (l) => '-' + l.toLowerCase())
+const isOn = (r) => /^on[A-Z]/.test(r.p)
+const isAria = (r) => !r.s && r.p.startsWith('aria') && r.a === 'aria-' + r.p.slice(4).toLowerCase()
+
+// [export name, rows, prefix, rule, space, comma, target]
+const groups = [
+  ['ariaRows', both.filter(isAria), 'aria', 3, false, false, 'both'],
+  ['noSpaceRows', both.filter((r) => !r.s && !isAria(r)), '', 0, false, false, 'both'],
+  ['commonRows', both.filter((r) => r.s && !r.c && !isOn(r)), '', 1, true, false, 'both'],
+  ['commonOnRows', both.filter((r) => r.s && !r.c && isOn(r)), 'on', 1, true, false, 'both'],
+  ['htmlRows', htmlOnly.filter((r) => !r.c && !isOn(r)), '', 1, true, false, 'html'],
+  ['htmlOnRows', htmlOnly.filter((r) => !r.c && isOn(r)), 'on', 1, true, false, 'html'],
+  ['htmlCommaRows', htmlOnly.filter((r) => r.c), '', 1, true, true, 'html'],
+  ['svgRows', svgOnly.filter((r) => !r.c && (r.a === r.p || (r.a !== dash(r.p) && r.a !== r.p.toLowerCase()))), '', 0, true, false, 'svg'],
+  ['svgDashRows', svgOnly.filter((r) => !r.c && r.a !== r.p && r.a === dash(r.p)), '', 2, true, false, 'svg'],
+  ['svgLowerRows', svgOnly.filter((r) => !r.c && r.a !== r.p && r.a !== dash(r.p) && r.a === r.p.toLowerCase() && !isOn(r)), '', 1, true, false, 'svg'],
+  ['svgOnRows', svgOnly.filter((r) => !r.c && r.a !== r.p && r.a !== dash(r.p) && r.a === r.p.toLowerCase() && isOn(r)), 'on', 1, true, false, 'svg'],
+  ['svgCommaRows', svgOnly.filter((r) => r.c), '', 0, true, true, 'svg'],
+]
+const seen = new Set()
+for (const [, list] of groups) for (const r of list) seen.add(key(r))
+for (const r of [...both, ...htmlOnly, ...svgOnly]) if (!seen.has(key(r))) throw new Error('unassigned row ' + r.p)
+for (const r of both) if (r.s !== !isAria(r) && r.p !== 'role') throw new Error('unexpected space ' + r.p)
+
+function attribute(rule, prefix, name) {
+  const p = prefix + name
+  return rule === 0 ? p : rule === 1 ? p.toLowerCase() : rule === 2 ? dash(p) : 'aria-' + name.toLowerCase()
 }
-
-function records(schema) {
-  const result = new Map()
-  for (const info of Object.values(schema.property)) {
-    const key = [info.property, info.attribute, Boolean(info.space), Boolean(info.commaSeparated)].join("\0")
-    if (!result.has(key)) result.set(key, info)
-  }
-  return result
+const lines = [
+  '// Generated from property-information@7.2.0 by scripts/generate-schema.mjs: every property of the merged',
+  '// `html` and `svg` schemas, as the row groups that find.lil loads (see there).',
+]
+for (const [name, list, prefix, rule] of groups) {
+  const text = list.map((r) => {
+    const n = r.p.slice(prefix.length)
+    if (!r.p.startsWith(prefix) || n.includes(' ') || n.includes('|') || r.a.includes(' ')) throw new Error('bad row ' + r.p)
+    return r.a === attribute(rule, prefix, n) ? n : n + '|' + r.a
+  }).join(' ')
+  lines.push(`export string ${name} = ${JSON.stringify(text)};`)
 }
-
-const htmlRecords = records(html)
-const svgRecords = records(svg)
-const common = [...htmlRecords.keys()].filter((key) => svgRecords.has(key))
-const commonRows = common.map((key) => serialize(htmlRecords.get(key), true))
-for (const key of common) {
-  htmlRecords.delete(key)
-  svgRecords.delete(key)
-}
-
-const source = [
-  "// Generated from property-information@7.2.0 by scripts/generate-schema.mjs.",
-  `export string commonSchemaBlob = ${JSON.stringify(commonRows.join("\n"))};`,
-  `export string htmlSchemaBlob = ${JSON.stringify([...htmlRecords.values()].map((info) => serialize(info, false)).join("\n"))};`,
-  `export string svgSchemaBlob = ${JSON.stringify([...svgRecords.values()].map((info) => serialize(info, false)).join("\n"))};`,
-  "",
-].join("\n")
-
-writeFileSync(resolve(root, "src/property-information/lib/schema-data.lil"), source)
+writeFileSync(resolve(root, 'src/property-information/lib/schema-data.lil'), lines.join('\n') + '\n')
diff -ruN a/scripts/measure-graph.mjs b/scripts/measure-graph.mjs
--- a/scripts/measure-graph.mjs	2026-09-23 20:40:45.392860437 +0000
+++ b/scripts/measure-graph.mjs	2026-09-23 20:40:45.405011424 +0000
@@ -67,7 +67,9 @@
 if (!officialTerserNoMangle.code) throw new Error("Terser did not produce the no-mangle diagnostic")
 const officialEsbuild = await bundle("react-markdown", {metafile: false, minify: true})
 
-const lilPath = resolve(root, "dist", "react-markdown.esm.js")
+// The official graph is bundled for the browser, so the comparison is the
+// port's browser build (the package's `browser` condition).
+const lilPath = resolve(root, "dist", "react-markdown.browser.js")
 const lilCode = readFileSync(lilPath)
 const lilBundle = await bundle(lilPath)
 const lilImports = imports(lilBundle)
diff -ruN a/scripts/source-graph.mjs b/scripts/source-graph.mjs
--- a/scripts/source-graph.mjs	2026-09-23 20:40:45.394042442 +0000
+++ b/scripts/source-graph.mjs	2026-09-23 20:40:45.413449364 +0000
@@ -120,6 +120,8 @@
     return source.replace(before, after)
   }
   if (id === "remark-parse" && path === "src/micromark/character-entities.lil") {
+    // The table as an object literal (upstream's form) reads no host global.
+    if (source.startsWith("export JsValue characterEntities = object {")) return source
     const before = "export JsValue characterEntities = JSON.parse("
     const after = 'export JsValue characterEntities = JS.invoke(JSON, "parse", '
     if (!source.startsWith(before)) throw new Error("remark-parse entity table no longer matches the audited host-call wiring")
diff -ruN a/source-graph.lock.json b/source-graph.lock.json
--- a/source-graph.lock.json	2026-09-23 20:40:45.444852453 +0000
+++ b/source-graph.lock.json	2026-09-23 20:40:45.464162444 +0000
@@ -5,9 +5,9 @@
     {
       "id": "unified",
       "package": "@itslil/unified@11.0.6",
-      "siblingRevision": "80a4812a33a80b5626521451e130e1ddbf36a091",
-      "sourceSha256": "0a5260db8b9719da1c15e85b05aac82fe85c2ca4e50faac5a97b10c7ceb68529",
-      "graphSha256": "0c8a1f8180612cada2c3eeeddcd42033c0cb737d80c07245d85541ab642bff5e",
+      "siblingRevision": null,
+      "sourceSha256": "066761813ad8539b8f8069a5e1f2ff7e34bc61e3de80da6fe36144aeab6afbb4",
+      "graphSha256": "5b9a5806848b020b48fea9a3109552555d64c53a84adbada325e69a8e913b35c",
       "upstream": {
         "repository": "https://github.com/unifiedjs/unified.git",
         "tag": "11.0.5",
@@ -18,8 +18,8 @@
         {
           "path": "src/entry.lil",
           "graphPath": "src/graph/unified/entry.lil",
-          "sha256": "ccfd14e7bcc6aeca1a01f095e01c23457792ef7ffe2a01d11d3d74349a9620e1",
-          "graphSha256": "ccfd14e7bcc6aeca1a01f095e01c23457792ef7ffe2a01d11d3d74349a9620e1",
+          "sha256": "baa802c68e7c54ef57c3023187f6a52ef16c186a234314010f4b18f71b263782",
+          "graphSha256": "baa802c68e7c54ef57c3023187f6a52ef16c186a234314010f4b18f71b263782",
           "upstream": [
             {
               "path": "unified@11.0.5/lib/index.js",
@@ -30,8 +30,8 @@
         {
           "path": "src/extend.lil",
           "graphPath": "src/graph/unified/extend.lil",
-          "sha256": "5612b854f838998e9a0a78f43ae6bbcbe9e047558611e4f9787075debd4afc64",
-          "graphSha256": "5612b854f838998e9a0a78f43ae6bbcbe9e047558611e4f9787075debd4afc64",
+          "sha256": "4055d2d1d1a84eb61b897c2f6d706c2c17d283df4d520a0620d18b753f340b12",
+          "graphSha256": "4055d2d1d1a84eb61b897c2f6d706c2c17d283df4d520a0620d18b753f340b12",
           "upstream": [
             {
               "path": "extend@3.0.2/index.js",
@@ -42,8 +42,20 @@
         {
           "path": "src/host.lil",
           "graphPath": "src/graph/unified/host.lil",
-          "sha256": "a5d9a8b45adedcde57dcfb5fa27105037840c0b7a5c1e33a59f302cc618a914c",
-          "graphSha256": "0722d7ccfd00b30c599b87fa47c033768d0c6371e0449005b4b3718d2ef831e7",
+          "sha256": "745c7baa0a9b13001eae15b75a8be9ea063a88a51945e56728522d44a331ee89",
+          "graphSha256": "2d58906ffe8a4c8952e0eb832945fb27d258cff702f227de4b9579d6542cd540",
+          "upstream": [
+            {
+              "path": "unified@11.0.5/lib/callable-instance.js",
+              "sha256": "28c9baa7aea87597c4eca19a2e2b3342d3017347fabf7d513278787b5a950c58"
+            }
+          ]
+        },
+        {
+          "path": "src/index.lil",
+          "graphPath": "src/graph/unified/index.lil",
+          "sha256": "04a636b7c62fda30c4a61ff9bedf1ff640583b332107843c0a79ec186aa4aab7",
+          "graphSha256": "04a636b7c62fda30c4a61ff9bedf1ff640583b332107843c0a79ec186aa4aab7",
           "upstream": [
             {
               "path": "unified@11.0.5/lib/callable-instance.js",
@@ -54,8 +66,8 @@
         {
           "path": "src/plain.lil",
           "graphPath": "src/graph/unified/plain.lil",
-          "sha256": "903533b020487768987eff2dfb90a0593b9c9008b998da6833baae7faa309c58",
-          "graphSha256": "903533b020487768987eff2dfb90a0593b9c9008b998da6833baae7faa309c58",
+          "sha256": "8a52a7e342eebf815c188da089eba5acaad37b604d13111a471f7437ed1d4249",
+          "graphSha256": "8a52a7e342eebf815c188da089eba5acaad37b604d13111a471f7437ed1d4249",
           "upstream": [
             {
               "path": "is-plain-obj@4.1.0/index.js",
@@ -66,8 +78,8 @@
         {
           "path": "src/trough.lil",
           "graphPath": "src/graph/unified/trough.lil",
-          "sha256": "0aca356ccbd1e212aeea1de55a1dde4f5dc6e0bff518f613ae76fa32cea1acf0",
-          "graphSha256": "0aca356ccbd1e212aeea1de55a1dde4f5dc6e0bff518f613ae76fa32cea1acf0",
+          "sha256": "9759f1e87e4d67fcd65b85963c2cbb6b5b206ac0212d4045bfb2c04125067eff",
+          "graphSha256": "9759f1e87e4d67fcd65b85963c2cbb6b5b206ac0212d4045bfb2c04125067eff",
           "upstream": [
             {
               "path": "trough@2.2.0/lib/index.js",
@@ -78,8 +90,8 @@
         {
           "path": "src/vfile.lil",
           "graphPath": "src/graph/unified/vfile.lil",
-          "sha256": "8f00dd9d7a32402c6d7611c82456191a446ff2a4257d239307fbe7dd88263797",
-          "graphSha256": "8f00dd9d7a32402c6d7611c82456191a446ff2a4257d239307fbe7dd88263797",
+          "sha256": "044d55ffc6d6d3506521dfb940667253cd6393add8f77ed689aac98e42271195",
+          "graphSha256": "044d55ffc6d6d3506521dfb940667253cd6393add8f77ed689aac98e42271195",
           "upstream": [
             {
               "path": "vfile@6.0.3/lib/index.js",
@@ -100,9 +112,9 @@
     {
       "id": "remark-parse",
       "package": "@itslil/remark-parse@11.0.2",
-      "siblingRevision": "986f3242941155ce865b036d03ba2dc9381416bc",
-      "sourceSha256": "635c3bdd8c716b379fe1013611ae27c95824f823220cd0c41744fdd313a86a2f",
-      "graphSha256": "1b24f0714810323819c482ec89f8741af897e045ed308bcaeb3ab3ea39143c98",
+      "siblingRevision": null,
+      "sourceSha256": "07051b93cbe51a2c1b98f923860132147794041ee91fb5eed0d9094458182112",
+      "graphSha256": "53e969fc5f5bf32ec641887604119f1f20d6c5219a2696aec1deb328032a36b1",
       "upstream": {
         "repository": "https://github.com/remarkjs/remark.git",
         "tag": "remark-parse@11.0.0",
@@ -113,8 +125,8 @@
         {
           "path": "src/entry.lil",
           "graphPath": "src/graph/remark-parse/entry.lil",
-          "sha256": "c0c4cdf6994aa61d0b649faf972fc6e53073c611356f2d3baae5518188d02c94",
-          "graphSha256": "c0c4cdf6994aa61d0b649faf972fc6e53073c611356f2d3baae5518188d02c94",
+          "sha256": "b9fd3fe9132173c317426f247b2b1b64a29bbb90a0748e8af7002f700b75e5e2",
+          "graphSha256": "b9fd3fe9132173c317426f247b2b1b64a29bbb90a0748e8af7002f700b75e5e2",
           "upstream": [
             {
               "path": "remark-parse@11.0.0/lib/index.js",
@@ -125,8 +137,8 @@
         {
           "path": "src/from-markdown.lil",
           "graphPath": "src/graph/remark-parse/from-markdown.lil",
-          "sha256": "1343d38132359c39469f7af763beae7ce4e008c2c673b9f0da47c32a08244de5",
-          "graphSha256": "1343d38132359c39469f7af763beae7ce4e008c2c673b9f0da47c32a08244de5",
+          "sha256": "78894f1bafbd7b1717b28b6f4ffa140cca0fcec02c92e7b6951a5c33ca551bae",
+          "graphSha256": "78894f1bafbd7b1717b28b6f4ffa140cca0fcec02c92e7b6951a5c33ca551bae",
           "upstream": [
             {
               "path": "mdast-util-from-markdown@2.0.3/dev/lib/index.js",
@@ -137,8 +149,8 @@
         {
           "path": "src/micromark/character-entities.lil",
           "graphPath": "src/graph/remark-parse/micromark/character-entities.lil",
-          "sha256": "9cc1b93ab188a1fc88dac103a9a94ae38a796c65b0ee90e4cf2415592f854002",
-          "graphSha256": "aad21163644e19a5fc833ebb12fb93a9692a7ce1ac5721fdccabca73ca0da49b",
+          "sha256": "c8b6618cbfd0fdf86d5c36b8a4ed1e4ee141a25088251142a20fe599fca210b3",
+          "graphSha256": "c8b6618cbfd0fdf86d5c36b8a4ed1e4ee141a25088251142a20fe599fca210b3",
           "upstream": [
             {
               "path": "character-entities@2.0.2/index.js",
@@ -149,8 +161,8 @@
         {
           "path": "src/micromark/constructs.lil",
           "graphPath": "src/graph/remark-parse/micromark/constructs.lil",
-          "sha256": "87d6280f596f1fd5e966377361a65a9c1d30e23dcb377a00964d9b9c08169c18",
-          "graphSha256": "87d6280f596f1fd5e966377361a65a9c1d30e23dcb377a00964d9b9c08169c18",
+          "sha256": "74cb525c1651cc300f78f4770c648d3593c4e2dc3f4a45351e459b29be14d84a",
+          "graphSha256": "74cb525c1651cc300f78f4770c648d3593c4e2dc3f4a45351e459b29be14d84a",
           "upstream": [
             {
               "path": "micromark@4.0.2/dev/lib/constructs.js",
@@ -161,8 +173,8 @@
         {
           "path": "src/micromark/core/attention.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/attention.lil",
-          "sha256": "68abc4a24dbac2460d47a895eb6ae13c1292b88201e76ec82ed9a6d5e8cc6bf2",
-          "graphSha256": "68abc4a24dbac2460d47a895eb6ae13c1292b88201e76ec82ed9a6d5e8cc6bf2",
+          "sha256": "35d47c10695c713c471cc468088ac8cf975ce0d48e7dff237d951112a09f5632",
+          "graphSha256": "35d47c10695c713c471cc468088ac8cf975ce0d48e7dff237d951112a09f5632",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/attention.js",
@@ -173,8 +185,8 @@
         {
           "path": "src/micromark/core/autolink.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/autolink.lil",
-          "sha256": "ad643553d680123a113d17fe746d8e542b0287370b377321327311d511733754",
-          "graphSha256": "ad643553d680123a113d17fe746d8e542b0287370b377321327311d511733754",
+          "sha256": "97e7eb8b4b33f23df8600b7d811b5cfbbb8df23bfc01dfa25fb528a085d55703",
+          "graphSha256": "97e7eb8b4b33f23df8600b7d811b5cfbbb8df23bfc01dfa25fb528a085d55703",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/autolink.js",
@@ -185,8 +197,8 @@
         {
           "path": "src/micromark/core/blank-line.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/blank-line.lil",
-          "sha256": "343b83ddc9fc78780601af8a4f47d5031ef13384f8f803360e33d415feba9dd2",
-          "graphSha256": "343b83ddc9fc78780601af8a4f47d5031ef13384f8f803360e33d415feba9dd2",
+          "sha256": "115d2dcacdf715b02efce52caac3960d5b855939a68dc0a20817491e8b1194b0",
+          "graphSha256": "115d2dcacdf715b02efce52caac3960d5b855939a68dc0a20817491e8b1194b0",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/blank-line.js",
@@ -197,8 +209,8 @@
         {
           "path": "src/micromark/core/block-quote.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/block-quote.lil",
-          "sha256": "2470532d1426c6bcf3bf22b4e990c273c148a86b0e212930fef755db3243f1b9",
-          "graphSha256": "2470532d1426c6bcf3bf22b4e990c273c148a86b0e212930fef755db3243f1b9",
+          "sha256": "eae45318d2972f4be0fd616cb243d59dd366cb61209b13796d651cfc8297ab49",
+          "graphSha256": "eae45318d2972f4be0fd616cb243d59dd366cb61209b13796d651cfc8297ab49",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/block-quote.js",
@@ -209,8 +221,8 @@
         {
           "path": "src/micromark/core/character-escape.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/character-escape.lil",
-          "sha256": "6062220ebd95f9e35017a399066849d31fcb878dd0063da70b511425c165cbc2",
-          "graphSha256": "6062220ebd95f9e35017a399066849d31fcb878dd0063da70b511425c165cbc2",
+          "sha256": "20193c0809adfe18e77d04a7a782ad437d912f3de997a402930af8e46031c6ab",
+          "graphSha256": "20193c0809adfe18e77d04a7a782ad437d912f3de997a402930af8e46031c6ab",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/character-escape.js",
@@ -221,8 +233,8 @@
         {
           "path": "src/micromark/core/character-reference.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/character-reference.lil",
-          "sha256": "5df6b5ba18999fc55a54b6bdff332b21013a4a26816ac57dc12ef19650dd66d5",
-          "graphSha256": "5df6b5ba18999fc55a54b6bdff332b21013a4a26816ac57dc12ef19650dd66d5",
+          "sha256": "b355201b60833cf854ca7328b4cd1a9d405ae491b4d1b5a70178313c8c16c71a",
+          "graphSha256": "b355201b60833cf854ca7328b4cd1a9d405ae491b4d1b5a70178313c8c16c71a",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/character-reference.js",
@@ -233,8 +245,8 @@
         {
           "path": "src/micromark/core/code-fenced.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/code-fenced.lil",
-          "sha256": "3ce57df5cee12be95b896bb03c3315471585cebe549ab099fa40eb843db118d5",
-          "graphSha256": "3ce57df5cee12be95b896bb03c3315471585cebe549ab099fa40eb843db118d5",
+          "sha256": "3f4bb3f8b531e3729291c8dcdeab6d6a8e6fc2449eafac161d818f2ddbb27a1e",
+          "graphSha256": "3f4bb3f8b531e3729291c8dcdeab6d6a8e6fc2449eafac161d818f2ddbb27a1e",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/code-fenced.js",
@@ -245,8 +257,8 @@
         {
           "path": "src/micromark/core/code-indented.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/code-indented.lil",
-          "sha256": "7449b99550b4ca4e462dcca9239302753e4c877f20a82b06479756d4201c4957",
-          "graphSha256": "7449b99550b4ca4e462dcca9239302753e4c877f20a82b06479756d4201c4957",
+          "sha256": "fb8705076d7529695e7da77e235cd90de1af56c92b7ec746de3af9499a2f8be8",
+          "graphSha256": "fb8705076d7529695e7da77e235cd90de1af56c92b7ec746de3af9499a2f8be8",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/code-indented.js",
@@ -257,8 +269,8 @@
         {
           "path": "src/micromark/core/code-text.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/code-text.lil",
-          "sha256": "950ea75ab25aff10060869fb37f5272318dd855b0682b86e3f743e7092b92ac9",
-          "graphSha256": "950ea75ab25aff10060869fb37f5272318dd855b0682b86e3f743e7092b92ac9",
+          "sha256": "d35e0cdd7e33ae98579152b3114a826ff43fe69b3802b9e9db7c58fae8ee4388",
+          "graphSha256": "d35e0cdd7e33ae98579152b3114a826ff43fe69b3802b9e9db7c58fae8ee4388",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/code-text.js",
@@ -269,8 +281,8 @@
         {
           "path": "src/micromark/core/content.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/content.lil",
-          "sha256": "a1e54b21fea0fcb4291a075f6298aa5500945a4f32f9002f92d3810dc6dd0170",
-          "graphSha256": "a1e54b21fea0fcb4291a075f6298aa5500945a4f32f9002f92d3810dc6dd0170",
+          "sha256": "05cc704bd51c0eaf8b9740bd243bbd92842e77a0c6d0372834c76d42ca6a7620",
+          "graphSha256": "05cc704bd51c0eaf8b9740bd243bbd92842e77a0c6d0372834c76d42ca6a7620",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/content.js",
@@ -281,8 +293,8 @@
         {
           "path": "src/micromark/core/definition.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/definition.lil",
-          "sha256": "19c16287f2a80ba4e7832440121214a1f6daee2f489cc1004445a852bda4c8c7",
-          "graphSha256": "19c16287f2a80ba4e7832440121214a1f6daee2f489cc1004445a852bda4c8c7",
+          "sha256": "38becb049d8509a9239480090acf683d90d24ad4ef4e8b35f14e92c326788011",
+          "graphSha256": "38becb049d8509a9239480090acf683d90d24ad4ef4e8b35f14e92c326788011",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/definition.js",
@@ -293,8 +305,8 @@
         {
           "path": "src/micromark/core/hard-break-escape.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/hard-break-escape.lil",
-          "sha256": "8b80f9aece609713d3dcd490b73037315d81ab0cc58adb5ff80ca75cb4c1f688",
-          "graphSha256": "8b80f9aece609713d3dcd490b73037315d81ab0cc58adb5ff80ca75cb4c1f688",
+          "sha256": "92b8a9e28270f61184e6572a8e0640c33f7fbd163c24dde1255d7211e7ca0b2a",
+          "graphSha256": "92b8a9e28270f61184e6572a8e0640c33f7fbd163c24dde1255d7211e7ca0b2a",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/hard-break-escape.js",
@@ -305,8 +317,8 @@
         {
           "path": "src/micromark/core/heading-atx.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/heading-atx.lil",
-          "sha256": "64264c21f87ed1888524f7c21d32bc240bcb42c95b715bd6f15b3b9f685958ba",
-          "graphSha256": "64264c21f87ed1888524f7c21d32bc240bcb42c95b715bd6f15b3b9f685958ba",
+          "sha256": "9d2fd673aab15d904d2b876aa66e7c92ac30c14f8b838ea34b58d4af0f9d269c",
+          "graphSha256": "9d2fd673aab15d904d2b876aa66e7c92ac30c14f8b838ea34b58d4af0f9d269c",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/heading-atx.js",
@@ -317,8 +329,8 @@
         {
           "path": "src/micromark/core/html-flow.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/html-flow.lil",
-          "sha256": "bd6708d6086a450a20a5e36549b715a0ed6881bd983927bd4d135790a3e050f7",
-          "graphSha256": "bd6708d6086a450a20a5e36549b715a0ed6881bd983927bd4d135790a3e050f7",
+          "sha256": "ba2daf14d7cd5c29ab123ce10374c26fb784770eebf8348ce73bb5a58c3a67ef",
+          "graphSha256": "ba2daf14d7cd5c29ab123ce10374c26fb784770eebf8348ce73bb5a58c3a67ef",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/html-flow.js",
@@ -329,8 +341,8 @@
         {
           "path": "src/micromark/core/html-text.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/html-text.lil",
-          "sha256": "73935c0d5fb15ca89a6f41be87055d4206f3b31c05bc6a85d89c1bf31ee61e24",
-          "graphSha256": "73935c0d5fb15ca89a6f41be87055d4206f3b31c05bc6a85d89c1bf31ee61e24",
+          "sha256": "1e386a681a07b86327fc72dd7735683e86367f0a183a817976a302c0b3da26fa",
+          "graphSha256": "1e386a681a07b86327fc72dd7735683e86367f0a183a817976a302c0b3da26fa",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/html-text.js",
@@ -341,8 +353,8 @@
         {
           "path": "src/micromark/core/label-end.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/label-end.lil",
-          "sha256": "2edd430ed5d783a41eb8aaf748c792b35bcedb0da92c3526273ed3311366619d",
-          "graphSha256": "2edd430ed5d783a41eb8aaf748c792b35bcedb0da92c3526273ed3311366619d",
+          "sha256": "4192d0a356f2953a56f695f038c3920ab20305956b310d7061b4cd8bc5274f1d",
+          "graphSha256": "4192d0a356f2953a56f695f038c3920ab20305956b310d7061b4cd8bc5274f1d",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/label-end.js",
@@ -353,8 +365,8 @@
         {
           "path": "src/micromark/core/label-start-image.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/label-start-image.lil",
-          "sha256": "a476c096e652c8a016f519ef530a6ea14f3e6ac7b1252927baa1f0d66868dc12",
-          "graphSha256": "a476c096e652c8a016f519ef530a6ea14f3e6ac7b1252927baa1f0d66868dc12",
+          "sha256": "e4a9ba23857880369a5b7912a5316c41173094574b6b5b6721447a399a7e7673",
+          "graphSha256": "e4a9ba23857880369a5b7912a5316c41173094574b6b5b6721447a399a7e7673",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/label-start-image.js",
@@ -365,8 +377,8 @@
         {
           "path": "src/micromark/core/label-start-link.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/label-start-link.lil",
-          "sha256": "ae64132563044b1ef0879e01f80578b65b099d3e80a524d1ae806dff41fb1c81",
-          "graphSha256": "ae64132563044b1ef0879e01f80578b65b099d3e80a524d1ae806dff41fb1c81",
+          "sha256": "e350229f5af2055c3a065af25bacaa22765e13f1fb9c137df624cfac1b89d002",
+          "graphSha256": "e350229f5af2055c3a065af25bacaa22765e13f1fb9c137df624cfac1b89d002",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/label-start-link.js",
@@ -377,8 +389,8 @@
         {
           "path": "src/micromark/core/line-ending.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/line-ending.lil",
-          "sha256": "ed00711f15d2d16b1bcbcc119e31926b589ab18e440bebf50fdc3d827a3b422e",
-          "graphSha256": "ed00711f15d2d16b1bcbcc119e31926b589ab18e440bebf50fdc3d827a3b422e",
+          "sha256": "a448ed6a6485a776f3e67941bebef001cdbe486f23bdfd4664842178973d39be",
+          "graphSha256": "a448ed6a6485a776f3e67941bebef001cdbe486f23bdfd4664842178973d39be",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/line-ending.js",
@@ -389,8 +401,8 @@
         {
           "path": "src/micromark/core/list.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/list.lil",
-          "sha256": "c853e1f7ea7539f359fd31575b28b234c149aed64b14995b6af15de7212d518f",
-          "graphSha256": "c853e1f7ea7539f359fd31575b28b234c149aed64b14995b6af15de7212d518f",
+          "sha256": "3a8e976a8a6ccdc8330f26561caa457040ecf0c2d86773551abc505180ef4119",
+          "graphSha256": "3a8e976a8a6ccdc8330f26561caa457040ecf0c2d86773551abc505180ef4119",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/list.js",
@@ -401,8 +413,8 @@
         {
           "path": "src/micromark/core/setext-underline.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/setext-underline.lil",
-          "sha256": "5555df873ca4993f71ae6a6ce6b8feea65a8bb88e672cb28f298197d80da1f36",
-          "graphSha256": "5555df873ca4993f71ae6a6ce6b8feea65a8bb88e672cb28f298197d80da1f36",
+          "sha256": "4e4ca3c5448dd4955789148a6f77ee089c20d55702db91657a92f0641bc6752b",
+          "graphSha256": "4e4ca3c5448dd4955789148a6f77ee089c20d55702db91657a92f0641bc6752b",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/setext-underline.js",
@@ -413,8 +425,8 @@
         {
           "path": "src/micromark/core/thematic-break.lil",
           "graphPath": "src/graph/remark-parse/micromark/core/thematic-break.lil",
-          "sha256": "755953b5988f0e4dde1459d243ce12e5d964a456dd32a8c8b62e21ed24c151e1",
-          "graphSha256": "755953b5988f0e4dde1459d243ce12e5d964a456dd32a8c8b62e21ed24c151e1",
+          "sha256": "5776bda5a2850218bd3c4c9fdc796d46c8979b2deac17c7e19376c6816a54313",
+          "graphSha256": "5776bda5a2850218bd3c4c9fdc796d46c8979b2deac17c7e19376c6816a54313",
           "upstream": [
             {
               "path": "micromark-core-commonmark@2.0.3/dev/lib/thematic-break.js",
@@ -425,8 +437,8 @@
         {
           "path": "src/micromark/create-tokenizer.lil",
           "graphPath": "src/graph/remark-parse/micromark/create-tokenizer.lil",
-          "sha256": "bcf4ce93df4e32d89585011992b760fa910c6b88a69f206dd2e09e751052d6f9",
-          "graphSha256": "bcf4ce93df4e32d89585011992b760fa910c6b88a69f206dd2e09e751052d6f9",
+          "sha256": "d587bd8a701c469fb05407a1b988231862430bc2d1cbaa0198218a705d41e690",
+          "graphSha256": "d587bd8a701c469fb05407a1b988231862430bc2d1cbaa0198218a705d41e690",
           "upstream": [
             {
               "path": "micromark@4.0.2/dev/lib/create-tokenizer.js",
@@ -437,8 +449,8 @@
         {
           "path": "src/micromark/decode-named.lil",
           "graphPath": "src/graph/remark-parse/micromark/decode-named.lil",
-          "sha256": "b749446edcdd8a4d051fee685ae72d07714d04d0b14d02e4e7955b3540cd493b",
-          "graphSha256": "b749446edcdd8a4d051fee685ae72d07714d04d0b14d02e4e7955b3540cd493b",
+          "sha256": "e4150b19e19a214e7ccfb5d6762bdce62fde30b8cc907642d911f9ea87b9e6e8",
+          "graphSha256": "e4150b19e19a214e7ccfb5d6762bdce62fde30b8cc907642d911f9ea87b9e6e8",
           "upstream": [
             {
               "path": "decode-named-character-reference@1.3.0/index.js",
@@ -449,8 +461,8 @@
         {
           "path": "src/micromark/factory-destination.lil",
           "graphPath": "src/graph/remark-parse/micromark/factory-destination.lil",
-          "sha256": "af165fc6522b16cdd41126055fe79446e4b6cc76058742f5a80fab72605d60de",
-          "graphSha256": "af165fc6522b16cdd41126055fe79446e4b6cc76058742f5a80fab72605d60de",
+          "sha256": "5be6c3c02bacd604c1501b85b6785602ed8b447a85679f85e6ee7351964bb161",
+          "graphSha256": "5be6c3c02bacd604c1501b85b6785602ed8b447a85679f85e6ee7351964bb161",
           "upstream": [
             {
               "path": "micromark-factory-destination@2.0.1/dev/index.js",
@@ -461,8 +473,8 @@
         {
           "path": "src/micromark/factory-label.lil",
           "graphPath": "src/graph/remark-parse/micromark/factory-label.lil",
-          "sha256": "96f7ba45f26175d12451e5b5e669c7a262d879c52d041428351dedf6704b2ecf",
-          "graphSha256": "96f7ba45f26175d12451e5b5e669c7a262d879c52d041428351dedf6704b2ecf",
+          "sha256": "6376115906bbfbce66d13f0a50ff3b921922135b18b6e66d290eb0b54221f21d",
+          "graphSha256": "6376115906bbfbce66d13f0a50ff3b921922135b18b6e66d290eb0b54221f21d",
           "upstream": [
             {
               "path": "micromark-factory-label@2.0.1/dev/index.js",
@@ -473,8 +485,8 @@
         {
           "path": "src/micromark/factory-space.lil",
           "graphPath": "src/graph/remark-parse/micromark/factory-space.lil",
-          "sha256": "9012b380fb616777a4819fb51434fe7722a234593d155c25b69d5d0db14b19c6",
-          "graphSha256": "9012b380fb616777a4819fb51434fe7722a234593d155c25b69d5d0db14b19c6",
+          "sha256": "a5932f5e1b06d6517cdc43a5b0a12cfc9a6b875e94d9c937213f5192fb3b50ba",
+          "graphSha256": "a5932f5e1b06d6517cdc43a5b0a12cfc9a6b875e94d9c937213f5192fb3b50ba",
           "upstream": [
             {
               "path": "micromark-factory-space@2.0.1/dev/index.js",
@@ -485,8 +497,8 @@
         {
           "path": "src/micromark/factory-title.lil",
           "graphPath": "src/graph/remark-parse/micromark/factory-title.lil",
-          "sha256": "d874bd7921cc041c46c9414f1b4808b7c79fd3f42813502f06993b0bfb0f1b13",
-          "graphSha256": "d874bd7921cc041c46c9414f1b4808b7c79fd3f42813502f06993b0bfb0f1b13",
+          "sha256": "f22c5e3e9919b86b7f2e0cc8b38f6cc1182e88917c9f3fcb61cbdba56404a07c",
+          "graphSha256": "f22c5e3e9919b86b7f2e0cc8b38f6cc1182e88917c9f3fcb61cbdba56404a07c",
           "upstream": [
             {
               "path": "micromark-factory-title@2.0.1/dev/index.js",
@@ -497,8 +509,8 @@
         {
           "path": "src/micromark/factory-whitespace.lil",
           "graphPath": "src/graph/remark-parse/micromark/factory-whitespace.lil",
-          "sha256": "62d5c0b47eea9941788dd42bad2d616701ad48049190f681deca09ed6d3479bd",
-          "graphSha256": "62d5c0b47eea9941788dd42bad2d616701ad48049190f681deca09ed6d3479bd",
+          "sha256": "e63b322526936f7e746978a4a3e6d56b315dcd01bde6732cf9d94bfb93845d36",
+          "graphSha256": "e63b322526936f7e746978a4a3e6d56b315dcd01bde6732cf9d94bfb93845d36",
           "upstream": [
             {
               "path": "micromark-factory-whitespace@2.0.1/dev/index.js",
@@ -509,8 +521,8 @@
         {
           "path": "src/micromark/host.lil",
           "graphPath": "src/graph/remark-parse/micromark/host.lil",
-          "sha256": "f2d1dcd4fc9adfe8451052e0397dc80dd6d3bd554f57a262bbb34b21c71302c8",
-          "graphSha256": "4dea487f419d6134db3277f048672a80bba67a990c5cebe376144f85abff4c75",
+          "sha256": "6badb1a179beafd2e5fc92d05c784290b3407412ab9a58769a85dbfe33e2f357",
+          "graphSha256": "ba98ab9f1dc8d9dc1bed4fb4278280da8b4e3724276594f59b4a4fd29efc849e",
           "upstream": [
             {
               "path": "micromark@4.0.2/dev/lib/create-tokenizer.js",
@@ -521,8 +533,8 @@
         {
           "path": "src/micromark/initialize/content.lil",
           "graphPath": "src/graph/remark-parse/micromark/initialize/content.lil",
-          "sha256": "478689d51eb6fd278d85162871cfea3cd1fbe22eea41d0470844d3afd4377452",
-          "graphSha256": "478689d51eb6fd278d85162871cfea3cd1fbe22eea41d0470844d3afd4377452",
+          "sha256": "3f4959b21c63be2570bbc74960af330e4b6a9646fbb348cef55300baabff2d45",
+          "graphSha256": "3f4959b21c63be2570bbc74960af330e4b6a9646fbb348cef55300baabff2d45",
           "upstream": [
             {
               "path": "micromark@4.0.2/dev/lib/initialize/content.js",
@@ -533,8 +545,8 @@
         {
           "path": "src/micromark/initialize/document.lil",
           "graphPath": "src/graph/remark-parse/micromark/initialize/document.lil",
-          "sha256": "4da841b9b4af4bc44617d04e30c6e1166590336ec2b01534a2af80ad414b6df3",
-          "graphSha256": "4da841b9b4af4bc44617d04e30c6e1166590336ec2b01534a2af80ad414b6df3",
+          "sha256": "d74de0315b97b124e43f302f64eb4cfd2cc1466dafd00639d20b818a84958856",
+          "graphSha256": "d74de0315b97b124e43f302f64eb4cfd2cc1466dafd00639d20b818a84958856",
           "upstream": [
             {
               "path": "micromark@4.0.2/dev/lib/initialize/document.js",
@@ -545,8 +557,8 @@
         {
           "path": "src/micromark/initialize/flow.lil",
           "graphPath": "src/graph/remark-parse/micromark/initialize/flow.lil",
-          "sha256": "085ee22e91469e9c261eed670445a74383682af8b0a1103ca14d2c1d4c5ccbbf",
-          "graphSha256": "085ee22e91469e9c261eed670445a74383682af8b0a1103ca14d2c1d4c5ccbbf",
+          "sha256": "4ed3a38963b8557e3d1cf818ad7ba0f9bcd3f90c2268f91962891ec3fb14f1e7",
+          "graphSha256": "4ed3a38963b8557e3d1cf818ad7ba0f9bcd3f90c2268f91962891ec3fb14f1e7",
           "upstream": [
             {
               "path": "micromark@4.0.2/dev/lib/initialize/flow.js",
@@ -557,8 +569,8 @@
         {
           "path": "src/micromark/initialize/text.lil",
           "graphPath": "src/graph/remark-parse/micromark/initialize/text.lil",
-          "sha256": "9d0a38e95dc29c2d060362b6fc5ccbde281edbe18b45749f5c2fb116c5145eee",
-          "graphSha256": "9d0a38e95dc29c2d060362b6fc5ccbde281edbe18b45749f5c2fb116c5145eee",
+          "sha256": "85651ec8148ff18619ca4e46f5e5e4ea5c810c537b6e69d807fc824e4a1f1905",
+          "graphSha256": "85651ec8148ff18619ca4e46f5e5e4ea5c810c537b6e69d807fc824e4a1f1905",
           "upstream": [
             {
               "path": "micromark@4.0.2/dev/lib/initialize/text.js",
@@ -569,8 +581,8 @@
         {
           "path": "src/micromark/parse.lil",
           "graphPath": "src/graph/remark-parse/micromark/parse.lil",
-          "sha256": "f481b1897ea699b8917be891cfc0ae05a88855716a4fb502db89b107ff0949ea",
-          "graphSha256": "f481b1897ea699b8917be891cfc0ae05a88855716a4fb502db89b107ff0949ea",
+          "sha256": "b036c2932912ef350c629c126601292bda49caab2a0152d77988dcb979481d83",
+          "graphSha256": "b036c2932912ef350c629c126601292bda49caab2a0152d77988dcb979481d83",
           "upstream": [
             {
               "path": "micromark@4.0.2/dev/lib/parse.js",
@@ -593,8 +605,8 @@
         {
           "path": "src/micromark/preprocess.lil",
           "graphPath": "src/graph/remark-parse/micromark/preprocess.lil",
-          "sha256": "986ccaeff2d1f35af7854be840023164da7c964a203542efc3176602aa092adb",
-          "graphSha256": "986ccaeff2d1f35af7854be840023164da7c964a203542efc3176602aa092adb",
+          "sha256": "ce28b92f70b3bd54428324b0f6695ca162aa5df1d2e0f431fde1f8ef4638e9b8",
+          "graphSha256": "ce28b92f70b3bd54428324b0f6695ca162aa5df1d2e0f431fde1f8ef4638e9b8",
           "upstream": [
             {
               "path": "micromark@4.0.2/dev/lib/preprocess.js",
@@ -605,8 +617,8 @@
         {
           "path": "src/micromark/splice-buffer.lil",
           "graphPath": "src/graph/remark-parse/micromark/splice-buffer.lil",
-          "sha256": "cf538daab2d4ec303964969784fd9b47ff756495e1e55da7b3af06bb16394a7a",
-          "graphSha256": "cf538daab2d4ec303964969784fd9b47ff756495e1e55da7b3af06bb16394a7a",
+          "sha256": "9fcce40f739a2119a7bb0b44ad7ec6c69344173854feedef3e528d168102f512",
+          "graphSha256": "9fcce40f739a2119a7bb0b44ad7ec6c69344173854feedef3e528d168102f512",
           "upstream": [
             {
               "path": "micromark-util-subtokenize@2.1.0/dev/lib/splice-buffer.js",
@@ -665,8 +677,8 @@
         {
           "path": "src/micromark/util-character.lil",
           "graphPath": "src/graph/remark-parse/micromark/util-character.lil",
-          "sha256": "7a1c236776c0d3dfb12d9b6f2ddbe181cb06a0d040d0ce71a43782a9c8a96397",
-          "graphSha256": "7a1c236776c0d3dfb12d9b6f2ddbe181cb06a0d040d0ce71a43782a9c8a96397",
+          "sha256": "634af98913dfb47ab2fcdf6fd17e2b4733bc26608f78609e110ce0d33bdf4a15",
+          "graphSha256": "634af98913dfb47ab2fcdf6fd17e2b4733bc26608f78609e110ce0d33bdf4a15",
           "upstream": [
             {
               "path": "micromark-util-character@2.1.1/dev/index.js",
@@ -677,8 +689,8 @@
         {
           "path": "src/micromark/util-chunked.lil",
           "graphPath": "src/graph/remark-parse/micromark/util-chunked.lil",
-          "sha256": "dc63c7d2096496959c0c5317e7375ce7c61d23dd69b129b61b0d9bf79e4045cd",
-          "graphSha256": "dc63c7d2096496959c0c5317e7375ce7c61d23dd69b129b61b0d9bf79e4045cd",
+          "sha256": "da747a8e78fa84b0a467616f68d83384737e53ad1b016e83f9fb803d2edf8e54",
+          "graphSha256": "da747a8e78fa84b0a467616f68d83384737e53ad1b016e83f9fb803d2edf8e54",
           "upstream": [
             {
               "path": "micromark-util-chunked@2.0.1/index.js",
@@ -689,8 +701,8 @@
         {
           "path": "src/micromark/util-classify-character.lil",
           "graphPath": "src/graph/remark-parse/micromark/util-classify-character.lil",
-          "sha256": "8f193927ef6c0a6ec30125dcfe5f24e70e0d1acb178a0f701a6be61050a9b841",
-          "graphSha256": "8f193927ef6c0a6ec30125dcfe5f24e70e0d1acb178a0f701a6be61050a9b841",
+          "sha256": "80373b35201103ddfd5b5a942d97e1ab492fee40858c395d9e9113d2255d4477",
+          "graphSha256": "80373b35201103ddfd5b5a942d97e1ab492fee40858c395d9e9113d2255d4477",
           "upstream": [
             {
               "path": "micromark-util-classify-character@2.0.1/dev/index.js",
@@ -701,8 +713,8 @@
         {
           "path": "src/micromark/util-combine-extensions.lil",
           "graphPath": "src/graph/remark-parse/micromark/util-combine-extensions.lil",
-          "sha256": "d15e6db103822db1cfeb54f40512af44ef04b3daf5794fce9b679dc99c8753b4",
-          "graphSha256": "d15e6db103822db1cfeb54f40512af44ef04b3daf5794fce9b679dc99c8753b4",
+          "sha256": "45dda94d1f191795642dac1e553985170a046717ad685685a9c61453a1768cfd",
+          "graphSha256": "45dda94d1f191795642dac1e553985170a046717ad685685a9c61453a1768cfd",
           "upstream": [
             {
               "path": "micromark-util-combine-extensions@2.0.1/index.js",
@@ -713,8 +725,8 @@
         {
           "path": "src/micromark/util-decode-numeric.lil",
           "graphPath": "src/graph/remark-parse/micromark/util-decode-numeric.lil",
-          "sha256": "78b1050ac7751b5b09436c728a616bd87d98be53e64ac8200a624b37faa63b71",
-          "graphSha256": "78b1050ac7751b5b09436c728a616bd87d98be53e64ac8200a624b37faa63b71",
+          "sha256": "03d27ae0446af7f4aa870de13c4c41b65bf53f7a12731c8ee96694f34f95f4fc",
+          "graphSha256": "03d27ae0446af7f4aa870de13c4c41b65bf53f7a12731c8ee96694f34f95f4fc",
           "upstream": [
             {
               "path": "micromark-util-decode-numeric-character-reference@2.0.2/index.js",
@@ -725,8 +737,8 @@
         {
           "path": "src/micromark/util-decode-string.lil",
           "graphPath": "src/graph/remark-parse/micromark/util-decode-string.lil",
-          "sha256": "a1df9eee87107f4a39ef8c607fae7fad68113b0023f1a7f2f6a8abfc28284233",
-          "graphSha256": "a1df9eee87107f4a39ef8c607fae7fad68113b0023f1a7f2f6a8abfc28284233",
+          "sha256": "df8465926f8b05577467dfc4fb4ed42f28b71574e541002cfc2395275c5fc907",
+          "graphSha256": "df8465926f8b05577467dfc4fb4ed42f28b71574e541002cfc2395275c5fc907",
           "upstream": [
             {
               "path": "micromark-util-decode-string@2.0.1/index.js",
@@ -761,8 +773,8 @@
         {
           "path": "src/micromark/util-resolve-all.lil",
           "graphPath": "src/graph/remark-parse/micromark/util-resolve-all.lil",
-          "sha256": "4857aeec8d7eb15053668e81899371cd3bb6d9a23a68740204e9ff818120dfd2",
-          "graphSha256": "4857aeec8d7eb15053668e81899371cd3bb6d9a23a68740204e9ff818120dfd2",
+          "sha256": "d7df6ed1af3b6b922a3f3150f0fc8f94cf4f38852f32819513c56a5a0c546a74",
+          "graphSha256": "d7df6ed1af3b6b922a3f3150f0fc8f94cf4f38852f32819513c56a5a0c546a74",
           "upstream": [
             {
               "path": "micromark-util-resolve-all@2.0.1/index.js",
@@ -773,8 +785,8 @@
         {
           "path": "src/micromark/util-subtokenize.lil",
           "graphPath": "src/graph/remark-parse/micromark/util-subtokenize.lil",
-          "sha256": "926fd04ec5e2c276cecb468d29b02a7f6645683a959f6af97792c00c23c22fb4",
-          "graphSha256": "926fd04ec5e2c276cecb468d29b02a7f6645683a959f6af97792c00c23c22fb4",
+          "sha256": "2b22576838b22844e1b6d793a4ee542deb73a396ae594b53ce1d9550ff0fe69a",
+          "graphSha256": "2b22576838b22844e1b6d793a4ee542deb73a396ae594b53ce1d9550ff0fe69a",
           "upstream": [
             {
               "path": "micromark-util-subtokenize@2.1.0/dev/index.js",
@@ -785,8 +797,8 @@
         {
           "path": "src/stringify-position.lil",
           "graphPath": "src/graph/remark-parse/stringify-position.lil",
-          "sha256": "ec72ceeee1f9f93f1e58d367dd6d6b3ef291cf24518d871e50ad2db09d2deaf7",
-          "graphSha256": "ec72ceeee1f9f93f1e58d367dd6d6b3ef291cf24518d871e50ad2db09d2deaf7",
+          "sha256": "4a625ab59ab7059b489f26d9089f6f69530cb5a56044eaf1ad71f4de8d1b0663",
+          "graphSha256": "4a625ab59ab7059b489f26d9089f6f69530cb5a56044eaf1ad71f4de8d1b0663",
           "upstream": [
             {
               "path": "unist-util-stringify-position@4.0.0/lib/index.js",
@@ -797,8 +809,8 @@
         {
           "path": "src/to-string.lil",
           "graphPath": "src/graph/remark-parse/to-string.lil",
-          "sha256": "c35561280f64eab656cee076848359526bc9fd364b429ad31fecd82ececc9fb3",
-          "graphSha256": "c35561280f64eab656cee076848359526bc9fd364b429ad31fecd82ececc9fb3",
+          "sha256": "d1d84463da85bc5a419f8755e502fcfa8a26c60e2dddd6c737e7b2c9d9884a93",
+          "graphSha256": "d1d84463da85bc5a419f8755e502fcfa8a26c60e2dddd6c737e7b2c9d9884a93",
           "upstream": [
             {
               "path": "mdast-util-to-string@4.0.0/lib/index.js",
diff -ruN a/src/browser/decode-named.lil b/src/browser/decode-named.lil
--- a/src/browser/decode-named.lil	1970-01-01 00:00:00.000000000 +0000
+++ b/src/browser/decode-named.lil	2026-09-23 20:40:45.364081454 +0000
@@ -0,0 +1,25 @@
+// The browser build's decoder (the package's `browser` condition), as
+// decode-named-character-reference@1.3.0's `index.dom.js`: the document's
+// own HTML parser decodes a named reference, so the 2,125-entry table stays
+// out of the bundle. Node and the development build keep the table.
+extern JsValue document;
+
+JsValue element = JS.invoke(document, "createElement", "i");
+
+export JsValue decodeNamedCharacterReference(string value) {
+  string characterReference = "&" + value + ";";
+  JS.set(element, "innerHTML", characterReference);
+  JsValue character = JS.get(element, "textContent");
+  // A reference that does not need its semicolon (`&not`) matches a prefix
+  // of a longer one (`&notit;` reads `¬it;`): a trailing `;` after decoding,
+  // unless the reference is `&semi;` itself, means the match was partial.
+  int last = JS.number(JS.invoke(character, "charCodeAt", JS.number(JS.get(character, "length")) - 1)).toInt();
+  if (last == 59 && value != "semi") {
+    return false;
+  }
+  // Unchanged text was not a reference.
+  if (JS.strictEqual(character, characterReference)) {
+    return false;
+  }
+  return character;
+}
diff -ruN a/src/devlop.lil b/src/devlop.lil
--- a/src/devlop.lil	2026-09-23 20:40:45.015690419 +0000
+++ b/src/devlop.lil	2026-09-23 20:40:45.372453060 +0000
@@ -1,34 +1,18 @@
-import { Error, isErrorInstance } from "./host.lil";
+// devlop@1.1.0: the development build (`development` true) throws an `AssertionError`-shaped `Error`;
+// production does nothing. Every caller passes a message.
+import { Error, Object } from "./host.lil";
 
 extern JsValue development;
 
-void assertion(JsValue message) {
-  if (!development.truthy()) {
-    return;
+export void unreachable(string message) {
+  if (development.truthy()) {
+    throw JS.invoke(Object, "assign", JS.construct(Error, message), object {
+      name: "Assertion",
+      code: "ERR_ASSERTION",
+      actual: false,
+      expected: true,
+      generated: false,
+      operator: "ok"
+    });
   }
-  string text = "Unreachable";
-  bool generated = true;
-  if (message.truthy()) {
-    if (isErrorInstance(message)) {
-      throw message;
-    }
-    text = JS.string(message);
-    generated = false;
-  }
-  JsValue error = JS.construct(Error, text);
-  error["name"] = "Assertion";
-  error["code"] = "ERR_ASSERTION";
-  error["actual"] = false;
-  error["expected"] = true;
-  error["generated"] = generated;
-  error["operator"] = "ok";
-  throw error;
-}
-
-export void ok(JsValue value, JsValue message) {
-  if (!value.truthy()) assertion(message);
-}
-
-export void unreachable(JsValue message) {
-  assertion(message);
 }
diff -ruN a/src/graph/remark-parse/entry.lil b/src/graph/remark-parse/entry.lil
--- a/src/graph/remark-parse/entry.lil	2026-09-23 20:40:45.125691387 +0000
+++ b/src/graph/remark-parse/entry.lil	2026-09-23 20:40:45.258155774 +0000
@@ -1,4 +1,4 @@
-import { fromMarkdownImpl as fromMarkdown } from "./from-markdown.lil";
+import { fromMarkdown } from "./from-markdown.lil";
 import { call1, objectAssign, undef } from "./micromark/host.lil";
 
 JsValue readData(JsValue self, string key) {
diff -ruN a/src/graph/remark-parse/from-markdown.lil b/src/graph/remark-parse/from-markdown.lil
--- a/src/graph/remark-parse/from-markdown.lil	2026-09-23 20:40:45.135691474 +0000
+++ b/src/graph/remark-parse/from-markdown.lil	2026-09-23 20:40:45.258772257 +0000
@@ -1,41 +1,23 @@
-import {
-  arrLen,
-  ErrorRef,
-  eventContext,
-  eventKind,
-  eventToken,
-  fail,
-  hasOwn,
-  includesStr,
-  isNullish,
-  jsInt,
-  jsString,
-  objGet,
-  objSet,
-  objectAssign,
-  parseIntBase,
-  pop,
-  push,
-  replaceStr,
-  tokenType,
-  undef
-} from "./micromark/host.lil";
-import { parse as parseMarkdown } from "./micromark/parse.lil";
-import { postprocess as postprocessEvents } from "./micromark/postprocess.lil";
-import { preprocess as preprocessMarkdown } from "./micromark/preprocess.lil";
+// mdast-util-from-markdown@2.0.x lib/index.js.
+//
+// Upstream keeps the compile state (`stack`, `tokenStack`, `data`) on a context object and every handler
+// reads it through `this`, which is a fresh `Object.assign({sliceSerialize}, context)` per event. Those
+// copies share the context's arrays and `data`, so `this.stack` is always the one `stack`: the handlers
+// here close over them instead. `this` is still what handlers pass on (`sliceSerialize`, error handlers,
+// extension handlers called through `this.config`).
+import { hasOwn } from "./micromark/host.lil";
+import { parse } from "./micromark/parse.lil";
+import { postprocess } from "./micromark/postprocess.lil";
+import { preprocess } from "./micromark/preprocess.lil";
 import { decodeNumericCharacterReference } from "./micromark/util-decode-numeric.lil";
 import { decodeString } from "./micromark/util-decode-string.lil";
 import { normalizeIdentifier } from "./micromark/util-normalize-identifier.lil";
 import { decodeNamedCharacterReference } from "./micromark/decode-named.lil";
-import { splice } from "./micromark/util-chunked.lil";
-import { cEqualsTo } from "./micromark/symbol/codes.lil";
-import { kNumericBaseDecimal, kNumericBaseHexadecimal } from "./micromark/symbol/constants.lil";
 import {
   tBlockQuote,
   tBlockQuoteMarker,
   tBlockQuotePrefix,
   tBlockQuotePrefixWhitespace,
-  tCharacterReferenceMarkerHexadecimal,
   tCharacterReferenceMarkerNumeric,
   tLineEnding,
   tLineEndingBlank,
@@ -49,905 +31,541 @@
   tListUnordered
 } from "./micromark/symbol/types.lil";
 import { stringifyPosition } from "./stringify-position.lil";
-import { mdastToString } from "./to-string.lil";
-
-Regex trimFenceEdges = new Regex("^(\\r?\\n|\\r)|(\\r?\\n|\\r)$", "g");
-Regex trimTrailingBreak = new Regex("(\\r?\\n|\\r)$", "g");
+import { toString } from "./to-string.lil";
 
-JsValue emptyPoint() {
-  return JS.object("line", 1, "column", 1, "offset", 0);
-}
+extern JsValue Error;
+extern JsValue Number;
+extern JsValue Object;
 
-JsValue point(JsValue d) {
-  return JS.object("line", objGet(d, "line"), "column", objGet(d, "column"), "offset", objGet(d, "offset"));
+extern class Tokenizer {
+  JsValue write(JsValue chunks);
 }
 
-JsValue copyPointValue(JsValue d) {
-  return objectAssign(JS.object(), d);
+extern class Parser {
+  Tokenizer document();
 }
 
-JsValue makeEvent(string kind, JsValue token, JsValue context) {
-  return JS.array(kind, token, context);
-}
+Regex fenceEdges = new Regex("^(\\r?\\n|\\r)|(\\r?\\n|\\r)$", "g");
+Regex trailingBreak = new Regex("(\\r?\\n|\\r)$", "g");
 
-JsValue positionOf(JsValue token) {
-  return JS.object("start", objGet(token, "start"), "end", objGet(token, "end"));
+JsValue point(JsValue d) {
+  return JS.object("line", d["line"], "column", d["column"], "offset", d["offset"]);
 }
 
-void throwCloseError(JsValue token) {
-  throw JS.construct(ErrorRef, "Cannot close `" + tokenType(token) + "` (" + stringifyPosition(positionOf(token)) + "): it’s not open");
+string slice(JsValue self, JsValue token) {
+  return JS.assume(JS.invoke(self, "sliceSerialize", token));
 }
 
 JsValue defaultOnError = JS.method2((JsValue _self, JsValue left, JsValue right) => {
-  if (left.truthy()) {
-    throw JS.construct(ErrorRef, "Cannot close `" + tokenType(left) + "` (" + stringifyPosition(positionOf(left)) + "): a different token (`" + tokenType(right) + "`, " + stringifyPosition(positionOf(right)) + ") is open");
-  }
-  throw JS.construct(ErrorRef, "Cannot close document, a token (`" + tokenType(right) + "`, " + stringifyPosition(positionOf(right)) + ") is still open");
-  return undef();
+  throw JS.construct(Error, if (left.truthy()) {
+    "Cannot close `" + left["type"] + "` (" + stringifyPosition(left) + "): a different token (`" + right["type"] + "`, " + stringifyPosition(right) + ") is open"
+  } else {
+    "Cannot close document, a token (`" + right["type"] + "`, " + stringifyPosition(right) + ") is still open"
+  });
+  return JS.undefined();
 });
 
-void applyExtension(JsValue combined, JsValue ext) {
-  for (string key in ext) {
-    if (!hasOwn(ext, key)) {
-      continue;
-    }
-    if (key == "canContainEols") {
-      JsValue right = objGet(ext, key);
-      if (right.truthy()) {
-        JsValue left = objGet(combined, key);
-        int i = 0;
-        int n = arrLen(right);
-        while (i < n) {
-          push(left, right[i]);
-          i = i + 1;
-        }
-      }
-    } else if (key == "transforms") {
-      JsValue right = objGet(ext, key);
-      if (right.truthy()) {
-        JsValue left = objGet(combined, key);
-        int i = 0;
-        int n = arrLen(right);
-        while (i < n) {
-          push(left, right[i]);
-          i = i + 1;
-        }
-      }
-    } else if (key == "enter" || key == "exit") {
-      JsValue right = objGet(ext, key);
-      if (right.truthy()) {
-        objectAssign(objGet(combined, key), right);
-      }
-    }
-  }
-}
-
 void configure(JsValue combined, JsValue extensions) {
-  int index = -1;
-  int n = arrLen(extensions);
-  while (true) {
-    index = index + 1;
-    if (index >= n) {
-      break;
-    }
+  for (float index = 0; index < extensions.length; index++) {
     JsValue value = extensions[index];
     if (value.isArray()) {
       configure(combined, value);
     } else {
-      applyExtension(combined, value);
+      extension(combined, value);
     }
   }
 }
 
-JsValue makeFragment() {
-  return JS.object("type", "fragment", "children", JS.array());
-}
-
-JsValue nodeWithChildren(string typ) {
-  return JS.object("type", typ, "children", JS.array(), "position", undef());
-}
-
-JsValue createBlockQuote = JS.method1((JsValue _self, JsValue _token) => {
-  return nodeWithChildren("blockquote");
-});
-
-JsValue createCodeFlow = JS.method1((JsValue _self, JsValue _token) => {
-  JsValue node = JS.object("type", "code", "lang", null, "meta", null, "value", "");
-  objSet(node, "position", undef());
-  return node;
-});
-
-JsValue createCodeText = JS.method1((JsValue _self, JsValue _token) => {
-  return JS.object("type", "inlineCode", "value", "", "position", undef());
-});
-
-JsValue createDefinition = JS.method1((JsValue _self, JsValue _token) => {
-  JsValue node = JS.object("type", "definition", "identifier", "", "label", null, "title", null);
-  objSet(node, "url", "");
-  objSet(node, "position", undef());
-  return node;
-});
-
-JsValue createEmphasis = JS.method1((JsValue _self, JsValue _token) => {
-  return nodeWithChildren("emphasis");
-});
-
-JsValue createHeading = JS.method1((JsValue _self, JsValue _token) => {
-  return JS.object("type", "heading", "depth", 0, "children", JS.array(), "position", undef());
-});
-
-JsValue createHardBreak = JS.method1((JsValue _self, JsValue _token) => {
-  return JS.object("type", "break", "position", undef());
-});
-
-JsValue createHtml = JS.method1((JsValue _self, JsValue _token) => {
-  return JS.object("type", "html", "value", "", "position", undef());
-});
-
-JsValue createImage = JS.method1((JsValue _self, JsValue _token) => {
-  JsValue node = JS.object("type", "image", "title", null, "url", "", "alt", null);
-  objSet(node, "position", undef());
-  return node;
-});
-
-JsValue createLink = JS.method1((JsValue _self, JsValue _token) => {
-  JsValue node = JS.object("type", "link", "title", null, "url", "", "children", JS.array());
-  objSet(node, "position", undef());
-  return node;
-});
-
-JsValue createList = JS.method1((JsValue _self, JsValue token) => {
-  JsValue node = JS.object("type", "list", "ordered", tokenType(token) == tListOrdered, "start", null, "spread", objGet(token, "_spread"));
-  objSet(node, "children", JS.array());
-  objSet(node, "position", undef());
-  return node;
-});
-
-JsValue createListItem = JS.method1((JsValue _self, JsValue token) => {
-  JsValue node = JS.object("type", "listItem", "spread", objGet(token, "_spread"), "checked", null, "children", JS.array());
-  objSet(node, "position", undef());
-  return node;
-});
-
-JsValue createParagraph = JS.method1((JsValue _self, JsValue _token) => {
-  return nodeWithChildren("paragraph");
-});
-
-JsValue createStrong = JS.method1((JsValue _self, JsValue _token) => {
-  return nodeWithChildren("strong");
-});
-
-JsValue createText() {
-  return JS.object("type", "text", "value", "", "position", undef());
-}
-
-JsValue createThematicBreak = JS.method1((JsValue _self, JsValue _token) => {
-  return JS.object("type", "thematicBreak", "position", undef());
-});
-
-JsValue opener(JsValue enterFn, JsValue create, JsValue and) {
-  return JS.method1((JsValue self, JsValue token) => {
-    JS.call(enterFn, self, JS.call(create, undef(), token), token);
-    if (and.truthy()) {
-      JS.call(and, self, token);
-    }
-    return undef();
-  });
-}
-
-JsValue closer(JsValue exitFn, JsValue and) {
-  return JS.method1((JsValue self, JsValue token) => {
-    if (and.truthy()) {
-      JS.call(and, self, token);
+void extension(JsValue combined, JsValue ext) {
+  for (string key in ext) {
+    if (hasOwn(ext, key)) {
+      if (key == "canContainEols" || key == "transforms") {
+        JsValue right = ext[key];
+        if (right.truthy()) {
+          JsValue left = combined[key];
+          JsValue[] items = JS.assume(right);
+          JS.apply(left["push"], left, [...items]);
+        }
+      } else if (key == "enter" || key == "exit") {
+        JsValue right = ext[key];
+        if (right.truthy()) {
+          JS.invoke(Object, "assign", combined[key], right);
+        }
+      }
     }
-    JS.call(exitFn, self, token);
-    return undef();
-  });
-}
-
-JsValue sliceOf(JsValue self, JsValue token) {
-  return JS.call(objGet(self, "sliceSerialize"), self, token);
-}
-
-string sliceString(JsValue self, JsValue token) {
-  return jsString(sliceOf(self, token));
-}
-
-JsValue stackTop(JsValue self) {
-  JsValue stack = objGet(self, "stack");
-  return stack[arrLen(stack) - 1];
-}
-
-JsValue stackAt(JsValue self, int fromEnd) {
-  JsValue stack = objGet(self, "stack");
-  return stack[arrLen(stack) - fromEnd];
-}
-
-JsValue dataOf(JsValue self) {
-  return objGet(self, "data");
-}
-
-void finishReferenceLike(JsValue self) {
-  JsValue node = stackTop(self);
-  if (objGet(dataOf(self), "inReference").truthy()) {
-    JsValue referenceType = objGet(dataOf(self), "referenceType");
-    if (!referenceType.truthy()) {
-      referenceType = "shortcut";
-    }
-    objSet(node, "type", jsString(objGet(node, "type")) + "Reference");
-    objSet(node, "referenceType", referenceType);
-    JS.delete(node, "url");
-    JS.delete(node, "title");
-  } else {
-    JS.delete(node, "identifier");
-    JS.delete(node, "label");
   }
-  objSet(dataOf(self), "referenceType", undef());
 }
 
-bool isListType(string typ) {
-  return typ == tListOrdered || typ == tListUnordered;
-}
-
-bool isListOrQuote(string typ) {
-  return isListType(typ) || typ == tBlockQuote;
-}
-
-bool isListPrefixNoise(string typ) {
-  if (typ == tLinePrefix || typ == tListItemValue || typ == tListItemMarker) {
-    return true;
-  }
-  return typ == tListItemPrefix || typ == tListItemPrefixWhitespace;
-}
-
-bool isListTailNoise(string typ) {
-  if (typ == tLinePrefix || typ == tBlockQuotePrefix || typ == tBlockQuotePrefixWhitespace) {
-    return true;
-  }
-  return typ == tBlockQuoteMarker || typ == tListItemIndent;
-}
-
-int prepareList(JsValue events, int start, int length) {
-  int index = start - 1;
-  int containerBalance = -1;
+float prepareList(JsValue events, float start, float length) {
+  float index = start - 1;
+  float containerBalance = -1;
   bool listSpread = false;
-  JsValue listItem = undef();
-  int lineIndex = 0;
-  int firstBlankLineIndex = 0;
+  JsValue listItem = JS.undefined();
+  float lineIndex = 0;
+  float firstBlankLineIndex = 0;
   bool atMarker = false;
 
-  while (true) {
-    index = index + 1;
-    if (index > length) {
-      break;
-    }
-    JsValue ev = events[index];
-    string typ = tokenType(eventToken(ev));
-    string kind = eventKind(ev);
-
-    if (isListOrQuote(typ)) {
-      if (kind == "enter") {
-        containerBalance = containerBalance + 1;
+  while (index < length) {
+    index++;
+    JsValue event = events[index];
+    JsValue type = event[1]["type"];
+    bool enter = event[0] == "enter";
+    bool list = type == tListUnordered || type == tListOrdered;
+    if (list || type == tBlockQuote) {
+      if (enter) {
+        containerBalance++;
       } else {
-        containerBalance = containerBalance - 1;
+        containerBalance--;
       }
       atMarker = false;
-    } else if (typ == tLineEndingBlank) {
-      if (kind == "enter") {
+    } else if (type == tLineEndingBlank) {
+      if (enter) {
         if (listItem.truthy() && !atMarker && containerBalance == 0 && firstBlankLineIndex == 0) {
           firstBlankLineIndex = index;
         }
         atMarker = false;
       }
-    } else if (!isListPrefixNoise(typ)) {
+    } else if (type != tLinePrefix && type != tListItemValue && type != tListItemMarker && type != tListItemPrefix && type != tListItemPrefixWhitespace) {
       atMarker = false;
     }
 
-    bool atItemPrefix = containerBalance == 0 && kind == "enter" && typ == tListItemPrefix;
-    bool atListClose = containerBalance == -1 && kind == "exit" && isListType(typ);
-    if (atItemPrefix || atListClose) {
+    if (containerBalance == 0 && enter && type == tListItemPrefix || containerBalance == -1 && event[0] == "exit" && list) {
       if (listItem.truthy()) {
-        int tailIndex = index;
+        float tailIndex = index;
         lineIndex = 0;
-        while (true) {
-          tailIndex = tailIndex - 1;
-          if (tailIndex < 0) {
-            break;
-          }
+        while (tailIndex > 0) {
+          tailIndex--;
           JsValue tailEvent = events[tailIndex];
-          string tailType = tokenType(eventToken(tailEvent));
+          JsValue tailType = tailEvent[1]["type"];
           if (tailType == tLineEnding || tailType == tLineEndingBlank) {
-            if (eventKind(tailEvent) == "exit") {
+            if (tailEvent[0] == "exit") {
               continue;
             }
             if (lineIndex != 0) {
-              objSet(eventToken(events[lineIndex]), "type", tLineEndingBlank);
+              events[lineIndex][1]["type"] = tLineEndingBlank;
               listSpread = true;
             }
-            objSet(eventToken(tailEvent), "type", tLineEnding);
+            tailEvent[1]["type"] = tLineEnding;
             lineIndex = tailIndex;
-          } else if (isListTailNoise(tailType)) {
-          } else {
+          } else if (tailType != tLinePrefix && tailType != tBlockQuotePrefix && tailType != tBlockQuotePrefixWhitespace && tailType != tBlockQuoteMarker && tailType != tListItemIndent) {
             break;
           }
         }
-
-        if (firstBlankLineIndex != 0) {
-          bool beforeSlurp = true;
-          if (lineIndex != 0) {
-            if (firstBlankLineIndex >= lineIndex) {
-              beforeSlurp = false;
-            }
-          }
-          if (beforeSlurp) {
-            objSet(listItem, "_spread", true);
-          }
+        if (firstBlankLineIndex != 0 && (lineIndex == 0 || firstBlankLineIndex < lineIndex)) {
+          listItem["_spread"] = true;
         }
-
-        JsValue endPoint = objGet(eventToken(ev), "end");
-        int insertAt = index;
-        if (lineIndex != 0) {
-          endPoint = objGet(eventToken(events[lineIndex]), "start");
-          insertAt = lineIndex;
-        }
-        objSet(listItem, "end", copyPointValue(endPoint));
-        JsValue[] exitEvent = [makeEvent("exit", listItem, eventContext(ev))];
-        splice(events, insertAt, 0, exitEvent);
-        index = index + 1;
-        length = length + 1;
-      }
-
-      if (typ == tListItemPrefix) {
-        JsValue item = JS.object();
-        objSet(item, "type", "listItem");
-        objSet(item, "_spread", false);
-        objSet(item, "start", copyPointValue(objGet(eventToken(ev), "start")));
-        objSet(item, "end", undef());
-        listItem = item;
-        JsValue[] enterEvent = [makeEvent("enter", item, eventContext(ev))];
-        splice(events, index, 0, enterEvent);
-        index = index + 1;
-        length = length + 1;
+        listItem["end"] = JS.invoke(Object, "assign", JS.object(), if (lineIndex != 0) { events[lineIndex][1]["start"] } else { event[1]["end"] });
+        JS.invoke(events, "splice", if (lineIndex != 0) { lineIndex } else { index }, 0, JS.array("exit", listItem, event[2]));
+        index++;
+        length++;
+      }
+      if (type == tListItemPrefix) {
+        listItem = JS.object("type", "listItem", "_spread", false, "start", JS.invoke(Object, "assign", JS.object(), event[1]["start"]), "end", JS.undefined());
+        JS.invoke(events, "splice", index, 0, JS.array("enter", listItem, event[2]));
+        index++;
+        length++;
         firstBlankLineIndex = 0;
         atMarker = true;
       }
     }
   }
-
-  objSet(eventToken(events[start]), "_spread", listSpread);
+  events[start][1]["_spread"] = listSpread;
   return length;
 }
 
-JsValue compiler(JsValue options) {
-  JsValue eols = JS.array();
-  push(eols, "emphasis");
-  push(eols, "fragment");
-  push(eols, "heading");
-  push(eols, "paragraph");
-  push(eols, "strong");
-  JsValue transforms = JS.array();
-  JsValue enterMap = JS.object();
-  JsValue exitMap = JS.object();
-  JsValue config = JS.object("transforms", transforms, "canContainEols", eols, "enter", enterMap, "exit", exitMap);
-
-  JsValue enterFn = JS.undefined();
-  JsValue exitFn = JS.undefined();
-  JsValue bufferFn = JS.undefined();
-  JsValue resumeFn = JS.undefined();
-  JsValue onenterdata = JS.undefined();
-  JsValue onexitdata = JS.undefined();
-
-  bufferFn = JS.method0((JsValue self) => {
-    push(objGet(self, "stack"), makeFragment());
-    return undef();
-  });
-
-  resumeFn = JS.method0((JsValue self) => {
-    return mdastToString(pop(objGet(self, "stack")), undef());
-  });
-
-  enterFn = JS.method3((JsValue self, JsValue node, JsValue token, JsValue errorHandler) => {
-    JsValue parent = stackTop(self);
-    JsValue siblings = objGet(parent, "children");
-    push(siblings, node);
-    push(objGet(self, "stack"), node);
-    JsValue pair = JS.array();
-    push(pair, token);
-    if (errorHandler.truthy()) {
-      push(pair, errorHandler);
-    } else {
-      push(pair, undef());
-    }
-    push(objGet(self, "tokenStack"), pair);
-    objSet(node, "position", JS.object("start", point(objGet(token, "start")), "end", undef()));
-    return undef();
+export JsValue fromMarkdown(JsValue value, JsValue encoding, JsValue options) {
+  if (encoding.truthy() && JS.typeOf(encoding) == "object") {
+    options = encoding;
+    encoding = JS.undefined();
+  }
+
+  JsValue tree = JS.object("type", "root", "children", JS.array());
+  JsValue[] stack = [tree];
+  JsValue[] tokenStack = [];
+  JsValue data = JS.object();
+  // Assigned below; `onexitlineending` reads `config.canContainEols` live, as upstream does.
+  JsValue config = JS.undefined();
+
+  func()->JsValue top = () => stack[stack.length - 1];
+
+  JsValue buffer = JS.method0((JsValue _self) => {
+    stack.push(JS.object("type", "fragment", "children", JS.array()));
+    return JS.undefined();
+  });
+
+  JsValue resume = JS.method0((JsValue _self) => toString(stack.pop()));
+
+  JsValue enter = JS.method3((JsValue _self, JsValue node, JsValue token, JsValue errorHandler) => {
+    JS.invoke(top()["children"], "push", node);
+    stack.push(node);
+    tokenStack.push(JS.array(token, JS.or(errorHandler, JS.undefined())));
+    node["position"] = JS.object("start", point(token["start"]), "end", JS.undefined());
+    return JS.undefined();
   });
 
-  exitFn = JS.method2((JsValue self, JsValue token, JsValue onExitError) => {
-    JsValue node = pop(objGet(self, "stack"));
-    JsValue open = pop(objGet(self, "tokenStack"));
-    if (isNullish(open)) {
-      throwCloseError(token);
-    } else if (tokenType(open[0]) != tokenType(token)) {
+  JsValue exit = JS.method2((JsValue self, JsValue token, JsValue onExitError) => {
+    JsValue node = stack.pop();
+    JsValue open = tokenStack.pop();
+    if (!open.truthy()) {
+      throw JS.construct(Error, "Cannot close `" + token["type"] + "` (" + stringifyPosition(token) + "): it’s not open");
+    }
+    if (open[0]["type"] != token["type"]) {
       if (onExitError.truthy()) {
         JS.call(onExitError, self, token, open[0]);
       } else {
-        JsValue handler = open[1];
-        if (!handler.truthy()) {
-          handler = defaultOnError;
-        }
-        JS.call(handler, self, token, open[0]);
+        JS.call(JS.or(open[1], defaultOnError), self, token, open[0]);
       }
     }
-    objSet(objGet(node, "position"), "end", point(objGet(token, "end")));
-    return undef();
+    node["position"]["end"] = point(token["end"]);
+    return JS.undefined();
   });
 
-  JsValue onenterlistordered = JS.method1((JsValue self, JsValue _token) => {
-    objSet(dataOf(self), "expectingFirstListItemValue", true);
-    return undef();
+  func(func(JsValue)->JsValue, JsValue)->JsValue opener = (func(JsValue)->JsValue create, JsValue and) => JS.method1((JsValue _self, JsValue token) => {
+    JS.call(enter, JS.undefined(), create(token), token);
+    if (and.truthy()) {
+      JS.call(and, JS.undefined(), token);
+    }
+    return JS.undefined();
   });
 
-  JsValue onenterlistitemvalue = JS.method1((JsValue self, JsValue token) => {
-    if (objGet(dataOf(self), "expectingFirstListItemValue").truthy()) {
-      JsValue ancestor = stackAt(self, 2);
-      objSet(ancestor, "start", parseIntBase(sliceString(self, token), kNumericBaseDecimal));
-      objSet(dataOf(self), "expectingFirstListItemValue", undef());
+  func(JsValue)->JsValue closer = (JsValue and) => JS.method1((JsValue self, JsValue token) => {
+    if (and.truthy()) {
+      JS.call(and, JS.undefined(), token);
     }
-    return undef();
+    JS.call(exit, self, token);
+    return JS.undefined();
   });
 
-  JsValue onexitcodefencedfenceinfo = JS.method1((JsValue self, JsValue _token) => {
-    JsValue data = JS.call(resumeFn, self);
-    objSet(stackTop(self), "lang", data);
-    return undef();
+  // The handlers that only move the text of the node on top of the stack into a field of its parent.
+  func(string)->JsValue resumeInto = (string field) => JS.method0((JsValue _self) => {
+    JsValue value = JS.call(resume, JS.undefined());
+    top()[field] = value;
+    return JS.undefined();
   });
 
-  JsValue onexitcodefencedfencemeta = JS.method1((JsValue self, JsValue _token) => {
-    JsValue data = JS.call(resumeFn, self);
-    objSet(stackTop(self), "meta", data);
-    return undef();
+  JsValue onenterlistordered = JS.method0((JsValue _self) => {
+    data["expectingFirstListItemValue"] = true;
+    return JS.undefined();
   });
 
-  JsValue onexitcodefencedfence = JS.method1((JsValue self, JsValue _token) => {
-    if (objGet(dataOf(self), "flowCodeInside").truthy()) {
-      return undef();
+  JsValue onexitcodefencedfence = JS.method0((JsValue _self) => {
+    if (!data["flowCodeInside"].truthy()) {
+      JS.call(buffer, JS.undefined());
+      data["flowCodeInside"] = true;
     }
-    JS.call(bufferFn, self);
-    objSet(dataOf(self), "flowCodeInside", true);
-    return undef();
+    return JS.undefined();
   });
 
-  JsValue onexitcodefenced = JS.method1((JsValue self, JsValue _token) => {
-    string data = jsString(JS.call(resumeFn, self));
-    objSet(stackTop(self), "value", replaceStr(data, trimFenceEdges, ""));
-    objSet(dataOf(self), "flowCodeInside", undef());
-    return undef();
+  JsValue onexitcodefenced = JS.method0((JsValue _self) => {
+    JsValue value = JS.call(resume, JS.undefined());
+    top()["value"] = JS.invoke(value, "replace", fenceEdges, "");
+    data["flowCodeInside"] = JS.undefined();
+    return JS.undefined();
   });
 
-  JsValue onexitcodeindented = JS.method1((JsValue self, JsValue _token) => {
-    string data = jsString(JS.call(resumeFn, self));
-    objSet(stackTop(self), "value", replaceStr(data, trimTrailingBreak, ""));
-    return undef();
+  JsValue onexitcodeindented = JS.method0((JsValue _self) => {
+    JsValue value = JS.call(resume, JS.undefined());
+    top()["value"] = JS.invoke(value, "replace", trailingBreak, "");
+    return JS.undefined();
   });
 
   JsValue onexitdefinitionlabelstring = JS.method1((JsValue self, JsValue token) => {
-    JsValue label = JS.call(resumeFn, self);
-    JsValue node = stackTop(self);
-    objSet(node, "label", label);
-    objSet(node, "identifier", normalizeIdentifier(sliceString(self, token)).toLowerCase());
-    return undef();
-  });
-
-  JsValue onexitdefinitiontitlestring = JS.method1((JsValue self, JsValue _token) => {
-    JsValue data = JS.call(resumeFn, self);
-    objSet(stackTop(self), "title", data);
-    return undef();
-  });
-
-  JsValue onexitdefinitiondestinationstring = JS.method1((JsValue self, JsValue _token) => {
-    JsValue data = JS.call(resumeFn, self);
-    objSet(stackTop(self), "url", data);
-    return undef();
+    JsValue label = JS.call(resume, JS.undefined());
+    JsValue node = top();
+    node["label"] = label;
+    node["identifier"] = normalizeIdentifier(slice(self, token)).toLowerCase();
+    return JS.undefined();
   });
 
-  JsValue onexitatxheadingsequence = JS.method1((JsValue self, JsValue token) => {
-    JsValue node = stackTop(self);
-    if (!objGet(node, "depth").truthy()) {
-      objSet(node, "depth", sliceString(self, token).length);
-    }
-    return undef();
+  JsValue onexitsetextheadingtext = JS.method0((JsValue _self) => {
+    data["setextHeadingSlurpLineEnding"] = true;
+    return JS.undefined();
   });
 
-  JsValue onexitsetextheadingtext = JS.method1((JsValue self, JsValue _token) => {
-    objSet(dataOf(self), "setextHeadingSlurpLineEnding", true);
-    return undef();
+  JsValue onexitsetextheading = JS.method0((JsValue _self) => {
+    data["setextHeadingSlurpLineEnding"] = JS.undefined();
+    return JS.undefined();
   });
 
-  JsValue onexitsetextheadinglinesequence = JS.method1((JsValue self, JsValue token) => {
-    string seq = sliceString(self, token);
-    int depth = 2;
-    if (seq.length > 0) {
-      JsValue cp = JS.call(JS.get(seq, "codePointAt"), seq, 0);
-      if (jsInt(cp) == cEqualsTo) {
-        depth = 1;
-      }
+  JsValue onenterdata = JS.method1((JsValue _self, JsValue token) => {
+    JsValue siblings = top()["children"];
+    JsValue tail = siblings[siblings.length - 1];
+    if (!tail.truthy() || tail["type"] != "text") {
+      tail = JS.object("type", "text", "value", "", "position", JS.object("start", point(token["start"]), "end", JS.undefined()));
+      JS.invoke(siblings, "push", tail);
     }
-    objSet(stackTop(self), "depth", depth);
-    return undef();
-  });
-
-  JsValue onexitsetextheading = JS.method1((JsValue self, JsValue _token) => {
-    objSet(dataOf(self), "setextHeadingSlurpLineEnding", undef());
-    return undef();
+    stack.push(tail);
+    return JS.undefined();
   });
 
-  onenterdata = JS.method1((JsValue self, JsValue token) => {
-    JsValue node = stackTop(self);
-    JsValue siblings = objGet(node, "children");
-    JsValue tail = undef();
-    if (arrLen(siblings) > 0) {
-      tail = siblings[arrLen(siblings) - 1];
-    }
-    if (isNullish(tail) || jsString(objGet(tail, "type")) != "text") {
-      tail = createText();
-      objSet(tail, "position", JS.object("start", point(objGet(token, "start")), "end", undef()));
-      push(siblings, tail);
-    }
-    push(objGet(self, "stack"), tail);
-    return undef();
+  JsValue onexitdata = JS.method1((JsValue self, JsValue token) => {
+    JsValue tail = stack.pop();
+    tail["value"] = JS.add(tail["value"], slice(self, token));
+    tail["position"]["end"] = point(token["end"]);
+    return JS.undefined();
   });
 
-  onexitdata = JS.method1((JsValue self, JsValue token) => {
-    JsValue tail = pop(objGet(self, "stack"));
-    objSet(tail, "value", jsString(objGet(tail, "value")) + sliceString(self, token));
-    objSet(objGet(tail, "position"), "end", point(objGet(token, "end")));
-    return undef();
+  JsValue onexithardbreak = JS.method0((JsValue _self) => {
+    data["atHardBreak"] = true;
+    return JS.undefined();
   });
 
-  JsValue onexitlineending = JS.method1((JsValue self, JsValue token) => {
-    JsValue context = stackTop(self);
-    if (objGet(dataOf(self), "atHardBreak").truthy()) {
-      JsValue kids = objGet(context, "children");
-      JsValue tail = kids[arrLen(kids) - 1];
-      objSet(objGet(tail, "position"), "end", point(objGet(token, "end")));
-      objSet(dataOf(self), "atHardBreak", undef());
-      return undef();
-    }
-    if (!objGet(dataOf(self), "setextHeadingSlurpLineEnding").truthy() && includesStr(objGet(config, "canContainEols"), jsString(objGet(context, "type")))) {
-      JS.call(onenterdata, self, token);
-      JS.call(onexitdata, self, token);
+  // onexitlink and onexitimage: the same body upstream.
+  JsValue onexitmedia = JS.method0((JsValue _self) => {
+    JsValue node = top();
+    if (data["inReference"].truthy()) {
+      JsValue referenceType = JS.or(data["referenceType"], "shortcut");
+      node["type"] = JS.add(node["type"], "Reference");
+      node["referenceType"] = referenceType;
+      JS.delete(node, "url");
+      JS.delete(node, "title");
+    } else {
+      JS.delete(node, "identifier");
+      JS.delete(node, "label");
     }
-    return undef();
-  });
-
-  JsValue onexithardbreak = JS.method1((JsValue self, JsValue _token) => {
-    objSet(dataOf(self), "atHardBreak", true);
-    return undef();
-  });
-
-  JsValue onexithtmlflow = JS.method1((JsValue self, JsValue _token) => {
-    JsValue data = JS.call(resumeFn, self);
-    objSet(stackTop(self), "value", data);
-    return undef();
-  });
-
-  JsValue onexithtmltext = JS.method1((JsValue self, JsValue _token) => {
-    JsValue data = JS.call(resumeFn, self);
-    objSet(stackTop(self), "value", data);
-    return undef();
-  });
-
-  JsValue onexitcodetext = JS.method1((JsValue self, JsValue _token) => {
-    JsValue data = JS.call(resumeFn, self);
-    objSet(stackTop(self), "value", data);
-    return undef();
-  });
-
-  JsValue onexitlink = JS.method1((JsValue self, JsValue _token) => {
-    finishReferenceLike(self);
-    return undef();
-  });
-
-  JsValue onexitimage = JS.method1((JsValue self, JsValue _token) => {
-    finishReferenceLike(self);
-    return undef();
-  });
-
-  JsValue onexitlabeltext = JS.method1((JsValue self, JsValue token) => {
-    string serialized = sliceString(self, token);
-    JsValue ancestor = stackAt(self, 2);
-    objSet(ancestor, "label", decodeString(serialized));
-    objSet(ancestor, "identifier", normalizeIdentifier(serialized).toLowerCase());
-    return undef();
+    data["referenceType"] = JS.undefined();
+    return JS.undefined();
   });
 
-  JsValue onexitlabel = JS.method1((JsValue self, JsValue _token) => {
-    JsValue fragment = stackTop(self);
-    JsValue value = JS.call(resumeFn, self);
-    JsValue node = stackTop(self);
-    objSet(dataOf(self), "inReference", true);
-    if (jsString(objGet(node, "type")) == "link") {
-      objSet(node, "children", objGet(fragment, "children"));
+  JsValue onexitlabel = JS.method0((JsValue _self) => {
+    JsValue fragment = top();
+    JsValue value = JS.call(resume, JS.undefined());
+    JsValue node = top();
+    data["inReference"] = true;
+    if (node["type"] == "link") {
+      node["children"] = fragment["children"];
     } else {
-      objSet(node, "alt", value);
+      node["alt"] = value;
     }
-    return undef();
+    return JS.undefined();
   });
 
-  JsValue onexitresourcedestinationstring = JS.method1((JsValue self, JsValue _token) => {
-    JsValue data = JS.call(resumeFn, self);
-    objSet(stackTop(self), "url", data);
-    return undef();
+  JsValue onexitresource = JS.method0((JsValue _self) => {
+    data["inReference"] = JS.undefined();
+    return JS.undefined();
   });
 
-  JsValue onexitresourcetitlestring = JS.method1((JsValue self, JsValue _token) => {
-    JsValue data = JS.call(resumeFn, self);
-    objSet(stackTop(self), "title", data);
-    return undef();
-  });
-
-  JsValue onexitresource = JS.method1((JsValue self, JsValue _token) => {
-    objSet(dataOf(self), "inReference", undef());
-    return undef();
-  });
-
-  JsValue onenterreference = JS.method1((JsValue self, JsValue _token) => {
-    objSet(dataOf(self), "referenceType", "collapsed");
-    return undef();
+  JsValue onenterreference = JS.method0((JsValue _self) => {
+    data["referenceType"] = "collapsed";
+    return JS.undefined();
   });
 
   JsValue onexitreferencestring = JS.method1((JsValue self, JsValue token) => {
-    JsValue label = JS.call(resumeFn, self);
-    JsValue node = stackTop(self);
-    objSet(node, "label", label);
-    objSet(node, "identifier", normalizeIdentifier(sliceString(self, token)).toLowerCase());
-    objSet(dataOf(self), "referenceType", "full");
-    return undef();
-  });
-
-  JsValue onexitcharacterreferencemarker = JS.method1((JsValue self, JsValue token) => {
-    objSet(dataOf(self), "characterReferenceType", objGet(token, "type"));
-    return undef();
-  });
-
-  JsValue onexitcharacterreferencevalue = JS.method1((JsValue self, JsValue token) => {
-    string data = sliceString(self, token);
-    JsValue typ = objGet(dataOf(self), "characterReferenceType");
-    string value = "";
-    if (typ.truthy()) {
-      int base = kNumericBaseHexadecimal;
-      if (jsString(typ) == tCharacterReferenceMarkerNumeric) {
-        base = kNumericBaseDecimal;
-      }
-      value = decodeNumericCharacterReference(data, base);
-      objSet(dataOf(self), "characterReferenceType", undef());
-    } else {
-      JsValue result = decodeNamedCharacterReference(data);
-      if (JS.strictEqual(result, false)) {
-        fail("expected reference to decode");
-      }
-      value = jsString(result);
-    }
-    JsValue tail = stackTop(self);
-    objSet(tail, "value", jsString(objGet(tail, "value")) + value);
-    return undef();
+    JS.call(onexitdefinitionlabelstring, self, token);
+    data["referenceType"] = "full";
+    return JS.undefined();
   });
 
-  JsValue onexitcharacterreference = JS.method1((JsValue self, JsValue token) => {
-    JsValue tail = pop(objGet(self, "stack"));
-    objSet(objGet(tail, "position"), "end", point(objGet(token, "end")));
-    return undef();
+  JsValue onexitcharacterreferencemarker = JS.method1((JsValue _self, JsValue token) => {
+    data["characterReferenceType"] = token["type"];
+    return JS.undefined();
   });
 
-  JsValue onexitautolinkprotocol = JS.method1((JsValue self, JsValue token) => {
-    JS.call(onexitdata, self, token);
-    objSet(stackTop(self), "url", sliceOf(self, token));
-    return undef();
+  JsValue onexitcharacterreference = JS.method1((JsValue _self, JsValue token) => {
+    stack.pop()["position"]["end"] = point(token["end"]);
+    return JS.undefined();
   });
 
-  JsValue onexitautolinkemail = JS.method1((JsValue self, JsValue token) => {
+  // onexitautolinkprotocol and onexitautolinkemail: the URL is the slice, after a `mailto:` for an email.
+  func(string)->JsValue autolinkUrl = (string prefix) => JS.method1((JsValue self, JsValue token) => {
     JS.call(onexitdata, self, token);
-    objSet(stackTop(self), "url", "mailto:" + sliceString(self, token));
-    return undef();
+    top()["url"] = prefix + slice(self, token);
+    return JS.undefined();
   });
 
-  objSet(enterMap, "autolink", opener(enterFn, createLink, undef()));
-  objSet(enterMap, "autolinkProtocol", onenterdata);
-  objSet(enterMap, "autolinkEmail", onenterdata);
-  objSet(enterMap, "atxHeading", opener(enterFn, createHeading, undef()));
-  objSet(enterMap, "blockQuote", opener(enterFn, createBlockQuote, undef()));
-  objSet(enterMap, "characterEscape", onenterdata);
-  objSet(enterMap, "characterReference", onenterdata);
-  objSet(enterMap, "codeFenced", opener(enterFn, createCodeFlow, undef()));
-  objSet(enterMap, "codeFencedFenceInfo", bufferFn);
-  objSet(enterMap, "codeFencedFenceMeta", bufferFn);
-  objSet(enterMap, "codeIndented", opener(enterFn, createCodeFlow, bufferFn));
-  objSet(enterMap, "codeText", opener(enterFn, createCodeText, bufferFn));
-  objSet(enterMap, "codeTextData", onenterdata);
-  objSet(enterMap, "data", onenterdata);
-  objSet(enterMap, "codeFlowValue", onenterdata);
-  objSet(enterMap, "definition", opener(enterFn, createDefinition, undef()));
-  objSet(enterMap, "definitionDestinationString", bufferFn);
-  objSet(enterMap, "definitionLabelString", bufferFn);
-  objSet(enterMap, "definitionTitleString", bufferFn);
-  objSet(enterMap, "emphasis", opener(enterFn, createEmphasis, undef()));
-  objSet(enterMap, "hardBreakEscape", opener(enterFn, createHardBreak, undef()));
-  objSet(enterMap, "hardBreakTrailing", opener(enterFn, createHardBreak, undef()));
-  objSet(enterMap, "htmlFlow", opener(enterFn, createHtml, bufferFn));
-  objSet(enterMap, "htmlFlowData", onenterdata);
-  objSet(enterMap, "htmlText", opener(enterFn, createHtml, bufferFn));
-  objSet(enterMap, "htmlTextData", onenterdata);
-  objSet(enterMap, "image", opener(enterFn, createImage, undef()));
-  objSet(enterMap, "label", bufferFn);
-  objSet(enterMap, "link", opener(enterFn, createLink, undef()));
-  objSet(enterMap, "listItem", opener(enterFn, createListItem, undef()));
-  objSet(enterMap, "listItemValue", onenterlistitemvalue);
-  objSet(enterMap, "listOrdered", opener(enterFn, createList, onenterlistordered));
-  objSet(enterMap, "listUnordered", opener(enterFn, createList, undef()));
-  objSet(enterMap, "paragraph", opener(enterFn, createParagraph, undef()));
-  objSet(enterMap, "reference", onenterreference);
-  objSet(enterMap, "referenceString", bufferFn);
-  objSet(enterMap, "resourceDestinationString", bufferFn);
-  objSet(enterMap, "resourceTitleString", bufferFn);
-  objSet(enterMap, "setextHeading", opener(enterFn, createHeading, undef()));
-  objSet(enterMap, "strong", opener(enterFn, createStrong, undef()));
-  objSet(enterMap, "thematicBreak", opener(enterFn, createThematicBreak, undef()));
-
-  objSet(exitMap, "atxHeading", closer(exitFn, undef()));
-  objSet(exitMap, "atxHeadingSequence", onexitatxheadingsequence);
-  objSet(exitMap, "autolink", closer(exitFn, undef()));
-  objSet(exitMap, "autolinkEmail", onexitautolinkemail);
-  objSet(exitMap, "autolinkProtocol", onexitautolinkprotocol);
-  objSet(exitMap, "blockQuote", closer(exitFn, undef()));
-  objSet(exitMap, "characterEscapeValue", onexitdata);
-  objSet(exitMap, "characterReferenceMarkerHexadecimal", onexitcharacterreferencemarker);
-  objSet(exitMap, "characterReferenceMarkerNumeric", onexitcharacterreferencemarker);
-  objSet(exitMap, "characterReferenceValue", onexitcharacterreferencevalue);
-  objSet(exitMap, "characterReference", onexitcharacterreference);
-  objSet(exitMap, "codeFenced", closer(exitFn, onexitcodefenced));
-  objSet(exitMap, "codeFencedFence", onexitcodefencedfence);
-  objSet(exitMap, "codeFencedFenceInfo", onexitcodefencedfenceinfo);
-  objSet(exitMap, "codeFencedFenceMeta", onexitcodefencedfencemeta);
-  objSet(exitMap, "codeFlowValue", onexitdata);
-  objSet(exitMap, "codeIndented", closer(exitFn, onexitcodeindented));
-  objSet(exitMap, "codeText", closer(exitFn, onexitcodetext));
-  objSet(exitMap, "codeTextData", onexitdata);
-  objSet(exitMap, "data", onexitdata);
-  objSet(exitMap, "definition", closer(exitFn, undef()));
-  objSet(exitMap, "definitionDestinationString", onexitdefinitiondestinationstring);
-  objSet(exitMap, "definitionLabelString", onexitdefinitionlabelstring);
-  objSet(exitMap, "definitionTitleString", onexitdefinitiontitlestring);
-  objSet(exitMap, "emphasis", closer(exitFn, undef()));
-  objSet(exitMap, "hardBreakEscape", closer(exitFn, onexithardbreak));
-  objSet(exitMap, "hardBreakTrailing", closer(exitFn, onexithardbreak));
-  objSet(exitMap, "htmlFlow", closer(exitFn, onexithtmlflow));
-  objSet(exitMap, "htmlFlowData", onexitdata);
-  objSet(exitMap, "htmlText", closer(exitFn, onexithtmltext));
-  objSet(exitMap, "htmlTextData", onexitdata);
-  objSet(exitMap, "image", closer(exitFn, onexitimage));
-  objSet(exitMap, "label", onexitlabel);
-  objSet(exitMap, "labelText", onexitlabeltext);
-  objSet(exitMap, "lineEnding", onexitlineending);
-  objSet(exitMap, "link", closer(exitFn, onexitlink));
-  objSet(exitMap, "listItem", closer(exitFn, undef()));
-  objSet(exitMap, "listOrdered", closer(exitFn, undef()));
-  objSet(exitMap, "listUnordered", closer(exitFn, undef()));
-  objSet(exitMap, "paragraph", closer(exitFn, undef()));
-  objSet(exitMap, "referenceString", onexitreferencestring);
-  objSet(exitMap, "resourceDestinationString", onexitresourcedestinationstring);
-  objSet(exitMap, "resourceTitleString", onexitresourcetitlestring);
-  objSet(exitMap, "resource", onexitresource);
-  objSet(exitMap, "setextHeading", closer(exitFn, onexitsetextheading));
-  objSet(exitMap, "setextHeadingLineSequence", onexitsetextheadinglinesequence);
-  objSet(exitMap, "setextHeadingText", onexitsetextheadingtext);
-  objSet(exitMap, "strong", closer(exitFn, undef()));
-  objSet(exitMap, "thematicBreak", closer(exitFn, undef()));
-
-  JsValue settings = options;
-  if (isNullish(settings)) {
-    settings = JS.object();
-  }
-  JsValue mdastExtensions = objGet(settings, "mdastExtensions");
-  if (isNullish(mdastExtensions)) {
-    mdastExtensions = JS.array();
-  }
-  configure(config, mdastExtensions);
-
-  JsValue data = JS.object();
+  func(JsValue)->JsValue codeFlow = (JsValue _token) => JS.object("type", "code", "lang", null, "meta", null, "value", "");
+  func(JsValue)->JsValue hardBreak = (JsValue _token) => JS.object("type", "break");
+  func(JsValue)->JsValue heading = (JsValue _token) => JS.object("type", "heading", "depth", 0, "children", JS.array());
+  func(JsValue)->JsValue html = (JsValue _token) => JS.object("type", "html", "value", "");
+  func(JsValue)->JsValue link = (JsValue _token) => JS.object("type", "link", "title", null, "url", "", "children", JS.array());
+  func(JsValue)->JsValue list = (JsValue token) => JS.object("type", "list", "ordered", token["type"] == "listOrdered", "start", null, "spread", token["_spread"], "children", JS.array());
 
-  return JS.method1((JsValue _self, JsValue events) => {
-    JsValue tree = JS.object("type", "root", "children", JS.array(), "position", undef());
-    JsValue stack = JS.array();
-    push(stack, tree);
-    JsValue tokenStack = JS.array();
-    JsValue context = JS.object();
-    objSet(context, "stack", stack);
-    objSet(context, "tokenStack", tokenStack);
-    objSet(context, "config", config);
-    objSet(context, "enter", enterFn);
-    objSet(context, "exit", exitFn);
-    objSet(context, "buffer", bufferFn);
-    objSet(context, "resume", resumeFn);
-    objSet(context, "data", data);
-
-    int[] listStack = [];
-    int index = -1;
-    while (true) {
-      index = index + 1;
-      if (index >= arrLen(events)) {
-        break;
-      }
-      string typ = tokenType(eventToken(events[index]));
-      if (isListType(typ)) {
-        if (eventKind(events[index]) == "enter") {
-          listStack.push(index);
+  JsValue transforms = JS.array();
+  JsValue eols = JS.array("emphasis", "fragment", "heading", "paragraph", "strong");
+  JsValue enterMap = JS.object(
+      "autolink", opener(link, JS.undefined()),
+      "autolinkProtocol", onenterdata,
+      "autolinkEmail", onenterdata,
+      "atxHeading", opener(heading, JS.undefined()),
+      "blockQuote", opener((JsValue _token) => JS.object("type", "blockquote", "children", JS.array()), JS.undefined()),
+      "characterEscape", onenterdata,
+      "characterReference", onenterdata,
+      "codeFenced", opener(codeFlow, JS.undefined()),
+      "codeFencedFenceInfo", buffer,
+      "codeFencedFenceMeta", buffer,
+      "codeIndented", opener(codeFlow, buffer),
+      "codeText", opener((JsValue _token) => JS.object("type", "inlineCode", "value", ""), buffer),
+      "codeTextData", onenterdata,
+      "data", onenterdata,
+      "codeFlowValue", onenterdata,
+      "definition", opener((JsValue _token) => JS.object("type", "definition", "identifier", "", "label", null, "title", null, "url", ""), JS.undefined()),
+      "definitionDestinationString", buffer,
+      "definitionLabelString", buffer,
+      "definitionTitleString", buffer,
+      "emphasis", opener((JsValue _token) => JS.object("type", "emphasis", "children", JS.array()), JS.undefined()),
+      "hardBreakEscape", opener(hardBreak, JS.undefined()),
+      "hardBreakTrailing", opener(hardBreak, JS.undefined()),
+      "htmlFlow", opener(html, buffer),
+      "htmlFlowData", onenterdata,
+      "htmlText", opener(html, buffer),
+      "htmlTextData", onenterdata,
+      "image", opener((JsValue _token) => JS.object("type", "image", "title", null, "url", "", "alt", null), JS.undefined()),
+      "label", buffer,
+      "link", opener(link, JS.undefined()),
+      "listItem", opener((JsValue token) => JS.object("type", "listItem", "spread", token["_spread"], "checked", null, "children", JS.array()), JS.undefined()),
+      "listItemValue", JS.method1((JsValue self, JsValue token) => {
+        if (data["expectingFirstListItemValue"].truthy()) {
+          stack[stack.length - 2]["start"] = JS.invoke(Number, "parseInt", slice(self, token), 10);
+          data["expectingFirstListItemValue"] = JS.undefined();
+        }
+        return JS.undefined();
+      }),
+      "listOrdered", opener(list, onenterlistordered),
+      "listUnordered", opener(list, JS.undefined()),
+      "paragraph", opener((JsValue _token) => JS.object("type", "paragraph", "children", JS.array()), JS.undefined()),
+      "reference", onenterreference,
+      "referenceString", buffer,
+      "resourceDestinationString", buffer,
+      "resourceTitleString", buffer,
+      "setextHeading", opener(heading, JS.undefined()),
+      "strong", opener((JsValue _token) => JS.object("type", "strong", "children", JS.array()), JS.undefined()),
+      "thematicBreak", opener((JsValue _token) => JS.object("type", "thematicBreak"), JS.undefined())
+    );
+  JsValue exitMap = JS.object(
+      "atxHeading", closer(JS.undefined()),
+      "atxHeadingSequence", JS.method1((JsValue self, JsValue token) => {
+        JsValue node = top();
+        if (!node["depth"].truthy()) {
+          node["depth"] = slice(self, token).length;
+        }
+        return JS.undefined();
+      }),
+      "autolink", closer(JS.undefined()),
+      "autolinkEmail", autolinkUrl("mailto:"),
+      "autolinkProtocol", autolinkUrl(""),
+      "blockQuote", closer(JS.undefined()),
+      "characterEscapeValue", onexitdata,
+      "characterReferenceMarkerHexadecimal", onexitcharacterreferencemarker,
+      "characterReferenceMarkerNumeric", onexitcharacterreferencemarker,
+      "characterReferenceValue", JS.method1((JsValue self, JsValue token) => {
+        string text = slice(self, token);
+        JsValue type = data["characterReferenceType"];
+        JsValue value = JS.undefined();
+        if (type.truthy()) {
+          value = decodeNumericCharacterReference(text, if (type == tCharacterReferenceMarkerNumeric) { 10 } else { 16 });
+          data["characterReferenceType"] = JS.undefined();
         } else {
-          int tail = listStack.pop();
-          index = prepareList(events, tail, index);
+          value = decodeNamedCharacterReference(text);
         }
+        JsValue tail = top();
+        tail["value"] = JS.add(tail["value"], value);
+        return JS.undefined();
+      }),
+      "characterReference", onexitcharacterreference,
+      "codeFenced", closer(onexitcodefenced),
+      "codeFencedFence", onexitcodefencedfence,
+      "codeFencedFenceInfo", resumeInto("lang"),
+      "codeFencedFenceMeta", resumeInto("meta"),
+      "codeFlowValue", onexitdata,
+      "codeIndented", closer(onexitcodeindented),
+      "codeText", closer(resumeInto("value")),
+      "codeTextData", onexitdata,
+      "data", onexitdata,
+      "definition", closer(JS.undefined()),
+      "definitionDestinationString", resumeInto("url"),
+      "definitionLabelString", onexitdefinitionlabelstring,
+      "definitionTitleString", resumeInto("title"),
+      "emphasis", closer(JS.undefined()),
+      "hardBreakEscape", closer(onexithardbreak),
+      "hardBreakTrailing", closer(onexithardbreak),
+      "htmlFlow", closer(resumeInto("value")),
+      "htmlFlowData", onexitdata,
+      "htmlText", closer(resumeInto("value")),
+      "htmlTextData", onexitdata,
+      "image", closer(onexitmedia),
+      "label", onexitlabel,
+      "labelText", JS.method1((JsValue self, JsValue token) => {
+        string text = slice(self, token);
+        JsValue ancestor = stack[stack.length - 2];
+        ancestor["label"] = decodeString(text);
+        ancestor["identifier"] = normalizeIdentifier(text).toLowerCase();
+        return JS.undefined();
+      }),
+      "lineEnding", JS.method1((JsValue self, JsValue token) => {
+        JsValue context = top();
+        if (data["atHardBreak"].truthy()) {
+          JsValue children = context["children"];
+          children[children.length - 1]["position"]["end"] = point(token["end"]);
+          data["atHardBreak"] = JS.undefined();
+          return JS.undefined();
+        }
+        if (!data["setextHeadingSlurpLineEnding"].truthy() && JS.invoke(config["canContainEols"], "includes", context["type"]).truthy()) {
+          JS.call(onenterdata, JS.undefined(), token);
+          JS.call(onexitdata, self, token);
+        }
+        return JS.undefined();
+      }),
+      "link", closer(onexitmedia),
+      "listItem", closer(JS.undefined()),
+      "listOrdered", closer(JS.undefined()),
+      "listUnordered", closer(JS.undefined()),
+      "paragraph", closer(JS.undefined()),
+      "referenceString", onexitreferencestring,
+      "resourceDestinationString", resumeInto("url"),
+      "resourceTitleString", resumeInto("title"),
+      "resource", onexitresource,
+      "setextHeading", closer(onexitsetextheading),
+      "setextHeadingLineSequence", JS.method1((JsValue self, JsValue token) => {
+        top()["depth"] = if (JS.strictEqual(JS.invoke(slice(self, token), "codePointAt", 0), 61)) { 1 } else { 2 };
+        return JS.undefined();
+      }),
+      "setextHeadingText", onexitsetextheadingtext,
+      "strong", closer(JS.undefined()),
+      "thematicBreak", closer(JS.undefined())
+    );
+  config = JS.object("transforms", transforms, "canContainEols", eols, "enter", enterMap, "exit", exitMap);
+  configure(config, JS.or(JS.or(options, JS.object())["mdastExtensions"], JS.array()));
+
+  Parser parser = JS.assume(parse(options));
+  JsValue events = postprocess(parser.document().write(JS.call(preprocess(), JS.undefined(), value, encoding, true)));
+
+  JsValue context = JS.object("stack", stack, "tokenStack", tokenStack, "config", config, "enter", enter, "exit", exit, "buffer", buffer, "resume", resume, "data", data);
+  float[] listStack = [];
+  for (float index = 0; index < events.length; index++) {
+    JsValue type = events[index][1]["type"];
+    if (type == tListOrdered || type == tListUnordered) {
+      if (events[index][0] == "enter") {
+        listStack.push(index);
+      } else {
+        index = prepareList(events, listStack.pop(), index);
       }
     }
+  }
 
-    index = -1;
-    while (true) {
-      index = index + 1;
-      if (index >= arrLen(events)) {
-        break;
-      }
-      JsValue ev = events[index];
-      JsValue handlerMap = objGet(config, eventKind(ev));
-      string typ = tokenType(eventToken(ev));
-      if (hasOwn(handlerMap, typ)) {
-        JsValue assigned = objectAssign(JS.object("sliceSerialize", objGet(eventContext(ev), "sliceSerialize")), context);
-        JS.call(objGet(handlerMap, typ), assigned, eventToken(ev));
-      }
+  for (float index = 0; index < events.length; index++) {
+    JsValue event = events[index];
+    JsValue handler = config[event[0]];
+    if (hasOwn(handler, event[1]["type"])) {
+      JS.call(handler[event[1]["type"]], JS.invoke(Object, "assign", JS.object("sliceSerialize", event[2]["sliceSerialize"]), context), event[1]);
     }
+  }
 
-    if (arrLen(tokenStack) > 0) {
-      JsValue tail = tokenStack[arrLen(tokenStack) - 1];
-      JsValue handler = tail[1];
-      if (!handler.truthy()) {
-        handler = defaultOnError;
-      }
-      JS.call(handler, context, undef(), tail[0]);
-    }
+  if (tokenStack.length > 0) {
+    JsValue tail = tokenStack[tokenStack.length - 1];
+    JS.call(JS.or(tail[1], defaultOnError), context, JS.undefined(), tail[0]);
+  }
 
-    JsValue startPoint = emptyPoint();
-    JsValue endPoint = emptyPoint();
-    if (arrLen(events) > 0) {
-      startPoint = objGet(eventToken(events[0]), "start");
-      endPoint = objGet(eventToken(events[arrLen(events) - 2]), "end");
-    }
-    objSet(tree, "position", JS.object("start", point(startPoint), "end", point(endPoint)));
-
-    index = -1;
-    JsValue transformList = objGet(config, "transforms");
-    while (true) {
-      index = index + 1;
-      if (index >= arrLen(transformList)) {
-        break;
-      }
-      JsValue next = JS.call(transformList[index], undef(), tree);
-      if (next.truthy()) {
-        tree = next;
-      }
-    }
-    return tree;
-  });
-}
+  JsValue start = JS.object("line", 1, "column", 1, "offset", 0);
+  JsValue end = start;
+  if (events.length > 0) {
+    start = events[0][1]["start"];
+    end = events[events.length - 2][1]["end"];
+  }
+  tree["position"] = JS.object("start", point(start), "end", point(end));
 
-export JsValue fromMarkdownImpl(JsValue value, JsValue encoding, JsValue options) {
-  JsValue enc = encoding;
-  JsValue settings = options;
-  if (enc.truthy() && JS.typeOf(enc) == "object") {
-    settings = enc;
-    enc = undef();
+  JsValue transformList = config["transforms"];
+  for (float index = 0; index < transformList.length; index++) {
+    tree = JS.or(JS.call(transformList[index], transformList, tree), tree);
   }
-  JsValue compileFn = compiler(settings);
-  JsValue parser = parseMarkdown(settings);
-  JsValue tokenizer = JS.call(objGet(parser, "document"), parser);
-  JsValue chunks = JS.call(preprocessMarkdown(), undef(), value, enc, true);
-  JsValue events = JS.call(objGet(tokenizer, "write"), tokenizer, chunks);
-  return JS.call(compileFn, undef(), postprocessEvents(events));
+  return tree;
 }
diff -ruN a/src/graph/remark-parse/micromark/character-entities.lil b/src/graph/remark-parse/micromark/character-entities.lil
--- a/src/graph/remark-parse/micromark/character-entities.lil	2026-09-23 20:40:45.223255672 +0000
+++ b/src/graph/remark-parse/micromark/character-entities.lil	2026-09-23 20:40:45.266692628 +0000
@@ -1 +1,2127 @@
-export JsValue characterEntities = JS.invoke(JSON, "parse", "{\"AElig\":\"Æ\",\"AMP\":\"&\",\"Aacute\":\"Á\",\"Abreve\":\"Ă\",\"Acirc\":\"Â\",\"Acy\":\"А\",\"Afr\":\"𝔄\",\"Agrave\":\"À\",\"Alpha\":\"Α\",\"Amacr\":\"Ā\",\"And\":\"⩓\",\"Aogon\":\"Ą\",\"Aopf\":\"𝔸\",\"ApplyFunction\":\"⁡\",\"Aring\":\"Å\",\"Ascr\":\"𝒜\",\"Assign\":\"≔\",\"Atilde\":\"Ã\",\"Auml\":\"Ä\",\"Backslash\":\"∖\",\"Barv\":\"⫧\",\"Barwed\":\"⌆\",\"Bcy\":\"Б\",\"Because\":\"∵\",\"Bernoullis\":\"ℬ\",\"Beta\":\"Β\",\"Bfr\":\"𝔅\",\"Bopf\":\"𝔹\",\"Breve\":\"˘\",\"Bscr\":\"ℬ\",\"Bumpeq\":\"≎\",\"CHcy\":\"Ч\",\"COPY\":\"©\",\"Cacute\":\"Ć\",\"Cap\":\"⋒\",\"CapitalDifferentialD\":\"ⅅ\",\"Cayleys\":\"ℭ\",\"Ccaron\":\"Č\",\"Ccedil\":\"Ç\",\"Ccirc\":\"Ĉ\",\"Cconint\":\"∰\",\"Cdot\":\"Ċ\",\"Cedilla\":\"¸\",\"CenterDot\":\"·\",\"Cfr\":\"ℭ\",\"Chi\":\"Χ\",\"CircleDot\":\"⊙\",\"CircleMinus\":\"⊖\",\"CirclePlus\":\"⊕\",\"CircleTimes\":\"⊗\",\"ClockwiseContourIntegral\":\"∲\",\"CloseCurlyDoubleQuote\":\"”\",\"CloseCurlyQuote\":\"’\",\"Colon\":\"∷\",\"Colone\":\"⩴\",\"Congruent\":\"≡\",\"Conint\":\"∯\",\"ContourIntegral\":\"∮\",\"Copf\":\"ℂ\",\"Coproduct\":\"∐\",\"CounterClockwiseContourIntegral\":\"∳\",\"Cross\":\"⨯\",\"Cscr\":\"𝒞\",\"Cup\":\"⋓\",\"CupCap\":\"≍\",\"DD\":\"ⅅ\",\"DDotrahd\":\"⤑\",\"DJcy\":\"Ђ\",\"DScy\":\"Ѕ\",\"DZcy\":\"Џ\",\"Dagger\":\"‡\",\"Darr\":\"↡\",\"Dashv\":\"⫤\",\"Dcaron\":\"Ď\",\"Dcy\":\"Д\",\"Del\":\"∇\",\"Delta\":\"Δ\",\"Dfr\":\"𝔇\",\"DiacriticalAcute\":\"´\",\"DiacriticalDot\":\"˙\",\"DiacriticalDoubleAcute\":\"˝\",\"DiacriticalGrave\":\"`\",\"DiacriticalTilde\":\"˜\",\"Diamond\":\"⋄\",\"DifferentialD\":\"ⅆ\",\"Dopf\":\"𝔻\",\"Dot\":\"¨\",\"DotDot\":\"⃜\",\"DotEqual\":\"≐\",\"DoubleContourIntegral\":\"∯\",\"DoubleDot\":\"¨\",\"DoubleDownArrow\":\"⇓\",\"DoubleLeftArrow\":\"⇐\",\"DoubleLeftRightArrow\":\"⇔\",\"DoubleLeftTee\":\"⫤\",\"DoubleLongLeftArrow\":\"⟸\",\"DoubleLongLeftRightArrow\":\"⟺\",\"DoubleLongRightArrow\":\"⟹\",\"DoubleRightArrow\":\"⇒\",\"DoubleRightTee\":\"⊨\",\"DoubleUpArrow\":\"⇑\",\"DoubleUpDownArrow\":\"⇕\",\"DoubleVerticalBar\":\"∥\",\"DownArrow\":\"↓\",\"DownArrowBar\":\"⤓\",\"DownArrowUpArrow\":\"⇵\",\"DownBreve\":\"̑\",\"DownLeftRightVector\":\"⥐\",\"DownLeftTeeVector\":\"⥞\",\"DownLeftVector\":\"↽\",\"DownLeftVectorBar\":\"⥖\",\"DownRightTeeVector\":\"⥟\",\"DownRightVector\":\"⇁\",\"DownRightVectorBar\":\"⥗\",\"DownTee\":\"⊤\",\"DownTeeArrow\":\"↧\",\"Downarrow\":\"⇓\",\"Dscr\":\"𝒟\",\"Dstrok\":\"Đ\",\"ENG\":\"Ŋ\",\"ETH\":\"Ð\",\"Eacute\":\"É\",\"Ecaron\":\"Ě\",\"Ecirc\":\"Ê\",\"Ecy\":\"Э\",\"Edot\":\"Ė\",\"Efr\":\"𝔈\",\"Egrave\":\"È\",\"Element\":\"∈\",\"Emacr\":\"Ē\",\"EmptySmallSquare\":\"◻\",\"EmptyVerySmallSquare\":\"▫\",\"Eogon\":\"Ę\",\"Eopf\":\"𝔼\",\"Epsilon\":\"Ε\",\"Equal\":\"⩵\",\"EqualTilde\":\"≂\",\"Equilibrium\":\"⇌\",\"Escr\":\"ℰ\",\"Esim\":\"⩳\",\"Eta\":\"Η\",\"Euml\":\"Ë\",\"Exists\":\"∃\",\"ExponentialE\":\"ⅇ\",\"Fcy\":\"Ф\",\"Ffr\":\"𝔉\",\"FilledSmallSquare\":\"◼\",\"FilledVerySmallSquare\":\"▪\",\"Fopf\":\"𝔽\",\"ForAll\":\"∀\",\"Fouriertrf\":\"ℱ\",\"Fscr\":\"ℱ\",\"GJcy\":\"Ѓ\",\"GT\":\">\",\"Gamma\":\"Γ\",\"Gammad\":\"Ϝ\",\"Gbreve\":\"Ğ\",\"Gcedil\":\"Ģ\",\"Gcirc\":\"Ĝ\",\"Gcy\":\"Г\",\"Gdot\":\"Ġ\",\"Gfr\":\"𝔊\",\"Gg\":\"⋙\",\"Gopf\":\"𝔾\",\"GreaterEqual\":\"≥\",\"GreaterEqualLess\":\"⋛\",\"GreaterFullEqual\":\"≧\",\"GreaterGreater\":\"⪢\",\"GreaterLess\":\"≷\",\"GreaterSlantEqual\":\"⩾\",\"GreaterTilde\":\"≳\",\"Gscr\":\"𝒢\",\"Gt\":\"≫\",\"HARDcy\":\"Ъ\",\"Hacek\":\"ˇ\",\"Hat\":\"^\",\"Hcirc\":\"Ĥ\",\"Hfr\":\"ℌ\",\"HilbertSpace\":\"ℋ\",\"Hopf\":\"ℍ\",\"HorizontalLine\":\"─\",\"Hscr\":\"ℋ\",\"Hstrok\":\"Ħ\",\"HumpDownHump\":\"≎\",\"HumpEqual\":\"≏\",\"IEcy\":\"Е\",\"IJlig\":\"Ĳ\",\"IOcy\":\"Ё\",\"Iacute\":\"Í\",\"Icirc\":\"Î\",\"Icy\":\"И\",\"Idot\":\"İ\",\"Ifr\":\"ℑ\",\"Igrave\":\"Ì\",\"Im\":\"ℑ\",\"Imacr\":\"Ī\",\"ImaginaryI\":\"ⅈ\",\"Implies\":\"⇒\",\"Int\":\"∬\",\"Integral\":\"∫\",\"Intersection\":\"⋂\",\"InvisibleComma\":\"⁣\",\"InvisibleTimes\":\"⁢\",\"Iogon\":\"Į\",\"Iopf\":\"𝕀\",\"Iota\":\"Ι\",\"Iscr\":\"ℐ\",\"Itilde\":\"Ĩ\",\"Iukcy\":\"І\",\"Iuml\":\"Ï\",\"Jcirc\":\"Ĵ\",\"Jcy\":\"Й\",\"Jfr\":\"𝔍\",\"Jopf\":\"𝕁\",\"Jscr\":\"𝒥\",\"Jsercy\":\"Ј\",\"Jukcy\":\"Є\",\"KHcy\":\"Х\",\"KJcy\":\"Ќ\",\"Kappa\":\"Κ\",\"Kcedil\":\"Ķ\",\"Kcy\":\"К\",\"Kfr\":\"𝔎\",\"Kopf\":\"𝕂\",\"Kscr\":\"𝒦\",\"LJcy\":\"Љ\",\"LT\":\"<\",\"Lacute\":\"Ĺ\",\"Lambda\":\"Λ\",\"Lang\":\"⟪\",\"Laplacetrf\":\"ℒ\",\"Larr\":\"↞\",\"Lcaron\":\"Ľ\",\"Lcedil\":\"Ļ\",\"Lcy\":\"Л\",\"LeftAngleBracket\":\"⟨\",\"LeftArrow\":\"←\",\"LeftArrowBar\":\"⇤\",\"LeftArrowRightArrow\":\"⇆\",\"LeftCeiling\":\"⌈\",\"LeftDoubleBracket\":\"⟦\",\"LeftDownTeeVector\":\"⥡\",\"LeftDownVector\":\"⇃\",\"LeftDownVectorBar\":\"⥙\",\"LeftFloor\":\"⌊\",\"LeftRightArrow\":\"↔\",\"LeftRightVector\":\"⥎\",\"LeftTee\":\"⊣\",\"LeftTeeArrow\":\"↤\",\"LeftTeeVector\":\"⥚\",\"LeftTriangle\":\"⊲\",\"LeftTriangleBar\":\"⧏\",\"LeftTriangleEqual\":\"⊴\",\"LeftUpDownVector\":\"⥑\",\"LeftUpTeeVector\":\"⥠\",\"LeftUpVector\":\"↿\",\"LeftUpVectorBar\":\"⥘\",\"LeftVector\":\"↼\",\"LeftVectorBar\":\"⥒\",\"Leftarrow\":\"⇐\",\"Leftrightarrow\":\"⇔\",\"LessEqualGreater\":\"⋚\",\"LessFullEqual\":\"≦\",\"LessGreater\":\"≶\",\"LessLess\":\"⪡\",\"LessSlantEqual\":\"⩽\",\"LessTilde\":\"≲\",\"Lfr\":\"𝔏\",\"Ll\":\"⋘\",\"Lleftarrow\":\"⇚\",\"Lmidot\":\"Ŀ\",\"LongLeftArrow\":\"⟵\",\"LongLeftRightArrow\":\"⟷\",\"LongRightArrow\":\"⟶\",\"Longleftarrow\":\"⟸\",\"Longleftrightarrow\":\"⟺\",\"Longrightarrow\":\"⟹\",\"Lopf\":\"𝕃\",\"LowerLeftArrow\":\"↙\",\"LowerRightArrow\":\"↘\",\"Lscr\":\"ℒ\",\"Lsh\":\"↰\",\"Lstrok\":\"Ł\",\"Lt\":\"≪\",\"Map\":\"⤅\",\"Mcy\":\"М\",\"MediumSpace\":\" \",\"Mellintrf\":\"ℳ\",\"Mfr\":\"𝔐\",\"MinusPlus\":\"∓\",\"Mopf\":\"𝕄\",\"Mscr\":\"ℳ\",\"Mu\":\"Μ\",\"NJcy\":\"Њ\",\"Nacute\":\"Ń\",\"Ncaron\":\"Ň\",\"Ncedil\":\"Ņ\",\"Ncy\":\"Н\",\"NegativeMediumSpace\":\"​\",\"NegativeThickSpace\":\"​\",\"NegativeThinSpace\":\"​\",\"NegativeVeryThinSpace\":\"​\",\"NestedGreaterGreater\":\"≫\",\"NestedLessLess\":\"≪\",\"NewLine\":\"\\n\",\"Nfr\":\"𝔑\",\"NoBreak\":\"⁠\",\"NonBreakingSpace\":\" \",\"Nopf\":\"ℕ\",\"Not\":\"⫬\",\"NotCongruent\":\"≢\",\"NotCupCap\":\"≭\",\"NotDoubleVerticalBar\":\"∦\",\"NotElement\":\"∉\",\"NotEqual\":\"≠\",\"NotEqualTilde\":\"≂̸\",\"NotExists\":\"∄\",\"NotGreater\":\"≯\",\"NotGreaterEqual\":\"≱\",\"NotGreaterFullEqual\":\"≧̸\",\"NotGreaterGreater\":\"≫̸\",\"NotGreaterLess\":\"≹\",\"NotGreaterSlantEqual\":\"⩾̸\",\"NotGreaterTilde\":\"≵\",\"NotHumpDownHump\":\"≎̸\",\"NotHumpEqual\":\"≏̸\",\"NotLeftTriangle\":\"⋪\",\"NotLeftTriangleBar\":\"⧏̸\",\"NotLeftTriangleEqual\":\"⋬\",\"NotLess\":\"≮\",\"NotLessEqual\":\"≰\",\"NotLessGreater\":\"≸\",\"NotLessLess\":\"≪̸\",\"NotLessSlantEqual\":\"⩽̸\",\"NotLessTilde\":\"≴\",\"NotNestedGreaterGreater\":\"⪢̸\",\"NotNestedLessLess\":\"⪡̸\",\"NotPrecedes\":\"⊀\",\"NotPrecedesEqual\":\"⪯̸\",\"NotPrecedesSlantEqual\":\"⋠\",\"NotReverseElement\":\"∌\",\"NotRightTriangle\":\"⋫\",\"NotRightTriangleBar\":\"⧐̸\",\"NotRightTriangleEqual\":\"⋭\",\"NotSquareSubset\":\"⊏̸\",\"NotSquareSubsetEqual\":\"⋢\",\"NotSquareSuperset\":\"⊐̸\",\"NotSquareSupersetEqual\":\"⋣\",\"NotSubset\":\"⊂⃒\",\"NotSubsetEqual\":\"⊈\",\"NotSucceeds\":\"⊁\",\"NotSucceedsEqual\":\"⪰̸\",\"NotSucceedsSlantEqual\":\"⋡\",\"NotSucceedsTilde\":\"≿̸\",\"NotSuperset\":\"⊃⃒\",\"NotSupersetEqual\":\"⊉\",\"NotTilde\":\"≁\",\"NotTildeEqual\":\"≄\",\"NotTildeFullEqual\":\"≇\",\"NotTildeTilde\":\"≉\",\"NotVerticalBar\":\"∤\",\"Nscr\":\"𝒩\",\"Ntilde\":\"Ñ\",\"Nu\":\"Ν\",\"OElig\":\"Œ\",\"Oacute\":\"Ó\",\"Ocirc\":\"Ô\",\"Ocy\":\"О\",\"Odblac\":\"Ő\",\"Ofr\":\"𝔒\",\"Ograve\":\"Ò\",\"Omacr\":\"Ō\",\"Omega\":\"Ω\",\"Omicron\":\"Ο\",\"Oopf\":\"𝕆\",\"OpenCurlyDoubleQuote\":\"“\",\"OpenCurlyQuote\":\"‘\",\"Or\":\"⩔\",\"Oscr\":\"𝒪\",\"Oslash\":\"Ø\",\"Otilde\":\"Õ\",\"Otimes\":\"⨷\",\"Ouml\":\"Ö\",\"OverBar\":\"‾\",\"OverBrace\":\"⏞\",\"OverBracket\":\"⎴\",\"OverParenthesis\":\"⏜\",\"PartialD\":\"∂\",\"Pcy\":\"П\",\"Pfr\":\"𝔓\",\"Phi\":\"Φ\",\"Pi\":\"Π\",\"PlusMinus\":\"±\",\"Poincareplane\":\"ℌ\",\"Popf\":\"ℙ\",\"Pr\":\"⪻\",\"Precedes\":\"≺\",\"PrecedesEqual\":\"⪯\",\"PrecedesSlantEqual\":\"≼\",\"PrecedesTilde\":\"≾\",\"Prime\":\"″\",\"Product\":\"∏\",\"Proportion\":\"∷\",\"Proportional\":\"∝\",\"Pscr\":\"𝒫\",\"Psi\":\"Ψ\",\"QUOT\":\"\\\"\",\"Qfr\":\"𝔔\",\"Qopf\":\"ℚ\",\"Qscr\":\"𝒬\",\"RBarr\":\"⤐\",\"REG\":\"®\",\"Racute\":\"Ŕ\",\"Rang\":\"⟫\",\"Rarr\":\"↠\",\"Rarrtl\":\"⤖\",\"Rcaron\":\"Ř\",\"Rcedil\":\"Ŗ\",\"Rcy\":\"Р\",\"Re\":\"ℜ\",\"ReverseElement\":\"∋\",\"ReverseEquilibrium\":\"⇋\",\"ReverseUpEquilibrium\":\"⥯\",\"Rfr\":\"ℜ\",\"Rho\":\"Ρ\",\"RightAngleBracket\":\"⟩\",\"RightArrow\":\"→\",\"RightArrowBar\":\"⇥\",\"RightArrowLeftArrow\":\"⇄\",\"RightCeiling\":\"⌉\",\"RightDoubleBracket\":\"⟧\",\"RightDownTeeVector\":\"⥝\",\"RightDownVector\":\"⇂\",\"RightDownVectorBar\":\"⥕\",\"RightFloor\":\"⌋\",\"RightTee\":\"⊢\",\"RightTeeArrow\":\"↦\",\"RightTeeVector\":\"⥛\",\"RightTriangle\":\"⊳\",\"RightTriangleBar\":\"⧐\",\"RightTriangleEqual\":\"⊵\",\"RightUpDownVector\":\"⥏\",\"RightUpTeeVector\":\"⥜\",\"RightUpVector\":\"↾\",\"RightUpVectorBar\":\"⥔\",\"RightVector\":\"⇀\",\"RightVectorBar\":\"⥓\",\"Rightarrow\":\"⇒\",\"Ropf\":\"ℝ\",\"RoundImplies\":\"⥰\",\"Rrightarrow\":\"⇛\",\"Rscr\":\"ℛ\",\"Rsh\":\"↱\",\"RuleDelayed\":\"⧴\",\"SHCHcy\":\"Щ\",\"SHcy\":\"Ш\",\"SOFTcy\":\"Ь\",\"Sacute\":\"Ś\",\"Sc\":\"⪼\",\"Scaron\":\"Š\",\"Scedil\":\"Ş\",\"Scirc\":\"Ŝ\",\"Scy\":\"С\",\"Sfr\":\"𝔖\",\"ShortDownArrow\":\"↓\",\"ShortLeftArrow\":\"←\",\"ShortRightArrow\":\"→\",\"ShortUpArrow\":\"↑\",\"Sigma\":\"Σ\",\"SmallCircle\":\"∘\",\"Sopf\":\"𝕊\",\"Sqrt\":\"√\",\"Square\":\"□\",\"SquareIntersection\":\"⊓\",\"SquareSubset\":\"⊏\",\"SquareSubsetEqual\":\"⊑\",\"SquareSuperset\":\"⊐\",\"SquareSupersetEqual\":\"⊒\",\"SquareUnion\":\"⊔\",\"Sscr\":\"𝒮\",\"Star\":\"⋆\",\"Sub\":\"⋐\",\"Subset\":\"⋐\",\"SubsetEqual\":\"⊆\",\"Succeeds\":\"≻\",\"SucceedsEqual\":\"⪰\",\"SucceedsSlantEqual\":\"≽\",\"SucceedsTilde\":\"≿\",\"SuchThat\":\"∋\",\"Sum\":\"∑\",\"Sup\":\"⋑\",\"Superset\":\"⊃\",\"SupersetEqual\":\"⊇\",\"Supset\":\"⋑\",\"THORN\":\"Þ\",\"TRADE\":\"™\",\"TSHcy\":\"Ћ\",\"TScy\":\"Ц\",\"Tab\":\"\\t\",\"Tau\":\"Τ\",\"Tcaron\":\"Ť\",\"Tcedil\":\"Ţ\",\"Tcy\":\"Т\",\"Tfr\":\"𝔗\",\"Therefore\":\"∴\",\"Theta\":\"Θ\",\"ThickSpace\":\"  \",\"ThinSpace\":\" \",\"Tilde\":\"∼\",\"TildeEqual\":\"≃\",\"TildeFullEqual\":\"≅\",\"TildeTilde\":\"≈\",\"Topf\":\"𝕋\",\"TripleDot\":\"⃛\",\"Tscr\":\"𝒯\",\"Tstrok\":\"Ŧ\",\"Uacute\":\"Ú\",\"Uarr\":\"↟\",\"Uarrocir\":\"⥉\",\"Ubrcy\":\"Ў\",\"Ubreve\":\"Ŭ\",\"Ucirc\":\"Û\",\"Ucy\":\"У\",\"Udblac\":\"Ű\",\"Ufr\":\"𝔘\",\"Ugrave\":\"Ù\",\"Umacr\":\"Ū\",\"UnderBar\":\"_\",\"UnderBrace\":\"⏟\",\"UnderBracket\":\"⎵\",\"UnderParenthesis\":\"⏝\",\"Union\":\"⋃\",\"UnionPlus\":\"⊎\",\"Uogon\":\"Ų\",\"Uopf\":\"𝕌\",\"UpArrow\":\"↑\",\"UpArrowBar\":\"⤒\",\"UpArrowDownArrow\":\"⇅\",\"UpDownArrow\":\"↕\",\"UpEquilibrium\":\"⥮\",\"UpTee\":\"⊥\",\"UpTeeArrow\":\"↥\",\"Uparrow\":\"⇑\",\"Updownarrow\":\"⇕\",\"UpperLeftArrow\":\"↖\",\"UpperRightArrow\":\"↗\",\"Upsi\":\"ϒ\",\"Upsilon\":\"Υ\",\"Uring\":\"Ů\",\"Uscr\":\"𝒰\",\"Utilde\":\"Ũ\",\"Uuml\":\"Ü\",\"VDash\":\"⊫\",\"Vbar\":\"⫫\",\"Vcy\":\"В\",\"Vdash\":\"⊩\",\"Vdashl\":\"⫦\",\"Vee\":\"⋁\",\"Verbar\":\"‖\",\"Vert\":\"‖\",\"VerticalBar\":\"∣\",\"VerticalLine\":\"|\",\"VerticalSeparator\":\"❘\",\"VerticalTilde\":\"≀\",\"VeryThinSpace\":\" \",\"Vfr\":\"𝔙\",\"Vopf\":\"𝕍\",\"Vscr\":\"𝒱\",\"Vvdash\":\"⊪\",\"Wcirc\":\"Ŵ\",\"Wedge\":\"⋀\",\"Wfr\":\"𝔚\",\"Wopf\":\"𝕎\",\"Wscr\":\"𝒲\",\"Xfr\":\"𝔛\",\"Xi\":\"Ξ\",\"Xopf\":\"𝕏\",\"Xscr\":\"𝒳\",\"YAcy\":\"Я\",\"YIcy\":\"Ї\",\"YUcy\":\"Ю\",\"Yacute\":\"Ý\",\"Ycirc\":\"Ŷ\",\"Ycy\":\"Ы\",\"Yfr\":\"𝔜\",\"Yopf\":\"𝕐\",\"Yscr\":\"𝒴\",\"Yuml\":\"Ÿ\",\"ZHcy\":\"Ж\",\"Zacute\":\"Ź\",\"Zcaron\":\"Ž\",\"Zcy\":\"З\",\"Zdot\":\"Ż\",\"ZeroWidthSpace\":\"​\",\"Zeta\":\"Ζ\",\"Zfr\":\"ℨ\",\"Zopf\":\"ℤ\",\"Zscr\":\"𝒵\",\"aacute\":\"á\",\"abreve\":\"ă\",\"ac\":\"∾\",\"acE\":\"∾̳\",\"acd\":\"∿\",\"acirc\":\"â\",\"acute\":\"´\",\"acy\":\"а\",\"aelig\":\"æ\",\"af\":\"⁡\",\"afr\":\"𝔞\",\"agrave\":\"à\",\"alefsym\":\"ℵ\",\"aleph\":\"ℵ\",\"alpha\":\"α\",\"amacr\":\"ā\",\"amalg\":\"⨿\",\"amp\":\"&\",\"and\":\"∧\",\"andand\":\"⩕\",\"andd\":\"⩜\",\"andslope\":\"⩘\",\"andv\":\"⩚\",\"ang\":\"∠\",\"ange\":\"⦤\",\"angle\":\"∠\",\"angmsd\":\"∡\",\"angmsdaa\":\"⦨\",\"angmsdab\":\"⦩\",\"angmsdac\":\"⦪\",\"angmsdad\":\"⦫\",\"angmsdae\":\"⦬\",\"angmsdaf\":\"⦭\",\"angmsdag\":\"⦮\",\"angmsdah\":\"⦯\",\"angrt\":\"∟\",\"angrtvb\":\"⊾\",\"angrtvbd\":\"⦝\",\"angsph\":\"∢\",\"angst\":\"Å\",\"angzarr\":\"⍼\",\"aogon\":\"ą\",\"aopf\":\"𝕒\",\"ap\":\"≈\",\"apE\":\"⩰\",\"apacir\":\"⩯\",\"ape\":\"≊\",\"apid\":\"≋\",\"apos\":\"'\",\"approx\":\"≈\",\"approxeq\":\"≊\",\"aring\":\"å\",\"ascr\":\"𝒶\",\"ast\":\"*\",\"asymp\":\"≈\",\"asympeq\":\"≍\",\"atilde\":\"ã\",\"auml\":\"ä\",\"awconint\":\"∳\",\"awint\":\"⨑\",\"bNot\":\"⫭\",\"backcong\":\"≌\",\"backepsilon\":\"϶\",\"backprime\":\"‵\",\"backsim\":\"∽\",\"backsimeq\":\"⋍\",\"barvee\":\"⊽\",\"barwed\":\"⌅\",\"barwedge\":\"⌅\",\"bbrk\":\"⎵\",\"bbrktbrk\":\"⎶\",\"bcong\":\"≌\",\"bcy\":\"б\",\"bdquo\":\"„\",\"becaus\":\"∵\",\"because\":\"∵\",\"bemptyv\":\"⦰\",\"bepsi\":\"϶\",\"bernou\":\"ℬ\",\"beta\":\"β\",\"beth\":\"ℶ\",\"between\":\"≬\",\"bfr\":\"𝔟\",\"bigcap\":\"⋂\",\"bigcirc\":\"◯\",\"bigcup\":\"⋃\",\"bigodot\":\"⨀\",\"bigoplus\":\"⨁\",\"bigotimes\":\"⨂\",\"bigsqcup\":\"⨆\",\"bigstar\":\"★\",\"bigtriangledown\":\"▽\",\"bigtriangleup\":\"△\",\"biguplus\":\"⨄\",\"bigvee\":\"⋁\",\"bigwedge\":\"⋀\",\"bkarow\":\"⤍\",\"blacklozenge\":\"⧫\",\"blacksquare\":\"▪\",\"blacktriangle\":\"▴\",\"blacktriangledown\":\"▾\",\"blacktriangleleft\":\"◂\",\"blacktriangleright\":\"▸\",\"blank\":\"␣\",\"blk12\":\"▒\",\"blk14\":\"░\",\"blk34\":\"▓\",\"block\":\"█\",\"bne\":\"=⃥\",\"bnequiv\":\"≡⃥\",\"bnot\":\"⌐\",\"bopf\":\"𝕓\",\"bot\":\"⊥\",\"bottom\":\"⊥\",\"bowtie\":\"⋈\",\"boxDL\":\"╗\",\"boxDR\":\"╔\",\"boxDl\":\"╖\",\"boxDr\":\"╓\",\"boxH\":\"═\",\"boxHD\":\"╦\",\"boxHU\":\"╩\",\"boxHd\":\"╤\",\"boxHu\":\"╧\",\"boxUL\":\"╝\",\"boxUR\":\"╚\",\"boxUl\":\"╜\",\"boxUr\":\"╙\",\"boxV\":\"║\",\"boxVH\":\"╬\",\"boxVL\":\"╣\",\"boxVR\":\"╠\",\"boxVh\":\"╫\",\"boxVl\":\"╢\",\"boxVr\":\"╟\",\"boxbox\":\"⧉\",\"boxdL\":\"╕\",\"boxdR\":\"╒\",\"boxdl\":\"┐\",\"boxdr\":\"┌\",\"boxh\":\"─\",\"boxhD\":\"╥\",\"boxhU\":\"╨\",\"boxhd\":\"┬\",\"boxhu\":\"┴\",\"boxminus\":\"⊟\",\"boxplus\":\"⊞\",\"boxtimes\":\"⊠\",\"boxuL\":\"╛\",\"boxuR\":\"╘\",\"boxul\":\"┘\",\"boxur\":\"└\",\"boxv\":\"│\",\"boxvH\":\"╪\",\"boxvL\":\"╡\",\"boxvR\":\"╞\",\"boxvh\":\"┼\",\"boxvl\":\"┤\",\"boxvr\":\"├\",\"bprime\":\"‵\",\"breve\":\"˘\",\"brvbar\":\"¦\",\"bscr\":\"𝒷\",\"bsemi\":\"⁏\",\"bsim\":\"∽\",\"bsime\":\"⋍\",\"bsol\":\"\\\\\",\"bsolb\":\"⧅\",\"bsolhsub\":\"⟈\",\"bull\":\"•\",\"bullet\":\"•\",\"bump\":\"≎\",\"bumpE\":\"⪮\",\"bumpe\":\"≏\",\"bumpeq\":\"≏\",\"cacute\":\"ć\",\"cap\":\"∩\",\"capand\":\"⩄\",\"capbrcup\":\"⩉\",\"capcap\":\"⩋\",\"capcup\":\"⩇\",\"capdot\":\"⩀\",\"caps\":\"∩︀\",\"caret\":\"⁁\",\"caron\":\"ˇ\",\"ccaps\":\"⩍\",\"ccaron\":\"č\",\"ccedil\":\"ç\",\"ccirc\":\"ĉ\",\"ccups\":\"⩌\",\"ccupssm\":\"⩐\",\"cdot\":\"ċ\",\"cedil\":\"¸\",\"cemptyv\":\"⦲\",\"cent\":\"¢\",\"centerdot\":\"·\",\"cfr\":\"𝔠\",\"chcy\":\"ч\",\"check\":\"✓\",\"checkmark\":\"✓\",\"chi\":\"χ\",\"cir\":\"○\",\"cirE\":\"⧃\",\"circ\":\"ˆ\",\"circeq\":\"≗\",\"circlearrowleft\":\"↺\",\"circlearrowright\":\"↻\",\"circledR\":\"®\",\"circledS\":\"Ⓢ\",\"circledast\":\"⊛\",\"circledcirc\":\"⊚\",\"circleddash\":\"⊝\",\"cire\":\"≗\",\"cirfnint\":\"⨐\",\"cirmid\":\"⫯\",\"cirscir\":\"⧂\",\"clubs\":\"♣\",\"clubsuit\":\"♣\",\"colon\":\":\",\"colone\":\"≔\",\"coloneq\":\"≔\",\"comma\":\",\",\"commat\":\"@\",\"comp\":\"∁\",\"compfn\":\"∘\",\"complement\":\"∁\",\"complexes\":\"ℂ\",\"cong\":\"≅\",\"congdot\":\"⩭\",\"conint\":\"∮\",\"copf\":\"𝕔\",\"coprod\":\"∐\",\"copy\":\"©\",\"copysr\":\"℗\",\"crarr\":\"↵\",\"cross\":\"✗\",\"cscr\":\"𝒸\",\"csub\":\"⫏\",\"csube\":\"⫑\",\"csup\":\"⫐\",\"csupe\":\"⫒\",\"ctdot\":\"⋯\",\"cudarrl\":\"⤸\",\"cudarrr\":\"⤵\",\"cuepr\":\"⋞\",\"cuesc\":\"⋟\",\"cularr\":\"↶\",\"cularrp\":\"⤽\",\"cup\":\"∪\",\"cupbrcap\":\"⩈\",\"cupcap\":\"⩆\",\"cupcup\":\"⩊\",\"cupdot\":\"⊍\",\"cupor\":\"⩅\",\"cups\":\"∪︀\",\"curarr\":\"↷\",\"curarrm\":\"⤼\",\"curlyeqprec\":\"⋞\",\"curlyeqsucc\":\"⋟\",\"curlyvee\":\"⋎\",\"curlywedge\":\"⋏\",\"curren\":\"¤\",\"curvearrowleft\":\"↶\",\"curvearrowright\":\"↷\",\"cuvee\":\"⋎\",\"cuwed\":\"⋏\",\"cwconint\":\"∲\",\"cwint\":\"∱\",\"cylcty\":\"⌭\",\"dArr\":\"⇓\",\"dHar\":\"⥥\",\"dagger\":\"†\",\"daleth\":\"ℸ\",\"darr\":\"↓\",\"dash\":\"‐\",\"dashv\":\"⊣\",\"dbkarow\":\"⤏\",\"dblac\":\"˝\",\"dcaron\":\"ď\",\"dcy\":\"д\",\"dd\":\"ⅆ\",\"ddagger\":\"‡\",\"ddarr\":\"⇊\",\"ddotseq\":\"⩷\",\"deg\":\"°\",\"delta\":\"δ\",\"demptyv\":\"⦱\",\"dfisht\":\"⥿\",\"dfr\":\"𝔡\",\"dharl\":\"⇃\",\"dharr\":\"⇂\",\"diam\":\"⋄\",\"diamond\":\"⋄\",\"diamondsuit\":\"♦\",\"diams\":\"♦\",\"die\":\"¨\",\"digamma\":\"ϝ\",\"disin\":\"⋲\",\"div\":\"÷\",\"divide\":\"÷\",\"divideontimes\":\"⋇\",\"divonx\":\"⋇\",\"djcy\":\"ђ\",\"dlcorn\":\"⌞\",\"dlcrop\":\"⌍\",\"dollar\":\"$\",\"dopf\":\"𝕕\",\"dot\":\"˙\",\"doteq\":\"≐\",\"doteqdot\":\"≑\",\"dotminus\":\"∸\",\"dotplus\":\"∔\",\"dotsquare\":\"⊡\",\"doublebarwedge\":\"⌆\",\"downarrow\":\"↓\",\"downdownarrows\":\"⇊\",\"downharpoonleft\":\"⇃\",\"downharpoonright\":\"⇂\",\"drbkarow\":\"⤐\",\"drcorn\":\"⌟\",\"drcrop\":\"⌌\",\"dscr\":\"𝒹\",\"dscy\":\"ѕ\",\"dsol\":\"⧶\",\"dstrok\":\"đ\",\"dtdot\":\"⋱\",\"dtri\":\"▿\",\"dtrif\":\"▾\",\"duarr\":\"⇵\",\"duhar\":\"⥯\",\"dwangle\":\"⦦\",\"dzcy\":\"џ\",\"dzigrarr\":\"⟿\",\"eDDot\":\"⩷\",\"eDot\":\"≑\",\"eacute\":\"é\",\"easter\":\"⩮\",\"ecaron\":\"ě\",\"ecir\":\"≖\",\"ecirc\":\"ê\",\"ecolon\":\"≕\",\"ecy\":\"э\",\"edot\":\"ė\",\"ee\":\"ⅇ\",\"efDot\":\"≒\",\"efr\":\"𝔢\",\"eg\":\"⪚\",\"egrave\":\"è\",\"egs\":\"⪖\",\"egsdot\":\"⪘\",\"el\":\"⪙\",\"elinters\":\"⏧\",\"ell\":\"ℓ\",\"els\":\"⪕\",\"elsdot\":\"⪗\",\"emacr\":\"ē\",\"empty\":\"∅\",\"emptyset\":\"∅\",\"emptyv\":\"∅\",\"emsp13\":\" \",\"emsp14\":\" \",\"emsp\":\" \",\"eng\":\"ŋ\",\"ensp\":\" \",\"eogon\":\"ę\",\"eopf\":\"𝕖\",\"epar\":\"⋕\",\"eparsl\":\"⧣\",\"eplus\":\"⩱\",\"epsi\":\"ε\",\"epsilon\":\"ε\",\"epsiv\":\"ϵ\",\"eqcirc\":\"≖\",\"eqcolon\":\"≕\",\"eqsim\":\"≂\",\"eqslantgtr\":\"⪖\",\"eqslantless\":\"⪕\",\"equals\":\"=\",\"equest\":\"≟\",\"equiv\":\"≡\",\"equivDD\":\"⩸\",\"eqvparsl\":\"⧥\",\"erDot\":\"≓\",\"erarr\":\"⥱\",\"escr\":\"ℯ\",\"esdot\":\"≐\",\"esim\":\"≂\",\"eta\":\"η\",\"eth\":\"ð\",\"euml\":\"ë\",\"euro\":\"€\",\"excl\":\"!\",\"exist\":\"∃\",\"expectation\":\"ℰ\",\"exponentiale\":\"ⅇ\",\"fallingdotseq\":\"≒\",\"fcy\":\"ф\",\"female\":\"♀\",\"ffilig\":\"ﬃ\",\"fflig\":\"ﬀ\",\"ffllig\":\"ﬄ\",\"ffr\":\"𝔣\",\"filig\":\"ﬁ\",\"fjlig\":\"fj\",\"flat\":\"♭\",\"fllig\":\"ﬂ\",\"fltns\":\"▱\",\"fnof\":\"ƒ\",\"fopf\":\"𝕗\",\"forall\":\"∀\",\"fork\":\"⋔\",\"forkv\":\"⫙\",\"fpartint\":\"⨍\",\"frac12\":\"½\",\"frac13\":\"⅓\",\"frac14\":\"¼\",\"frac15\":\"⅕\",\"frac16\":\"⅙\",\"frac18\":\"⅛\",\"frac23\":\"⅔\",\"frac25\":\"⅖\",\"frac34\":\"¾\",\"frac35\":\"⅗\",\"frac38\":\"⅜\",\"frac45\":\"⅘\",\"frac56\":\"⅚\",\"frac58\":\"⅝\",\"frac78\":\"⅞\",\"frasl\":\"⁄\",\"frown\":\"⌢\",\"fscr\":\"𝒻\",\"gE\":\"≧\",\"gEl\":\"⪌\",\"gacute\":\"ǵ\",\"gamma\":\"γ\",\"gammad\":\"ϝ\",\"gap\":\"⪆\",\"gbreve\":\"ğ\",\"gcirc\":\"ĝ\",\"gcy\":\"г\",\"gdot\":\"ġ\",\"ge\":\"≥\",\"gel\":\"⋛\",\"geq\":\"≥\",\"geqq\":\"≧\",\"geqslant\":\"⩾\",\"ges\":\"⩾\",\"gescc\":\"⪩\",\"gesdot\":\"⪀\",\"gesdoto\":\"⪂\",\"gesdotol\":\"⪄\",\"gesl\":\"⋛︀\",\"gesles\":\"⪔\",\"gfr\":\"𝔤\",\"gg\":\"≫\",\"ggg\":\"⋙\",\"gimel\":\"ℷ\",\"gjcy\":\"ѓ\",\"gl\":\"≷\",\"glE\":\"⪒\",\"gla\":\"⪥\",\"glj\":\"⪤\",\"gnE\":\"≩\",\"gnap\":\"⪊\",\"gnapprox\":\"⪊\",\"gne\":\"⪈\",\"gneq\":\"⪈\",\"gneqq\":\"≩\",\"gnsim\":\"⋧\",\"gopf\":\"𝕘\",\"grave\":\"`\",\"gscr\":\"ℊ\",\"gsim\":\"≳\",\"gsime\":\"⪎\",\"gsiml\":\"⪐\",\"gt\":\">\",\"gtcc\":\"⪧\",\"gtcir\":\"⩺\",\"gtdot\":\"⋗\",\"gtlPar\":\"⦕\",\"gtquest\":\"⩼\",\"gtrapprox\":\"⪆\",\"gtrarr\":\"⥸\",\"gtrdot\":\"⋗\",\"gtreqless\":\"⋛\",\"gtreqqless\":\"⪌\",\"gtrless\":\"≷\",\"gtrsim\":\"≳\",\"gvertneqq\":\"≩︀\",\"gvnE\":\"≩︀\",\"hArr\":\"⇔\",\"hairsp\":\" \",\"half\":\"½\",\"hamilt\":\"ℋ\",\"hardcy\":\"ъ\",\"harr\":\"↔\",\"harrcir\":\"⥈\",\"harrw\":\"↭\",\"hbar\":\"ℏ\",\"hcirc\":\"ĥ\",\"hearts\":\"♥\",\"heartsuit\":\"♥\",\"hellip\":\"…\",\"hercon\":\"⊹\",\"hfr\":\"𝔥\",\"hksearow\":\"⤥\",\"hkswarow\":\"⤦\",\"hoarr\":\"⇿\",\"homtht\":\"∻\",\"hookleftarrow\":\"↩\",\"hookrightarrow\":\"↪\",\"hopf\":\"𝕙\",\"horbar\":\"―\",\"hscr\":\"𝒽\",\"hslash\":\"ℏ\",\"hstrok\":\"ħ\",\"hybull\":\"⁃\",\"hyphen\":\"‐\",\"iacute\":\"í\",\"ic\":\"⁣\",\"icirc\":\"î\",\"icy\":\"и\",\"iecy\":\"е\",\"iexcl\":\"¡\",\"iff\":\"⇔\",\"ifr\":\"𝔦\",\"igrave\":\"ì\",\"ii\":\"ⅈ\",\"iiiint\":\"⨌\",\"iiint\":\"∭\",\"iinfin\":\"⧜\",\"iiota\":\"℩\",\"ijlig\":\"ĳ\",\"imacr\":\"ī\",\"image\":\"ℑ\",\"imagline\":\"ℐ\",\"imagpart\":\"ℑ\",\"imath\":\"ı\",\"imof\":\"⊷\",\"imped\":\"Ƶ\",\"in\":\"∈\",\"incare\":\"℅\",\"infin\":\"∞\",\"infintie\":\"⧝\",\"inodot\":\"ı\",\"int\":\"∫\",\"intcal\":\"⊺\",\"integers\":\"ℤ\",\"intercal\":\"⊺\",\"intlarhk\":\"⨗\",\"intprod\":\"⨼\",\"iocy\":\"ё\",\"iogon\":\"į\",\"iopf\":\"𝕚\",\"iota\":\"ι\",\"iprod\":\"⨼\",\"iquest\":\"¿\",\"iscr\":\"𝒾\",\"isin\":\"∈\",\"isinE\":\"⋹\",\"isindot\":\"⋵\",\"isins\":\"⋴\",\"isinsv\":\"⋳\",\"isinv\":\"∈\",\"it\":\"⁢\",\"itilde\":\"ĩ\",\"iukcy\":\"і\",\"iuml\":\"ï\",\"jcirc\":\"ĵ\",\"jcy\":\"й\",\"jfr\":\"𝔧\",\"jmath\":\"ȷ\",\"jopf\":\"𝕛\",\"jscr\":\"𝒿\",\"jsercy\":\"ј\",\"jukcy\":\"є\",\"kappa\":\"κ\",\"kappav\":\"ϰ\",\"kcedil\":\"ķ\",\"kcy\":\"к\",\"kfr\":\"𝔨\",\"kgreen\":\"ĸ\",\"khcy\":\"х\",\"kjcy\":\"ќ\",\"kopf\":\"𝕜\",\"kscr\":\"𝓀\",\"lAarr\":\"⇚\",\"lArr\":\"⇐\",\"lAtail\":\"⤛\",\"lBarr\":\"⤎\",\"lE\":\"≦\",\"lEg\":\"⪋\",\"lHar\":\"⥢\",\"lacute\":\"ĺ\",\"laemptyv\":\"⦴\",\"lagran\":\"ℒ\",\"lambda\":\"λ\",\"lang\":\"⟨\",\"langd\":\"⦑\",\"langle\":\"⟨\",\"lap\":\"⪅\",\"laquo\":\"«\",\"larr\":\"←\",\"larrb\":\"⇤\",\"larrbfs\":\"⤟\",\"larrfs\":\"⤝\",\"larrhk\":\"↩\",\"larrlp\":\"↫\",\"larrpl\":\"⤹\",\"larrsim\":\"⥳\",\"larrtl\":\"↢\",\"lat\":\"⪫\",\"latail\":\"⤙\",\"late\":\"⪭\",\"lates\":\"⪭︀\",\"lbarr\":\"⤌\",\"lbbrk\":\"❲\",\"lbrace\":\"{\",\"lbrack\":\"[\",\"lbrke\":\"⦋\",\"lbrksld\":\"⦏\",\"lbrkslu\":\"⦍\",\"lcaron\":\"ľ\",\"lcedil\":\"ļ\",\"lceil\":\"⌈\",\"lcub\":\"{\",\"lcy\":\"л\",\"ldca\":\"⤶\",\"ldquo\":\"“\",\"ldquor\":\"„\",\"ldrdhar\":\"⥧\",\"ldrushar\":\"⥋\",\"ldsh\":\"↲\",\"le\":\"≤\",\"leftarrow\":\"←\",\"leftarrowtail\":\"↢\",\"leftharpoondown\":\"↽\",\"leftharpoonup\":\"↼\",\"leftleftarrows\":\"⇇\",\"leftrightarrow\":\"↔\",\"leftrightarrows\":\"⇆\",\"leftrightharpoons\":\"⇋\",\"leftrightsquigarrow\":\"↭\",\"leftthreetimes\":\"⋋\",\"leg\":\"⋚\",\"leq\":\"≤\",\"leqq\":\"≦\",\"leqslant\":\"⩽\",\"les\":\"⩽\",\"lescc\":\"⪨\",\"lesdot\":\"⩿\",\"lesdoto\":\"⪁\",\"lesdotor\":\"⪃\",\"lesg\":\"⋚︀\",\"lesges\":\"⪓\",\"lessapprox\":\"⪅\",\"lessdot\":\"⋖\",\"lesseqgtr\":\"⋚\",\"lesseqqgtr\":\"⪋\",\"lessgtr\":\"≶\",\"lesssim\":\"≲\",\"lfisht\":\"⥼\",\"lfloor\":\"⌊\",\"lfr\":\"𝔩\",\"lg\":\"≶\",\"lgE\":\"⪑\",\"lhard\":\"↽\",\"lharu\":\"↼\",\"lharul\":\"⥪\",\"lhblk\":\"▄\",\"ljcy\":\"љ\",\"ll\":\"≪\",\"llarr\":\"⇇\",\"llcorner\":\"⌞\",\"llhard\":\"⥫\",\"lltri\":\"◺\",\"lmidot\":\"ŀ\",\"lmoust\":\"⎰\",\"lmoustache\":\"⎰\",\"lnE\":\"≨\",\"lnap\":\"⪉\",\"lnapprox\":\"⪉\",\"lne\":\"⪇\",\"lneq\":\"⪇\",\"lneqq\":\"≨\",\"lnsim\":\"⋦\",\"loang\":\"⟬\",\"loarr\":\"⇽\",\"lobrk\":\"⟦\",\"longleftarrow\":\"⟵\",\"longleftrightarrow\":\"⟷\",\"longmapsto\":\"⟼\",\"longrightarrow\":\"⟶\",\"looparrowleft\":\"↫\",\"looparrowright\":\"↬\",\"lopar\":\"⦅\",\"lopf\":\"𝕝\",\"loplus\":\"⨭\",\"lotimes\":\"⨴\",\"lowast\":\"∗\",\"lowbar\":\"_\",\"loz\":\"◊\",\"lozenge\":\"◊\",\"lozf\":\"⧫\",\"lpar\":\"(\",\"lparlt\":\"⦓\",\"lrarr\":\"⇆\",\"lrcorner\":\"⌟\",\"lrhar\":\"⇋\",\"lrhard\":\"⥭\",\"lrm\":\"‎\",\"lrtri\":\"⊿\",\"lsaquo\":\"‹\",\"lscr\":\"𝓁\",\"lsh\":\"↰\",\"lsim\":\"≲\",\"lsime\":\"⪍\",\"lsimg\":\"⪏\",\"lsqb\":\"[\",\"lsquo\":\"‘\",\"lsquor\":\"‚\",\"lstrok\":\"ł\",\"lt\":\"<\",\"ltcc\":\"⪦\",\"ltcir\":\"⩹\",\"ltdot\":\"⋖\",\"lthree\":\"⋋\",\"ltimes\":\"⋉\",\"ltlarr\":\"⥶\",\"ltquest\":\"⩻\",\"ltrPar\":\"⦖\",\"ltri\":\"◃\",\"ltrie\":\"⊴\",\"ltrif\":\"◂\",\"lurdshar\":\"⥊\",\"luruhar\":\"⥦\",\"lvertneqq\":\"≨︀\",\"lvnE\":\"≨︀\",\"mDDot\":\"∺\",\"macr\":\"¯\",\"male\":\"♂\",\"malt\":\"✠\",\"maltese\":\"✠\",\"map\":\"↦\",\"mapsto\":\"↦\",\"mapstodown\":\"↧\",\"mapstoleft\":\"↤\",\"mapstoup\":\"↥\",\"marker\":\"▮\",\"mcomma\":\"⨩\",\"mcy\":\"м\",\"mdash\":\"—\",\"measuredangle\":\"∡\",\"mfr\":\"𝔪\",\"mho\":\"℧\",\"micro\":\"µ\",\"mid\":\"∣\",\"midast\":\"*\",\"midcir\":\"⫰\",\"middot\":\"·\",\"minus\":\"−\",\"minusb\":\"⊟\",\"minusd\":\"∸\",\"minusdu\":\"⨪\",\"mlcp\":\"⫛\",\"mldr\":\"…\",\"mnplus\":\"∓\",\"models\":\"⊧\",\"mopf\":\"𝕞\",\"mp\":\"∓\",\"mscr\":\"𝓂\",\"mstpos\":\"∾\",\"mu\":\"μ\",\"multimap\":\"⊸\",\"mumap\":\"⊸\",\"nGg\":\"⋙̸\",\"nGt\":\"≫⃒\",\"nGtv\":\"≫̸\",\"nLeftarrow\":\"⇍\",\"nLeftrightarrow\":\"⇎\",\"nLl\":\"⋘̸\",\"nLt\":\"≪⃒\",\"nLtv\":\"≪̸\",\"nRightarrow\":\"⇏\",\"nVDash\":\"⊯\",\"nVdash\":\"⊮\",\"nabla\":\"∇\",\"nacute\":\"ń\",\"nang\":\"∠⃒\",\"nap\":\"≉\",\"napE\":\"⩰̸\",\"napid\":\"≋̸\",\"napos\":\"ŉ\",\"napprox\":\"≉\",\"natur\":\"♮\",\"natural\":\"♮\",\"naturals\":\"ℕ\",\"nbsp\":\" \",\"nbump\":\"≎̸\",\"nbumpe\":\"≏̸\",\"ncap\":\"⩃\",\"ncaron\":\"ň\",\"ncedil\":\"ņ\",\"ncong\":\"≇\",\"ncongdot\":\"⩭̸\",\"ncup\":\"⩂\",\"ncy\":\"н\",\"ndash\":\"–\",\"ne\":\"≠\",\"neArr\":\"⇗\",\"nearhk\":\"⤤\",\"nearr\":\"↗\",\"nearrow\":\"↗\",\"nedot\":\"≐̸\",\"nequiv\":\"≢\",\"nesear\":\"⤨\",\"nesim\":\"≂̸\",\"nexist\":\"∄\",\"nexists\":\"∄\",\"nfr\":\"𝔫\",\"ngE\":\"≧̸\",\"nge\":\"≱\",\"ngeq\":\"≱\",\"ngeqq\":\"≧̸\",\"ngeqslant\":\"⩾̸\",\"nges\":\"⩾̸\",\"ngsim\":\"≵\",\"ngt\":\"≯\",\"ngtr\":\"≯\",\"nhArr\":\"⇎\",\"nharr\":\"↮\",\"nhpar\":\"⫲\",\"ni\":\"∋\",\"nis\":\"⋼\",\"nisd\":\"⋺\",\"niv\":\"∋\",\"njcy\":\"њ\",\"nlArr\":\"⇍\",\"nlE\":\"≦̸\",\"nlarr\":\"↚\",\"nldr\":\"‥\",\"nle\":\"≰\",\"nleftarrow\":\"↚\",\"nleftrightarrow\":\"↮\",\"nleq\":\"≰\",\"nleqq\":\"≦̸\",\"nleqslant\":\"⩽̸\",\"nles\":\"⩽̸\",\"nless\":\"≮\",\"nlsim\":\"≴\",\"nlt\":\"≮\",\"nltri\":\"⋪\",\"nltrie\":\"⋬\",\"nmid\":\"∤\",\"nopf\":\"𝕟\",\"not\":\"¬\",\"notin\":\"∉\",\"notinE\":\"⋹̸\",\"notindot\":\"⋵̸\",\"notinva\":\"∉\",\"notinvb\":\"⋷\",\"notinvc\":\"⋶\",\"notni\":\"∌\",\"notniva\":\"∌\",\"notnivb\":\"⋾\",\"notnivc\":\"⋽\",\"npar\":\"∦\",\"nparallel\":\"∦\",\"nparsl\":\"⫽⃥\",\"npart\":\"∂̸\",\"npolint\":\"⨔\",\"npr\":\"⊀\",\"nprcue\":\"⋠\",\"npre\":\"⪯̸\",\"nprec\":\"⊀\",\"npreceq\":\"⪯̸\",\"nrArr\":\"⇏\",\"nrarr\":\"↛\",\"nrarrc\":\"⤳̸\",\"nrarrw\":\"↝̸\",\"nrightarrow\":\"↛\",\"nrtri\":\"⋫\",\"nrtrie\":\"⋭\",\"nsc\":\"⊁\",\"nsccue\":\"⋡\",\"nsce\":\"⪰̸\",\"nscr\":\"𝓃\",\"nshortmid\":\"∤\",\"nshortparallel\":\"∦\",\"nsim\":\"≁\",\"nsime\":\"≄\",\"nsimeq\":\"≄\",\"nsmid\":\"∤\",\"nspar\":\"∦\",\"nsqsube\":\"⋢\",\"nsqsupe\":\"⋣\",\"nsub\":\"⊄\",\"nsubE\":\"⫅̸\",\"nsube\":\"⊈\",\"nsubset\":\"⊂⃒\",\"nsubseteq\":\"⊈\",\"nsubseteqq\":\"⫅̸\",\"nsucc\":\"⊁\",\"nsucceq\":\"⪰̸\",\"nsup\":\"⊅\",\"nsupE\":\"⫆̸\",\"nsupe\":\"⊉\",\"nsupset\":\"⊃⃒\",\"nsupseteq\":\"⊉\",\"nsupseteqq\":\"⫆̸\",\"ntgl\":\"≹\",\"ntilde\":\"ñ\",\"ntlg\":\"≸\",\"ntriangleleft\":\"⋪\",\"ntrianglelefteq\":\"⋬\",\"ntriangleright\":\"⋫\",\"ntrianglerighteq\":\"⋭\",\"nu\":\"ν\",\"num\":\"#\",\"numero\":\"№\",\"numsp\":\" \",\"nvDash\":\"⊭\",\"nvHarr\":\"⤄\",\"nvap\":\"≍⃒\",\"nvdash\":\"⊬\",\"nvge\":\"≥⃒\",\"nvgt\":\">⃒\",\"nvinfin\":\"⧞\",\"nvlArr\":\"⤂\",\"nvle\":\"≤⃒\",\"nvlt\":\"<⃒\",\"nvltrie\":\"⊴⃒\",\"nvrArr\":\"⤃\",\"nvrtrie\":\"⊵⃒\",\"nvsim\":\"∼⃒\",\"nwArr\":\"⇖\",\"nwarhk\":\"⤣\",\"nwarr\":\"↖\",\"nwarrow\":\"↖\",\"nwnear\":\"⤧\",\"oS\":\"Ⓢ\",\"oacute\":\"ó\",\"oast\":\"⊛\",\"ocir\":\"⊚\",\"ocirc\":\"ô\",\"ocy\":\"о\",\"odash\":\"⊝\",\"odblac\":\"ő\",\"odiv\":\"⨸\",\"odot\":\"⊙\",\"odsold\":\"⦼\",\"oelig\":\"œ\",\"ofcir\":\"⦿\",\"ofr\":\"𝔬\",\"ogon\":\"˛\",\"ograve\":\"ò\",\"ogt\":\"⧁\",\"ohbar\":\"⦵\",\"ohm\":\"Ω\",\"oint\":\"∮\",\"olarr\":\"↺\",\"olcir\":\"⦾\",\"olcross\":\"⦻\",\"oline\":\"‾\",\"olt\":\"⧀\",\"omacr\":\"ō\",\"omega\":\"ω\",\"omicron\":\"ο\",\"omid\":\"⦶\",\"ominus\":\"⊖\",\"oopf\":\"𝕠\",\"opar\":\"⦷\",\"operp\":\"⦹\",\"oplus\":\"⊕\",\"or\":\"∨\",\"orarr\":\"↻\",\"ord\":\"⩝\",\"order\":\"ℴ\",\"orderof\":\"ℴ\",\"ordf\":\"ª\",\"ordm\":\"º\",\"origof\":\"⊶\",\"oror\":\"⩖\",\"orslope\":\"⩗\",\"orv\":\"⩛\",\"oscr\":\"ℴ\",\"oslash\":\"ø\",\"osol\":\"⊘\",\"otilde\":\"õ\",\"otimes\":\"⊗\",\"otimesas\":\"⨶\",\"ouml\":\"ö\",\"ovbar\":\"⌽\",\"par\":\"∥\",\"para\":\"¶\",\"parallel\":\"∥\",\"parsim\":\"⫳\",\"parsl\":\"⫽\",\"part\":\"∂\",\"pcy\":\"п\",\"percnt\":\"%\",\"period\":\".\",\"permil\":\"‰\",\"perp\":\"⊥\",\"pertenk\":\"‱\",\"pfr\":\"𝔭\",\"phi\":\"φ\",\"phiv\":\"ϕ\",\"phmmat\":\"ℳ\",\"phone\":\"☎\",\"pi\":\"π\",\"pitchfork\":\"⋔\",\"piv\":\"ϖ\",\"planck\":\"ℏ\",\"planckh\":\"ℎ\",\"plankv\":\"ℏ\",\"plus\":\"+\",\"plusacir\":\"⨣\",\"plusb\":\"⊞\",\"pluscir\":\"⨢\",\"plusdo\":\"∔\",\"plusdu\":\"⨥\",\"pluse\":\"⩲\",\"plusmn\":\"±\",\"plussim\":\"⨦\",\"plustwo\":\"⨧\",\"pm\":\"±\",\"pointint\":\"⨕\",\"popf\":\"𝕡\",\"pound\":\"£\",\"pr\":\"≺\",\"prE\":\"⪳\",\"prap\":\"⪷\",\"prcue\":\"≼\",\"pre\":\"⪯\",\"prec\":\"≺\",\"precapprox\":\"⪷\",\"preccurlyeq\":\"≼\",\"preceq\":\"⪯\",\"precnapprox\":\"⪹\",\"precneqq\":\"⪵\",\"precnsim\":\"⋨\",\"precsim\":\"≾\",\"prime\":\"′\",\"primes\":\"ℙ\",\"prnE\":\"⪵\",\"prnap\":\"⪹\",\"prnsim\":\"⋨\",\"prod\":\"∏\",\"profalar\":\"⌮\",\"profline\":\"⌒\",\"profsurf\":\"⌓\",\"prop\":\"∝\",\"propto\":\"∝\",\"prsim\":\"≾\",\"prurel\":\"⊰\",\"pscr\":\"𝓅\",\"psi\":\"ψ\",\"puncsp\":\" \",\"qfr\":\"𝔮\",\"qint\":\"⨌\",\"qopf\":\"𝕢\",\"qprime\":\"⁗\",\"qscr\":\"𝓆\",\"quaternions\":\"ℍ\",\"quatint\":\"⨖\",\"quest\":\"?\",\"questeq\":\"≟\",\"quot\":\"\\\"\",\"rAarr\":\"⇛\",\"rArr\":\"⇒\",\"rAtail\":\"⤜\",\"rBarr\":\"⤏\",\"rHar\":\"⥤\",\"race\":\"∽̱\",\"racute\":\"ŕ\",\"radic\":\"√\",\"raemptyv\":\"⦳\",\"rang\":\"⟩\",\"rangd\":\"⦒\",\"range\":\"⦥\",\"rangle\":\"⟩\",\"raquo\":\"»\",\"rarr\":\"→\",\"rarrap\":\"⥵\",\"rarrb\":\"⇥\",\"rarrbfs\":\"⤠\",\"rarrc\":\"⤳\",\"rarrfs\":\"⤞\",\"rarrhk\":\"↪\",\"rarrlp\":\"↬\",\"rarrpl\":\"⥅\",\"rarrsim\":\"⥴\",\"rarrtl\":\"↣\",\"rarrw\":\"↝\",\"ratail\":\"⤚\",\"ratio\":\"∶\",\"rationals\":\"ℚ\",\"rbarr\":\"⤍\",\"rbbrk\":\"❳\",\"rbrace\":\"}\",\"rbrack\":\"]\",\"rbrke\":\"⦌\",\"rbrksld\":\"⦎\",\"rbrkslu\":\"⦐\",\"rcaron\":\"ř\",\"rcedil\":\"ŗ\",\"rceil\":\"⌉\",\"rcub\":\"}\",\"rcy\":\"р\",\"rdca\":\"⤷\",\"rdldhar\":\"⥩\",\"rdquo\":\"”\",\"rdquor\":\"”\",\"rdsh\":\"↳\",\"real\":\"ℜ\",\"realine\":\"ℛ\",\"realpart\":\"ℜ\",\"reals\":\"ℝ\",\"rect\":\"▭\",\"reg\":\"®\",\"rfisht\":\"⥽\",\"rfloor\":\"⌋\",\"rfr\":\"𝔯\",\"rhard\":\"⇁\",\"rharu\":\"⇀\",\"rharul\":\"⥬\",\"rho\":\"ρ\",\"rhov\":\"ϱ\",\"rightarrow\":\"→\",\"rightarrowtail\":\"↣\",\"rightharpoondown\":\"⇁\",\"rightharpoonup\":\"⇀\",\"rightleftarrows\":\"⇄\",\"rightleftharpoons\":\"⇌\",\"rightrightarrows\":\"⇉\",\"rightsquigarrow\":\"↝\",\"rightthreetimes\":\"⋌\",\"ring\":\"˚\",\"risingdotseq\":\"≓\",\"rlarr\":\"⇄\",\"rlhar\":\"⇌\",\"rlm\":\"‏\",\"rmoust\":\"⎱\",\"rmoustache\":\"⎱\",\"rnmid\":\"⫮\",\"roang\":\"⟭\",\"roarr\":\"⇾\",\"robrk\":\"⟧\",\"ropar\":\"⦆\",\"ropf\":\"𝕣\",\"roplus\":\"⨮\",\"rotimes\":\"⨵\",\"rpar\":\")\",\"rpargt\":\"⦔\",\"rppolint\":\"⨒\",\"rrarr\":\"⇉\",\"rsaquo\":\"›\",\"rscr\":\"𝓇\",\"rsh\":\"↱\",\"rsqb\":\"]\",\"rsquo\":\"’\",\"rsquor\":\"’\",\"rthree\":\"⋌\",\"rtimes\":\"⋊\",\"rtri\":\"▹\",\"rtrie\":\"⊵\",\"rtrif\":\"▸\",\"rtriltri\":\"⧎\",\"ruluhar\":\"⥨\",\"rx\":\"℞\",\"sacute\":\"ś\",\"sbquo\":\"‚\",\"sc\":\"≻\",\"scE\":\"⪴\",\"scap\":\"⪸\",\"scaron\":\"š\",\"sccue\":\"≽\",\"sce\":\"⪰\",\"scedil\":\"ş\",\"scirc\":\"ŝ\",\"scnE\":\"⪶\",\"scnap\":\"⪺\",\"scnsim\":\"⋩\",\"scpolint\":\"⨓\",\"scsim\":\"≿\",\"scy\":\"с\",\"sdot\":\"⋅\",\"sdotb\":\"⊡\",\"sdote\":\"⩦\",\"seArr\":\"⇘\",\"searhk\":\"⤥\",\"searr\":\"↘\",\"searrow\":\"↘\",\"sect\":\"§\",\"semi\":\";\",\"seswar\":\"⤩\",\"setminus\":\"∖\",\"setmn\":\"∖\",\"sext\":\"✶\",\"sfr\":\"𝔰\",\"sfrown\":\"⌢\",\"sharp\":\"♯\",\"shchcy\":\"щ\",\"shcy\":\"ш\",\"shortmid\":\"∣\",\"shortparallel\":\"∥\",\"shy\":\"­\",\"sigma\":\"σ\",\"sigmaf\":\"ς\",\"sigmav\":\"ς\",\"sim\":\"∼\",\"simdot\":\"⩪\",\"sime\":\"≃\",\"simeq\":\"≃\",\"simg\":\"⪞\",\"simgE\":\"⪠\",\"siml\":\"⪝\",\"simlE\":\"⪟\",\"simne\":\"≆\",\"simplus\":\"⨤\",\"simrarr\":\"⥲\",\"slarr\":\"←\",\"smallsetminus\":\"∖\",\"smashp\":\"⨳\",\"smeparsl\":\"⧤\",\"smid\":\"∣\",\"smile\":\"⌣\",\"smt\":\"⪪\",\"smte\":\"⪬\",\"smtes\":\"⪬︀\",\"softcy\":\"ь\",\"sol\":\"/\",\"solb\":\"⧄\",\"solbar\":\"⌿\",\"sopf\":\"𝕤\",\"spades\":\"♠\",\"spadesuit\":\"♠\",\"spar\":\"∥\",\"sqcap\":\"⊓\",\"sqcaps\":\"⊓︀\",\"sqcup\":\"⊔\",\"sqcups\":\"⊔︀\",\"sqsub\":\"⊏\",\"sqsube\":\"⊑\",\"sqsubset\":\"⊏\",\"sqsubseteq\":\"⊑\",\"sqsup\":\"⊐\",\"sqsupe\":\"⊒\",\"sqsupset\":\"⊐\",\"sqsupseteq\":\"⊒\",\"squ\":\"□\",\"square\":\"□\",\"squarf\":\"▪\",\"squf\":\"▪\",\"srarr\":\"→\",\"sscr\":\"𝓈\",\"ssetmn\":\"∖\",\"ssmile\":\"⌣\",\"sstarf\":\"⋆\",\"star\":\"☆\",\"starf\":\"★\",\"straightepsilon\":\"ϵ\",\"straightphi\":\"ϕ\",\"strns\":\"¯\",\"sub\":\"⊂\",\"subE\":\"⫅\",\"subdot\":\"⪽\",\"sube\":\"⊆\",\"subedot\":\"⫃\",\"submult\":\"⫁\",\"subnE\":\"⫋\",\"subne\":\"⊊\",\"subplus\":\"⪿\",\"subrarr\":\"⥹\",\"subset\":\"⊂\",\"subseteq\":\"⊆\",\"subseteqq\":\"⫅\",\"subsetneq\":\"⊊\",\"subsetneqq\":\"⫋\",\"subsim\":\"⫇\",\"subsub\":\"⫕\",\"subsup\":\"⫓\",\"succ\":\"≻\",\"succapprox\":\"⪸\",\"succcurlyeq\":\"≽\",\"succeq\":\"⪰\",\"succnapprox\":\"⪺\",\"succneqq\":\"⪶\",\"succnsim\":\"⋩\",\"succsim\":\"≿\",\"sum\":\"∑\",\"sung\":\"♪\",\"sup1\":\"¹\",\"sup2\":\"²\",\"sup3\":\"³\",\"sup\":\"⊃\",\"supE\":\"⫆\",\"supdot\":\"⪾\",\"supdsub\":\"⫘\",\"supe\":\"⊇\",\"supedot\":\"⫄\",\"suphsol\":\"⟉\",\"suphsub\":\"⫗\",\"suplarr\":\"⥻\",\"supmult\":\"⫂\",\"supnE\":\"⫌\",\"supne\":\"⊋\",\"supplus\":\"⫀\",\"supset\":\"⊃\",\"supseteq\":\"⊇\",\"supseteqq\":\"⫆\",\"supsetneq\":\"⊋\",\"supsetneqq\":\"⫌\",\"supsim\":\"⫈\",\"supsub\":\"⫔\",\"supsup\":\"⫖\",\"swArr\":\"⇙\",\"swarhk\":\"⤦\",\"swarr\":\"↙\",\"swarrow\":\"↙\",\"swnwar\":\"⤪\",\"szlig\":\"ß\",\"target\":\"⌖\",\"tau\":\"τ\",\"tbrk\":\"⎴\",\"tcaron\":\"ť\",\"tcedil\":\"ţ\",\"tcy\":\"т\",\"tdot\":\"⃛\",\"telrec\":\"⌕\",\"tfr\":\"𝔱\",\"there4\":\"∴\",\"therefore\":\"∴\",\"theta\":\"θ\",\"thetasym\":\"ϑ\",\"thetav\":\"ϑ\",\"thickapprox\":\"≈\",\"thicksim\":\"∼\",\"thinsp\":\" \",\"thkap\":\"≈\",\"thksim\":\"∼\",\"thorn\":\"þ\",\"tilde\":\"˜\",\"times\":\"×\",\"timesb\":\"⊠\",\"timesbar\":\"⨱\",\"timesd\":\"⨰\",\"tint\":\"∭\",\"toea\":\"⤨\",\"top\":\"⊤\",\"topbot\":\"⌶\",\"topcir\":\"⫱\",\"topf\":\"𝕥\",\"topfork\":\"⫚\",\"tosa\":\"⤩\",\"tprime\":\"‴\",\"trade\":\"™\",\"triangle\":\"▵\",\"triangledown\":\"▿\",\"triangleleft\":\"◃\",\"trianglelefteq\":\"⊴\",\"triangleq\":\"≜\",\"triangleright\":\"▹\",\"trianglerighteq\":\"⊵\",\"tridot\":\"◬\",\"trie\":\"≜\",\"triminus\":\"⨺\",\"triplus\":\"⨹\",\"trisb\":\"⧍\",\"tritime\":\"⨻\",\"trpezium\":\"⏢\",\"tscr\":\"𝓉\",\"tscy\":\"ц\",\"tshcy\":\"ћ\",\"tstrok\":\"ŧ\",\"twixt\":\"≬\",\"twoheadleftarrow\":\"↞\",\"twoheadrightarrow\":\"↠\",\"uArr\":\"⇑\",\"uHar\":\"⥣\",\"uacute\":\"ú\",\"uarr\":\"↑\",\"ubrcy\":\"ў\",\"ubreve\":\"ŭ\",\"ucirc\":\"û\",\"ucy\":\"у\",\"udarr\":\"⇅\",\"udblac\":\"ű\",\"udhar\":\"⥮\",\"ufisht\":\"⥾\",\"ufr\":\"𝔲\",\"ugrave\":\"ù\",\"uharl\":\"↿\",\"uharr\":\"↾\",\"uhblk\":\"▀\",\"ulcorn\":\"⌜\",\"ulcorner\":\"⌜\",\"ulcrop\":\"⌏\",\"ultri\":\"◸\",\"umacr\":\"ū\",\"uml\":\"¨\",\"uogon\":\"ų\",\"uopf\":\"𝕦\",\"uparrow\":\"↑\",\"updownarrow\":\"↕\",\"upharpoonleft\":\"↿\",\"upharpoonright\":\"↾\",\"uplus\":\"⊎\",\"upsi\":\"υ\",\"upsih\":\"ϒ\",\"upsilon\":\"υ\",\"upuparrows\":\"⇈\",\"urcorn\":\"⌝\",\"urcorner\":\"⌝\",\"urcrop\":\"⌎\",\"uring\":\"ů\",\"urtri\":\"◹\",\"uscr\":\"𝓊\",\"utdot\":\"⋰\",\"utilde\":\"ũ\",\"utri\":\"▵\",\"utrif\":\"▴\",\"uuarr\":\"⇈\",\"uuml\":\"ü\",\"uwangle\":\"⦧\",\"vArr\":\"⇕\",\"vBar\":\"⫨\",\"vBarv\":\"⫩\",\"vDash\":\"⊨\",\"vangrt\":\"⦜\",\"varepsilon\":\"ϵ\",\"varkappa\":\"ϰ\",\"varnothing\":\"∅\",\"varphi\":\"ϕ\",\"varpi\":\"ϖ\",\"varpropto\":\"∝\",\"varr\":\"↕\",\"varrho\":\"ϱ\",\"varsigma\":\"ς\",\"varsubsetneq\":\"⊊︀\",\"varsubsetneqq\":\"⫋︀\",\"varsupsetneq\":\"⊋︀\",\"varsupsetneqq\":\"⫌︀\",\"vartheta\":\"ϑ\",\"vartriangleleft\":\"⊲\",\"vartriangleright\":\"⊳\",\"vcy\":\"в\",\"vdash\":\"⊢\",\"vee\":\"∨\",\"veebar\":\"⊻\",\"veeeq\":\"≚\",\"vellip\":\"⋮\",\"verbar\":\"|\",\"vert\":\"|\",\"vfr\":\"𝔳\",\"vltri\":\"⊲\",\"vnsub\":\"⊂⃒\",\"vnsup\":\"⊃⃒\",\"vopf\":\"𝕧\",\"vprop\":\"∝\",\"vrtri\":\"⊳\",\"vscr\":\"𝓋\",\"vsubnE\":\"⫋︀\",\"vsubne\":\"⊊︀\",\"vsupnE\":\"⫌︀\",\"vsupne\":\"⊋︀\",\"vzigzag\":\"⦚\",\"wcirc\":\"ŵ\",\"wedbar\":\"⩟\",\"wedge\":\"∧\",\"wedgeq\":\"≙\",\"weierp\":\"℘\",\"wfr\":\"𝔴\",\"wopf\":\"𝕨\",\"wp\":\"℘\",\"wr\":\"≀\",\"wreath\":\"≀\",\"wscr\":\"𝓌\",\"xcap\":\"⋂\",\"xcirc\":\"◯\",\"xcup\":\"⋃\",\"xdtri\":\"▽\",\"xfr\":\"𝔵\",\"xhArr\":\"⟺\",\"xharr\":\"⟷\",\"xi\":\"ξ\",\"xlArr\":\"⟸\",\"xlarr\":\"⟵\",\"xmap\":\"⟼\",\"xnis\":\"⋻\",\"xodot\":\"⨀\",\"xopf\":\"𝕩\",\"xoplus\":\"⨁\",\"xotime\":\"⨂\",\"xrArr\":\"⟹\",\"xrarr\":\"⟶\",\"xscr\":\"𝓍\",\"xsqcup\":\"⨆\",\"xuplus\":\"⨄\",\"xutri\":\"△\",\"xvee\":\"⋁\",\"xwedge\":\"⋀\",\"yacute\":\"ý\",\"yacy\":\"я\",\"ycirc\":\"ŷ\",\"ycy\":\"ы\",\"yen\":\"¥\",\"yfr\":\"𝔶\",\"yicy\":\"ї\",\"yopf\":\"𝕪\",\"yscr\":\"𝓎\",\"yucy\":\"ю\",\"yuml\":\"ÿ\",\"zacute\":\"ź\",\"zcaron\":\"ž\",\"zcy\":\"з\",\"zdot\":\"ż\",\"zeetrf\":\"ℨ\",\"zeta\":\"ζ\",\"zfr\":\"𝔷\",\"zhcy\":\"ж\",\"zigrarr\":\"⇝\",\"zopf\":\"𝕫\",\"zscr\":\"𝓏\",\"zwj\":\"‍\",\"zwnj\":\"‌\"}");
+export JsValue characterEntities = object {
+  AElig: "Æ",
+  AMP: "&",
+  Aacute: "Á",
+  Abreve: "Ă",
+  Acirc: "Â",
+  Acy: "А",
+  Afr: "𝔄",
+  Agrave: "À",
+  Alpha: "Α",
+  Amacr: "Ā",
+  And: "⩓",
+  Aogon: "Ą",
+  Aopf: "𝔸",
+  ApplyFunction: "⁡",
+  Aring: "Å",
+  Ascr: "𝒜",
+  Assign: "≔",
+  Atilde: "Ã",
+  Auml: "Ä",
+  Backslash: "∖",
+  Barv: "⫧",
+  Barwed: "⌆",
+  Bcy: "Б",
+  Because: "∵",
+  Bernoullis: "ℬ",
+  Beta: "Β",
+  Bfr: "𝔅",
+  Bopf: "𝔹",
+  Breve: "˘",
+  Bscr: "ℬ",
+  Bumpeq: "≎",
+  CHcy: "Ч",
+  COPY: "©",
+  Cacute: "Ć",
+  Cap: "⋒",
+  CapitalDifferentialD: "ⅅ",
+  Cayleys: "ℭ",
+  Ccaron: "Č",
+  Ccedil: "Ç",
+  Ccirc: "Ĉ",
+  Cconint: "∰",
+  Cdot: "Ċ",
+  Cedilla: "¸",
+  CenterDot: "·",
+  Cfr: "ℭ",
+  Chi: "Χ",
+  CircleDot: "⊙",
+  CircleMinus: "⊖",
+  CirclePlus: "⊕",
+  CircleTimes: "⊗",
+  ClockwiseContourIntegral: "∲",
+  CloseCurlyDoubleQuote: "”",
+  CloseCurlyQuote: "’",
+  Colon: "∷",
+  Colone: "⩴",
+  Congruent: "≡",
+  Conint: "∯",
+  ContourIntegral: "∮",
+  Copf: "ℂ",
+  Coproduct: "∐",
+  CounterClockwiseContourIntegral: "∳",
+  Cross: "⨯",
+  Cscr: "𝒞",
+  Cup: "⋓",
+  CupCap: "≍",
+  DD: "ⅅ",
+  DDotrahd: "⤑",
+  DJcy: "Ђ",
+  DScy: "Ѕ",
+  DZcy: "Џ",
+  Dagger: "‡",
+  Darr: "↡",
+  Dashv: "⫤",
+  Dcaron: "Ď",
+  Dcy: "Д",
+  Del: "∇",
+  Delta: "Δ",
+  Dfr: "𝔇",
+  DiacriticalAcute: "´",
+  DiacriticalDot: "˙",
+  DiacriticalDoubleAcute: "˝",
+  DiacriticalGrave: "`",
+  DiacriticalTilde: "˜",
+  Diamond: "⋄",
+  DifferentialD: "ⅆ",
+  Dopf: "𝔻",
+  Dot: "¨",
+  DotDot: "⃜",
+  DotEqual: "≐",
+  DoubleContourIntegral: "∯",
+  DoubleDot: "¨",
+  DoubleDownArrow: "⇓",
+  DoubleLeftArrow: "⇐",
+  DoubleLeftRightArrow: "⇔",
+  DoubleLeftTee: "⫤",
+  DoubleLongLeftArrow: "⟸",
+  DoubleLongLeftRightArrow: "⟺",
+  DoubleLongRightArrow: "⟹",
+  DoubleRightArrow: "⇒",
+  DoubleRightTee: "⊨",
+  DoubleUpArrow: "⇑",
+  DoubleUpDownArrow: "⇕",
+  DoubleVerticalBar: "∥",
+  DownArrow: "↓",
+  DownArrowBar: "⤓",
+  DownArrowUpArrow: "⇵",
+  DownBreve: "̑",
+  DownLeftRightVector: "⥐",
+  DownLeftTeeVector: "⥞",
+  DownLeftVector: "↽",
+  DownLeftVectorBar: "⥖",
+  DownRightTeeVector: "⥟",
+  DownRightVector: "⇁",
+  DownRightVectorBar: "⥗",
+  DownTee: "⊤",
+  DownTeeArrow: "↧",
+  Downarrow: "⇓",
+  Dscr: "𝒟",
+  Dstrok: "Đ",
+  ENG: "Ŋ",
+  ETH: "Ð",
+  Eacute: "É",
+  Ecaron: "Ě",
+  Ecirc: "Ê",
+  Ecy: "Э",
+  Edot: "Ė",
+  Efr: "𝔈",
+  Egrave: "È",
+  Element: "∈",
+  Emacr: "Ē",
+  EmptySmallSquare: "◻",
+  EmptyVerySmallSquare: "▫",
+  Eogon: "Ę",
+  Eopf: "𝔼",
+  Epsilon: "Ε",
+  Equal: "⩵",
+  EqualTilde: "≂",
+  Equilibrium: "⇌",
+  Escr: "ℰ",
+  Esim: "⩳",
+  Eta: "Η",
+  Euml: "Ë",
+  Exists: "∃",
+  ExponentialE: "ⅇ",
+  Fcy: "Ф",
+  Ffr: "𝔉",
+  FilledSmallSquare: "◼",
+  FilledVerySmallSquare: "▪",
+  Fopf: "𝔽",
+  ForAll: "∀",
+  Fouriertrf: "ℱ",
+  Fscr: "ℱ",
+  GJcy: "Ѓ",
+  GT: ">",
+  Gamma: "Γ",
+  Gammad: "Ϝ",
+  Gbreve: "Ğ",
+  Gcedil: "Ģ",
+  Gcirc: "Ĝ",
+  Gcy: "Г",
+  Gdot: "Ġ",
+  Gfr: "𝔊",
+  Gg: "⋙",
+  Gopf: "𝔾",
+  GreaterEqual: "≥",
+  GreaterEqualLess: "⋛",
+  GreaterFullEqual: "≧",
+  GreaterGreater: "⪢",
+  GreaterLess: "≷",
+  GreaterSlantEqual: "⩾",
+  GreaterTilde: "≳",
+  Gscr: "𝒢",
+  Gt: "≫",
+  HARDcy: "Ъ",
+  Hacek: "ˇ",
+  Hat: "^",
+  Hcirc: "Ĥ",
+  Hfr: "ℌ",
+  HilbertSpace: "ℋ",
+  Hopf: "ℍ",
+  HorizontalLine: "─",
+  Hscr: "ℋ",
+  Hstrok: "Ħ",
+  HumpDownHump: "≎",
+  HumpEqual: "≏",
+  IEcy: "Е",
+  IJlig: "Ĳ",
+  IOcy: "Ё",
+  Iacute: "Í",
+  Icirc: "Î",
+  Icy: "И",
+  Idot: "İ",
+  Ifr: "ℑ",
+  Igrave: "Ì",
+  Im: "ℑ",
+  Imacr: "Ī",
+  ImaginaryI: "ⅈ",
+  Implies: "⇒",
+  Int: "∬",
+  Integral: "∫",
+  Intersection: "⋂",
+  InvisibleComma: "⁣",
+  InvisibleTimes: "⁢",
+  Iogon: "Į",
+  Iopf: "𝕀",
+  Iota: "Ι",
+  Iscr: "ℐ",
+  Itilde: "Ĩ",
+  Iukcy: "І",
+  Iuml: "Ï",
+  Jcirc: "Ĵ",
+  Jcy: "Й",
+  Jfr: "𝔍",
+  Jopf: "𝕁",
+  Jscr: "𝒥",
+  Jsercy: "Ј",
+  Jukcy: "Є",
+  KHcy: "Х",
+  KJcy: "Ќ",
+  Kappa: "Κ",
+  Kcedil: "Ķ",
+  Kcy: "К",
+  Kfr: "𝔎",
+  Kopf: "𝕂",
+  Kscr: "𝒦",
+  LJcy: "Љ",
+  LT: "<",
+  Lacute: "Ĺ",
+  Lambda: "Λ",
+  Lang: "⟪",
+  Laplacetrf: "ℒ",
+  Larr: "↞",
+  Lcaron: "Ľ",
+  Lcedil: "Ļ",
+  Lcy: "Л",
+  LeftAngleBracket: "⟨",
+  LeftArrow: "←",
+  LeftArrowBar: "⇤",
+  LeftArrowRightArrow: "⇆",
+  LeftCeiling: "⌈",
+  LeftDoubleBracket: "⟦",
+  LeftDownTeeVector: "⥡",
+  LeftDownVector: "⇃",
+  LeftDownVectorBar: "⥙",
+  LeftFloor: "⌊",
+  LeftRightArrow: "↔",
+  LeftRightVector: "⥎",
+  LeftTee: "⊣",
+  LeftTeeArrow: "↤",
+  LeftTeeVector: "⥚",
+  LeftTriangle: "⊲",
+  LeftTriangleBar: "⧏",
+  LeftTriangleEqual: "⊴",
+  LeftUpDownVector: "⥑",
+  LeftUpTeeVector: "⥠",
+  LeftUpVector: "↿",
+  LeftUpVectorBar: "⥘",
+  LeftVector: "↼",
+  LeftVectorBar: "⥒",
+  Leftarrow: "⇐",
+  Leftrightarrow: "⇔",
+  LessEqualGreater: "⋚",
+  LessFullEqual: "≦",
+  LessGreater: "≶",
+  LessLess: "⪡",
+  LessSlantEqual: "⩽",
+  LessTilde: "≲",
+  Lfr: "𝔏",
+  Ll: "⋘",
+  Lleftarrow: "⇚",
+  Lmidot: "Ŀ",
+  LongLeftArrow: "⟵",
+  LongLeftRightArrow: "⟷",
+  LongRightArrow: "⟶",
+  Longleftarrow: "⟸",
+  Longleftrightarrow: "⟺",
+  Longrightarrow: "⟹",
+  Lopf: "𝕃",
+  LowerLeftArrow: "↙",
+  LowerRightArrow: "↘",
+  Lscr: "ℒ",
+  Lsh: "↰",
+  Lstrok: "Ł",
+  Lt: "≪",
+  Map: "⤅",
+  Mcy: "М",
+  MediumSpace: " ",
+  Mellintrf: "ℳ",
+  Mfr: "𝔐",
+  MinusPlus: "∓",
+  Mopf: "𝕄",
+  Mscr: "ℳ",
+  Mu: "Μ",
+  NJcy: "Њ",
+  Nacute: "Ń",
+  Ncaron: "Ň",
+  Ncedil: "Ņ",
+  Ncy: "Н",
+  NegativeMediumSpace: "​",
+  NegativeThickSpace: "​",
+  NegativeThinSpace: "​",
+  NegativeVeryThinSpace: "​",
+  NestedGreaterGreater: "≫",
+  NestedLessLess: "≪",
+  NewLine: "\n",
+  Nfr: "𝔑",
+  NoBreak: "⁠",
+  NonBreakingSpace: " ",
+  Nopf: "ℕ",
+  Not: "⫬",
+  NotCongruent: "≢",
+  NotCupCap: "≭",
+  NotDoubleVerticalBar: "∦",
+  NotElement: "∉",
+  NotEqual: "≠",
+  NotEqualTilde: "≂̸",
+  NotExists: "∄",
+  NotGreater: "≯",
+  NotGreaterEqual: "≱",
+  NotGreaterFullEqual: "≧̸",
+  NotGreaterGreater: "≫̸",
+  NotGreaterLess: "≹",
+  NotGreaterSlantEqual: "⩾̸",
+  NotGreaterTilde: "≵",
+  NotHumpDownHump: "≎̸",
+  NotHumpEqual: "≏̸",
+  NotLeftTriangle: "⋪",
+  NotLeftTriangleBar: "⧏̸",
+  NotLeftTriangleEqual: "⋬",
+  NotLess: "≮",
+  NotLessEqual: "≰",
+  NotLessGreater: "≸",
+  NotLessLess: "≪̸",
+  NotLessSlantEqual: "⩽̸",
+  NotLessTilde: "≴",
+  NotNestedGreaterGreater: "⪢̸",
+  NotNestedLessLess: "⪡̸",
+  NotPrecedes: "⊀",
+  NotPrecedesEqual: "⪯̸",
+  NotPrecedesSlantEqual: "⋠",
+  NotReverseElement: "∌",
+  NotRightTriangle: "⋫",
+  NotRightTriangleBar: "⧐̸",
+  NotRightTriangleEqual: "⋭",
+  NotSquareSubset: "⊏̸",
+  NotSquareSubsetEqual: "⋢",
+  NotSquareSuperset: "⊐̸",
+  NotSquareSupersetEqual: "⋣",
+  NotSubset: "⊂⃒",
+  NotSubsetEqual: "⊈",
+  NotSucceeds: "⊁",
+  NotSucceedsEqual: "⪰̸",
+  NotSucceedsSlantEqual: "⋡",
+  NotSucceedsTilde: "≿̸",
+  NotSuperset: "⊃⃒",
+  NotSupersetEqual: "⊉",
+  NotTilde: "≁",
+  NotTildeEqual: "≄",
+  NotTildeFullEqual: "≇",
+  NotTildeTilde: "≉",
+  NotVerticalBar: "∤",
+  Nscr: "𝒩",
+  Ntilde: "Ñ",
+  Nu: "Ν",
+  OElig: "Œ",
+  Oacute: "Ó",
+  Ocirc: "Ô",
+  Ocy: "О",
+  Odblac: "Ő",
+  Ofr: "𝔒",
+  Ograve: "Ò",
+  Omacr: "Ō",
+  Omega: "Ω",
+  Omicron: "Ο",
+  Oopf: "𝕆",
+  OpenCurlyDoubleQuote: "“",
+  OpenCurlyQuote: "‘",
+  Or: "⩔",
+  Oscr: "𝒪",
+  Oslash: "Ø",
+  Otilde: "Õ",
+  Otimes: "⨷",
+  Ouml: "Ö",
+  OverBar: "‾",
+  OverBrace: "⏞",
+  OverBracket: "⎴",
+  OverParenthesis: "⏜",
+  PartialD: "∂",
+  Pcy: "П",
+  Pfr: "𝔓",
+  Phi: "Φ",
+  Pi: "Π",
+  PlusMinus: "±",
+  Poincareplane: "ℌ",
+  Popf: "ℙ",
+  Pr: "⪻",
+  Precedes: "≺",
+  PrecedesEqual: "⪯",
+  PrecedesSlantEqual: "≼",
+  PrecedesTilde: "≾",
+  Prime: "″",
+  Product: "∏",
+  Proportion: "∷",
+  Proportional: "∝",
+  Pscr: "𝒫",
+  Psi: "Ψ",
+  QUOT: "\"",
+  Qfr: "𝔔",
+  Qopf: "ℚ",
+  Qscr: "𝒬",
+  RBarr: "⤐",
+  REG: "®",
+  Racute: "Ŕ",
+  Rang: "⟫",
+  Rarr: "↠",
+  Rarrtl: "⤖",
+  Rcaron: "Ř",
+  Rcedil: "Ŗ",
+  Rcy: "Р",
+  Re: "ℜ",
+  ReverseElement: "∋",
+  ReverseEquilibrium: "⇋",
+  ReverseUpEquilibrium: "⥯",
+  Rfr: "ℜ",
+  Rho: "Ρ",
+  RightAngleBracket: "⟩",
+  RightArrow: "→",
+  RightArrowBar: "⇥",
+  RightArrowLeftArrow: "⇄",
+  RightCeiling: "⌉",
+  RightDoubleBracket: "⟧",
+  RightDownTeeVector: "⥝",
+  RightDownVector: "⇂",
+  RightDownVectorBar: "⥕",
+  RightFloor: "⌋",
+  RightTee: "⊢",
+  RightTeeArrow: "↦",
+  RightTeeVector: "⥛",
+  RightTriangle: "⊳",
+  RightTriangleBar: "⧐",
+  RightTriangleEqual: "⊵",
+  RightUpDownVector: "⥏",
+  RightUpTeeVector: "⥜",
+  RightUpVector: "↾",
+  RightUpVectorBar: "⥔",
+  RightVector: "⇀",
+  RightVectorBar: "⥓",
+  Rightarrow: "⇒",
+  Ropf: "ℝ",
+  RoundImplies: "⥰",
+  Rrightarrow: "⇛",
+  Rscr: "ℛ",
+  Rsh: "↱",
+  RuleDelayed: "⧴",
+  SHCHcy: "Щ",
+  SHcy: "Ш",
+  SOFTcy: "Ь",
+  Sacute: "Ś",
+  Sc: "⪼",
+  Scaron: "Š",
+  Scedil: "Ş",
+  Scirc: "Ŝ",
+  Scy: "С",
+  Sfr: "𝔖",
+  ShortDownArrow: "↓",
+  ShortLeftArrow: "←",
+  ShortRightArrow: "→",
+  ShortUpArrow: "↑",
+  Sigma: "Σ",
+  SmallCircle: "∘",
+  Sopf: "𝕊",
+  Sqrt: "√",
+  Square: "□",
+  SquareIntersection: "⊓",
+  SquareSubset: "⊏",
+  SquareSubsetEqual: "⊑",
+  SquareSuperset: "⊐",
+  SquareSupersetEqual: "⊒",
+  SquareUnion: "⊔",
+  Sscr: "𝒮",
+  Star: "⋆",
+  Sub: "⋐",
+  Subset: "⋐",
+  SubsetEqual: "⊆",
+  Succeeds: "≻",
+  SucceedsEqual: "⪰",
+  SucceedsSlantEqual: "≽",
+  SucceedsTilde: "≿",
+  SuchThat: "∋",
+  Sum: "∑",
+  Sup: "⋑",
+  Superset: "⊃",
+  SupersetEqual: "⊇",
+  Supset: "⋑",
+  THORN: "Þ",
+  TRADE: "™",
+  TSHcy: "Ћ",
+  TScy: "Ц",
+  Tab: "\t",
+  Tau: "Τ",
+  Tcaron: "Ť",
+  Tcedil: "Ţ",
+  Tcy: "Т",
+  Tfr: "𝔗",
+  Therefore: "∴",
+  Theta: "Θ",
+  ThickSpace: "  ",
+  ThinSpace: " ",
+  Tilde: "∼",
+  TildeEqual: "≃",
+  TildeFullEqual: "≅",
+  TildeTilde: "≈",
+  Topf: "𝕋",
+  TripleDot: "⃛",
+  Tscr: "𝒯",
+  Tstrok: "Ŧ",
+  Uacute: "Ú",
+  Uarr: "↟",
+  Uarrocir: "⥉",
+  Ubrcy: "Ў",
+  Ubreve: "Ŭ",
+  Ucirc: "Û",
+  Ucy: "У",
+  Udblac: "Ű",
+  Ufr: "𝔘",
+  Ugrave: "Ù",
+  Umacr: "Ū",
+  UnderBar: "_",
+  UnderBrace: "⏟",
+  UnderBracket: "⎵",
+  UnderParenthesis: "⏝",
+  Union: "⋃",
+  UnionPlus: "⊎",
+  Uogon: "Ų",
+  Uopf: "𝕌",
+  UpArrow: "↑",
+  UpArrowBar: "⤒",
+  UpArrowDownArrow: "⇅",
+  UpDownArrow: "↕",
+  UpEquilibrium: "⥮",
+  UpTee: "⊥",
+  UpTeeArrow: "↥",
+  Uparrow: "⇑",
+  Updownarrow: "⇕",
+  UpperLeftArrow: "↖",
+  UpperRightArrow: "↗",
+  Upsi: "ϒ",
+  Upsilon: "Υ",
+  Uring: "Ů",
+  Uscr: "𝒰",
+  Utilde: "Ũ",
+  Uuml: "Ü",
+  VDash: "⊫",
+  Vbar: "⫫",
+  Vcy: "В",
+  Vdash: "⊩",
+  Vdashl: "⫦",
+  Vee: "⋁",
+  Verbar: "‖",
+  Vert: "‖",
+  VerticalBar: "∣",
+  VerticalLine: "|",
+  VerticalSeparator: "❘",
+  VerticalTilde: "≀",
+  VeryThinSpace: " ",
+  Vfr: "𝔙",
+  Vopf: "𝕍",
+  Vscr: "𝒱",
+  Vvdash: "⊪",
+  Wcirc: "Ŵ",
+  Wedge: "⋀",
+  Wfr: "𝔚",
+  Wopf: "𝕎",
+  Wscr: "𝒲",
+  Xfr: "𝔛",
+  Xi: "Ξ",
+  Xopf: "𝕏",
+  Xscr: "𝒳",
+  YAcy: "Я",
+  YIcy: "Ї",
+  YUcy: "Ю",
+  Yacute: "Ý",
+  Ycirc: "Ŷ",
+  Ycy: "Ы",
+  Yfr: "𝔜",
+  Yopf: "𝕐",
+  Yscr: "𝒴",
+  Yuml: "Ÿ",
+  ZHcy: "Ж",
+  Zacute: "Ź",
+  Zcaron: "Ž",
+  Zcy: "З",
+  Zdot: "Ż",
+  ZeroWidthSpace: "​",
+  Zeta: "Ζ",
+  Zfr: "ℨ",
+  Zopf: "ℤ",
+  Zscr: "𝒵",
+  aacute: "á",
+  abreve: "ă",
+  ac: "∾",
+  acE: "∾̳",
+  acd: "∿",
+  acirc: "â",
+  acute: "´",
+  acy: "а",
+  aelig: "æ",
+  af: "⁡",
+  afr: "𝔞",
+  agrave: "à",
+  alefsym: "ℵ",
+  aleph: "ℵ",
+  alpha: "α",
+  amacr: "ā",
+  amalg: "⨿",
+  amp: "&",
+  and: "∧",
+  andand: "⩕",
+  andd: "⩜",
+  andslope: "⩘",
+  andv: "⩚",
+  ang: "∠",
+  ange: "⦤",
+  angle: "∠",
+  angmsd: "∡",
+  angmsdaa: "⦨",
+  angmsdab: "⦩",
+  angmsdac: "⦪",
+  angmsdad: "⦫",
+  angmsdae: "⦬",
+  angmsdaf: "⦭",
+  angmsdag: "⦮",
+  angmsdah: "⦯",
+  angrt: "∟",
+  angrtvb: "⊾",
+  angrtvbd: "⦝",
+  angsph: "∢",
+  angst: "Å",
+  angzarr: "⍼",
+  aogon: "ą",
+  aopf: "𝕒",
+  ap: "≈",
+  apE: "⩰",
+  apacir: "⩯",
+  ape: "≊",
+  apid: "≋",
+  apos: "'",
+  approx: "≈",
+  approxeq: "≊",
+  aring: "å",
+  ascr: "𝒶",
+  ast: "*",
+  asymp: "≈",
+  asympeq: "≍",
+  atilde: "ã",
+  auml: "ä",
+  awconint: "∳",
+  awint: "⨑",
+  bNot: "⫭",
+  backcong: "≌",
+  backepsilon: "϶",
+  backprime: "‵",
+  backsim: "∽",
+  backsimeq: "⋍",
+  barvee: "⊽",
+  barwed: "⌅",
+  barwedge: "⌅",
+  bbrk: "⎵",
+  bbrktbrk: "⎶",
+  bcong: "≌",
+  bcy: "б",
+  bdquo: "„",
+  becaus: "∵",
+  because: "∵",
+  bemptyv: "⦰",
+  bepsi: "϶",
+  bernou: "ℬ",
+  beta: "β",
+  beth: "ℶ",
+  between: "≬",
+  bfr: "𝔟",
+  bigcap: "⋂",
+  bigcirc: "◯",
+  bigcup: "⋃",
+  bigodot: "⨀",
+  bigoplus: "⨁",
+  bigotimes: "⨂",
+  bigsqcup: "⨆",
+  bigstar: "★",
+  bigtriangledown: "▽",
+  bigtriangleup: "△",
+  biguplus: "⨄",
+  bigvee: "⋁",
+  bigwedge: "⋀",
+  bkarow: "⤍",
+  blacklozenge: "⧫",
+  blacksquare: "▪",
+  blacktriangle: "▴",
+  blacktriangledown: "▾",
+  blacktriangleleft: "◂",
+  blacktriangleright: "▸",
+  blank: "␣",
+  blk12: "▒",
+  blk14: "░",
+  blk34: "▓",
+  block: "█",
+  bne: "=⃥",
+  bnequiv: "≡⃥",
+  bnot: "⌐",
+  bopf: "𝕓",
+  bot: "⊥",
+  bottom: "⊥",
+  bowtie: "⋈",
+  boxDL: "╗",
+  boxDR: "╔",
+  boxDl: "╖",
+  boxDr: "╓",
+  boxH: "═",
+  boxHD: "╦",
+  boxHU: "╩",
+  boxHd: "╤",
+  boxHu: "╧",
+  boxUL: "╝",
+  boxUR: "╚",
+  boxUl: "╜",
+  boxUr: "╙",
+  boxV: "║",
+  boxVH: "╬",
+  boxVL: "╣",
+  boxVR: "╠",
+  boxVh: "╫",
+  boxVl: "╢",
+  boxVr: "╟",
+  boxbox: "⧉",
+  boxdL: "╕",
+  boxdR: "╒",
+  boxdl: "┐",
+  boxdr: "┌",
+  boxh: "─",
+  boxhD: "╥",
+  boxhU: "╨",
+  boxhd: "┬",
+  boxhu: "┴",
+  boxminus: "⊟",
+  boxplus: "⊞",
+  boxtimes: "⊠",
+  boxuL: "╛",
+  boxuR: "╘",
+  boxul: "┘",
+  boxur: "└",
+  boxv: "│",
+  boxvH: "╪",
+  boxvL: "╡",
+  boxvR: "╞",
+  boxvh: "┼",
+  boxvl: "┤",
+  boxvr: "├",
+  bprime: "‵",
+  breve: "˘",
+  brvbar: "¦",
+  bscr: "𝒷",
+  bsemi: "⁏",
+  bsim: "∽",
+  bsime: "⋍",
+  bsol: "\\",
+  bsolb: "⧅",
+  bsolhsub: "⟈",
+  bull: "•",
+  bullet: "•",
+  bump: "≎",
+  bumpE: "⪮",
+  bumpe: "≏",
+  bumpeq: "≏",
+  cacute: "ć",
+  cap: "∩",
+  capand: "⩄",
+  capbrcup: "⩉",
+  capcap: "⩋",
+  capcup: "⩇",
+  capdot: "⩀",
+  caps: "∩︀",
+  caret: "⁁",
+  caron: "ˇ",
+  ccaps: "⩍",
+  ccaron: "č",
+  ccedil: "ç",
+  ccirc: "ĉ",
+  ccups: "⩌",
+  ccupssm: "⩐",
+  cdot: "ċ",
+  cedil: "¸",
+  cemptyv: "⦲",
+  cent: "¢",
+  centerdot: "·",
+  cfr: "𝔠",
+  chcy: "ч",
+  check: "✓",
+  checkmark: "✓",
+  chi: "χ",
+  cir: "○",
+  cirE: "⧃",
+  circ: "ˆ",
+  circeq: "≗",
+  circlearrowleft: "↺",
+  circlearrowright: "↻",
+  circledR: "®",
+  circledS: "Ⓢ",
+  circledast: "⊛",
+  circledcirc: "⊚",
+  circleddash: "⊝",
+  cire: "≗",
+  cirfnint: "⨐",
+  cirmid: "⫯",
+  cirscir: "⧂",
+  clubs: "♣",
+  clubsuit: "♣",
+  colon: ":",
+  colone: "≔",
+  coloneq: "≔",
+  comma: ",",
+  commat: "@",
+  comp: "∁",
+  compfn: "∘",
+  complement: "∁",
+  complexes: "ℂ",
+  cong: "≅",
+  congdot: "⩭",
+  conint: "∮",
+  copf: "𝕔",
+  coprod: "∐",
+  copy: "©",
+  copysr: "℗",
+  crarr: "↵",
+  cross: "✗",
+  cscr: "𝒸",
+  csub: "⫏",
+  csube: "⫑",
+  csup: "⫐",
+  csupe: "⫒",
+  ctdot: "⋯",
+  cudarrl: "⤸",
+  cudarrr: "⤵",
+  cuepr: "⋞",
+  cuesc: "⋟",
+  cularr: "↶",
+  cularrp: "⤽",
+  cup: "∪",
+  cupbrcap: "⩈",
+  cupcap: "⩆",
+  cupcup: "⩊",
+  cupdot: "⊍",
+  cupor: "⩅",
+  cups: "∪︀",
+  curarr: "↷",
+  curarrm: "⤼",
+  curlyeqprec: "⋞",
+  curlyeqsucc: "⋟",
+  curlyvee: "⋎",
+  curlywedge: "⋏",
+  curren: "¤",
+  curvearrowleft: "↶",
+  curvearrowright: "↷",
+  cuvee: "⋎",
+  cuwed: "⋏",
+  cwconint: "∲",
+  cwint: "∱",
+  cylcty: "⌭",
+  dArr: "⇓",
+  dHar: "⥥",
+  dagger: "†",
+  daleth: "ℸ",
+  darr: "↓",
+  dash: "‐",
+  dashv: "⊣",
+  dbkarow: "⤏",
+  dblac: "˝",
+  dcaron: "ď",
+  dcy: "д",
+  dd: "ⅆ",
+  ddagger: "‡",
+  ddarr: "⇊",
+  ddotseq: "⩷",
+  deg: "°",
+  delta: "δ",
+  demptyv: "⦱",
+  dfisht: "⥿",
+  dfr: "𝔡",
+  dharl: "⇃",
+  dharr: "⇂",
+  diam: "⋄",
+  diamond: "⋄",
+  diamondsuit: "♦",
+  diams: "♦",
+  die: "¨",
+  digamma: "ϝ",
+  disin: "⋲",
+  div: "÷",
+  divide: "÷",
+  divideontimes: "⋇",
+  divonx: "⋇",
+  djcy: "ђ",
+  dlcorn: "⌞",
+  dlcrop: "⌍",
+  dollar: "$",
+  dopf: "𝕕",
+  dot: "˙",
+  doteq: "≐",
+  doteqdot: "≑",
+  dotminus: "∸",
+  dotplus: "∔",
+  dotsquare: "⊡",
+  doublebarwedge: "⌆",
+  downarrow: "↓",
+  downdownarrows: "⇊",
+  downharpoonleft: "⇃",
+  downharpoonright: "⇂",
+  drbkarow: "⤐",
+  drcorn: "⌟",
+  drcrop: "⌌",
+  dscr: "𝒹",
+  dscy: "ѕ",
+  dsol: "⧶",
+  dstrok: "đ",
+  dtdot: "⋱",
+  dtri: "▿",
+  dtrif: "▾",
+  duarr: "⇵",
+  duhar: "⥯",
+  dwangle: "⦦",
+  dzcy: "џ",
+  dzigrarr: "⟿",
+  eDDot: "⩷",
+  eDot: "≑",
+  eacute: "é",
+  easter: "⩮",
+  ecaron: "ě",
+  ecir: "≖",
+  ecirc: "ê",
+  ecolon: "≕",
+  ecy: "э",
+  edot: "ė",
+  ee: "ⅇ",
+  efDot: "≒",
+  efr: "𝔢",
+  eg: "⪚",
+  egrave: "è",
+  egs: "⪖",
+  egsdot: "⪘",
+  el: "⪙",
+  elinters: "⏧",
+  ell: "ℓ",
+  els: "⪕",
+  elsdot: "⪗",
+  emacr: "ē",
+  empty: "∅",
+  emptyset: "∅",
+  emptyv: "∅",
+  emsp13: " ",
+  emsp14: " ",
+  emsp: " ",
+  eng: "ŋ",
+  ensp: " ",
+  eogon: "ę",
+  eopf: "𝕖",
+  epar: "⋕",
+  eparsl: "⧣",
+  eplus: "⩱",
+  epsi: "ε",
+  epsilon: "ε",
+  epsiv: "ϵ",
+  eqcirc: "≖",
+  eqcolon: "≕",
+  eqsim: "≂",
+  eqslantgtr: "⪖",
+  eqslantless: "⪕",
+  equals: "=",
+  equest: "≟",
+  equiv: "≡",
+  equivDD: "⩸",
+  eqvparsl: "⧥",
+  erDot: "≓",
+  erarr: "⥱",
+  escr: "ℯ",
+  esdot: "≐",
+  esim: "≂",
+  eta: "η",
+  eth: "ð",
+  euml: "ë",
+  euro: "€",
+  excl: "!",
+  exist: "∃",
+  expectation: "ℰ",
+  exponentiale: "ⅇ",
+  fallingdotseq: "≒",
+  fcy: "ф",
+  female: "♀",
+  ffilig: "ﬃ",
+  fflig: "ﬀ",
+  ffllig: "ﬄ",
+  ffr: "𝔣",
+  filig: "ﬁ",
+  fjlig: "fj",
+  flat: "♭",
+  fllig: "ﬂ",
+  fltns: "▱",
+  fnof: "ƒ",
+  fopf: "𝕗",
+  forall: "∀",
+  fork: "⋔",
+  forkv: "⫙",
+  fpartint: "⨍",
+  frac12: "½",
+  frac13: "⅓",
+  frac14: "¼",
+  frac15: "⅕",
+  frac16: "⅙",
+  frac18: "⅛",
+  frac23: "⅔",
+  frac25: "⅖",
+  frac34: "¾",
+  frac35: "⅗",
+  frac38: "⅜",
+  frac45: "⅘",
+  frac56: "⅚",
+  frac58: "⅝",
+  frac78: "⅞",
+  frasl: "⁄",
+  frown: "⌢",
+  fscr: "𝒻",
+  gE: "≧",
+  gEl: "⪌",
+  gacute: "ǵ",
+  gamma: "γ",
+  gammad: "ϝ",
+  gap: "⪆",
+  gbreve: "ğ",
+  gcirc: "ĝ",
+  gcy: "г",
+  gdot: "ġ",
+  ge: "≥",
+  gel: "⋛",
+  geq: "≥",
+  geqq: "≧",
+  geqslant: "⩾",
+  ges: "⩾",
+  gescc: "⪩",
+  gesdot: "⪀",
+  gesdoto: "⪂",
+  gesdotol: "⪄",
+  gesl: "⋛︀",
+  gesles: "⪔",
+  gfr: "𝔤",
+  gg: "≫",
+  ggg: "⋙",
+  gimel: "ℷ",
+  gjcy: "ѓ",
+  gl: "≷",
+  glE: "⪒",
+  gla: "⪥",
+  glj: "⪤",
+  gnE: "≩",
+  gnap: "⪊",
+  gnapprox: "⪊",
+  gne: "⪈",
+  gneq: "⪈",
+  gneqq: "≩",
+  gnsim: "⋧",
+  gopf: "𝕘",
+  grave: "`",
+  gscr: "ℊ",
+  gsim: "≳",
+  gsime: "⪎",
+  gsiml: "⪐",
+  gt: ">",
+  gtcc: "⪧",
+  gtcir: "⩺",
+  gtdot: "⋗",
+  gtlPar: "⦕",
+  gtquest: "⩼",
+  gtrapprox: "⪆",
+  gtrarr: "⥸",
+  gtrdot: "⋗",
+  gtreqless: "⋛",
+  gtreqqless: "⪌",
+  gtrless: "≷",
+  gtrsim: "≳",
+  gvertneqq: "≩︀",
+  gvnE: "≩︀",
+  hArr: "⇔",
+  hairsp: " ",
+  half: "½",
+  hamilt: "ℋ",
+  hardcy: "ъ",
+  harr: "↔",
+  harrcir: "⥈",
+  harrw: "↭",
+  hbar: "ℏ",
+  hcirc: "ĥ",
+  hearts: "♥",
+  heartsuit: "♥",
+  hellip: "…",
+  hercon: "⊹",
+  hfr: "𝔥",
+  hksearow: "⤥",
+  hkswarow: "⤦",
+  hoarr: "⇿",
+  homtht: "∻",
+  hookleftarrow: "↩",
+  hookrightarrow: "↪",
+  hopf: "𝕙",
+  horbar: "―",
+  hscr: "𝒽",
+  hslash: "ℏ",
+  hstrok: "ħ",
+  hybull: "⁃",
+  hyphen: "‐",
+  iacute: "í",
+  ic: "⁣",
+  icirc: "î",
+  icy: "и",
+  iecy: "е",
+  iexcl: "¡",
+  iff: "⇔",
+  ifr: "𝔦",
+  igrave: "ì",
+  ii: "ⅈ",
+  iiiint: "⨌",
+  iiint: "∭",
+  iinfin: "⧜",
+  iiota: "℩",
+  ijlig: "ĳ",
+  imacr: "ī",
+  image: "ℑ",
+  imagline: "ℐ",
+  imagpart: "ℑ",
+  imath: "ı",
+  imof: "⊷",
+  imped: "Ƶ",
+  in: "∈",
+  incare: "℅",
+  infin: "∞",
+  infintie: "⧝",
+  inodot: "ı",
+  int: "∫",
+  intcal: "⊺",
+  integers: "ℤ",
+  intercal: "⊺",
+  intlarhk: "⨗",
+  intprod: "⨼",
+  iocy: "ё",
+  iogon: "į",
+  iopf: "𝕚",
+  iota: "ι",
+  iprod: "⨼",
+  iquest: "¿",
+  iscr: "𝒾",
+  isin: "∈",
+  isinE: "⋹",
+  isindot: "⋵",
+  isins: "⋴",
+  isinsv: "⋳",
+  isinv: "∈",
+  it: "⁢",
+  itilde: "ĩ",
+  iukcy: "і",
+  iuml: "ï",
+  jcirc: "ĵ",
+  jcy: "й",
+  jfr: "𝔧",
+  jmath: "ȷ",
+  jopf: "𝕛",
+  jscr: "𝒿",
+  jsercy: "ј",
+  jukcy: "є",
+  kappa: "κ",
+  kappav: "ϰ",
+  kcedil: "ķ",
+  kcy: "к",
+  kfr: "𝔨",
+  kgreen: "ĸ",
+  khcy: "х",
+  kjcy: "ќ",
+  kopf: "𝕜",
+  kscr: "𝓀",
+  lAarr: "⇚",
+  lArr: "⇐",
+  lAtail: "⤛",
+  lBarr: "⤎",
+  lE: "≦",
+  lEg: "⪋",
+  lHar: "⥢",
+  lacute: "ĺ",
+  laemptyv: "⦴",
+  lagran: "ℒ",
+  lambda: "λ",
+  lang: "⟨",
+  langd: "⦑",
+  langle: "⟨",
+  lap: "⪅",
+  laquo: "«",
+  larr: "←",
+  larrb: "⇤",
+  larrbfs: "⤟",
+  larrfs: "⤝",
+  larrhk: "↩",
+  larrlp: "↫",
+  larrpl: "⤹",
+  larrsim: "⥳",
+  larrtl: "↢",
+  lat: "⪫",
+  latail: "⤙",
+  late: "⪭",
+  lates: "⪭︀",
+  lbarr: "⤌",
+  lbbrk: "❲",
+  lbrace: "{",
+  lbrack: "[",
+  lbrke: "⦋",
+  lbrksld: "⦏",
+  lbrkslu: "⦍",
+  lcaron: "ľ",
+  lcedil: "ļ",
+  lceil: "⌈",
+  lcub: "{",
+  lcy: "л",
+  ldca: "⤶",
+  ldquo: "“",
+  ldquor: "„",
+  ldrdhar: "⥧",
+  ldrushar: "⥋",
+  ldsh: "↲",
+  le: "≤",
+  leftarrow: "←",
+  leftarrowtail: "↢",
+  leftharpoondown: "↽",
+  leftharpoonup: "↼",
+  leftleftarrows: "⇇",
+  leftrightarrow: "↔",
+  leftrightarrows: "⇆",
+  leftrightharpoons: "⇋",
+  leftrightsquigarrow: "↭",
+  leftthreetimes: "⋋",
+  leg: "⋚",
+  leq: "≤",
+  leqq: "≦",
+  leqslant: "⩽",
+  les: "⩽",
+  lescc: "⪨",
+  lesdot: "⩿",
+  lesdoto: "⪁",
+  lesdotor: "⪃",
+  lesg: "⋚︀",
+  lesges: "⪓",
+  lessapprox: "⪅",
+  lessdot: "⋖",
+  lesseqgtr: "⋚",
+  lesseqqgtr: "⪋",
+  lessgtr: "≶",
+  lesssim: "≲",
+  lfisht: "⥼",
+  lfloor: "⌊",
+  lfr: "𝔩",
+  lg: "≶",
+  lgE: "⪑",
+  lhard: "↽",
+  lharu: "↼",
+  lharul: "⥪",
+  lhblk: "▄",
+  ljcy: "љ",
+  ll: "≪",
+  llarr: "⇇",
+  llcorner: "⌞",
+  llhard: "⥫",
+  lltri: "◺",
+  lmidot: "ŀ",
+  lmoust: "⎰",
+  lmoustache: "⎰",
+  lnE: "≨",
+  lnap: "⪉",
+  lnapprox: "⪉",
+  lne: "⪇",
+  lneq: "⪇",
+  lneqq: "≨",
+  lnsim: "⋦",
+  loang: "⟬",
+  loarr: "⇽",
+  lobrk: "⟦",
+  longleftarrow: "⟵",
+  longleftrightarrow: "⟷",
+  longmapsto: "⟼",
+  longrightarrow: "⟶",
+  looparrowleft: "↫",
+  looparrowright: "↬",
+  lopar: "⦅",
+  lopf: "𝕝",
+  loplus: "⨭",
+  lotimes: "⨴",
+  lowast: "∗",
+  lowbar: "_",
+  loz: "◊",
+  lozenge: "◊",
+  lozf: "⧫",
+  lpar: "(",
+  lparlt: "⦓",
+  lrarr: "⇆",
+  lrcorner: "⌟",
+  lrhar: "⇋",
+  lrhard: "⥭",
+  lrm: "‎",
+  lrtri: "⊿",
+  lsaquo: "‹",
+  lscr: "𝓁",
+  lsh: "↰",
+  lsim: "≲",
+  lsime: "⪍",
+  lsimg: "⪏",
+  lsqb: "[",
+  lsquo: "‘",
+  lsquor: "‚",
+  lstrok: "ł",
+  lt: "<",
+  ltcc: "⪦",
+  ltcir: "⩹",
+  ltdot: "⋖",
+  lthree: "⋋",
+  ltimes: "⋉",
+  ltlarr: "⥶",
+  ltquest: "⩻",
+  ltrPar: "⦖",
+  ltri: "◃",
+  ltrie: "⊴",
+  ltrif: "◂",
+  lurdshar: "⥊",
+  luruhar: "⥦",
+  lvertneqq: "≨︀",
+  lvnE: "≨︀",
+  mDDot: "∺",
+  macr: "¯",
+  male: "♂",
+  malt: "✠",
+  maltese: "✠",
+  map: "↦",
+  mapsto: "↦",
+  mapstodown: "↧",
+  mapstoleft: "↤",
+  mapstoup: "↥",
+  marker: "▮",
+  mcomma: "⨩",
+  mcy: "м",
+  mdash: "—",
+  measuredangle: "∡",
+  mfr: "𝔪",
+  mho: "℧",
+  micro: "µ",
+  mid: "∣",
+  midast: "*",
+  midcir: "⫰",
+  middot: "·",
+  minus: "−",
+  minusb: "⊟",
+  minusd: "∸",
+  minusdu: "⨪",
+  mlcp: "⫛",
+  mldr: "…",
+  mnplus: "∓",
+  models: "⊧",
+  mopf: "𝕞",
+  mp: "∓",
+  mscr: "𝓂",
+  mstpos: "∾",
+  mu: "μ",
+  multimap: "⊸",
+  mumap: "⊸",
+  nGg: "⋙̸",
+  nGt: "≫⃒",
+  nGtv: "≫̸",
+  nLeftarrow: "⇍",
+  nLeftrightarrow: "⇎",
+  nLl: "⋘̸",
+  nLt: "≪⃒",
+  nLtv: "≪̸",
+  nRightarrow: "⇏",
+  nVDash: "⊯",
+  nVdash: "⊮",
+  nabla: "∇",
+  nacute: "ń",
+  nang: "∠⃒",
+  nap: "≉",
+  napE: "⩰̸",
+  napid: "≋̸",
+  napos: "ŉ",
+  napprox: "≉",
+  natur: "♮",
+  natural: "♮",
+  naturals: "ℕ",
+  nbsp: " ",
+  nbump: "≎̸",
+  nbumpe: "≏̸",
+  ncap: "⩃",
+  ncaron: "ň",
+  ncedil: "ņ",
+  ncong: "≇",
+  ncongdot: "⩭̸",
+  ncup: "⩂",
+  ncy: "н",
+  ndash: "–",
+  ne: "≠",
+  neArr: "⇗",
+  nearhk: "⤤",
+  nearr: "↗",
+  nearrow: "↗",
+  nedot: "≐̸",
+  nequiv: "≢",
+  nesear: "⤨",
+  nesim: "≂̸",
+  nexist: "∄",
+  nexists: "∄",
+  nfr: "𝔫",
+  ngE: "≧̸",
+  nge: "≱",
+  ngeq: "≱",
+  ngeqq: "≧̸",
+  ngeqslant: "⩾̸",
+  nges: "⩾̸",
+  ngsim: "≵",
+  ngt: "≯",
+  ngtr: "≯",
+  nhArr: "⇎",
+  nharr: "↮",
+  nhpar: "⫲",
+  ni: "∋",
+  nis: "⋼",
+  nisd: "⋺",
+  niv: "∋",
+  njcy: "њ",
+  nlArr: "⇍",
+  nlE: "≦̸",
+  nlarr: "↚",
+  nldr: "‥",
+  nle: "≰",
+  nleftarrow: "↚",
+  nleftrightarrow: "↮",
+  nleq: "≰",
+  nleqq: "≦̸",
+  nleqslant: "⩽̸",
+  nles: "⩽̸",
+  nless: "≮",
+  nlsim: "≴",
+  nlt: "≮",
+  nltri: "⋪",
+  nltrie: "⋬",
+  nmid: "∤",
+  nopf: "𝕟",
+  not: "¬",
+  notin: "∉",
+  notinE: "⋹̸",
+  notindot: "⋵̸",
+  notinva: "∉",
+  notinvb: "⋷",
+  notinvc: "⋶",
+  notni: "∌",
+  notniva: "∌",
+  notnivb: "⋾",
+  notnivc: "⋽",
+  npar: "∦",
+  nparallel: "∦",
+  nparsl: "⫽⃥",
+  npart: "∂̸",
+  npolint: "⨔",
+  npr: "⊀",
+  nprcue: "⋠",
+  npre: "⪯̸",
+  nprec: "⊀",
+  npreceq: "⪯̸",
+  nrArr: "⇏",
+  nrarr: "↛",
+  nrarrc: "⤳̸",
+  nrarrw: "↝̸",
+  nrightarrow: "↛",
+  nrtri: "⋫",
+  nrtrie: "⋭",
+  nsc: "⊁",
+  nsccue: "⋡",
+  nsce: "⪰̸",
+  nscr: "𝓃",
+  nshortmid: "∤",
+  nshortparallel: "∦",
+  nsim: "≁",
+  nsime: "≄",
+  nsimeq: "≄",
+  nsmid: "∤",
+  nspar: "∦",
+  nsqsube: "⋢",
+  nsqsupe: "⋣",
+  nsub: "⊄",
+  nsubE: "⫅̸",
+  nsube: "⊈",
+  nsubset: "⊂⃒",
+  nsubseteq: "⊈",
+  nsubseteqq: "⫅̸",
+  nsucc: "⊁",
+  nsucceq: "⪰̸",
+  nsup: "⊅",
+  nsupE: "⫆̸",
+  nsupe: "⊉",
+  nsupset: "⊃⃒",
+  nsupseteq: "⊉",
+  nsupseteqq: "⫆̸",
+  ntgl: "≹",
+  ntilde: "ñ",
+  ntlg: "≸",
+  ntriangleleft: "⋪",
+  ntrianglelefteq: "⋬",
+  ntriangleright: "⋫",
+  ntrianglerighteq: "⋭",
+  nu: "ν",
+  num: "#",
+  numero: "№",
+  numsp: " ",
+  nvDash: "⊭",
+  nvHarr: "⤄",
+  nvap: "≍⃒",
+  nvdash: "⊬",
+  nvge: "≥⃒",
+  nvgt: ">⃒",
+  nvinfin: "⧞",
+  nvlArr: "⤂",
+  nvle: "≤⃒",
+  nvlt: "<⃒",
+  nvltrie: "⊴⃒",
+  nvrArr: "⤃",
+  nvrtrie: "⊵⃒",
+  nvsim: "∼⃒",
+  nwArr: "⇖",
+  nwarhk: "⤣",
+  nwarr: "↖",
+  nwarrow: "↖",
+  nwnear: "⤧",
+  oS: "Ⓢ",
+  oacute: "ó",
+  oast: "⊛",
+  ocir: "⊚",
+  ocirc: "ô",
+  ocy: "о",
+  odash: "⊝",
+  odblac: "ő",
+  odiv: "⨸",
+  odot: "⊙",
+  odsold: "⦼",
+  oelig: "œ",
+  ofcir: "⦿",
+  ofr: "𝔬",
+  ogon: "˛",
+  ograve: "ò",
+  ogt: "⧁",
+  ohbar: "⦵",
+  ohm: "Ω",
+  oint: "∮",
+  olarr: "↺",
+  olcir: "⦾",
+  olcross: "⦻",
+  oline: "‾",
+  olt: "⧀",
+  omacr: "ō",
+  omega: "ω",
+  omicron: "ο",
+  omid: "⦶",
+  ominus: "⊖",
+  oopf: "𝕠",
+  opar: "⦷",
+  operp: "⦹",
+  oplus: "⊕",
+  or: "∨",
+  orarr: "↻",
+  ord: "⩝",
+  order: "ℴ",
+  orderof: "ℴ",
+  ordf: "ª",
+  ordm: "º",
+  origof: "⊶",
+  oror: "⩖",
+  orslope: "⩗",
+  orv: "⩛",
+  oscr: "ℴ",
+  oslash: "ø",
+  osol: "⊘",
+  otilde: "õ",
+  otimes: "⊗",
+  otimesas: "⨶",
+  ouml: "ö",
+  ovbar: "⌽",
+  par: "∥",
+  para: "¶",
+  parallel: "∥",
+  parsim: "⫳",
+  parsl: "⫽",
+  part: "∂",
+  pcy: "п",
+  percnt: "%",
+  period: ".",
+  permil: "‰",
+  perp: "⊥",
+  pertenk: "‱",
+  pfr: "𝔭",
+  phi: "φ",
+  phiv: "ϕ",
+  phmmat: "ℳ",
+  phone: "☎",
+  pi: "π",
+  pitchfork: "⋔",
+  piv: "ϖ",
+  planck: "ℏ",
+  planckh: "ℎ",
+  plankv: "ℏ",
+  plus: "+",
+  plusacir: "⨣",
+  plusb: "⊞",
+  pluscir: "⨢",
+  plusdo: "∔",
+  plusdu: "⨥",
+  pluse: "⩲",
+  plusmn: "±",
+  plussim: "⨦",
+  plustwo: "⨧",
+  pm: "±",
+  pointint: "⨕",
+  popf: "𝕡",
+  pound: "£",
+  pr: "≺",
+  prE: "⪳",
+  prap: "⪷",
+  prcue: "≼",
+  pre: "⪯",
+  prec: "≺",
+  precapprox: "⪷",
+  preccurlyeq: "≼",
+  preceq: "⪯",
+  precnapprox: "⪹",
+  precneqq: "⪵",
+  precnsim: "⋨",
+  precsim: "≾",
+  prime: "′",
+  primes: "ℙ",
+  prnE: "⪵",
+  prnap: "⪹",
+  prnsim: "⋨",
+  prod: "∏",
+  profalar: "⌮",
+  profline: "⌒",
+  profsurf: "⌓",
+  prop: "∝",
+  propto: "∝",
+  prsim: "≾",
+  prurel: "⊰",
+  pscr: "𝓅",
+  psi: "ψ",
+  puncsp: " ",
+  qfr: "𝔮",
+  qint: "⨌",
+  qopf: "𝕢",
+  qprime: "⁗",
+  qscr: "𝓆",
+  quaternions: "ℍ",
+  quatint: "⨖",
+  quest: "?",
+  questeq: "≟",
+  quot: "\"",
+  rAarr: "⇛",
+  rArr: "⇒",
+  rAtail: "⤜",
+  rBarr: "⤏",
+  rHar: "⥤",
+  race: "∽̱",
+  racute: "ŕ",
+  radic: "√",
+  raemptyv: "⦳",
+  rang: "⟩",
+  rangd: "⦒",
+  range: "⦥",
+  rangle: "⟩",
+  raquo: "»",
+  rarr: "→",
+  rarrap: "⥵",
+  rarrb: "⇥",
+  rarrbfs: "⤠",
+  rarrc: "⤳",
+  rarrfs: "⤞",
+  rarrhk: "↪",
+  rarrlp: "↬",
+  rarrpl: "⥅",
+  rarrsim: "⥴",
+  rarrtl: "↣",
+  rarrw: "↝",
+  ratail: "⤚",
+  ratio: "∶",
+  rationals: "ℚ",
+  rbarr: "⤍",
+  rbbrk: "❳",
+  rbrace: "}",
+  rbrack: "]",
+  rbrke: "⦌",
+  rbrksld: "⦎",
+  rbrkslu: "⦐",
+  rcaron: "ř",
+  rcedil: "ŗ",
+  rceil: "⌉",
+  rcub: "}",
+  rcy: "р",
+  rdca: "⤷",
+  rdldhar: "⥩",
+  rdquo: "”",
+  rdquor: "”",
+  rdsh: "↳",
+  real: "ℜ",
+  realine: "ℛ",
+  realpart: "ℜ",
+  reals: "ℝ",
+  rect: "▭",
+  reg: "®",
+  rfisht: "⥽",
+  rfloor: "⌋",
+  rfr: "𝔯",
+  rhard: "⇁",
+  rharu: "⇀",
+  rharul: "⥬",
+  rho: "ρ",
+  rhov: "ϱ",
+  rightarrow: "→",
+  rightarrowtail: "↣",
+  rightharpoondown: "⇁",
+  rightharpoonup: "⇀",
+  rightleftarrows: "⇄",
+  rightleftharpoons: "⇌",
+  rightrightarrows: "⇉",
+  rightsquigarrow: "↝",
+  rightthreetimes: "⋌",
+  ring: "˚",
+  risingdotseq: "≓",
+  rlarr: "⇄",
+  rlhar: "⇌",
+  rlm: "‏",
+  rmoust: "⎱",
+  rmoustache: "⎱",
+  rnmid: "⫮",
+  roang: "⟭",
+  roarr: "⇾",
+  robrk: "⟧",
+  ropar: "⦆",
+  ropf: "𝕣",
+  roplus: "⨮",
+  rotimes: "⨵",
+  rpar: ")",
+  rpargt: "⦔",
+  rppolint: "⨒",
+  rrarr: "⇉",
+  rsaquo: "›",
+  rscr: "𝓇",
+  rsh: "↱",
+  rsqb: "]",
+  rsquo: "’",
+  rsquor: "’",
+  rthree: "⋌",
+  rtimes: "⋊",
+  rtri: "▹",
+  rtrie: "⊵",
+  rtrif: "▸",
+  rtriltri: "⧎",
+  ruluhar: "⥨",
+  rx: "℞",
+  sacute: "ś",
+  sbquo: "‚",
+  sc: "≻",
+  scE: "⪴",
+  scap: "⪸",
+  scaron: "š",
+  sccue: "≽",
+  sce: "⪰",
+  scedil: "ş",
+  scirc: "ŝ",
+  scnE: "⪶",
+  scnap: "⪺",
+  scnsim: "⋩",
+  scpolint: "⨓",
+  scsim: "≿",
+  scy: "с",
+  sdot: "⋅",
+  sdotb: "⊡",
+  sdote: "⩦",
+  seArr: "⇘",
+  searhk: "⤥",
+  searr: "↘",
+  searrow: "↘",
+  sect: "§",
+  semi: ";",
+  seswar: "⤩",
+  setminus: "∖",
+  setmn: "∖",
+  sext: "✶",
+  sfr: "𝔰",
+  sfrown: "⌢",
+  sharp: "♯",
+  shchcy: "щ",
+  shcy: "ш",
+  shortmid: "∣",
+  shortparallel: "∥",
+  shy: "­",
+  sigma: "σ",
+  sigmaf: "ς",
+  sigmav: "ς",
+  sim: "∼",
+  simdot: "⩪",
+  sime: "≃",
+  simeq: "≃",
+  simg: "⪞",
+  simgE: "⪠",
+  siml: "⪝",
+  simlE: "⪟",
+  simne: "≆",
+  simplus: "⨤",
+  simrarr: "⥲",
+  slarr: "←",
+  smallsetminus: "∖",
+  smashp: "⨳",
+  smeparsl: "⧤",
+  smid: "∣",
+  smile: "⌣",
+  smt: "⪪",
+  smte: "⪬",
+  smtes: "⪬︀",
+  softcy: "ь",
+  sol: "/",
+  solb: "⧄",
+  solbar: "⌿",
+  sopf: "𝕤",
+  spades: "♠",
+  spadesuit: "♠",
+  spar: "∥",
+  sqcap: "⊓",
+  sqcaps: "⊓︀",
+  sqcup: "⊔",
+  sqcups: "⊔︀",
+  sqsub: "⊏",
+  sqsube: "⊑",
+  sqsubset: "⊏",
+  sqsubseteq: "⊑",
+  sqsup: "⊐",
+  sqsupe: "⊒",
+  sqsupset: "⊐",
+  sqsupseteq: "⊒",
+  squ: "□",
+  square: "□",
+  squarf: "▪",
+  squf: "▪",
+  srarr: "→",
+  sscr: "𝓈",
+  ssetmn: "∖",
+  ssmile: "⌣",
+  sstarf: "⋆",
+  star: "☆",
+  starf: "★",
+  straightepsilon: "ϵ",
+  straightphi: "ϕ",
+  strns: "¯",
+  sub: "⊂",
+  subE: "⫅",
+  subdot: "⪽",
+  sube: "⊆",
+  subedot: "⫃",
+  submult: "⫁",
+  subnE: "⫋",
+  subne: "⊊",
+  subplus: "⪿",
+  subrarr: "⥹",
+  subset: "⊂",
+  subseteq: "⊆",
+  subseteqq: "⫅",
+  subsetneq: "⊊",
+  subsetneqq: "⫋",
+  subsim: "⫇",
+  subsub: "⫕",
+  subsup: "⫓",
+  succ: "≻",
+  succapprox: "⪸",
+  succcurlyeq: "≽",
+  succeq: "⪰",
+  succnapprox: "⪺",
+  succneqq: "⪶",
+  succnsim: "⋩",
+  succsim: "≿",
+  sum: "∑",
+  sung: "♪",
+  sup1: "¹",
+  sup2: "²",
+  sup3: "³",
+  sup: "⊃",
+  supE: "⫆",
+  supdot: "⪾",
+  supdsub: "⫘",
+  supe: "⊇",
+  supedot: "⫄",
+  suphsol: "⟉",
+  suphsub: "⫗",
+  suplarr: "⥻",
+  supmult: "⫂",
+  supnE: "⫌",
+  supne: "⊋",
+  supplus: "⫀",
+  supset: "⊃",
+  supseteq: "⊇",
+  supseteqq: "⫆",
+  supsetneq: "⊋",
+  supsetneqq: "⫌",
+  supsim: "⫈",
+  supsub: "⫔",
+  supsup: "⫖",
+  swArr: "⇙",
+  swarhk: "⤦",
+  swarr: "↙",
+  swarrow: "↙",
+  swnwar: "⤪",
+  szlig: "ß",
+  target: "⌖",
+  tau: "τ",
+  tbrk: "⎴",
+  tcaron: "ť",
+  tcedil: "ţ",
+  tcy: "т",
+  tdot: "⃛",
+  telrec: "⌕",
+  tfr: "𝔱",
+  there4: "∴",
+  therefore: "∴",
+  theta: "θ",
+  thetasym: "ϑ",
+  thetav: "ϑ",
+  thickapprox: "≈",
+  thicksim: "∼",
+  thinsp: " ",
+  thkap: "≈",
+  thksim: "∼",
+  thorn: "þ",
+  tilde: "˜",
+  times: "×",
+  timesb: "⊠",
+  timesbar: "⨱",
+  timesd: "⨰",
+  tint: "∭",
+  toea: "⤨",
+  top: "⊤",
+  topbot: "⌶",
+  topcir: "⫱",
+  topf: "𝕥",
+  topfork: "⫚",
+  tosa: "⤩",
+  tprime: "‴",
+  trade: "™",
+  triangle: "▵",
+  triangledown: "▿",
+  triangleleft: "◃",
+  trianglelefteq: "⊴",
+  triangleq: "≜",
+  triangleright: "▹",
+  trianglerighteq: "⊵",
+  tridot: "◬",
+  trie: "≜",
+  triminus: "⨺",
+  triplus: "⨹",
+  trisb: "⧍",
+  tritime: "⨻",
+  trpezium: "⏢",
+  tscr: "𝓉",
+  tscy: "ц",
+  tshcy: "ћ",
+  tstrok: "ŧ",
+  twixt: "≬",
+  twoheadleftarrow: "↞",
+  twoheadrightarrow: "↠",
+  uArr: "⇑",
+  uHar: "⥣",
+  uacute: "ú",
+  uarr: "↑",
+  ubrcy: "ў",
+  ubreve: "ŭ",
+  ucirc: "û",
+  ucy: "у",
+  udarr: "⇅",
+  udblac: "ű",
+  udhar: "⥮",
+  ufisht: "⥾",
+  ufr: "𝔲",
+  ugrave: "ù",
+  uharl: "↿",
+  uharr: "↾",
+  uhblk: "▀",
+  ulcorn: "⌜",
+  ulcorner: "⌜",
+  ulcrop: "⌏",
+  ultri: "◸",
+  umacr: "ū",
+  uml: "¨",
+  uogon: "ų",
+  uopf: "𝕦",
+  uparrow: "↑",
+  updownarrow: "↕",
+  upharpoonleft: "↿",
+  upharpoonright: "↾",
+  uplus: "⊎",
+  upsi: "υ",
+  upsih: "ϒ",
+  upsilon: "υ",
+  upuparrows: "⇈",
+  urcorn: "⌝",
+  urcorner: "⌝",
+  urcrop: "⌎",
+  uring: "ů",
+  urtri: "◹",
+  uscr: "𝓊",
+  utdot: "⋰",
+  utilde: "ũ",
+  utri: "▵",
+  utrif: "▴",
+  uuarr: "⇈",
+  uuml: "ü",
+  uwangle: "⦧",
+  vArr: "⇕",
+  vBar: "⫨",
+  vBarv: "⫩",
+  vDash: "⊨",
+  vangrt: "⦜",
+  varepsilon: "ϵ",
+  varkappa: "ϰ",
+  varnothing: "∅",
+  varphi: "ϕ",
+  varpi: "ϖ",
+  varpropto: "∝",
+  varr: "↕",
+  varrho: "ϱ",
+  varsigma: "ς",
+  varsubsetneq: "⊊︀",
+  varsubsetneqq: "⫋︀",
+  varsupsetneq: "⊋︀",
+  varsupsetneqq: "⫌︀",
+  vartheta: "ϑ",
+  vartriangleleft: "⊲",
+  vartriangleright: "⊳",
+  vcy: "в",
+  vdash: "⊢",
+  vee: "∨",
+  veebar: "⊻",
+  veeeq: "≚",
+  vellip: "⋮",
+  verbar: "|",
+  vert: "|",
+  vfr: "𝔳",
+  vltri: "⊲",
+  vnsub: "⊂⃒",
+  vnsup: "⊃⃒",
+  vopf: "𝕧",
+  vprop: "∝",
+  vrtri: "⊳",
+  vscr: "𝓋",
+  vsubnE: "⫋︀",
+  vsubne: "⊊︀",
+  vsupnE: "⫌︀",
+  vsupne: "⊋︀",
+  vzigzag: "⦚",
+  wcirc: "ŵ",
+  wedbar: "⩟",
+  wedge: "∧",
+  wedgeq: "≙",
+  weierp: "℘",
+  wfr: "𝔴",
+  wopf: "𝕨",
+  wp: "℘",
+  wr: "≀",
+  wreath: "≀",
+  wscr: "𝓌",
+  xcap: "⋂",
+  xcirc: "◯",
+  xcup: "⋃",
+  xdtri: "▽",
+  xfr: "𝔵",
+  xhArr: "⟺",
+  xharr: "⟷",
+  xi: "ξ",
+  xlArr: "⟸",
+  xlarr: "⟵",
+  xmap: "⟼",
+  xnis: "⋻",
+  xodot: "⨀",
+  xopf: "𝕩",
+  xoplus: "⨁",
+  xotime: "⨂",
+  xrArr: "⟹",
+  xrarr: "⟶",
+  xscr: "𝓍",
+  xsqcup: "⨆",
+  xuplus: "⨄",
+  xutri: "△",
+  xvee: "⋁",
+  xwedge: "⋀",
+  yacute: "ý",
+  yacy: "я",
+  ycirc: "ŷ",
+  ycy: "ы",
+  yen: "¥",
+  yfr: "𝔶",
+  yicy: "ї",
+  yopf: "𝕪",
+  yscr: "𝓎",
+  yucy: "ю",
+  yuml: "ÿ",
+  zacute: "ź",
+  zcaron: "ž",
+  zcy: "з",
+  zdot: "ż",
+  zeetrf: "ℨ",
+  zeta: "ζ",
+  zfr: "𝔷",
+  zhcy: "ж",
+  zigrarr: "⇝",
+  zopf: "𝕫",
+  zscr: "𝓏",
+  zwj: "‍",
+  zwnj: "‌"
+};
diff -ruN a/src/graph/remark-parse/micromark/constructs.lil b/src/graph/remark-parse/micromark/constructs.lil
--- a/src/graph/remark-parse/micromark/constructs.lil	2026-09-23 20:40:45.155691650 +0000
+++ b/src/graph/remark-parse/micromark/constructs.lil	2026-09-23 20:40:45.267198297 +0000
@@ -19,91 +19,78 @@
 import { setextUnderline } from "./core/setext-underline.lil";
 import { thematicBreak } from "./core/thematic-break.lil";
 import { resolver as resolveText } from "./initialize/text.lil";
-import { cAmpersand, cAsterisk, cBackslash, cCarriageReturn, cCarriageReturnLineFeed, cDash, cDigit0, cDigit1, cDigit9, cEqualsTo, cExclamationMark, cGraveAccent, cGreaterThan, cHorizontalTab, cLeftSquareBracket, cLessThan, cLineFeed, cNumberSign, cPlusSign, cRightSquareBracket, cSpace, cTilde, cUnderscore, cVirtualSpace } from "./symbol/codes.lil";
 
-void setCode(JsValue map, int code, JsValue value) {
-  JS.set(map, JS.string(code), value);
-}
-
-JsValue arr1(JsValue a) {
-  JsValue list = JS.array();
-  list[0] = a;
-  return list;
-}
-
-JsValue arr2(JsValue a, JsValue b) {
-  JsValue list = JS.array();
-  list[0] = a;
-  list[1] = b;
-  return list;
-}
-
-export JsValue document = JS.object();
-setCode(document, cAsterisk, list);
-setCode(document, cPlusSign, list);
-setCode(document, cDash, list);
-int digit = cDigit0;
-while (digit <= cDigit9) {
-  setCode(document, digit, list);
-  digit = digit + 1;
-}
-setCode(document, cGreaterThan, blockQuote);
-
-export JsValue contentInitial = JS.object();
-setCode(contentInitial, cLeftSquareBracket, definition);
-
-export JsValue flowInitial = JS.object();
-setCode(flowInitial, cHorizontalTab, codeIndented);
-setCode(flowInitial, cVirtualSpace, codeIndented);
-setCode(flowInitial, cSpace, codeIndented);
-
-export JsValue flow = JS.object();
-setCode(flow, cNumberSign, headingAtx);
-setCode(flow, cAsterisk, thematicBreak);
-setCode(flow, cDash, arr2(setextUnderline, thematicBreak));
-setCode(flow, cLessThan, htmlFlow);
-setCode(flow, cEqualsTo, setextUnderline);
-setCode(flow, cUnderscore, thematicBreak);
-setCode(flow, cGraveAccent, codeFenced);
-setCode(flow, cTilde, codeFenced);
-
-export JsValue stringMap = JS.object();
-setCode(stringMap, cAmpersand, characterReference);
-setCode(stringMap, cBackslash, characterEscape);
-
-export JsValue text = JS.object();
-setCode(text, cCarriageReturn, lineEnding);
-setCode(text, cLineFeed, lineEnding);
-setCode(text, cCarriageReturnLineFeed, lineEnding);
-setCode(text, cExclamationMark, labelStartImage);
-setCode(text, cAmpersand, characterReference);
-setCode(text, cAsterisk, attention);
-setCode(text, cLessThan, arr2(autolink, htmlText));
-setCode(text, cLeftSquareBracket, labelStartLink);
-setCode(text, cBackslash, arr2(hardBreakEscape, characterEscape));
-setCode(text, cRightSquareBracket, labelEnd);
-setCode(text, cUnderscore, attention);
-setCode(text, cGraveAccent, codeText);
-
-export JsValue insideSpan = JS.object();
-JS.set(insideSpan, "null", arr2(attention, resolveText));
-
-export JsValue attentionMarkers = JS.object();
-JsValue markers = JS.array();
-markers[0] = cAsterisk;
-markers[1] = cUnderscore;
-JS.set(attentionMarkers, "null", markers);
-
-export JsValue disable = JS.object();
-JS.set(disable, "null", JS.array());
-
-export JsValue defaultConstructs = JS.object();
-JS.set(defaultConstructs, "document", document);
-JS.set(defaultConstructs, "contentInitial", contentInitial);
-JS.set(defaultConstructs, "flowInitial", flowInitial);
-JS.set(defaultConstructs, "flow", flow);
-JS.set(defaultConstructs, "string", stringMap);
-JS.set(defaultConstructs, "text", text);
-JS.set(defaultConstructs, "insideSpan", insideSpan);
-JS.set(defaultConstructs, "attentionMarkers", attentionMarkers);
-JS.set(defaultConstructs, "disable", disable);
+export JsValue document = JS.object(
+  "42", list,
+  "43", list,
+  "45", list,
+  "48", list,
+  "49", list,
+  "50", list,
+  "51", list,
+  "52", list,
+  "53", list,
+  "54", list,
+  "55", list,
+  "56", list,
+  "57", list,
+  "62", blockQuote
+);
+
+export JsValue contentInitial = JS.object("91", definition);
+
+export JsValue flowInitial = JS.object(
+  "-2", codeIndented,
+  "-1", codeIndented,
+  "32", codeIndented
+);
+
+export JsValue flow = JS.object(
+  "35", headingAtx,
+  "42", thematicBreak,
+  "45", JS.array(setextUnderline, thematicBreak),
+  "60", htmlFlow,
+  "61", setextUnderline,
+  "95", thematicBreak,
+  "96", codeFenced,
+  "126", codeFenced
+);
+
+export JsValue stringMap = JS.object(
+  "38", characterReference,
+  "92", characterEscape
+);
+
+export JsValue text = JS.object(
+  "-5", lineEnding,
+  "-4", lineEnding,
+  "-3", lineEnding,
+  "33", labelStartImage,
+  "38", characterReference,
+  "42", attention,
+  "60", JS.array(autolink, htmlText),
+  "91", labelStartLink,
+  "92", JS.array(hardBreakEscape, characterEscape),
+  "93", labelEnd,
+  "95", attention,
+  "96", codeText
+);
+
+export JsValue insideSpan = JS.object("null", JS.array(attention, resolveText));
+
+export JsValue attentionMarkers = JS.object("null", JS.array(42, 95));
+
+export JsValue disable = JS.object("null", JS.array());
+
+// Upstream passes its constructs module namespace, whose keys enumerate in sorted order.
+export JsValue defaultConstructs = JS.object(
+  "attentionMarkers", attentionMarkers,
+  "contentInitial", contentInitial,
+  "disable", disable,
+  "document", document,
+  "flow", flow,
+  "flowInitial", flowInitial,
+  "insideSpan", insideSpan,
+  "string", stringMap,
+  "text", text
+);
diff -ruN a/src/graph/remark-parse/micromark/core/attention.lil b/src/graph/remark-parse/micromark/core/attention.lil
--- a/src/graph/remark-parse/micromark/core/attention.lil	2026-09-23 20:40:45.184663061 +0000
+++ b/src/graph/remark-parse/micromark/core/attention.lil	2026-09-23 20:40:45.267314094 +0000
@@ -1,12 +1,14 @@
-import { arrLen, call1, copyPoint, includesVal, objGet, slice2, undef } from "../host.lil";
 import { pushAll, splice } from "../util-chunked.lil";
 import { classifyCharacter } from "../util-classify-character.lil";
 import { resolveAll } from "../util-resolve-all.lil";
-import { cAsterisk, codeEq } from "../symbol/codes.lil";
+import { cAsterisk } from "../symbol/codes.lil";
 import { kCharacterGroupPunctuation } from "../symbol/constants.lil";
-import { tAttentionSequence, tEmphasis, tEmphasisSequence, tEmphasisText, tStrong, tStrongSequence, tStrongText } from "../symbol/types.lil";
+import { tAttentionSequence, tData, tEmphasis, tEmphasisSequence, tEmphasisText, tStrong, tStrongSequence, tStrongText } from "../symbol/types.lil";
 
-struct PointView {
+import { copyPoint } from "../host.lil";
+
+// The tokenizer's own point and token objects, read and written in place.
+extern class AttentionPoint {
   float _bufferIndex;
   float _index;
   float line;
@@ -14,164 +16,118 @@
   float offset;
 }
 
-struct TokenView {
-  string type;
-  PointView start;
-  PointView end;
-}
-
-struct AttentionTokenView {
+extern class AttentionToken {
   string type;
-  PointView start;
-  PointView end;
+  AttentionPoint start;
+  AttentionPoint end;
   bool _open;
   bool _close;
 }
 
-void movePoint(PointView point, int offset) {
-  point.column = point.column + offset;
-  point.offset = point.offset + offset;
-  point._bufferIndex = point._bufferIndex + offset;
+AttentionPoint copyAttentionPoint(AttentionPoint point) {
+  return JS.assume(copyPoint(point));
 }
 
-JsValue ev(string kind, JsValue token, JsValue context) {
-  return JS.array(kind, token, context);
+void movePoint(AttentionPoint point, float offset) {
+  point.column += offset;
+  point.offset += offset;
+  point._bufferIndex += offset;
 }
 
 JsValue resolveAllAttention = JS.method2((JsValue _self, JsValue events, JsValue context) => {
-  int index = -1;
-  int n = arrLen(events);
+  float index = -1.0;
   while (true) {
-    index = index + 1;
-    if (index >= n) {
+    index++;
+    if (index >= events.length) {
       break;
     }
-    AttentionTokenView closeToken = JS.assume(events[index][1]);
-    if (events[index][0] == "enter" && closeToken.type == tAttentionSequence && closeToken._close) {
-      int open = index;
+    AttentionToken close = JS.assume(events[index][1]);
+    if (JS.strictEqual(events[index][0], "enter") && close.type == tAttentionSequence && close._close) {
+      float open = index;
       while (true) {
-        open = open - 1;
-        if (open < 0) {
+        open--;
+        if (open < 0.0) {
           break;
         }
-        AttentionTokenView openToken = JS.assume(events[open][1]);
-        if (events[open][0] == "exit" && openToken.type == tAttentionSequence && openToken._open) {
-          string openSer = JS.assume(call1(objGet(context, "sliceSerialize"), context, openToken));
-          string closeSer = JS.assume(call1(objGet(context, "sliceSerialize"), context, closeToken));
-          if (openSer.charCodeAt(0) == closeSer.charCodeAt(0)) {
-            int openSize = openToken.end.offset.toInt() - openToken.start.offset.toInt();
-            int closeSize = closeToken.end.offset.toInt() - closeToken.start.offset.toInt();
-            bool skip = false;
-            if ((openToken._close || closeToken._open) && (closeSize % 3 != 0) && ((openSize + closeSize) % 3 == 0)) {
-              skip = true;
-            }
-            if (!skip) {
-              int use = 1;
-              if (openSize > 1 && closeSize > 1) {
-                use = 2;
-              }
-              PointView start = JS.assume(copyPoint(openToken.end));
-              PointView end = JS.assume(copyPoint(closeToken.start));
-              movePoint(start, 0 - use);
-              movePoint(end, use);
-              string seqType = tEmphasisSequence;
-              string textType = tEmphasisText;
-              string groupType = tEmphasis;
-              if (use > 1) {
-                seqType = tStrongSequence;
-                textType = tStrongText;
-                groupType = tStrong;
-              }
-               TokenView openingSequence = JS.assume(JS.object("type", seqType, "start", start, "end", copyPoint(openToken.end)));
-               TokenView closingSequence = JS.assume(JS.object("type", seqType, "start", copyPoint(closeToken.start), "end", end));
-               TokenView text = JS.assume(JS.object("type", textType, "start", copyPoint(openToken.end), "end", copyPoint(closeToken.start)));
-               TokenView group = JS.assume(JS.object("type", groupType, "start", copyPoint(openingSequence.start), "end", copyPoint(closingSequence.end)));
-              openToken.end = JS.assume(copyPoint(openingSequence.start));
-              closeToken.start = JS.assume(copyPoint(closingSequence.end));
-              JsValue nextEvents = JS.array();
-              if (openToken.end.offset - openToken.start.offset != 0) {
-                 JsValue extraOpen = JS.array(ev("enter", openToken, context), ev("exit", openToken, context));
-                 nextEvents = pushAll(nextEvents, extraOpen);
-               }
-               JsValue opening = JS.array(
-                 ev("enter", group, context),
-                 ev("enter", openingSequence, context),
-                 ev("exit", openingSequence, context),
-                 ev("enter", text, context)
-               );
-              nextEvents = pushAll(nextEvents, opening);
-              nextEvents = pushAll(nextEvents, resolveAll(objGet(objGet(objGet(context, "parser"), "constructs"), "insideSpan")["null"], slice2(events, open + 1, index), context));
-               JsValue closing = JS.array(
-                 ev("exit", text, context),
-                 ev("enter", closingSequence, context),
-                 ev("exit", closingSequence, context),
-                 ev("exit", group, context)
-               );
-              nextEvents = pushAll(nextEvents, closing);
-              int extraOffset = 0;
-              if (closeToken.end.offset - closeToken.start.offset != 0) {
-                extraOffset = 2;
-                 JsValue extraClose = JS.array(ev("enter", closeToken, context), ev("exit", closeToken, context));
-                nextEvents = pushAll(nextEvents, extraClose);
-              }
-              splice(events, open - 1, index - open + 3, nextEvents);
-              index = open + arrLen(nextEvents) - extraOffset - 2;
-              n = arrLen(events);
-              break;
-            }
+        AttentionToken opening = JS.assume(events[open][1]);
+        if (JS.strictEqual(events[open][0], "exit") && opening.type == tAttentionSequence && opening._open && JS.strictEqual(JS.invoke(JS.invoke(context, "sliceSerialize", opening), "charCodeAt", 0), JS.invoke(JS.invoke(context, "sliceSerialize", close), "charCodeAt", 0))) {
+          float openSize = opening.end.offset - opening.start.offset;
+          float closeSize = close.end.offset - close.start.offset;
+          if ((opening._close || close._open) && JS.mod(closeSize, 3).truthy() && !JS.mod(openSize + closeSize, 3).truthy()) {
+            continue;
           }
+          float use = if (openSize > 1.0 && closeSize > 1.0) { 2.0 } else { 1.0 };
+          AttentionPoint start = copyAttentionPoint(opening.end);
+          AttentionPoint end = copyAttentionPoint(close.start);
+          movePoint(start, -use);
+          movePoint(end, use);
+          AttentionToken openingSequence = JS.assume(JS.object("type", if (use > 1.0) { tStrongSequence } else { tEmphasisSequence }, "start", start, "end", copyAttentionPoint(opening.end)));
+          AttentionToken closingSequence = JS.assume(JS.object("type", if (use > 1.0) { tStrongSequence } else { tEmphasisSequence }, "start", copyAttentionPoint(close.start), "end", end));
+          JsValue text = JS.object("type", if (use > 1.0) { tStrongText } else { tEmphasisText }, "start", copyAttentionPoint(opening.end), "end", copyAttentionPoint(close.start));
+          JsValue group = JS.object("type", if (use > 1.0) { tStrong } else { tEmphasis }, "start", copyAttentionPoint(openingSequence.start), "end", copyAttentionPoint(closingSequence.end));
+          opening.end = copyAttentionPoint(openingSequence.start);
+          close.start = copyAttentionPoint(closingSequence.end);
+          JsValue nextEvents = JS.array();
+          if (opening.end.offset - opening.start.offset != 0.0) {
+            nextEvents = pushAll(nextEvents, JS.array(JS.array("enter", opening, context), JS.array("exit", opening, context)));
+          }
+          nextEvents = pushAll(nextEvents, JS.array(JS.array("enter", group, context), JS.array("enter", openingSequence, context), JS.array("exit", openingSequence, context), JS.array("enter", text, context)));
+          nextEvents = pushAll(nextEvents, resolveAll(JS.get(JS.get(JS.get(JS.get(context, "parser"), "constructs"), "insideSpan"), "null"), JS.invoke(events, "slice", open + 1.0, index), context));
+          nextEvents = pushAll(nextEvents, JS.array(JS.array("exit", text, context), JS.array("enter", closingSequence, context), JS.array("exit", closingSequence, context), JS.array("exit", group, context)));
+          float offset = 0.0;
+          if (close.end.offset - close.start.offset != 0.0) {
+            offset = 2.0;
+            nextEvents = pushAll(nextEvents, JS.array(JS.array("enter", close, context), JS.array("exit", close, context)));
+          }
+          splice(events, open - 1.0, index - open + 3.0, nextEvents);
+          index = open + nextEvents.length - offset - 2.0;
+          break;
         }
       }
     }
   }
-  index = -1;
-  n = arrLen(events);
+  index = -1.0;
   while (true) {
-    index = index + 1;
-    if (index >= n) {
+    index++;
+    if (index >= events.length) {
       break;
     }
-    TokenView token = JS.assume(events[index][1]);
+    AttentionToken token = JS.assume(events[index][1]);
     if (token.type == tAttentionSequence) {
-      JS.set(events[index][1], "type", "data");
+      token.type = tData;
     }
   }
   return events;
 });
 
 JsValue tokenizeAttention = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue _nok) => {
-  JsValue attentionMarkers = objGet(objGet(objGet(self, "parser"), "constructs"), "attentionMarkers")["null"];
-  JsValue previous = objGet(self, "previous");
+  JsValue attentionMarkers = JS.get(JS.get(JS.get(JS.get(self, "parser"), "constructs"), "attentionMarkers"), "null");
+  JsValue previous = JS.get(self, "previous");
   JsValue before = classifyCharacter(previous);
-  int marker = 0;
+  JsValue marker = JS.undefined();
+  JsValue inside = JS.undefined();
 
-  JsValue inside = JS.method1((JsValue _s, JsValue code) => {
-    if (code == marker) {
-      call1(objGet(effects, "consume"), effects, code);
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    marker = code;
+    JS.invoke(effects, "enter", tAttentionSequence);
+    return JS.call(inside, JS.undefined(), code);
+  });
+
+  inside = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, marker)) {
+      JS.invoke(effects, "consume", code);
       return inside;
     }
-    AttentionTokenView token = JS.assume(call1(objGet(effects, "exit"), effects, tAttentionSequence));
+    AttentionToken token = JS.assume(JS.invoke(effects, "exit", tAttentionSequence));
     JsValue after = classifyCharacter(code);
-    bool openAfter = !after.truthy() || (after == kCharacterGroupPunctuation && before.truthy()) || includesVal(attentionMarkers, code);
-    bool closeBefore = !before.truthy() || (before == kCharacterGroupPunctuation && after.truthy()) || includesVal(attentionMarkers, previous);
-    bool isStar = marker == cAsterisk;
-    if (isStar) {
-      token._open = openAfter;
-      token._close = closeBefore;
-    } else {
-      token._open = openAfter && (before.truthy() || !closeBefore);
-      token._close = closeBefore && (after.truthy() || !openAfter);
-    }
-    return call1(ok, undef(), code);
-  });
-
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    marker = JS.number(code).toInt();
-    call1(objGet(effects, "enter"), effects, tAttentionSequence);
-    return call1(inside, undef(), code);
+    JsValue open = JS.or(JS.or(!after.truthy(), JS.and(JS.strictEqual(after, kCharacterGroupPunctuation), before)), JS.invoke(attentionMarkers, "includes", code));
+    JsValue close = JS.or(JS.or(!before.truthy(), JS.and(JS.strictEqual(before, kCharacterGroupPunctuation), after)), JS.invoke(attentionMarkers, "includes", previous));
+    token._open = (if (JS.strictEqual(marker, cAsterisk)) { open } else { JS.and(open, JS.or(before, !close.truthy())) }).truthy();
+    token._close = (if (JS.strictEqual(marker, cAsterisk)) { close } else { JS.and(close, JS.or(after, !open.truthy())) }).truthy();
+    return JS.call(ok, JS.undefined(), code);
   });
 
   return start;
 });
 
-export JsValue attention = JS.object("name", "attention", "tokenize", tokenizeAttention, "resolveAll", resolveAllAttention);
+export JsValue attention = JS.object("name", "attention", "resolveAll", resolveAllAttention, "tokenize", tokenizeAttention);
diff -ruN a/src/graph/remark-parse/micromark/core/autolink.lil b/src/graph/remark-parse/micromark/core/autolink.lil
--- a/src/graph/remark-parse/micromark/core/autolink.lil	2026-09-23 20:40:45.162380110 +0000
+++ b/src/graph/remark-parse/micromark/core/autolink.lil	2026-09-23 20:40:45.267692637 +0000
@@ -1,123 +1,125 @@
-import { call1, isEof, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { asciiAlpha, asciiAlphanumeric, asciiAtext, asciiControl } from "../util-character.lil";
 import { cAtSign, cColon, cDash, cDot, cGreaterThan, cLessThan, cPlusSign, cSpace, codeEq } from "../symbol/codes.lil";
 import { kAutolinkDomainSizeMax, kAutolinkSchemeSizeMax } from "../symbol/constants.lil";
 import { tAutolink, tAutolinkEmail, tAutolinkMarker, tAutolinkProtocol } from "../symbol/types.lil";
 
 JsValue tokenizeAutolink = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue nok) => {
-  int size = 0;
-  JsValue emailLabel = undef();
-  JsValue emailAtSignOrDot = undef();
+  float size = 0.0;
+  JsValue open = JS.undefined();
+  JsValue schemeOrEmailAtext = JS.undefined();
+  JsValue schemeInsideOrEmailAtext = JS.undefined();
+  JsValue urlInside = JS.undefined();
+  JsValue emailAtext = JS.undefined();
+  JsValue emailAtSignOrDot = JS.undefined();
+  JsValue emailLabel = JS.undefined();
+  JsValue emailValue = JS.undefined();
 
-  JsValue emailValue = JS.method1((JsValue _s, JsValue code) => {
-    if ((codeEq(code, cDash) || asciiAlphanumeric(code)) && size < kAutolinkDomainSizeMax) {
-      size = size + 1;
-      JsValue next = emailLabel;
-      if (codeEq(code, cDash)) {
-        next = emailValue;
-      }
-      call1(objGet(effects, "consume"), effects, code);
-      return next;
-    }
-    return call1(nok, undef(), code);
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tAutolink);
+    JS.invoke(effects, "enter", tAutolinkMarker);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tAutolinkMarker);
+    JS.invoke(effects, "enter", tAutolinkProtocol);
+    return open;
   });
 
-  emailLabel = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDot)) {
-      call1(objGet(effects, "consume"), effects, code);
-      size = 0;
-      return emailAtSignOrDot;
+  open = JS.method1((JsValue _s, JsValue code) => {
+    if (asciiAlpha(code)) {
+      JS.invoke(effects, "consume", code);
+      return schemeOrEmailAtext;
     }
-    if (codeEq(code, cGreaterThan)) {
-      JS.set(call1(objGet(effects, "exit"), effects, tAutolinkProtocol), "type", tAutolinkEmail);
-      call1(objGet(effects, "enter"), effects, tAutolinkMarker);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tAutolinkMarker);
-      call1(objGet(effects, "exit"), effects, tAutolink);
-      return ok;
+    if (codeEq(code, cAtSign)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    return call1(emailValue, undef(), code);
+    return JS.call(emailAtext, JS.undefined(), code);
   });
 
-  emailAtSignOrDot = JS.method1((JsValue _s, JsValue code) => {
-    if (asciiAlphanumeric(code)) {
-      return call1(emailLabel, undef(), code);
+  schemeOrEmailAtext = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cPlusSign) || codeEq(code, cDash) || codeEq(code, cDot) || asciiAlphanumeric(code)) {
+      size = 1.0;
+      return JS.call(schemeInsideOrEmailAtext, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(emailAtext, JS.undefined(), code);
   });
 
-  JsValue emailAtext = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cAtSign)) {
-      call1(objGet(effects, "consume"), effects, code);
-      return emailAtSignOrDot;
+  schemeInsideOrEmailAtext = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cColon)) {
+      JS.invoke(effects, "consume", code);
+      size = 0.0;
+      return urlInside;
     }
-    if (asciiAtext(code)) {
-      call1(objGet(effects, "consume"), effects, code);
-      return emailAtext;
+    if ((codeEq(code, cPlusSign) || codeEq(code, cDash) || codeEq(code, cDot) || asciiAlphanumeric(code)) && size < kAutolinkSchemeSizeMax) {
+      size++;
+      JS.invoke(effects, "consume", code);
+      return schemeInsideOrEmailAtext;
     }
-    return call1(nok, undef(), code);
+    size = 0.0;
+    return JS.call(emailAtext, JS.undefined(), code);
   });
 
-  JsValue urlInside = JS.method1((JsValue _s, JsValue code) => {
+  urlInside = JS.method1((JsValue _s, JsValue code) => {
     if (codeEq(code, cGreaterThan)) {
-      call1(objGet(effects, "exit"), effects, tAutolinkProtocol);
-      call1(objGet(effects, "enter"), effects, tAutolinkMarker);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tAutolinkMarker);
-      call1(objGet(effects, "exit"), effects, tAutolink);
+      JS.invoke(effects, "exit", tAutolinkProtocol);
+      JS.invoke(effects, "enter", tAutolinkMarker);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tAutolinkMarker);
+      JS.invoke(effects, "exit", tAutolink);
       return ok;
     }
-    if (isEof(code) || codeEq(code, cSpace) || codeEq(code, cLessThan) || asciiControl(code)) {
-      return call1(nok, undef(), code);
+    if (JS.strictEqual(code, null) || codeEq(code, cSpace) || codeEq(code, cLessThan) || asciiControl(code)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    call1(objGet(effects, "consume"), effects, code);
+    JS.invoke(effects, "consume", code);
     return urlInside;
   });
 
-  JsValue schemeInsideOrEmailAtext = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cColon)) {
-      call1(objGet(effects, "consume"), effects, code);
-      size = 0;
-      return urlInside;
+  emailAtext = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cAtSign)) {
+      JS.invoke(effects, "consume", code);
+      return emailAtSignOrDot;
     }
-    if ((codeEq(code, cPlusSign) || codeEq(code, cDash) || codeEq(code, cDot) || asciiAlphanumeric(code)) && size < kAutolinkSchemeSizeMax) {
-      size = size + 1;
-      call1(objGet(effects, "consume"), effects, code);
-      return schemeInsideOrEmailAtext;
+    if (asciiAtext(code)) {
+      JS.invoke(effects, "consume", code);
+      return emailAtext;
     }
-    size = 0;
-    return call1(emailAtext, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue schemeOrEmailAtext = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cPlusSign) || codeEq(code, cDash) || codeEq(code, cDot) || asciiAlphanumeric(code)) {
-      size = 1;
-      return call1(schemeInsideOrEmailAtext, undef(), code);
+  emailAtSignOrDot = JS.method1((JsValue _s, JsValue code) => {
+    if (asciiAlphanumeric(code)) {
+      return JS.call(emailLabel, JS.undefined(), code);
     }
-    return call1(emailAtext, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue open = JS.method1((JsValue _s, JsValue code) => {
-    if (asciiAlpha(code)) {
-      call1(objGet(effects, "consume"), effects, code);
-      return schemeOrEmailAtext;
+  emailLabel = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDot)) {
+      JS.invoke(effects, "consume", code);
+      size = 0.0;
+      return emailAtSignOrDot;
     }
-    if (codeEq(code, cAtSign)) {
-      return call1(nok, undef(), code);
+    if (codeEq(code, cGreaterThan)) {
+      JS.set(JS.invoke(effects, "exit", tAutolinkProtocol), "type", tAutolinkEmail);
+      JS.invoke(effects, "enter", tAutolinkMarker);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tAutolinkMarker);
+      JS.invoke(effects, "exit", tAutolink);
+      return ok;
     }
-    return call1(emailAtext, undef(), code);
+    return JS.call(emailValue, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tAutolink);
-    call1(objGet(effects, "enter"), effects, tAutolinkMarker);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tAutolinkMarker);
-    call1(objGet(effects, "enter"), effects, tAutolinkProtocol);
-    return open;
+  emailValue = JS.method1((JsValue _s, JsValue code) => {
+    if ((codeEq(code, cDash) || asciiAlphanumeric(code)) && size < kAutolinkDomainSizeMax) {
+      size++;
+      JsValue next = if (codeEq(code, cDash)) { emailValue } else { emailLabel };
+      JS.invoke(effects, "consume", code);
+      return next;
+    }
+    return JS.call(nok, JS.undefined(), code);
   });
 
   return start;
 });
 
-export JsValue autolink = makeNamedConstruct("autolink", tokenizeAutolink);
+export JsValue autolink = JS.object("name", "autolink", "tokenize", tokenizeAutolink);
diff -ruN a/src/graph/remark-parse/micromark/core/blank-line.lil b/src/graph/remark-parse/micromark/core/blank-line.lil
--- a/src/graph/remark-parse/micromark/core/blank-line.lil	2026-09-23 20:40:45.163137820 +0000
+++ b/src/graph/remark-parse/micromark/core/blank-line.lil	2026-09-23 20:40:45.268156883 +0000
@@ -1,25 +1,21 @@
-import { call1, isEof, makeConstruct, objGet, undef } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { markdownLineEnding, markdownSpace } from "../util-character.lil";
 import { tLinePrefix } from "../symbol/types.lil";
 
 JsValue tokenizeBlankLine = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue after = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      return call1(ok, undef(), code);
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      return JS.call(ok, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+  return JS.method1((JsValue _s, JsValue code) => {
     if (markdownSpace(code)) {
-      return call1(factorySpace(effects, after, tLinePrefix, 0), undef(), code);
+      return JS.call(factorySpace(effects, after, tLinePrefix, JS.undefined()), JS.undefined(), code);
     }
-    return call1(after, undef(), code);
+    return JS.call(after, JS.undefined(), code);
   });
-
-  return start;
 });
 
-export JsValue blankLine = makeConstruct(tokenizeBlankLine);
-JS.set(blankLine, "partial", true);
+export JsValue blankLine = JS.object("partial", true, "tokenize", tokenizeBlankLine);
diff -ruN a/src/graph/remark-parse/micromark/core/block-quote.lil b/src/graph/remark-parse/micromark/core/block-quote.lil
--- a/src/graph/remark-parse/micromark/core/block-quote.lil	2026-09-23 20:40:45.163461587 +0000
+++ b/src/graph/remark-parse/micromark/core/block-quote.lil	2026-09-23 20:40:45.268431548 +0000
@@ -1,4 +1,3 @@
-import { call1, call2, call3, includesStr, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { markdownSpace } from "../util-character.lil";
 import { cGreaterThan, codeEq } from "../symbol/codes.lil";
@@ -8,64 +7,55 @@
 JsValue tokenizeBlockQuoteStart = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue after = JS.method1((JsValue _s, JsValue code) => {
     if (markdownSpace(code)) {
-      call1(objGet(effects, "enter"), effects, tBlockQuotePrefixWhitespace);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tBlockQuotePrefixWhitespace);
-      call1(objGet(effects, "exit"), effects, tBlockQuotePrefix);
+      JS.invoke(effects, "enter", tBlockQuotePrefixWhitespace);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tBlockQuotePrefixWhitespace);
+      JS.invoke(effects, "exit", tBlockQuotePrefix);
       return ok;
     }
-    call1(objGet(effects, "exit"), effects, tBlockQuotePrefix);
-    return call1(ok, undef(), code);
+    JS.invoke(effects, "exit", tBlockQuotePrefix);
+    return JS.call(ok, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+  return JS.method1((JsValue _s, JsValue code) => {
     if (codeEq(code, cGreaterThan)) {
-      JsValue state = objGet(self, "containerState");
-      if (!objGet(state, "open").truthy()) {
-        JsValue fields = JS.object();
-        JS.set(fields, "_container", true);
-        call2(objGet(effects, "enter"), effects, tBlockQuote, fields);
+      JsValue state = JS.get(self, "containerState");
+      if (!JS.get(state, "open").truthy()) {
+        JS.invoke(effects, "enter", tBlockQuote, JS.object("_container", true));
         JS.set(state, "open", true);
       }
-      call1(objGet(effects, "enter"), effects, tBlockQuotePrefix);
-      call1(objGet(effects, "enter"), effects, tBlockQuoteMarker);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tBlockQuoteMarker);
+      JS.invoke(effects, "enter", tBlockQuotePrefix);
+      JS.invoke(effects, "enter", tBlockQuoteMarker);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tBlockQuoteMarker);
       return after;
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
-
-  return start;
-});
-
-export JsValue blockQuote = makeNamedConstruct("blockQuote", tokenizeBlockQuoteStart);
-
-JsValue tokenizeBlockQuoteContinuation = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  JsValue contBefore = JS.method1((JsValue _s, JsValue code) => {
-    return call1(call3(objGet(effects, "attempt"), effects, blockQuote, ok, nok), undef(), code);
-  });
-
-  JsValue contStart = JS.method1((JsValue _s, JsValue code) => {
-    if (markdownSpace(code)) {
-      float max = kTabSize;
-      if (includesStr(objGet(objGet(objGet(self, "parser"), "constructs"), "disable")["null"], "codeIndented")) {
-        max = 0;
-      }
-      return call1(factorySpace(effects, contBefore, tLinePrefix, max), undef(), code);
-    }
-    return call1(contBefore, undef(), code);
-  });
-
-  return contStart;
 });
 
 JsValue exitBlockQuote = JS.method1((JsValue _self, JsValue effects) => {
-  call1(objGet(effects, "exit"), effects, tBlockQuote);
-  return undef();
+  JS.invoke(effects, "exit", tBlockQuote);
+  return JS.undefined();
 });
 
-JsValue continuation = JS.object();
-JS.set(continuation, "tokenize", tokenizeBlockQuoteContinuation);
-JS.set(blockQuote, "continuation", continuation);
-JS.set(blockQuote, "exit", exitBlockQuote);
+// The continuation tokenizer attempts `blockQuote` itself, so it is written
+// inside the construct's own initializer.
+export JsValue blockQuote = JS.object(
+  "continuation", JS.object("tokenize", JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
+    JsValue contBefore = JS.method1((JsValue _s, JsValue code) => {
+      return JS.call(JS.invoke(effects, "attempt", blockQuote, ok, nok), JS.undefined(), code);
+    });
+
+    return JS.method1((JsValue _s, JsValue code) => {
+      if (markdownSpace(code)) {
+        JsValue max = if (JS.invoke(JS.get(JS.get(JS.get(self, "parser"), "constructs"), "disable")["null"], "includes", "codeIndented").truthy()) { JS.undefined() } else { kTabSize };
+        return JS.call(factorySpace(effects, contBefore, tLinePrefix, max), JS.undefined(), code);
+      }
+      return JS.call(contBefore, JS.undefined(), code);
+    });
+  })),
+  "exit", exitBlockQuote,
+  "name", "blockQuote",
+  "tokenize", tokenizeBlockQuoteStart
+);
diff -ruN a/src/graph/remark-parse/micromark/core/character-escape.lil b/src/graph/remark-parse/micromark/core/character-escape.lil
--- a/src/graph/remark-parse/micromark/core/character-escape.lil	2026-09-23 20:40:45.163765596 +0000
+++ b/src/graph/remark-parse/micromark/core/character-escape.lil	2026-09-23 20:40:45.268711001 +0000
@@ -1,28 +1,25 @@
-import { call1, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { asciiPunctuation } from "../util-character.lil";
 import { tCharacterEscape, tCharacterEscapeValue, tEscapeMarker } from "../symbol/types.lil";
 
 JsValue tokenizeCharacterEscape = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue inside = JS.method1((JsValue _s, JsValue code) => {
     if (asciiPunctuation(code)) {
-      call1(objGet(effects, "enter"), effects, tCharacterEscapeValue);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tCharacterEscapeValue);
-      call1(objGet(effects, "exit"), effects, tCharacterEscape);
+      JS.invoke(effects, "enter", tCharacterEscapeValue);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tCharacterEscapeValue);
+      JS.invoke(effects, "exit", tCharacterEscape);
       return ok;
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tCharacterEscape);
-    call1(objGet(effects, "enter"), effects, tEscapeMarker);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tEscapeMarker);
+  return JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tCharacterEscape);
+    JS.invoke(effects, "enter", tEscapeMarker);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tEscapeMarker);
     return inside;
   });
-
-  return start;
 });
 
-export JsValue characterEscape = makeNamedConstruct("characterEscape", tokenizeCharacterEscape);
+export JsValue characterEscape = JS.object("name", "characterEscape", "tokenize", tokenizeCharacterEscape);
diff -ruN a/src/graph/remark-parse/micromark/core/character-reference.lil b/src/graph/remark-parse/micromark/core/character-reference.lil
--- a/src/graph/remark-parse/micromark/core/character-reference.lil	2026-09-23 20:40:45.164140920 +0000
+++ b/src/graph/remark-parse/micromark/core/character-reference.lil	2026-09-23 20:40:45.268994042 +0000
@@ -1,90 +1,75 @@
-import { call1, jsString, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { decodeNamedCharacterReference } from "../decode-named.lil";
 import { asciiAlphanumeric, asciiDigit, asciiHexDigit } from "../util-character.lil";
 import { cLowercaseX, cNumberSign, cSemicolon, cUppercaseX, codeEq } from "../symbol/codes.lil";
 import { kCharacterReferenceDecimalSizeMax, kCharacterReferenceHexadecimalSizeMax, kCharacterReferenceNamedSizeMax } from "../symbol/constants.lil";
 import { tCharacterReference, tCharacterReferenceMarker, tCharacterReferenceMarkerHexadecimal, tCharacterReferenceMarkerNumeric, tCharacterReferenceValue } from "../symbol/types.lil";
 
-class CharacterReferenceState {
-  int size;
-  int max;
-  int kind;
-}
-
-bool testCode(int kind, JsValue code) {
-  if (kind == 0) {
-    return asciiAlphanumeric(code);
-  }
-  if (kind == 1) {
-    return asciiHexDigit(code);
-  }
-  return asciiDigit(code);
-}
-
 JsValue tokenizeCharacterReference = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  CharacterReferenceState st = new CharacterReferenceState();
+  float size = 0.0;
+  float max = 0.0;
+  func(JsValue)->bool test = asciiAlphanumeric;
+  JsValue open = JS.undefined();
+  JsValue numeric = JS.undefined();
+  JsValue value = JS.undefined();
 
-  JsValue value = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cSemicolon) && st.size > 0) {
-      JsValue token = call1(objGet(effects, "exit"), effects, tCharacterReferenceValue);
-      if (st.kind == 0) {
-        string serialized = jsString(call1(objGet(self, "sliceSerialize"), self, token));
-        if (!decodeNamedCharacterReference(serialized).truthy()) {
-          return call1(nok, undef(), code);
-        }
-      }
-      call1(objGet(effects, "enter"), effects, tCharacterReferenceMarker);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tCharacterReferenceMarker);
-      call1(objGet(effects, "exit"), effects, tCharacterReference);
-      return ok;
-    }
-    if (testCode(st.kind, code) && st.size < st.max) {
-      st.size = st.size + 1;
-      call1(objGet(effects, "consume"), effects, code);
-      return value;
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tCharacterReference);
+    JS.invoke(effects, "enter", tCharacterReferenceMarker);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tCharacterReferenceMarker);
+    return open;
+  });
+
+  open = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cNumberSign)) {
+      JS.invoke(effects, "enter", tCharacterReferenceMarkerNumeric);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tCharacterReferenceMarkerNumeric);
+      return numeric;
     }
-    return call1(nok, undef(), code);
+    JS.invoke(effects, "enter", tCharacterReferenceValue);
+    max = kCharacterReferenceNamedSizeMax;
+    test = asciiAlphanumeric;
+    return JS.call(value, JS.undefined(), code);
   });
 
-  JsValue numeric = JS.method1((JsValue _s, JsValue code) => {
+  numeric = JS.method1((JsValue _s, JsValue code) => {
     if (codeEq(code, cUppercaseX) || codeEq(code, cLowercaseX)) {
-      call1(objGet(effects, "enter"), effects, tCharacterReferenceMarkerHexadecimal);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tCharacterReferenceMarkerHexadecimal);
-      call1(objGet(effects, "enter"), effects, tCharacterReferenceValue);
-      st.max = kCharacterReferenceHexadecimalSizeMax;
-      st.kind = 1;
+      JS.invoke(effects, "enter", tCharacterReferenceMarkerHexadecimal);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tCharacterReferenceMarkerHexadecimal);
+      JS.invoke(effects, "enter", tCharacterReferenceValue);
+      max = kCharacterReferenceHexadecimalSizeMax;
+      test = asciiHexDigit;
       return value;
     }
-    call1(objGet(effects, "enter"), effects, tCharacterReferenceValue);
-    st.max = kCharacterReferenceDecimalSizeMax;
-    st.kind = 2;
-    return call1(value, undef(), code);
+    JS.invoke(effects, "enter", tCharacterReferenceValue);
+    max = kCharacterReferenceDecimalSizeMax;
+    test = asciiDigit;
+    return JS.call(value, JS.undefined(), code);
   });
 
-  JsValue open = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cNumberSign)) {
-      call1(objGet(effects, "enter"), effects, tCharacterReferenceMarkerNumeric);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tCharacterReferenceMarkerNumeric);
-      return numeric;
+  value = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cSemicolon) && size != 0.0) {
+      JsValue token = JS.invoke(effects, "exit", tCharacterReferenceValue);
+      if (test == asciiAlphanumeric && !decodeNamedCharacterReference(JS.assume(JS.invoke(self, "sliceSerialize", token))).truthy()) {
+        return JS.call(nok, JS.undefined(), code);
+      }
+      JS.invoke(effects, "enter", tCharacterReferenceMarker);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tCharacterReferenceMarker);
+      JS.invoke(effects, "exit", tCharacterReference);
+      return ok;
     }
-    call1(objGet(effects, "enter"), effects, tCharacterReferenceValue);
-    st.max = kCharacterReferenceNamedSizeMax;
-    st.kind = 0;
-    return call1(value, undef(), code);
-  });
-
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tCharacterReference);
-    call1(objGet(effects, "enter"), effects, tCharacterReferenceMarker);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tCharacterReferenceMarker);
-    return open;
+    if (test(code) && size < max) {
+      size++;
+      JS.invoke(effects, "consume", code);
+      return value;
+    }
+    return JS.call(nok, JS.undefined(), code);
   });
 
   return start;
 });
 
-export JsValue characterReference = makeNamedConstruct("characterReference", tokenizeCharacterReference);
+export JsValue characterReference = JS.object("name", "characterReference", "tokenize", tokenizeCharacterReference);
diff -ruN a/src/graph/remark-parse/micromark/core/code-fenced.lil b/src/graph/remark-parse/micromark/core/code-fenced.lil
--- a/src/graph/remark-parse/micromark/core/code-fenced.lil	2026-09-23 20:40:45.164497738 +0000
+++ b/src/graph/remark-parse/micromark/core/code-fenced.lil	2026-09-23 20:40:45.278268456 +0000
@@ -1,4 +1,3 @@
-import { arrLen, call0, call1, call2, call3, includesStr, isEof, jsString, makeConstruct, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { markdownLineEnding, markdownSpace } from "../util-character.lil";
 import { cGraveAccent, codeEq } from "../symbol/codes.lil";
@@ -7,226 +6,218 @@
 
 JsValue tokenizeNonLazyContinuation = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue lineStart = JS.method1((JsValue _s, JsValue code) => {
-    if (objGet(objGet(self, "parser"), "lazy")[objGet(call0(objGet(self, "now"), self), "line")].truthy()) {
-      return call1(nok, undef(), code);
+    if (JS.get(JS.get(self, "parser"), "lazy")[JS.get(JS.invoke(self, "now"), "line")].truthy()) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    return call1(ok, undef(), code);
+    return JS.call(ok, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code)) {
-      return call1(nok, undef(), code);
+  return JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    call1(objGet(effects, "enter"), effects, tLineEnding);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tLineEnding);
+    JS.invoke(effects, "enter", tLineEnding);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tLineEnding);
     return lineStart;
   });
-
-  return start;
 });
 
-JsValue nonLazyContinuation = makeConstruct(tokenizeNonLazyContinuation);
-JS.set(nonLazyContinuation, "partial", true);
+JsValue nonLazyContinuation = JS.object("partial", true, "tokenize", tokenizeNonLazyContinuation);
 
 JsValue tokenizeCodeFenced = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
   float initialPrefix = 0.0;
-  int sizeOpen = 0;
-  int marker = 0;
-  JsValue infoBefore = undef();
-  JsValue metaBefore = undef();
-  JsValue beforeContentChunk = undef();
-  JsValue atNonLazyBreak = undef();
+  float sizeOpen = 0.0;
+  JsValue marker = JS.undefined();
+  JsValue sequenceOpen = JS.undefined();
+  JsValue infoBefore = JS.undefined();
+  JsValue info = JS.undefined();
+  JsValue metaBefore = JS.undefined();
+  JsValue meta = JS.undefined();
+  JsValue atNonLazyBreak = JS.undefined();
+  JsValue contentBefore = JS.undefined();
+  JsValue contentStart = JS.undefined();
+  JsValue beforeContentChunk = JS.undefined();
+  JsValue contentChunk = JS.undefined();
+  JsValue after = JS.undefined();
 
   JsValue tokenizeCloseStart = JS.method3((JsValue _ctx, JsValue closeEffects, JsValue closeOk, JsValue closeNok) => {
-    int size = 0;
+    float size = 0.0;
+    JsValue beforeSequenceClose = JS.undefined();
+    JsValue sequenceClose = JS.undefined();
+    JsValue sequenceCloseAfter = JS.undefined();
+
+    JsValue closeLineStart = JS.method1((JsValue _s, JsValue code) => {
+      JS.invoke(closeEffects, "enter", tCodeFencedFence);
+      if (markdownSpace(code)) {
+        JsValue max = if (JS.invoke(JS.get(JS.get(JS.get(self, "parser"), "constructs"), "disable")["null"], "includes", "codeIndented").truthy()) { JS.undefined() } else { kTabSize };
+        return JS.call(factorySpace(closeEffects, beforeSequenceClose, tLinePrefix, max), JS.undefined(), code);
+      }
+      return JS.call(beforeSequenceClose, JS.undefined(), code);
+    });
 
-    JsValue sequenceCloseAfter = JS.method1((JsValue _s, JsValue code) => {
-      if (isEof(code) || markdownLineEnding(code)) {
-        call1(objGet(closeEffects, "exit"), closeEffects, tCodeFencedFence);
-        return call1(closeOk, undef(), code);
+    beforeSequenceClose = JS.method1((JsValue _s, JsValue code) => {
+      if (JS.strictEqual(code, marker)) {
+        JS.invoke(closeEffects, "enter", tCodeFencedFenceSequence);
+        return JS.call(sequenceClose, JS.undefined(), code);
       }
-      return call1(closeNok, undef(), code);
+      return JS.call(closeNok, JS.undefined(), code);
     });
 
-    JsValue sequenceClose = JS.method1((JsValue _s, JsValue code) => {
-      if (code == marker) {
-        size = size + 1;
-        call1(objGet(closeEffects, "consume"), closeEffects, code);
+    sequenceClose = JS.method1((JsValue _s, JsValue code) => {
+      if (JS.strictEqual(code, marker)) {
+        size++;
+        JS.invoke(closeEffects, "consume", code);
         return sequenceClose;
       }
       if (size >= sizeOpen) {
-        call1(objGet(closeEffects, "exit"), closeEffects, tCodeFencedFenceSequence);
+        JS.invoke(closeEffects, "exit", tCodeFencedFenceSequence);
         if (markdownSpace(code)) {
-          return call1(factorySpace(closeEffects, sequenceCloseAfter, tWhitespace, 0), undef(), code);
+          return JS.call(factorySpace(closeEffects, sequenceCloseAfter, tWhitespace, JS.undefined()), JS.undefined(), code);
         }
-        return call1(sequenceCloseAfter, undef(), code);
-      }
-      return call1(closeNok, undef(), code);
-    });
-
-    JsValue beforeSequenceClose = JS.method1((JsValue _s, JsValue code) => {
-      if (code == marker) {
-        call1(objGet(closeEffects, "enter"), closeEffects, tCodeFencedFenceSequence);
-        return call1(sequenceClose, undef(), code);
+        return JS.call(sequenceCloseAfter, JS.undefined(), code);
       }
-      return call1(closeNok, undef(), code);
+      return JS.call(closeNok, JS.undefined(), code);
     });
 
-    JsValue closeLineStart = JS.method1((JsValue _s, JsValue code) => {
-      call1(objGet(closeEffects, "enter"), closeEffects, tCodeFencedFence);
-      if (markdownSpace(code)) {
-        int max = kTabSize;
-        if (includesStr(objGet(objGet(objGet(self, "parser"), "constructs"), "disable")["null"], "codeIndented")) {
-          max = 0;
-        }
-        return call1(factorySpace(closeEffects, beforeSequenceClose, tLinePrefix, max), undef(), code);
+    sequenceCloseAfter = JS.method1((JsValue _s, JsValue code) => {
+      if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+        JS.invoke(closeEffects, "exit", tCodeFencedFence);
+        return JS.call(closeOk, JS.undefined(), code);
       }
-      return call1(beforeSequenceClose, undef(), code);
+      return JS.call(closeNok, JS.undefined(), code);
     });
 
-    JsValue startBefore = JS.method1((JsValue _s, JsValue code) => {
-      call1(objGet(closeEffects, "enter"), closeEffects, tLineEnding);
-      call1(objGet(closeEffects, "consume"), closeEffects, code);
-      call1(objGet(closeEffects, "exit"), closeEffects, tLineEnding);
+    return JS.method1((JsValue _s, JsValue code) => {
+      JS.invoke(closeEffects, "enter", tLineEnding);
+      JS.invoke(closeEffects, "consume", code);
+      JS.invoke(closeEffects, "exit", tLineEnding);
       return closeLineStart;
     });
-
-    return startBefore;
   });
 
-  JsValue closeStart = makeConstruct(tokenizeCloseStart);
-  JS.set(closeStart, "partial", true);
+  JsValue closeStart = JS.object("partial", true, "tokenize", tokenizeCloseStart);
 
-  JsValue after = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "exit"), effects, tCodeFenced);
-    return call1(ok, undef(), code);
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    JsValue tail = JS.get(self, "events")[JS.get(self, "events").length - 1.0];
+    initialPrefix = if (tail.truthy() && JS.strictEqual(JS.get(tail[1], "type"), tLinePrefix)) { JS.invoke(tail[2], "sliceSerialize", tail[1], true).length } else { 0.0 };
+    marker = code;
+    JS.invoke(effects, "enter", tCodeFenced);
+    JS.invoke(effects, "enter", tCodeFencedFence);
+    JS.invoke(effects, "enter", tCodeFencedFenceSequence);
+    return JS.call(sequenceOpen, JS.undefined(), code);
   });
 
-  JsValue contentChunk = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tCodeFlowValue);
-      return call1(beforeContentChunk, undef(), code);
+  sequenceOpen = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, marker)) {
+      sizeOpen++;
+      JS.invoke(effects, "consume", code);
+      return sequenceOpen;
     }
-    call1(objGet(effects, "consume"), effects, code);
-    return contentChunk;
+    if (sizeOpen < kCodeFencedSequenceSizeMin) {
+      return JS.call(nok, JS.undefined(), code);
+    }
+    JS.invoke(effects, "exit", tCodeFencedFenceSequence);
+    if (markdownSpace(code)) {
+      return JS.call(factorySpace(effects, infoBefore, tWhitespace, JS.undefined()), JS.undefined(), code);
+    }
+    return JS.call(infoBefore, JS.undefined(), code);
   });
 
-  beforeContentChunk = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      return call1(call3(objGet(effects, "check"), effects, nonLazyContinuation, atNonLazyBreak, after), undef(), code);
+  infoBefore = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tCodeFencedFence);
+      if (JS.get(self, "interrupt").truthy()) {
+        return JS.call(ok, JS.undefined(), code);
+      }
+      return JS.call(JS.invoke(effects, "check", nonLazyContinuation, atNonLazyBreak, after), JS.undefined(), code);
+    }
+    JS.invoke(effects, "enter", tCodeFencedFenceInfo);
+    JS.invoke(effects, "enter", tChunkString, JS.object("contentType", kContentTypeString));
+    return JS.call(info, JS.undefined(), code);
+  });
+
+  info = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tChunkString);
+      JS.invoke(effects, "exit", tCodeFencedFenceInfo);
+      return JS.call(infoBefore, JS.undefined(), code);
+    }
+    if (markdownSpace(code)) {
+      JS.invoke(effects, "exit", tChunkString);
+      JS.invoke(effects, "exit", tCodeFencedFenceInfo);
+      return JS.call(factorySpace(effects, metaBefore, tWhitespace, JS.undefined()), JS.undefined(), code);
+    }
+    if (codeEq(code, cGraveAccent) && JS.strictEqual(code, marker)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    call1(objGet(effects, "enter"), effects, tCodeFlowValue);
-    return call1(contentChunk, undef(), code);
+    JS.invoke(effects, "consume", code);
+    return info;
   });
 
-  JsValue contentStart = JS.method1((JsValue _s, JsValue code) => {
-    if (initialPrefix > 0.0 && markdownSpace(code)) {
-      return call1(factorySpace(effects, beforeContentChunk, tLinePrefix, initialPrefix + 1.0), undef(), code);
+  metaBefore = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      return JS.call(infoBefore, JS.undefined(), code);
     }
-    return call1(beforeContentChunk, undef(), code);
+    JS.invoke(effects, "enter", tCodeFencedFenceMeta);
+    JS.invoke(effects, "enter", tChunkString, JS.object("contentType", kContentTypeString));
+    return JS.call(meta, JS.undefined(), code);
   });
 
-  JsValue contentBefore = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tLineEnding);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tLineEnding);
-    return contentStart;
+  meta = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tChunkString);
+      JS.invoke(effects, "exit", tCodeFencedFenceMeta);
+      return JS.call(infoBefore, JS.undefined(), code);
+    }
+    if (codeEq(code, cGraveAccent) && JS.strictEqual(code, marker)) {
+      return JS.call(nok, JS.undefined(), code);
+    }
+    JS.invoke(effects, "consume", code);
+    return meta;
   });
 
   atNonLazyBreak = JS.method1((JsValue _s, JsValue code) => {
-    return call1(call3(objGet(effects, "attempt"), effects, closeStart, after, contentBefore), undef(), code);
+    return JS.call(JS.invoke(effects, "attempt", closeStart, after, contentBefore), JS.undefined(), code);
   });
 
-  JsValue info = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tChunkString);
-      call1(objGet(effects, "exit"), effects, tCodeFencedFenceInfo);
-      return call1(infoBefore, undef(), code);
-    }
-    if (markdownSpace(code)) {
-      call1(objGet(effects, "exit"), effects, tChunkString);
-      call1(objGet(effects, "exit"), effects, tCodeFencedFenceInfo);
-      return call1(factorySpace(effects, metaBefore, tWhitespace, 0), undef(), code);
-    }
-    if (codeEq(code, cGraveAccent) && code == marker) {
-      return call1(nok, undef(), code);
-    }
-    call1(objGet(effects, "consume"), effects, code);
-    return info;
+  contentBefore = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tLineEnding);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tLineEnding);
+    return contentStart;
   });
 
-  JsValue meta = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tChunkString);
-      call1(objGet(effects, "exit"), effects, tCodeFencedFenceMeta);
-      return call1(infoBefore, undef(), code);
-    }
-    if (codeEq(code, cGraveAccent) && code == marker) {
-      return call1(nok, undef(), code);
+  contentStart = JS.method1((JsValue _s, JsValue code) => {
+    if (initialPrefix > 0.0 && markdownSpace(code)) {
+      return JS.call(factorySpace(effects, beforeContentChunk, tLinePrefix, initialPrefix + 1.0), JS.undefined(), code);
     }
-    call1(objGet(effects, "consume"), effects, code);
-    return meta;
+    return JS.call(beforeContentChunk, JS.undefined(), code);
   });
 
-  metaBefore = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      return call1(infoBefore, undef(), code);
+  beforeContentChunk = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      return JS.call(JS.invoke(effects, "check", nonLazyContinuation, atNonLazyBreak, after), JS.undefined(), code);
     }
-    call1(objGet(effects, "enter"), effects, tCodeFencedFenceMeta);
-    JsValue fields = JS.object();
-    JS.set(fields, "contentType", kContentTypeString);
-    call2(objGet(effects, "enter"), effects, tChunkString, fields);
-    return call1(meta, undef(), code);
+    JS.invoke(effects, "enter", tCodeFlowValue);
+    return JS.call(contentChunk, JS.undefined(), code);
   });
 
-  infoBefore = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tCodeFencedFence);
-      if (objGet(self, "interrupt").truthy()) {
-        return call1(ok, undef(), code);
-      }
-      return call1(call3(objGet(effects, "check"), effects, nonLazyContinuation, atNonLazyBreak, after), undef(), code);
-    }
-    call1(objGet(effects, "enter"), effects, tCodeFencedFenceInfo);
-    JsValue fields = JS.object();
-    JS.set(fields, "contentType", kContentTypeString);
-    call2(objGet(effects, "enter"), effects, tChunkString, fields);
-    return call1(info, undef(), code);
-  });
-
-  JsValue sequenceOpen = JS.method1((JsValue _s, JsValue code) => {
-    if (code == marker) {
-      sizeOpen = sizeOpen + 1;
-      call1(objGet(effects, "consume"), effects, code);
-      return sequenceOpen;
+  contentChunk = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tCodeFlowValue);
+      return JS.call(beforeContentChunk, JS.undefined(), code);
     }
-    if (sizeOpen < kCodeFencedSequenceSizeMin) {
-      return call1(nok, undef(), code);
-    }
-    call1(objGet(effects, "exit"), effects, tCodeFencedFenceSequence);
-    if (markdownSpace(code)) {
-      return call1(factorySpace(effects, infoBefore, tWhitespace, 0), undef(), code);
-    }
-    return call1(infoBefore, undef(), code);
+    JS.invoke(effects, "consume", code);
+    return contentChunk;
   });
 
-  JsValue beforeSequenceOpen = JS.method1((JsValue _s, JsValue code) => {
-    JsValue events = objGet(self, "events");
-    JsValue tail = events[arrLen(events) - 1];
-    float prefix = 0.0;
-    if (tail.truthy() && objGet(tail[1], "type") == tLinePrefix) {
-      prefix = JS.number(jsString(call2(objGet(tail[2], "sliceSerialize"), tail[2], tail[1], true)).length);
-    }
-    initialPrefix = prefix;
-    marker = JS.number(code).toInt();
-    call1(objGet(effects, "enter"), effects, tCodeFenced);
-    call1(objGet(effects, "enter"), effects, tCodeFencedFence);
-    call1(objGet(effects, "enter"), effects, tCodeFencedFenceSequence);
-    return call1(sequenceOpen, undef(), code);
+  after = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "exit", tCodeFenced);
+    return JS.call(ok, JS.undefined(), code);
   });
 
-  return beforeSequenceOpen;
+  return start;
 });
 
-export JsValue codeFenced = JS.object("name", "codeFenced", "tokenize", tokenizeCodeFenced, "concrete", true);
+export JsValue codeFenced = JS.object("concrete", true, "name", "codeFenced", "tokenize", tokenizeCodeFenced);
diff -ruN a/src/graph/remark-parse/micromark/core/code-indented.lil b/src/graph/remark-parse/micromark/core/code-indented.lil
--- a/src/graph/remark-parse/micromark/core/code-indented.lil	2026-09-23 20:40:45.164898489 +0000
+++ b/src/graph/remark-parse/micromark/core/code-indented.lil	2026-09-23 20:40:45.278585921 +0000
@@ -1,86 +1,81 @@
-import { arrLen, call0, call1, call2, call3, isEof, jsString, makeConstruct, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
-import { markdownLineEnding, markdownSpace } from "../util-character.lil";
+import { markdownLineEnding } from "../util-character.lil";
 import { kTabSize } from "../symbol/constants.lil";
 import { tCodeFlowValue, tCodeIndented, tLineEnding, tLinePrefix } from "../symbol/types.lil";
 
 JsValue tokenizeFurtherStart = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  JsValue furtherStart = undef();
+  JsValue furtherStart = JS.undefined();
 
   JsValue afterPrefix = JS.method1((JsValue _s, JsValue code) => {
-    JsValue events = objGet(self, "events");
-    JsValue tail = events[arrLen(events) - 1];
-    if (tail.truthy() && objGet(tail[1], "type") == tLinePrefix && jsString(call2(objGet(tail[2], "sliceSerialize"), tail[2], tail[1], true)).length >= kTabSize) {
-      return call1(ok, undef(), code);
+    JsValue tail = JS.get(self, "events")[JS.get(self, "events").length - 1.0];
+    if (tail.truthy() && JS.strictEqual(JS.get(tail[1], "type"), tLinePrefix) && JS.invoke(tail[2], "sliceSerialize", tail[1], true).length >= kTabSize) {
+      return JS.call(ok, JS.undefined(), code);
     }
     if (markdownLineEnding(code)) {
-      return call1(furtherStart, undef(), code);
+      return JS.call(furtherStart, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
   furtherStart = JS.method1((JsValue _s, JsValue code) => {
-    if (objGet(objGet(self, "parser"), "lazy")[objGet(call0(objGet(self, "now"), self), "line")].truthy()) {
-      return call1(nok, undef(), code);
+    if (JS.get(JS.get(self, "parser"), "lazy")[JS.get(JS.invoke(self, "now"), "line")].truthy()) {
+      return JS.call(nok, JS.undefined(), code);
     }
     if (markdownLineEnding(code)) {
-      call1(objGet(effects, "enter"), effects, tLineEnding);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tLineEnding);
+      JS.invoke(effects, "enter", tLineEnding);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tLineEnding);
       return furtherStart;
     }
-    return call1(factorySpace(effects, afterPrefix, tLinePrefix, kTabSize + 1), undef(), code);
+    return JS.call(factorySpace(effects, afterPrefix, tLinePrefix, kTabSize + 1), JS.undefined(), code);
   });
 
   return furtherStart;
 });
 
-JsValue furtherStartConstruct = makeConstruct(tokenizeFurtherStart);
-JS.set(furtherStartConstruct, "partial", true);
+JsValue furtherStartConstruct = JS.object("partial", true, "tokenize", tokenizeFurtherStart);
 
 JsValue tokenizeCodeIndented = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  JsValue atBreak = undef();
+  JsValue atBreak = JS.undefined();
+  JsValue inside = JS.undefined();
 
-  JsValue inside = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tCodeFlowValue);
-      return call1(atBreak, undef(), code);
-    }
-    call1(objGet(effects, "consume"), effects, code);
-    return inside;
+  JsValue after = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "exit", tCodeIndented);
+    return JS.call(ok, JS.undefined(), code);
   });
 
-  JsValue after = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "exit"), effects, tCodeIndented);
-    return call1(ok, undef(), code);
+  JsValue afterPrefix = JS.method1((JsValue _s, JsValue code) => {
+    JsValue tail = JS.get(self, "events")[JS.get(self, "events").length - 1.0];
+    if (tail.truthy() && JS.strictEqual(JS.get(tail[1], "type"), tLinePrefix) && JS.invoke(tail[2], "sliceSerialize", tail[1], true).length >= kTabSize) {
+      return JS.call(atBreak, JS.undefined(), code);
+    }
+    return JS.call(nok, JS.undefined(), code);
   });
 
   atBreak = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code)) {
-      return call1(after, undef(), code);
+    if (JS.strictEqual(code, null)) {
+      return JS.call(after, JS.undefined(), code);
     }
     if (markdownLineEnding(code)) {
-      return call1(call3(objGet(effects, "attempt"), effects, furtherStartConstruct, atBreak, after), undef(), code);
+      return JS.call(JS.invoke(effects, "attempt", furtherStartConstruct, atBreak, after), JS.undefined(), code);
     }
-    call1(objGet(effects, "enter"), effects, tCodeFlowValue);
-    return call1(inside, undef(), code);
+    JS.invoke(effects, "enter", tCodeFlowValue);
+    return JS.call(inside, JS.undefined(), code);
   });
 
-  JsValue afterPrefix = JS.method1((JsValue _s, JsValue code) => {
-    JsValue events = objGet(self, "events");
-    JsValue tail = events[arrLen(events) - 1];
-    if (tail.truthy() && objGet(tail[1], "type") == tLinePrefix && jsString(call2(objGet(tail[2], "sliceSerialize"), tail[2], tail[1], true)).length >= kTabSize) {
-      return call1(atBreak, undef(), code);
+  inside = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tCodeFlowValue);
+      return JS.call(atBreak, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    JS.invoke(effects, "consume", code);
+    return inside;
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tCodeIndented);
-    return call1(factorySpace(effects, afterPrefix, tLinePrefix, kTabSize + 1), undef(), code);
+  return JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tCodeIndented);
+    return JS.call(factorySpace(effects, afterPrefix, tLinePrefix, kTabSize + 1), JS.undefined(), code);
   });
-
-  return start;
 });
 
-export JsValue codeIndented = makeNamedConstruct("codeIndented", tokenizeCodeIndented);
+export JsValue codeIndented = JS.object("name", "codeIndented", "tokenize", tokenizeCodeIndented);
diff -ruN a/src/graph/remark-parse/micromark/core/code-text.lil b/src/graph/remark-parse/micromark/core/code-text.lil
--- a/src/graph/remark-parse/micromark/core/code-text.lil	2026-09-23 20:40:45.172440711 +0000
+++ b/src/graph/remark-parse/micromark/core/code-text.lil	2026-09-23 20:40:45.278692733 +0000
@@ -1,128 +1,132 @@
-import { arrLen, call1, isEof, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { markdownLineEnding } from "../util-character.lil";
 import { cGraveAccent, cSpace, codeEq } from "../symbol/codes.lil";
 import { tCharacterEscape, tCodeText, tCodeTextData, tCodeTextPadding, tCodeTextSequence, tLineEnding } from "../symbol/types.lil";
 
 JsValue resolveCodeText = JS.method2((JsValue _self, JsValue events, JsValue _context) => {
-  int tailExitIndex = arrLen(events) - 4;
-  int headEnterIndex = 3;
-  int index = 0;
-  int enter = -1;
-  if ((objGet(events[headEnterIndex][1], "type") == tLineEnding || objGet(events[headEnterIndex][1], "type") == "space") && (objGet(events[tailExitIndex][1], "type") == tLineEnding || objGet(events[tailExitIndex][1], "type") == "space")) {
+  float tailExitIndex = events.length - 4.0;
+  float headEnterIndex = 3.0;
+  float index = 0.0;
+  float enter = -1.0;
+  if ((JS.strictEqual(JS.get(events[headEnterIndex][1], "type"), tLineEnding) || JS.strictEqual(JS.get(events[headEnterIndex][1], "type"), "space")) && (JS.strictEqual(JS.get(events[tailExitIndex][1], "type"), tLineEnding) || JS.strictEqual(JS.get(events[tailExitIndex][1], "type"), "space"))) {
     index = headEnterIndex;
-    while (index + 1 < tailExitIndex) {
-      index = index + 1;
-      if (objGet(events[index][1], "type") == tCodeTextData) {
+    while (true) {
+      index++;
+      if (index >= tailExitIndex) {
+        break;
+      }
+      if (JS.strictEqual(JS.get(events[index][1], "type"), tCodeTextData)) {
         JS.set(events[headEnterIndex][1], "type", tCodeTextPadding);
         JS.set(events[tailExitIndex][1], "type", tCodeTextPadding);
-        headEnterIndex = headEnterIndex + 2;
-        tailExitIndex = tailExitIndex - 2;
+        headEnterIndex += 2.0;
+        tailExitIndex -= 2.0;
         break;
       }
     }
   }
-  index = headEnterIndex - 1;
-  tailExitIndex = tailExitIndex + 1;
-  while (index < tailExitIndex) {
-    index = index + 1;
-    if (enter < 0) {
-      if (index != tailExitIndex && objGet(events[index][1], "type") != tLineEnding) {
+  index = headEnterIndex - 1.0;
+  tailExitIndex++;
+  while (true) {
+    index++;
+    if (index > tailExitIndex) {
+      break;
+    }
+    if (enter < 0.0) {
+      if (index != tailExitIndex && JS.strictNotEqual(JS.get(events[index][1], "type"), tLineEnding)) {
         enter = index;
       }
-    } else if (index == tailExitIndex || objGet(events[index][1], "type") == tLineEnding) {
+    } else if (index == tailExitIndex || JS.strictEqual(JS.get(events[index][1], "type"), tLineEnding)) {
       JS.set(events[enter][1], "type", tCodeTextData);
-      if (index != enter + 2) {
-        JS.set(events[enter][1], "end", objGet(events[index - 1][1], "end"));
-        JS.call(JS.get(events, "splice"), events, enter + 2, index - enter - 2);
-        tailExitIndex = tailExitIndex - (index - enter - 2);
-        index = enter + 2;
+      if (index != enter + 2.0) {
+        JS.set(events[enter][1], "end", JS.get(events[index - 1.0][1], "end"));
+        JS.invoke(events, "splice", enter + 2.0, index - enter - 2.0);
+        tailExitIndex -= index - enter - 2.0;
+        index = enter + 2.0;
       }
-      enter = -1;
+      enter = -1.0;
     }
   }
   return events;
 });
 
 JsValue previousCodeText = JS.method1((JsValue self, JsValue code) => {
-  if (!codeEq(code, cGraveAccent)) {
-    return true;
-  }
-  JsValue events = objGet(self, "events");
-  return objGet(events[arrLen(events) - 1][1], "type") == tCharacterEscape;
+  return !codeEq(code, cGraveAccent) || JS.strictEqual(JS.get(JS.get(self, "events")[JS.get(self, "events").length - 1.0][1], "type"), tCharacterEscape);
 });
 
-JsValue tokenizeCodeText = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  int sizeOpen = 0;
-  int size = 0;
-  JsValue token = undef();
-  JsValue between = undef();
-
-  JsValue data = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || codeEq(code, cSpace) || codeEq(code, cGraveAccent) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tCodeTextData);
-      return call1(between, undef(), code);
-    }
-    call1(objGet(effects, "consume"), effects, code);
-    return data;
+JsValue tokenizeCodeText = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue nok) => {
+  float sizeOpen = 0.0;
+  float size = 0.0;
+  JsValue token = JS.undefined();
+  JsValue sequenceOpen = JS.undefined();
+  JsValue between = JS.undefined();
+  JsValue data = JS.undefined();
+  JsValue sequenceClose = JS.undefined();
+
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tCodeText);
+    JS.invoke(effects, "enter", tCodeTextSequence);
+    return JS.call(sequenceOpen, JS.undefined(), code);
   });
 
-  JsValue sequenceClose = JS.method1((JsValue _s, JsValue code) => {
+  sequenceOpen = JS.method1((JsValue _s, JsValue code) => {
     if (codeEq(code, cGraveAccent)) {
-      call1(objGet(effects, "consume"), effects, code);
-      size = size + 1;
-      return sequenceClose;
-    }
-    if (size == sizeOpen) {
-      call1(objGet(effects, "exit"), effects, tCodeTextSequence);
-      call1(objGet(effects, "exit"), effects, tCodeText);
-      return call1(ok, undef(), code);
+      JS.invoke(effects, "consume", code);
+      sizeOpen++;
+      return sequenceOpen;
     }
-    JS.set(token, "type", tCodeTextData);
-    return call1(data, undef(), code);
+    JS.invoke(effects, "exit", tCodeTextSequence);
+    return JS.call(between, JS.undefined(), code);
   });
 
   between = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code)) {
-      return call1(nok, undef(), code);
+    if (JS.strictEqual(code, null)) {
+      return JS.call(nok, JS.undefined(), code);
     }
     if (codeEq(code, cSpace)) {
-      call1(objGet(effects, "enter"), effects, "space");
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, "space");
+      JS.invoke(effects, "enter", "space");
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", "space");
       return between;
     }
     if (codeEq(code, cGraveAccent)) {
-      token = call1(objGet(effects, "enter"), effects, tCodeTextSequence);
-      size = 0;
-      return call1(sequenceClose, undef(), code);
+      token = JS.invoke(effects, "enter", tCodeTextSequence);
+      size = 0.0;
+      return JS.call(sequenceClose, JS.undefined(), code);
     }
     if (markdownLineEnding(code)) {
-      call1(objGet(effects, "enter"), effects, tLineEnding);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tLineEnding);
+      JS.invoke(effects, "enter", tLineEnding);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tLineEnding);
       return between;
     }
-    call1(objGet(effects, "enter"), effects, tCodeTextData);
-    return call1(data, undef(), code);
+    JS.invoke(effects, "enter", tCodeTextData);
+    return JS.call(data, JS.undefined(), code);
   });
 
-  JsValue sequenceOpen = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cGraveAccent)) {
-      call1(objGet(effects, "consume"), effects, code);
-      sizeOpen = sizeOpen + 1;
-      return sequenceOpen;
+  data = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || codeEq(code, cSpace) || codeEq(code, cGraveAccent) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tCodeTextData);
+      return JS.call(between, JS.undefined(), code);
     }
-    call1(objGet(effects, "exit"), effects, tCodeTextSequence);
-    return call1(between, undef(), code);
+    JS.invoke(effects, "consume", code);
+    return data;
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tCodeText);
-    call1(objGet(effects, "enter"), effects, tCodeTextSequence);
-    return call1(sequenceOpen, undef(), code);
+  sequenceClose = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cGraveAccent)) {
+      JS.invoke(effects, "consume", code);
+      size++;
+      return sequenceClose;
+    }
+    if (size == sizeOpen) {
+      JS.invoke(effects, "exit", tCodeTextSequence);
+      JS.invoke(effects, "exit", tCodeText);
+      return JS.call(ok, JS.undefined(), code);
+    }
+    JS.set(token, "type", tCodeTextData);
+    return JS.call(data, JS.undefined(), code);
   });
 
   return start;
 });
 
-export JsValue codeText = JS.object("name", "codeText", "tokenize", tokenizeCodeText, "previous", previousCodeText, "resolve", resolveCodeText);
+export JsValue codeText = JS.object("name", "codeText", "previous", previousCodeText, "resolve", resolveCodeText, "tokenize", tokenizeCodeText);
diff -ruN a/src/graph/remark-parse/micromark/core/content.lil b/src/graph/remark-parse/micromark/core/content.lil
--- a/src/graph/remark-parse/micromark/core/content.lil	2026-09-23 20:40:45.172853875 +0000
+++ b/src/graph/remark-parse/micromark/core/content.lil	2026-09-23 20:40:45.285692795 +0000
@@ -1,4 +1,3 @@
-import { arrLen, call1, call2, call3, includesStr, isEof, jsString, makeConstruct, objGet, undef } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { markdownLineEnding } from "../util-character.lil";
 import { subtokenize } from "../util-subtokenize.lil";
@@ -12,75 +11,65 @@
 
 JsValue tokenizeContinuation = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue prefixed = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      return call1(nok, undef(), code);
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    JsValue events = objGet(self, "events");
-    JsValue tail = events[arrLen(events) - 1];
-    bool disabled = includesStr(objGet(objGet(objGet(self, "parser"), "constructs"), "disable")["null"], "codeIndented");
-    if (!disabled && tail.truthy() && objGet(tail[1], "type") == tLinePrefix && jsString(call2(objGet(tail[2], "sliceSerialize"), tail[2], tail[1], true)).length >= kTabSize) {
-      return call1(ok, undef(), code);
+    JsValue tail = JS.get(self, "events")[JS.get(self, "events").length - 1.0];
+    if (!JS.invoke(JS.get(JS.get(JS.get(self, "parser"), "constructs"), "disable")["null"], "includes", "codeIndented").truthy() && tail.truthy() && JS.strictEqual(JS.get(tail[1], "type"), tLinePrefix) && JS.invoke(tail[2], "sliceSerialize", tail[1], true).length >= kTabSize) {
+      return JS.call(ok, JS.undefined(), code);
     }
-    return call1(call3(objGet(effects, "interrupt"), effects, objGet(objGet(self, "parser"), "constructs")["flow"], nok, ok), undef(), code);
+    return JS.call(JS.invoke(effects, "interrupt", JS.get(JS.get(JS.get(self, "parser"), "constructs"), "flow"), nok, ok), JS.undefined(), code);
   });
 
-  JsValue startLookahead = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "exit"), effects, tChunkContent);
-    call1(objGet(effects, "enter"), effects, tLineEnding);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tLineEnding);
-    return factorySpace(effects, prefixed, tLinePrefix, 0);
+  return JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "exit", tChunkContent);
+    JS.invoke(effects, "enter", tLineEnding);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tLineEnding);
+    return factorySpace(effects, prefixed, tLinePrefix, JS.undefined());
   });
-
-  return startLookahead;
 });
 
-JsValue continuationConstruct = makeConstruct(tokenizeContinuation);
-JS.set(continuationConstruct, "partial", true);
+JsValue continuationConstruct = JS.object("partial", true, "tokenize", tokenizeContinuation);
 
 JsValue tokenizeContent = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue _nok) => {
-  JsValue previous = undef();
-  JsValue contentEnd = undef();
-  JsValue contentContinue = undef();
-
-  JsValue chunkInside = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code)) {
-      return call1(contentEnd, undef(), code);
+  JsValue previous = JS.undefined();
+  JsValue chunkInside = JS.undefined();
+  JsValue contentEnd = JS.undefined();
+  JsValue contentContinue = JS.undefined();
+
+  JsValue chunkStart = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tContent);
+    previous = JS.invoke(effects, "enter", tChunkContent, JS.object("contentType", kContentTypeContent));
+    return JS.call(chunkInside, JS.undefined(), code);
+  });
+
+  chunkInside = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null)) {
+      return JS.call(contentEnd, JS.undefined(), code);
     }
     if (markdownLineEnding(code)) {
-      return call1(call3(objGet(effects, "check"), effects, continuationConstruct, contentContinue, contentEnd), undef(), code);
+      return JS.call(JS.invoke(effects, "check", continuationConstruct, contentContinue, contentEnd), JS.undefined(), code);
     }
-    call1(objGet(effects, "consume"), effects, code);
+    JS.invoke(effects, "consume", code);
     return chunkInside;
   });
 
   contentEnd = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "exit"), effects, tChunkContent);
-    call1(objGet(effects, "exit"), effects, tContent);
-    return call1(ok, undef(), code);
+    JS.invoke(effects, "exit", tChunkContent);
+    JS.invoke(effects, "exit", tContent);
+    return JS.call(ok, JS.undefined(), code);
   });
 
   contentContinue = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tChunkContent);
-    JsValue fields = JS.object();
-    JS.set(fields, "contentType", kContentTypeContent);
-    JS.set(fields, "previous", previous);
-    JsValue next = call2(objGet(effects, "enter"), effects, tChunkContent, fields);
-    JS.set(previous, "next", next);
-    previous = next;
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tChunkContent);
+    JS.set(previous, "next", JS.invoke(effects, "enter", tChunkContent, JS.object("contentType", kContentTypeContent, "previous", previous)));
+    previous = JS.get(previous, "next");
     return chunkInside;
   });
 
-  JsValue chunkStart = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tContent);
-    JsValue fields = JS.object();
-    JS.set(fields, "contentType", kContentTypeContent);
-    previous = call2(objGet(effects, "enter"), effects, tChunkContent, fields);
-    return call1(chunkInside, undef(), code);
-  });
-
   return chunkStart;
 });
 
-export JsValue content = JS.object("tokenize", tokenizeContent, "resolve", resolveContent);
+export JsValue content = JS.object("resolve", resolveContent, "tokenize", tokenizeContent);
diff -ruN a/src/graph/remark-parse/micromark/core/definition.lil b/src/graph/remark-parse/micromark/core/definition.lil
--- a/src/graph/remark-parse/micromark/core/definition.lil	2026-09-23 20:40:45.173197769 +0000
+++ b/src/graph/remark-parse/micromark/core/definition.lil	2026-09-23 20:40:45.286520168 +0000
@@ -1,4 +1,3 @@
-import { apply, arrLen, call1, call2, call3, isEof, jsString, makeConstruct, makeNamedConstruct, objGet, push, undef } from "../host.lil";
 import { factoryDestination } from "../factory-destination.lil";
 import { factoryLabel } from "../factory-label.lil";
 import { factorySpace } from "../factory-space.lil";
@@ -11,100 +10,91 @@
 
 JsValue tokenizeTitleBefore = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue titleAfterOptionalWhitespace = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      return call1(ok, undef(), code);
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      return JS.call(ok, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
   JsValue titleAfter = JS.method1((JsValue _s, JsValue code) => {
     if (markdownSpace(code)) {
-      return call1(factorySpace(effects, titleAfterOptionalWhitespace, tWhitespace, 0), undef(), code);
+      return JS.call(factorySpace(effects, titleAfterOptionalWhitespace, tWhitespace, JS.undefined()), JS.undefined(), code);
     }
-    return call1(titleAfterOptionalWhitespace, undef(), code);
+    return JS.call(titleAfterOptionalWhitespace, JS.undefined(), code);
   });
 
   JsValue beforeMarker = JS.method1((JsValue _s, JsValue code) => {
-    return call1(factoryTitle(effects, titleAfter, nok, tDefinitionTitle, tDefinitionTitleMarker, tDefinitionTitleString), undef(), code);
+    return JS.call(factoryTitle(effects, titleAfter, nok, tDefinitionTitle, tDefinitionTitleMarker, tDefinitionTitleString), JS.undefined(), code);
   });
 
-  JsValue titleBefore = JS.method1((JsValue _s, JsValue code) => {
+  return JS.method1((JsValue _s, JsValue code) => {
     if (markdownLineEndingOrSpace(code)) {
-      return call1(factoryWhitespace(effects, beforeMarker), undef(), code);
+      return JS.call(factoryWhitespace(effects, beforeMarker), JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
-
-  return titleBefore;
 });
 
-JsValue titleBeforeConstruct = makeConstruct(tokenizeTitleBefore);
-JS.set(titleBeforeConstruct, "partial", true);
+JsValue titleBefore = JS.object("partial", true, "tokenize", tokenizeTitleBefore);
 
 JsValue tokenizeDefinition = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
   string identifier = "";
+  JsValue labelAfter = JS.undefined();
+  JsValue markerAfter = JS.undefined();
+  JsValue destinationBefore = JS.undefined();
+  JsValue destinationAfter = JS.undefined();
+  JsValue after = JS.undefined();
+  JsValue afterWhitespace = JS.undefined();
 
-  JsValue afterWhitespace = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tDefinition);
-      push(objGet(objGet(self, "parser"), "defined"), identifier);
-      return call1(ok, undef(), code);
-    }
-    return call1(nok, undef(), code);
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tDefinition);
+    return JS.call(factoryLabel(self, effects, labelAfter, nok, tDefinitionLabel, tDefinitionLabelMarker, tDefinitionLabelString), JS.undefined(), code);
   });
 
-  JsValue after = JS.method1((JsValue _s, JsValue code) => {
-    if (markdownSpace(code)) {
-      return call1(factorySpace(effects, afterWhitespace, tWhitespace, 0), undef(), code);
+  labelAfter = JS.method1((JsValue _s, JsValue code) => {
+    string label = JS.assume(JS.invoke(self, "sliceSerialize", JS.get(self, "events")[JS.get(self, "events").length - 1.0][1]));
+    identifier = normalizeIdentifier(label.slice(1, -1));
+    if (codeEq(code, cColon)) {
+      JS.invoke(effects, "enter", tDefinitionMarker);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tDefinitionMarker);
+      return markerAfter;
     }
-    return call1(afterWhitespace, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue destinationAfter = JS.method1((JsValue _s, JsValue code) => {
-    return call1(call3(objGet(effects, "attempt"), effects, titleBeforeConstruct, after, after), undef(), code);
+  markerAfter = JS.method1((JsValue _s, JsValue code) => {
+    if (markdownLineEndingOrSpace(code)) {
+      return JS.call(factoryWhitespace(effects, destinationBefore), JS.undefined(), code);
+    }
+    return JS.call(destinationBefore, JS.undefined(), code);
   });
 
-  JsValue destinationBefore = JS.method1((JsValue _s, JsValue code) => {
-    return call1(factoryDestination(effects, destinationAfter, nok, tDefinitionDestination, tDefinitionDestinationLiteral, tDefinitionDestinationLiteralMarker, tDefinitionDestinationRaw, tDefinitionDestinationString, 0), undef(), code);
+  destinationBefore = JS.method1((JsValue _s, JsValue code) => {
+    return JS.call(factoryDestination(effects, destinationAfter, nok, tDefinitionDestination, tDefinitionDestinationLiteral, tDefinitionDestinationLiteralMarker, tDefinitionDestinationRaw, tDefinitionDestinationString, JS.undefined()), JS.undefined(), code);
   });
 
-  JsValue markerAfter = JS.method1((JsValue _s, JsValue code) => {
-    if (markdownLineEndingOrSpace(code)) {
-      return call1(factoryWhitespace(effects, destinationBefore), undef(), code);
-    }
-    return call1(destinationBefore, undef(), code);
+  destinationAfter = JS.method1((JsValue _s, JsValue code) => {
+    return JS.call(JS.invoke(effects, "attempt", titleBefore, after, after), JS.undefined(), code);
   });
 
-  JsValue labelAfter = JS.method1((JsValue _s, JsValue code) => {
-    JsValue events = objGet(self, "events");
-    string raw = jsString(call1(objGet(self, "sliceSerialize"), self, events[arrLen(events) - 1][1]));
-    identifier = normalizeIdentifier(jsString(JS.call(JS.get(raw, "slice"), raw, 1, -1)));
-    if (codeEq(code, cColon)) {
-      call1(objGet(effects, "enter"), effects, tDefinitionMarker);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tDefinitionMarker);
-      return markerAfter;
+  after = JS.method1((JsValue _s, JsValue code) => {
+    if (markdownSpace(code)) {
+      return JS.call(factorySpace(effects, afterWhitespace, tWhitespace, JS.undefined()), JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(afterWhitespace, JS.undefined(), code);
   });
 
-  JsValue before = JS.method1((JsValue _s, JsValue code) => {
-    JsValue labelArgs = JS.array();
-    push(labelArgs, effects);
-    push(labelArgs, labelAfter);
-    push(labelArgs, nok);
-    push(labelArgs, tDefinitionLabel);
-    push(labelArgs, tDefinitionLabelMarker);
-    push(labelArgs, tDefinitionLabelString);
-    return call1(apply(factoryLabel, self, labelArgs), undef(), code);
-  });
-
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tDefinition);
-    return call1(before, undef(), code);
+  afterWhitespace = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tDefinition);
+      JS.invoke(JS.get(JS.get(self, "parser"), "defined"), "push", identifier);
+      return JS.call(ok, JS.undefined(), code);
+    }
+    return JS.call(nok, JS.undefined(), code);
   });
 
   return start;
 });
 
-export JsValue definition = makeNamedConstruct("definition", tokenizeDefinition);
+export JsValue definition = JS.object("name", "definition", "tokenize", tokenizeDefinition);
diff -ruN a/src/graph/remark-parse/micromark/core/hard-break-escape.lil b/src/graph/remark-parse/micromark/core/hard-break-escape.lil
--- a/src/graph/remark-parse/micromark/core/hard-break-escape.lil	2026-09-23 20:40:45.173537776 +0000
+++ b/src/graph/remark-parse/micromark/core/hard-break-escape.lil	2026-09-23 20:40:45.286692804 +0000
@@ -1,23 +1,20 @@
-import { call1, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { markdownLineEnding } from "../util-character.lil";
 import { tHardBreakEscape } from "../symbol/types.lil";
 
 JsValue tokenizeHardBreakEscape = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue after = JS.method1((JsValue _s, JsValue code) => {
     if (markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tHardBreakEscape);
-      return call1(ok, undef(), code);
+      JS.invoke(effects, "exit", tHardBreakEscape);
+      return JS.call(ok, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tHardBreakEscape);
-    call1(objGet(effects, "consume"), effects, code);
+  return JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tHardBreakEscape);
+    JS.invoke(effects, "consume", code);
     return after;
   });
-
-  return start;
 });
 
-export JsValue hardBreakEscape = makeNamedConstruct("hardBreakEscape", tokenizeHardBreakEscape);
+export JsValue hardBreakEscape = JS.object("name", "hardBreakEscape", "tokenize", tokenizeHardBreakEscape);
diff -ruN a/src/graph/remark-parse/micromark/core/heading-atx.lil b/src/graph/remark-parse/micromark/core/heading-atx.lil
--- a/src/graph/remark-parse/micromark/core/heading-atx.lil	2026-09-23 20:40:45.173691809 +0000
+++ b/src/graph/remark-parse/micromark/core/heading-atx.lil	2026-09-23 20:40:45.287166448 +0000
@@ -1,111 +1,91 @@
-import { arrLen, call1, eventEnter, eventExit, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { markdownLineEnding, markdownLineEndingOrSpace, markdownSpace } from "../util-character.lil";
 import { splice } from "../util-chunked.lil";
 import { cNumberSign, codeEq } from "../symbol/codes.lil";
 import { kAtxHeadingOpeningFenceSizeMax, kContentTypeText } from "../symbol/constants.lil";
 import { tAtxHeading, tAtxHeadingSequence, tAtxHeadingText, tChunkText, tWhitespace } from "../symbol/types.lil";
-import { isEof } from "../host.lil";
 
 JsValue resolveHeadingAtx = JS.method2((JsValue _self, JsValue events, JsValue context) => {
-  int contentEnd = arrLen(events) - 2;
-  int contentStart = 3;
-  if (objGet(events[contentStart][1], "type") == tWhitespace) {
-    contentStart = contentStart + 2;
+  float contentEnd = events.length - 2.0;
+  float contentStart = 3.0;
+  if (JS.strictEqual(JS.get(events[contentStart][1], "type"), tWhitespace)) {
+    contentStart += 2.0;
   }
-  if (contentEnd - 2 > contentStart && objGet(events[contentEnd][1], "type") == tWhitespace) {
-    contentEnd = contentEnd - 2;
+  if (contentEnd - 2.0 > contentStart && JS.strictEqual(JS.get(events[contentEnd][1], "type"), tWhitespace)) {
+    contentEnd -= 2.0;
   }
-  if (objGet(events[contentEnd][1], "type") == tAtxHeadingSequence && (contentStart == contentEnd - 1 || (contentEnd - 4 > contentStart && objGet(events[contentEnd - 2][1], "type") == tWhitespace))) {
-    if (contentStart + 1 == contentEnd) {
-      contentEnd = contentEnd - 2;
-    } else {
-      contentEnd = contentEnd - 4;
-    }
+  if (JS.strictEqual(JS.get(events[contentEnd][1], "type"), tAtxHeadingSequence) && (contentStart == contentEnd - 1.0 || (contentEnd - 4.0 > contentStart && JS.strictEqual(JS.get(events[contentEnd - 2.0][1], "type"), tWhitespace)))) {
+    contentEnd -= if (contentStart + 1.0 == contentEnd) { 2.0 } else { 4.0 };
   }
   if (contentEnd > contentStart) {
-    JsValue content = JS.object();
-    JS.set(content, "type", tAtxHeadingText);
-    JS.set(content, "start", objGet(events[contentStart][1], "start"));
-    JS.set(content, "end", objGet(events[contentEnd][1], "end"));
-    JsValue text = JS.object();
-    JS.set(text, "type", tChunkText);
-    JS.set(text, "start", objGet(events[contentStart][1], "start"));
-    JS.set(text, "end", objGet(events[contentEnd][1], "end"));
-    JS.set(text, "contentType", kContentTypeText);
-    JsValue replacement = JS.array(
-      eventEnter(content, context),
-      eventEnter(text, context),
-      eventExit(text, context),
-      eventExit(content, context)
-    );
-    splice(events, contentStart, contentEnd - contentStart + 1, replacement);
+    JsValue content = JS.object("type", tAtxHeadingText, "start", JS.get(events[contentStart][1], "start"), "end", JS.get(events[contentEnd][1], "end"));
+    JsValue text = JS.object("type", tChunkText, "start", JS.get(events[contentStart][1], "start"), "end", JS.get(events[contentEnd][1], "end"), "contentType", kContentTypeText);
+    splice(events, contentStart, contentEnd - contentStart + 1.0, JS.array(JS.array("enter", content, context), JS.array("enter", text, context), JS.array("exit", text, context), JS.array("exit", content, context)));
   }
   return events;
 });
 
 JsValue tokenizeHeadingAtx = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue nok) => {
-  int size = 0;
-  JsValue atBreak = undef();
+  float size = 0.0;
+  JsValue sequenceOpen = JS.undefined();
+  JsValue atBreak = JS.undefined();
+  JsValue sequenceFurther = JS.undefined();
+  JsValue data = JS.undefined();
 
-  JsValue data = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || codeEq(code, cNumberSign) || markdownLineEndingOrSpace(code)) {
-      call1(objGet(effects, "exit"), effects, tAtxHeadingText);
-      return call1(atBreak, undef(), code);
-    }
-    call1(objGet(effects, "consume"), effects, code);
-    return data;
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tAtxHeading);
+    JS.invoke(effects, "enter", tAtxHeadingSequence);
+    return JS.call(sequenceOpen, JS.undefined(), code);
   });
 
-  JsValue sequenceFurther = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cNumberSign)) {
-      call1(objGet(effects, "consume"), effects, code);
-      return sequenceFurther;
+  sequenceOpen = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cNumberSign) && size < kAtxHeadingOpeningFenceSizeMax) {
+      size++;
+      JS.invoke(effects, "consume", code);
+      return sequenceOpen;
     }
-    call1(objGet(effects, "exit"), effects, tAtxHeadingSequence);
-    return call1(atBreak, undef(), code);
+    if (JS.strictEqual(code, null) || markdownLineEndingOrSpace(code)) {
+      JS.invoke(effects, "exit", tAtxHeadingSequence);
+      return JS.call(atBreak, JS.undefined(), code);
+    }
+    return JS.call(nok, JS.undefined(), code);
   });
 
   atBreak = JS.method1((JsValue _s, JsValue code) => {
     if (codeEq(code, cNumberSign)) {
-      call1(objGet(effects, "enter"), effects, tAtxHeadingSequence);
-      return call1(sequenceFurther, undef(), code);
+      JS.invoke(effects, "enter", tAtxHeadingSequence);
+      return JS.call(sequenceFurther, JS.undefined(), code);
     }
-    if (isEof(code) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tAtxHeading);
-      return call1(ok, undef(), code);
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tAtxHeading);
+      return JS.call(ok, JS.undefined(), code);
     }
     if (markdownSpace(code)) {
-      return call1(factorySpace(effects, atBreak, tWhitespace, 0), undef(), code);
+      return JS.call(factorySpace(effects, atBreak, tWhitespace, JS.undefined()), JS.undefined(), code);
     }
-    call1(objGet(effects, "enter"), effects, tAtxHeadingText);
-    return call1(data, undef(), code);
+    JS.invoke(effects, "enter", tAtxHeadingText);
+    return JS.call(data, JS.undefined(), code);
   });
 
-  JsValue sequenceOpen = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cNumberSign) && size < kAtxHeadingOpeningFenceSizeMax) {
-      size = size + 1;
-      call1(objGet(effects, "consume"), effects, code);
-      return sequenceOpen;
-    }
-    if (isEof(code) || markdownLineEndingOrSpace(code)) {
-      call1(objGet(effects, "exit"), effects, tAtxHeadingSequence);
-      return call1(atBreak, undef(), code);
+  sequenceFurther = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cNumberSign)) {
+      JS.invoke(effects, "consume", code);
+      return sequenceFurther;
     }
-    return call1(nok, undef(), code);
+    JS.invoke(effects, "exit", tAtxHeadingSequence);
+    return JS.call(atBreak, JS.undefined(), code);
   });
 
-  JsValue before = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tAtxHeadingSequence);
-    return call1(sequenceOpen, undef(), code);
-  });
-
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tAtxHeading);
-    return call1(before, undef(), code);
+  data = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || codeEq(code, cNumberSign) || markdownLineEndingOrSpace(code)) {
+      JS.invoke(effects, "exit", tAtxHeadingText);
+      return JS.call(atBreak, JS.undefined(), code);
+    }
+    JS.invoke(effects, "consume", code);
+    return data;
   });
 
   return start;
 });
 
-export JsValue headingAtx = JS.object("name", "headingAtx", "tokenize", tokenizeHeadingAtx, "resolve", resolveHeadingAtx);
+export JsValue headingAtx = JS.object("name", "headingAtx", "resolve", resolveHeadingAtx, "tokenize", tokenizeHeadingAtx);
diff -ruN a/src/graph/remark-parse/micromark/core/html-flow.lil b/src/graph/remark-parse/micromark/core/html-flow.lil
--- a/src/graph/remark-parse/micromark/core/html-flow.lil	2026-09-23 20:40:45.174217950 +0000
+++ b/src/graph/remark-parse/micromark/core/html-flow.lil	2026-09-23 20:40:45.287492630 +0000
@@ -1,457 +1,453 @@
-import { arrLen, call0, call1, call2, call3, fromCharCode1, includesVal, isEof, makeConstruct, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { asciiAlpha, asciiAlphanumeric, markdownLineEnding, markdownLineEndingOrSpace, markdownSpace } from "../util-character.lil";
 import { htmlBlockNames, htmlRawNames } from "../util-html-tag-name.lil";
-import { cColon, cDash, cDot, cExclamationMark, cGreaterThan, cLeftSquareBracket, cLessThan, cQuestionMark, cRightSquareBracket, cSlash, cUnderscore, codeEq, codeInt } from "../symbol/codes.lil";
+import { cColon, cDash, cDot, cExclamationMark, cGreaterThan, cLeftSquareBracket, cLessThan, cQuestionMark, cRightSquareBracket, cSlash, cUnderscore, codeEq } from "../symbol/codes.lil";
 import { cApostrophe, cEqualsTo, cGraveAccent, cQuotationMark } from "../symbol/codes.lil";
 import { kCdataOpeningString, kHtmlBasic, kHtmlCdata, kHtmlComment, kHtmlComplete, kHtmlDeclaration, kHtmlInstruction, kHtmlRaw, kHtmlRawSizeMax } from "../symbol/constants.lil";
 import { tHtmlFlow, tHtmlFlowData, tLineEnding, tLinePrefix } from "../symbol/types.lil";
 import { blankLine } from "./blank-line.lil";
 
+extern JsValue String;
+
 JsValue resolveToHtmlFlow = JS.method2((JsValue _self, JsValue events, JsValue _context) => {
-  int index = arrLen(events);
+  float index = events.length;
   while (true) {
-    index = index - 1;
-    if (index < 0) {
-      break;
-    }
-    if (events[index][0] == "enter" && objGet(events[index][1], "type") == tHtmlFlow) {
+    index--;
+    if (index < 0.0 || (JS.strictEqual(events[index][0], "enter") && JS.strictEqual(JS.get(events[index][1], "type"), tHtmlFlow))) {
       break;
     }
   }
-  if (index > 1 && objGet(events[index - 2][1], "type") == tLinePrefix) {
-    JS.set(events[index][1], "start", objGet(events[index - 2][1], "start"));
-    JS.set(events[index + 1][1], "start", objGet(events[index - 2][1], "start"));
-    JS.call(JS.get(events, "splice"), events, index - 2, 2);
+  if (index > 1.0 && JS.strictEqual(JS.get(events[index - 2.0][1], "type"), tLinePrefix)) {
+    JS.set(events[index][1], "start", JS.get(events[index - 2.0][1], "start"));
+    JS.set(events[index + 1.0][1], "start", JS.get(events[index - 2.0][1], "start"));
+    JS.invoke(events, "splice", index - 2.0, 2);
   }
   return events;
 });
 
 JsValue tokenizeNonLazyContinuationStart = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue after = JS.method1((JsValue _s, JsValue code) => {
-    if (objGet(objGet(self, "parser"), "lazy")[objGet(call0(objGet(self, "now"), self), "line")].truthy()) {
-      return call1(nok, undef(), code);
+    if (JS.get(JS.get(self, "parser"), "lazy")[JS.get(JS.invoke(self, "now"), "line")].truthy()) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    return call1(ok, undef(), code);
+    return JS.call(ok, JS.undefined(), code);
   });
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+
+  return JS.method1((JsValue _s, JsValue code) => {
     if (markdownLineEnding(code)) {
-      call1(objGet(effects, "enter"), effects, tLineEnding);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tLineEnding);
+      JS.invoke(effects, "enter", tLineEnding);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tLineEnding);
       return after;
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
-  return start;
 });
 
-JsValue nonLazyContinuationStart = makeConstruct(tokenizeNonLazyContinuationStart);
-JS.set(nonLazyContinuationStart, "partial", true);
+JsValue nonLazyContinuationStart = JS.object("partial", true, "tokenize", tokenizeNonLazyContinuationStart);
 
 JsValue tokenizeBlankLineBefore = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue nok) => {
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tLineEnding);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tLineEnding);
-    return call3(objGet(effects, "attempt"), effects, blankLine, ok, nok);
+  return JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tLineEnding);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tLineEnding);
+    return JS.invoke(effects, "attempt", blankLine, ok, nok);
   });
-  return start;
 });
 
-JsValue blankLineBefore = makeConstruct(tokenizeBlankLineBefore);
-JS.set(blankLineBefore, "partial", true);
+JsValue blankLineBefore = JS.object("partial", true, "tokenize", tokenizeBlankLineBefore);
 
 JsValue tokenizeHtmlFlow = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  int marker = 0;
+  float marker = 0.0;
   bool closingTag = false;
   string buffer = "";
-  int index = 0;
-  int markerB = 0;
-  JsValue continuationAfter = undef();
-  JsValue continuationStart = undef();
-  JsValue continuationBefore = undef();
-  JsValue continuationCommentInside = undef();
-  JsValue continuationRawTagOpen = undef();
-  JsValue continuationCdataInside = undef();
-  JsValue continuationDeclarationInside = undef();
-  JsValue completeAttributeNameBefore = undef();
-  JsValue completeAttributeNameAfter = undef();
-
-  JsValue consume = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "consume"), effects, code);
-    return undef();
-  });
-
-  continuationAfter = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "exit"), effects, tHtmlFlow);
-    return call1(ok, undef(), code);
-  });
+  float index = 0.0;
+  JsValue markerB = JS.undefined();
+  JsValue open = JS.undefined();
+  JsValue declarationOpen = JS.undefined();
+  JsValue commentOpenInside = JS.undefined();
+  JsValue cdataOpenInside = JS.undefined();
+  JsValue tagCloseStart = JS.undefined();
+  JsValue tagName = JS.undefined();
+  JsValue basicSelfClosing = JS.undefined();
+  JsValue completeClosingTagAfter = JS.undefined();
+  JsValue completeAttributeNameBefore = JS.undefined();
+  JsValue completeAttributeName = JS.undefined();
+  JsValue completeAttributeNameAfter = JS.undefined();
+  JsValue completeAttributeValueBefore = JS.undefined();
+  JsValue completeAttributeValueQuoted = JS.undefined();
+  JsValue completeAttributeValueUnquoted = JS.undefined();
+  JsValue completeAttributeValueQuotedAfter = JS.undefined();
+  JsValue completeEnd = JS.undefined();
+  JsValue completeAfter = JS.undefined();
+  JsValue continuation = JS.undefined();
+  JsValue continuationStart = JS.undefined();
+  JsValue continuationStartNonLazy = JS.undefined();
+  JsValue continuationBefore = JS.undefined();
+  JsValue continuationCommentInside = JS.undefined();
+  JsValue continuationRawTagOpen = JS.undefined();
+  JsValue continuationRawEndTag = JS.undefined();
+  JsValue continuationCdataInside = JS.undefined();
+  JsValue continuationDeclarationInside = JS.undefined();
+  JsValue continuationClose = JS.undefined();
+  JsValue continuationAfter = JS.undefined();
+
+  auto consume = (JsValue code) => {
+    JS.invoke(effects, "consume", code);
+  };
 
-  JsValue continuationClose = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tHtmlFlowData);
-      return call1(continuationAfter, undef(), code);
-    }
-    call1(consume, undef(), code);
-    return continuationClose;
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tHtmlFlow);
+    JS.invoke(effects, "enter", tHtmlFlowData);
+    consume(code);
+    return open;
   });
 
-  JsValue continuation = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDash) && marker == kHtmlComment) {
-      call1(consume, undef(), code);
-      return continuationCommentInside;
+  open = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cExclamationMark)) {
+      consume(code);
+      return declarationOpen;
     }
-    if (codeEq(code, cLessThan) && marker == kHtmlRaw) {
-      call1(consume, undef(), code);
-      return continuationRawTagOpen;
+    if (codeEq(code, cSlash)) {
+      consume(code);
+      closingTag = true;
+      return tagCloseStart;
     }
-    if (codeEq(code, cGreaterThan) && marker == kHtmlDeclaration) {
-      call1(consume, undef(), code);
-      return continuationClose;
+    if (codeEq(code, cQuestionMark)) {
+      consume(code);
+      marker = kHtmlInstruction;
+      return if (JS.get(self, "interrupt").truthy()) { ok } else { continuationDeclarationInside };
     }
-    if (codeEq(code, cQuestionMark) && marker == kHtmlInstruction) {
-      call1(consume, undef(), code);
-      return continuationDeclarationInside;
+    if (asciiAlpha(code)) {
+      consume(code);
+      buffer = JS.assume(JS.invoke(String, "fromCharCode", code));
+      return tagName;
     }
-    if (codeEq(code, cRightSquareBracket) && marker == kHtmlCdata) {
-      call1(consume, undef(), code);
-      return continuationCdataInside;
+    return JS.call(nok, JS.undefined(), code);
+  });
+
+  declarationOpen = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDash)) {
+      consume(code);
+      marker = kHtmlComment;
+      return commentOpenInside;
     }
-    if (markdownLineEnding(code) && (marker == kHtmlBasic || marker == kHtmlComplete)) {
-      call1(objGet(effects, "exit"), effects, tHtmlFlowData);
-      return call1(call3(objGet(effects, "check"), effects, blankLineBefore, continuationAfter, continuationStart), undef(), code);
+    if (codeEq(code, cLeftSquareBracket)) {
+      consume(code);
+      marker = kHtmlCdata;
+      index = 0;
+      return cdataOpenInside;
     }
-    if (isEof(code) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tHtmlFlowData);
-      return call1(continuationStart, undef(), code);
+    if (asciiAlpha(code)) {
+      consume(code);
+      marker = kHtmlDeclaration;
+      return if (JS.get(self, "interrupt").truthy()) { ok } else { continuationDeclarationInside };
     }
-    call1(consume, undef(), code);
-    return continuation;
-  });
-
-  JsValue continuationStartNonLazy = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tLineEnding);
-    call1(consume, undef(), code);
-    call1(objGet(effects, "exit"), effects, tLineEnding);
-    return continuationBefore;
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  continuationStart = JS.method1((JsValue _s, JsValue code) => {
-    return call1(call3(objGet(effects, "check"), effects, nonLazyContinuationStart, continuationStartNonLazy, continuationAfter), undef(), code);
+  commentOpenInside = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDash)) {
+      consume(code);
+      return if (JS.get(self, "interrupt").truthy()) { ok } else { continuationDeclarationInside };
+    }
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  continuationBefore = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      return call1(continuationStart, undef(), code);
+  cdataOpenInside = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, JS.invoke(kCdataOpeningString, "charCodeAt", index))) {
+      index++;
+      consume(code);
+      if (index == kCdataOpeningString.length) {
+        return if (JS.get(self, "interrupt").truthy()) { ok } else { continuation };
+      }
+      return cdataOpenInside;
     }
-    call1(objGet(effects, "enter"), effects, tHtmlFlowData);
-    return call1(continuation, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  continuationCommentInside = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDash)) {
-      call1(consume, undef(), code);
-      return continuationDeclarationInside;
+  tagCloseStart = JS.method1((JsValue _s, JsValue code) => {
+    if (asciiAlpha(code)) {
+      consume(code);
+      buffer = JS.assume(JS.invoke(String, "fromCharCode", code));
+      return tagName;
     }
-    return call1(continuation, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue continuationRawEndTag = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cGreaterThan)) {
+  tagName = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || codeEq(code, cSlash) || codeEq(code, cGreaterThan) || markdownLineEndingOrSpace(code)) {
+      bool slash = codeEq(code, cSlash);
       string name = buffer.toLowerCase();
-      if (includesVal(htmlRawNames, name)) {
-        call1(consume, undef(), code);
-        return continuationClose;
+      if (!slash && !closingTag && htmlRawNames.includes(name)) {
+        marker = kHtmlRaw;
+        if (JS.get(self, "interrupt").truthy()) {
+          return JS.call(ok, JS.undefined(), code);
+        }
+        return JS.call(continuation, JS.undefined(), code);
+      }
+      if (htmlBlockNames.includes(name)) {
+        marker = kHtmlBasic;
+        if (slash) {
+          consume(code);
+          return basicSelfClosing;
+        }
+        if (JS.get(self, "interrupt").truthy()) {
+          return JS.call(ok, JS.undefined(), code);
+        }
+        return JS.call(continuation, JS.undefined(), code);
+      }
+      marker = kHtmlComplete;
+      if (JS.get(self, "interrupt").truthy() && !JS.get(JS.get(self, "parser"), "lazy")[JS.get(JS.invoke(self, "now"), "line")].truthy()) {
+        return JS.call(nok, JS.undefined(), code);
+      }
+      if (closingTag) {
+        return JS.call(completeClosingTagAfter, JS.undefined(), code);
       }
-      return call1(continuation, undef(), code);
+      return JS.call(completeAttributeNameBefore, JS.undefined(), code);
     }
-    if (asciiAlpha(code) && buffer.length < kHtmlRawSizeMax) {
-      call1(consume, undef(), code);
-      buffer = buffer + fromCharCode1(code);
-      return continuationRawEndTag;
+    if (codeEq(code, cDash) || asciiAlphanumeric(code)) {
+      consume(code);
+      buffer += JS.assume(JS.invoke(String, "fromCharCode", code));
+      return tagName;
     }
-    return call1(continuation, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  continuationRawTagOpen = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cSlash)) {
-      call1(consume, undef(), code);
-      buffer = "";
-      return continuationRawEndTag;
+  basicSelfClosing = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cGreaterThan)) {
+      consume(code);
+      return if (JS.get(self, "interrupt").truthy()) { ok } else { continuation };
     }
-    return call1(continuation, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  continuationCdataInside = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cRightSquareBracket)) {
-      call1(consume, undef(), code);
-      return continuationDeclarationInside;
+  completeClosingTagAfter = JS.method1((JsValue _s, JsValue code) => {
+    if (markdownSpace(code)) {
+      consume(code);
+      return completeClosingTagAfter;
     }
-    return call1(continuation, undef(), code);
+    return JS.call(completeEnd, JS.undefined(), code);
   });
 
-  continuationDeclarationInside = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cGreaterThan)) {
-      call1(consume, undef(), code);
-      return continuationClose;
+  completeAttributeNameBefore = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cSlash)) {
+      consume(code);
+      return completeEnd;
     }
-    if (codeEq(code, cDash) && marker == kHtmlComment) {
-      call1(consume, undef(), code);
-      return continuationDeclarationInside;
+    if (codeEq(code, cColon) || codeEq(code, cUnderscore) || asciiAlpha(code)) {
+      consume(code);
+      return completeAttributeName;
     }
-    return call1(continuation, undef(), code);
+    if (markdownSpace(code)) {
+      consume(code);
+      return completeAttributeNameBefore;
+    }
+    return JS.call(completeEnd, JS.undefined(), code);
   });
 
-  JsValue completeAfter = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      return call1(continuation, undef(), code);
-    }
-    if (markdownSpace(code)) {
-      call1(consume, undef(), code);
-      return completeAfter;
+  completeAttributeName = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDash) || codeEq(code, cDot) || codeEq(code, cColon) || codeEq(code, cUnderscore) || asciiAlphanumeric(code)) {
+      consume(code);
+      return completeAttributeName;
     }
-    return call1(nok, undef(), code);
+    return JS.call(completeAttributeNameAfter, JS.undefined(), code);
   });
 
-  JsValue completeEnd = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cGreaterThan)) {
-      call1(consume, undef(), code);
-      return completeAfter;
+  completeAttributeNameAfter = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cEqualsTo)) {
+      consume(code);
+      return completeAttributeValueBefore;
     }
-    return call1(nok, undef(), code);
+    if (markdownSpace(code)) {
+      consume(code);
+      return completeAttributeNameAfter;
+    }
+    return JS.call(completeAttributeNameBefore, JS.undefined(), code);
   });
 
-  JsValue completeAttributeValueQuotedAfter = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cSlash) || codeEq(code, cGreaterThan) || markdownSpace(code)) {
-      return call1(completeAttributeNameBefore, undef(), code);
+  completeAttributeValueBefore = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || codeEq(code, cLessThan) || codeEq(code, cEqualsTo) || codeEq(code, cGreaterThan) || codeEq(code, cGraveAccent)) {
+      return JS.call(nok, JS.undefined(), code);
+    }
+    if (codeEq(code, cQuotationMark) || codeEq(code, cApostrophe)) {
+      consume(code);
+      markerB = code;
+      return completeAttributeValueQuoted;
+    }
+    if (markdownSpace(code)) {
+      consume(code);
+      return completeAttributeValueBefore;
     }
-    return call1(nok, undef(), code);
+    return JS.call(completeAttributeValueUnquoted, JS.undefined(), code);
   });
 
-  JsValue completeAttributeValueQuoted = JS.method1((JsValue _s, JsValue code) => {
-    if (code == markerB) {
-      call1(consume, undef(), code);
-      markerB = 0;
+  completeAttributeValueQuoted = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, markerB)) {
+      consume(code);
+      markerB = null;
       return completeAttributeValueQuotedAfter;
     }
-    if (isEof(code) || markdownLineEnding(code)) {
-      return call1(nok, undef(), code);
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    call1(consume, undef(), code);
+    consume(code);
     return completeAttributeValueQuoted;
   });
 
-  JsValue completeAttributeValueUnquoted = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || codeEq(code, cQuotationMark) || codeEq(code, cApostrophe) || codeEq(code, cSlash) || codeEq(code, cLessThan) || codeEq(code, cEqualsTo) || codeEq(code, cGreaterThan) || codeEq(code, cGraveAccent) || markdownLineEndingOrSpace(code)) {
-      return call1(completeAttributeNameAfter, undef(), code);
+  completeAttributeValueUnquoted = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || codeEq(code, cQuotationMark) || codeEq(code, cApostrophe) || codeEq(code, cSlash) || codeEq(code, cLessThan) || codeEq(code, cEqualsTo) || codeEq(code, cGreaterThan) || codeEq(code, cGraveAccent) || markdownLineEndingOrSpace(code)) {
+      return JS.call(completeAttributeNameAfter, JS.undefined(), code);
     }
-    call1(consume, undef(), code);
+    consume(code);
     return completeAttributeValueUnquoted;
   });
 
-  JsValue completeAttributeValueBefore = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || codeEq(code, cLessThan) || codeEq(code, cEqualsTo) || codeEq(code, cGreaterThan) || codeEq(code, cGraveAccent)) {
-      return call1(nok, undef(), code);
-    }
-    if (codeEq(code, cQuotationMark) || codeEq(code, cApostrophe)) {
-      call1(consume, undef(), code);
-      markerB = JS.number(code).toInt();
-      return completeAttributeValueQuoted;
+  completeAttributeValueQuotedAfter = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cSlash) || codeEq(code, cGreaterThan) || markdownSpace(code)) {
+      return JS.call(completeAttributeNameBefore, JS.undefined(), code);
     }
-    if (markdownSpace(code)) {
-      call1(consume, undef(), code);
-      return completeAttributeValueBefore;
+    return JS.call(nok, JS.undefined(), code);
+  });
+
+  completeEnd = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cGreaterThan)) {
+      consume(code);
+      return completeAfter;
     }
-    return call1(completeAttributeValueUnquoted, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  completeAttributeNameAfter = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cEqualsTo)) {
-      call1(consume, undef(), code);
-      return completeAttributeValueBefore;
+  completeAfter = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      return JS.call(continuation, JS.undefined(), code);
     }
     if (markdownSpace(code)) {
-      call1(consume, undef(), code);
-      return completeAttributeNameAfter;
+      consume(code);
+      return completeAfter;
     }
-    return call1(completeAttributeNameBefore, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue completeAttributeName = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDash) || codeEq(code, cDot) || codeEq(code, cColon) || codeEq(code, cUnderscore) || asciiAlphanumeric(code)) {
-      call1(consume, undef(), code);
-      return completeAttributeName;
+  continuation = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDash) && marker == kHtmlComment) {
+      consume(code);
+      return continuationCommentInside;
     }
-    return call1(completeAttributeNameAfter, undef(), code);
-  });
-
-  completeAttributeNameBefore = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cSlash)) {
-      call1(consume, undef(), code);
-      return completeEnd;
+    if (codeEq(code, cLessThan) && marker == kHtmlRaw) {
+      consume(code);
+      return continuationRawTagOpen;
     }
-    if (codeEq(code, cColon) || codeEq(code, cUnderscore) || asciiAlpha(code)) {
-      call1(consume, undef(), code);
-      return completeAttributeName;
+    if (codeEq(code, cGreaterThan) && marker == kHtmlDeclaration) {
+      consume(code);
+      return continuationClose;
     }
-    if (markdownSpace(code)) {
-      call1(consume, undef(), code);
-      return completeAttributeNameBefore;
+    if (codeEq(code, cQuestionMark) && marker == kHtmlInstruction) {
+      consume(code);
+      return continuationDeclarationInside;
+    }
+    if (codeEq(code, cRightSquareBracket) && marker == kHtmlCdata) {
+      consume(code);
+      return continuationCdataInside;
+    }
+    if (markdownLineEnding(code) && (marker == kHtmlBasic || marker == kHtmlComplete)) {
+      JS.invoke(effects, "exit", tHtmlFlowData);
+      return JS.call(JS.invoke(effects, "check", blankLineBefore, continuationAfter, continuationStart), JS.undefined(), code);
+    }
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tHtmlFlowData);
+      return JS.call(continuationStart, JS.undefined(), code);
     }
-    return call1(completeEnd, undef(), code);
+    consume(code);
+    return continuation;
   });
 
-  JsValue completeClosingTagAfter = JS.method1((JsValue _s, JsValue code) => {
-    if (markdownSpace(code)) {
-      call1(consume, undef(), code);
-      return completeClosingTagAfter;
-    }
-    return call1(completeEnd, undef(), code);
+  continuationStart = JS.method1((JsValue _s, JsValue code) => {
+    return JS.call(JS.invoke(effects, "check", nonLazyContinuationStart, continuationStartNonLazy, continuationAfter), JS.undefined(), code);
   });
 
-  JsValue basicSelfClosing = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cGreaterThan)) {
-      call1(consume, undef(), code);
-      if (objGet(self, "interrupt").truthy()) {
-        return ok;
-      }
-      return continuation;
-    }
-    return call1(nok, undef(), code);
+  continuationStartNonLazy = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tLineEnding);
+    consume(code);
+    JS.invoke(effects, "exit", tLineEnding);
+    return continuationBefore;
   });
 
-  JsValue tagName = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || codeEq(code, cSlash) || codeEq(code, cGreaterThan) || markdownLineEndingOrSpace(code)) {
-      bool slash = codeEq(code, cSlash);
-      string name = buffer.toLowerCase();
-      if (!slash && !closingTag && includesVal(htmlRawNames, name)) {
-        marker = kHtmlRaw;
-        if (objGet(self, "interrupt").truthy()) {
-          return call1(ok, undef(), code);
-        }
-        return call1(continuation, undef(), code);
-      }
-      if (includesVal(htmlBlockNames, name)) {
-        marker = kHtmlBasic;
-        if (slash) {
-          call1(consume, undef(), code);
-          return basicSelfClosing;
-        }
-        if (objGet(self, "interrupt").truthy()) {
-          return call1(ok, undef(), code);
-        }
-        return call1(continuation, undef(), code);
-      }
-      marker = kHtmlComplete;
-      if (objGet(self, "interrupt").truthy() && !objGet(objGet(self, "parser"), "lazy")[objGet(call0(objGet(self, "now"), self), "line")].truthy()) {
-        return call1(nok, undef(), code);
-      }
-      if (closingTag) {
-        return call1(completeClosingTagAfter, undef(), code);
-      }
-      return call1(completeAttributeNameBefore, undef(), code);
-    }
-    if (codeEq(code, cDash) || asciiAlphanumeric(code)) {
-      call1(consume, undef(), code);
-      buffer = buffer + fromCharCode1(code);
-      return tagName;
+  continuationBefore = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      return JS.call(continuationStart, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    JS.invoke(effects, "enter", tHtmlFlowData);
+    return JS.call(continuation, JS.undefined(), code);
   });
 
-  JsValue tagCloseStart = JS.method1((JsValue _s, JsValue code) => {
-    if (asciiAlpha(code)) {
-      call1(consume, undef(), code);
-      buffer = fromCharCode1(code);
-      return tagName;
+  continuationCommentInside = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDash)) {
+      consume(code);
+      return continuationDeclarationInside;
     }
-    return call1(nok, undef(), code);
+    return JS.call(continuation, JS.undefined(), code);
   });
 
-  JsValue cdataOpenInside = JS.method1((JsValue _s, JsValue code) => {
-    int currentIndex = index;
-    index = index + 1;
-    if (!isEof(code) && codeInt(code) == kCdataOpeningString.charCodeAt(currentIndex)) {
-      call1(consume, undef(), code);
-      if (currentIndex + 1 == kCdataOpeningString.length) {
-        if (objGet(self, "interrupt").truthy()) {
-          return ok;
-        }
-        return continuation;
-      }
-      return cdataOpenInside;
+  continuationRawTagOpen = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cSlash)) {
+      consume(code);
+      buffer = "";
+      return continuationRawEndTag;
     }
-    return call1(nok, undef(), code);
+    return JS.call(continuation, JS.undefined(), code);
   });
 
-  JsValue commentOpenInside = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDash)) {
-      call1(consume, undef(), code);
-      if (objGet(self, "interrupt").truthy()) {
-        return ok;
+  continuationRawEndTag = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cGreaterThan)) {
+      if (htmlRawNames.includes(buffer.toLowerCase())) {
+        consume(code);
+        return continuationClose;
       }
-      return continuationDeclarationInside;
+      return JS.call(continuation, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    if (asciiAlpha(code) && buffer.length < kHtmlRawSizeMax) {
+      consume(code);
+      buffer += JS.assume(JS.invoke(String, "fromCharCode", code));
+      return continuationRawEndTag;
+    }
+    return JS.call(continuation, JS.undefined(), code);
   });
 
-  JsValue declarationOpen = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDash)) {
-      call1(consume, undef(), code);
-      marker = kHtmlComment;
-      return commentOpenInside;
-    }
-    if (codeEq(code, cLeftSquareBracket)) {
-      call1(consume, undef(), code);
-      marker = kHtmlCdata;
-      index = 0;
-      return cdataOpenInside;
-    }
-    if (asciiAlpha(code)) {
-      call1(consume, undef(), code);
-      marker = kHtmlDeclaration;
-      if (objGet(self, "interrupt").truthy()) {
-        return ok;
-      }
+  continuationCdataInside = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cRightSquareBracket)) {
+      consume(code);
       return continuationDeclarationInside;
     }
-    return call1(nok, undef(), code);
+    return JS.call(continuation, JS.undefined(), code);
   });
 
-  JsValue open = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cExclamationMark)) {
-      call1(consume, undef(), code);
-      return declarationOpen;
-    }
-    if (codeEq(code, cSlash)) {
-      call1(consume, undef(), code);
-      closingTag = true;
-      return tagCloseStart;
+  continuationDeclarationInside = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cGreaterThan)) {
+      consume(code);
+      return continuationClose;
     }
-    if (codeEq(code, cQuestionMark)) {
-      call1(consume, undef(), code);
-      marker = kHtmlInstruction;
-      if (objGet(self, "interrupt").truthy()) {
-        return ok;
-      }
+    if (codeEq(code, cDash) && marker == kHtmlComment) {
+      consume(code);
       return continuationDeclarationInside;
     }
-    if (asciiAlpha(code)) {
-      call1(consume, undef(), code);
-      buffer = fromCharCode1(code);
-      return tagName;
+    return JS.call(continuation, JS.undefined(), code);
+  });
+
+  continuationClose = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tHtmlFlowData);
+      return JS.call(continuationAfter, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    consume(code);
+    return continuationClose;
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tHtmlFlow);
-    call1(objGet(effects, "enter"), effects, tHtmlFlowData);
-    call1(consume, undef(), code);
-    return open;
+  continuationAfter = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "exit", tHtmlFlow);
+    return JS.call(ok, JS.undefined(), code);
   });
 
   return start;
 });
 
-export JsValue htmlFlow = JS.object("name", "htmlFlow", "tokenize", tokenizeHtmlFlow, "concrete", true, "resolveTo", resolveToHtmlFlow);
+export JsValue htmlFlow = JS.object("concrete", true, "name", "htmlFlow", "resolveTo", resolveToHtmlFlow, "tokenize", tokenizeHtmlFlow);
diff -ruN a/src/graph/remark-parse/micromark/core/html-text.lil b/src/graph/remark-parse/micromark/core/html-text.lil
--- a/src/graph/remark-parse/micromark/core/html-text.lil	2026-09-23 20:40:45.174493537 +0000
+++ b/src/graph/remark-parse/micromark/core/html-text.lil	2026-09-23 20:40:45.287692813 +0000
@@ -1,368 +1,382 @@
-import { call1, includesStr, isEof, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { asciiAlpha, asciiAlphanumeric, markdownLineEnding, markdownLineEndingOrSpace, markdownSpace } from "../util-character.lil";
-import { cApostrophe, cColon, cDash, cDot, cEqualsTo, cExclamationMark, cGraveAccent, cGreaterThan, cLeftSquareBracket, cLessThan, cQuestionMark, cQuotationMark, cRightSquareBracket, cSlash, cUnderscore, codeEq, codeInt } from "../symbol/codes.lil";
+import { cApostrophe, cColon, cDash, cDot, cEqualsTo, cExclamationMark, cGraveAccent, cGreaterThan, cLeftSquareBracket, cLessThan, cQuestionMark, cQuotationMark, cRightSquareBracket, cSlash, cUnderscore, codeEq } from "../symbol/codes.lil";
 import { kCdataOpeningString, kTabSize } from "../symbol/constants.lil";
 import { tHtmlText, tHtmlTextData, tLineEnding, tLinePrefix } from "../symbol/types.lil";
 
 JsValue tokenizeHtmlText = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  int marker = 0;
-  int index = 0;
-  JsValue returnState = undef();
-  JsValue tagOpenBetween = undef();
-  JsValue instructionClose = undef();
-  JsValue cdataClose = undef();
-  JsValue commentClose = undef();
-  JsValue commentEnd = undef();
-
-  JsValue consume = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "consume"), effects, code);
-    return undef();
-  });
-
-  JsValue lineEndingAfterPrefix = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tHtmlTextData);
-    return call1(returnState, undef(), code);
-  });
-
-  JsValue lineEndingAfter = JS.method1((JsValue _s, JsValue code) => {
-    if (markdownSpace(code)) {
-      float max = kTabSize;
-      if (includesStr(objGet(objGet(objGet(self, "parser"), "constructs"), "disable")["null"], "codeIndented")) {
-        max = 0;
-      }
-      return call1(factorySpace(effects, lineEndingAfterPrefix, tLinePrefix, max), undef(), code);
-    }
-    return call1(lineEndingAfterPrefix, undef(), code);
-  });
+  JsValue marker = JS.undefined();
+  float index = 0.0;
+  JsValue returnState = JS.undefined();
+  JsValue open = JS.undefined();
+  JsValue declarationOpen = JS.undefined();
+  JsValue commentOpenInside = JS.undefined();
+  JsValue comment = JS.undefined();
+  JsValue commentClose = JS.undefined();
+  JsValue commentEnd = JS.undefined();
+  JsValue cdataOpenInside = JS.undefined();
+  JsValue cdata = JS.undefined();
+  JsValue cdataClose = JS.undefined();
+  JsValue cdataEnd = JS.undefined();
+  JsValue declaration = JS.undefined();
+  JsValue instruction = JS.undefined();
+  JsValue instructionClose = JS.undefined();
+  JsValue tagCloseStart = JS.undefined();
+  JsValue tagClose = JS.undefined();
+  JsValue tagCloseBetween = JS.undefined();
+  JsValue tagOpen = JS.undefined();
+  JsValue tagOpenBetween = JS.undefined();
+  JsValue tagOpenAttributeName = JS.undefined();
+  JsValue tagOpenAttributeNameAfter = JS.undefined();
+  JsValue tagOpenAttributeValueBefore = JS.undefined();
+  JsValue tagOpenAttributeValueQuoted = JS.undefined();
+  JsValue tagOpenAttributeValueUnquoted = JS.undefined();
+  JsValue tagOpenAttributeValueQuotedAfter = JS.undefined();
+  JsValue end = JS.undefined();
+  JsValue lineEndingBefore = JS.undefined();
+  JsValue lineEndingAfter = JS.undefined();
+  JsValue lineEndingAfterPrefix = JS.undefined();
+
+  auto consume = (JsValue code) => {
+    JS.invoke(effects, "consume", code);
+  };
 
-  JsValue lineEndingBefore = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "exit"), effects, tHtmlTextData);
-    call1(objGet(effects, "enter"), effects, tLineEnding);
-    call1(consume, undef(), code);
-    call1(objGet(effects, "exit"), effects, tLineEnding);
-    return lineEndingAfter;
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tHtmlText);
+    JS.invoke(effects, "enter", tHtmlTextData);
+    consume(code);
+    return open;
   });
 
-  JsValue end = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cGreaterThan)) {
-      call1(consume, undef(), code);
-      call1(objGet(effects, "exit"), effects, tHtmlTextData);
-      call1(objGet(effects, "exit"), effects, tHtmlText);
-      return ok;
+  open = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cExclamationMark)) {
+      consume(code);
+      return declarationOpen;
     }
-    return call1(nok, undef(), code);
-  });
-
-  JsValue tagOpenAttributeValueQuotedAfter = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cSlash) || codeEq(code, cGreaterThan) || markdownLineEndingOrSpace(code)) {
-      return call1(tagOpenBetween, undef(), code);
+    if (codeEq(code, cSlash)) {
+      consume(code);
+      return tagCloseStart;
     }
-    return call1(nok, undef(), code);
-  });
-
-  JsValue tagOpenAttributeValueUnquoted = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || codeEq(code, cQuotationMark) || codeEq(code, cApostrophe) || codeEq(code, cLessThan) || codeEq(code, cEqualsTo) || codeEq(code, cGraveAccent)) {
-      return call1(nok, undef(), code);
+    if (codeEq(code, cQuestionMark)) {
+      consume(code);
+      return instruction;
     }
-    if (codeEq(code, cSlash) || codeEq(code, cGreaterThan) || markdownLineEndingOrSpace(code)) {
-      return call1(tagOpenBetween, undef(), code);
+    if (asciiAlpha(code)) {
+      consume(code);
+      return tagOpen;
     }
-    call1(consume, undef(), code);
-    return tagOpenAttributeValueUnquoted;
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue tagOpenAttributeValueQuoted = JS.method1((JsValue _s, JsValue code) => {
-    if (code == marker) {
-      call1(consume, undef(), code);
-      marker = 0;
-      return tagOpenAttributeValueQuotedAfter;
+  declarationOpen = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDash)) {
+      consume(code);
+      return commentOpenInside;
     }
-    if (isEof(code)) {
-      return call1(nok, undef(), code);
+    if (codeEq(code, cLeftSquareBracket)) {
+      consume(code);
+      index = 0;
+      return cdataOpenInside;
     }
-    if (markdownLineEnding(code)) {
-      returnState = tagOpenAttributeValueQuoted;
-      return call1(lineEndingBefore, undef(), code);
+    if (asciiAlpha(code)) {
+      consume(code);
+      return declaration;
     }
-    call1(consume, undef(), code);
-    return tagOpenAttributeValueQuoted;
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue tagOpenAttributeValueBefore = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || codeEq(code, cLessThan) || codeEq(code, cEqualsTo) || codeEq(code, cGreaterThan) || codeEq(code, cGraveAccent)) {
-      return call1(nok, undef(), code);
+  commentOpenInside = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDash)) {
+      consume(code);
+      return commentEnd;
     }
-    if (codeEq(code, cQuotationMark) || codeEq(code, cApostrophe)) {
-      call1(consume, undef(), code);
-      marker = JS.number(code).toInt();
-      return tagOpenAttributeValueQuoted;
+    return JS.call(nok, JS.undefined(), code);
+  });
+
+  comment = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    if (markdownLineEnding(code)) {
-      returnState = tagOpenAttributeValueBefore;
-      return call1(lineEndingBefore, undef(), code);
+    if (codeEq(code, cDash)) {
+      consume(code);
+      return commentClose;
     }
-    if (markdownSpace(code)) {
-      call1(consume, undef(), code);
-      return tagOpenAttributeValueBefore;
+    if (markdownLineEnding(code)) {
+      returnState = comment;
+      return JS.call(lineEndingBefore, JS.undefined(), code);
     }
-    call1(consume, undef(), code);
-    return tagOpenAttributeValueUnquoted;
+    consume(code);
+    return comment;
   });
 
-  JsValue tagOpenAttributeNameAfter = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cEqualsTo)) {
-      call1(consume, undef(), code);
-      return tagOpenAttributeValueBefore;
+  commentClose = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDash)) {
+      consume(code);
+      return commentEnd;
     }
-    if (markdownLineEnding(code)) {
-      returnState = tagOpenAttributeNameAfter;
-      return call1(lineEndingBefore, undef(), code);
+    return JS.call(comment, JS.undefined(), code);
+  });
+
+  commentEnd = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cGreaterThan)) {
+      return JS.call(end, JS.undefined(), code);
     }
-    if (markdownSpace(code)) {
-      call1(consume, undef(), code);
-      return tagOpenAttributeNameAfter;
+    if (codeEq(code, cDash)) {
+      return JS.call(commentClose, JS.undefined(), code);
     }
-    return call1(tagOpenBetween, undef(), code);
+    return JS.call(comment, JS.undefined(), code);
   });
 
-  JsValue tagOpenAttributeName = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDash) || codeEq(code, cDot) || codeEq(code, cColon) || codeEq(code, cUnderscore) || asciiAlphanumeric(code)) {
-      call1(consume, undef(), code);
-      return tagOpenAttributeName;
+  cdataOpenInside = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, JS.invoke(kCdataOpeningString, "charCodeAt", index))) {
+      index++;
+      consume(code);
+      return if (index == kCdataOpeningString.length) { cdata } else { cdataOpenInside };
     }
-    return call1(tagOpenAttributeNameAfter, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  tagOpenBetween = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cSlash)) {
-      call1(consume, undef(), code);
-      return end;
+  cdata = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    if (codeEq(code, cColon) || codeEq(code, cUnderscore) || asciiAlpha(code)) {
-      call1(consume, undef(), code);
-      return tagOpenAttributeName;
+    if (codeEq(code, cRightSquareBracket)) {
+      consume(code);
+      return cdataClose;
     }
     if (markdownLineEnding(code)) {
-      returnState = tagOpenBetween;
-      return call1(lineEndingBefore, undef(), code);
-    }
-    if (markdownSpace(code)) {
-      call1(consume, undef(), code);
-      return tagOpenBetween;
+      returnState = cdata;
+      return JS.call(lineEndingBefore, JS.undefined(), code);
     }
-    return call1(end, undef(), code);
+    consume(code);
+    return cdata;
   });
 
-  JsValue tagOpen = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDash) || asciiAlphanumeric(code)) {
-      call1(consume, undef(), code);
-      return tagOpen;
-    }
-    if (codeEq(code, cSlash) || codeEq(code, cGreaterThan) || markdownLineEndingOrSpace(code)) {
-      return call1(tagOpenBetween, undef(), code);
+  cdataClose = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cRightSquareBracket)) {
+      consume(code);
+      return cdataEnd;
     }
-    return call1(nok, undef(), code);
+    return JS.call(cdata, JS.undefined(), code);
   });
 
-  JsValue tagCloseBetween = JS.method1((JsValue _s, JsValue code) => {
-    if (markdownLineEnding(code)) {
-      returnState = tagCloseBetween;
-      return call1(lineEndingBefore, undef(), code);
+  cdataEnd = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cGreaterThan)) {
+      return JS.call(end, JS.undefined(), code);
     }
-    if (markdownSpace(code)) {
-      call1(consume, undef(), code);
-      return tagCloseBetween;
+    if (codeEq(code, cRightSquareBracket)) {
+      consume(code);
+      return cdataEnd;
     }
-    return call1(end, undef(), code);
+    return JS.call(cdata, JS.undefined(), code);
   });
 
-  JsValue tagClose = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDash) || asciiAlphanumeric(code)) {
-      call1(consume, undef(), code);
-      return tagClose;
+  declaration = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || codeEq(code, cGreaterThan)) {
+      return JS.call(end, JS.undefined(), code);
     }
-    return call1(tagCloseBetween, undef(), code);
-  });
-
-  JsValue tagCloseStart = JS.method1((JsValue _s, JsValue code) => {
-    if (asciiAlpha(code)) {
-      call1(consume, undef(), code);
-      return tagClose;
+    if (markdownLineEnding(code)) {
+      returnState = declaration;
+      return JS.call(lineEndingBefore, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    consume(code);
+    return declaration;
   });
 
-  JsValue instruction = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code)) {
-      return call1(nok, undef(), code);
+  instruction = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null)) {
+      return JS.call(nok, JS.undefined(), code);
     }
     if (codeEq(code, cQuestionMark)) {
-      call1(consume, undef(), code);
+      consume(code);
       return instructionClose;
     }
     if (markdownLineEnding(code)) {
       returnState = instruction;
-      return call1(lineEndingBefore, undef(), code);
+      return JS.call(lineEndingBefore, JS.undefined(), code);
     }
-    call1(consume, undef(), code);
+    consume(code);
     return instruction;
   });
 
   instructionClose = JS.method1((JsValue _s, JsValue code) => {
     if (codeEq(code, cGreaterThan)) {
-      return call1(end, undef(), code);
+      return JS.call(end, JS.undefined(), code);
     }
-    return call1(instruction, undef(), code);
+    return JS.call(instruction, JS.undefined(), code);
   });
 
-  JsValue declaration = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || codeEq(code, cGreaterThan)) {
-      return call1(end, undef(), code);
-    }
-    if (markdownLineEnding(code)) {
-      returnState = declaration;
-      return call1(lineEndingBefore, undef(), code);
+  tagCloseStart = JS.method1((JsValue _s, JsValue code) => {
+    if (asciiAlpha(code)) {
+      consume(code);
+      return tagClose;
     }
-    call1(consume, undef(), code);
-    return declaration;
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue cdata = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code)) {
-      return call1(nok, undef(), code);
-    }
-    if (codeEq(code, cRightSquareBracket)) {
-      call1(consume, undef(), code);
-      return cdataClose;
-    }
-    if (markdownLineEnding(code)) {
-      returnState = cdata;
-      return call1(lineEndingBefore, undef(), code);
+  tagClose = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDash) || asciiAlphanumeric(code)) {
+      consume(code);
+      return tagClose;
     }
-    call1(consume, undef(), code);
-    return cdata;
+    return JS.call(tagCloseBetween, JS.undefined(), code);
   });
 
-  JsValue cdataEnd = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cGreaterThan)) {
-      return call1(end, undef(), code);
+  tagCloseBetween = JS.method1((JsValue _s, JsValue code) => {
+    if (markdownLineEnding(code)) {
+      returnState = tagCloseBetween;
+      return JS.call(lineEndingBefore, JS.undefined(), code);
     }
-    if (codeEq(code, cRightSquareBracket)) {
-      call1(consume, undef(), code);
-      return cdataEnd;
+    if (markdownSpace(code)) {
+      consume(code);
+      return tagCloseBetween;
     }
-    return call1(cdata, undef(), code);
+    return JS.call(end, JS.undefined(), code);
   });
 
-  cdataClose = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cRightSquareBracket)) {
-      call1(consume, undef(), code);
-      return cdataEnd;
+  tagOpen = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDash) || asciiAlphanumeric(code)) {
+      consume(code);
+      return tagOpen;
     }
-    return call1(cdata, undef(), code);
-  });
-
-  JsValue cdataOpenInside = JS.method1((JsValue _s, JsValue code) => {
-    int currentIndex = index;
-    index = index + 1;
-    if (!isEof(code) && codeInt(code) == kCdataOpeningString.charCodeAt(currentIndex)) {
-      call1(consume, undef(), code);
-      if (currentIndex + 1 == kCdataOpeningString.length) {
-        return cdata;
-      }
-      return cdataOpenInside;
+    if (codeEq(code, cSlash) || codeEq(code, cGreaterThan) || markdownLineEndingOrSpace(code)) {
+      return JS.call(tagOpenBetween, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue comment = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code)) {
-      return call1(nok, undef(), code);
+  tagOpenBetween = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cSlash)) {
+      consume(code);
+      return end;
     }
-    if (codeEq(code, cDash)) {
-      call1(consume, undef(), code);
-      return commentClose;
+    if (codeEq(code, cColon) || codeEq(code, cUnderscore) || asciiAlpha(code)) {
+      consume(code);
+      return tagOpenAttributeName;
     }
     if (markdownLineEnding(code)) {
-      returnState = comment;
-      return call1(lineEndingBefore, undef(), code);
+      returnState = tagOpenBetween;
+      return JS.call(lineEndingBefore, JS.undefined(), code);
     }
-    call1(consume, undef(), code);
-    return comment;
+    if (markdownSpace(code)) {
+      consume(code);
+      return tagOpenBetween;
+    }
+    return JS.call(end, JS.undefined(), code);
   });
 
-  commentClose = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDash)) {
-      call1(consume, undef(), code);
-      return commentEnd;
+  tagOpenAttributeName = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cDash) || codeEq(code, cDot) || codeEq(code, cColon) || codeEq(code, cUnderscore) || asciiAlphanumeric(code)) {
+      consume(code);
+      return tagOpenAttributeName;
     }
-    return call1(comment, undef(), code);
+    return JS.call(tagOpenAttributeNameAfter, JS.undefined(), code);
   });
 
-  commentEnd = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cGreaterThan)) {
-      return call1(end, undef(), code);
+  tagOpenAttributeNameAfter = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cEqualsTo)) {
+      consume(code);
+      return tagOpenAttributeValueBefore;
     }
-    if (codeEq(code, cDash)) {
-      return call1(commentClose, undef(), code);
+    if (markdownLineEnding(code)) {
+      returnState = tagOpenAttributeNameAfter;
+      return JS.call(lineEndingBefore, JS.undefined(), code);
     }
-    return call1(comment, undef(), code);
+    if (markdownSpace(code)) {
+      consume(code);
+      return tagOpenAttributeNameAfter;
+    }
+    return JS.call(tagOpenBetween, JS.undefined(), code);
   });
 
-  JsValue commentOpenInside = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDash)) {
-      call1(consume, undef(), code);
-      return commentEnd;
+  tagOpenAttributeValueBefore = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || codeEq(code, cLessThan) || codeEq(code, cEqualsTo) || codeEq(code, cGreaterThan) || codeEq(code, cGraveAccent)) {
+      return JS.call(nok, JS.undefined(), code);
+    }
+    if (codeEq(code, cQuotationMark) || codeEq(code, cApostrophe)) {
+      consume(code);
+      marker = code;
+      return tagOpenAttributeValueQuoted;
+    }
+    if (markdownLineEnding(code)) {
+      returnState = tagOpenAttributeValueBefore;
+      return JS.call(lineEndingBefore, JS.undefined(), code);
+    }
+    if (markdownSpace(code)) {
+      consume(code);
+      return tagOpenAttributeValueBefore;
     }
-    return call1(nok, undef(), code);
+    consume(code);
+    return tagOpenAttributeValueUnquoted;
   });
 
-  JsValue declarationOpen = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cDash)) {
-      call1(consume, undef(), code);
-      return commentOpenInside;
+  tagOpenAttributeValueQuoted = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, marker)) {
+      consume(code);
+      marker = JS.undefined();
+      return tagOpenAttributeValueQuotedAfter;
     }
-    if (codeEq(code, cLeftSquareBracket)) {
-      call1(consume, undef(), code);
-      index = 0;
-      return cdataOpenInside;
+    if (JS.strictEqual(code, null)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    if (asciiAlpha(code)) {
-      call1(consume, undef(), code);
-      return declaration;
+    if (markdownLineEnding(code)) {
+      returnState = tagOpenAttributeValueQuoted;
+      return JS.call(lineEndingBefore, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    consume(code);
+    return tagOpenAttributeValueQuoted;
   });
 
-  JsValue open = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cExclamationMark)) {
-      call1(consume, undef(), code);
-      return declarationOpen;
+  tagOpenAttributeValueUnquoted = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || codeEq(code, cQuotationMark) || codeEq(code, cApostrophe) || codeEq(code, cLessThan) || codeEq(code, cEqualsTo) || codeEq(code, cGraveAccent)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    if (codeEq(code, cSlash)) {
-      call1(consume, undef(), code);
-      return tagCloseStart;
+    if (codeEq(code, cSlash) || codeEq(code, cGreaterThan) || markdownLineEndingOrSpace(code)) {
+      return JS.call(tagOpenBetween, JS.undefined(), code);
     }
-    if (codeEq(code, cQuestionMark)) {
-      call1(consume, undef(), code);
-      return instruction;
+    consume(code);
+    return tagOpenAttributeValueUnquoted;
+  });
+
+  tagOpenAttributeValueQuotedAfter = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cSlash) || codeEq(code, cGreaterThan) || markdownLineEndingOrSpace(code)) {
+      return JS.call(tagOpenBetween, JS.undefined(), code);
     }
-    if (asciiAlpha(code)) {
-      call1(consume, undef(), code);
-      return tagOpen;
+    return JS.call(nok, JS.undefined(), code);
+  });
+
+  end = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cGreaterThan)) {
+      consume(code);
+      JS.invoke(effects, "exit", tHtmlTextData);
+      JS.invoke(effects, "exit", tHtmlText);
+      return ok;
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tHtmlText);
-    call1(objGet(effects, "enter"), effects, tHtmlTextData);
-    call1(consume, undef(), code);
-    return open;
+  lineEndingBefore = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "exit", tHtmlTextData);
+    JS.invoke(effects, "enter", tLineEnding);
+    consume(code);
+    JS.invoke(effects, "exit", tLineEnding);
+    return lineEndingAfter;
+  });
+
+  lineEndingAfter = JS.method1((JsValue _s, JsValue code) => {
+    if (markdownSpace(code)) {
+      JsValue max = if (JS.invoke(JS.get(JS.get(JS.get(self, "parser"), "constructs"), "disable")["null"], "includes", "codeIndented").truthy()) { JS.undefined() } else { kTabSize };
+      return JS.call(factorySpace(effects, lineEndingAfterPrefix, tLinePrefix, max), JS.undefined(), code);
+    }
+    return JS.call(lineEndingAfterPrefix, JS.undefined(), code);
+  });
+
+  lineEndingAfterPrefix = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tHtmlTextData);
+    return JS.call(returnState, JS.undefined(), code);
   });
 
   return start;
 });
 
-export JsValue htmlText = makeNamedConstruct("htmlText", tokenizeHtmlText);
+export JsValue htmlText = JS.object("name", "htmlText", "tokenize", tokenizeHtmlText);
diff -ruN a/src/graph/remark-parse/micromark/core/label-end.lil b/src/graph/remark-parse/micromark/core/label-end.lil
--- a/src/graph/remark-parse/micromark/core/label-end.lil	2026-09-23 20:40:45.174774674 +0000
+++ b/src/graph/remark-parse/micromark/core/label-end.lil	2026-09-23 20:40:45.288359674 +0000
@@ -1,4 +1,3 @@
-import { apply, arrLen, call0, call1, call2, call3, copyPoint, hasOwn, includesVal, isUndef, jsString, makeConstruct, makeNamedConstruct, objGet, push, slice2, undef } from "../host.lil";
 import { factoryDestination } from "../factory-destination.lil";
 import { factoryLabel } from "../factory-label.lil";
 import { factoryTitle } from "../factory-title.lil";
@@ -11,165 +10,151 @@
 import { kLinkResourceDestinationBalanceMax } from "../symbol/constants.lil";
 import { tData, tImage, tLabel, tLabelEnd, tLabelImage, tLabelLink, tLabelMarker, tLabelText, tLink, tReference, tReferenceMarker, tReferenceString, tResource, tResourceDestination, tResourceDestinationLiteral, tResourceDestinationLiteralMarker, tResourceDestinationRaw, tResourceDestinationString, tResourceMarker, tResourceTitle, tResourceTitleMarker, tResourceTitleString } from "../symbol/types.lil";
 
-JsValue ev(string kind, JsValue token, JsValue context) {
-  return JS.array(kind, token, context);
-}
+import { copyPoint } from "../host.lil";
 
-JsValue resolveAllLabelEnd = JS.method2((JsValue _self, JsValue events, JsValue _context) => {
-  int index = -1;
+JsValue resolveAllLabelEnd = JS.method1((JsValue _self, JsValue events) => {
+  float index = -1.0;
   JsValue newEvents = JS.array();
-  int n = arrLen(events);
   while (true) {
-    index = index + 1;
-    if (index >= n) {
+    index++;
+    if (index >= events.length) {
       break;
     }
     JsValue token = events[index][1];
-    JS.call(JS.get(newEvents, "push"), newEvents, events[index]);
-    string typ = jsString(objGet(token, "type"));
-    if (typ == tLabelImage || typ == tLabelLink || typ == tLabelEnd) {
-      int offset = 2;
-      if (typ == tLabelImage) {
-        offset = 4;
-      }
+    JS.invoke(newEvents, "push", events[index]);
+    if (JS.strictEqual(JS.get(token, "type"), tLabelImage) || JS.strictEqual(JS.get(token, "type"), tLabelLink) || JS.strictEqual(JS.get(token, "type"), tLabelEnd)) {
+      float offset = if (JS.strictEqual(JS.get(token, "type"), tLabelImage)) { 4.0 } else { 2.0 };
       JS.set(token, "type", tData);
-      index = index + offset;
+      index += offset;
     }
   }
-  if (arrLen(events) != arrLen(newEvents)) {
-    splice(events, 0, arrLen(events), newEvents);
+  if (events.length != newEvents.length) {
+    splice(events, 0, events.length, newEvents);
   }
   return events;
 });
 
 JsValue resolveToLabelEnd = JS.method2((JsValue _self, JsValue events, JsValue context) => {
-  int index = arrLen(events);
-  int offset = 0;
-  JsValue open = undef();
-  JsValue close = undef();
+  float index = events.length;
+  float offset = 0.0;
+  float open = 0.0;
+  float close = 0.0;
   while (true) {
-    index = index - 1;
-    if (index < 0) {
+    index--;
+    if (index < 0.0) {
       break;
     }
     JsValue token = events[index][1];
-    string typ = jsString(objGet(token, "type"));
-    if (open.truthy()) {
-      if (typ == tLink || (typ == tLabelLink && objGet(token, "_inactive").truthy())) {
+    if (open != 0.0) {
+      if (JS.strictEqual(JS.get(token, "type"), tLink) || (JS.strictEqual(JS.get(token, "type"), tLabelLink) && JS.get(token, "_inactive").truthy())) {
         break;
       }
-      if (events[index][0] == "enter" && typ == tLabelLink) {
+      if (JS.strictEqual(events[index][0], "enter") && JS.strictEqual(JS.get(token, "type"), tLabelLink)) {
         JS.set(token, "_inactive", true);
       }
-    } else if (close.truthy()) {
-      if (events[index][0] == "enter" && (typ == tLabelImage || typ == tLabelLink) && !objGet(token, "_balanced").truthy()) {
+    } else if (close != 0.0) {
+      if (JS.strictEqual(events[index][0], "enter") && (JS.strictEqual(JS.get(token, "type"), tLabelImage) || JS.strictEqual(JS.get(token, "type"), tLabelLink)) && !JS.get(token, "_balanced").truthy()) {
         open = index;
-        if (typ != tLabelLink) {
-          offset = 2;
+        if (JS.strictNotEqual(JS.get(token, "type"), tLabelLink)) {
+          offset = 2.0;
           break;
         }
       }
-    } else if (typ == tLabelEnd) {
+    } else if (JS.strictEqual(JS.get(token, "type"), tLabelEnd)) {
       close = index;
     }
   }
-  int openI = JS.number(open).toInt();
-  int closeI = JS.number(close).toInt();
-  string groupType = tImage;
-  if (objGet(events[openI][1], "type") == tLabelLink) {
-    groupType = tLink;
-  }
-  JsValue group = JS.object();
-  JS.set(group, "type", groupType);
-  JS.set(group, "start", copyPoint(objGet(events[openI][1], "start")));
-  JS.set(group, "end", copyPoint(objGet(events[arrLen(events) - 1][1], "end")));
-  JsValue label = JS.object();
-  JS.set(label, "type", tLabel);
-  JS.set(label, "start", copyPoint(objGet(events[openI][1], "start")));
-  JS.set(label, "end", copyPoint(objGet(events[closeI][1], "end")));
-  JsValue text = JS.object();
-  JS.set(text, "type", tLabelText);
-  JS.set(text, "start", copyPoint(objGet(events[openI + offset + 2][1], "end")));
-  JS.set(text, "end", copyPoint(objGet(events[closeI - 2][1], "start")));
-  JsValue media = JS.array();
-  JS.call(JS.get(media, "push"), media, ev("enter", group, context));
-  JS.call(JS.get(media, "push"), media, ev("enter", label, context));
-  media = pushAll(media, slice2(events, openI + 1, openI + offset + 3));
-  JsValue textEnter = JS.array();
-  JS.call(JS.get(textEnter, "push"), textEnter, ev("enter", text, context));
-  media = pushAll(media, textEnter);
-  media = pushAll(media, resolveAll(objGet(objGet(objGet(context, "parser"), "constructs"), "insideSpan")["null"], slice2(events, openI + offset + 4, closeI - 3), context));
-  JsValue mid = JS.array();
-  JS.call(JS.get(mid, "push"), mid, ev("exit", text, context));
-  JS.call(JS.get(mid, "push"), mid, events[closeI - 2]);
-  JS.call(JS.get(mid, "push"), mid, events[closeI - 1]);
-  JS.call(JS.get(mid, "push"), mid, ev("exit", label, context));
-  media = pushAll(media, mid);
-  media = pushAll(media, slice2(events, closeI + 1, arrLen(events)));
-  JsValue groupExit = JS.array();
-  JS.call(JS.get(groupExit, "push"), groupExit, ev("exit", group, context));
-  media = pushAll(media, groupExit);
-  splice(events, openI, arrLen(events), media);
+  JsValue group = JS.object(
+    "type", if (JS.strictEqual(JS.get(events[open][1], "type"), tLabelLink)) { tLink } else { tImage },
+    "start", copyPoint(JS.get(events[open][1], "start")),
+    "end", copyPoint(JS.get(events[events.length - 1.0][1], "end"))
+  );
+  JsValue label = JS.object(
+    "type", tLabel,
+    "start", copyPoint(JS.get(events[open][1], "start")),
+    "end", copyPoint(JS.get(events[close][1], "end"))
+  );
+  JsValue text = JS.object(
+    "type", tLabelText,
+    "start", copyPoint(JS.get(events[open + offset + 2.0][1], "end")),
+    "end", copyPoint(JS.get(events[close - 2.0][1], "start"))
+  );
+  JsValue media = JS.array(JS.array("enter", group, context), JS.array("enter", label, context));
+  media = pushAll(media, JS.invoke(events, "slice", open + 1.0, open + offset + 3.0));
+  media = pushAll(media, JS.array(JS.array("enter", text, context)));
+  media = pushAll(media, resolveAll(JS.get(JS.get(JS.get(JS.get(context, "parser"), "constructs"), "insideSpan"), "null"), JS.invoke(events, "slice", open + offset + 4.0, close - 3.0), context));
+  media = pushAll(media, JS.array(JS.array("exit", text, context), events[close - 2.0], events[close - 1.0], JS.array("exit", label, context)));
+  media = pushAll(media, JS.invoke(events, "slice", close + 1.0));
+  media = pushAll(media, JS.array(JS.array("exit", group, context)));
+  splice(events, open, events.length, media);
   return events;
 });
 
 JsValue tokenizeResource = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue nok) => {
-  JsValue resourceEnd = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cRightParenthesis)) {
-      call1(objGet(effects, "enter"), effects, tResourceMarker);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tResourceMarker);
-      call1(objGet(effects, "exit"), effects, tResource);
-      return ok;
-    }
-    return call1(nok, undef(), code);
+  JsValue resourceBefore = JS.undefined();
+  JsValue resourceOpen = JS.undefined();
+  JsValue resourceDestinationAfter = JS.undefined();
+  JsValue resourceDestinationMissing = JS.undefined();
+  JsValue resourceBetween = JS.undefined();
+  JsValue resourceTitleAfter = JS.undefined();
+  JsValue resourceEnd = JS.undefined();
+
+  JsValue resourceStart = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tResource);
+    JS.invoke(effects, "enter", tResourceMarker);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tResourceMarker);
+    return resourceBefore;
   });
 
-  JsValue resourceTitleAfter = JS.method1((JsValue _s, JsValue code) => {
+  resourceBefore = JS.method1((JsValue _s, JsValue code) => {
     if (markdownLineEndingOrSpace(code)) {
-      return call1(factoryWhitespace(effects, resourceEnd), undef(), code);
+      return JS.call(factoryWhitespace(effects, resourceOpen), JS.undefined(), code);
     }
-    return call1(resourceEnd, undef(), code);
+    return JS.call(resourceOpen, JS.undefined(), code);
   });
 
-  JsValue resourceBetween = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cQuotationMark) || codeEq(code, cApostrophe) || codeEq(code, cLeftParenthesis)) {
-      return call1(factoryTitle(effects, resourceTitleAfter, nok, tResourceTitle, tResourceTitleMarker, tResourceTitleString), undef(), code);
+  resourceOpen = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cRightParenthesis)) {
+      return JS.call(resourceEnd, JS.undefined(), code);
     }
-    return call1(resourceEnd, undef(), code);
+    return JS.call(factoryDestination(effects, resourceDestinationAfter, resourceDestinationMissing, tResourceDestination, tResourceDestinationLiteral, tResourceDestinationLiteralMarker, tResourceDestinationRaw, tResourceDestinationString, kLinkResourceDestinationBalanceMax), JS.undefined(), code);
   });
 
-  JsValue resourceDestinationMissing = JS.method1((JsValue _s, JsValue code) => {
-    return call1(nok, undef(), code);
-  });
-
-  JsValue resourceDestinationAfter = JS.method1((JsValue _s, JsValue code) => {
+  resourceDestinationAfter = JS.method1((JsValue _s, JsValue code) => {
     if (markdownLineEndingOrSpace(code)) {
-      return call1(factoryWhitespace(effects, resourceBetween), undef(), code);
+      return JS.call(factoryWhitespace(effects, resourceBetween), JS.undefined(), code);
     }
-    return call1(resourceEnd, undef(), code);
+    return JS.call(resourceEnd, JS.undefined(), code);
   });
 
-  JsValue resourceOpen = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cRightParenthesis)) {
-      return call1(resourceEnd, undef(), code);
+  resourceDestinationMissing = JS.method1((JsValue _s, JsValue code) => {
+    return JS.call(nok, JS.undefined(), code);
+  });
+
+  resourceBetween = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cQuotationMark) || codeEq(code, cApostrophe) || codeEq(code, cLeftParenthesis)) {
+      return JS.call(factoryTitle(effects, resourceTitleAfter, nok, tResourceTitle, tResourceTitleMarker, tResourceTitleString), JS.undefined(), code);
     }
-    return call1(factoryDestination(effects, resourceDestinationAfter, resourceDestinationMissing, tResourceDestination, tResourceDestinationLiteral, tResourceDestinationLiteralMarker, tResourceDestinationRaw, tResourceDestinationString, kLinkResourceDestinationBalanceMax), undef(), code);
+    return JS.call(resourceEnd, JS.undefined(), code);
   });
 
-  JsValue resourceBefore = JS.method1((JsValue _s, JsValue code) => {
+  resourceTitleAfter = JS.method1((JsValue _s, JsValue code) => {
     if (markdownLineEndingOrSpace(code)) {
-      return call1(factoryWhitespace(effects, resourceOpen), undef(), code);
+      return JS.call(factoryWhitespace(effects, resourceEnd), JS.undefined(), code);
     }
-    return call1(resourceOpen, undef(), code);
+    return JS.call(resourceEnd, JS.undefined(), code);
   });
 
-  JsValue resourceStart = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tResource);
-    call1(objGet(effects, "enter"), effects, tResourceMarker);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tResourceMarker);
-    return resourceBefore;
+  resourceEnd = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cRightParenthesis)) {
+      JS.invoke(effects, "enter", tResourceMarker);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tResourceMarker);
+      JS.invoke(effects, "exit", tResource);
+      return ok;
+    }
+    return JS.call(nok, JS.undefined(), code);
   });
 
   return resourceStart;
@@ -177,135 +162,108 @@
 
 JsValue tokenizeReferenceFull = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue referenceFullAfter = JS.method1((JsValue _s, JsValue code) => {
-    JsValue events = objGet(self, "events");
-    string raw = jsString(call1(objGet(self, "sliceSerialize"), self, events[arrLen(events) - 1][1]));
-    string id = normalizeIdentifier(jsString(JS.call(JS.get(raw, "slice"), raw, 1, -1)));
-    if (includesVal(objGet(objGet(self, "parser"), "defined"), id)) {
-      return call1(ok, undef(), code);
+    string label = JS.assume(JS.invoke(self, "sliceSerialize", JS.get(self, "events")[JS.get(self, "events").length - 1.0][1]));
+    if (JS.invoke(JS.get(JS.get(self, "parser"), "defined"), "includes", normalizeIdentifier(label.slice(1, -1))).truthy()) {
+      return JS.call(ok, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
   JsValue referenceFullMissing = JS.method1((JsValue _s, JsValue code) => {
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue referenceFull = JS.method1((JsValue _s, JsValue code) => {
-    JsValue labelArgs = JS.array();
-    push(labelArgs, effects);
-    push(labelArgs, referenceFullAfter);
-    push(labelArgs, referenceFullMissing);
-    push(labelArgs, tReference);
-    push(labelArgs, tReferenceMarker);
-    push(labelArgs, tReferenceString);
-    return call1(apply(factoryLabel, self, labelArgs), undef(), code);
+  return JS.method1((JsValue _s, JsValue code) => {
+    return JS.call(factoryLabel(self, effects, referenceFullAfter, referenceFullMissing, tReference, tReferenceMarker, tReferenceString), JS.undefined(), code);
   });
-
-  return referenceFull;
 });
 
 JsValue tokenizeReferenceCollapsed = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue referenceCollapsedOpen = JS.method1((JsValue _s, JsValue code) => {
     if (codeEq(code, cRightSquareBracket)) {
-      call1(objGet(effects, "enter"), effects, tReferenceMarker);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tReferenceMarker);
-      call1(objGet(effects, "exit"), effects, tReference);
+      JS.invoke(effects, "enter", tReferenceMarker);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tReferenceMarker);
+      JS.invoke(effects, "exit", tReference);
       return ok;
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue referenceCollapsedStart = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tReference);
-    call1(objGet(effects, "enter"), effects, tReferenceMarker);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tReferenceMarker);
+  return JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tReference);
+    JS.invoke(effects, "enter", tReferenceMarker);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tReferenceMarker);
     return referenceCollapsedOpen;
   });
-
-  return referenceCollapsedStart;
 });
 
-JsValue resourceConstruct = makeConstruct(tokenizeResource);
-JsValue referenceFullConstruct = makeConstruct(tokenizeReferenceFull);
-JsValue referenceCollapsedConstruct = makeConstruct(tokenizeReferenceCollapsed);
+JsValue resourceConstruct = JS.object("tokenize", tokenizeResource);
+JsValue referenceFullConstruct = JS.object("tokenize", tokenizeReferenceFull);
+JsValue referenceCollapsedConstruct = JS.object("tokenize", tokenizeReferenceCollapsed);
 
 JsValue tokenizeLabelEnd = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  int index = arrLen(objGet(self, "events"));
-  JsValue labelStart = undef();
+  float index = JS.get(self, "events").length;
+  JsValue labelStart = JS.undefined();
+  JsValue defined = JS.undefined();
   while (true) {
-    index = index - 1;
-    if (index < 0) {
+    index--;
+    if (index < 0.0) {
       break;
     }
-    JsValue token = objGet(self, "events")[index][1];
-    string typ = jsString(objGet(token, "type"));
-    if ((typ == tLabelImage || typ == tLabelLink) && !objGet(token, "_balanced").truthy()) {
+    JsValue token = JS.get(self, "events")[index][1];
+    if ((JS.strictEqual(JS.get(token, "type"), tLabelImage) || JS.strictEqual(JS.get(token, "type"), tLabelLink)) && !JS.get(token, "_balanced").truthy()) {
       labelStart = token;
       break;
     }
   }
-
-  bool defined = false;
+  JsValue after = JS.undefined();
+  JsValue referenceNotFull = JS.undefined();
 
   JsValue labelEndOk = JS.method1((JsValue _s, JsValue code) => {
-    return call1(ok, undef(), code);
+    return JS.call(ok, JS.undefined(), code);
   });
 
   JsValue labelEndNok = JS.method1((JsValue _s, JsValue code) => {
-    if (labelStart.truthy()) {
-      JS.set(labelStart, "_balanced", true);
-    }
-    return call1(nok, undef(), code);
+    JS.set(labelStart, "_balanced", true);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue referenceNotFull = JS.method1((JsValue _s, JsValue code) => {
-    return call1(call3(objGet(effects, "attempt"), effects, referenceCollapsedConstruct, labelEndOk, labelEndNok), undef(), code);
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    if (!labelStart.truthy()) {
+      return JS.call(nok, JS.undefined(), code);
+    }
+    if (JS.get(labelStart, "_inactive").truthy()) {
+      return JS.call(labelEndNok, JS.undefined(), code);
+    }
+    defined = JS.invoke(JS.get(JS.get(self, "parser"), "defined"), "includes", normalizeIdentifier(JS.assume(JS.invoke(self, "sliceSerialize", JS.object("start", JS.get(labelStart, "end"), "end", JS.invoke(self, "now"))))));
+    JS.invoke(effects, "enter", tLabelEnd);
+    JS.invoke(effects, "enter", tLabelMarker);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tLabelMarker);
+    JS.invoke(effects, "exit", tLabelEnd);
+    return after;
   });
 
-  JsValue after = JS.method1((JsValue _s, JsValue code) => {
+  after = JS.method1((JsValue _s, JsValue code) => {
     if (codeEq(code, cLeftParenthesis)) {
-      JsValue fallback = labelEndNok;
-      if (defined) {
-        fallback = labelEndOk;
-      }
-      return call1(call3(objGet(effects, "attempt"), effects, resourceConstruct, labelEndOk, fallback), undef(), code);
+      return JS.call(JS.invoke(effects, "attempt", resourceConstruct, labelEndOk, if (defined.truthy()) { labelEndOk } else { labelEndNok }), JS.undefined(), code);
     }
     if (codeEq(code, cLeftSquareBracket)) {
-      JsValue fallback = labelEndNok;
-      if (defined) {
-        fallback = referenceNotFull;
-      }
-      return call1(call3(objGet(effects, "attempt"), effects, referenceFullConstruct, labelEndOk, fallback), undef(), code);
+      return JS.call(JS.invoke(effects, "attempt", referenceFullConstruct, labelEndOk, if (defined.truthy()) { referenceNotFull } else { labelEndNok }), JS.undefined(), code);
     }
-    if (defined) {
-      return call1(labelEndOk, undef(), code);
+    if (defined.truthy()) {
+      return JS.call(labelEndOk, JS.undefined(), code);
     }
-    return call1(labelEndNok, undef(), code);
+    return JS.call(labelEndNok, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    if (!labelStart.truthy()) {
-      return call1(nok, undef(), code);
-    }
-    if (objGet(labelStart, "_inactive").truthy()) {
-      return call1(labelEndNok, undef(), code);
-    }
-    JsValue range = JS.object();
-    JS.set(range, "start", objGet(labelStart, "end"));
-    JS.set(range, "end", call0(objGet(self, "now"), self));
-    string id = normalizeIdentifier(jsString(call1(objGet(self, "sliceSerialize"), self, range)));
-    defined = includesVal(objGet(objGet(self, "parser"), "defined"), id);
-    call1(objGet(effects, "enter"), effects, tLabelEnd);
-    call1(objGet(effects, "enter"), effects, tLabelMarker);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tLabelMarker);
-    call1(objGet(effects, "exit"), effects, tLabelEnd);
-    return after;
+  referenceNotFull = JS.method1((JsValue _s, JsValue code) => {
+    return JS.call(JS.invoke(effects, "attempt", referenceCollapsedConstruct, labelEndOk, labelEndNok), JS.undefined(), code);
   });
 
   return start;
 });
 
-export JsValue labelEnd = JS.object("name", "labelEnd", "tokenize", tokenizeLabelEnd, "resolveAll", resolveAllLabelEnd, "resolveTo", resolveToLabelEnd);
+export JsValue labelEnd = JS.object("name", "labelEnd", "resolveAll", resolveAllLabelEnd, "resolveTo", resolveToLabelEnd, "tokenize", tokenizeLabelEnd);
diff -ruN a/src/graph/remark-parse/micromark/core/label-start-image.lil b/src/graph/remark-parse/micromark/core/label-start-image.lil
--- a/src/graph/remark-parse/micromark/core/label-start-image.lil	2026-09-23 20:40:45.178691853 +0000
+++ b/src/graph/remark-parse/micromark/core/label-start-image.lil	2026-09-23 20:40:45.288698349 +0000
@@ -1,37 +1,33 @@
-import { call1, hasOwn, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { cCaret, cLeftSquareBracket, codeEq } from "../symbol/codes.lil";
 import { tLabelImage, tLabelImageMarker, tLabelMarker } from "../symbol/types.lil";
 import { labelEnd } from "./label-end.lil";
 
 JsValue tokenizeLabelStartImage = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue after = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cCaret) && hasOwn(objGet(objGet(self, "parser"), "constructs"), "_hiddenFootnoteSupport")) {
-      return call1(nok, undef(), code);
+    if (codeEq(code, cCaret) && JS.in("_hiddenFootnoteSupport", JS.get(JS.get(self, "parser"), "constructs"))) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    return call1(ok, undef(), code);
+    return JS.call(ok, JS.undefined(), code);
   });
 
   JsValue open = JS.method1((JsValue _s, JsValue code) => {
     if (codeEq(code, cLeftSquareBracket)) {
-      call1(objGet(effects, "enter"), effects, tLabelMarker);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tLabelMarker);
-      call1(objGet(effects, "exit"), effects, tLabelImage);
+      JS.invoke(effects, "enter", tLabelMarker);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tLabelMarker);
+      JS.invoke(effects, "exit", tLabelImage);
       return after;
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tLabelImage);
-    call1(objGet(effects, "enter"), effects, tLabelImageMarker);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tLabelImageMarker);
+  return JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tLabelImage);
+    JS.invoke(effects, "enter", tLabelImageMarker);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tLabelImageMarker);
     return open;
   });
-
-  return start;
 });
 
-export JsValue labelStartImage = makeNamedConstruct("labelStartImage", tokenizeLabelStartImage);
-JS.set(labelStartImage, "resolveAll", objGet(labelEnd, "resolveAll"));
+export JsValue labelStartImage = JS.object("name", "labelStartImage", "resolveAll", JS.get(labelEnd, "resolveAll"), "tokenize", tokenizeLabelStartImage);
diff -ruN a/src/graph/remark-parse/micromark/core/label-start-link.lil b/src/graph/remark-parse/micromark/core/label-start-link.lil
--- a/src/graph/remark-parse/micromark/core/label-start-link.lil	2026-09-23 20:40:45.183185272 +0000
+++ b/src/graph/remark-parse/micromark/core/label-start-link.lil	2026-09-23 20:40:45.288982281 +0000
@@ -1,27 +1,23 @@
-import { call1, hasOwn, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { cCaret, codeEq } from "../symbol/codes.lil";
 import { tLabelLink, tLabelMarker } from "../symbol/types.lil";
 import { labelEnd } from "./label-end.lil";
 
 JsValue tokenizeLabelStartLink = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue after = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cCaret) && hasOwn(objGet(objGet(self, "parser"), "constructs"), "_hiddenFootnoteSupport")) {
-      return call1(nok, undef(), code);
+    if (codeEq(code, cCaret) && JS.in("_hiddenFootnoteSupport", JS.get(JS.get(self, "parser"), "constructs"))) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    return call1(ok, undef(), code);
+    return JS.call(ok, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tLabelLink);
-    call1(objGet(effects, "enter"), effects, tLabelMarker);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tLabelMarker);
-    call1(objGet(effects, "exit"), effects, tLabelLink);
+  return JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tLabelLink);
+    JS.invoke(effects, "enter", tLabelMarker);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tLabelMarker);
+    JS.invoke(effects, "exit", tLabelLink);
     return after;
   });
-
-  return start;
 });
 
-export JsValue labelStartLink = makeNamedConstruct("labelStartLink", tokenizeLabelStartLink);
-JS.set(labelStartLink, "resolveAll", objGet(labelEnd, "resolveAll"));
+export JsValue labelStartLink = JS.object("name", "labelStartLink", "resolveAll", JS.get(labelEnd, "resolveAll"), "tokenize", tokenizeLabelStartLink);
diff -ruN a/src/graph/remark-parse/micromark/core/line-ending.lil b/src/graph/remark-parse/micromark/core/line-ending.lil
--- a/src/graph/remark-parse/micromark/core/line-ending.lil	2026-09-23 20:40:45.183489001 +0000
+++ b/src/graph/remark-parse/micromark/core/line-ending.lil	2026-09-23 20:40:45.296425687 +0000
@@ -1,16 +1,13 @@
-import { call1, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { tLineEnding, tLinePrefix } from "../symbol/types.lil";
 
 JsValue tokenizeLineEnding = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue _nok) => {
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tLineEnding);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tLineEnding);
-    return factorySpace(effects, ok, tLinePrefix, 0);
+  return JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tLineEnding);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tLineEnding);
+    return factorySpace(effects, ok, tLinePrefix, JS.undefined());
   });
-
-  return start;
 });
 
-export JsValue lineEnding = makeNamedConstruct("lineEnding", tokenizeLineEnding);
+export JsValue lineEnding = JS.object("name", "lineEnding", "tokenize", tokenizeLineEnding);
diff -ruN a/src/graph/remark-parse/micromark/core/list.lil b/src/graph/remark-parse/micromark/core/list.lil
--- a/src/graph/remark-parse/micromark/core/list.lil	2026-09-23 20:40:45.183780257 +0000
+++ b/src/graph/remark-parse/micromark/core/list.lil	2026-09-23 20:40:45.296784480 +0000
@@ -1,4 +1,3 @@
-import { arrLen, call1, call2, call3, includesStr, jsInt, jsString, makeConstruct, objGet, undef } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { asciiDigit, markdownSpace } from "../util-character.lil";
 import { cAsterisk, cDash, cDigit1, cDot, cPlusSign, cRightParenthesis, codeEq } from "../symbol/codes.lil";
@@ -9,188 +8,152 @@
 
 JsValue tokenizeListItemPrefixWhitespace = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue afterPrefix = JS.method1((JsValue _s, JsValue code) => {
-    JsValue events = objGet(self, "events");
-    JsValue tail = events[arrLen(events) - 1];
-    if (!markdownSpace(code) && tail.truthy() && objGet(tail[1], "type") == tListItemPrefixWhitespace) {
-      return call1(ok, undef(), code);
-    }
-    return call1(nok, undef(), code);
-  });
-  int max = kTabSize + 1;
-  if (includesStr(objGet(objGet(objGet(self, "parser"), "constructs"), "disable")["null"], "codeIndented")) {
-    max = 0;
-  }
+    JsValue tail = JS.get(self, "events")[JS.get(self, "events").length - 1.0];
+    if (!markdownSpace(code) && tail.truthy() && JS.strictEqual(JS.get(tail[1], "type"), tListItemPrefixWhitespace)) {
+      return JS.call(ok, JS.undefined(), code);
+    }
+    return JS.call(nok, JS.undefined(), code);
+  });
+  JsValue max = if (JS.invoke(JS.get(JS.get(JS.get(self, "parser"), "constructs"), "disable")["null"], "includes", "codeIndented").truthy()) { JS.undefined() } else { kTabSize + 1 };
   return factorySpace(effects, afterPrefix, tListItemPrefixWhitespace, max);
 });
 
-JsValue listItemPrefixWhitespaceConstruct = makeConstruct(tokenizeListItemPrefixWhitespace);
-JS.set(listItemPrefixWhitespaceConstruct, "partial", true);
+JsValue listItemPrefixWhitespaceConstruct = JS.object("partial", true, "tokenize", tokenizeListItemPrefixWhitespace);
 
 JsValue tokenizeIndent = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
   JsValue afterPrefix = JS.method1((JsValue _s, JsValue code) => {
-    JsValue events = objGet(self, "events");
-    JsValue tail = events[arrLen(events) - 1];
-    if (tail.truthy() && objGet(tail[1], "type") == tListItemIndent && jsString(call2(objGet(tail[2], "sliceSerialize"), tail[2], tail[1], true)).length == jsInt(objGet(objGet(self, "containerState"), "size"))) {
-      return call1(ok, undef(), code);
+    JsValue tail = JS.get(self, "events")[JS.get(self, "events").length - 1.0];
+    if (tail.truthy() && JS.strictEqual(JS.get(tail[1], "type"), tListItemIndent) && JS.strictEqual(JS.get(JS.invoke(tail[2], "sliceSerialize", tail[1], true), "length"), JS.get(JS.get(self, "containerState"), "size"))) {
+      return JS.call(ok, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
-  return factorySpace(effects, afterPrefix, tListItemIndent, JS.number(objGet(objGet(self, "containerState"), "size")) + 1.0);
+  return factorySpace(effects, afterPrefix, tListItemIndent, JS.add(JS.get(JS.get(self, "containerState"), "size"), 1));
 });
 
-JsValue indentConstruct = makeConstruct(tokenizeIndent);
-JS.set(indentConstruct, "partial", true);
-
-export JsValue list = JS.object();
-JS.set(list, "name", "list");
+JsValue indentConstruct = JS.object("partial", true, "tokenize", tokenizeIndent);
 
 JsValue tokenizeListStart = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  JsValue events = objGet(self, "events");
-  JsValue tail = events[arrLen(events) - 1];
-  float initialSize = 0.0;
-  if (tail.truthy() && objGet(tail[1], "type") == tLinePrefix) {
-    initialSize = JS.number(jsString(call2(objGet(tail[2], "sliceSerialize"), tail[2], tail[1], true)).length);
-  }
-  int size = 0;
-
-  JsValue endOfPrefix = JS.method1((JsValue _s, JsValue code) => {
-    JsValue prefix = call1(objGet(effects, "exit"), effects, tListItemPrefix);
-    JS.set(objGet(self, "containerState"), "size", initialSize + JS.number(jsString(call2(objGet(self, "sliceSerialize"), self, prefix, true)).length));
-    return call1(ok, undef(), code);
-  });
-
-  JsValue otherPrefix = JS.method1((JsValue _s, JsValue code) => {
-    if (markdownSpace(code)) {
-      call1(objGet(effects, "enter"), effects, tListItemPrefixWhitespace);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tListItemPrefixWhitespace);
-      return endOfPrefix;
-    }
-    return call1(nok, undef(), code);
-  });
-
-  JsValue onBlank = JS.method1((JsValue _s, JsValue code) => {
-    JS.set(objGet(self, "containerState"), "initialBlankLine", true);
-    initialSize = initialSize + 1.0;
-    return call1(endOfPrefix, undef(), code);
-  });
-
-  JsValue atMarker = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tListItemMarker);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tListItemMarker);
-    if (!objGet(objGet(self, "containerState"), "marker").truthy()) {
-      JS.set(objGet(self, "containerState"), "marker", code);
-    }
-    JsValue blankOk = onBlank;
-    if (objGet(self, "interrupt").truthy()) {
-      blankOk = nok;
-    }
-    return call3(objGet(effects, "check"), effects, blankLine, blankOk, call3(objGet(effects, "attempt"), effects, listItemPrefixWhitespaceConstruct, endOfPrefix, otherPrefix));
-  });
-
-  JsValue inside = JS.method1((JsValue _s, JsValue code) => {
-    if (asciiDigit(code) && size + 1 < kListItemValueSizeMax) {
-      size = size + 1;
-      call1(objGet(effects, "consume"), effects, code);
-      return inside;
-    }
-    bool sizeOk = !objGet(self, "interrupt").truthy() || size < 2;
-    JsValue marker = objGet(objGet(self, "containerState"), "marker");
-    bool markerOk = false;
-    if (marker.truthy()) {
-      markerOk = code == marker;
-    } else {
-      markerOk = codeEq(code, cRightParenthesis) || codeEq(code, cDot);
-    }
-    if (sizeOk && markerOk) {
-      call1(objGet(effects, "exit"), effects, tListItemValue);
-      return call1(atMarker, undef(), code);
-    }
-    return call1(nok, undef(), code);
-  });
+  JsValue tail = JS.get(self, "events")[JS.get(self, "events").length - 1.0];
+  float initialSize = if (tail.truthy() && JS.strictEqual(JS.get(tail[1], "type"), tLinePrefix)) { JS.invoke(tail[2], "sliceSerialize", tail[1], true).length } else { 0.0 };
+  float size = 0.0;
+  JsValue inside = JS.undefined();
+  JsValue atMarker = JS.undefined();
+  JsValue onBlank = JS.undefined();
+  JsValue otherPrefix = JS.undefined();
+  JsValue endOfPrefix = JS.undefined();
 
   JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    JsValue state = objGet(self, "containerState");
-    string kind = tListOrdered;
-    if (objGet(state, "type").truthy()) {
-      kind = jsString(objGet(state, "type"));
-    } else if (codeEq(code, cAsterisk) || codeEq(code, cPlusSign) || codeEq(code, cDash)) {
-      kind = tListUnordered;
-    }
-    bool allowed = false;
-    if (kind == tListUnordered) {
-      allowed = !objGet(state, "marker").truthy() || code == objGet(state, "marker");
-    } else {
-      allowed = asciiDigit(code);
-    }
-    if (allowed) {
-      if (!objGet(state, "type").truthy()) {
+    JsValue state = JS.get(self, "containerState");
+    JsValue kind = JS.or(JS.get(state, "type"), if (codeEq(code, cAsterisk) || codeEq(code, cPlusSign) || codeEq(code, cDash)) { tListUnordered } else { tListOrdered });
+    bool unordered = JS.strictEqual(kind, tListUnordered);
+    if (if (unordered) { !JS.get(state, "marker").truthy() || JS.strictEqual(code, JS.get(state, "marker")) } else { asciiDigit(code) }) {
+      if (!JS.get(state, "type").truthy()) {
         JS.set(state, "type", kind);
-        JsValue fields = JS.object();
-        JS.set(fields, "_container", true);
-        call2(objGet(effects, "enter"), effects, kind, fields);
+        JS.invoke(effects, "enter", kind, JS.object("_container", true));
       }
-      if (kind == tListUnordered) {
-        call1(objGet(effects, "enter"), effects, tListItemPrefix);
+      if (unordered) {
+        JS.invoke(effects, "enter", tListItemPrefix);
         if (codeEq(code, cAsterisk) || codeEq(code, cDash)) {
-          return call1(call3(objGet(effects, "check"), effects, thematicBreak, nok, atMarker), undef(), code);
+          return JS.call(JS.invoke(effects, "check", thematicBreak, nok, atMarker), JS.undefined(), code);
         }
-        return call1(atMarker, undef(), code);
+        return JS.call(atMarker, JS.undefined(), code);
       }
-      if (!objGet(self, "interrupt").truthy() || codeEq(code, cDigit1)) {
-        call1(objGet(effects, "enter"), effects, tListItemPrefix);
-        call1(objGet(effects, "enter"), effects, tListItemValue);
-        return call1(inside, undef(), code);
+      if (!JS.get(self, "interrupt").truthy() || codeEq(code, cDigit1)) {
+        JS.invoke(effects, "enter", tListItemPrefix);
+        JS.invoke(effects, "enter", tListItemValue);
+        return JS.call(inside, JS.undefined(), code);
       }
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  return start;
-});
+  inside = JS.method1((JsValue _s, JsValue code) => {
+    if (asciiDigit(code) && size + 1.0 < kListItemValueSizeMax) {
+      size++;
+      JS.invoke(effects, "consume", code);
+      return inside;
+    }
+    JsValue marker = JS.get(JS.get(self, "containerState"), "marker");
+    if ((!JS.get(self, "interrupt").truthy() || size < 2.0) && (if (marker.truthy()) { JS.strictEqual(code, marker) } else { codeEq(code, cRightParenthesis) || codeEq(code, cDot) })) {
+      JS.invoke(effects, "exit", tListItemValue);
+      return JS.call(atMarker, JS.undefined(), code);
+    }
+    return JS.call(nok, JS.undefined(), code);
+  });
 
-JsValue tokenizeListContinuation = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  JS.set(objGet(self, "containerState"), "_closeFlow", undef());
+  atMarker = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tListItemMarker);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", tListItemMarker);
+    JsValue state = JS.get(self, "containerState");
+    JS.set(state, "marker", JS.or(JS.get(state, "marker"), code));
+    return JS.invoke(effects, "check", blankLine, if (JS.get(self, "interrupt").truthy()) { nok } else { onBlank }, JS.invoke(effects, "attempt", listItemPrefixWhitespaceConstruct, endOfPrefix, otherPrefix));
+  });
 
-  JsValue notInCurrentItem = JS.method1((JsValue _s, JsValue code) => {
-    JS.set(objGet(self, "containerState"), "_closeFlow", true);
-    JS.set(self, "interrupt", undef());
-    int max = kTabSize;
-    if (includesStr(objGet(objGet(objGet(self, "parser"), "constructs"), "disable")["null"], "codeIndented")) {
-      max = 0;
-    }
-    return call1(factorySpace(effects, call3(objGet(effects, "attempt"), effects, list, ok, nok), tLinePrefix, max), undef(), code);
+  onBlank = JS.method1((JsValue _s, JsValue code) => {
+    JS.set(JS.get(self, "containerState"), "initialBlankLine", true);
+    initialSize++;
+    return JS.call(endOfPrefix, JS.undefined(), code);
   });
 
-  JsValue notBlank = JS.method1((JsValue _s, JsValue code) => {
-    if (objGet(objGet(self, "containerState"), "furtherBlankLines").truthy() || !markdownSpace(code)) {
-      JS.set(objGet(self, "containerState"), "furtherBlankLines", undef());
-      JS.set(objGet(self, "containerState"), "initialBlankLine", undef());
-      return call1(notInCurrentItem, undef(), code);
+  otherPrefix = JS.method1((JsValue _s, JsValue code) => {
+    if (markdownSpace(code)) {
+      JS.invoke(effects, "enter", tListItemPrefixWhitespace);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tListItemPrefixWhitespace);
+      return endOfPrefix;
     }
-    JS.set(objGet(self, "containerState"), "furtherBlankLines", undef());
-    JS.set(objGet(self, "containerState"), "initialBlankLine", undef());
-    return call1(call3(objGet(effects, "attempt"), effects, indentConstruct, ok, notInCurrentItem), undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue onBlank = JS.method1((JsValue _s, JsValue code) => {
-    if (!objGet(objGet(self, "containerState"), "furtherBlankLines").truthy()) {
-      JS.set(objGet(self, "containerState"), "furtherBlankLines", objGet(objGet(self, "containerState"), "initialBlankLine"));
-    }
-    return call1(factorySpace(effects, ok, tListItemIndent, JS.number(objGet(objGet(self, "containerState"), "size")) + 1.0), undef(), code);
+  endOfPrefix = JS.method1((JsValue _s, JsValue code) => {
+    JS.set(JS.get(self, "containerState"), "size", initialSize + JS.invoke(self, "sliceSerialize", JS.invoke(effects, "exit", tListItemPrefix), true).length);
+    return JS.call(ok, JS.undefined(), code);
   });
 
-  return call3(objGet(effects, "check"), effects, blankLine, onBlank, notBlank);
+  return start;
 });
 
 JsValue tokenizeListEnd = JS.method1((JsValue self, JsValue effects) => {
-  call1(objGet(effects, "exit"), effects, objGet(objGet(self, "containerState"), "type"));
-  return undef();
+  JS.invoke(effects, "exit", JS.get(JS.get(self, "containerState"), "type"));
+  return JS.undefined();
 });
 
-JS.set(list, "tokenize", tokenizeListStart);
-JsValue continuation = JS.object();
-JS.set(continuation, "tokenize", tokenizeListContinuation);
-JS.set(list, "continuation", continuation);
-JS.set(list, "exit", tokenizeListEnd);
+// The continuation tokenizer attempts `list` itself, so it is written inside the
+// construct's own initializer.
+export JsValue list = JS.object(
+  "continuation", JS.object("tokenize", JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
+    JS.set(JS.get(self, "containerState"), "_closeFlow", JS.undefined());
+
+    JsValue notInCurrentItem = JS.method1((JsValue _s, JsValue code) => {
+      JS.set(JS.get(self, "containerState"), "_closeFlow", true);
+      JS.set(self, "interrupt", JS.undefined());
+      JsValue max = if (JS.invoke(JS.get(JS.get(JS.get(self, "parser"), "constructs"), "disable")["null"], "includes", "codeIndented").truthy()) { JS.undefined() } else { kTabSize };
+      return JS.call(factorySpace(effects, JS.invoke(effects, "attempt", list, ok, nok), tLinePrefix, max), JS.undefined(), code);
+    });
+
+    JsValue onBlank = JS.method1((JsValue _s, JsValue code) => {
+      JsValue state = JS.get(self, "containerState");
+      JS.set(state, "furtherBlankLines", JS.or(JS.get(state, "furtherBlankLines"), JS.get(state, "initialBlankLine")));
+      return JS.call(factorySpace(effects, ok, tListItemIndent, JS.add(JS.get(state, "size"), 1)), JS.undefined(), code);
+    });
+
+    JsValue notBlank = JS.method1((JsValue _s, JsValue code) => {
+      JsValue state = JS.get(self, "containerState");
+      if (JS.get(state, "furtherBlankLines").truthy() || !markdownSpace(code)) {
+        JS.set(state, "furtherBlankLines", JS.undefined());
+        JS.set(state, "initialBlankLine", JS.undefined());
+        return JS.call(notInCurrentItem, JS.undefined(), code);
+      }
+      JS.set(state, "furtherBlankLines", JS.undefined());
+      JS.set(state, "initialBlankLine", JS.undefined());
+      return JS.call(JS.invoke(effects, "attempt", indentConstruct, ok, notInCurrentItem), JS.undefined(), code);
+    });
+
+    return JS.invoke(effects, "check", blankLine, onBlank, notBlank);
+  })),
+  "exit", tokenizeListEnd,
+  "name", "list",
+  "tokenize", tokenizeListStart
+);
diff -ruN a/src/graph/remark-parse/micromark/core/setext-underline.lil b/src/graph/remark-parse/micromark/core/setext-underline.lil
--- a/src/graph/remark-parse/micromark/core/setext-underline.lil	2026-09-23 20:40:45.184073306 +0000
+++ b/src/graph/remark-parse/micromark/core/setext-underline.lil	2026-09-23 20:40:45.297190029 +0000
@@ -1,106 +1,102 @@
-import { arrLen, call0, call1, copyPoint, eventEnter, eventExit, isEof, makeNamedConstruct, objGet, push, undef } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { markdownLineEnding, markdownSpace } from "../util-character.lil";
 import { tContent, tDefinition, tLineEnding, tLinePrefix, tLineSuffix, tParagraph, tSetextHeading, tSetextHeadingLine, tSetextHeadingLineSequence, tSetextHeadingText } from "../symbol/types.lil";
 
+import { copyPoint } from "../host.lil";
+
 JsValue resolveToSetextUnderline = JS.method2((JsValue _self, JsValue events, JsValue context) => {
-  int index = arrLen(events);
-  int content = -1;
-  int text = -1;
-  int definition = -1;
+  float index = events.length;
+  float content = 0.0;
+  float text = 0.0;
+  float definition = 0.0;
   while (true) {
-    index = index - 1;
-    if (index < 0) {
+    index--;
+    if (index < 0.0) {
       break;
     }
-    if (events[index][0] == "enter") {
-      if (objGet(events[index][1], "type") == tContent) {
+    if (JS.strictEqual(events[index][0], "enter")) {
+      if (JS.strictEqual(JS.get(events[index][1], "type"), tContent)) {
         content = index;
         break;
       }
-      if (objGet(events[index][1], "type") == tParagraph) {
+      if (JS.strictEqual(JS.get(events[index][1], "type"), tParagraph)) {
         text = index;
       }
     } else {
-      if (objGet(events[index][1], "type") == tContent) {
-        JS.call(JS.get(events, "splice"), events, index, 1);
+      if (JS.strictEqual(JS.get(events[index][1], "type"), tContent)) {
+        JS.invoke(events, "splice", index, 1);
       }
-      if (definition < 0 && objGet(events[index][1], "type") == tDefinition) {
+      if (definition == 0.0 && JS.strictEqual(JS.get(events[index][1], "type"), tDefinition)) {
         definition = index;
       }
     }
   }
-  JsValue heading = JS.object();
-  JS.set(heading, "type", tSetextHeading);
-  JS.set(heading, "start", copyPoint(objGet(events[content][1], "start")));
-  JS.set(heading, "end", copyPoint(objGet(events[arrLen(events) - 1][1], "end")));
+  JsValue heading = JS.object(
+    "type", tSetextHeading,
+    "start", copyPoint(JS.get(events[content][1], "start")),
+    "end", copyPoint(JS.get(events[events.length - 1.0][1], "end"))
+  );
   JS.set(events[text][1], "type", tSetextHeadingText);
-  if (definition > 0) {
-    JsValue enterH = eventEnter(heading, context);
-    JS.call(JS.get(events, "splice"), events, text, 0, enterH);
-    JsValue exitC = eventExit(events[content][1], context);
-    JS.call(JS.get(events, "splice"), events, definition + 1, 0, exitC);
-    JS.set(events[content][1], "end", copyPoint(objGet(events[definition][1], "end")));
+  if (definition != 0.0) {
+    JS.invoke(events, "splice", text, 0, JS.array("enter", heading, context));
+    JS.invoke(events, "splice", definition + 1.0, 0, JS.array("exit", events[content][1], context));
+    JS.set(events[content][1], "end", copyPoint(JS.get(events[definition][1], "end")));
   } else {
     events[content][1] = heading;
   }
-  JsValue exitH = eventExit(heading, context);
-  push(events, exitH);
+  JS.invoke(events, "push", JS.array("exit", heading, context));
   return events;
 });
 
 JsValue tokenizeSetextUnderline = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  int marker = 0;
+  JsValue marker = JS.undefined();
+  JsValue inside = JS.undefined();
+  JsValue after = JS.undefined();
 
-  JsValue after = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tSetextHeadingLine);
-      return call1(ok, undef(), code);
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    float index = JS.get(self, "events").length;
+    bool paragraph = false;
+    while (true) {
+      index--;
+      if (index < 0.0) {
+        break;
+      }
+      JsValue typ = JS.get(JS.get(self, "events")[index][1], "type");
+      if (JS.strictNotEqual(typ, tLineEnding) && JS.strictNotEqual(typ, tLinePrefix) && JS.strictNotEqual(typ, tContent)) {
+        paragraph = JS.strictEqual(typ, tParagraph);
+        break;
+      }
     }
-    return call1(nok, undef(), code);
+    if (!JS.get(JS.get(self, "parser"), "lazy")[JS.get(JS.invoke(self, "now"), "line")].truthy() && (JS.get(self, "interrupt").truthy() || paragraph)) {
+      JS.invoke(effects, "enter", tSetextHeadingLine);
+      marker = code;
+      JS.invoke(effects, "enter", tSetextHeadingLineSequence);
+      return JS.call(inside, JS.undefined(), code);
+    }
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue inside = JS.method1((JsValue _s, JsValue code) => {
-    if (code == marker) {
-      call1(objGet(effects, "consume"), effects, code);
+  inside = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, marker)) {
+      JS.invoke(effects, "consume", code);
       return inside;
     }
-    call1(objGet(effects, "exit"), effects, tSetextHeadingLineSequence);
+    JS.invoke(effects, "exit", tSetextHeadingLineSequence);
     if (markdownSpace(code)) {
-      return call1(factorySpace(effects, after, tLineSuffix, 0), undef(), code);
+      return JS.call(factorySpace(effects, after, tLineSuffix, JS.undefined()), JS.undefined(), code);
     }
-    return call1(after, undef(), code);
+    return JS.call(after, JS.undefined(), code);
   });
 
-  JsValue before = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tSetextHeadingLineSequence);
-    return call1(inside, undef(), code);
-  });
-
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    int index = arrLen(objGet(self, "events"));
-    bool paragraph = false;
-    while (true) {
-      index = index - 1;
-      if (index < 0) {
-        break;
-      }
-      string typ = JS.string(objGet(objGet(self, "events")[index][1], "type"));
-      if (typ != tLineEnding && typ != tLinePrefix && typ != tContent) {
-        paragraph = typ == tParagraph;
-        break;
-      }
-    }
-    JsValue lazy = objGet(objGet(self, "parser"), "lazy")[objGet(call0(objGet(self, "now"), self), "line")];
-    if (!lazy.truthy() && (objGet(self, "interrupt").truthy() || paragraph)) {
-      call1(objGet(effects, "enter"), effects, tSetextHeadingLine);
-      marker = JS.number(code).toInt();
-      return call1(before, undef(), code);
+  after = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tSetextHeadingLine);
+      return JS.call(ok, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
   return start;
 });
 
-export JsValue setextUnderline = JS.object("name", "setextUnderline", "tokenize", tokenizeSetextUnderline, "resolveTo", resolveToSetextUnderline);
+export JsValue setextUnderline = JS.object("name", "setextUnderline", "resolveTo", resolveToSetextUnderline, "tokenize", tokenizeSetextUnderline);
diff -ruN a/src/graph/remark-parse/micromark/core/thematic-break.lil b/src/graph/remark-parse/micromark/core/thematic-break.lil
--- a/src/graph/remark-parse/micromark/core/thematic-break.lil	2026-09-23 20:40:45.184366816 +0000
+++ b/src/graph/remark-parse/micromark/core/thematic-break.lil	2026-09-23 20:40:45.297554723 +0000
@@ -1,50 +1,46 @@
-import { call1, isEof, makeNamedConstruct, objGet, undef } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { markdownLineEnding, markdownSpace } from "../util-character.lil";
 import { kThematicBreakMarkerCountMin } from "../symbol/constants.lil";
 import { tThematicBreak, tThematicBreakSequence, tWhitespace } from "../symbol/types.lil";
 
 JsValue tokenizeThematicBreak = JS.method3((JsValue _self, JsValue effects, JsValue ok, JsValue nok) => {
-  int size = 0;
-  int marker = 0;
-  JsValue atBreak = undef();
-
-  JsValue sequence = JS.method1((JsValue _s, JsValue code) => {
-    if (code == marker) {
-      call1(objGet(effects, "consume"), effects, code);
-      size = size + 1;
-      return sequence;
-    }
-    call1(objGet(effects, "exit"), effects, tThematicBreakSequence);
-    if (markdownSpace(code)) {
-      return call1(factorySpace(effects, atBreak, tWhitespace, 0), undef(), code);
-    }
-    return call1(atBreak, undef(), code);
+  float size = 0.0;
+  JsValue marker = JS.undefined();
+  JsValue atBreak = JS.undefined();
+  JsValue sequence = JS.undefined();
+
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", tThematicBreak);
+    marker = code;
+    return JS.call(atBreak, JS.undefined(), code);
   });
 
   atBreak = JS.method1((JsValue _s, JsValue code) => {
-    if (code == marker) {
-      call1(objGet(effects, "enter"), effects, tThematicBreakSequence);
-      return call1(sequence, undef(), code);
+    if (JS.strictEqual(code, marker)) {
+      JS.invoke(effects, "enter", tThematicBreakSequence);
+      return JS.call(sequence, JS.undefined(), code);
     }
-    if (size >= kThematicBreakMarkerCountMin && (isEof(code) || markdownLineEnding(code))) {
-      call1(objGet(effects, "exit"), effects, tThematicBreak);
-      return call1(ok, undef(), code);
+    if (size >= kThematicBreakMarkerCountMin && (JS.strictEqual(code, null) || markdownLineEnding(code))) {
+      JS.invoke(effects, "exit", tThematicBreak);
+      return JS.call(ok, JS.undefined(), code);
     }
-    return call1(nok, undef(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  JsValue before = JS.method1((JsValue _s, JsValue code) => {
-    marker = JS.number(code).toInt();
-    return call1(atBreak, undef(), code);
-  });
-
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tThematicBreak);
-    return call1(before, undef(), code);
+  sequence = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, marker)) {
+      JS.invoke(effects, "consume", code);
+      size++;
+      return sequence;
+    }
+    JS.invoke(effects, "exit", tThematicBreakSequence);
+    if (markdownSpace(code)) {
+      return JS.call(factorySpace(effects, atBreak, tWhitespace, JS.undefined()), JS.undefined(), code);
+    }
+    return JS.call(atBreak, JS.undefined(), code);
   });
 
   return start;
 });
 
-export JsValue thematicBreak = makeNamedConstruct("thematicBreak", tokenizeThematicBreak);
+export JsValue thematicBreak = JS.object("name", "thematicBreak", "tokenize", tokenizeThematicBreak);
diff -ruN a/src/graph/remark-parse/micromark/create-tokenizer.lil b/src/graph/remark-parse/micromark/create-tokenizer.lil
--- a/src/graph/remark-parse/micromark/create-tokenizer.lil	2026-09-23 20:40:45.158691677 +0000
+++ b/src/graph/remark-parse/micromark/create-tokenizer.lil	2026-09-23 20:40:45.297692900 +0000
@@ -1,398 +1,299 @@
-import {
-  arrLen,
-  arrayFrom,
-  call0,
-  call1,
-  call2,
-  call3,
-  codeEof,
-  eventEnter,
-  eventExit,
-  fromCharCode1,
-  includesVal,
-  isEof,
-  isStr,
-  jsInt,
-  jsNum,
-  jsString,
-  objGet,
-  objGetVal,
-  objSetVal,
-  objectAssign,
-  objectCreate,
-  pop,
-  push,
-  slice1,
-  slice2,
-  undef
-} from "./host.lil";
+import { TokenizerPoint, TokenizerToken, asList, copyPoint, isEof, undef } from "./host.lil";
 import { markdownLineEnding } from "./util-character.lil";
 import { pushAll, splice } from "./util-chunked.lil";
 import { resolveAll } from "./util-resolve-all.lil";
-import {
-  cCarriageReturn,
-  cCarriageReturnLineFeed,
-  cHorizontalTab,
-  cLineFeed,
-  cVirtualSpace,
-  codeEq
-} from "./symbol/codes.lil";
-import { vCr, vHt, vLf, vSpace } from "./symbol/values.lil";
-
-struct PointState {
-  float _bufferIndex;
-  float _index;
-  float line;
-  float column;
-  float offset;
 
-}
-
-JsValue sliceChunks(JsValue chunks, JsValue token) {
-  JsValue start = objGet(token, "start");
-  JsValue end = objGet(token, "end");
-  int startIndex = jsInt(objGet(start, "_index"));
-  int startBufferIndex = jsInt(objGet(start, "_bufferIndex"));
-  int endIndex = jsInt(objGet(end, "_index"));
-  int endBufferIndex = jsInt(objGet(end, "_bufferIndex"));
-  JsValue[] view = [];
+extern JsValue Object;
+extern JsValue Array;
+extern JsValue String;
+
+JsValue sliceChunks(JsValue chunks, TokenizerToken token) {
+  float startIndex = token.start._index;
+  float startBufferIndex = token.start._bufferIndex;
+  float endIndex = token.end._index;
+  float endBufferIndex = token.end._bufferIndex;
   if (startIndex == endIndex) {
-    string chunk = JS.assume(chunks[startIndex]);
-    view = [chunk.slice(startBufferIndex, endBufferIndex)];
-  } else {
-    view = JS.assume(slice2(chunks, startIndex, endIndex));
-    if (startBufferIndex > -1) {
-      JsValue head = view[0];
-      if (isStr(head)) {
-        view[0] = JS.call(JS.get(head, "slice"), head, startBufferIndex);
-      } else {
-        JS.call(JS.get(view, "shift"), view);
-      }
-    }
-    if (endBufferIndex > 0) {
-      string chunk = JS.assume(chunks[endIndex]);
-      push(view, chunk.slice(0, endBufferIndex));
+    JsValue chunk = chunks[startIndex];
+    return JS.array(JS.call(JS.get(chunk, "slice"), chunk, startBufferIndex, endBufferIndex));
+  }
+  JsValue view = JS.call(JS.get(chunks, "slice"), chunks, startIndex, endIndex);
+  if (startBufferIndex > -1.0) {
+    JsValue head = view[0];
+    if (JS.typeOf(head) == "string") {
+      view[0] = JS.call(JS.get(head, "slice"), head, startBufferIndex);
+    } else {
+      JS.call(JS.get(view, "shift"), view);
     }
   }
+  if (endBufferIndex > 0.0) {
+    JsValue chunk = chunks[endIndex];
+    JS.call(JS.get(view, "push"), view, JS.call(JS.get(chunk, "slice"), chunk, 0, endBufferIndex));
+  }
   return view;
 }
 
-string serializeChunks(JsValue chunks, JsValue expandTabs) {
-  int index = -1;
-  string[] result = [];
+JsValue serializeChunks(JsValue chunks, JsValue expandTabs) {
+  JsValue[] result = [];
   bool atTab = false;
-  int n = arrLen(chunks);
-  while (true) {
-    index = index + 1;
-    if (index >= n) {
-      break;
-    }
+  for (float index = 0.0; index < chunks.length; index++) {
     JsValue chunk = chunks[index];
-    string value = "";
-    if (isStr(chunk)) {
-      value = jsString(chunk);
-    } else if (codeEq(chunk, cCarriageReturn)) {
-      value = vCr;
-    } else if (codeEq(chunk, cLineFeed)) {
-      value = vLf;
-    } else if (codeEq(chunk, cCarriageReturnLineFeed)) {
-      value = vCr + vLf;
-    } else if (codeEq(chunk, cHorizontalTab)) {
-      if (expandTabs.truthy()) {
-        value = vSpace;
+    JsValue value = chunk;
+    if (JS.typeOf(chunk) != "string") {
+      if (JS.strictEqual(chunk, -5)) {
+        value = "\r";
+      } else if (JS.strictEqual(chunk, -4)) {
+        value = "\n";
+      } else if (JS.strictEqual(chunk, -3)) {
+        value = "\r\n";
+      } else if (JS.strictEqual(chunk, -2)) {
+        value = if (expandTabs.truthy()) { " " } else { "\t" };
+      } else if (JS.strictEqual(chunk, -1)) {
+        if (!expandTabs.truthy() && atTab) {
+          continue;
+        }
+        value = " ";
       } else {
-        value = vHt;
+        value = JS.call(JS.get(String, "fromCharCode"), String, chunk);
       }
-    } else if (codeEq(chunk, cVirtualSpace)) {
-      if (!expandTabs.truthy() && atTab) {
-        continue;
-      }
-      value = vSpace;
-    } else {
-      value = fromCharCode1(chunk);
     }
-    atTab = codeEq(chunk, cHorizontalTab);
+    atTab = JS.strictEqual(chunk, -2);
     result.push(value);
   }
-  return result.join("");
-}
-
-pure PointState clonePointState(PointState point) {
-  return PointState{point._bufferIndex, point._index, point.line, point.column, point.offset};
-}
-
-pure JsValue pointValue(PointState point) {
-  return JS.object(
-    "_bufferIndex", point._bufferIndex,
-    "_index", point._index,
-    "line", point.line,
-    "column", point.column,
-    "offset", point.offset
-  );
+  return JS.call(JS.get(result, "join"), result, "");
 }
 
 export JsValue createTokenizer(JsValue parser, JsValue initialize, JsValue from) {
-  float line = 1.0;
-  float column = 1.0;
-  float offset = 0.0;
-  if (from.truthy()) {
-    JsValue fromLine = objGet(from, "line");
-    if (fromLine.truthy()) {
-      line = jsNum(fromLine);
-    }
-    JsValue fromColumn = objGet(from, "column");
-    if (fromColumn.truthy()) {
-      column = jsNum(fromColumn);
-    }
-    JsValue fromOffset = objGet(from, "offset");
-    if (fromOffset.truthy()) {
-      offset = jsNum(fromOffset);
-    }
-  }
-  PointState point = PointState{-1.0, 0.0, line, column, offset};
+  TokenizerPoint point = JS.assume(JS.object(
+    "_bufferIndex", -1,
+    "_index", 0,
+    "line", JS.or(JS.and(from, JS.get(from, "line")), 1),
+    "column", JS.or(JS.and(from, JS.get(from, "column")), 1),
+    "offset", JS.or(JS.and(from, JS.get(from, "offset")), 0)
+  ));
   JsValue columnStart = JS.object();
-  JsValue resolveAllConstructs = JS.array();
+  JsValue[] resolveAllConstructs = [];
   JsValue chunks = JS.array();
   JsValue[] stack = [];
-  JsValue context = JS.object();
+  JsValue context = undef();
   JsValue effects = undef();
   JsValue state = undef();
 
+  JsValue now = JS.method0((JsValue _self) => {
+    return copyPoint(point);
+  });
+
   JsValue accountForPotentialSkip = JS.method0((JsValue _self) => {
-    string lineKey = JS.string(point.line);
-    if (JS.has(columnStart, lineKey) && point.column < 2.0) {
-      point.column = jsNum(objGetVal(columnStart, lineKey));
-      point.offset = point.offset + jsNum(objGetVal(columnStart, lineKey)) - 1.0;
+    if (JS.in(point.line, columnStart) && point.column < 2.0) {
+      float column = JS.assume(columnStart[point.line]);
+      point.column = column;
+      point.offset = point.offset + column - 1.0;
     }
     return undef();
   });
 
   JsValue addResult = JS.method2((JsValue _self, JsValue construct, JsValue from) => {
-    if (objGet(construct, "resolveAll").truthy() && !includesVal(resolveAllConstructs, construct)) {
-      push(resolveAllConstructs, construct);
+    if (JS.get(construct, "resolveAll").truthy() && !resolveAllConstructs.includes(construct)) {
+      resolveAllConstructs.push(construct);
     }
-    if (objGet(construct, "resolve").truthy()) {
-      JsValue events = objGet(context, "events");
-      JsValue resolved = call2(objGet(construct, "resolve"), construct, slice1(events, from), context);
-      splice(events, from, arrLen(events) - jsInt(from), resolved);
+    if (JS.get(construct, "resolve").truthy()) {
+      JsValue events = JS.get(context, "events");
+      float start = JS.assume(from);
+      splice(events, from, events.length - start, JS.invoke(construct, "resolve", JS.invoke(events, "slice", from), context));
     }
-    if (objGet(construct, "resolveTo").truthy()) {
-      JS.set(context, "events", call2(objGet(construct, "resolveTo"), construct, objGet(context, "events"), context));
+    if (JS.get(construct, "resolveTo").truthy()) {
+      JS.set(context, "events", JS.invoke(construct, "resolveTo", JS.get(context, "events"), context));
     }
     return undef();
   });
 
   JsValue constructFactory = JS.method2((JsValue _self, JsValue onreturn, JsValue fields) => {
     return JS.method3((JsValue _hookSelf, JsValue constructs, JsValue returnState, JsValue bogusState) => {
-      JsValue[] listOfConstructs = [];
-      int constructIndex = 0;
+      JsValue listOfConstructs = undef();
+      float constructIndex = 0.0;
       JsValue currentConstruct = undef();
       float eventStart = 0.0;
       JsValue restore = undef();
       JsValue handleConstruct = undef();
-      JsValue handleListOfConstructs = undef();
 
       JsValue ok = JS.method1((JsValue _s, JsValue code) => {
-        call3(onreturn, JS.undefined(), currentConstruct, restore, eventStart);
+        JS.call(onreturn, undef(), currentConstruct, restore, eventStart);
         return returnState;
       });
       JsValue nok = JS.method1((JsValue _s, JsValue code) => {
-        call0(restore, JS.undefined());
-        constructIndex = constructIndex + 1;
+        JS.call(restore, undef());
+        constructIndex = constructIndex + 1.0;
         if (constructIndex < listOfConstructs.length) {
-          return call1(handleConstruct, JS.undefined(), listOfConstructs[constructIndex]);
+          return JS.call(handleConstruct, undef(), listOfConstructs[constructIndex]);
         }
         return bogusState;
       });
 
       handleConstruct = JS.method1((JsValue _s, JsValue construct) => {
         return JS.method1((JsValue _stateSelf, JsValue code) => {
-          PointState startPoint = clonePointState(point);
-          JsValue startPrevious = objGet(context, "previous");
-          JsValue startCurrentConstruct = objGet(context, "currentConstruct");
-          eventStart = arrLen(objGet(context, "events"));
-          JsValue[] startStack = JS.assume(arrayFrom(stack));
+          JsValue startPoint = JS.call(now, undef());
+          JsValue startPrevious = JS.get(context, "previous");
+          JsValue startCurrentConstruct = JS.get(context, "currentConstruct");
+          eventStart = JS.get(context, "events").length;
+          JsValue startStack = JS.call(JS.get(Array, "from"), Array, stack);
           restore = JS.method0((JsValue _restoreSelf) => {
-            point = startPoint;
+            point = JS.assume(startPoint);
             JS.set(context, "previous", startPrevious);
             JS.set(context, "currentConstruct", startCurrentConstruct);
-            JS.set(objGet(context, "events"), "length", eventStart);
-            stack = startStack;
-            call0(accountForPotentialSkip, JS.undefined());
+            JS.set(JS.get(context, "events"), "length", eventStart);
+            stack = JS.assume(startStack);
+            JS.call(accountForPotentialSkip, undef());
             return undef();
           });
           currentConstruct = construct;
-          if (!objGet(construct, "partial").truthy()) {
+          if (!JS.get(construct, "partial").truthy()) {
             JS.set(context, "currentConstruct", construct);
           }
-          JsValue name = objGet(construct, "name");
+          JsValue name = JS.get(construct, "name");
           if (name.truthy()) {
-            JsValue disable = objGet(objGet(objGet(context, "parser"), "constructs"), "disable");
-            JsValue disabled = objGet(disable, "null");
-            if (JS.call(JS.get(disabled, "includes"), disabled, objGet(construct, "name")).truthy()) {
-              return call1(nok, JS.undefined(), code);
+            JsValue disabled = JS.get(JS.get(JS.get(JS.get(context, "parser"), "constructs"), "disable"), "null");
+            if (JS.call(JS.get(disabled, "includes"), disabled, name).truthy()) {
+              return JS.call(nok, undef(), code);
             }
           }
-          JsValue selfCtx = context;
+          JsValue self = context;
           if (fields.truthy()) {
-            selfCtx = objectAssign(objectCreate(context), fields);
+            self = JS.call(JS.get(Object, "assign"), Object, JS.call(JS.get(Object, "create"), Object, context), fields);
           }
-          JsValue next = call3(objGet(construct, "tokenize"), selfCtx, effects, ok, nok);
-          return call1(next, JS.undefined(), code);
+          JsValue next = JS.call(JS.get(construct, "tokenize"), self, effects, ok, nok);
+          return JS.call(next, undef(), code);
         });
       });
 
-      handleListOfConstructs = JS.method1((JsValue _s, JsValue list) => {
-        listOfConstructs = JS.assume(list);
-        constructIndex = 0;
-        if (listOfConstructs.length == 0) {
+      JsValue handleListOfConstructs = JS.method1((JsValue _s, JsValue list) => {
+        listOfConstructs = list;
+        constructIndex = 0.0;
+        if (list.length == 0.0) {
           return bogusState;
         }
-        return call1(handleConstruct, JS.undefined(), listOfConstructs[0]);
+        return JS.call(handleConstruct, undef(), list[0]);
       });
 
       if (constructs.isArray()) {
-        return call1(handleListOfConstructs, JS.undefined(), constructs);
+        return JS.call(handleListOfConstructs, undef(), constructs);
       }
-      if (JS.has(constructs, "tokenize")) {
-        JsValue[] list = [constructs];
-        return call1(handleListOfConstructs, JS.undefined(), list);
+      if (JS.in("tokenize", constructs)) {
+        return JS.call(handleListOfConstructs, undef(), JS.array(constructs));
       }
       return JS.method1((JsValue _s, JsValue code) => {
-        JsValue left = undef();
-        JsValue all = undef();
+        JsValue left = code;
+        JsValue all = code;
         if (!isEof(code)) {
-          left = objGetVal(constructs, code);
-          all = objGet(constructs, "null");
-        }
-        JsValue[] leftList = [];
-        if (left.isArray()) {
-          leftList = JS.assume(left);
-        } else if (left.truthy()) {
-          leftList = [left];
-        }
-        JsValue[] allList = [];
-        if (all.isArray()) {
-          allList = JS.assume(all);
-        } else if (all.truthy()) {
-          allList = [all];
+          left = constructs[code];
+          all = JS.get(constructs, "null");
         }
+        JsValue[] leftList = JS.assume(asList(left));
+        JsValue[] allList = JS.assume(asList(all));
         JsValue[] list = [...leftList, ...allList];
-        return call1(call1(handleListOfConstructs, JS.undefined(), list), JS.undefined(), code);
+        return JS.call(JS.call(handleListOfConstructs, undef(), list), undef(), code);
       });
     });
   });
 
-  JsValue nowFn = JS.method0((JsValue _self) => {
-    return pointValue(point);
-  });
-  JsValue defineSkipFn = JS.method1((JsValue _self, JsValue value) => {
-    objSetVal(columnStart, objGet(value, "line"), objGet(value, "column"));
-    call0(accountForPotentialSkip, JS.undefined());
+  JsValue onsuccessfulcheck = JS.method3((JsValue _s, JsValue _construct, JsValue restore, JsValue _from) => {
+    JS.call(restore, undef());
     return undef();
   });
-  JsValue sliceStreamFn = JS.method1((JsValue _self, JsValue token) => {
-    return sliceChunks(chunks, token);
-  });
-  JsValue sliceSerializeFn = JS.method2((JsValue _self, JsValue token, JsValue expandTabs) => {
-    return serializeChunks(sliceChunks(chunks, token), expandTabs);
-  });
-  JsValue consumeFn = JS.method1((JsValue _self, JsValue code) => {
-    if (markdownLineEnding(code)) {
-      point.line = point.line + 1.0;
-      point.column = 1.0;
-      float add = if (codeEq(code, cCarriageReturnLineFeed)) { 2.0 } else { 1.0 };
-      point.offset = point.offset + add;
-      call0(accountForPotentialSkip, JS.undefined());
-    } else if (!codeEq(code, cVirtualSpace)) {
-      point.column = point.column + 1.0;
-      point.offset = point.offset + 1.0;
-    }
-    if (point._bufferIndex < 0.0) {
-      point._index = point._index + 1.0;
-    } else {
-      point._bufferIndex = point._bufferIndex + 1.0;
-      JsValue chunk = chunks[point._index.toInt()];
-      float chunkLength = chunk.length;
-      if (point._bufferIndex == chunkLength) {
-        point._bufferIndex = -1.0;
-        point._index = point._index + 1.0;
+
+  JsValue attempt = JS.call(constructFactory, undef(), JS.method3((JsValue _s, JsValue construct, JsValue _restore, JsValue from) => {
+      JS.call(addResult, undef(), construct, from);
+      return undef();
+    }), undef());
+  JsValue check = JS.call(constructFactory, undef(), onsuccessfulcheck, undef());
+  JsValue interrupt = JS.call(constructFactory, undef(), onsuccessfulcheck, JS.object("interrupt", true));
+  effects = JS.object(
+    "attempt", attempt,
+    "check", check,
+    "consume", JS.method1((JsValue _self, JsValue code) => {
+      if (markdownLineEnding(code)) {
+        point.line = point.line + 1.0;
+        point.column = 1.0;
+        point.offset = point.offset + if (JS.strictEqual(code, -3)) { 2.0 } else { 1.0 };
+        JS.call(accountForPotentialSkip, undef());
+      } else if (!JS.strictEqual(code, -1)) {
+        point.column = point.column + 1.0;
+        point.offset = point.offset + 1.0;
       }
-    }
-    JS.set(context, "previous", code);
-    return undef();
-  });
-  JsValue enterFn = JS.method2((JsValue _self, JsValue typ, JsValue fields) => {
-    JsValue token = fields;
-    if (!fields.truthy()) {
-      token = JS.object();
-    }
-    JS.set(token, "type", typ);
-    JS.set(token, "start", pointValue(point));
-    push(objGet(context, "events"), eventEnter(token, context));
-    stack.push(token);
-    return token;
-  });
-  JsValue exitFn = JS.method1((JsValue _self, JsValue typ) => {
-    JsValue token = stack.pop();
-    JS.set(token, "end", pointValue(point));
-    push(objGet(context, "events"), eventExit(token, context));
-    return token;
-  });
-  JsValue writeFn = JS.method1((JsValue _self, JsValue slice) => {
-    chunks = pushAll(chunks, slice);
-    while (point._index < arrLen(chunks)) {
-      JsValue chunk = chunks[point._index.toInt()];
-      if (chunk is string) {
-        float chunkIndex = point._index;
-        if (point._bufferIndex < 0.0) {
-          point._bufferIndex = 0.0;
+      if (point._bufferIndex < 0.0) {
+        point._index = point._index + 1.0;
+      } else {
+        point._bufferIndex = point._bufferIndex + 1.0;
+        if (point._bufferIndex == chunks[point._index].length) {
+          point._bufferIndex = -1.0;
+          point._index = point._index + 1.0;
         }
-        string text = chunk;
-        while (point._index == chunkIndex && point._bufferIndex < text.length) {
-          state = call1(state, JS.undefined(), text.charCodeAt(point._bufferIndex.toInt()));
+      }
+      JS.set(context, "previous", code);
+      return undef();
+    }),
+    "enter", JS.method2((JsValue _self, JsValue type, JsValue fields) => {
+      JsValue token = JS.or(fields, JS.object());
+      JS.set(token, "type", type);
+      JS.set(token, "start", JS.call(now, undef()));
+      JsValue events = JS.get(context, "events");
+      JS.call(JS.get(events, "push"), events, JS.array("enter", token, context));
+      stack.push(token);
+      return token;
+    }),
+    "exit", JS.method1((JsValue _self, JsValue type) => {
+      JsValue token = stack.pop();
+      JS.set(token, "end", JS.call(now, undef()));
+      JsValue events = JS.get(context, "events");
+      JS.call(JS.get(events, "push"), events, JS.array("exit", token, context));
+      return token;
+    }),
+    "interrupt", interrupt
+  );
+
+  context = JS.object(
+    "code", null,
+    "containerState", JS.object(),
+    "defineSkip", JS.method1((JsValue _self, JsValue value) => {
+      columnStart[JS.get(value, "line")] = JS.get(value, "column");
+      JS.call(accountForPotentialSkip, undef());
+      return undef();
+    }),
+    "events", JS.array(),
+    "now", now,
+    "parser", parser,
+    "previous", null,
+    "sliceSerialize", JS.method2((JsValue _self, JsValue token, JsValue expandTabs) => {
+      return serializeChunks(sliceChunks(chunks, JS.assume(token)), expandTabs);
+    }),
+    "sliceStream", JS.method1((JsValue _self, JsValue token) => {
+      return sliceChunks(chunks, JS.assume(token));
+    }),
+    "write", JS.method1((JsValue _self, JsValue slice) => {
+      chunks = pushAll(chunks, slice);
+      while (point._index < chunks.length) {
+        JsValue chunk = chunks[point._index];
+        if (JS.typeOf(chunk) == "string") {
+          float chunkIndex = point._index;
+          if (point._bufferIndex < 0.0) {
+            point._bufferIndex = 0.0;
+          }
+          while (point._index == chunkIndex && point._bufferIndex < chunk.length) {
+            state = JS.call(state, undef(), JS.call(JS.get(chunk, "charCodeAt"), chunk, point._bufferIndex));
+          }
+        } else {
+          state = JS.call(state, undef(), chunk);
         }
-      } else {
-        state = call1(state, JS.undefined(), chunk);
       }
-    }
-    if (!isEof(chunks[arrLen(chunks) - 1])) {
-      return JS.array();
-    }
-    call2(addResult, JS.undefined(), initialize, 0);
-    JS.set(context, "events", resolveAll(resolveAllConstructs, objGet(context, "events"), context));
-    return objGet(context, "events");
-  });
-  JsValue onsuccessfulconstruct = JS.method3((JsValue _s, JsValue construct, JsValue _restore, JsValue from) => {
-    call2(addResult, JS.undefined(), construct, from);
-    return undef();
-  });
-  JsValue onsuccessfulcheck = JS.method3((JsValue _s, JsValue _construct, JsValue restore, JsValue _from) => {
-    call0(restore, JS.undefined());
-    return undef();
-  });
-  JsValue interruptFields = JS.object("interrupt", true);
-  effects = JS.object(
-    "attempt", call2(constructFactory, JS.undefined(), onsuccessfulconstruct, undef()),
-    "check", call2(constructFactory, JS.undefined(), onsuccessfulcheck, undef()),
-    "consume", consumeFn,
-    "enter", enterFn,
-    "exit", exitFn,
-    "interrupt", call2(constructFactory, JS.undefined(), onsuccessfulcheck, interruptFields)
+      if (!isEof(chunks[chunks.length - 1.0])) {
+        return JS.array();
+      }
+      JS.call(addResult, undef(), initialize, 0);
+      JS.set(context, "events", resolveAll(resolveAllConstructs, JS.get(context, "events"), context));
+      return JS.get(context, "events");
+    })
   );
-  JS.set(context, "code", codeEof);
-  JS.set(context, "containerState", JS.object());
-  JS.set(context, "defineSkip", defineSkipFn);
-  JS.set(context, "events", JS.array());
-  JS.set(context, "now", nowFn);
-  JS.set(context, "parser", parser);
-  JS.set(context, "previous", codeEof);
-  JS.set(context, "sliceSerialize", sliceSerializeFn);
-  JS.set(context, "sliceStream", sliceStreamFn);
-  JS.set(context, "write", writeFn);
-  state = call1(objGet(initialize, "tokenize"), context, effects);
-  if (objGet(initialize, "resolveAll").truthy()) {
-    push(resolveAllConstructs, initialize);
+  state = JS.call(JS.get(initialize, "tokenize"), context, effects);
+  if (JS.get(initialize, "resolveAll").truthy()) {
+    resolveAllConstructs.push(initialize);
   }
   return context;
 }
diff -ruN a/src/graph/remark-parse/micromark/decode-named.lil b/src/graph/remark-parse/micromark/decode-named.lil
--- a/src/graph/remark-parse/micromark/decode-named.lil	2026-09-23 20:40:45.188691941 +0000
+++ b/src/graph/remark-parse/micromark/decode-named.lil	2026-09-23 20:40:45.298156901 +0000
@@ -1,9 +1,9 @@
 import { characterEntities } from "./character-entities.lil";
-import { hasOwn, objGet } from "./host.lil";
+import { hasOwn } from "./host.lil";
 
 export JsValue decodeNamedCharacterReference(string value) {
   if (hasOwn(characterEntities, value)) {
-    return objGet(characterEntities, value);
+    return characterEntities[value];
   }
   return false;
 }
diff -ruN a/src/graph/remark-parse/micromark/factory-destination.lil b/src/graph/remark-parse/micromark/factory-destination.lil
--- a/src/graph/remark-parse/micromark/factory-destination.lil	2026-09-23 20:40:45.192357739 +0000
+++ b/src/graph/remark-parse/micromark/factory-destination.lil	2026-09-23 20:40:45.298438488 +0000
@@ -1,16 +1,10 @@
-import { call1, call2, objGet, undef } from "./host.lil";
 import { asciiControl, markdownLineEnding, markdownLineEndingOrSpace } from "./util-character.lil";
-import { cLessThan, codeEq } from "./symbol/codes.lil";
-import { isEof } from "./host.lil";
+import { cBackslash, cGreaterThan, cLeftParenthesis, cLessThan, cRightParenthesis, cSpace, codeEq } from "./symbol/codes.lil";
 import { kContentTypeString } from "./symbol/constants.lil";
 import { tChunkString } from "./symbol/types.lil";
 
-JsValue chunkStringFields() {
-  JsValue fields = JS.object();
-  JS.set(fields, "contentType", kContentTypeString);
-  return fields;
-}
-
+// `max` is optional, as upstream's: `JS.undefined()` (elided at the call site)
+// means no limit on nested parentheses.
 export JsValue factoryDestination(
   JsValue effects,
   JsValue ok,
@@ -20,108 +14,101 @@
   string literalMarkerType,
   string rawType,
   string stringType,
-  float max
+  JsValue max
 ) {
-  float limit = 1.0 / 0.0;
-  if (max != 0.0) {
-    limit = max;
-  }
-  int balance = 0;
-  JsValue enclosed = undef();
-  JsValue enclosedBefore = undef();
-  JsValue raw = undef();
-
-  JsValue enclosedEscape = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cLessThan) || codeEq(code, 62) || codeEq(code, 92)) {
-      call1(objGet(effects, "consume"), effects, code);
-      return enclosed;
-    }
-    return call1(enclosed, JS.undefined(), code);
-  });
+  float limit = if (max.truthy()) { JS.number(max) } else { 1.0 / 0.0 };
+  float balance = 0.0;
+  JsValue enclosedBefore = JS.undefined();
+  JsValue enclosed = JS.undefined();
+  JsValue enclosedEscape = JS.undefined();
+  JsValue raw = JS.undefined();
+  JsValue rawEscape = JS.undefined();
 
-  enclosed = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, 62)) {
-      call1(objGet(effects, "exit"), effects, tChunkString);
-      call1(objGet(effects, "exit"), effects, stringType);
-      return call1(enclosedBefore, JS.undefined(), code);
-    }
-    if (isEof(code) || codeEq(code, 60) || markdownLineEnding(code)) {
-      return call1(nok, JS.undefined(), code);
-    }
-    call1(objGet(effects, "consume"), effects, code);
-    if (codeEq(code, 92)) {
-      return enclosedEscape;
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cLessThan)) {
+      JS.invoke(effects, "enter", typ);
+      JS.invoke(effects, "enter", literalType);
+      JS.invoke(effects, "enter", literalMarkerType);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", literalMarkerType);
+      return enclosedBefore;
+    }
+    if (JS.strictEqual(code, null) || codeEq(code, cSpace) || codeEq(code, cRightParenthesis) || asciiControl(code)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    return enclosed;
+    JS.invoke(effects, "enter", typ);
+    JS.invoke(effects, "enter", rawType);
+    JS.invoke(effects, "enter", stringType);
+    JS.invoke(effects, "enter", tChunkString, JS.object("contentType", kContentTypeString));
+    return JS.call(raw, JS.undefined(), code);
   });
 
   enclosedBefore = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, 62)) {
-      call1(objGet(effects, "enter"), effects, literalMarkerType);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, literalMarkerType);
-      call1(objGet(effects, "exit"), effects, literalType);
-      call1(objGet(effects, "exit"), effects, typ);
+    if (codeEq(code, cGreaterThan)) {
+      JS.invoke(effects, "enter", literalMarkerType);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", literalMarkerType);
+      JS.invoke(effects, "exit", literalType);
+      JS.invoke(effects, "exit", typ);
       return ok;
     }
-    call1(objGet(effects, "enter"), effects, stringType);
-    call2(objGet(effects, "enter"), effects, tChunkString, chunkStringFields());
-    return call1(enclosed, JS.undefined(), code);
+    JS.invoke(effects, "enter", stringType);
+    JS.invoke(effects, "enter", tChunkString, JS.object("contentType", kContentTypeString));
+    return JS.call(enclosed, JS.undefined(), code);
   });
 
-  JsValue rawEscape = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, 40) || codeEq(code, 41) || codeEq(code, 92)) {
-      call1(objGet(effects, "consume"), effects, code);
-      return raw;
+  enclosed = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cGreaterThan)) {
+      JS.invoke(effects, "exit", tChunkString);
+      JS.invoke(effects, "exit", stringType);
+      return JS.call(enclosedBefore, JS.undefined(), code);
     }
-    return call1(raw, JS.undefined(), code);
+    if (JS.strictEqual(code, null) || codeEq(code, cLessThan) || markdownLineEnding(code)) {
+      return JS.call(nok, JS.undefined(), code);
+    }
+    JS.invoke(effects, "consume", code);
+    return if (codeEq(code, cBackslash)) { enclosedEscape } else { enclosed };
+  });
+
+  enclosedEscape = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cLessThan) || codeEq(code, cGreaterThan) || codeEq(code, cBackslash)) {
+      JS.invoke(effects, "consume", code);
+      return enclosed;
+    }
+    return JS.call(enclosed, JS.undefined(), code);
   });
 
   raw = JS.method1((JsValue _s, JsValue code) => {
-    if (balance == 0 && (isEof(code) || codeEq(code, 41) || markdownLineEndingOrSpace(code))) {
-      call1(objGet(effects, "exit"), effects, tChunkString);
-      call1(objGet(effects, "exit"), effects, stringType);
-      call1(objGet(effects, "exit"), effects, rawType);
-      call1(objGet(effects, "exit"), effects, typ);
-      return call1(ok, JS.undefined(), code);
-    }
-    if (balance < limit && codeEq(code, 40)) {
-      call1(objGet(effects, "consume"), effects, code);
-      balance = balance + 1;
+    if (balance == 0.0 && (JS.strictEqual(code, null) || codeEq(code, cRightParenthesis) || markdownLineEndingOrSpace(code))) {
+      JS.invoke(effects, "exit", tChunkString);
+      JS.invoke(effects, "exit", stringType);
+      JS.invoke(effects, "exit", rawType);
+      JS.invoke(effects, "exit", typ);
+      return JS.call(ok, JS.undefined(), code);
+    }
+    if (balance < limit && codeEq(code, cLeftParenthesis)) {
+      JS.invoke(effects, "consume", code);
+      balance++;
       return raw;
     }
-    if (codeEq(code, 41)) {
-      call1(objGet(effects, "consume"), effects, code);
-      balance = balance - 1;
+    if (codeEq(code, cRightParenthesis)) {
+      JS.invoke(effects, "consume", code);
+      balance--;
       return raw;
     }
-    if (isEof(code) || codeEq(code, 32) || codeEq(code, 40) || asciiControl(code)) {
-      return call1(nok, JS.undefined(), code);
+    if (JS.strictEqual(code, null) || codeEq(code, cSpace) || codeEq(code, cLeftParenthesis) || asciiControl(code)) {
+      return JS.call(nok, JS.undefined(), code);
     }
-    call1(objGet(effects, "consume"), effects, code);
-    if (codeEq(code, 92)) {
-      return rawEscape;
-    }
-    return raw;
+    JS.invoke(effects, "consume", code);
+    return if (codeEq(code, cBackslash)) { rawEscape } else { raw };
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, 60)) {
-      call1(objGet(effects, "enter"), effects, typ);
-      call1(objGet(effects, "enter"), effects, literalType);
-      call1(objGet(effects, "enter"), effects, literalMarkerType);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, literalMarkerType);
-      return enclosedBefore;
-    }
-    if (isEof(code) || codeEq(code, 32) || codeEq(code, 41) || asciiControl(code)) {
-      return call1(nok, JS.undefined(), code);
+  rawEscape = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cLeftParenthesis) || codeEq(code, cRightParenthesis) || codeEq(code, cBackslash)) {
+      JS.invoke(effects, "consume", code);
+      return raw;
     }
-    call1(objGet(effects, "enter"), effects, typ);
-    call1(objGet(effects, "enter"), effects, rawType);
-    call1(objGet(effects, "enter"), effects, stringType);
-    call2(objGet(effects, "enter"), effects, tChunkString, chunkStringFields());
-    return call1(raw, JS.undefined(), code);
+    return JS.call(raw, JS.undefined(), code);
   });
 
   return start;
diff -ruN a/src/graph/remark-parse/micromark/factory-label.lil b/src/graph/remark-parse/micromark/factory-label.lil
--- a/src/graph/remark-parse/micromark/factory-label.lil	2026-09-23 20:40:45.192660967 +0000
+++ b/src/graph/remark-parse/micromark/factory-label.lil	2026-09-23 20:40:45.298728642 +0000
@@ -1,96 +1,81 @@
-import { call1, call2, hasOwn, objGet, undef } from "./host.lil";
 import { markdownLineEnding, markdownSpace } from "./util-character.lil";
 import { cBackslash, cCaret, cLeftSquareBracket, cRightSquareBracket, codeEq } from "./symbol/codes.lil";
-import { isEof } from "./host.lil";
 import { kContentTypeString, kLinkReferenceSizeMax } from "./symbol/constants.lil";
 import { tChunkString, tLineEnding } from "./symbol/types.lil";
 
-export JsValue factoryLabel = JS.methodRest((JsValue self, JsValue args) => {
-  JsValue effects = args[0];
-  JsValue ok = args[1];
-  JsValue nok = args[2];
-  string typ = JS.string(args[3]);
-  string markerType = JS.string(args[4]);
-  string stringType = JS.string(args[5]);
-  int size = 0;
+// Upstream calls this with the tokenize context as `this`; here it is the first
+// parameter.
+export JsValue factoryLabel(JsValue self, JsValue effects, JsValue ok, JsValue nok, string typ, string markerType, string stringType) {
+  float size = 0.0;
   bool seen = false;
-  JsValue labelInside = undef();
-  JsValue atBreak = undef();
+  JsValue atBreak = JS.undefined();
+  JsValue labelInside = JS.undefined();
+  JsValue labelEscape = JS.undefined();
 
-  JsValue labelEscape = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cLeftSquareBracket) || codeEq(code, cBackslash) || codeEq(code, cRightSquareBracket)) {
-      call1(objGet(effects, "consume"), effects, code);
-      size = size + 1;
-      return labelInside;
-    }
-    return call1(labelInside, JS.undefined(), code);
-  });
-
-  labelInside = JS.method1((JsValue _s, JsValue code) => {
-    if (
-      isEof(code) ||
-      codeEq(code, cLeftSquareBracket) ||
-      codeEq(code, cRightSquareBracket) ||
-      markdownLineEnding(code)
-    ) {
-      call1(objGet(effects, "exit"), effects, tChunkString);
-      return call1(atBreak, JS.undefined(), code);
-    }
-    int previousSize = size;
-    size = size + 1;
-    if (previousSize > kLinkReferenceSizeMax) {
-      call1(objGet(effects, "exit"), effects, tChunkString);
-      return call1(atBreak, JS.undefined(), code);
-    }
-    call1(objGet(effects, "consume"), effects, code);
-    if (!seen) {
-      seen = !markdownSpace(code);
-    }
-    if (codeEq(code, cBackslash)) {
-      return labelEscape;
-    }
-    return labelInside;
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    JS.invoke(effects, "enter", typ);
+    JS.invoke(effects, "enter", markerType);
+    JS.invoke(effects, "consume", code);
+    JS.invoke(effects, "exit", markerType);
+    JS.invoke(effects, "enter", stringType);
+    return atBreak;
   });
 
   atBreak = JS.method1((JsValue _s, JsValue code) => {
-    bool hiddenFootnote = codeEq(code, cCaret) && size == 0 && hasOwn(objGet(objGet(self, "parser"), "constructs"), "_hiddenFootnoteSupport");
     if (
       size > kLinkReferenceSizeMax ||
-      isEof(code) ||
+      JS.strictEqual(code, null) ||
       codeEq(code, cLeftSquareBracket) ||
       (codeEq(code, cRightSquareBracket) && !seen) ||
-      hiddenFootnote
+      (codeEq(code, cCaret) && size == 0.0 && JS.in("_hiddenFootnoteSupport", JS.get(JS.get(self, "parser"), "constructs")))
     ) {
-      return call1(nok, JS.undefined(), code);
+      return JS.call(nok, JS.undefined(), code);
     }
     if (codeEq(code, cRightSquareBracket)) {
-      call1(objGet(effects, "exit"), effects, stringType);
-      call1(objGet(effects, "enter"), effects, markerType);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, markerType);
-      call1(objGet(effects, "exit"), effects, typ);
+      JS.invoke(effects, "exit", stringType);
+      JS.invoke(effects, "enter", markerType);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", markerType);
+      JS.invoke(effects, "exit", typ);
       return ok;
     }
     if (markdownLineEnding(code)) {
-      call1(objGet(effects, "enter"), effects, tLineEnding);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tLineEnding);
+      JS.invoke(effects, "enter", tLineEnding);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tLineEnding);
       return atBreak;
     }
-    JsValue fields = JS.object();
-    JS.set(fields, "contentType", kContentTypeString);
-    call2(objGet(effects, "enter"), effects, tChunkString, fields);
-    return call1(labelInside, JS.undefined(), code);
+    JS.invoke(effects, "enter", tChunkString, JS.object("contentType", kContentTypeString));
+    return JS.call(labelInside, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, typ);
-    call1(objGet(effects, "enter"), effects, markerType);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, markerType);
-    call1(objGet(effects, "enter"), effects, stringType);
-    return atBreak;
+  labelInside = JS.method1((JsValue _s, JsValue code) => {
+    if (
+      JS.strictEqual(code, null) ||
+      codeEq(code, cLeftSquareBracket) ||
+      codeEq(code, cRightSquareBracket) ||
+      markdownLineEnding(code) ||
+      size > kLinkReferenceSizeMax
+    ) {
+      JS.invoke(effects, "exit", tChunkString);
+      return JS.call(atBreak, JS.undefined(), code);
+    }
+    size++;
+    JS.invoke(effects, "consume", code);
+    if (!seen) {
+      seen = !markdownSpace(code);
+    }
+    return if (codeEq(code, cBackslash)) { labelEscape } else { labelInside };
+  });
+
+  labelEscape = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cLeftSquareBracket) || codeEq(code, cBackslash) || codeEq(code, cRightSquareBracket)) {
+      JS.invoke(effects, "consume", code);
+      size++;
+      return labelInside;
+    }
+    return JS.call(labelInside, JS.undefined(), code);
   });
 
   return start;
-});
+}
diff -ruN a/src/graph/remark-parse/micromark/factory-space.lil b/src/graph/remark-parse/micromark/factory-space.lil
--- a/src/graph/remark-parse/micromark/factory-space.lil	2026-09-23 20:40:45.192948887 +0000
+++ b/src/graph/remark-parse/micromark/factory-space.lil	2026-09-23 20:40:45.298995472 +0000
@@ -1,31 +1,27 @@
-import { call1, objGet, undef } from "./host.lil";
 import { markdownSpace } from "./util-character.lil";
 
-export JsValue factorySpace(JsValue effects, JsValue ok, string typ, float max) {
-  float limit = 1.0 / 0.0;
-  if (max != 0.0) {
-    limit = max - 1.0;
-  }
+// `max` is optional, as upstream's: pass `JS.undefined()` (elided at the call
+// site) or a falsy value for no limit.
+export JsValue factorySpace(JsValue effects, JsValue ok, string typ, JsValue max) {
+  float limit = if (max.truthy()) { JS.number(max) - 1.0 } else { 1.0 / 0.0 };
   float size = 0.0;
-  JsValue prefix = undef();
+  JsValue prefix = JS.undefined();
 
-  prefix = JS.method1((JsValue _self, JsValue code) => {
+  prefix = JS.method1((JsValue _s, JsValue code) => {
     if (markdownSpace(code) && size < limit) {
-      size = size + 1.0;
-      call1(objGet(effects, "consume"), effects, code);
+      size++;
+      JS.invoke(effects, "consume", code);
       return prefix;
     }
-    call1(objGet(effects, "exit"), effects, typ);
-    return call1(ok, JS.undefined(), code);
+    JS.invoke(effects, "exit", typ);
+    return JS.call(ok, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _self, JsValue code) => {
+  return JS.method1((JsValue _s, JsValue code) => {
     if (markdownSpace(code)) {
-      call1(objGet(effects, "enter"), effects, typ);
-      return call1(prefix, JS.undefined(), code);
+      JS.invoke(effects, "enter", typ);
+      return JS.call(prefix, JS.undefined(), code);
     }
-    return call1(ok, JS.undefined(), code);
+    return JS.call(ok, JS.undefined(), code);
   });
-
-  return start;
 }
diff -ruN a/src/graph/remark-parse/micromark/factory-title.lil b/src/graph/remark-parse/micromark/factory-title.lil
--- a/src/graph/remark-parse/micromark/factory-title.lil	2026-09-23 20:40:45.193230334 +0000
+++ b/src/graph/remark-parse/micromark/factory-title.lil	2026-09-23 20:40:45.306382724 +0000
@@ -1,83 +1,73 @@
-import { call1, call2, objGet, undef } from "./host.lil";
 import { factorySpace } from "./factory-space.lil";
 import { markdownLineEnding } from "./util-character.lil";
 import { cApostrophe, cBackslash, cLeftParenthesis, cQuotationMark, cRightParenthesis, codeEq } from "./symbol/codes.lil";
-import { isEof } from "./host.lil";
 import { kContentTypeString } from "./symbol/constants.lil";
 import { tChunkString, tLineEnding, tLinePrefix } from "./symbol/types.lil";
 
 export JsValue factoryTitle(JsValue effects, JsValue ok, JsValue nok, string typ, string markerType, string stringType) {
-  int marker = 0;
-  JsValue inside = undef();
-  JsValue atBreak = undef();
-  JsValue begin = undef();
-
-  JsValue escape = JS.method1((JsValue _s, JsValue code) => {
-    if (code == marker || codeEq(code, cBackslash)) {
-      call1(objGet(effects, "consume"), effects, code);
-      return inside;
+  JsValue marker = JS.undefined();
+  JsValue begin = JS.undefined();
+  JsValue atBreak = JS.undefined();
+  JsValue inside = JS.undefined();
+  JsValue escape = JS.undefined();
+
+  JsValue start = JS.method1((JsValue _s, JsValue code) => {
+    if (codeEq(code, cQuotationMark) || codeEq(code, cApostrophe) || codeEq(code, cLeftParenthesis)) {
+      JS.invoke(effects, "enter", typ);
+      JS.invoke(effects, "enter", markerType);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", markerType);
+      marker = if (codeEq(code, cLeftParenthesis)) { cRightParenthesis } else { code };
+      return begin;
     }
-    return call1(inside, JS.undefined(), code);
+    return JS.call(nok, JS.undefined(), code);
   });
 
-  inside = JS.method1((JsValue _s, JsValue code) => {
-    if (code == marker || isEof(code) || markdownLineEnding(code)) {
-      call1(objGet(effects, "exit"), effects, tChunkString);
-      return call1(atBreak, JS.undefined(), code);
-    }
-    call1(objGet(effects, "consume"), effects, code);
-    if (codeEq(code, cBackslash)) {
-      return escape;
+  begin = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, marker)) {
+      JS.invoke(effects, "enter", markerType);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", markerType);
+      JS.invoke(effects, "exit", typ);
+      return ok;
     }
-    return inside;
+    JS.invoke(effects, "enter", stringType);
+    return JS.call(atBreak, JS.undefined(), code);
   });
 
   atBreak = JS.method1((JsValue _s, JsValue code) => {
-    if (code == marker) {
-      call1(objGet(effects, "exit"), effects, stringType);
-      return call1(begin, JS.undefined(), marker);
+    if (JS.strictEqual(code, marker)) {
+      JS.invoke(effects, "exit", stringType);
+      return JS.call(begin, JS.undefined(), marker);
     }
-    if (isEof(code)) {
-      return call1(nok, JS.undefined(), code);
+    if (JS.strictEqual(code, null)) {
+      return JS.call(nok, JS.undefined(), code);
     }
     if (markdownLineEnding(code)) {
-      call1(objGet(effects, "enter"), effects, tLineEnding);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tLineEnding);
-      return factorySpace(effects, atBreak, tLinePrefix, 0);
-    }
-    JsValue fields = JS.object();
-    JS.set(fields, "contentType", kContentTypeString);
-    call2(objGet(effects, "enter"), effects, tChunkString, fields);
-    return call1(inside, JS.undefined(), code);
+      JS.invoke(effects, "enter", tLineEnding);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tLineEnding);
+      return factorySpace(effects, atBreak, tLinePrefix, JS.undefined());
+    }
+    JS.invoke(effects, "enter", tChunkString, JS.object("contentType", kContentTypeString));
+    return JS.call(inside, JS.undefined(), code);
   });
 
-  begin = JS.method1((JsValue _s, JsValue code) => {
-    if (code == marker) {
-      call1(objGet(effects, "enter"), effects, markerType);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, markerType);
-      call1(objGet(effects, "exit"), effects, typ);
-      return ok;
+  inside = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, marker) || JS.strictEqual(code, null) || markdownLineEnding(code)) {
+      JS.invoke(effects, "exit", tChunkString);
+      return JS.call(atBreak, JS.undefined(), code);
     }
-    call1(objGet(effects, "enter"), effects, stringType);
-    return call1(atBreak, JS.undefined(), code);
+    JS.invoke(effects, "consume", code);
+    return if (codeEq(code, cBackslash)) { escape } else { inside };
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    if (codeEq(code, cQuotationMark) || codeEq(code, cApostrophe) || codeEq(code, cLeftParenthesis)) {
-      call1(objGet(effects, "enter"), effects, typ);
-      call1(objGet(effects, "enter"), effects, markerType);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, markerType);
-      if (codeEq(code, cLeftParenthesis)) {
-        marker = cRightParenthesis;
-      } else {
-        marker = JS.number(code).toInt();
-      }
-      return begin;
+  escape = JS.method1((JsValue _s, JsValue code) => {
+    if (JS.strictEqual(code, marker) || codeEq(code, cBackslash)) {
+      JS.invoke(effects, "consume", code);
+      return inside;
     }
-    return call1(nok, JS.undefined(), code);
+    return JS.call(inside, JS.undefined(), code);
   });
 
   return start;
diff -ruN a/src/graph/remark-parse/micromark/factory-whitespace.lil b/src/graph/remark-parse/micromark/factory-whitespace.lil
--- a/src/graph/remark-parse/micromark/factory-whitespace.lil	2026-09-23 20:40:45.193502394 +0000
+++ b/src/graph/remark-parse/micromark/factory-whitespace.lil	2026-09-23 20:40:45.306728211 +0000
@@ -1,28 +1,23 @@
-import { call1, objGet, undef } from "./host.lil";
 import { factorySpace } from "./factory-space.lil";
 import { markdownLineEnding, markdownSpace } from "./util-character.lil";
 import { tLineEnding, tLinePrefix, tLineSuffix } from "./symbol/types.lil";
 
 export JsValue factoryWhitespace(JsValue effects, JsValue ok) {
   bool seen = false;
-  JsValue start = undef();
+  JsValue start = JS.undefined();
 
-  start = JS.method1((JsValue _self, JsValue code) => {
+  start = JS.method1((JsValue _s, JsValue code) => {
     if (markdownLineEnding(code)) {
-      call1(objGet(effects, "enter"), effects, tLineEnding);
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tLineEnding);
+      JS.invoke(effects, "enter", tLineEnding);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", tLineEnding);
       seen = true;
       return start;
     }
     if (markdownSpace(code)) {
-      string typ = tLineSuffix;
-      if (seen) {
-        typ = tLinePrefix;
-      }
-      return call1(factorySpace(effects, start, typ, 0), JS.undefined(), code);
+      return JS.call(factorySpace(effects, start, if (seen) { tLinePrefix } else { tLineSuffix }, JS.undefined()), JS.undefined(), code);
     }
-    return call1(ok, JS.undefined(), code);
+    return JS.call(ok, JS.undefined(), code);
   });
 
   return start;
diff -ruN a/src/graph/remark-parse/micromark/host.lil b/src/graph/remark-parse/micromark/host.lil
--- a/src/graph/remark-parse/micromark/host.lil	2026-09-23 20:40:45.193780385 +0000
+++ b/src/graph/remark-parse/micromark/host.lil	2026-09-23 20:40:45.307073147 +0000
@@ -7,6 +7,21 @@
 
 export JsValue codeEof = null;
 
+// Typed views of the tokenizer's plain point and token objects: field reads and writes stay direct member
+// accesses with no conversion.
+export extern class TokenizerPoint {
+  float _bufferIndex;
+  float _index;
+  float line;
+  float column;
+  float offset;
+}
+
+export extern class TokenizerToken {
+  TokenizerPoint start;
+  TokenizerPoint end;
+}
+
 export JsValue undef() {
   return JS.undefined();
 }
@@ -43,17 +58,12 @@
   return !isNullish(value) && JS.typeOf(value) == "object";
 }
 
+// String() already gives "undefined" and "null" for those two values.
 export string jsString(JsValue value) {
   if (value is string) {
     return value;
   }
-  if (JS.isUndefined(value)) {
-    return "undefined";
-  }
-  if (JS.isNullish(value)) {
-    return "null";
-  }
-  return JS.string(JS.call(String, undef(), value));
+  return JS.assume(JS.call(String, JS.undefined(), value));
 }
 
 export float jsNum(JsValue value) {
@@ -109,11 +119,13 @@
   JS.set(obj, JS.string(key), value);
 }
 
+JsValue hasOwnProperty = JS.get(JS.object(), "hasOwnProperty");
+
 export bool hasOwn(JsValue obj, JsValue key) {
   if (isNullish(obj)) {
     return false;
   }
-  return JS.call(JS.get(JS.get(Object, "prototype"), "hasOwnProperty"), obj, key).truthy();
+  return JS.call(hasOwnProperty, obj, key).truthy();
 }
 
 export bool hasOwnStr(JsValue obj, string key) {
@@ -219,15 +231,15 @@
 }
 
 export string fromCharCode1(JsValue code) {
-  return jsString(JS.call(JS.get(String, "fromCharCode"), String, code));
+  return JS.assume(JS.invoke(String, "fromCharCode", code));
 }
 
 export string fromCharCode2(JsValue a, JsValue b) {
-  return jsString(JS.call(JS.get(String, "fromCharCode"), String, a, b));
+  return JS.assume(JS.invoke(String, "fromCharCode", a, b));
 }
 
 export string fromCodePoint(JsValue code) {
-  return jsString(JS.call(JS.get(String, "fromCodePoint"), String, code));
+  return JS.assume(JS.invoke(String, "fromCodePoint", code));
 }
 
 export float parseIntBase(string value, int base) {
@@ -250,6 +262,11 @@
   return JS.number(JS.call(JS.get(Math, "trunc"), Math, value));
 }
 
+// A construct list: an array as is, one construct as a list of one, nothing as an empty list.
+export JsValue asList(JsValue value) {
+  return if (value.isArray()) { value } else { if (value.truthy()) { JS.array(value) } else { JS.array() } };
+}
+
 export JsValue copyPoint(JsValue point) {
   return JS.object(
     "_bufferIndex", JS.get(point, "_bufferIndex"),
@@ -317,8 +334,7 @@
   if (isNullish(enc)) {
     enc = undef();
   }
-  JsValue decoder = JS.construct(TextDecoder, enc);
-  return jsString(JS.call(JS.get(decoder, "decode"), decoder, value));
+  return JS.assume(JS.invoke(JS.construct(TextDecoder, enc), "decode", value));
 }
 
 export void fail(string message) {
@@ -346,7 +362,7 @@
 }
 
 export string encodeUriComp(string value) {
-  return jsString(JS.call(encodeURIComponent, undef(), value));
+  return JS.assume(JS.call(encodeURIComponent, JS.undefined(), value));
 }
 
 export JsValue regexExec(Regex pattern, string value) {
diff -ruN a/src/graph/remark-parse/micromark/initialize/content.lil b/src/graph/remark-parse/micromark/initialize/content.lil
--- a/src/graph/remark-parse/micromark/initialize/content.lil	2026-09-23 20:40:45.194220389 +0000
+++ b/src/graph/remark-parse/micromark/initialize/content.lil	2026-09-23 20:40:45.307208661 +0000
@@ -1,61 +1,55 @@
-import { call1, call2, call3, makeConstruct, objGet } from "../host.lil";
+import { isEof } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { markdownLineEnding } from "../util-character.lil";
-import { isEof } from "../host.lil";
-import { kContentTypeText } from "../symbol/constants.lil";
-import { tChunkText, tLineEnding, tLinePrefix, tParagraph } from "../symbol/types.lil";
 
-JsValue initializeContent = JS.method1((JsValue self, JsValue effects) => {
+export JsValue contentConstruct = JS.object("tokenize", JS.method1((JsValue self, JsValue effects) => {
   JsValue previous = JS.undefined();
-  JsValue lineStart = JS.undefined();
   JsValue contentStart = JS.undefined();
+  JsValue lineStart = JS.undefined();
 
   JsValue data = JS.method1((JsValue _s, JsValue code) => {
     if (isEof(code)) {
-      call1(objGet(effects, "exit"), effects, tChunkText);
-      call1(objGet(effects, "exit"), effects, tParagraph);
-      call1(objGet(effects, "consume"), effects, code);
+      JS.invoke(effects, "exit", "chunkText");
+      JS.invoke(effects, "exit", "paragraph");
+      JS.invoke(effects, "consume", code);
       return JS.undefined();
     }
     if (markdownLineEnding(code)) {
-      call1(objGet(effects, "consume"), effects, code);
-      call1(objGet(effects, "exit"), effects, tChunkText);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", "chunkText");
       return lineStart;
     }
-    call1(objGet(effects, "consume"), effects, code);
+    JS.invoke(effects, "consume", code);
     return data;
   });
 
   lineStart = JS.method1((JsValue _s, JsValue code) => {
-    JsValue fields = JS.object();
-    JS.set(fields, "contentType", kContentTypeText);
-    JS.set(fields, "previous", previous);
-    JsValue token = call2(objGet(effects, "enter"), effects, tChunkText, fields);
+    JsValue token = JS.invoke(effects, "enter", "chunkText", JS.object("contentType", "text", "previous", previous));
     if (previous.truthy()) {
       JS.set(previous, "next", token);
     }
     previous = token;
-    return call1(data, JS.undefined(), code);
-  });
-
-  JsValue paragraphInitial = JS.method1((JsValue _s, JsValue code) => {
-    call1(objGet(effects, "enter"), effects, tParagraph);
-    return call1(lineStart, JS.undefined(), code);
+    return JS.call(data, JS.undefined(), code);
   });
 
-  JsValue afterContentStartConstruct = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code)) {
-      call1(objGet(effects, "consume"), effects, code);
-      return JS.undefined();
-    }
-    call1(objGet(effects, "enter"), effects, tLineEnding);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tLineEnding);
-    return factorySpace(effects, contentStart, tLinePrefix, 0);
-  });
-
-  contentStart = call3(objGet(effects, "attempt"), effects, objGet(objGet(self, "parser"), "constructs")["contentInitial"], afterContentStartConstruct, paragraphInitial);
+  contentStart = JS.invoke(
+    effects,
+    "attempt",
+    JS.get(JS.get(JS.get(self, "parser"), "constructs"), "contentInitial"),
+    JS.method1((JsValue _s, JsValue code) => {
+      if (isEof(code)) {
+        JS.invoke(effects, "consume", code);
+        return JS.undefined();
+      }
+      JS.invoke(effects, "enter", "lineEnding");
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", "lineEnding");
+      return factorySpace(effects, contentStart, "linePrefix", 0.0);
+    }),
+    JS.method1((JsValue _s, JsValue code) => {
+      JS.invoke(effects, "enter", "paragraph");
+      return JS.call(lineStart, JS.undefined(), code);
+    })
+  );
   return contentStart;
-});
-
-export JsValue contentConstruct = makeConstruct(initializeContent);
+}));
diff -ruN a/src/graph/remark-parse/micromark/initialize/document.lil b/src/graph/remark-parse/micromark/initialize/document.lil
--- a/src/graph/remark-parse/micromark/initialize/document.lil	2026-09-23 20:40:45.194735935 +0000
+++ b/src/graph/remark-parse/micromark/initialize/document.lil	2026-09-23 20:40:45.307756808 +0000
@@ -1,277 +1,216 @@
-import {
-  arrLen,
-  call0,
-  call1,
-  call2,
-  call3,
-  codeEof,
-  copyPoint,
-  includesStr,
-  isEof,
-  isFn,
-  makeConstruct,
-  objGet,
-  objGetVal,
-  objSetVal,
-  push,
-  slice1,
-  undef
-} from "../host.lil";
+import { copyPoint, isEof } from "../host.lil";
 import { factorySpace } from "../factory-space.lil";
 import { markdownLineEnding } from "../util-character.lil";
 import { splice } from "../util-chunked.lil";
-import { kContentTypeFlow, kTabSize } from "../symbol/constants.lil";
-import { tChunkFlow, tLinePrefix } from "../symbol/types.lil";
 
-JsValue tokenizeContainer = JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
-  JsValue disable = objGet(objGet(objGet(self, "parser"), "constructs"), "disable")["null"];
-  int max = 0;
-  if (!includesStr(disable, "codeIndented")) {
-    max = kTabSize;
-  }
+JsValue containerConstruct = JS.object("tokenize", JS.method3((JsValue self, JsValue effects, JsValue ok, JsValue nok) => {
+  JsValue constructs = JS.get(JS.get(self, "parser"), "constructs");
   return factorySpace(
     effects,
-    call3(objGet(effects, "attempt"), effects, objGet(objGet(self, "parser"), "constructs")["document"], ok, nok),
-    tLinePrefix,
-    max
+    JS.invoke(effects, "attempt", JS.get(constructs, "document"), ok, nok),
+    "linePrefix",
+    if (JS.invoke(JS.get(JS.get(constructs, "disable"), "null"), "includes", "codeIndented").truthy()) { 0.0 } else { 4.0 }
   );
-});
-
-JsValue containerConstruct = JS.object();
-JS.set(containerConstruct, "tokenize", tokenizeContainer);
-
-struct ContainerEntry {
-  JsValue construct;
-  JsValue state;
-}
-
-class DocumentState {
-  ContainerEntry[] stack;
-  int continued;
-  JsValue effects;
-  JsValue childFlow;
-  JsValue childToken;
-  float lineStartOffset;
-  JsValue start;
-  JsValue documentContinued;
+}));
 
-  init(JsValue nextEffects) {
-    this.effects = nextEffects;
-  }
-}
-
-void exitContainers(JsValue self, JsValue effects, ContainerEntry[] stack, int size) {
-  int index = stack.length;
-  while (index > size) {
-    index = index - 1;
-    ContainerEntry entry = stack[index];
-    JS.set(self, "containerState", entry.state);
-    call1(objGet(entry.construct, "exit"), self, effects);
-  }
-  while (stack.length > size) {
-    stack.pop();
-  }
-}
-
-void closeFlow(JsValue self, DocumentState st) {
-  JsValue childFlow = st.childFlow;
-  if (childFlow.truthy()) {
-    JsValue[] eofSlice = [];
-    push(eofSlice, codeEof);
-    call1(objGet(childFlow, "write"), childFlow, eofSlice);
-  }
-  st.childToken = undef();
-  st.childFlow = undef();
-  JS.set(objGet(self, "containerState"), "_closeFlow", undef());
-}
-
-void writeToChild(JsValue self, DocumentState st, JsValue token, bool endOfFile) {
-  JsValue childFlow = st.childFlow;
-  JsValue stream = call1(objGet(self, "sliceStream"), self, token);
-  if (endOfFile) {
-    push(stream, codeEof);
-  }
-  JS.set(token, "previous", st.childToken);
-  if (st.childToken.truthy()) {
-    JS.set(st.childToken, "next", token);
-  }
-  st.childToken = token;
-  call1(objGet(childFlow, "defineSkip"), childFlow, objGet(token, "start"));
-  call1(objGet(childFlow, "write"), childFlow, stream);
-  if (objGet(objGet(self, "parser"), "lazy")[objGet(objGet(token, "start"), "line")].truthy()) {
-    int index = arrLen(objGet(childFlow, "events"));
-    float lineStartOffset = st.lineStartOffset;
-    while (true) {
-      index = index - 1;
-      if (index < 0) {
-        break;
-      }
-      JsValue evTok = objGet(childFlow, "events")[index][1];
-      if (JS.number(objGet(objGet(evTok, "start"), "offset")) < lineStartOffset && (!objGet(evTok, "end").truthy() || JS.number(objGet(objGet(evTok, "end"), "offset")) > lineStartOffset)) {
-        return;
-      }
-    }
-    int indexBeforeExits = arrLen(objGet(self, "events"));
-    int indexBeforeFlow = indexBeforeExits;
-    bool seen = false;
-    JsValue point = undef();
-    while (true) {
-      indexBeforeFlow = indexBeforeFlow - 1;
-      if (indexBeforeFlow < 0) {
-        break;
+// After a container closes (or a lazy line turns out not to continue it), move the exits of the closed
+// containers to just after the last flow chunk, ending them where that chunk ended.
+void moveExits(JsValue events, float indexBeforeExits, float indexBeforeFlow, JsValue point) {
+  float index = indexBeforeExits;
+  while (index < events.length) {
+    JS.set(events[index][1], "end", copyPoint(point));
+    index++;
+  }
+  splice(events, indexBeforeFlow + 1.0, 0, JS.invoke(events, "slice", indexBeforeExits));
+  JS.set(events, "length", index);
+}
+
+export JsValue documentConstruct = JS.object("tokenize", JS.method1((JsValue self, JsValue effects) => {
+  JsValue stack = JS.array();
+  float continued = 0.0;
+  JsValue childFlow = JS.undefined();
+  JsValue childToken = JS.undefined();
+  JsValue lineStartOffset = JS.undefined();
+  JsValue start = JS.undefined();
+  JsValue checkNewContainers = JS.undefined();
+  JsValue documentContinued = JS.undefined();
+  JsValue flowStart = JS.undefined();
+
+  func(float)->void exitContainers = (float size) => {
+    float index = stack.length;
+    while (index > size) {
+      index--;
+      JsValue entry = stack[index];
+      JS.set(self, "containerState", entry[1]);
+      JS.call(JS.get(entry[0], "exit"), self, effects);
+    }
+    JS.set(stack, "length", size);
+  };
+
+  func()->void closeFlow = () => {
+    JS.invoke(childFlow, "write", JS.array(null));
+    childToken = JS.undefined();
+    childFlow = JS.undefined();
+    JS.set(JS.get(self, "containerState"), "_closeFlow", JS.undefined());
+  };
+
+  func(JsValue, bool)->void writeToChild = (JsValue token, bool endOfFile) => {
+    JsValue stream = JS.invoke(self, "sliceStream", token);
+    if (endOfFile) {
+      JS.invoke(stream, "push", null);
+    }
+    JS.set(token, "previous", childToken);
+    if (childToken.truthy()) {
+      JS.set(childToken, "next", token);
+    }
+    childToken = token;
+    JS.invoke(childFlow, "defineSkip", JS.get(token, "start"));
+    JS.invoke(childFlow, "write", stream);
+    if (JS.get(JS.get(self, "parser"), "lazy")[JS.get(JS.get(token, "start"), "line")].truthy()) {
+      JsValue childEvents = JS.get(childFlow, "events");
+      float index = childEvents.length;
+      while (index > 0.0) {
+        index--;
+        JsValue child = childEvents[index][1];
+        JsValue end = JS.get(child, "end");
+        if (JS.lessThan(JS.get(JS.get(child, "start"), "offset"), lineStartOffset) && (!end.truthy() || JS.greaterThan(JS.get(end, "offset"), lineStartOffset))) {
+          return;
+        }
       }
-      JsValue ev = objGet(self, "events")[indexBeforeFlow];
-      if (ev[0] == "exit" && objGet(ev[1], "type") == tChunkFlow) {
-        if (seen) {
-          point = objGet(ev[1], "end");
+      JsValue events = JS.get(self, "events");
+      float indexBeforeExits = events.length;
+      float indexBeforeFlow = indexBeforeExits;
+      bool seen = false;
+      JsValue point = JS.undefined();
+      while (true) {
+        indexBeforeFlow--;
+        if (indexBeforeFlow < 0.0) {
           break;
         }
-        seen = true;
+        JsValue event = events[indexBeforeFlow];
+        if (JS.strictEqual(event[0], "exit") && JS.strictEqual(JS.get(event[1], "type"), "chunkFlow")) {
+          if (seen) {
+            point = JS.get(event[1], "end");
+            break;
+          }
+          seen = true;
+        }
       }
+      exitContainers(continued);
+      moveExits(JS.get(self, "events"), indexBeforeExits, indexBeforeFlow, point);
     }
-    exitContainers(self, st.effects, st.stack, st.continued);
-    int fix = indexBeforeExits;
-    while (fix < arrLen(objGet(self, "events"))) {
-      JS.set(objGet(self, "events")[fix][1], "end", copyPoint(point));
-      fix = fix + 1;
-    }
-    splice(objGet(self, "events"), indexBeforeFlow + 1, 0, slice1(objGet(self, "events"), indexBeforeExits));
-    JS.set(objGet(self, "events"), "length", fix);
-  }
-}
-
-JsValue createFlowTokenizer(JsValue self) {
-  JsValue parser = objGet(self, "parser");
-  return call1(objGet(parser, "flow"), parser, call0(objGet(self, "now"), self));
-}
-
-JsValue initializeDocument = JS.method1((JsValue self, JsValue effects) => {
-  DocumentState st = new DocumentState(effects);
+  };
 
   JsValue flowContinue = JS.method1((JsValue _s, JsValue code) => {
     if (isEof(code)) {
-      writeToChild(self, st, call1(objGet(effects, "exit"), effects, tChunkFlow), true);
-      exitContainers(self, effects, st.stack, 0);
-      call1(objGet(effects, "consume"), effects, code);
-      return undef();
+      writeToChild(JS.invoke(effects, "exit", "chunkFlow"), true);
+      exitContainers(0.0);
+      JS.invoke(effects, "consume", code);
+      return JS.undefined();
     }
     if (markdownLineEnding(code)) {
-      call1(objGet(effects, "consume"), effects, code);
-      writeToChild(self, st, call1(objGet(effects, "exit"), effects, tChunkFlow), false);
-      st.continued = 0;
-      JS.set(self, "interrupt", undef());
-      return st.start;
+      JS.invoke(effects, "consume", code);
+      writeToChild(JS.invoke(effects, "exit", "chunkFlow"), false);
+      continued = 0.0;
+      JS.set(self, "interrupt", JS.undefined());
+      return start;
     }
-    call1(objGet(effects, "consume"), effects, code);
+    JS.invoke(effects, "consume", code);
     return flowContinue;
   });
 
-  JsValue flowStart = JS.method1((JsValue _s, JsValue code) => {
+  flowStart = JS.method1((JsValue _s, JsValue code) => {
     if (isEof(code)) {
-      if (st.childFlow.truthy()) {
-        closeFlow(self, st);
+      if (childFlow.truthy()) {
+        closeFlow();
       }
-      exitContainers(self, effects, st.stack, 0);
-      call1(objGet(effects, "consume"), effects, code);
-      return undef();
-    }
-    if (!st.childFlow.truthy()) {
-      st.childFlow = createFlowTokenizer(self);
-    }
-    JsValue fields = JS.object();
-    JS.set(fields, "_tokenizer", st.childFlow);
-    JS.set(fields, "contentType", kContentTypeFlow);
-    JS.set(fields, "previous", st.childToken);
-    call2(objGet(effects, "enter"), effects, tChunkFlow, fields);
-    return call1(flowContinue, JS.undefined(), code);
+      exitContainers(0.0);
+      JS.invoke(effects, "consume", code);
+      return JS.undefined();
+    }
+    childFlow = JS.or(childFlow, JS.invoke(JS.get(self, "parser"), "flow", JS.invoke(self, "now")));
+    JS.invoke(effects, "enter", "chunkFlow", JS.object("_tokenizer", childFlow, "contentType", "flow", "previous", childToken));
+    return JS.call(flowContinue, JS.undefined(), code);
   });
 
   JsValue containerContinue = JS.method1((JsValue _s, JsValue code) => {
-    st.continued = st.continued + 1;
-    st.stack.push(ContainerEntry{objGet(self, "currentConstruct"), objGet(self, "containerState")});
-    return call1(st.documentContinued, JS.undefined(), code);
+    continued++;
+    JS.invoke(stack, "push", JS.array(JS.get(self, "currentConstruct"), JS.get(self, "containerState")));
+    return JS.call(documentContinued, JS.undefined(), code);
   });
 
-  JsValue documentContinued = JS.method1((JsValue _s, JsValue code) => {
+  documentContinued = JS.method1((JsValue _s, JsValue code) => {
     JS.set(self, "containerState", JS.object());
-    return call1(call3(objGet(effects, "attempt"), effects, containerConstruct, containerContinue, flowStart), JS.undefined(), code);
-  });
-
-  JsValue thereIsNoNewContainer = JS.method1((JsValue _s, JsValue code) => {
-    objSetVal(objGet(objGet(self, "parser"), "lazy"), objGet(call0(objGet(self, "now"), self), "line"), st.continued != arrLen(st.stack));
-    st.lineStartOffset = JS.number(objGet(call0(objGet(self, "now"), self), "offset"));
-    return call1(flowStart, JS.undefined(), code);
+    return JS.call(JS.invoke(effects, "attempt", containerConstruct, containerContinue, flowStart), JS.undefined(), code);
   });
 
   JsValue thereIsANewContainer = JS.method1((JsValue _s, JsValue code) => {
-    if (st.childFlow.truthy()) {
-      closeFlow(self, st);
+    if (childFlow.truthy()) {
+      closeFlow();
     }
-    exitContainers(self, effects, st.stack, st.continued);
-    return call1(documentContinued, JS.undefined(), code);
+    exitContainers(continued);
+    return JS.call(documentContinued, JS.undefined(), code);
   });
 
-  JsValue checkNewContainers = JS.method1((JsValue _s, JsValue code) => {
-    if (st.continued == st.stack.length) {
-      if (!st.childFlow.truthy()) {
-        return call1(documentContinued, JS.undefined(), code);
+  JsValue thereIsNoNewContainer = JS.method1((JsValue _s, JsValue code) => {
+    JS.get(JS.get(self, "parser"), "lazy")[JS.get(JS.invoke(self, "now"), "line")] = continued != stack.length;
+    lineStartOffset = JS.get(JS.invoke(self, "now"), "offset");
+    return JS.call(flowStart, JS.undefined(), code);
+  });
+
+  checkNewContainers = JS.method1((JsValue _s, JsValue code) => {
+    if (continued == stack.length) {
+      if (!childFlow.truthy()) {
+        return JS.call(documentContinued, JS.undefined(), code);
       }
-      JsValue current = objGet(st.childFlow, "currentConstruct");
-      if (current.truthy() && objGet(current, "concrete").truthy()) {
-        return call1(flowStart, JS.undefined(), code);
+      JsValue current = JS.get(childFlow, "currentConstruct");
+      if (JS.and(current, JS.get(current, "concrete")).truthy()) {
+        return JS.call(flowStart, JS.undefined(), code);
       }
-      JS.set(self, "interrupt", current.truthy() && !objGet(st.childFlow, "_gfmTableDynamicInterruptHack").truthy());
+      JS.set(self, "interrupt", current.truthy() && !JS.get(childFlow, "_gfmTableDynamicInterruptHack").truthy());
     }
     JS.set(self, "containerState", JS.object());
-    return call1(call3(objGet(effects, "check"), effects, containerConstruct, thereIsANewContainer, thereIsNoNewContainer), JS.undefined(), code);
+    return JS.call(JS.invoke(effects, "check", containerConstruct, thereIsANewContainer, thereIsNoNewContainer), JS.undefined(), code);
   });
 
   JsValue documentContinue = JS.method1((JsValue _s, JsValue code) => {
-    st.continued = st.continued + 1;
-    if (objGet(objGet(self, "containerState"), "_closeFlow").truthy()) {
-      JS.set(objGet(self, "containerState"), "_closeFlow", undef());
-      if (st.childFlow.truthy()) {
-        closeFlow(self, st);
-      }
-      int indexBeforeExits = arrLen(objGet(self, "events"));
-      int indexBeforeFlow = indexBeforeExits;
-      JsValue point = undef();
+    continued++;
+    JsValue containerState = JS.get(self, "containerState");
+    if (JS.get(containerState, "_closeFlow").truthy()) {
+      JS.set(containerState, "_closeFlow", JS.undefined());
+      if (childFlow.truthy()) {
+        closeFlow();
+      }
+      JsValue events = JS.get(self, "events");
+      float indexBeforeExits = events.length;
+      float indexBeforeFlow = indexBeforeExits;
+      JsValue point = JS.undefined();
       while (true) {
-        indexBeforeFlow = indexBeforeFlow - 1;
-        if (indexBeforeFlow < 0) {
+        indexBeforeFlow--;
+        if (indexBeforeFlow < 0.0) {
           break;
         }
-        JsValue ev = objGet(self, "events")[indexBeforeFlow];
-        if (ev[0] == "exit" && objGet(ev[1], "type") == tChunkFlow) {
-          point = objGet(ev[1], "end");
+        JsValue event = events[indexBeforeFlow];
+        if (JS.strictEqual(event[0], "exit") && JS.strictEqual(JS.get(event[1], "type"), "chunkFlow")) {
+          point = JS.get(event[1], "end");
           break;
         }
       }
-      exitContainers(self, effects, st.stack, st.continued);
-      int fix = indexBeforeExits;
-      while (fix < arrLen(objGet(self, "events"))) {
-        JS.set(objGet(self, "events")[fix][1], "end", copyPoint(point));
-        fix = fix + 1;
-      }
-      splice(objGet(self, "events"), indexBeforeFlow + 1, 0, slice1(objGet(self, "events"), indexBeforeExits));
-      JS.set(objGet(self, "events"), "length", fix);
-      return call1(checkNewContainers, JS.undefined(), code);
+      exitContainers(continued);
+      moveExits(JS.get(self, "events"), indexBeforeExits, indexBeforeFlow, point);
+      return JS.call(checkNewContainers, JS.undefined(), code);
     }
-    return call1(st.start, JS.undefined(), code);
+    return JS.call(start, JS.undefined(), code);
   });
 
-  JsValue start = JS.method1((JsValue _s, JsValue code) => {
-    if (st.continued < st.stack.length) {
-      ContainerEntry item = st.stack[st.continued];
-      JS.set(self, "containerState", item.state);
-      return call1(call3(objGet(effects, "attempt"), effects, objGet(item.construct, "continuation"), documentContinue, checkNewContainers), JS.undefined(), code);
+  start = JS.method1((JsValue _s, JsValue code) => {
+    if (continued < stack.length) {
+      JsValue item = stack[continued];
+      JS.set(self, "containerState", item[1]);
+      return JS.call(JS.invoke(effects, "attempt", JS.get(item[0], "continuation"), documentContinue, checkNewContainers), JS.undefined(), code);
     }
-    return call1(checkNewContainers, JS.undefined(), code);
+    return JS.call(checkNewContainers, JS.undefined(), code);
   });
 
-  st.start = start;
-  st.documentContinued = documentContinued;
   return start;
-});
-
-export JsValue documentConstruct = makeConstruct(initializeDocument);
+}));
diff -ruN a/src/graph/remark-parse/micromark/initialize/flow.lil b/src/graph/remark-parse/micromark/initialize/flow.lil
--- a/src/graph/remark-parse/micromark/initialize/flow.lil	2026-09-23 20:40:45.198692030 +0000
+++ b/src/graph/remark-parse/micromark/initialize/flow.lil	2026-09-23 20:40:45.308097937 +0000
@@ -1,50 +1,44 @@
-import { call1, call2, call3, makeConstruct, objGet, undef } from "../host.lil";
-import { factorySpace } from "../factory-space.lil";
-import { markdownLineEnding } from "../util-character.lil";
 import { isEof } from "../host.lil";
-import { tLineEnding, tLineEndingBlank, tLinePrefix } from "../symbol/types.lil";
+import { factorySpace } from "../factory-space.lil";
 import { blankLine } from "../core/blank-line.lil";
 import { content } from "../core/content.lil";
 
-JsValue initializeFlow = JS.method1((JsValue self, JsValue effects) => {
-  JsValue constructs = objGet(self, "parser")["constructs"];
+export JsValue flowConstruct = JS.object("tokenize", JS.method1((JsValue self, JsValue effects) => {
+  JsValue constructs = JS.get(JS.get(self, "parser"), "constructs");
   JsValue initial = JS.undefined();
 
-  JsValue afterConstruct = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code)) {
-      call1(objGet(effects, "consume"), effects, code);
-      return undef();
-    }
-    call1(objGet(effects, "enter"), effects, tLineEnding);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tLineEnding);
-    JS.set(self, "currentConstruct", undef());
-    return initial;
-  });
+  func(string)->JsValue after = (string type) => {
+    return JS.method1((JsValue _s, JsValue code) => {
+      if (isEof(code)) {
+        JS.invoke(effects, "consume", code);
+        return JS.undefined();
+      }
+      JS.invoke(effects, "enter", type);
+      JS.invoke(effects, "consume", code);
+      JS.invoke(effects, "exit", type);
+      JS.set(self, "currentConstruct", JS.undefined());
+      return initial;
+    });
+  };
 
-  JsValue atBlankEnding = JS.method1((JsValue _s, JsValue code) => {
-    if (isEof(code)) {
-      call1(objGet(effects, "consume"), effects, code);
-      return undef();
-    }
-    call1(objGet(effects, "enter"), effects, tLineEndingBlank);
-    call1(objGet(effects, "consume"), effects, code);
-    call1(objGet(effects, "exit"), effects, tLineEndingBlank);
-    JS.set(self, "currentConstruct", undef());
-    return initial;
-  });
-
-  JsValue flowAttempt = call3(
-    objGet(effects, "attempt"),
+  JsValue afterConstruct = after("lineEnding");
+  initial = JS.invoke(
     effects,
-    objGet(constructs, "flow"),
-    afterConstruct,
-    call3(objGet(effects, "attempt"), effects, content, afterConstruct, undef())
+    "attempt",
+    blankLine,
+    after("lineEndingBlank"),
+    JS.invoke(
+      effects,
+      "attempt",
+      JS.get(constructs, "flowInitial"),
+      afterConstruct,
+      factorySpace(
+        effects,
+        JS.invoke(effects, "attempt", JS.get(constructs, "flow"), afterConstruct, JS.invoke(effects, "attempt", content, afterConstruct)),
+        "linePrefix",
+        0.0
+      )
+    )
   );
-  JsValue spaced = factorySpace(effects, flowAttempt, tLinePrefix, 0);
-  JsValue flowInitial = call3(objGet(effects, "attempt"), effects, objGet(constructs, "flowInitial"), afterConstruct, spaced);
-  initial = call3(objGet(effects, "attempt"), effects, blankLine, atBlankEnding, flowInitial);
   return initial;
-});
-
-export JsValue flowConstruct = makeConstruct(initializeFlow);
+}));
diff -ruN a/src/graph/remark-parse/micromark/initialize/text.lil b/src/graph/remark-parse/micromark/initialize/text.lil
--- a/src/graph/remark-parse/micromark/initialize/text.lil	2026-09-23 20:40:45.202447435 +0000
+++ b/src/graph/remark-parse/micromark/initialize/text.lil	2026-09-23 20:40:45.308379635 +0000
@@ -1,200 +1,148 @@
-import { arrLen, call1, call2, call3, copyPoint, isEof, isFn, isStr, isUndef, jsInt, objGet, objGetVal, objectAssign, undef } from "../host.lil";
-import { cSpace, codeEq } from "../symbol/codes.lil";
-import { kHardBreakPrefixSizeMin } from "../symbol/constants.lil";
-import { tData, tHardBreakTrailing, tLineEnding, tLineSuffix } from "../symbol/types.lil";
-import { cHorizontalTab, cVirtualSpace } from "../symbol/codes.lil";
-
-JsValue resolveAllLineSuffixes(JsValue events, JsValue context) {
-  int eventIndex = 0;
-  int n = arrLen(events);
-  while (true) {
-    eventIndex = eventIndex + 1;
-    if (eventIndex > n) {
-      break;
-    }
-    bool atEnd = eventIndex == n;
-    bool lineEnd = false;
-    if (!atEnd && objGet(events[eventIndex][1], "type") == tLineEnding) {
-      lineEnd = true;
-    }
-    if ((atEnd || lineEnd) && objGet(events[eventIndex - 1][1], "type") == tData) {
-      JsValue data = events[eventIndex - 1][1];
-      JsValue chunks = call1(objGet(context, "sliceStream"), context, data);
-      int index = arrLen(chunks);
-      int bufferIndex = -1;
-      int size = 0;
+import { TokenizerToken, copyPoint, isEof } from "../host.lil";
+
+extern JsValue Object;
+
+JsValue resolveAllLineSuffixes = JS.method2((JsValue _self, JsValue events, JsValue context) => {
+  for (float eventIndex = 1.0; eventIndex <= events.length; eventIndex++) {
+    if (
+      (eventIndex == events.length || JS.strictEqual(JS.get(events[eventIndex][1], "type"), "lineEnding")) &&
+      JS.strictEqual(JS.get(events[eventIndex - 1.0][1], "type"), "data")
+    ) {
+      TokenizerToken data = JS.assume(events[eventIndex - 1.0][1]);
+      JsValue chunks = JS.invoke(context, "sliceStream", data);
+      float index = chunks.length;
+      float bufferIndex = -1.0;
+      float size = 0.0;
       bool tabs = false;
       while (true) {
-        index = index - 1;
-        if (index < 0) {
+        index--;
+        if (index < 0.0) {
           break;
         }
         JsValue chunk = chunks[index];
-        if (isStr(chunk)) {
-          string text = JS.string(chunk);
-          bufferIndex = text.length;
-          while (bufferIndex > 0 && text.charCodeAt(bufferIndex - 1) == cSpace) {
-            size = size + 1;
-            bufferIndex = bufferIndex - 1;
+        if (JS.typeOf(chunk) == "string") {
+          bufferIndex = chunk.length;
+          while (JS.strictEqual(JS.invoke(chunk, "charCodeAt", bufferIndex - 1.0), 32)) {
+            size++;
+            bufferIndex--;
           }
-          if (bufferIndex > 0) {
+          if (bufferIndex != 0.0) {
             break;
           }
-          bufferIndex = -1;
-        } else if (codeEq(chunk, cHorizontalTab)) {
+          bufferIndex = -1.0;
+        } else if (JS.strictEqual(chunk, -2)) {
           tabs = true;
-          size = size + 1;
-        } else if (codeEq(chunk, cVirtualSpace)) {
-        } else {
-          index = index + 1;
+          size++;
+        } else if (!JS.strictEqual(chunk, -1)) {
+          index++;
           break;
         }
       }
-      if (objGet(context, "_contentTypeTextTrailing").truthy() && eventIndex == n) {
-        size = 0;
+      if (JS.get(context, "_contentTypeTextTrailing").truthy() && eventIndex == events.length) {
+        size = 0.0;
       }
-      if (size > 0) {
-        string typ = tLineSuffix;
-        if (!(eventIndex == n || tabs || size < kHardBreakPrefixSizeMin)) {
-          typ = tHardBreakTrailing;
-        }
-        JsValue start = JS.object();
-        if (index != 0) {
-          JS.set(start, "_bufferIndex", bufferIndex);
-        } else {
-          JS.set(start, "_bufferIndex", jsInt(objGet(objGet(data, "start"), "_bufferIndex")) + bufferIndex);
-        }
-        JS.set(start, "_index", jsInt(objGet(objGet(data, "start"), "_index")) + index);
-        JS.set(start, "line", objGet(objGet(data, "end"), "line"));
-        JS.set(start, "column", jsInt(objGet(objGet(data, "end"), "column")) - size);
-        JS.set(start, "offset", jsInt(objGet(objGet(data, "end"), "offset")) - size);
-        JsValue token = JS.object();
-        JS.set(token, "type", typ);
-        JS.set(token, "start", start);
-        JS.set(token, "end", copyPoint(objGet(data, "end")));
-        JS.set(data, "end", copyPoint(objGet(token, "start")));
-        if (objGet(objGet(data, "start"), "offset") == objGet(objGet(data, "end"), "offset")) {
-          objectAssign(data, token);
+      if (size != 0.0) {
+        JsValue token = JS.object(
+          "type", if (eventIndex == events.length || tabs || size < 2.0) { "lineSuffix" } else { "hardBreakTrailing" },
+          "start", JS.object(
+            "_bufferIndex", if (index != 0.0) { bufferIndex } else { data.start._bufferIndex + bufferIndex },
+            "_index", data.start._index + index,
+            "line", data.end.line,
+            "column", data.end.column - size,
+            "offset", data.end.offset - size
+          ),
+          "end", copyPoint(data.end)
+        );
+        JS.set(data, "end", copyPoint(JS.get(token, "start")));
+        if (data.start.offset == data.end.offset) {
+          JS.call(JS.get(Object, "assign"), Object, data, token);
         } else {
-          JsValue enter = JS.array();
-          enter[0] = "enter";
-          enter[1] = token;
-          enter[2] = context;
-          JsValue exit = JS.array();
-          exit[0] = "exit";
-          exit[1] = token;
-          exit[2] = context;
-          JS.call(JS.get(events, "splice"), events, eventIndex, 0, enter, exit);
-          eventIndex = eventIndex + 2;
-          n = arrLen(events);
+          JS.invoke(events, "splice", eventIndex, 0, JS.array("enter", token, context), JS.array("exit", token, context));
+          eventIndex = eventIndex + 2.0;
         }
       }
-      eventIndex = eventIndex + 1;
+      eventIndex++;
     }
   }
   return events;
-}
+});
 
 JsValue createResolver(JsValue extraResolver) {
   return JS.method2((JsValue _self, JsValue events, JsValue context) => {
-    int index = -1;
-    JsValue enter = undef();
-    int n = arrLen(events);
-    while (true) {
-      index = index + 1;
-      if (index > n) {
-        break;
-      }
-      if (isUndef(enter)) {
-        if (index < n && events[index].truthy() && objGet(events[index][1], "type") == tData) {
+    float enter = -1.0;
+    for (float index = 0.0; index <= events.length; index++) {
+      if (enter < 0.0) {
+        if (JS.and(events[index], JS.strictEqual(JS.get(events[index][1], "type"), "data")).truthy()) {
           enter = index;
-          index = index + 1;
+          index++;
         }
-      } else if (index >= n || !events[index].truthy() || objGet(events[index][1], "type") != tData) {
-        int enterI = jsInt(enter);
-        if (index != enterI + 2) {
-          JS.set(events[enterI][1], "end", objGet(events[index - 1][1], "end"));
-          JS.call(JS.get(events, "splice"), events, enterI + 2, index - enterI - 2);
-          index = enterI + 2;
-          n = arrLen(events);
+      } else if (!JS.and(events[index], JS.strictEqual(JS.get(events[index][1], "type"), "data")).truthy()) {
+        if (index != enter + 2.0) {
+          JS.set(events[enter][1], "end", JS.get(events[index - 1.0][1], "end"));
+          JS.invoke(events, "splice", enter + 2.0, index - enter - 2.0);
+          index = enter + 2.0;
         }
-        enter = undef();
+        enter = -1.0;
       }
     }
-    if (isFn(extraResolver)) {
-      return call2(extraResolver, JS.undefined(), events, context);
+    if (extraResolver.truthy()) {
+      return JS.call(extraResolver, JS.undefined(), events, context);
     }
     return events;
   });
 }
 
 JsValue initializeFactory(string field, JsValue extraResolver) {
-  JsValue construct = JS.object();
-  JS.set(construct, "resolveAll", createResolver(extraResolver));
-  JS.set(construct, "tokenize", JS.method1((JsValue self, JsValue effects) => {
-    JsValue constructs = objGet(objGet(self, "parser"), "constructs")[field];
-    JsValue text = JS.undefined();
-
-    JsValue atBreak = JS.method1((JsValue _s, JsValue code) => {
-      if (isEof(code)) {
-        return true;
-      }
-      JsValue list = objGetVal(constructs, code);
-      if (!list.truthy()) {
-        return false;
-      }
-      int index = -1;
-      int n = arrLen(list);
-      while (true) {
-        index = index + 1;
-        if (index >= n) {
-          break;
-        }
-        JsValue item = list[index];
-        JsValue prev = objGet(item, "previous");
-        if (!isFn(prev) || call1(prev, self, objGet(self, "previous")).truthy()) {
+  return JS.object(
+    "resolveAll", createResolver(extraResolver),
+    "tokenize", JS.method1((JsValue self, JsValue effects) => {
+      JsValue constructs = JS.get(JS.get(JS.get(self, "parser"), "constructs"), field);
+      JsValue text = JS.undefined();
+
+      func(JsValue)->bool atBreak = (JsValue code) => {
+        if (isEof(code)) {
           return true;
         }
-      }
-      return false;
-    });
-
-    JsValue data = JS.method1((JsValue _s, JsValue code) => {
-      if (call1(atBreak, undef(), code).truthy()) {
-        call1(JS.get(effects, "exit"), effects, tData);
-        return call1(text, JS.undefined(), code);
-      }
-      call1(JS.get(effects, "consume"), effects, code);
-      return data;
-    });
-
-    JsValue notText = JS.method1((JsValue _s, JsValue code) => {
-      if (isEof(code)) {
-        call1(JS.get(effects, "consume"), effects, code);
-        return undef();
-      }
-      call1(JS.get(effects, "enter"), effects, tData);
-      call1(JS.get(effects, "consume"), effects, code);
-      return data;
-    });
-
-    JsValue start = JS.method1((JsValue _s, JsValue code) => {
-      if (call1(atBreak, undef(), code).truthy()) {
-        return call1(text, JS.undefined(), code);
-      }
-      return call1(notText, JS.undefined(), code);
-    });
+        JsValue list = constructs[code];
+        if (list.truthy()) {
+          for (float index = 0.0; index < list.length; index++) {
+            JsValue previous = JS.get(list[index], "previous");
+            if (!previous.truthy() || JS.call(previous, self, JS.get(self, "previous")).truthy()) {
+              return true;
+            }
+          }
+        }
+        return false;
+      };
 
-    text = call3(JS.get(effects, "attempt"), effects, constructs, start, notText);
-    return start;
-  }));
-  return construct;
+      JsValue notText = JS.undefined();
+      JsValue data = JS.method1((JsValue _s, JsValue code) => {
+        if (atBreak(code)) {
+          JS.invoke(effects, "exit", "data");
+          return JS.call(text, JS.undefined(), code);
+        }
+        JS.invoke(effects, "consume", code);
+        return data;
+      });
+      notText = JS.method1((JsValue _s, JsValue code) => {
+        if (isEof(code)) {
+          JS.invoke(effects, "consume", code);
+          return JS.undefined();
+        }
+        JS.invoke(effects, "enter", "data");
+        JS.invoke(effects, "consume", code);
+        return data;
+      });
+      JsValue start = JS.method1((JsValue _s, JsValue code) => {
+        return JS.call(if (atBreak(code)) { text } else { notText }, JS.undefined(), code);
+      });
+      text = JS.invoke(effects, "attempt", constructs, start, notText);
+      return start;
+    })
+  );
 }
 
-export JsValue resolver = JS.object();
-JS.set(resolver, "resolveAll", createResolver(undef()));
+export JsValue resolver = JS.object("resolveAll", createResolver(JS.undefined()));
 
-export JsValue stringConstruct = initializeFactory("string", undef());
-export JsValue textConstruct = initializeFactory("text", JS.method2((JsValue _s, JsValue events, JsValue context) => {
-  return resolveAllLineSuffixes(events, context);
-}));
+export JsValue stringConstruct = initializeFactory("string", JS.undefined());
+export JsValue textConstruct = initializeFactory("text", resolveAllLineSuffixes);
diff -ruN a/src/graph/remark-parse/micromark/parse.lil b/src/graph/remark-parse/micromark/parse.lil
--- a/src/graph/remark-parse/micromark/parse.lil	2026-09-23 20:40:45.194085487 +0000
+++ b/src/graph/remark-parse/micromark/parse.lil	2026-09-23 20:40:45.308721355 +0000
@@ -1,4 +1,3 @@
-import { arrLen, call1, isNullish, objGet, optionOrEmpty, push } from "./host.lil";
 import { combineExtensions } from "./util-combine-extensions.lil";
 import { createTokenizer } from "./create-tokenizer.lil";
 import { contentConstruct } from "./initialize/content.lil";
@@ -7,34 +6,24 @@
 import { stringConstruct, textConstruct } from "./initialize/text.lil";
 import { defaultConstructs } from "./constructs.lil";
 
-JsValue makeCreate(JsValue parser, JsValue initial) {
-  return JS.method1((JsValue _self, JsValue from) => {
-    return createTokenizer(parser, initial, from);
-  });
-}
-
 export JsValue parse(JsValue options) {
-  JsValue settings = optionOrEmpty(options);
-  JsValue list = JS.array();
-  push(list, defaultConstructs);
-  JsValue extras = objGet(settings, "extensions");
-  if (extras.truthy()) {
-    int i = 0;
-    int n = arrLen(extras);
-    while (i < n) {
-      push(list, extras[i]);
-      i = i + 1;
-    }
-  }
-  JsValue constructs = combineExtensions(list);
-  JsValue parser = JS.object();
-  JS.set(parser, "constructs", constructs);
-  JS.set(parser, "defined", JS.array());
-  JS.set(parser, "lazy", JS.object());
-  JS.set(parser, "content", makeCreate(parser, contentConstruct));
-  JS.set(parser, "document", makeCreate(parser, documentConstruct));
-  JS.set(parser, "flow", makeCreate(parser, flowConstruct));
-  JS.set(parser, "string", makeCreate(parser, stringConstruct));
-  JS.set(parser, "text", makeCreate(parser, textConstruct));
+  JsValue parser = JS.undefined();
+  func(JsValue)->JsValue create = (JsValue initial) => {
+    return JS.method1((JsValue _self, JsValue from) => {
+      return createTokenizer(parser, initial, from);
+    });
+  };
+  JsValue[] extensions = JS.assume(JS.or(JS.get(JS.or(options, JS.object()), "extensions"), JS.array()));
+  JsValue[] list = [defaultConstructs, ...extensions];
+  parser = JS.object(
+    "constructs", combineExtensions(list),
+    "content", create(contentConstruct),
+    "defined", JS.array(),
+    "document", create(documentConstruct),
+    "flow", create(flowConstruct),
+    "lazy", JS.object(),
+    "string", create(stringConstruct),
+    "text", create(textConstruct)
+  );
   return parser;
 }
diff -ruN a/src/graph/remark-parse/micromark/preprocess.lil b/src/graph/remark-parse/micromark/preprocess.lil
--- a/src/graph/remark-parse/micromark/preprocess.lil	2026-09-23 20:40:45.203167664 +0000
+++ b/src/graph/remark-parse/micromark/preprocess.lil	2026-09-23 20:40:45.316592483 +0000
@@ -1,6 +1,4 @@
-import { codeEof, decodeBytes, MathRef, push } from "./host.lil";
-import { cByteOrderMarker, cCarriageReturn, cCarriageReturnLineFeed, cCr, cHorizontalTab, cHt, cLf, cLineFeed, cNul, cReplacementCharacter, cVirtualSpace } from "./symbol/codes.lil";
-import { kTabSize } from "./symbol/constants.lil";
+extern JsValue TextDecoder;
 
 export JsValue preprocess() {
   float column = 1.0;
@@ -10,57 +8,56 @@
 
   return JS.method3((JsValue _self, JsValue value, JsValue encoding, JsValue end) => {
     JsValue[] chunks = [];
-    string text = buffer + decodeBytes(value, encoding);
+    string decoded = JS.assume(if (JS.typeOf(value) == "string") { value } else { JS.invoke(JS.construct(TextDecoder, JS.or(encoding, JS.undefined())), "decode", value) });
+    string text = buffer + decoded;
     int startPosition = 0;
     buffer = "";
     if (start) {
-      if (text.length > 0 && text.charCodeAt(0) == cByteOrderMarker) {
+      if (JS.strictEqual(JS.invoke(text, "charCodeAt", 0), 65279)) {
         startPosition = 1;
       }
       start = false;
     }
-    int n = text.length;
-    while (startPosition < n) {
+    while (startPosition < text.length) {
       int endPosition = startPosition;
-      int found = -1;
-      while (endPosition < n) {
+      int code = -1;
+      while (endPosition < text.length) {
         int ch = text.charCodeAt(endPosition);
-        if (ch == cNul || ch == cHt || ch == cLf || ch == cCr) {
-          found = ch;
+        if (ch == 0 || ch == 9 || ch == 10 || ch == 13) {
+          code = ch;
           break;
         }
-        endPosition = endPosition + 1;
+        endPosition++;
       }
-      if (found < 0) {
+      if (code < 0) {
         buffer = text.slice(startPosition);
         break;
       }
-      int code = found;
-      if (code == cLf && startPosition == endPosition && atCarriageReturn) {
-        push(chunks, cCarriageReturnLineFeed);
+      if (code == 10 && startPosition == endPosition && atCarriageReturn) {
+        chunks.push(-3);
         atCarriageReturn = false;
       } else {
         if (atCarriageReturn) {
-          push(chunks, cCarriageReturn);
+          chunks.push(-5);
           atCarriageReturn = false;
         }
         if (startPosition < endPosition) {
-          push(chunks, text.slice(startPosition, endPosition));
+          chunks.push(text.slice(startPosition, endPosition));
           column = column + (endPosition - startPosition);
         }
-        if (code == cNul) {
-          push(chunks, cReplacementCharacter);
-          column = column + 1.0;
-        } else if (code == cHt) {
-          float next = JS.number(JS.call(JS.get(MathRef, "ceil"), MathRef, column / JS.number(kTabSize))) * JS.number(kTabSize);
-          push(chunks, cHorizontalTab);
+        if (code == 0) {
+          chunks.push(65533);
+          column++;
+        } else if (code == 9) {
+          float next = (column / 4.0).ceil() * 4.0;
+          chunks.push(-2);
           while (column < next) {
-            push(chunks, cVirtualSpace);
-            column = column + 1.0;
+            chunks.push(-1);
+            column++;
           }
-          column = column + 1.0;
-        } else if (code == cLf) {
-          push(chunks, cLineFeed);
+          column++;
+        } else if (code == 10) {
+          chunks.push(-4);
           column = 1.0;
         } else {
           atCarriageReturn = true;
@@ -71,12 +68,12 @@
     }
     if (end.truthy()) {
       if (atCarriageReturn) {
-        push(chunks, cCarriageReturn);
+        chunks.push(-5);
       }
       if (buffer.length > 0) {
-        push(chunks, buffer);
+        chunks.push(buffer);
       }
-      push(chunks, codeEof);
+      chunks.push(null);
     }
     return chunks;
   });
diff -ruN a/src/graph/remark-parse/micromark/splice-buffer.lil b/src/graph/remark-parse/micromark/splice-buffer.lil
--- a/src/graph/remark-parse/micromark/splice-buffer.lil	2026-09-23 20:40:45.203494106 +0000
+++ b/src/graph/remark-parse/micromark/splice-buffer.lil	2026-09-23 20:40:45.325693147 +0000
@@ -1,183 +1,113 @@
-import {
-  apply,
-  arrayFrom,
-  arrLen,
-  fail,
-  MathRef,
-  NumberRef,
-  ObjectRef,
-  invoke0,
-  invoke1,
-  invoke2,
-  isNullish,
-  jsInt,
-  jsNum,
-  jsString,
-  objGet,
-  pop,
-  push,
-  slice1,
-  slice2,
-  undef
-} from "./host.lil";
-import { kV8MaxSafeChunkSize } from "./symbol/constants.lil";
+extern JsValue Object;
+extern JsValue Array;
+extern JsValue Math;
+extern JsValue RangeError;
 
 void chunkedPush(JsValue list, JsValue right) {
-  int n = arrLen(right);
-  if (n < kV8MaxSafeChunkSize) {
-    JsValue parameters = arrayFrom(right);
-    apply(JS.get(list, "push"), list, parameters);
+  if (right.length < 10000.0) {
+    JS.apply(JS.get(list, "push"), list, right);
   } else {
-    int chunkStart = 0;
-    while (chunkStart < n) {
-      JsValue part = slice2(right, chunkStart, chunkStart + kV8MaxSafeChunkSize);
-      apply(JS.get(list, "push"), list, part);
-      chunkStart = chunkStart + kV8MaxSafeChunkSize;
+    for (float chunkStart = 0.0; chunkStart < right.length; chunkStart = chunkStart + 10000.0) {
+      JS.apply(JS.get(list, "push"), list, JS.invoke(right, "slice", chunkStart, chunkStart + 10000.0));
     }
   }
 }
 
-JsValue sbLeft(JsValue self) {
-  return objGet(self, "left");
-}
-
-JsValue sbRight(JsValue self) {
-  return objGet(self, "right");
-}
-
-int sbSize(JsValue self) {
-  return arrLen(sbLeft(self)) + arrLen(sbRight(self));
-}
-
 void setCursor(JsValue self, float n) {
-  JsValue left = sbLeft(self);
-  JsValue right = sbRight(self);
-  float leftLen = JS.number(JS.get(left, "length"));
-  float rightLen = JS.number(JS.get(right, "length"));
-  if (n == leftLen || (n > leftLen && rightLen == 0.0) || (n < 0.0 && leftLen == 0.0)) {
+  JsValue left = JS.get(self, "left");
+  JsValue right = JS.get(self, "right");
+  if (n == left.length || (n > left.length && right.length == 0.0) || (n < 0.0 && left.length == 0.0)) {
     return;
   }
-  if (n < leftLen) {
-    JsValue removed = invoke2(left, "splice", n, JS.get(NumberRef, "POSITIVE_INFINITY"));
-    JS.call(JS.get(removed, "reverse"), removed);
-    chunkedPush(right, removed);
+  if (n < left.length) {
+    chunkedPush(right, JS.invoke(JS.invoke(left, "splice", n, 1.0 / 0.0), "reverse"));
   } else {
-    JsValue removed = invoke2(right, "splice", leftLen + rightLen - n, JS.get(NumberRef, "POSITIVE_INFINITY"));
-    JS.call(JS.get(removed, "reverse"), removed);
-    chunkedPush(left, removed);
+    chunkedPush(left, JS.invoke(JS.invoke(right, "splice", left.length + right.length - n, 1.0 / 0.0), "reverse"));
   }
 }
 
-JsValue sbGet(JsValue self, JsValue indexVal) {
-  float index = jsNum(indexVal);
-  int size = sbSize(self);
-  if (index < 0.0 || index >= JS.number(size)) {
-    fail("Cannot access index `" + jsString(indexVal) + "` in a splice buffer of size `" + JS.string(size) + "`");
-  }
-  JsValue left = sbLeft(self);
-  int leftLen = arrLen(left);
-  if (index < JS.number(leftLen)) {
-    return left[jsInt(indexVal)];
-  }
-  JsValue right = sbRight(self);
-  int rightLen = arrLen(right);
-  return right[rightLen - jsInt(indexVal) + leftLen - 1];
-}
-
-JsValue sbSlice(JsValue self, JsValue startVal, JsValue endVal) {
-  float stop = JS.number(JS.get(NumberRef, "POSITIVE_INFINITY"));
-  if (!(isNullish(endVal))) {
-    stop = jsNum(endVal);
-  }
-  float start = jsNum(startVal);
-  JsValue left = sbLeft(self);
-  JsValue right = sbRight(self);
-  float leftLen = JS.number(JS.get(left, "length"));
-  if (stop < leftLen) {
-    return slice2(left, start, stop);
-  }
-  if (start > leftLen) {
-    float rightLen = JS.number(JS.get(right, "length"));
-    JsValue part = slice2(right, rightLen - stop + leftLen, rightLen - start + leftLen);
-    return JS.call(JS.get(part, "reverse"), part);
-  }
-  JsValue head = slice1(left, start);
-  float rightLen = JS.number(JS.get(right, "length"));
-  JsValue tail = slice1(right, rightLen - stop + leftLen);
-  JS.call(JS.get(tail, "reverse"), tail);
-  return JS.call(JS.get(head, "concat"), head, tail);
-}
-
-JsValue sbSplice(JsValue self, JsValue startVal, JsValue deleteCount, JsValue items) {
-  float count = 0.0;
-  if (deleteCount.truthy()) {
-    count = jsNum(deleteCount);
-  }
-  setCursor(self, JS.number(JS.call(JS.get(MathRef, "trunc"), MathRef, jsNum(startVal))));
-  JsValue right = sbRight(self);
-  JsValue removed = invoke2(right, "splice", JS.number(JS.get(right, "length")) - count, JS.get(NumberRef, "POSITIVE_INFINITY"));
-  if (items.truthy()) {
-    chunkedPush(sbLeft(self), items);
-  }
-  return JS.call(JS.get(removed, "reverse"), removed);
+float sizeOf(JsValue self) {
+  return JS.get(self, "left").length + JS.get(self, "right").length;
 }
 
 export JsValue SpliceBuffer = JS.method1((JsValue self, JsValue initial) => {
-  if (initial.truthy()) {
-    JS.set(self, "left", arrayFrom(initial));
-  } else {
-    JS.set(self, "left", JS.array());
-  }
+  JS.set(self, "left", if (initial.truthy()) { JS.call(JS.get(Array, "from"), Array, initial) } else { JS.array() });
   JS.set(self, "right", JS.array());
-  return self;
+  return JS.undefined();
 });
 
-JsValue proto = JS.get(SpliceBuffer, "prototype");
-proto["get"] = JS.method1(sbGet);
-proto["slice"] = JS.method2(sbSlice);
-proto["splice"] = JS.method3(sbSplice);
-proto["shift"] = JS.method0((JsValue self) => {
-  setCursor(self, 0.0);
-  return pop(sbRight(self));
-});
-proto["pop"] = JS.method0((JsValue self) => {
-  setCursor(self, JS.number(JS.get(NumberRef, "POSITIVE_INFINITY")));
-  return pop(sbLeft(self));
-});
-proto["push"] = JS.method1((JsValue self, JsValue item) => {
-  setCursor(self, JS.number(JS.get(NumberRef, "POSITIVE_INFINITY")));
-  push(sbLeft(self), item);
-  return undef();
-});
-proto["pushMany"] = JS.method1((JsValue self, JsValue items) => {
-  setCursor(self, JS.number(JS.get(NumberRef, "POSITIVE_INFINITY")));
-  chunkedPush(sbLeft(self), items);
-  return undef();
-});
-proto["unshift"] = JS.method1((JsValue self, JsValue item) => {
-  setCursor(self, 0.0);
-  push(sbRight(self), item);
-  return undef();
-});
-proto["unshiftMany"] = JS.method1((JsValue self, JsValue items) => {
-  setCursor(self, 0.0);
-  JsValue reversed = arrayFrom(items);
-  JS.call(JS.get(reversed, "reverse"), reversed);
-  chunkedPush(sbRight(self), reversed);
-  return undef();
-});
-proto["setCursor"] = JS.method1((JsValue self, JsValue n) => {
-  setCursor(self, jsNum(n));
-  return undef();
-});
+JS.call(JS.get(Object, "assign"), Object, JS.get(SpliceBuffer, "prototype"), JS.object(
+  "get", JS.method1((JsValue self, JsValue indexVal) => {
+    float index = JS.assume(indexVal);
+    JsValue left = JS.get(self, "left");
+    JsValue right = JS.get(self, "right");
+    if (index < 0.0 || index >= left.length + right.length) {
+      throw JS.construct(RangeError, "Cannot access index `" + indexVal + "` in a splice buffer of size `" + JS.string(left.length + right.length) + "`");
+    }
+    if (index < left.length) {
+      return left[index];
+    }
+    return right[right.length - index + left.length - 1.0];
+  }),
+  "shift", JS.method0((JsValue self) => {
+    setCursor(self, 0.0);
+    return JS.invoke(JS.get(self, "right"), "pop");
+  }),
+  "slice", JS.method2((JsValue self, JsValue startVal, JsValue endVal) => {
+    float start = JS.assume(startVal);
+    float stop = JS.assume(if (JS.isNullish(endVal)) { 1.0 / 0.0 } else { endVal });
+    JsValue left = JS.get(self, "left");
+    JsValue right = JS.get(self, "right");
+    if (stop < left.length) {
+      return JS.invoke(left, "slice", startVal, stop);
+    }
+    if (start > left.length) {
+      return JS.invoke(JS.invoke(right, "slice", right.length - stop + left.length, right.length - start + left.length), "reverse");
+    }
+    return JS.invoke(JS.invoke(left, "slice", startVal), "concat", JS.invoke(JS.invoke(right, "slice", right.length - stop + left.length), "reverse"));
+  }),
+  "splice", JS.method3((JsValue self, JsValue start, JsValue deleteCount, JsValue items) => {
+    float count = JS.assume(JS.or(deleteCount, 0));
+    setCursor(self, JS.assume(JS.invoke(Math, "trunc", start)));
+    JsValue right = JS.get(self, "right");
+    JsValue removed = JS.invoke(right, "splice", right.length - count, 1.0 / 0.0);
+    if (items.truthy()) {
+      chunkedPush(JS.get(self, "left"), items);
+    }
+    return JS.invoke(removed, "reverse");
+  }),
+  "pop", JS.method0((JsValue self) => {
+    setCursor(self, 1.0 / 0.0);
+    return JS.invoke(JS.get(self, "left"), "pop");
+  }),
+  "push", JS.method1((JsValue self, JsValue item) => {
+    setCursor(self, 1.0 / 0.0);
+    JS.invoke(JS.get(self, "left"), "push", item);
+    return JS.undefined();
+  }),
+  "pushMany", JS.method1((JsValue self, JsValue items) => {
+    setCursor(self, 1.0 / 0.0);
+    chunkedPush(JS.get(self, "left"), items);
+    return JS.undefined();
+  }),
+  "unshift", JS.method1((JsValue self, JsValue item) => {
+    setCursor(self, 0.0);
+    JS.invoke(JS.get(self, "right"), "push", item);
+    return JS.undefined();
+  }),
+  "unshiftMany", JS.method1((JsValue self, JsValue items) => {
+    setCursor(self, 0.0);
+    chunkedPush(JS.get(self, "right"), JS.invoke(items, "reverse"));
+    return JS.undefined();
+  }),
+  "setCursor", JS.method1((JsValue self, JsValue n) => {
+    setCursor(self, JS.assume(n));
+    return JS.undefined();
+  })
+));
 
-JS.call(JS.get(ObjectRef, "defineProperty"), ObjectRef, proto, "length", JS.object(
-  "enumerable",
-  true,
-  "configurable",
-  true,
-  "get",
-  JS.method0((JsValue self) => {
-    return arrLen(sbLeft(self)) + arrLen(sbRight(self));
+JS.invoke(Object, "defineProperty", JS.get(SpliceBuffer, "prototype"), "length", JS.object(
+  "get", JS.method0((JsValue self) => {
+    return sizeOf(self);
   })
 ));
diff -ruN a/src/graph/remark-parse/micromark/util-character.lil b/src/graph/remark-parse/micromark/util-character.lil
--- a/src/graph/remark-parse/micromark/util-character.lil	2026-09-23 20:40:45.203846757 +0000
+++ b/src/graph/remark-parse/micromark/util-character.lil	2026-09-23 20:40:45.328297056 +0000
@@ -1,62 +1,34 @@
-import { fromCharCode1 } from "./host.lil";
+extern JsValue String;
 
-Regex asciiAlphaRe = new Regex("[A-Za-z]", "");
-Regex asciiAlphanumericRe = new Regex("[\\dA-Za-z]", "");
-Regex asciiAtextRe = new Regex("[#-'*+\\--9=?A-Z^-~]", "");
-Regex asciiDigitRe = new Regex("\\d", "");
-Regex asciiHexDigitRe = new Regex("[\\dA-Fa-f]", "");
-Regex asciiPunctuationRe = new Regex("[!-/:-@[-`{-~]", "");
-Regex unicodePunctuationRe = new Regex("\\p{P}|\\p{S}", "u");
-Regex unicodeWhitespaceRe = new Regex("\\s", "");
-
-bool regexCode(Regex pattern, JsValue code) {
-  return JS.strictNotEqual(code, null) && JS.number(code) > -1.0 && pattern.test(fromCharCode1(code));
-}
-
-export bool asciiAlpha(JsValue code) {
-  return regexCode(asciiAlphaRe, code);
+// A code is a character code (number), a negative virtual code, or `null` at the
+// end of input. Each check is a closure over its expression, as upstream's.
+func(JsValue)->bool regexCheck(Regex regex) {
+  return (JsValue code) => JS.strictNotEqual(code, null) && JS.greaterThan(code, -1) && regex.test(JS.assume(JS.invoke(String, "fromCharCode", code)));
 }
 
-export bool asciiAlphanumeric(JsValue code) {
-  return regexCode(asciiAlphanumericRe, code);
-}
-
-export bool asciiAtext(JsValue code) {
-  return regexCode(asciiAtextRe, code);
-}
+export func(JsValue)->bool asciiAlpha = regexCheck(new Regex("[A-Za-z]", ""));
+export func(JsValue)->bool asciiAlphanumeric = regexCheck(new Regex("[\\dA-Za-z]", ""));
+export func(JsValue)->bool asciiAtext = regexCheck(new Regex("[#-'*+\\--9=?A-Z^-~]", ""));
 
 export bool asciiControl(JsValue code) {
-  return JS.strictNotEqual(code, null) && (JS.number(code) < 32.0 || JS.strictEqual(code, 127));
-}
-
-export bool asciiDigit(JsValue code) {
-  return regexCode(asciiDigitRe, code);
-}
-
-export bool asciiHexDigit(JsValue code) {
-  return regexCode(asciiHexDigitRe, code);
+  return JS.strictNotEqual(code, null) && (JS.lessThan(code, 32) || JS.strictEqual(code, 127));
 }
 
-export bool asciiPunctuation(JsValue code) {
-  return regexCode(asciiPunctuationRe, code);
-}
+export func(JsValue)->bool asciiDigit = regexCheck(new Regex("\\d", ""));
+export func(JsValue)->bool asciiHexDigit = regexCheck(new Regex("[\\dA-Fa-f]", ""));
+export func(JsValue)->bool asciiPunctuation = regexCheck(new Regex("[!-/:-@[-`{-~]", ""));
 
 export bool markdownLineEnding(JsValue code) {
-  return JS.strictNotEqual(code, null) && JS.number(code) < -2.0;
+  return JS.strictNotEqual(code, null) && JS.lessThan(code, -2);
 }
 
 export bool markdownLineEndingOrSpace(JsValue code) {
-  return JS.strictNotEqual(code, null) && (JS.number(code) < 0.0 || JS.strictEqual(code, 32));
+  return JS.strictNotEqual(code, null) && (JS.lessThan(code, 0) || JS.strictEqual(code, 32));
 }
 
 export bool markdownSpace(JsValue code) {
   return JS.strictEqual(code, -2) || JS.strictEqual(code, -1) || JS.strictEqual(code, 32);
 }
 
-export bool unicodePunctuation(JsValue code) {
-  return regexCode(unicodePunctuationRe, code);
-}
-
-export bool unicodeWhitespace(JsValue code) {
-  return regexCode(unicodeWhitespaceRe, code);
-}
+export func(JsValue)->bool unicodePunctuation = regexCheck(new Regex("\\p{P}|\\p{S}", "u"));
+export func(JsValue)->bool unicodeWhitespace = regexCheck(new Regex("\\s", ""));
diff -ruN a/src/graph/remark-parse/micromark/util-chunked.lil b/src/graph/remark-parse/micromark/util-chunked.lil
--- a/src/graph/remark-parse/micromark/util-chunked.lil	2026-09-23 20:40:45.212614064 +0000
+++ b/src/graph/remark-parse/micromark/util-chunked.lil	2026-09-23 20:40:45.328602629 +0000
@@ -1,49 +1,37 @@
-import { apply, arrayFrom, arrLen, invoke2, slice2 } from "./host.lil";
-import { kV8MaxSafeChunkSize } from "./symbol/constants.lil";
+extern JsValue Array;
 
 export void splice(JsValue list, JsValue startVal, JsValue removeVal, JsValue items) {
-  float end = JS.number(JS.get(list, "length"));
-  float start = JS.number(startVal);
-  float remove = JS.number(removeVal);
+  float end = list.length;
+  float start = JS.assume(startVal);
+  float remove = JS.assume(removeVal);
+  float chunkStart = 0.0;
   if (start < 0.0) {
-    if (0.0 - start > end) {
-      start = 0.0;
-    } else {
-      start = end + start;
-    }
+    start = if (-start > end) { 0.0 } else { end + start };
   } else {
-    if (start > end) {
-      start = end;
-    }
-  }
-  if (remove < 0.0) {
-    remove = 0.0;
+    start = if (start > end) { end } else { start };
   }
-  int itemCount = arrLen(items);
-  if (itemCount < kV8MaxSafeChunkSize) {
-    JsValue parameters = arrayFrom(items);
-    JS.call(JS.get(parameters, "unshift"), parameters, start, remove);
-    apply(JS.get(list, "splice"), list, parameters);
+  remove = if (remove > 0.0) { remove } else { 0.0 };
+  if (items.length < 10000.0) {
+    JsValue parameters = JS.call(JS.get(Array, "from"), Array, items);
+    JS.invoke(parameters, "unshift", start, remove);
+    JS.apply(JS.get(list, "splice"), list, parameters);
   } else {
-    if (remove > 0.0) {
-      invoke2(list, "splice", start, remove);
+    if (remove != 0.0) {
+      JS.invoke(list, "splice", start, remove);
     }
-    int chunkStart = 0;
-    float cursor = start;
-    while (chunkStart < itemCount) {
-      int next = chunkStart + kV8MaxSafeChunkSize;
-      JsValue parameters = slice2(items, chunkStart, next);
-      JS.call(JS.get(parameters, "unshift"), parameters, cursor, 0);
-      apply(JS.get(list, "splice"), list, parameters);
-      chunkStart = chunkStart + kV8MaxSafeChunkSize;
-      cursor = cursor + JS.number(kV8MaxSafeChunkSize);
+    while (chunkStart < items.length) {
+      JsValue parameters = JS.invoke(items, "slice", chunkStart, chunkStart + 10000.0);
+      JS.invoke(parameters, "unshift", start, 0);
+      JS.apply(JS.get(list, "splice"), list, parameters);
+      chunkStart = chunkStart + 10000.0;
+      start = start + 10000.0;
     }
   }
 }
 
 export JsValue pushAll(JsValue list, JsValue items) {
-  if (arrLen(list) > 0) {
-    splice(list, JS.get(list, "length"), 0, items);
+  if (list.length > 0.0) {
+    splice(list, list.length, 0, items);
     return list;
   }
   return items;
diff -ruN a/src/graph/remark-parse/micromark/util-classify-character.lil b/src/graph/remark-parse/micromark/util-classify-character.lil
--- a/src/graph/remark-parse/micromark/util-classify-character.lil	2026-09-23 20:40:45.212973126 +0000
+++ b/src/graph/remark-parse/micromark/util-classify-character.lil	2026-09-23 20:40:45.328693173 +0000
@@ -1,13 +1,13 @@
-import { isEof, undef } from "./host.lil";
 import { markdownLineEndingOrSpace, unicodePunctuation, unicodeWhitespace } from "./util-character.lil";
 import { kCharacterGroupPunctuation, kCharacterGroupWhitespace } from "./symbol/constants.lil";
 
+// 1 (whitespace), 2 (punctuation), or undefined (anything else), as upstream.
 export JsValue classifyCharacter(JsValue code) {
-  if (isEof(code) || markdownLineEndingOrSpace(code) || unicodeWhitespace(code)) {
+  if (JS.strictEqual(code, null) || markdownLineEndingOrSpace(code) || unicodeWhitespace(code)) {
     return kCharacterGroupWhitespace;
   }
   if (unicodePunctuation(code)) {
     return kCharacterGroupPunctuation;
   }
-  return undef();
+  return JS.undefined();
 }
diff -ruN a/src/graph/remark-parse/micromark/util-combine-extensions.lil b/src/graph/remark-parse/micromark/util-combine-extensions.lil
--- a/src/graph/remark-parse/micromark/util-combine-extensions.lil	2026-09-23 20:40:45.213305048 +0000
+++ b/src/graph/remark-parse/micromark/util-combine-extensions.lil	2026-09-23 20:40:45.335693235 +0000
@@ -1,111 +1,50 @@
-import { arrLen, hasOwn, isNullish, objGet, objSet, push } from "./host.lil";
+import { asList, hasOwn } from "./host.lil";
 import { splice } from "./util-chunked.lil";
 
-void mergeConstructs(JsValue existing, JsValue list) {
-  JsValue before = JS.array();
-  int index = -1;
-  int n = arrLen(list);
-  while (true) {
-    index = index + 1;
-    if (index >= n) {
-      break;
-    }
-    JsValue item = list[index];
-    if (JS.get(item, "add") == "after") {
-      push(existing, item);
-    } else {
-      push(before, item);
-    }
-  }
-  splice(existing, 0, 0, before);
-}
-
-void syntaxExtension(JsValue all, JsValue extension) {
-  for (string hook in extension) {
-    if (!hasOwn(extension, hook)) {
-      continue;
-    }
-    JsValue maybe = JS.undefined();
-    if (hasOwn(all, hook)) {
-      maybe = objGet(all, hook);
-    }
-    JsValue left = maybe;
-    if (isNullish(left)) {
-      left = JS.object();
-      objSet(all, hook, left);
-    }
-    JsValue right = objGet(extension, hook);
-    if (right.truthy()) {
-      for (string code in right) {
-        if (!hasOwn(right, code)) {
-          continue;
-        }
-        if (!hasOwn(left, code)) {
-          objSet(left, code, JS.array());
-        }
-        JsValue value = objGet(right, code);
-        JsValue asList = JS.array();
-        if (value.isArray()) {
-          asList = value;
-        } else if (value.truthy()) {
-          push(asList, value);
-        }
-        mergeConstructs(objGet(left, code), asList);
+// Syntax and HTML extensions merge the same way per hook; only what happens per key differs.
+JsValue combine(JsValue extensions, bool html) {
+  JsValue all = JS.object();
+  for (float index = 0.0; index < extensions.length; index++) {
+    JsValue extension = extensions[index];
+    for (string hook in extension) {
+      JsValue left = JS.get(all, hook);
+      if (!hasOwn(all, hook) || !left.truthy()) {
+        left = JS.object();
+        JS.set(all, hook, left);
       }
-    }
-  }
-}
-
-void htmlExtension(JsValue all, JsValue extension) {
-  for (string hook in extension) {
-    if (!hasOwn(extension, hook)) {
-      continue;
-    }
-    JsValue maybe = JS.undefined();
-    if (hasOwn(all, hook)) {
-      maybe = objGet(all, hook);
-    }
-    JsValue left = maybe;
-    if (isNullish(left)) {
-      left = JS.object();
-      objSet(all, hook, left);
-    }
-    JsValue right = objGet(extension, hook);
-    if (right.truthy()) {
-      for (string typ in right) {
-        if (!hasOwn(right, typ)) {
-          continue;
+      JsValue right = JS.get(extension, hook);
+      if (right.truthy()) {
+        for (string key in right) {
+          JsValue value = JS.get(right, key);
+          if (html) {
+            JS.set(left, key, value);
+          } else {
+            if (!hasOwn(left, key)) {
+              JS.set(left, key, JS.array());
+            }
+            JsValue list = asList(value);
+            JsValue existing = JS.get(left, key);
+            JsValue[] before = [];
+            for (float item = 0.0; item < list.length; item++) {
+              if (JS.strictEqual(JS.get(list[item], "add"), "after")) {
+                JS.invoke(existing, "push", list[item]);
+              } else {
+                before.push(list[item]);
+              }
+            }
+            splice(existing, 0, 0, before);
+          }
         }
-        objSet(left, typ, objGet(right, typ));
       }
     }
   }
+  return all;
 }
 
 export JsValue combineExtensions(JsValue extensions) {
-  JsValue all = JS.object();
-  int index = -1;
-  int n = arrLen(extensions);
-  while (true) {
-    index = index + 1;
-    if (index >= n) {
-      break;
-    }
-    syntaxExtension(all, extensions[index]);
-  }
-  return all;
+  return combine(extensions, false);
 }
 
 export JsValue combineHtmlExtensions(JsValue htmlExtensions) {
-  JsValue handlers = JS.object();
-  int index = -1;
-  int n = arrLen(htmlExtensions);
-  while (true) {
-    index = index + 1;
-    if (index >= n) {
-      break;
-    }
-    htmlExtension(handlers, htmlExtensions[index]);
-  }
-  return handlers;
+  return combine(htmlExtensions, true);
 }
diff -ruN a/src/graph/remark-parse/micromark/util-decode-numeric.lil b/src/graph/remark-parse/micromark/util-decode-numeric.lil
--- a/src/graph/remark-parse/micromark/util-decode-numeric.lil	2026-09-23 20:40:45.213681373 +0000
+++ b/src/graph/remark-parse/micromark/util-decode-numeric.lil	2026-09-23 20:40:45.336447014 +0000
@@ -1,33 +1,20 @@
-import { fromCodePoint } from "./host.lil";
-import { parseIntBase } from "./host.lil";
-import { cCr, cHt, cSpace, cTilde, cVt } from "./symbol/codes.lil";
-import { vReplacementCharacter } from "./symbol/values.lil";
+extern JsValue Number;
+extern JsValue String;
 
 export string decodeNumericCharacterReference(string value, int base) {
-  float codeF = parseIntBase(value, base);
-  int code = codeF.toInt();
-  bool unsafe = false;
-  if (code < cHt) {
-    unsafe = true;
-  } else if (code == cVt) {
-    unsafe = true;
-  } else if (code > cCr && code < cSpace) {
-    unsafe = true;
-  } else if (code > cTilde && code < 160) {
-    unsafe = true;
-  } else if (code > 55295 && code < 57344) {
-    unsafe = true;
-  } else if (code > 64975 && code < 65008) {
-    unsafe = true;
-  } else if ((code & 65535) == 65535) {
-    unsafe = true;
-  } else if ((code & 65535) == 65534) {
-    unsafe = true;
-  } else if (code > 1114111) {
-    unsafe = true;
+  int code = JS.number(JS.call(Number["parseInt"], Number, value, base)).toInt();
+  if (
+    code < 9 ||
+    code == 11 ||
+    (code > 13 && code < 32) ||
+    (code > 126 && code < 160) ||
+    (code > 55295 && code < 57344) ||
+    (code > 64975 && code < 65008) ||
+    (code & 65535) == 65535 ||
+    (code & 65535) == 65534 ||
+    code > 1114111
+  ) {
+    return "�";
   }
-  if (unsafe) {
-    return vReplacementCharacter;
-  }
-  return fromCodePoint(code);
+  return JS.assume(JS.call(String["fromCodePoint"], String, code));
 }
diff -ruN a/src/graph/remark-parse/micromark/util-decode-string.lil b/src/graph/remark-parse/micromark/util-decode-string.lil
--- a/src/graph/remark-parse/micromark/util-decode-string.lil	2026-09-23 20:40:45.214051357 +0000
+++ b/src/graph/remark-parse/micromark/util-decode-string.lil	2026-09-23 20:40:45.336757676 +0000
@@ -1,39 +1,23 @@
 import { decodeNamedCharacterReference } from "./decode-named.lil";
 import { decodeNumericCharacterReference } from "./util-decode-numeric.lil";
-import { cLowercaseX, cNumberSign, cUppercaseX, codeEq } from "./symbol/codes.lil";
-import { kNumericBaseDecimal, kNumericBaseHexadecimal } from "./symbol/constants.lil";
-import { jsString } from "./host.lil";
 
 Regex characterEscapeOrReference = new Regex("\\\\([!-/:-@[-`{-~])|&(#(?:\\d{1,7}|x[\\da-f]{1,6})|[\\da-z]{1,31});", "gi");
 
-JsValue decodeOne(JsValue _self, JsValue args) {
-  string whole = jsString(args[0]);
-  JsValue escapePart = args[1];
-  JsValue refPart = args[2];
-  if (escapePart.truthy()) {
-    return escapePart;
-  }
-  string ref = jsString(refPart);
-  int head = ref.charCodeAt(0);
-  if (head == cNumberSign) {
-    int second = ref.charCodeAt(1);
-    bool hex = second == cLowercaseX || second == cUppercaseX;
-    string digits = "";
-    if (hex) {
-      digits = ref.slice(2);
-      return decodeNumericCharacterReference(digits, kNumericBaseHexadecimal);
+export string decodeString(string value) {
+  return JS.assume(JS.call(JS.get(value, "replace"), value, characterEscapeOrReference, JS.method3((JsValue _s, JsValue whole, JsValue escape, JsValue reference) => {
+    if (escape.truthy()) {
+      return escape;
+    }
+    string ref = JS.assume(reference);
+    if (ref.charCodeAt(0) == 35) {
+      int head = ref.charCodeAt(1);
+      bool hex = head == 120 || head == 88;
+      return decodeNumericCharacterReference(ref.slice(if (hex) { 2 } else { 1 }), if (hex) { 16 } else { 10 });
+    }
+    JsValue named = decodeNamedCharacterReference(ref);
+    if (!named.truthy()) {
+      named = whole;
     }
-    digits = ref.slice(1);
-    return decodeNumericCharacterReference(digits, kNumericBaseDecimal);
-  }
-  JsValue named = decodeNamedCharacterReference(ref);
-  if (named.truthy()) {
     return named;
-  }
-  return whole;
-}
-
-export string decodeString(string value) {
-  JsValue replacer = JS.methodRest(decodeOne);
-  return jsString(JS.call(JS.get(value, "replace"), value, characterEscapeOrReference, replacer));
+  })));
 }
diff -ruN a/src/graph/remark-parse/micromark/util-resolve-all.lil b/src/graph/remark-parse/micromark/util-resolve-all.lil
--- a/src/graph/remark-parse/micromark/util-resolve-all.lil	2026-09-23 20:40:45.222369532 +0000
+++ b/src/graph/remark-parse/micromark/util-resolve-all.lil	2026-09-23 20:40:45.337763481 +0000
@@ -1,19 +1,11 @@
-import { arrLen, includesVal, isFn, objGet, push } from "./host.lil";
-
 export JsValue resolveAll(JsValue constructs, JsValue events, JsValue context) {
-  JsValue called = JS.array();
-  int index = -1;
-  int n = arrLen(constructs);
+  JsValue[] called = [];
   JsValue next = events;
-  while (true) {
-    index = index + 1;
-    if (index >= n) {
-      break;
-    }
-    JsValue resolve = objGet(constructs[index], "resolveAll");
-    if (isFn(resolve) && !includesVal(called, resolve)) {
+  for (float index = 0.0; index < constructs.length; index++) {
+    JsValue resolve = JS.get(constructs[index], "resolveAll");
+    if (resolve.truthy() && !called.includes(resolve)) {
       next = JS.call(resolve, JS.undefined(), next, context);
-      push(called, resolve);
+      called.push(resolve);
     }
   }
   return next;
diff -ruN a/src/graph/remark-parse/micromark/util-subtokenize.lil b/src/graph/remark-parse/micromark/util-subtokenize.lil
--- a/src/graph/remark-parse/micromark/util-subtokenize.lil	2026-09-23 20:40:45.222837559 +0000
+++ b/src/graph/remark-parse/micromark/util-subtokenize.lil	2026-09-23 20:40:45.338234343 +0000
@@ -1,240 +1,178 @@
-import {
-  arrLen,
-  call1,
-  call2,
-  copyPoint,
-  jsInt,
-  jsString,
-  objGet,
-  objectAssign,
-  push,
-  slice2,
-  undef,
-  unshift
-} from "./host.lil";
+import { copyPoint } from "./host.lil";
 import { splice } from "./util-chunked.lil";
 import { SpliceBuffer } from "./splice-buffer.lil";
-import { codeEof } from "./host.lil";
-import { tChunkFlow, tChunkText, tContent, tLineEnding, tLineEndingBlank, tLinePrefix, tListItemIndent, tListItemPrefix } from "./symbol/types.lil";
 
-export { SpliceBuffer };
-
-JsValue sbGet(JsValue events, int index) {
-  return call1(objGet(events, "get"), events, index);
-}
-
-JsValue sbSlice(JsValue events, JsValue start, JsValue end) {
-  return call2(objGet(events, "slice"), events, start, end);
-}
+extern JsValue Object;
 
-void sbSplice(JsValue events, JsValue start, JsValue remove, JsValue items) {
-  JS.call(objGet(events, "splice"), events, start, remove, items);
-}
+export { SpliceBuffer };
 
-int sbLen(JsValue events) {
-  return jsInt(objGet(events, "length"));
+JsValue typeOf(JsValue event) {
+  return JS.get(event[1], "type");
 }
 
-JsValue subcontent(JsValue events, int eventIndex) {
-  JsValue ev = sbGet(events, eventIndex);
-  JsValue token = ev[1];
-  JsValue context = ev[2];
-  int startPosition = eventIndex - 1;
-  JsValue startPositions = JS.array();
-  JsValue tokenizer = objGet(token, "_tokenizer");
+JsValue subcontent(JsValue events, float eventIndex) {
+  JsValue event = JS.invoke(events, "get", eventIndex);
+  JsValue token = event[1];
+  JsValue context = event[2];
+  float startPosition = eventIndex - 1.0;
+  JsValue[] startPositions = [];
+  JsValue tokenizer = JS.get(token, "_tokenizer");
   if (!tokenizer.truthy()) {
-    JsValue parser = objGet(context, "parser");
-    JsValue contentType = objGet(token, "contentType");
-    tokenizer = call1(objGet(parser, jsString(contentType)), parser, objGet(token, "start"));
-    if (objGet(token, "_contentTypeTextTrailing").truthy()) {
+    tokenizer = JS.invoke(JS.get(context, "parser"), JS.assume(JS.get(token, "contentType")), JS.get(token, "start"));
+    if (JS.get(token, "_contentTypeTextTrailing").truthy()) {
       JS.set(tokenizer, "_contentTypeTextTrailing", true);
     }
   }
-  JsValue childEvents = objGet(tokenizer, "events");
+  JsValue childEvents = JS.get(tokenizer, "events");
   JsValue jumps = JS.array();
   JsValue gaps = JS.object();
-  JsValue previous = undef();
-  int index = -1;
+  JsValue previous = JS.undefined();
   JsValue current = token;
-  int adjust = 0;
-  int start = 0;
-  JsValue breaks = JS.array();
-  push(breaks, start);
+  float adjust = 0.0;
+  JsValue breaks = JS.array(0);
 
   while (current.truthy()) {
     while (true) {
-      startPosition = startPosition + 1;
-      if (sbGet(events, startPosition)[1] == current) {
+      startPosition++;
+      if (JS.strictEqual(JS.invoke(events, "get", startPosition)[1], current)) {
         break;
       }
     }
-    push(startPositions, startPosition);
-    if (!objGet(current, "_tokenizer").truthy()) {
-      JsValue stream = call1(objGet(context, "sliceStream"), context, current);
-      if (!objGet(current, "next").truthy()) {
-        push(stream, codeEof);
+    startPositions.push(startPosition);
+    if (!JS.get(current, "_tokenizer").truthy()) {
+      JsValue stream = JS.invoke(context, "sliceStream", current);
+      if (!JS.get(current, "next").truthy()) {
+        JS.invoke(stream, "push", null);
       }
       if (previous.truthy()) {
-        call1(objGet(tokenizer, "defineSkip"), tokenizer, objGet(current, "start"));
+        JS.invoke(tokenizer, "defineSkip", JS.get(current, "start"));
       }
-      if (objGet(current, "_isInFirstContentOfListItem").truthy()) {
+      if (JS.get(current, "_isInFirstContentOfListItem").truthy()) {
         JS.set(tokenizer, "_gfmTasklistFirstContentOfListItem", true);
       }
-      call1(objGet(tokenizer, "write"), tokenizer, stream);
-      if (objGet(current, "_isInFirstContentOfListItem").truthy()) {
-        JS.set(tokenizer, "_gfmTasklistFirstContentOfListItem", undef());
+      JS.invoke(tokenizer, "write", stream);
+      if (JS.get(current, "_isInFirstContentOfListItem").truthy()) {
+        JS.set(tokenizer, "_gfmTasklistFirstContentOfListItem", JS.undefined());
       }
     }
     previous = current;
-    current = objGet(current, "next");
+    current = JS.get(current, "next");
   }
 
   current = token;
-  int childLen = arrLen(childEvents);
-  while (true) {
-    index = index + 1;
-    if (index >= childLen) {
-      break;
-    }
+  for (float index = 0.0; index < childEvents.length; index++) {
+    JsValue exit = childEvents[index];
+    JsValue enter = childEvents[index - 1.0];
     if (
-      childEvents[index][0] == "exit" &&
-      childEvents[index - 1][0] == "enter" &&
-      objGet(childEvents[index][1], "type") == objGet(childEvents[index - 1][1], "type") &&
-      objGet(objGet(childEvents[index][1], "start"), "line") != objGet(objGet(childEvents[index][1], "end"), "line")
+      JS.strictEqual(exit[0], "exit") &&
+      JS.strictEqual(enter[0], "enter") &&
+      JS.strictEqual(typeOf(exit), typeOf(enter)) &&
+      !JS.strictEqual(JS.get(JS.get(exit[1], "start"), "line"), JS.get(JS.get(exit[1], "end"), "line"))
     ) {
-      start = index + 1;
-      push(breaks, start);
-      JS.set(current, "_tokenizer", undef());
-      JS.set(current, "previous", undef());
-      current = objGet(current, "next");
+      JS.invoke(breaks, "push", index + 1.0);
+      JS.set(current, "_tokenizer", JS.undefined());
+      JS.set(current, "previous", JS.undefined());
+      current = JS.get(current, "next");
     }
   }
 
   JS.set(tokenizer, "events", JS.array());
 
   if (current.truthy()) {
-    JS.set(current, "_tokenizer", undef());
-    JS.set(current, "previous", undef());
+    JS.set(current, "_tokenizer", JS.undefined());
+    JS.set(current, "previous", JS.undefined());
   } else {
-    JS.call(objGet(breaks, "pop"), breaks);
+    JS.invoke(breaks, "pop");
   }
 
-  index = arrLen(breaks);
-  while (true) {
-    index = index - 1;
-    if (index < 0) {
-      break;
-    }
-    JsValue endBreak = undef();
-    if (index + 1 < arrLen(breaks)) {
-      endBreak = breaks[index + 1];
-    }
-    JsValue sl = slice2(childEvents, breaks[index], endBreak);
-    int startPos = jsInt(JS.call(objGet(startPositions, "pop"), startPositions));
-    JsValue jump = JS.array();
-    push(jump, startPos);
-    push(jump, startPos + arrLen(sl) - 1);
-    push(jumps, jump);
-    sbSplice(events, startPos, 2, sl);
-  }
-
-  JS.call(objGet(jumps, "reverse"), jumps);
-  index = -1;
-  int jumpLen = arrLen(jumps);
-  while (true) {
-    index = index + 1;
-    if (index >= jumpLen) {
-      break;
-    }
-    JsValue jump = jumps[index];
-    int jumpStart = jsInt(jump[0]);
-    int jumpEnd = jsInt(jump[1]);
-    JS.set(gaps, JS.string(adjust + jumpStart), adjust + jumpEnd);
-    adjust = adjust + jumpEnd - jumpStart - 1;
+  float index = breaks.length;
+  while (index > 0.0) {
+    index--;
+    JsValue slice = JS.invoke(childEvents, "slice", breaks[index], breaks[index + 1.0]);
+    JsValue start = startPositions.pop();
+    float first = JS.assume(start);
+    JS.invoke(jumps, "push", JS.array(start, first + slice.length - 1.0));
+    JS.invoke(events, "splice", start, 2, slice);
+  }
+
+  JS.invoke(jumps, "reverse");
+  for (float jump = 0.0; jump < jumps.length; jump++) {
+    float jumpStart = JS.assume(jumps[jump][0]);
+    float jumpEnd = JS.assume(jumps[jump][1]);
+    gaps[adjust + jumpStart] = adjust + jumpEnd;
+    adjust = adjust + jumpEnd - jumpStart - 1.0;
   }
   return gaps;
 }
 
 export bool subtokenize(JsValue eventsArray) {
   JsValue jumps = JS.object();
-  int index = -1;
-  JsValue events = JS.construct(SpliceBuffer, eventsArray);
   bool more = false;
+  JsValue events = JS.construct(SpliceBuffer, eventsArray);
 
-  while (true) {
-    index = index + 1;
-    if (index >= sbLen(events)) {
-      break;
+  for (float index = 0.0; index < events.length; index++) {
+    while (JS.in(index, jumps)) {
+      index = JS.assume(jumps[index]);
     }
-    while (JS.has(jumps, JS.string(index))) {
-      index = jsInt(objGet(jumps, JS.string(index)));
-    }
-    JsValue event = sbGet(events, index);
+    JsValue event = JS.invoke(events, "get", index);
     if (
-      index > 0 &&
-      objGet(event[1], "type") == tChunkFlow &&
-      objGet(sbGet(events, index - 1)[1], "type") == tListItemPrefix
+      index != 0.0 &&
+      JS.strictEqual(typeOf(event), "chunkFlow") &&
+      JS.strictEqual(typeOf(JS.invoke(events, "get", index - 1.0)), "listItemPrefix")
     ) {
-      JsValue subevents = objGet(objGet(event[1], "_tokenizer"), "events");
-      int otherIndex = 0;
-      if (otherIndex < arrLen(subevents) && objGet(subevents[otherIndex][1], "type") == tLineEndingBlank) {
-        otherIndex = otherIndex + 2;
+      JsValue subevents = JS.get(JS.get(event[1], "_tokenizer"), "events");
+      float otherIndex = 0.0;
+      if (otherIndex < subevents.length && JS.strictEqual(typeOf(subevents[otherIndex]), "lineEndingBlank")) {
+        otherIndex = otherIndex + 2.0;
       }
-      if (otherIndex < arrLen(subevents) && objGet(subevents[otherIndex][1], "type") == tContent) {
+      if (otherIndex < subevents.length && JS.strictEqual(typeOf(subevents[otherIndex]), "content")) {
         while (true) {
-          otherIndex = otherIndex + 1;
-          if (otherIndex >= arrLen(subevents)) {
+          otherIndex++;
+          if (otherIndex >= subevents.length || JS.strictEqual(typeOf(subevents[otherIndex]), "content")) {
             break;
           }
-          if (objGet(subevents[otherIndex][1], "type") == tContent) {
-            break;
-          }
-          if (objGet(subevents[otherIndex][1], "type") == tChunkText) {
+          if (JS.strictEqual(typeOf(subevents[otherIndex]), "chunkText")) {
             JS.set(subevents[otherIndex][1], "_isInFirstContentOfListItem", true);
-            otherIndex = otherIndex + 1;
+            otherIndex++;
           }
         }
       }
     }
 
-    if (event[0] == "enter") {
-      if (objGet(event[1], "contentType").truthy()) {
-        objectAssign(jumps, subcontent(events, index));
-        index = jsInt(objGet(jumps, JS.string(index)));
+    if (JS.strictEqual(event[0], "enter")) {
+      if (JS.get(event[1], "contentType").truthy()) {
+        JS.call(JS.get(Object, "assign"), Object, jumps, subcontent(events, index));
+        index = JS.assume(jumps[index]);
         more = true;
       }
-    } else if (objGet(event[1], "_container").truthy()) {
-      int otherIndex = index;
-      int lineIndex = 0;
-      while (true) {
-        otherIndex = otherIndex - 1;
-        if (otherIndex < 0) {
-          break;
-        }
-        JsValue otherEvent = sbGet(events, otherIndex);
-        string otherType = jsString(objGet(otherEvent[1], "type"));
-        if (otherType == tLineEnding || otherType == tLineEndingBlank) {
-          if (otherEvent[0] == "enter") {
-            if (lineIndex > 0) {
-              JS.set(sbGet(events, lineIndex)[1], "type", tLineEndingBlank);
+    } else if (JS.get(event[1], "_container").truthy()) {
+      float otherIndex = index;
+      float lineIndex = 0.0;
+      while (otherIndex > 0.0) {
+        otherIndex--;
+        JsValue otherEvent = JS.invoke(events, "get", otherIndex);
+        JsValue otherType = typeOf(otherEvent);
+        if (JS.strictEqual(otherType, "lineEnding") || JS.strictEqual(otherType, "lineEndingBlank")) {
+          if (JS.strictEqual(otherEvent[0], "enter")) {
+            if (lineIndex != 0.0) {
+              JS.set(JS.invoke(events, "get", lineIndex)[1], "type", "lineEndingBlank");
             }
-            JS.set(otherEvent[1], "type", tLineEnding);
+            JS.set(otherEvent[1], "type", "lineEnding");
             lineIndex = otherIndex;
           }
-        } else if (otherType == tLinePrefix || otherType == tListItemIndent) {
-        } else {
+        } else if (!(JS.strictEqual(otherType, "linePrefix") || JS.strictEqual(otherType, "listItemIndent"))) {
           break;
         }
       }
-      if (lineIndex > 0) {
-        JS.set(event[1], "end", copyPoint(objGet(sbGet(events, lineIndex)[1], "start")));
-        JsValue parameters = sbSlice(events, lineIndex, index);
-        unshift(parameters, event);
-        sbSplice(events, lineIndex, index - lineIndex + 1, parameters);
+      if (lineIndex != 0.0) {
+        JS.set(event[1], "end", copyPoint(JS.get(JS.invoke(events, "get", lineIndex)[1], "start")));
+        JsValue parameters = JS.invoke(events, "slice", lineIndex, index);
+        JS.invoke(parameters, "unshift", event);
+        JS.invoke(events, "splice", lineIndex, index - lineIndex + 1.0, parameters);
       }
     }
   }
 
-  splice(eventsArray, 0, 1.0 / 0.0, sbSlice(events, 0, undef()));
+  splice(eventsArray, 0, 1.0 / 0.0, JS.invoke(events, "slice", 0));
   return !more;
 }
diff -ruN a/src/graph/remark-parse/stringify-position.lil b/src/graph/remark-parse/stringify-position.lil
--- a/src/graph/remark-parse/stringify-position.lil	2026-09-23 20:40:45.145691562 +0000
+++ b/src/graph/remark-parse/stringify-position.lil	2026-09-23 20:40:45.338609716 +0000
@@ -1,38 +1,13 @@
-import { hasOwn, isNullish, jsString, objGet } from "./micromark/host.lil";
-
-string indexPart(JsValue value) {
-  if (value.truthy() && JS.typeOf(value) == "number") {
-    return jsString(value);
-  }
-  return "1";
-}
-
-string pointPart(JsValue pt) {
-  if (isNullish(pt)) {
-    return indexPart(JS.undefined()) + ":" + indexPart(JS.undefined());
-  }
-  return indexPart(objGet(pt, "line")) + ":" + indexPart(objGet(pt, "column"));
+// unist-util-stringify-position, as this package uses it: every caller passes a token, and upstream
+// builds `{start: token.start, end: token.end}` for it, which always takes the `start`/`end` branch.
+JsValue index(JsValue value) {
+  return if (value.truthy() && JS.typeOf(value) == "number") { value } else { 1 };
 }
 
-string positionPart(JsValue pos) {
-  if (isNullish(pos)) {
-    return pointPart(JS.undefined()) + "-" + pointPart(JS.undefined());
-  }
-  return pointPart(objGet(pos, "start")) + "-" + pointPart(objGet(pos, "end"));
+string point(JsValue value) {
+  return index(JS.and(value, value["line"])) + ":" + index(JS.and(value, value["column"]));
 }
 
-export string stringifyPosition(JsValue value) {
-  if (!value.truthy() || JS.typeOf(value) != "object") {
-    return "";
-  }
-  if (hasOwn(value, "position") || hasOwn(value, "type")) {
-    return positionPart(objGet(value, "position"));
-  }
-  if (hasOwn(value, "start") || hasOwn(value, "end")) {
-    return positionPart(value);
-  }
-  if (hasOwn(value, "line") || hasOwn(value, "column")) {
-    return pointPart(value);
-  }
-  return "";
+export string stringifyPosition(JsValue token) {
+  return point(token["start"]) + "-" + point(token["end"]);
 }
diff -ruN a/src/graph/remark-parse/to-string.lil b/src/graph/remark-parse/to-string.lil
--- a/src/graph/remark-parse/to-string.lil	2026-09-23 20:40:45.223623512 +0000
+++ b/src/graph/remark-parse/to-string.lil	2026-09-23 20:40:45.338693261 +0000
@@ -1,55 +1,26 @@
-import { arrLen, hasOwn, isNullish, jsString, objGet, optionOrEmpty } from "./micromark/host.lil";
-
-JsValue walkOne = JS.undefined();
-
-string walkAll(JsValue values, JsValue includeImageAlt, JsValue includeHtml) {
-  string out = "";
-  int index = 0;
-  int n = arrLen(values);
-  while (index < n) {
-    out = out + jsString(JS.call(walkOne, JS.undefined(), values[index], includeImageAlt, includeHtml));
-    index = index + 1;
-  }
-  return out;
-}
-
-JsValue initWalk() {
-  walkOne = JS.method3((JsValue _self, JsValue value, JsValue includeImageAlt, JsValue includeHtml) => {
-    if (value.truthy() && JS.typeOf(value) == "object") {
-      if (hasOwn(value, "value")) {
-        if (jsString(objGet(value, "type")) == "html" && !includeHtml.truthy()) {
-          return "";
-        }
-        return objGet(value, "value");
-      }
-      if (includeImageAlt.truthy() && hasOwn(value, "alt") && objGet(value, "alt").truthy()) {
-        return objGet(value, "alt");
-      }
-      if (hasOwn(value, "children")) {
-        return walkAll(objGet(value, "children"), includeImageAlt, includeHtml);
-      }
+// mdast-util-to-string, as `resume` calls it: no options, so `includeImageAlt` and `includeHtml` are both true.
+export JsValue toString(JsValue value) {
+  if (value.truthy() && JS.typeOf(value) == "object") {
+    if (JS.in("value", value)) {
+      return value["value"];
     }
-    if (!isNullish(value) && value.isArray()) {
-      return walkAll(value, includeImageAlt, includeHtml);
+    if (JS.in("alt", value) && value["alt"].truthy()) {
+      return value["alt"];
     }
-    return "";
-  });
-  return walkOne;
+    if (JS.in("children", value)) {
+      return all(value["children"]);
+    }
+  }
+  if (value.isArray()) {
+    return all(value);
+  }
+  return "";
 }
 
-JsValue walker = initWalk();
-
-export string mdastToString(JsValue value, JsValue options) {
-  JsValue settings = optionOrEmpty(options);
-  JsValue includeImageAlt = true;
-  JsValue includeHtml = true;
-  JsValue rawAlt = objGet(settings, "includeImageAlt");
-  JsValue rawHtml = objGet(settings, "includeHtml");
-  if (JS.typeOf(rawAlt) == "boolean") {
-    includeImageAlt = rawAlt;
-  }
-  if (JS.typeOf(rawHtml) == "boolean") {
-    includeHtml = rawHtml;
+JsValue all(JsValue values) {
+  JsValue[] result = [];
+  for (float index = 0; index < values.length; index++) {
+    result.push(toString(values[index]));
   }
-  return jsString(JS.call(walker, JS.undefined(), value, includeImageAlt, includeHtml));
+  return JS.invoke(result, "join", "");
 }
diff -ruN a/src/graph/unified/entry.lil b/src/graph/unified/entry.lil
--- a/src/graph/unified/entry.lil	2026-09-23 20:40:45.115691299 +0000
+++ b/src/graph/unified/entry.lil	2026-09-23 20:40:45.256692540 +0000
@@ -1,22 +1,25 @@
 import {
-  Number,
+  Array,
+  Error,
   Object,
   Promise,
   String,
   TypeError,
   bail,
+  defineMembers,
+  finishClass,
   isFn,
   looksLikeAValue,
-  objectHasOwn,
-  sliceArgs,
-  throwError,
-  throwType
+  requireNew
 } from "./host.lil";
-import { isPlainObj } from "./plain.lil";
-import { cloneNamespace, extendWith } from "./extend.lil";
+import { hasOwnProperty, isPlainObj } from "./plain.lil";
+import { extendWith } from "./extend.lil";
 import { trough } from "./trough.lil";
-import { VFile, asVFile } from "./vfile.lil";
+import { VFile } from "./vfile.lil";
 
+// unified@11 over host objects: `Processor` is a callable instance (`processor()` copies it), its
+// methods live on `Processor.prototype` with class-method attributes, and plugins read and write the
+// members below by name.
 export extern class ProcessorApi {
   JsValue copy;
   JsValue use;
@@ -33,23 +36,46 @@
   JsValue compiler;
   JsValue Parser;
   JsValue Compiler;
+  JsValue namespace;
+  JsValue frozen;
+  JsValue freezeIndex;
+  JsValue transformers;
+}
+
+extern class UnifiedPresetView {
+  JsValue plugins;
+  JsValue settings;
+}
+
+extern class UnifiedFileView {
+  JsValue value;
+  JsValue result;
+}
+
+bool looksLikeAVFile(JsValue value) {
+  return value.truthy() && JS.typeOf(value) == "object" && JS.in("message", value) && JS.in("messages", value);
+}
+
+JsValue asVFile(JsValue value) {
+  return if (looksLikeAVFile(value)) { value } else { JS.construct(VFile, value) };
 }
 
 void assertParser(string name, JsValue value) {
   if (!isFn(value)) {
-    throwType("Cannot `" + name + "` without `parser`");
+    throw JS.construct(TypeError, "Cannot `" + name + "` without `parser`");
   }
 }
 
 void assertCompiler(string name, JsValue value) {
   if (!isFn(value)) {
-    throwType("Cannot `" + name + "` without `compiler`");
+    throw JS.construct(TypeError, "Cannot `" + name + "` without `compiler`");
   }
 }
 
 void assertUnfrozen(string name, JsValue frozen) {
   if (frozen.truthy()) {
-    throwError(
+    throw JS.construct(
+      Error,
       "Cannot call `" +
         name +
         "` on a frozen processor.\nCreate a new processor first, by calling it: use `processor()` instead of `processor`."
@@ -58,416 +84,271 @@
 }
 
 void assertNode(JsValue node) {
-  if (!isPlainObj(node) || JS.typeOf(JS.get(node, "type")) != "string") {
-    throwType("Expected node, got `" + JS.string(node) + "`");
+  if (!isPlainObj(node) || !(JS.get(node, "type") is string)) {
+    throw JS.construct(TypeError, "Expected node, got `" + node + "`");
   }
 }
 
 void assertDone(string name, string asyncName, bool complete) {
   if (!complete) {
-    throwError("`" + name + "` finished async. Use `" + asyncName + "` instead");
+    throw JS.construct(Error, "`" + name + "` finished async. Use `" + asyncName + "` instead");
   }
 }
 
-JsValue pickParser(JsValue self) {
-  JsValue parser = JS.get(self, "parser");
-  if (!parser.truthy()) {
-    parser = JS.get(self, "Parser");
+// `use(value, ...parameters)` (top) and the per-item `add(value)` of a list share one dispatch:
+// a list at the top level is a list of plugins, inside a list it is `[plugin, ...parameters]`.
+void addValue(JsValue attachers, JsValue namespace, JsValue value, JsValue parameters, bool top) {
+  if (isFn(value)) {
+    addPlugin(attachers, value, parameters);
+  } else if (JS.typeOf(value) == "object") {
+    if (!value.isArray()) {
+      UnifiedPresetView preset = JS.assume(value);
+      if (!JS.in("plugins", value) && !JS.in("settings", value)) {
+        throw JS.construct(
+          Error,
+          "Expected usable value but received an empty preset, which is probably a mistake: presets typically come with `plugins` and sometimes with `settings`, but this has neither"
+        );
+      }
+      addList(attachers, namespace, preset.plugins);
+      if (preset.settings.truthy()) {
+        UnifiedPresetView settings = JS.assume(namespace);
+        settings.settings = extendWith(settings.settings, preset.settings);
+      }
+    } else if (top) {
+      addList(attachers, namespace, value);
+    } else {
+      addPlugin(attachers, value[0], JS.invoke(value, "slice", 1.0));
+    }
+  } else {
+    throw JS.construct(TypeError, "Expected usable value, not `" + value + "`");
   }
-  return parser;
 }
 
-JsValue pickCompiler(JsValue self) {
-  JsValue compiler = JS.get(self, "compiler");
-  if (!compiler.truthy()) {
-    compiler = JS.get(self, "Compiler");
+void addList(JsValue attachers, JsValue namespace, JsValue plugins) {
+  if (!JS.isNullish(plugins)) {
+    if (!plugins.isArray()) {
+      throw JS.construct(TypeError, "Expected a list of plugins, not `" + plugins + "`");
+    }
+    for (float index = 0; index < plugins.length; index++) {
+      addValue(attachers, namespace, plugins[index], JS.array(), false);
+    }
   }
-  return compiler;
 }
 
-void addPlugin(JsValue[] attachers, JsValue plugin, JsValue parameters) {
-  int index = -1;
-  int entryIndex = -1;
-  int n = attachers.length;
-  while (true) {
-    index = index + 1;
-    if (index >= n) {
-      break;
-    }
+void addPlugin(JsValue attachers, JsValue plugin, JsValue parameters) {
+  float entryIndex = -1;
+  for (float index = 0; index < attachers.length; index++) {
     if (JS.strictEqual(attachers[index][0], plugin)) {
       entryIndex = index;
       break;
     }
   }
-  if (entryIndex == -1) {
-    JsValue[] entry = sliceArgs(parameters, 0);
-    JS.call(JS.get(entry, "unshift"), entry, plugin);
-    attachers.push(entry);
-    return;
-  }
-  if (parameters.length.toInt() > 0) {
+  if (entryIndex > -1) {
+    if (parameters.length == 0.0) return;
     JsValue primary = parameters[0];
-    JsValue[] rest = sliceArgs(parameters, 1);
     JsValue currentPrimary = attachers[entryIndex][1];
-    if (isPlainObj(currentPrimary) && isPlainObj(primary)) {
-      primary = extendWith(currentPrimary, primary);
-    }
-    JsValue[] entry = [];
-    entry.push(plugin);
-    entry.push(primary);
-    int r = 0;
-    int rn = rest.length;
-    while (r < rn) {
-      entry.push(rest[r]);
-      r = r + 1;
-    }
-    JS.splice(attachers, entryIndex * 1.0, 1.0, entry);
+    if (isPlainObj(currentPrimary) && isPlainObj(primary)) parameters[0] = extendWith(currentPrimary, primary);
   }
-}
-
-void addList(JsValue[] attachers, JsValue namespace, JsValue plugins) {
-  if (JS.isNullish(plugins)) {
-    return;
+  JsValue entry = JS.invoke(JS.array(plugin), "concat", parameters);
+  if (entryIndex < 0) {
+    JS.invoke(attachers, "push", entry);
+  } else {
+    attachers[entryIndex] = entry;
   }
-  if (!plugins.isArray()) {
-    throwType("Expected a list of plugins, not `" + JS.string(plugins) + "`");
-  }
-  int index = -1;
-  int n = plugins.length.toInt();
-  while (true) {
-    index = index + 1;
-    if (index >= n) {
-      break;
-    }
-    addOne(attachers, namespace, plugins[index]);
-  }
-}
-
-void addPreset(JsValue[] attachers, JsValue namespace, JsValue result) {
-  if (!JS.in("plugins", result) && !JS.in("settings", result)) {
-    throwError(
-      "Expected usable value but received an empty preset, which is probably a mistake: presets typically come with `plugins` and sometimes with `settings`, but this has neither"
-    );
-  }
-  addList(attachers, namespace, JS.get(result, "plugins"));
-  JsValue settings = JS.get(result, "settings");
-  if (settings.truthy()) {
-    JS.set(namespace, "settings", extendWith(JS.get(namespace, "settings"), settings));
-  }
-}
-
-void addOne(JsValue[] attachers, JsValue namespace, JsValue value) {
-  if (isFn(value)) {
-    addPlugin(attachers, value, JS.array());
-    return;
-  }
-  if (JS.typeOf(value) == "object") {
-    if (value.isArray()) {
-      addPlugin(attachers, value[0], sliceArgs(value, 1));
-      return;
-    }
-    addPreset(attachers, namespace, value);
-    return;
-  }
-  throwType("Expected usable value, not `" + JS.string(value) + "`");
 }
 
-JsValue useCall(JsValue self, JsValue args) {
-  assertUnfrozen("use", JS.get(self, "frozen"));
-  JsValue[] attachers = JS.assume(JS.get(self, "attachers"));
-  JsValue namespace = JS.get(self, "namespace");
-  int n = args.length.toInt();
-  JsValue value = JS.undefined();
-  if (n > 0) {
-    value = args[0];
-  }
-  if (JS.isNullish(value)) {
-    return self;
-  }
-  if (isFn(value)) {
-    addPlugin(attachers, value, sliceArgs(args, 1));
-    return self;
-  }
-  if (JS.typeOf(value) == "object") {
-    if (value.isArray()) {
-      addList(attachers, namespace, value);
-    } else {
-      addPreset(attachers, namespace, value);
-    }
-    return self;
-  }
-  throw JS.construct(TypeError, "Expected usable value, not `" + JS.string(value) + "`");
-}
+JsValue executeWith(JsValue executor, JsValue done) {
+  return if (done.truthy()) { JS.call(executor, JS.undefined(), JS.undefined(), done) } else { JS.construct(Promise, executor) };
+}
+
+JsValue Processor = JS.method0((JsValue self) => {
+  requireNew(self, "Processor");
+  // CallableInstance: calling the processor applies the prototype's `copy` (as it was at construction).
+  JsValue proto = JS.get(Processor, "prototype");
+  JsValue copy = JS.get(proto, "copy");
+  JsValue processor = JS.undefined();
+  processor = JS.staticRest((JsValue parameters) => JS.apply(copy, processor, parameters));
+  return JS.invoke(Object, "assign", JS.invoke(Object, "setPrototypeOf", processor, proto), JS.object(
+    "Compiler", JS.undefined(),
+    "Parser", JS.undefined(),
+    "attachers", JS.array(),
+    "compiler", JS.undefined(),
+    "freezeIndex", -1.0,
+    "frozen", JS.undefined(),
+    "namespace", JS.object(),
+    "parser", JS.undefined(),
+    "transformers", trough()
+  ));
+});
+finishClass(Processor, "Processor");
 
-JsValue dataCall(JsValue self, JsValue args) {
-  JsValue namespace = JS.get(self, "namespace");
-  int n = args.length.toInt();
-  JsValue key = JS.undefined();
-  if (n > 0) {
-    key = args[0];
-  }
+JsValue dataMethod = JS.methodRest((JsValue self, JsValue parameters) => {
+  ProcessorApi processor = JS.assume(self);
+  JsValue key = parameters[0];
   if (key is string) {
-    if (n == 2) {
-      assertUnfrozen("data", JS.get(self, "frozen"));
-      JS.set(namespace, key, args[1]);
+    if (parameters.length == 2.0) {
+      assertUnfrozen("data", processor.frozen);
+      JS.set(processor.namespace, key, parameters[1]);
       return self;
     }
-    if (objectHasOwn(namespace, key)) {
-      JsValue got = JS.get(namespace, key);
-      if (got.truthy()) {
-        return got;
-      }
-    }
-    return JS.undefined();
+    return JS.or(JS.and(JS.call(hasOwnProperty, processor.namespace, key), JS.get(processor.namespace, key)), JS.undefined());
   }
   if (key.truthy()) {
-    assertUnfrozen("data", JS.get(self, "frozen"));
-    JS.set(self, "namespace", key);
+    assertUnfrozen("data", processor.frozen);
+    processor.namespace = key;
     return self;
   }
-  return namespace;
-}
+  return processor.namespace;
+});
 
-JsValue freezeCall(JsValue self) {
-  if (JS.get(self, "frozen").truthy()) {
-    return self;
+JsValue useMethod = JS.methodRest((JsValue self, JsValue parameters) => {
+  ProcessorApi processor = JS.assume(self);
+  JsValue attachers = processor.attachers;
+  JsValue namespace = processor.namespace;
+  assertUnfrozen("use", processor.frozen);
+  JsValue value = parameters[0];
+  if (!JS.isNullish(value)) {
+    addValue(attachers, namespace, value, JS.invoke(JS.invoke(Array, "from", parameters), "slice", 1.0), true);
   }
-  JsValue[] attachers = JS.assume(JS.get(self, "attachers"));
-  JsValue transformers = JS.get(self, "transformers");
-  while (true) {
-    float nextIndex = JS.number(JS.get(self, "freezeIndex")) + 1.0;
-    JS.set(self, "freezeIndex", nextIndex);
-    if (nextIndex >= attachers.length) {
-      break;
-    }
-    int idx = nextIndex.toInt();
-    JsValue entry = attachers[idx];
-    JsValue attacher = entry[0];
-    JsValue options = sliceArgs(entry, 1);
-    if (options.length.toInt() > 0 && JS.isFalse(options[0])) {
-      continue;
-    }
-    if (options.length.toInt() > 0 && JS.strictEqual(options[0], true)) {
-      JS.splice(options, 0.0, 1.0, JS.undefined());
-    }
-    JsValue transformer = JS.apply(attacher, self, options);
-    if (isFn(transformer)) {
-      JS.call(JS.get(transformers, "use"), transformers, transformer);
-    }
-  }
-  JS.set(self, "frozen", true);
-  JS.set(self, "freezeIndex", JS.get(Number, "POSITIVE_INFINITY"));
   return self;
-}
+});
 
-JsValue parseCall(JsValue self, JsValue file) {
-  JS.call(JS.get(self, "freeze"), self);
-  JsValue realFile = asVFile(file);
-  JsValue parser = pickParser(self);
-  assertParser("parse", parser);
-  return JS.call(parser, JS.undefined(), JS.call(String, JS.undefined(), realFile), realFile);
-}
-
-JsValue stringifyCall(JsValue self, JsValue tree, JsValue file) {
-  JS.call(JS.get(self, "freeze"), self);
-  JsValue realFile = asVFile(file);
-  JsValue compiler = pickCompiler(self);
-  assertCompiler("stringify", compiler);
-  assertNode(tree);
-  return JS.call(compiler, JS.undefined(), tree, realFile);
-}
-
-JsValue runCall(JsValue self, JsValue tree, JsValue file, JsValue done) {
-  assertNode(tree);
-  JS.call(JS.get(self, "freeze"), self);
-  if (!done.truthy() && isFn(file)) {
-    done = file;
-    file = JS.undefined();
-  }
-  JsValue transformers = JS.get(self, "transformers");
-  JsValue originalTree = tree;
-  JsValue executor = JS.method2((JsValue _t, JsValue resolve, JsValue reject) => {
+defineMembers(JS.object(
+  "copy", JS.method0((JsValue self) => {
+    ProcessorApi processor = JS.assume(self);
+    JsValue destination = JS.construct(Processor);
+    JsValue attachers = processor.attachers;
+    for (float index = 0; index < attachers.length; index++) {
+      JS.apply(JS.get(destination, "use"), destination, attachers[index]);
+    }
+    JS.invoke(destination, "data", extendWith(JS.object(), processor.namespace));
+    return destination;
+  }),
+  "data", dataMethod,
+  "freeze", JS.method0((JsValue self) => {
+    ProcessorApi processor = JS.assume(self);
+    if (processor.frozen.truthy()) return self;
+    while (true) {
+      float index = JS.number(processor.freezeIndex) + 1;
+      processor.freezeIndex = index;
+      if (!(index < processor.attachers.length)) break;
+      JsValue entry = processor.attachers[index];
+      JsValue options = JS.invoke(entry, "slice", 1.0);
+      if (JS.strictEqual(options[0], false)) continue;
+      if (JS.strictEqual(options[0], true)) options[0] = JS.undefined();
+      JsValue transformer = JS.apply(entry[0], self, options);
+      if (isFn(transformer)) JS.invoke(processor.transformers, "use", transformer);
+    }
+    processor.frozen = true;
+    processor.freezeIndex = 1.0 / 0.0;
+    return self;
+  }),
+  "parse", JS.method1((JsValue self, JsValue file) => {
+    ProcessorApi processor = JS.assume(self);
+    JS.invoke(self, "freeze");
     JsValue realFile = asVFile(file);
-    JsValue realDone = JS.method3((JsValue _s, JsValue error, JsValue outputTree, JsValue outFile) => {
-      JsValue resulting = outputTree;
-      if (!resulting.truthy()) {
-        resulting = originalTree;
-      }
-      if (error.truthy()) {
-        JS.call(reject, JS.undefined(), error);
-        return JS.undefined();
-      }
-      if (resolve.truthy()) {
-        JS.call(resolve, JS.undefined(), resulting);
+    JsValue parser = JS.or(processor.parser, processor.Parser);
+    assertParser("parse", parser);
+    return JS.call(parser, JS.undefined(), JS.call(String, JS.undefined(), realFile), realFile);
+  }),
+  "process", JS.method2((JsValue self, JsValue file, JsValue done) => {
+    ProcessorApi processor = JS.assume(self);
+    JS.invoke(self, "freeze");
+    assertParser("process", JS.or(processor.parser, processor.Parser));
+    assertCompiler("process", JS.or(processor.compiler, processor.Compiler));
+    return executeWith((JsValue resolve, JsValue reject) => {
+      JsValue realFile = asVFile(file);
+      JsValue parseTree = JS.invoke(self, "parse", realFile);
+      JS.invoke(self, "run", parseTree, realFile, (JsValue error, JsValue tree, JsValue outFile) => {
+        if (error.truthy() || !tree.truthy() || !outFile.truthy()) {
+          JS.call(reject, JS.undefined(), error);
+        } else {
+          UnifiedFileView output = JS.assume(outFile);
+          JsValue compileResult = JS.invoke(self, "stringify", tree, outFile);
+          if (looksLikeAValue(compileResult)) {
+            output.value = compileResult;
+          } else {
+            output.result = compileResult;
+          }
+          if (resolve.truthy()) {
+            JS.call(resolve, JS.undefined(), outFile);
+          } else {
+            JS.call(done, JS.undefined(), JS.undefined(), outFile);
+          }
+        }
         return JS.undefined();
-      }
-      JS.call(done, JS.undefined(), JS.undefined(), resulting, outFile);
+      });
+      return JS.undefined();
+    }, done);
+  }),
+  "processSync", JS.method1((JsValue self, JsValue file) => {
+    ProcessorApi processor = JS.assume(self);
+    bool complete = false;
+    JsValue result = JS.undefined();
+    JS.invoke(self, "freeze");
+    assertParser("processSync", JS.or(processor.parser, processor.Parser));
+    assertCompiler("processSync", JS.or(processor.compiler, processor.Compiler));
+    JS.invoke(self, "process", file, (JsValue error, JsValue outFile) => {
+      complete = true;
+      bail(error);
+      result = outFile;
       return JS.undefined();
     });
-    JS.call(JS.get(transformers, "run"), transformers, tree, realFile, realDone);
-    return JS.undefined();
-  });
-  if (done.truthy()) {
-    JS.call(executor, JS.undefined(), JS.undefined(), done);
-    return JS.undefined();
-  }
-  return JS.construct(Promise, executor);
-}
-
-JsValue runSyncCall(JsValue self, JsValue tree, JsValue file) {
-  bool complete = false;
-  JsValue result = JS.undefined();
-  JsValue realDone = JS.method3((JsValue _s, JsValue error, JsValue outTree, JsValue _f) => {
-    bail(error);
-    result = outTree;
-    complete = true;
-    return JS.undefined();
-  });
-  JS.call(JS.get(self, "run"), self, tree, file, realDone);
-  assertDone("runSync", "run", complete);
-  return result;
-}
-
-JsValue processCall(JsValue self, JsValue file, JsValue done) {
-  JS.call(JS.get(self, "freeze"), self);
-  assertParser("process", pickParser(self));
-  assertCompiler("process", pickCompiler(self));
-  JsValue executor = JS.method2((JsValue _t, JsValue resolve, JsValue reject) => {
-    JsValue realFile = asVFile(file);
-    JsValue parseTree = JS.call(JS.get(self, "parse"), self, realFile);
-    JsValue runDone = JS.method3((JsValue _s, JsValue error, JsValue tree, JsValue outFile) => {
-      if (error.truthy() || !tree.truthy() || !outFile.truthy()) {
-        JS.call(reject, JS.undefined(), error);
+    assertDone("processSync", "process", complete);
+    return result;
+  }),
+  "run", JS.method3((JsValue self, JsValue tree, JsValue file, JsValue done) => {
+    ProcessorApi processor = JS.assume(self);
+    assertNode(tree);
+    JS.invoke(self, "freeze");
+    JsValue transformers = processor.transformers;
+    if (!done.truthy() && isFn(file)) {
+      done = file;
+      file = JS.undefined();
+    }
+    return executeWith((JsValue resolve, JsValue reject) => {
+      JS.invoke(transformers, "run", tree, asVFile(file), (JsValue error, JsValue outputTree, JsValue outFile) => {
+        JsValue resultingTree = JS.or(outputTree, tree);
+        if (error.truthy()) {
+          JS.call(reject, JS.undefined(), error);
+        } else if (resolve.truthy()) {
+          JS.call(resolve, JS.undefined(), resultingTree);
+        } else {
+          JS.call(done, JS.undefined(), JS.undefined(), resultingTree, outFile);
+        }
         return JS.undefined();
-      }
-      JsValue compileResult = JS.call(JS.get(self, "stringify"), self, tree, outFile);
-      if (looksLikeAValue(compileResult)) {
-        JS.set(outFile, "value", compileResult);
-      } else {
-        JS.set(outFile, "result", compileResult);
-      }
-      if (resolve.truthy()) {
-        JS.call(resolve, JS.undefined(), outFile);
-        return JS.undefined();
-      }
-      JS.call(done, JS.undefined(), JS.undefined(), outFile);
+      });
+      return JS.undefined();
+    }, done);
+  }),
+  "runSync", JS.method2((JsValue self, JsValue tree, JsValue file) => {
+    bool complete = false;
+    JsValue result = JS.undefined();
+    JS.invoke(self, "run", tree, file, (JsValue error, JsValue outTree) => {
+      bail(error);
+      result = outTree;
+      complete = true;
       return JS.undefined();
     });
-    JS.call(JS.get(self, "run"), self, parseTree, realFile, runDone);
-    return JS.undefined();
-  });
-  if (done.truthy()) {
-    JS.call(executor, JS.undefined(), JS.undefined(), done);
-    return JS.undefined();
-  }
-  return JS.construct(Promise, executor);
-}
-
-JsValue processSyncCall(JsValue self, JsValue file) {
-  JS.call(JS.get(self, "freeze"), self);
-  assertParser("processSync", pickParser(self));
-  assertCompiler("processSync", pickCompiler(self));
-  bool complete = false;
-  JsValue result = JS.undefined();
-  JsValue realDone = JS.method2((JsValue _s, JsValue error, JsValue outFile) => {
-    complete = true;
-    bail(error);
-    result = outFile;
-    return JS.undefined();
-  });
-  JS.call(JS.get(self, "process"), self, file, realDone);
-  assertDone("processSync", "process", complete);
-  return result;
-}
-
-JsValue ProcessorConstructor = JS.undefined();
-
-JsValue copyProcessor(JsValue self) {
-  JsValue destination = JS.construct(ProcessorConstructor);
-  JsValue[] attachers = JS.assume(JS.get(self, "attachers"));
-  int index = -1;
-  int n = attachers.length;
-  while (true) {
-    index = index + 1;
-    if (index >= n) {
-      break;
-    }
-    JS.apply(JS.get(destination, "use"), destination, attachers[index]);
-  }
-  JS.call(JS.get(destination, "data"), destination, cloneNamespace(JS.get(self, "namespace")));
-  return destination;
-}
-
-JsValue useMethod = JS.methodRest(useCall);
-JsValue copyMethod = JS.method0((JsValue self) => {
-  return copyProcessor(self);
-});
-JsValue parseMethod = JS.method1(parseCall);
-JsValue runMethod = JS.method3(runCall);
-JsValue runSyncMethod = JS.method2(runSyncCall);
-JsValue stringifyMethod = JS.method2(stringifyCall);
-JsValue processMethod = JS.method2(processCall);
-JsValue processSyncMethod = JS.method1(processSyncCall);
-JsValue dataMethod = JS.methodRest(dataCall);
-JsValue freezeMethod = JS.method0(freezeCall);
-ProcessorConstructor = JS.method0((JsValue self) => {
-  if (JS.isUndefined(self)) {
-    throw JS.construct(TypeError, "Class constructor Processor cannot be invoked without 'new'");
-  }
-  return initializeProcessorValue();
-});
-JS.invoke(Object, "defineProperty", ProcessorConstructor, "name", JS.object("configurable", true, "value", "Processor"));
-JsValue processorPrototype = JS.get(ProcessorConstructor, "prototype");
-
-void defineProcessorMethod(string name, JsValue method, int length) {
-  JS.invoke(Object, "defineProperty", method, "name", JS.object("configurable", true, "value", name));
-  JS.invoke(Object, "defineProperty", method, "length", JS.object("configurable", true, "value", length * 1.0));
-  JS.invoke(
-    Object,
-    "defineProperty",
-    processorPrototype,
-    name,
-    JS.object("configurable", true, "writable", true, "value", method)
-  );
-}
-
-void initializeProcessorPrototype() {
-  defineProcessorMethod("copy", copyMethod, 0);
-  defineProcessorMethod("data", dataMethod, 2);
-  defineProcessorMethod("freeze", freezeMethod, 0);
-  defineProcessorMethod("parse", parseMethod, 1);
-  defineProcessorMethod("process", processMethod, 2);
-  defineProcessorMethod("processSync", processSyncMethod, 1);
-  defineProcessorMethod("run", runMethod, 3);
-  defineProcessorMethod("runSync", runSyncMethod, 2);
-  defineProcessorMethod("stringify", stringifyMethod, 2);
-  defineProcessorMethod("use", useMethod, 1);
-  JS.invoke(Object, "defineProperty", ProcessorConstructor, "prototype", JS.object("writable", false));
-}
-
-initializeProcessorPrototype();
-
-JsValue initializeProcessorValue() {
-  JsValue proc = JS.undefined();
-  proc = JS.staticRest((JsValue _args) => {
-    return copyProcessor(proc);
-  });
-  JS.call(JS.get(Object, "setPrototypeOf"), Object, proc, processorPrototype);
-  JS.set(proc, "Compiler", JS.undefined());
-  JS.set(proc, "Parser", JS.undefined());
-  JS.set(proc, "attachers", JS.array());
-  JS.set(proc, "compiler", JS.undefined());
-  JS.set(proc, "freezeIndex", -1.0);
-  JS.set(proc, "frozen", JS.undefined());
-  JS.set(proc, "namespace", JS.object());
-  JS.set(proc, "parser", JS.undefined());
-  JS.set(proc, "transformers", trough());
-  return proc;
-}
+    assertDone("runSync", "run", complete);
+    return result;
+  }),
+  "stringify", JS.method2((JsValue self, JsValue tree, JsValue file) => {
+    ProcessorApi processor = JS.assume(self);
+    JS.invoke(self, "freeze");
+    JsValue realFile = asVFile(file);
+    JsValue compiler = JS.or(processor.compiler, processor.Compiler);
+    assertCompiler("stringify", compiler);
+    assertNode(tree);
+    return JS.call(compiler, JS.undefined(), tree, realFile);
+  }),
+  "use", useMethod
+), JS.get(Processor, "prototype"));
+JS.invoke(Object, "defineProperty", dataMethod, "length", JS.object("value", 2.0));
+JS.invoke(Object, "defineProperty", useMethod, "length", JS.object("value", 1.0));
 
-JsValue unifiedValue = JS.construct(ProcessorConstructor);
-JS.call(JS.get(unifiedValue, "freeze"), unifiedValue);
+JsValue unifiedValue = JS.invoke(JS.construct(Processor), "freeze");
 
 export JsValue unified = unifiedValue;
 export JsValue VFileRuntime = VFile;
-export JsValue ProcessorRuntime = ProcessorConstructor;
+export JsValue ProcessorRuntime = Processor;
diff -ruN a/src/graph/unified/extend.lil b/src/graph/unified/extend.lil
--- a/src/graph/unified/extend.lil	2026-09-23 20:40:45.065690859 +0000
+++ b/src/graph/unified/extend.lil	2026-09-23 20:40:45.257004527 +0000
@@ -1,66 +1,40 @@
-import { Object, objectHasOwn } from "./host.lil";
-import { isPlainObjectExtend } from "./plain.lil";
-
-void setProperty(JsValue target, string name, JsValue newValue) {
-  if (name == "__proto__") {
-    JS.invoke(
-      Object,
-      "defineProperty",
-      target,
-      "__proto__",
-      JS.object("enumerable", true, "configurable", true, "writable", true, "value", newValue)
-    );
-    return;
-  }
-  JS.set(target, name, newValue);
-}
+import { Object } from "./host.lil";
+import { hasOwnProperty, isPlainObjectExtend } from "./plain.lil";
 
 JsValue getProperty(JsValue obj, string name) {
   if (name == "__proto__") {
-    if (!objectHasOwn(obj, name)) {
-      return JS.undefined();
-    }
-    JsValue desc = JS.invoke(Object, "getOwnPropertyDescriptor", obj, name);
-    return JS.get(desc, "value");
+    if (!JS.call(hasOwnProperty, obj, name).truthy()) return JS.undefined();
+    return JS.get(JS.invoke(Object, "getOwnPropertyDescriptor", obj, name), "value");
   }
   return JS.get(obj, name);
 }
 
+// extend@3 with `deep = true` and one source: `extend(true, target, source)`.
 export JsValue extendWith(JsValue target, JsValue source) {
-  if (JS.isNullish(target) || (JS.typeOf(target) != "object" && JS.typeOf(target) != "function")) {
-    target = JS.object();
-  }
-  if (JS.isNullish(source)) {
-    return target;
-  }
-  for (string name in source) {
-    JsValue src = getProperty(target, name);
-    JsValue copy = getProperty(source, name);
-    if (!JS.strictEqual(target, copy)) {
-      if (copy.truthy() && (isPlainObjectExtend(copy) || copy.isArray())) {
-        JsValue clone = JS.undefined();
-        if (copy.isArray()) {
-          if (src.truthy() && src.isArray()) {
-            clone = src;
+  if (JS.isNullish(target) || JS.typeOf(target) != "object" && JS.typeOf(target) != "function") target = JS.object();
+  if (!JS.isNullish(source)) {
+    for (string name in source) {
+      JsValue src = getProperty(target, name);
+      JsValue copy = getProperty(source, name);
+      if (!JS.strictEqual(target, copy)) {
+        JsValue value = copy;
+        if (copy.truthy() && (isPlainObjectExtend(copy) || copy.isArray())) {
+          JsValue clone = if (copy.isArray()) {
+            if (src.truthy() && src.isArray()) { src } else { JS.array() }
           } else {
-            clone = JS.array();
-          }
+            if (src.truthy() && isPlainObjectExtend(src)) { src } else { JS.object() }
+          };
+          value = extendWith(clone, copy);
+        } else if (JS.isUndefined(copy)) {
+          continue;
+        }
+        if (name == "__proto__") {
+          JS.invoke(Object, "defineProperty", target, name, JS.object("enumerable", true, "configurable", true, "value", value, "writable", true));
         } else {
-          if (src.truthy() && isPlainObjectExtend(src)) {
-            clone = src;
-          } else {
-            clone = JS.object();
-          }
+          JS.set(target, name, value);
         }
-        setProperty(target, name, extendWith(clone, copy));
-      } else if (JS.typeOf(copy) != "undefined") {
-        setProperty(target, name, copy);
       }
     }
   }
   return target;
 }
-
-export JsValue cloneNamespace(JsValue namespace) {
-  return extendWith(JS.object(), namespace);
-}
diff -ruN a/src/graph/unified/host.lil b/src/graph/unified/host.lil
--- a/src/graph/unified/host.lil	2026-09-23 20:40:45.075690947 +0000
+++ b/src/graph/unified/host.lil	2026-09-23 20:40:45.257322323 +0000
@@ -1,93 +1,57 @@
 import { Array, Error, JSON, Number, Object, Promise, String, Symbol, TextDecoder, TypeError, globalThis } from "../../host.lil";
 export { Array, Error, JSON, Number, Object, Promise, String, Symbol, TextDecoder, TypeError, globalThis };
 
-export JsValue NULL = JS.invoke(JSON, "parse", "null");
-export JsValue HAS_OWN = JS.get(JS.get(Object, "prototype"), "hasOwnProperty");
-export JsValue TO_STRING = JS.get(JS.get(Object, "prototype"), "toString");
-export JsValue ARRAY_SLICE = JS.get(JS.get(Array, "prototype"), "slice");
-
-export JsValue undef() {
-  return JS.undefined();
-}
-
-export bool isUndef(JsValue value) {
-  return JS.isUndefined(value);
-}
-
-export bool isNullish(JsValue value) {
-  return JS.isNullish(value);
-}
-
 export bool isFn(JsValue value) {
   return JS.typeOf(value) == "function";
 }
 
-export bool isStr(JsValue value) {
-  return JS.typeOf(value) == "string";
-}
-
-export bool isObj(JsValue value) {
-  return !JS.isNullish(value) && JS.typeOf(value) == "object";
-}
-
-export int len(JsValue value) {
-  return value.length.toInt();
-}
-
-export JsValue[] copyArray(JsValue list) {
-  return JS.assume(JS.call(ARRAY_SLICE, list));
-}
-
-export JsValue[] sliceArgs(JsValue list, int start) {
-  return JS.assume(JS.call(ARRAY_SLICE, list, start));
-}
-
-export bool objectHasOwn(JsValue obj, string key) {
-  return JS.call(HAS_OWN, obj, key).truthy();
-}
-
-export bool isErrorInstance(JsValue value) {
-  if (JS.isNullish(value)) {
-    return false;
-  }
-  string t = JS.typeOf(value);
-  if (t != "object" && t != "function") {
-    return false;
-  }
-  return JS.invoke(JS.get(Error, "prototype"), "isPrototypeOf", value).truthy();
+export bool isUint8Array(JsValue value) {
+  return value.truthy() && JS.typeOf(value) == "object" && JS.in("byteLength", value) && JS.in("byteOffset", value);
 }
 
 export bool looksLikeAValue(JsValue value) {
-  if (JS.typeOf(value) == "string") {
-    return true;
-  }
-  return isUint8Array(value);
+  return value is string || isUint8Array(value);
 }
 
-export bool isUint8Array(JsValue value) {
-  if (!value.truthy()) {
-    return false;
-  }
-  if (JS.typeOf(value) != "object") {
-    return false;
+export void bail(JsValue error) {
+  if (error.truthy()) {
+    throw error;
   }
-  return JS.in("byteLength", value) && JS.in("byteOffset", value);
-}
-
-export void throwError(string message) {
-  throw JS.construct(Error, message);
 }
 
-export void throwType(string message) {
-  throw JS.construct(TypeError, message);
+// `class X` refuses a call without `new`; the host-callable constructors here do the same.
+export void requireNew(JsValue self, string name) {
+  if (JS.isUndefined(self)) {
+    throw JS.construct(TypeError, "Class constructor " + name + " cannot be invoked without 'new'");
+  }
 }
 
-export void bail(JsValue error) {
-  if (error.truthy()) {
-    throw error;
+// A function's own `name` is configurable; redefining only its value keeps the other attributes.
+export void nameFunction(JsValue fn, JsValue name) {
+  JS.invoke(Object, "defineProperty", fn, "name", JS.object("value", name));
+}
+
+// Class-member semantics on `proto`, in table order: a function becomes a method named after its key
+// (non-enumerable, writable, configurable); a `{get, set}` pair becomes an accessor whose functions are
+// named `get key` / `set key`.
+export void defineMembers(JsValue members, JsValue proto) {
+  for (string key in members) {
+    JsValue member = JS.get(members, key);
+    JsValue descriptor = member;
+    if (isFn(member)) {
+      nameFunction(member, key);
+      descriptor = JS.object("writable", true, "value", member);
+    } else {
+      nameFunction(JS.get(member, "get"), "get " + key);
+      nameFunction(JS.get(member, "set"), "set " + key);
+    }
+    JS.set(descriptor, "configurable", true);
+    JS.invoke(Object, "defineProperty", proto, key, descriptor);
   }
 }
 
-export JsValue protoOf(JsValue value) {
-  return JS.invoke(Object, "getPrototypeOf", value);
+// What `class Name` gives its constructor: the name, and a non-writable `prototype`.
+export void finishClass(JsValue ctor, string name) {
+  nameFunction(ctor, name);
+  JS.invoke(Object, "defineProperty", ctor, "prototype", JS.object("writable", false));
 }
diff -ruN a/src/graph/unified/index.lil b/src/graph/unified/index.lil
--- a/src/graph/unified/index.lil	1970-01-01 00:00:00.000000000 +0000
+++ b/src/graph/unified/index.lil	2026-09-23 20:40:45.257607537 +0000
@@ -0,0 +1,4 @@
+// The published ESM: unified's public API only. entry.lil keeps the runtime exports other ports link.
+import { unified } from "./entry.lil";
+
+export { unified };
diff -ruN a/src/graph/unified/plain.lil b/src/graph/unified/plain.lil
--- a/src/graph/unified/plain.lil	2026-09-23 20:40:45.085691035 +0000
+++ b/src/graph/unified/plain.lil	2026-09-23 20:40:45.257659494 +0000
@@ -1,54 +1,26 @@
-import { HAS_OWN, Object, Symbol, TO_STRING, protoOf } from "./host.lil";
+import { Object, Symbol } from "./host.lil";
 
+export JsValue hasOwnProperty = JS.get(JS.object(), "hasOwnProperty");
+
+// is-plain-obj@4
 export bool isPlainObj(JsValue value) {
-  if (JS.typeOf(value) != "object" || JS.isNullish(value)) {
-    return false;
-  }
-  JsValue prototype = protoOf(value);
-  bool protoOk = JS.isNullish(prototype) || JS.strictEqual(prototype, JS.get(Object, "prototype"));
-  if (!protoOk) {
-    protoOk = JS.isNullish(protoOf(prototype));
-  }
-  if (!protoOk) {
-    return false;
-  }
-  if (JS.in(JS.get(Symbol, "toStringTag"), value)) {
-    return false;
-  }
-  if (JS.in(JS.get(Symbol, "iterator"), value)) {
-    return false;
-  }
-  return true;
+  if (JS.typeOf(value) != "object" || JS.isNullish(value)) return false;
+  JsValue prototype = JS.invoke(Object, "getPrototypeOf", value);
+  return (JS.isNullish(prototype) || JS.strictEqual(prototype, JS.get(Object, "prototype")) || JS.isNullish(JS.invoke(Object, "getPrototypeOf", prototype))) &&
+    !JS.in(JS.get(Symbol, "toStringTag"), value) && !JS.in(JS.get(Symbol, "iterator"), value);
 }
 
+// extend@3's own check: a `[object Object]` whose constructor is Object-like and whose last key is own.
 export bool isPlainObjectExtend(JsValue obj) {
-  if (!obj.truthy()) {
-    return false;
-  }
-  JsValue tag = JS.call(TO_STRING, obj);
-  if (JS.string(tag) != "[object Object]") {
-    return false;
-  }
-  bool hasOwnConstructor = JS.call(HAS_OWN, obj, "constructor").truthy();
+  if (!obj.truthy() || JS.call(JS.get(JS.get(Object, "prototype"), "toString"), obj) != "[object Object]") return false;
   JsValue ctor = JS.get(obj, "constructor");
-  bool hasIsPrototypeOf = false;
-  if (ctor.truthy()) {
-    JsValue constructorPrototype = JS.get(ctor, "prototype");
-    if (constructorPrototype.truthy()) {
-      hasIsPrototypeOf = JS.call(HAS_OWN, JS.get(ctor, "prototype"), "isPrototypeOf").truthy();
-    }
+  if (ctor.truthy() && !JS.call(hasOwnProperty, obj, "constructor").truthy()) {
+    JsValue ctorPrototype = JS.get(ctor, "prototype");
+    if (!ctorPrototype.truthy() || !JS.call(hasOwnProperty, ctorPrototype, "isPrototypeOf").truthy()) return false;
   }
-  if (ctor.truthy() && !hasOwnConstructor && !hasIsPrototypeOf) {
-    return false;
-  }
-  string last = "";
-  bool saw = false;
+  JsValue last = JS.undefined();
   for (string key in obj) {
     last = key;
-    saw = true;
-  }
-  if (!saw) {
-    return true;
   }
-  return JS.call(HAS_OWN, obj, last).truthy();
+  return JS.isUndefined(last) || JS.call(hasOwnProperty, obj, last).truthy();
 }
diff -ruN a/src/graph/unified/trough.lil b/src/graph/unified/trough.lil
--- a/src/graph/unified/trough.lil	2026-09-23 20:40:45.095691123 +0000
+++ b/src/graph/unified/trough.lil	2026-09-23 20:40:45.257703988 +0000
@@ -1,136 +1,93 @@
-import {
-  NULL,
-  copyArray,
-  isErrorInstance,
-  isFn,
-  sliceArgs,
-  throwType
-} from "./host.lil";
+import { Array, Error, TypeError, isFn } from "./host.lil";
 
-JsValue wrapMiddleware(JsValue middleware, JsValue callback) {
-  bool called = false;
+// trough@2: `run` feeds values through the middleware in order; `wrap` adapts one middleware
+// (sync return, returned promise, or a `done` callback) to a node-style callback.
 
-  JsValue doneFn = JS.methodRest((JsValue _s, JsValue outArgs) => {
-    if (called) {
-      return JS.undefined();
+JsValue wrap(JsValue middleware, JsValue callback) {
+  bool called = false;
+  JsValue done = JS.staticRest((JsValue output) => {
+    if (!called) {
+      called = true;
+      JS.apply(callback, JS.undefined(), output);
     }
-    called = true;
-    JS.apply(callback, JS.undefined(), outArgs);
     return JS.undefined();
   });
-
-  JsValue thenFn = JS.method1((JsValue _s, JsValue value) => {
-    JS.call(doneFn, JS.undefined(), NULL, value);
-    return JS.undefined();
-  });
-
-  return JS.methodRest((JsValue self, JsValue parameters) => {
-    JsValue[] params = copyArray(parameters);
-    bool fnExpectsCallback = JS.number(middleware["length"]) > params.length;
-    if (fnExpectsCallback) {
-      params.push(doneFn);
-    }
+  JsValue then = (JsValue value) => {
+    JsValue? nothing = null;
+    return JS.call(done, JS.undefined(), nothing, value);
+  };
+  return JS.staticRest((JsValue args) => {
+    JsValue parameters = JS.invoke(Array, "from", args);
+    bool fnExpectsCallback = JS.number(JS.get(middleware, "length")) > parameters.length;
+    if (fnExpectsCallback) JS.invoke(parameters, "push", done);
     JsValue result = JS.undefined();
     try {
-      result = JS.apply(middleware, self, params);
+      result = JS.apply(middleware, JS.undefined(), parameters);
     } catch (JsValue exception) {
-      if (fnExpectsCallback && called) {
-        throw exception;
-      }
-      JS.call(doneFn, JS.undefined(), exception);
-      return JS.undefined();
+      if (fnExpectsCallback && called) throw exception;
+      return JS.call(done, JS.undefined(), exception);
     }
     if (!fnExpectsCallback) {
-      if (result.truthy() && JS.get(result, "then").truthy() && isFn(JS.get(result, "then"))) {
-        JS.invoke(result, "then", thenFn, doneFn);
-      } else if (isErrorInstance(result)) {
-        JS.call(doneFn, JS.undefined(), result);
+      JsValue resultThen = JS.and(result, JS.get(result, "then"));
+      if (resultThen.truthy() && isFn(JS.get(result, "then"))) {
+        JS.invoke(result, "then", then, done);
+      } else if (JS.invoke(JS.get(Error, "prototype"), "isPrototypeOf", result).truthy()) {
+        JS.call(done, JS.undefined(), result);
       } else {
-        JS.call(thenFn, JS.undefined(), result);
+        JS.call(then, JS.undefined(), result);
       }
     }
     return JS.undefined();
   });
 }
 
-JsValue troughRun(JsValue self, JsValue args) {
-  JsValue[] values = copyArray(args);
-  JsValue callback = values.pop();
-  if (!isFn(callback)) {
-    throwType("Expected function as last argument, not " + JS.string(callback));
-  }
-  int middlewareIndex = -1;
-  JsValue[] middleware = JS.assume(JS.get(self, "fns"));
+// `[null, ...list]`
+JsValue withNull(JsValue list) {
+  JsValue? nothing = null;
+  return JS.invoke(JS.array(nothing), "concat", list);
+}
 
-  JsValue nextFn = JS.undefined();
-  nextFn = JS.methodRest((JsValue _s, JsValue nextArgs) => {
-    middlewareIndex = middlewareIndex + 1;
-    JsValue fn = JS.undefined();
-    if (middlewareIndex < middleware.length) {
-      fn = middleware[middlewareIndex];
-    }
-    JsValue error = JS.undefined();
-    JsValue[] output = [];
-    int na = nextArgs.length.toInt();
-    if (na > 0) {
-      error = nextArgs[0];
-      output = sliceArgs(nextArgs, 1);
-    }
-    if (error.truthy()) {
-      JS.call(callback, JS.undefined(), error);
-      return JS.undefined();
-    }
-    JsValue[] currentValues = values;
-    int index = -1;
-    int vlen = currentValues.length;
-    while (true) {
-      index = index + 1;
-      if (index >= vlen) {
-        break;
-      }
-      JsValue slot = JS.undefined();
-      if (index < output.length) {
-        slot = output[index];
+export JsValue trough() {
+  JsValue fns = JS.array();
+  JsValue pipeline = JS.undefined();
+  pipeline = JS.object(
+    "run", JS.staticRest((JsValue args) => {
+      JsValue values = JS.invoke(Array, "from", args);
+      float middlewareIndex = -1;
+      JsValue callback = JS.invoke(values, "pop");
+      if (!isFn(callback)) {
+        throw JS.construct(TypeError, "Expected function as last argument, not " + callback);
       }
-      if (JS.isNullish(slot)) {
-        output[index] = currentValues[index];
+      JsValue next = JS.undefined();
+      next = JS.staticRest((JsValue nextArgs) => {
+        middlewareIndex++;
+        JsValue fn = fns[middlewareIndex];
+        JsValue error = nextArgs[0];
+        if (error.truthy()) {
+          return JS.call(callback, JS.undefined(), error);
+        }
+        JsValue output = JS.invoke(JS.invoke(Array, "from", nextArgs), "slice", 1.0);
+        for (float index = 0; index < values.length; index++) {
+          if (JS.isNullish(output[index])) output[index] = values[index];
+        }
+        values = output;
+        if (fn.truthy()) {
+          JS.apply(wrap(fn, next), JS.undefined(), output);
+        } else {
+          JS.apply(callback, JS.undefined(), withNull(output));
+        }
+        return JS.undefined();
+      });
+      JS.apply(next, JS.undefined(), withNull(values));
+      return JS.undefined();
+    }),
+    "use", (JsValue middelware) => {
+      if (!isFn(middelware)) {
+        throw JS.construct(TypeError, "Expected `middelware` to be a function, not " + middelware);
       }
+      JS.invoke(fns, "push", middelware);
+      return pipeline;
     }
-    values = output;
-    if (isFn(fn)) {
-      JsValue wrapped = wrapMiddleware(fn, nextFn);
-      JS.apply(wrapped, JS.undefined(), output);
-    } else {
-      JsValue[] packed = copyArray(output);
-      JS.call(JS.get(packed, "unshift"), packed, NULL);
-      JS.apply(callback, JS.undefined(), packed);
-    }
-    return JS.undefined();
-  });
-
-  JsValue[] start = copyArray(values);
-  JS.call(JS.get(start, "unshift"), start, NULL);
-  JS.apply(nextFn, JS.undefined(), start);
-  return JS.undefined();
-}
-
-JsValue troughUse(JsValue self, JsValue middleware) {
-  if (!isFn(middleware)) {
-    throwType("Expected `middelware` to be a function, not " + JS.string(middleware));
-  }
-  JsValue[] functions = JS.assume(JS.get(self, "fns"));
-  functions.push(middleware);
-  return self;
-}
-
-JsValue troughRunMethod = JS.methodRest(troughRun);
-JsValue troughUseMethod = JS.method1(troughUse);
-
-export JsValue trough() {
-  JsValue pipeline = JS.object();
-  JsValue[] middleware = [];
-  JS.set(pipeline, "fns", middleware);
-  JS.set(pipeline, "run", troughRunMethod);
-  JS.set(pipeline, "use", troughUseMethod);
+  );
   return pipeline;
 }
diff -ruN a/src/graph/unified/vfile.lil b/src/graph/unified/vfile.lil
--- a/src/graph/unified/vfile.lil	2026-09-23 20:40:45.105691210 +0000
+++ b/src/graph/unified/vfile.lil	2026-09-23 20:40:45.258027895 +0000
@@ -1,35 +1,81 @@
-import { Error, JSON, Object, TextDecoder, TypeError, globalThis, isUint8Array } from "./host.lil";
-
-string[] pathFields = "history path basename stem extname dirname".split(" ");
-JsValue messagePrototype = JS.object();
-
-bool includesField(string field) {
-  return pathFields.includes(field);
-}
-
-bool isUrl(JsValue value) {
-  return !JS.isNullish(value) && JS.typeOf(value) == "object" && JS.in("href", value) &&
-    JS.get(value, "href").truthy() && JS.in("protocol", value) && JS.get(value, "protocol").truthy() &&
-    JS.isUndefined(JS.get(value, "auth"));
-}
-
-string cwd() {
-  JsValue processValue = JS.get(globalThis, "process");
-  if (processValue.truthy() && JS.typeOf(JS.get(processValue, "cwd")) == "function") {
-    return JS.string(JS.call(JS.get(processValue, "cwd"), processValue));
-  }
-  return "/";
-}
-
-string pathBasename(string path, string extension) {
-  int start = 0;
-  int end = -1;
-  int index = path.length;
+import {
+  Error,
+  JSON,
+  Object,
+  TextDecoder,
+  TypeError,
+  globalThis,
+  defineMembers,
+  finishClass,
+  isFn,
+  isUint8Array,
+  requireNew
+} from "./host.lil";
+
+// vfile@6 (browser minpath / minurl, Node-aware minproc) and vfile-message@4 over host objects.
+
+extern class VFileView {
+  JsValue cwd;
+  JsValue data;
+  JsValue history;
+  JsValue messages;
+  JsValue value;
+  JsValue path;
+  JsValue basename;
+  JsValue dirname;
+  JsValue extname;
+  JsValue stem;
+}
+
+extern class VFileMessageView {
+  JsValue ancestors;
+  JsValue cause;
+  JsValue place;
+  JsValue position;
+  JsValue ruleId;
+  JsValue source;
+  JsValue start;
+  JsValue end;
+  JsValue line;
+  JsValue column;
+  JsValue message;
+  JsValue stack;
+  JsValue name;
+  JsValue file;
+  JsValue fatal;
+}
+
+extern class VFileUrlView {
+  JsValue href;
+  JsValue protocol;
+  JsValue auth;
+  JsValue hostname;
+  JsValue pathname;
+}
+
+JsValue order = JS.array("history", "path", "basename", "stem", "extname", "dirname");
+
+// ---------------------------------------------------------------- minpath (browser)
+
+bool isSlash(JsValue path, float index) {
+  return JS.strictEqual(JS.invoke(path, "codePointAt", index), 47.0);
+}
+
+void assertPath(JsValue path) {
+  if (!(path is string)) {
+    throw JS.construct(TypeError, "Path must be a string. Received " + JS.invoke(JSON, "stringify", path));
+  }
+}
+
+JsValue basename(JsValue path, JsValue extname) {
+  float start = 0;
+  float end = -1;
+  float index = path.length;
   bool seenNonSlash = false;
-  if (extension.length == 0 || extension.length > path.length) {
+  if (JS.isUndefined(extname) || extname.length == 0.0 || extname.length > path.length) {
     while (index > 0) {
-      index = index - 1;
-      if (path.charAt(index) == "/") {
+      index--;
+      if (isSlash(path, index)) {
         if (seenNonSlash) {
           start = index + 1;
           break;
@@ -39,15 +85,14 @@
         end = index + 1;
       }
     }
-    if (end < 0) return "";
-    return path.slice(start, end);
+    return if (end < 0) { "" } else { JS.invoke(path, "slice", start, end) };
   }
-  if (extension == path) return "";
-  int firstNonSlashEnd = -1;
-  int extensionIndex = extension.length - 1;
+  if (JS.strictEqual(extname, path)) return "";
+  float firstNonSlashEnd = -1;
+  float extnameIndex = extname.length - 1;
   while (index > 0) {
-    index = index - 1;
-    if (path.charAt(index) == "/") {
+    index--;
+    if (isSlash(path, index)) {
       if (seenNonSlash) {
         start = index + 1;
         break;
@@ -57,12 +102,12 @@
         seenNonSlash = true;
         firstNonSlashEnd = index + 1;
       }
-      if (extensionIndex > -1) {
-        if (path.charAt(index) == extension.charAt(extensionIndex)) {
-          extensionIndex = extensionIndex - 1;
-          if (extensionIndex < 0) end = index;
+      if (extnameIndex > -1) {
+        if (JS.strictEqual(JS.invoke(path, "codePointAt", index), JS.invoke(extname, "codePointAt", extnameIndex))) {
+          extnameIndex--;
+          if (extnameIndex < 0) end = index;
         } else {
-          extensionIndex = -1;
+          extnameIndex = -1;
           end = firstNonSlashEnd;
         }
       }
@@ -73,547 +118,458 @@
   } else if (end < 0) {
     end = path.length;
   }
-  return path.slice(start, end);
+  return JS.invoke(path, "slice", start, end);
 }
 
-string pathDirname(string path) {
-  if (path.length == 0) return ".";
-  int end = -1;
-  int index = path.length;
+JsValue dirname(JsValue path) {
+  if (path.length == 0.0) return ".";
+  float end = -1;
+  float index = path.length;
   bool unmatchedSlash = false;
   while (index > 1) {
-    index = index - 1;
-    if (path.charAt(index) == "/") {
+    index--;
+    if (isSlash(path, index)) {
       if (unmatchedSlash) {
         end = index;
         break;
       }
-    } else if (!unmatchedSlash) {
+    } else {
       unmatchedSlash = true;
     }
   }
-  if (end < 0) {
-    if (path.charAt(0) == "/") return "/";
-    return ".";
-  }
-  if (end == 1 && path.charAt(0) == "/") return "//";
-  return path.slice(0, end);
+  return if (end < 0) {
+    if (isSlash(path, 0)) { "/" } else { "." }
+  } else {
+    if (end == 1 && isSlash(path, 0)) { "//" } else { JS.invoke(path, "slice", 0.0, end) }
+  };
 }
 
-string pathExtname(string path) {
-  int index = path.length;
-  int end = -1;
-  int startPart = 0;
-  int startDot = -1;
-  int preDotState = 0;
+JsValue extname(JsValue path) {
+  float index = path.length;
+  float end = -1;
+  float startPart = 0;
+  float startDot = -1;
+  float preDotState = 0;
   bool unmatchedSlash = false;
   while (index > 0) {
-    index = index - 1;
-    string character = path.charAt(index);
-    if (character == "/") {
+    index--;
+    JsValue code = JS.invoke(path, "codePointAt", index);
+    if (JS.strictEqual(code, 47.0)) {
       if (unmatchedSlash) {
         startPart = index + 1;
         break;
       }
-    } else {
-      if (end < 0) {
-        unmatchedSlash = true;
-        end = index + 1;
-      }
-      if (character == ".") {
-        if (startDot < 0) {
-          startDot = index;
-        } else if (preDotState != 1) {
-          preDotState = 1;
-        }
-      } else if (startDot > -1) {
-        preDotState = -1;
+      continue;
+    }
+    if (end < 0) {
+      unmatchedSlash = true;
+      end = index + 1;
+    }
+    if (JS.strictEqual(code, 46.0)) {
+      if (startDot < 0) {
+        startDot = index;
+      } else {
+        preDotState = 1;
       }
+    } else if (startDot > -1) {
+      preDotState = -1;
     }
   }
-  if (startDot < 0 || end < 0 || preDotState == 0 || preDotState == 1 && startDot == end - 1 && startDot == startPart + 1) {
-    return "";
-  }
-  return path.slice(startDot, end);
-}
-
-string normalizePath(string path, bool allowAboveRoot) {
-  string result = "";
-  int lastSegmentLength = 0;
-  int lastSlash = -1;
-  int dots = 0;
-  int index = 0;
-  while (index <= path.length) {
-    string character = "/";
-    if (index < path.length) character = path.charAt(index);
-    if (character == "/") {
-      if (lastSlash == index - 1 || dots == 1) {
-      } else if (lastSlash != index - 1 && dots == 2) {
-        bool ordinaryParent = result.length < 2 || lastSegmentLength != 2 || result.charAt(result.length - 1) != "." || result.charAt(result.length - 2) != ".";
-        if (ordinaryParent) {
-          if (result.length > 2) {
-            int previousSlash = result.lastIndexOf("/");
-            if (previousSlash != result.length - 1) {
-              if (previousSlash < 0) {
-                result = "";
-                lastSegmentLength = 0;
-              } else {
-                result = result.slice(0, previousSlash);
-                lastSegmentLength = result.length - 1 - result.lastIndexOf("/");
+  return if (startDot < 0 || end < 0 || preDotState == 0 || preDotState == 1 && startDot == end - 1 && startDot == startPart + 1) {
+    ""
+  } else {
+    JS.invoke(path, "slice", startDot, end)
+  };
+}
+
+JsValue normalizeString(JsValue path, bool allowAboveRoot) {
+  JsValue result = "";
+  float lastSegmentLength = 0;
+  float lastSlash = -1;
+  float dots = 0;
+  JsValue code = JS.undefined();
+  for (float index = 0; index <= path.length; index++) {
+    if (index < path.length) {
+      code = JS.invoke(path, "codePointAt", index);
+    } else if (JS.strictEqual(code, 47.0)) {
+      break;
+    } else {
+      code = 47.0;
+    }
+    if (JS.strictEqual(code, 47.0)) {
+      if (lastSlash != index - 1 && dots != 1) {
+        if (dots == 2) {
+          if (result.length < 2 || lastSegmentLength != 2 || !JS.strictEqual(JS.invoke(result, "codePointAt", result.length - 1), 46.0) || !JS.strictEqual(JS.invoke(result, "codePointAt", result.length - 2), 46.0)) {
+            if (result.length > 2) {
+              float lastSlashIndex = JS.number(JS.invoke(result, "lastIndexOf", "/"));
+              if (lastSlashIndex != result.length - 1) {
+                if (lastSlashIndex < 0) {
+                  result = "";
+                  lastSegmentLength = 0;
+                } else {
+                  result = JS.invoke(result, "slice", 0.0, lastSlashIndex);
+                  lastSegmentLength = result.length - 1 - JS.number(JS.invoke(result, "lastIndexOf", "/"));
+                }
+                lastSlash = index;
+                dots = 0;
+                continue;
               }
+            } else if (result.length > 0) {
+              result = "";
+              lastSegmentLength = 0;
               lastSlash = index;
               dots = 0;
-              index = index + 1;
               continue;
             }
-          } else if (result.length > 0) {
-            result = "";
-            lastSegmentLength = 0;
-            lastSlash = index;
-            dots = 0;
-            index = index + 1;
-            continue;
           }
-        }
-        if (allowAboveRoot) {
-          if (result.length > 0) {
-            result = result + "/..";
-          } else {
-            result = "..";
+          if (allowAboveRoot) {
+            result = if (result.length > 0) { JS.add(result, "/..") } else { ".." };
+            lastSegmentLength = 2;
           }
-          lastSegmentLength = 2;
-        }
-      } else {
-        string segment = path.slice(lastSlash + 1, index);
-        if (result.length > 0) {
-          result = result + "/" + segment;
         } else {
-          result = segment;
+          JsValue segment = JS.invoke(path, "slice", lastSlash + 1, index);
+          result = if (result.length > 0) { JS.add(JS.add(result, "/"), segment) } else { segment };
+          lastSegmentLength = index - lastSlash - 1;
         }
-        lastSegmentLength = index - lastSlash - 1;
       }
       lastSlash = index;
       dots = 0;
-    } else if (character == "." && dots > -1) {
-      dots = dots + 1;
+    } else if (JS.strictEqual(code, 46.0) && dots > -1) {
+      dots++;
     } else {
       dots = -1;
     }
-    index = index + 1;
   }
   return result;
 }
 
-string normalize(string path) {
-  bool absolute = path.charAt(0) == "/";
-  string value = normalizePath(path, !absolute);
-  if (value.length == 0 && !absolute) value = ".";
-  if (value.length > 0 && path.charAt(path.length - 1) == "/") value = value + "/";
-  if (absolute) return "/" + value;
-  return value;
-}
-
-void assertPathValue(JsValue value) {
-  if (JS.typeOf(value) != "string") {
-    throw JS.construct(
-      TypeError,
-      "Path must be a string. Received " + JS.string(JS.invoke(JSON, "stringify", value))
-    );
-  }
-}
-
-string pathJoin(JsValue leftValue, JsValue rightValue) {
-  assertPathValue(leftValue);
-  assertPathValue(rightValue);
-  string left = JS.string(leftValue);
-  string right = JS.string(rightValue);
-  string joined = "";
-  if (left.length > 0) joined = left;
-  if (right.length > 0) {
-    if (joined.length > 0) {
-      joined = joined + "/" + right;
-    } else {
-      joined = right;
-    }
-  }
-  if (joined.length == 0) return ".";
-  return normalize(joined);
+JsValue join(JsValue left, JsValue right) {
+  assertPath(left);
+  assertPath(right);
+  JsValue joined = if (!left.truthy()) { right } else { if (right.truthy()) { JS.add(JS.add(left, "/"), right) } else { left } };
+  if (!joined.truthy()) return ".";
+  bool absolute = isSlash(joined, 0);
+  JsValue value = normalizeString(joined, !absolute);
+  if (value.length == 0.0 && !absolute) value = ".";
+  if (value.length > 0 && isSlash(joined, joined.length - 1)) value = JS.add(value, "/");
+  return if (absolute) { JS.add("/", value) } else { value };
 }
 
-void assertPart(JsValue part, string name) {
-  if (part.truthy() && JS.invoke(part, "includes", "/").truthy()) {
-    throw JS.construct(Error, "`" + name + "` cannot be a path: did not expect `/`");
-  }
-}
+// ---------------------------------------------------------------- minurl (browser)
 
-void assertNonEmpty(JsValue part, string name) {
-  if (!part.truthy()) throw JS.construct(Error, "`" + name + "` cannot be empty");
+bool isUrl(JsValue value) {
+  VFileUrlView url = JS.assume(value);
+  return value.truthy() && JS.typeOf(value) == "object" && JS.in("href", value) && url.href.truthy() &&
+    JS.in("protocol", value) && url.protocol.truthy() && JS.isUndefined(url.auth);
 }
 
-JsValue getPath(JsValue self) {
-  JsValue history = JS.get(self, "history");
-  if (history.length.toInt() == 0) return JS.undefined();
-  return history[history.length.toInt() - 1];
+void throwCode(string message, string code) {
+  throw JS.invoke(Object, "assign", JS.construct(TypeError, message), JS.object("code", code));
 }
 
-void setPath(JsValue self, JsValue value) {
-  JsValue path = value;
-  if (isUrl(path)) {
-    if (JS.string(JS.get(path, "protocol")) != "file:") {
-      JsValue error = JS.construct(TypeError, "The URL must be of scheme file");
-      error["code"] = "ERR_INVALID_URL_SCHEME";
-      throw error;
-    }
-    if (JS.string(JS.get(path, "hostname")).length > 0) {
-      JsValue error = JS.construct(TypeError, "File URL host must be \"localhost\" or empty on darwin");
-      error["code"] = "ERR_INVALID_FILE_URL_HOST";
-      throw error;
-    }
-    string pathname = JS.string(JS.get(path, "pathname"));
-    int index = 0;
-    while (index < pathname.length) {
-      if (pathname.charAt(index) == "%" && pathname.charAt(index + 1) == "2" &&
-          (pathname.charAt(index + 2) == "F" || pathname.charAt(index + 2) == "f")) {
-        JsValue error = JS.construct(TypeError, "File URL path must not include encoded / characters");
-        error["code"] = "ERR_INVALID_FILE_URL_PATH";
-        throw error;
+JsValue urlToPath(JsValue value) {
+  VFileUrlView url = JS.assume(value);
+  if (!JS.strictEqual(url.protocol, "file:")) {
+    throwCode("The URL must be of scheme file", "ERR_INVALID_URL_SCHEME");
+  }
+  if (!JS.strictEqual(url.hostname, "")) {
+    throwCode("File URL host must be \"localhost\" or empty on darwin", "ERR_INVALID_FILE_URL_HOST");
+  }
+  JsValue pathname = url.pathname;
+  for (float index = 0; index < pathname.length; index++) {
+    if (JS.strictEqual(JS.invoke(pathname, "codePointAt", index), 37.0) && JS.strictEqual(JS.invoke(pathname, "codePointAt", index + 1), 50.0)) {
+      JsValue third = JS.invoke(pathname, "codePointAt", index + 2);
+      if (JS.strictEqual(third, 70.0) || JS.strictEqual(third, 102.0)) {
+        throwCode("File URL path must not include encoded / characters", "ERR_INVALID_FILE_URL_PATH");
       }
-      index = index + 1;
     }
-    path = JS.invoke(globalThis, "decodeURIComponent", pathname);
-  }
-  assertNonEmpty(path, "path");
-  if (!JS.strictEqual(getPath(self), path)) {
-    JsValue[] history = JS.assume(JS.get(self, "history"));
-    history.push(path);
   }
+  return JS.invoke(globalThis, "decodeURIComponent", pathname);
 }
 
-JsValue pointFromPlace(JsValue place) {
-  if (place.truthy() && JS.in("start", place)) return JS.get(place, "start");
-  return place;
+// ---------------------------------------------------------------- vfile-message
+
+JsValue point(JsValue value) {
+  VFileMessageView p = JS.assume(value);
+  return JS.add(JS.add(index(JS.and(value, p.line)), ":"), index(JS.and(value, p.column)));
 }
 
-string stringifyPoint(JsValue point) {
-  if (!point.truthy()) return "1:1";
-  JsValue line = JS.get(point, "line");
-  JsValue column = JS.get(point, "column");
-  string lineText = "1";
-  string columnText = "1";
-  if (JS.typeOf(line) == "number" && line.truthy()) lineText = JS.string(line);
-  if (JS.typeOf(column) == "number" && column.truthy()) columnText = JS.string(column);
-  return lineText + ":" + columnText;
+JsValue position(JsValue value) {
+  VFileMessageView p = JS.assume(value);
+  return JS.add(JS.add(point(JS.and(value, p.start)), "-"), point(JS.and(value, p.end)));
 }
 
-string stringifyPlace(JsValue place) {
-  if (!place.truthy()) return "1:1";
-  if (JS.in("start", place) || JS.in("end", place)) {
-    return stringifyPoint(JS.get(place, "start")) + "-" + stringifyPoint(JS.get(place, "end"));
-  }
-  return stringifyPoint(place);
+JsValue index(JsValue value) {
+  return if (value.truthy() && value is float) { value } else { 1.0 };
+}
+
+JsValue stringifyPosition(JsValue value) {
+  VFileMessageView p = JS.assume(value);
+  return if (!value.truthy() || JS.typeOf(value) != "object") {
+    ""
+  } else {
+    if (JS.in("position", value) || JS.in("type", value)) {
+      position(p.position)
+    } else {
+      if (JS.in("start", value) || JS.in("end", value)) {
+        position(value)
+      } else {
+        if (JS.in("line", value) || JS.in("column", value)) { point(value) } else { "" }
+      }
+    }
+  };
 }
 
-JsValue createMessage(JsValue causeOrReason, JsValue optionsOrPlace, JsValue origin) {
-  if (JS.typeOf(optionsOrPlace) == "string") {
+JsValue createMessage(JsValue proto, JsValue causeOrReason, JsValue optionsOrPlace, JsValue origin) {
+  if (optionsOrPlace is string) {
     origin = optionsOrPlace;
     optionsOrPlace = JS.undefined();
   }
   JsValue reason = "";
-  JsValue options = JS.object();
   bool legacyCause = false;
-  if (optionsOrPlace.truthy()) {
-    if (JS.in("line", optionsOrPlace) && JS.in("column", optionsOrPlace)) {
-      options["place"] = optionsOrPlace;
-    } else if (JS.in("start", optionsOrPlace) && JS.in("end", optionsOrPlace)) {
-      options["place"] = optionsOrPlace;
-    } else if (JS.in("type", optionsOrPlace)) {
-      options["ancestors"] = JS.array(optionsOrPlace);
-      options["place"] = JS.get(optionsOrPlace, "position");
+  VFileMessageView options = JS.assume(
+    if (!optionsOrPlace.truthy()) {
+      JS.object()
     } else {
-      options = JS.invoke(Object, "assign", options, optionsOrPlace);
+      if (JS.in("line", optionsOrPlace) && JS.in("column", optionsOrPlace) || JS.in("start", optionsOrPlace) && JS.in("end", optionsOrPlace)) {
+        JS.object("place", optionsOrPlace)
+      } else {
+        if (JS.in("type", optionsOrPlace)) {
+          JS.object("ancestors", JS.array(optionsOrPlace), "place", JS.get(optionsOrPlace, "position"))
+        } else {
+          JS.invoke(Object, "assign", JS.object(), optionsOrPlace)
+        }
+      }
     }
-  }
-  if (JS.typeOf(causeOrReason) == "string") {
-    reason = JS.string(causeOrReason);
-  } else if (!JS.get(options, "cause").truthy() && causeOrReason.truthy()) {
+  );
+  if (causeOrReason is string) {
+    reason = causeOrReason;
+  } else if (!options.cause.truthy() && causeOrReason.truthy()) {
     legacyCause = true;
     reason = JS.get(causeOrReason, "message");
-    options["cause"] = causeOrReason;
+    options.cause = causeOrReason;
   }
-  if (!JS.get(options, "ruleId").truthy() && !JS.get(options, "source").truthy() && JS.typeOf(origin) == "string") {
-    string originText = JS.string(origin);
-    int colon = originText.indexOf(":");
-    if (colon < 0) {
-      options["ruleId"] = originText;
+  if (!options.ruleId.truthy() && !options.source.truthy() && origin is string) {
+    JsValue colon = JS.invoke(origin, "indexOf", ":");
+    if (JS.strictEqual(colon, -1.0)) {
+      options.ruleId = origin;
     } else {
-      options["source"] = originText.slice(0, colon);
-      options["ruleId"] = originText.slice(colon + 1);
+      options.source = JS.invoke(origin, "slice", 0.0, colon);
+      options.ruleId = JS.invoke(origin, "slice", JS.add(colon, 1.0));
     }
   }
-  JsValue ancestors = JS.get(options, "ancestors");
-  if (!JS.get(options, "place").truthy() && ancestors.truthy() && ancestors.length.toInt() > 0) {
-    JS.set(options, "place", JS.get(ancestors[ancestors.length.toInt() - 1], "position"));
-  }
-  JsValue place = JS.get(options, "place");
-  JsValue start = place;
-  if (place.truthy() && JS.in("start", place)) start = JS.get(place, "start");
-  JsValue message = JS.construct(Error);
-  JS.invoke(Object, "setPrototypeOf", message, messagePrototype);
-  message["ancestors"] = JS.undefined();
-  if (ancestors.truthy()) message["ancestors"] = ancestors;
-  message["cause"] = JS.undefined();
-  if (JS.get(options, "cause").truthy()) message["cause"] = JS.get(options, "cause");
-  message["column"] = JS.undefined();
-  if (start.truthy()) message["column"] = JS.get(start, "column");
-  message["fatal"] = JS.undefined();
-  message["file"] = "";
-  message["message"] = reason;
-  message["line"] = JS.undefined();
-  if (start.truthy()) message["line"] = JS.get(start, "line");
-  message["name"] = stringifyPlace(place);
-  message["place"] = JS.undefined();
-  if (place.truthy()) message["place"] = place;
-  message["reason"] = reason;
-  message["ruleId"] = JS.undefined();
-  if (JS.get(options, "ruleId").truthy()) message["ruleId"] = JS.get(options, "ruleId");
-  message["source"] = JS.undefined();
-  if (JS.get(options, "source").truthy()) message["source"] = JS.get(options, "source");
-  message["actual"] = JS.undefined();
-  message["expected"] = JS.undefined();
-  message["note"] = JS.undefined();
-  message["url"] = JS.undefined();
-  if (legacyCause && JS.typeOf(JS.get(JS.get(options, "cause"), "stack")) == "string") {
-    message["stack"] = JS.get(JS.get(options, "cause"), "stack");
-  } else {
-    message["stack"] = "";
-  }
-  return message;
+  JsValue ancestors = options.ancestors;
+  if (!options.place.truthy() && ancestors.truthy()) {
+    JsValue parent = ancestors[ancestors.length - 1];
+    if (parent.truthy()) options.place = JS.get(parent, "position");
+  }
+  JsValue place = options.place;
+  JsValue start = if (place.truthy() && JS.in("start", place)) { JS.get(place, "start") } else { place };
+  VFileMessageView startFields = JS.assume(start);
+  JsValue cause = options.cause;
+  return JS.invoke(Object, "assign", JS.invoke(Object, "setPrototypeOf", JS.construct(Error), proto), JS.object(
+    "ancestors", JS.or(ancestors, JS.undefined()),
+    "cause", JS.or(cause, JS.undefined()),
+    "column", if (start.truthy()) { startFields.column } else { JS.undefined() },
+    "fatal", JS.undefined(),
+    "file", "",
+    "message", reason,
+    "line", if (start.truthy()) { startFields.line } else { JS.undefined() },
+    "name", JS.or(stringifyPosition(place), "1:1"),
+    "place", JS.or(place, JS.undefined()),
+    "reason", reason,
+    "ruleId", JS.or(options.ruleId, JS.undefined()),
+    "source", JS.or(options.source, JS.undefined()),
+    "stack", if (legacyCause && cause.truthy() && JS.get(cause, "stack") is string) { JS.get(cause, "stack") } else { "" },
+    "actual", JS.undefined(),
+    "expected", JS.undefined(),
+    "note", JS.undefined(),
+    "url", JS.undefined()
+  ));
 }
 
-JsValue vfileConstruct(JsValue self, JsValue value) {
-  if (JS.isUndefined(self)) {
-    throw JS.construct(TypeError, "Class constructor VFile cannot be invoked without 'new'");
-  }
-  JsValue options = value;
-  if (!value.truthy()) {
-    options = JS.object();
-  } else if (isUrl(value)) {
-    options = JS.object("path", value);
-  } else if (JS.typeOf(value) == "string" || isUint8Array(value)) {
-    options = JS.object("value", value);
-  }
-  string initialCwd = cwd();
-  if (JS.in("cwd", options)) initialCwd = "";
-  JS.set(self, "cwd", initialCwd);
-  JS.set(self, "data", JS.object());
-  JS.set(self, "history", JS.array());
-  JS.set(self, "messages", JS.array());
-  int index = 0;
-  while (index < pathFields.length) {
-    string field = JS.string(pathFields[index]);
-    if (JS.in(field, options) && !JS.isNullish(JS.get(options, field)) && !JS.isUndefined(JS.get(options, field))) {
-      JsValue fieldValue = JS.get(options, field);
-      if (field == "history") fieldValue = JS.invoke(fieldValue, "slice");
-      JS.set(self, field, fieldValue);
-    }
-    index = index + 1;
-  }
-  for (string field in options) {
-    if (!includesField(field)) JS.set(self, field, JS.get(options, field));
-  }
-  return self;
-}
+export JsValue VFileMessage = JS.method3((JsValue self, JsValue reason, JsValue options, JsValue origin) => {
+  requireNew(self, "VFileMessage");
+  return createMessage(JS.get(VFileMessage, "prototype"), reason, options, origin);
+});
+finishClass(VFileMessage, "VFileMessage");
+JS.invoke(Object, "setPrototypeOf", VFileMessage, Error);
+JS.invoke(Object, "assign", JS.invoke(Object, "setPrototypeOf", JS.get(VFileMessage, "prototype"), JS.get(Error, "prototype")), JS.object(
+  "file", "",
+  "name", "",
+  "reason", "",
+  "message", "",
+  "stack", "",
+  "column", JS.undefined(),
+  "line", JS.undefined(),
+  "ancestors", JS.undefined(),
+  "cause", JS.undefined(),
+  "fatal", JS.undefined(),
+  "place", JS.undefined(),
+  "ruleId", JS.undefined(),
+  "source", JS.undefined()
+));
 
-JsValue vfileMessage(JsValue self, JsValue reason, JsValue options, JsValue origin) {
-  JsValue message = createMessage(reason, options, origin);
-  JsValue path = getPath(self);
-  if (path.truthy()) {
-    message["name"] = JS.string(path) + ":" + JS.string(JS.get(message, "name"));
-    message["file"] = path;
-  }
-  message["fatal"] = false;
-  JsValue[] messages = JS.assume(JS.get(self, "messages"));
-  messages.push(message);
-  return message;
+export JsValue createVFileMessage(JsValue reason, JsValue options) {
+  return createMessage(JS.get(VFileMessage, "prototype"), reason, options, JS.undefined());
 }
 
-JsValue vfileFail(JsValue self, JsValue reason, JsValue options, JsValue origin) {
-  JsValue message = JS.call(JS.get(self, "message"), self, reason, options, origin);
-  message["fatal"] = true;
-  throw message;
-}
+// ---------------------------------------------------------------- VFile
+
 
-JsValue vfileInfo(JsValue self, JsValue reason, JsValue options, JsValue origin) {
-  JsValue message = JS.call(JS.get(self, "message"), self, reason, options, origin);
-  message["fatal"] = JS.undefined();
-  return message;
+void assertPart(JsValue part, string name) {
+  if (part.truthy() && JS.invoke(part, "includes", "/").truthy()) {
+    throw JS.construct(Error, "`" + name + "` cannot be a path: did not expect `/`");
+  }
 }
 
-JsValue vfileToString(JsValue self, JsValue encoding) {
-  JsValue value = JS.get(self, "value");
-  if (JS.isUndefined(value)) return "";
-  if (JS.typeOf(value) == "string") return value;
-  JsValue selectedEncoding = JS.undefined();
-  if (encoding.truthy()) selectedEncoding = encoding;
-  JsValue decoder = JS.construct(TextDecoder, selectedEncoding);
-  return JS.invoke(decoder, "decode", value);
+void assertNonEmpty(JsValue part, string name) {
+  if (!part.truthy()) {
+    throw JS.construct(Error, "`" + name + "` cannot be empty");
+  }
 }
 
-export JsValue createVFileMessage(JsValue reason, JsValue options) {
-  return createMessage(reason, options, JS.undefined());
+void assertPathSet(JsValue path, string name) {
+  if (!path.truthy()) {
+    throw JS.construct(Error, "Setting `" + name + "` requires `path` to be set too");
+  }
 }
 
-export JsValue VFileMessage = JS.method3((JsValue self, JsValue reason, JsValue options, JsValue origin) => {
-  if (JS.isUndefined(self)) {
-    throw JS.construct(TypeError, "Class constructor VFileMessage cannot be invoked without 'new'");
+export JsValue VFile = JS.method1((JsValue self, JsValue value) => {
+  requireNew(self, "VFile");
+  JsValue options = if (!value.truthy()) {
+    JS.object()
+  } else {
+    if (isUrl(value)) {
+      JS.object("path", value)
+    } else {
+      if (value is string || isUint8Array(value)) { JS.object("value", value) } else { value }
+    }
+  };
+  // minproc: Node's `process.cwd()` where there is one, `/` in a browser.
+  JsValue process = JS.get(globalThis, "process");
+  JS.invoke(Object, "assign", self, JS.object(
+    "cwd", if (JS.in("cwd", options)) { "" } else { if (process.truthy() && isFn(JS.get(process, "cwd"))) { JS.invoke(process, "cwd") } else { "/" } },
+    "data", JS.object(),
+    "history", JS.array(),
+    "messages", JS.array()
+  ));
+  for (float index = 0; index < order.length; index++) {
+    string field = JS.assume(order[index]);
+    if (JS.in(field, options)) {
+      JsValue given = JS.get(options, field);
+      if (!JS.isNullish(given)) JS.set(self, field, if (field == "history") { JS.invoke(given, "slice") } else { given });
+    }
+  }
+  for (string field in options) {
+    if (!JS.invoke(order, "includes", field).truthy()) JS.set(self, field, JS.get(options, field));
   }
-  return createMessage(reason, options, origin);
+  return JS.undefined();
 });
-messagePrototype = JS.get(VFileMessage, "prototype");
-JS.invoke(Object, "setPrototypeOf", VFileMessage, Error);
-JS.invoke(Object, "setPrototypeOf", messagePrototype, JS.get(Error, "prototype"));
-JS.invoke(Object, "defineProperty", VFileMessage, "name", JS.object("configurable", true, "value", "VFileMessage"));
-messagePrototype["file"] = "";
-messagePrototype["name"] = "";
-messagePrototype["reason"] = "";
-messagePrototype["message"] = "";
-messagePrototype["stack"] = "";
-messagePrototype["column"] = JS.undefined();
-messagePrototype["line"] = JS.undefined();
-messagePrototype["ancestors"] = JS.undefined();
-messagePrototype["cause"] = JS.undefined();
-messagePrototype["fatal"] = JS.undefined();
-messagePrototype["place"] = JS.undefined();
-messagePrototype["ruleId"] = JS.undefined();
-messagePrototype["source"] = JS.undefined();
-JS.invoke(Object, "defineProperty", VFileMessage, "prototype", JS.object("writable", false));
-
-JsValue prototype = JS.undefined();
-export JsValue VFile = JS.method1(vfileConstruct);
-prototype = JS.get(VFile, "prototype");
-JS.invoke(Object, "setPrototypeOf", prototype, JS.get(Object, "prototype"));
-JS.invoke(Object, "defineProperty", VFile, "name", JS.object("configurable", true, "value", "VFile"));
-
-void defineVFileAccessor(string name, JsValue descriptor) {
-  JS.invoke(Object, "defineProperty", prototype, name, descriptor);
-  JsValue getter = JS.get(descriptor, "get");
-  JsValue setter = JS.get(descriptor, "set");
-  JS.invoke(Object, "defineProperty", getter, "name", JS.object("configurable", true, "value", "get " + name));
-  JS.invoke(Object, "defineProperty", setter, "name", JS.object("configurable", true, "value", "set " + name));
-}
-
-void defineVFileMethod(string name, JsValue method) {
-  JS.invoke(Object, "defineProperty", method, "name", JS.object("configurable", true, "value", name));
-  JS.invoke(
-    Object,
-    "defineProperty",
-    prototype,
-    name,
-    JS.object("configurable", true, "writable", true, "value", method)
-  );
-}
+finishClass(VFile, "VFile");
 
-JsValue failMethod = JS.method3(vfileFail);
-JsValue infoMethod = JS.method3(vfileInfo);
-JsValue messageMethod = JS.method3(vfileMessage);
-JsValue toStringMethod = JS.method1(vfileToString);
-defineVFileAccessor("basename", JS.object(
-  "configurable", true,
-  "get", JS.method0((JsValue self) => {
-    JsValue path = getPath(self);
-    if (JS.typeOf(path) == "string") return pathBasename(JS.string(path), "");
-    return JS.undefined();
-  }),
-  "set", JS.method1((JsValue self, JsValue value) => {
-    assertNonEmpty(value, "basename");
-    assertPart(value, "basename");
-    JsValue dirname = JS.get(self, "dirname");
-    JsValue directory = "";
-    if (dirname.truthy()) directory = dirname;
-    setPath(self, pathJoin(directory, value));
-    return JS.undefined();
-  })
-));
-defineVFileAccessor("dirname", JS.object(
-  "configurable", true,
-  "get", JS.method0((JsValue self) => {
-    JsValue path = getPath(self);
-    if (JS.typeOf(path) == "string") return pathDirname(JS.string(path));
-    return JS.undefined();
+JsValue vfileMembers = JS.object(
+  "basename", JS.object(
+    "get", JS.method0((JsValue self) => {
+      VFileView file = JS.assume(self);
+      return if (file.path is string) { basename(file.path, JS.undefined()) } else { JS.undefined() };
+    }),
+    "set", JS.method1((JsValue self, JsValue value) => {
+      VFileView file = JS.assume(self);
+      assertNonEmpty(value, "basename");
+      assertPart(value, "basename");
+      file.path = join(JS.or(file.dirname, ""), value);
+      return JS.undefined();
+    })
+  ),
+  "dirname", JS.object(
+    "get", JS.method0((JsValue self) => {
+      VFileView file = JS.assume(self);
+      return if (file.path is string) { dirname(file.path) } else { JS.undefined() };
+    }),
+    "set", JS.method1((JsValue self, JsValue value) => {
+      VFileView file = JS.assume(self);
+      assertPathSet(file.basename, "dirname");
+      file.path = join(JS.or(value, ""), file.basename);
+      return JS.undefined();
+    })
+  ),
+  "extname", JS.object(
+    "get", JS.method0((JsValue self) => {
+      VFileView file = JS.assume(self);
+      return if (file.path is string) { extname(file.path) } else { JS.undefined() };
+    }),
+    "set", JS.method1((JsValue self, JsValue value) => {
+      VFileView file = JS.assume(self);
+      assertPart(value, "extname");
+      assertPathSet(file.dirname, "extname");
+      if (value.truthy()) {
+        if (!JS.strictEqual(JS.invoke(value, "codePointAt", 0.0), 46.0)) {
+          throw JS.construct(Error, "`extname` must start with `.`");
+        }
+        if (JS.invoke(value, "includes", ".", 1.0).truthy()) {
+          throw JS.construct(Error, "`extname` cannot contain multiple dots");
+        }
+      }
+      file.path = join(file.dirname, JS.add(file.stem, JS.or(value, "")));
+      return JS.undefined();
+    })
+  ),
+  "path", JS.object(
+    "get", JS.method0((JsValue self) => {
+      VFileView file = JS.assume(self);
+      return file.history[file.history.length - 1];
+    }),
+    "set", JS.method1((JsValue self, JsValue path) => {
+      VFileView file = JS.assume(self);
+      if (isUrl(path)) path = urlToPath(path);
+      assertNonEmpty(path, "path");
+      if (!JS.strictEqual(file.path, path)) JS.invoke(file.history, "push", path);
+      return JS.undefined();
+    })
+  ),
+  "stem", JS.object(
+    "get", JS.method0((JsValue self) => {
+      VFileView file = JS.assume(self);
+      return if (file.path is string) { basename(file.path, file.extname) } else { JS.undefined() };
+    }),
+    "set", JS.method1((JsValue self, JsValue value) => {
+      VFileView file = JS.assume(self);
+      assertNonEmpty(value, "stem");
+      assertPart(value, "stem");
+      file.path = join(JS.or(file.dirname, ""), JS.add(value, JS.or(file.extname, "")));
+      return JS.undefined();
+    })
+  ),
+  "fail", JS.method3((JsValue self, JsValue reason, JsValue options, JsValue origin) => {
+    VFileMessageView message = JS.assume(JS.invoke(self, "message", reason, options, origin));
+    message.fatal = true;
+    throw message;
+    return message;
   }),
-  "set", JS.method1((JsValue self, JsValue value) => {
-    JsValue basename = JS.get(self, "basename");
-    if (!basename.truthy()) throw JS.construct(Error, "Setting `dirname` requires `path` to be set too");
-    JsValue directory = "";
-    if (value.truthy()) directory = value;
-    setPath(self, pathJoin(directory, basename));
-    return JS.undefined();
-  })
-));
-defineVFileAccessor("extname", JS.object(
-  "configurable", true,
-  "get", JS.method0((JsValue self) => {
-    JsValue path = getPath(self);
-    if (JS.typeOf(path) == "string") return pathExtname(JS.string(path));
-    return JS.undefined();
+  "info", JS.method3((JsValue self, JsValue reason, JsValue options, JsValue origin) => {
+    VFileMessageView message = JS.assume(JS.invoke(self, "message", reason, options, origin));
+    message.fatal = JS.undefined();
+    return message;
   }),
-  "set", JS.method1((JsValue self, JsValue value) => {
-    JsValue extname = value;
-    assertPart(extname, "extname");
-    JsValue dirname = JS.get(self, "dirname");
-    if (!dirname.truthy()) throw JS.construct(Error, "Setting `extname` requires `path` to be set too");
-    if (extname.truthy()) {
-      if (JS.number(JS.invoke(extname, "codePointAt", 0.0)).toInt() != 46) {
-        throw JS.construct(Error, "`extname` must start with `.`");
-      }
-      if (JS.invoke(extname, "includes", ".", 1.0).truthy()) {
-        throw JS.construct(Error, "`extname` cannot contain multiple dots");
-      }
-    }
-    string extension = "";
-    if (extname.truthy()) extension = JS.string(extname);
-    setPath(self, pathJoin(dirname, JS.string(JS.get(self, "stem")) + extension));
-    return JS.undefined();
-  })
-));
-defineVFileAccessor("path", JS.object(
-  "configurable", true,
-  "get", JS.method0((JsValue self) => getPath(self)),
-  "set", JS.method1((JsValue self, JsValue value) => { setPath(self, value); return JS.undefined(); })
-));
-defineVFileAccessor("stem", JS.object(
-  "configurable", true,
-  "get", JS.method0((JsValue self) => {
-    JsValue path = getPath(self);
-    if (JS.typeOf(path) == "string") return pathBasename(JS.string(path), JS.string(JS.get(self, "extname")));
-    return JS.undefined();
+  "message", JS.method3((JsValue self, JsValue reason, JsValue options, JsValue origin) => {
+    VFileView file = JS.assume(self);
+    VFileMessageView message = JS.assume(createMessage(JS.get(VFileMessage, "prototype"), reason, options, origin));
+    if (file.path.truthy()) {
+      message.name = JS.add(JS.add(file.path, ":"), message.name);
+      message.file = file.path;
+    }
+    message.fatal = false;
+    JS.invoke(file.messages, "push", message);
+    return message;
   }),
-  "set", JS.method1((JsValue self, JsValue value) => {
-    assertNonEmpty(value, "stem");
-    assertPart(value, "stem");
-    string stem = JS.string(value);
-    JsValue dirname = JS.get(self, "dirname");
-    string directory = "";
-    if (dirname.truthy()) directory = JS.string(dirname);
-    JsValue extname = JS.get(self, "extname");
-    string extension = "";
-    if (extname.truthy()) extension = JS.string(extname);
-    setPath(self, pathJoin(directory, stem + extension));
-    return JS.undefined();
+  "toString", JS.method1((JsValue self, JsValue encoding) => {
+    VFileView file = JS.assume(self);
+    return if (JS.isUndefined(file.value)) {
+      ""
+    } else {
+      if (file.value is string) { file.value } else { JS.invoke(JS.construct(TextDecoder, JS.or(encoding, JS.undefined())), "decode", file.value) }
+    };
   })
-));
-defineVFileMethod("fail", failMethod);
-defineVFileMethod("info", infoMethod);
-defineVFileMethod("message", messageMethod);
-defineVFileMethod("toString", toStringMethod);
-JS.invoke(Object, "defineProperty", VFile, "prototype", JS.object("writable", false));
-
-export bool looksLikeAVFile(JsValue value) {
-  if (!value.truthy() || JS.typeOf(value) != "object") return false;
-  return JS.in("message", value) && JS.in("messages", value);
-}
-
-export JsValue asVFile(JsValue value) {
-  if (looksLikeAVFile(value)) return value;
-  return JS.construct(VFile, value);
-}
+);
+defineMembers(vfileMembers, JS.get(VFile, "prototype"));
diff -ruN a/src/hast-util-to-jsx-runtime/lib/index.lil b/src/hast-util-to-jsx-runtime/lib/index.lil
--- a/src/hast-util-to-jsx-runtime/lib/index.lil	2026-09-23 20:40:45.232869376 +0000
+++ b/src/hast-util-to-jsx-runtime/lib/index.lil	2026-09-23 20:40:45.354376891 +0000
@@ -1,161 +1,101 @@
-import { Number, RegExp, objectHasOwn } from "../../host.lil";
-import { PropertyInfo, find, reactPropertyName } from "../../property-information/lib/find.lil";
+// hast-util-to-jsx-runtime@2.3.6 `toJsxRuntime(tree, options)` for the options react-markdown passes:
+// {Fragment, components, ignoreInvalidStyle: true, jsx, jsxs, passKeys: true, passNode: true} (production
+// `create`, React attribute and DOM style casing, table-cell `align` moved to `style`).
+import { Map, Object } from "../../host.lil";
+import { PropertyInfo, find, html, reactName, svg } from "../../property-information/lib/find.lil";
 import { styleToJs } from "../../style-to-js/index.lil";
 import { createVFileMessage } from "../../vfile/lib/index.lil";
 
-string docs = "https://github.com/syntax-tree/hast-util-to-jsx-runtime";
-JsValue identifierPattern = JS.construct(RegExp, "^[$_\\p{ID_Start}][$_\\u200C\\u200D\\p{ID_Continue}]*$", "u");
+export extern JsValue Fragment;
+export extern JsValue jsx;
+export extern JsValue jsxs;
+extern JsValue Number;
+extern JsValue RegExp;
+extern JsValue String;
 
-struct RuntimeState {
-  JsValue fragment;
-  JsValue[] ancestors;
+extern class State {
+  JsValue ancestors;
   JsValue components;
-  JsValue create;
-  string schema;
+  JsValue schema;
 }
 
-struct PropertyResult {
-  string key;
-  JsValue value;
-}
-
-bool isTableElement(string tag) {
-  return tag == "table" || tag == "tbody" || tag == "thead" || tag == "tfoot" || tag == "tr";
-}
-
-bool isTableCell(string tag) {
-  return tag == "td" || tag == "th";
-}
-
-bool whitespace(string value) {
-  int index = 0;
-  while (index < value.length) {
-    int code = value.charCodeAt(index);
-    if (code != 9 && code != 10 && code != 12 && code != 13 && code != 32) {
-      return false;
-    }
-    index = index + 1;
-  }
-  return true;
-}
-
-string stringifyList(JsValue value, bool comma) {
-  string separator = " ";
-  if (comma) {
-    separator = ", ";
-  }
-  return JS.string(JS.invoke(value, "join", separator)).trim();
-}
-
-void addNode(RuntimeState state, JsValue props, JsValue type, JsValue node) {
-  if (JS.typeOf(type) != "string" && !JS.strictEqual(type, state.fragment)) {
-    props["node"] = node;
+JsValue identifierName = JS.construct(RegExp, "^[$_\\p{ID_Start}][$_\\u200C\\u200D\\p{ID_Continue}]*$", "u");
+JsValue lowercaseStart = JS.construct(RegExp, "^[a-z]");
+JsValue whitespaceOnly = JS.construct(RegExp, "^[ \\t\\n\\f\\r]*$");
+
+export JsValue toJsxRuntime(JsValue tree, JsValue components) {
+  State state = JS.assume(object {
+    ancestors: JS.array(),
+    components: JS.or(components, object {}),
+    schema: html
+  });
+  JsValue result = one(state, tree, JS.undefined());
+  if (result.truthy() && JS.typeOf(result) != "string") {
+    return result;
   }
+  return create(Fragment, object { children: JS.or(result, JS.undefined()) }, JS.undefined());
 }
 
-void addChildren(JsValue props, JsValue[] children) {
-  int count = children.length;
-  if (count > 0) {
-    JsValue value = children[0];
-    if (count > 1) {
-      value = children;
-    }
-    if (value.truthy()) {
-      props["children"] = value;
-    }
+JsValue one(State state, JsValue node, JsValue key) {
+  JsValue type = JS.get(node, "type");
+  if (JS.strictEqual(type, "element")) {
+    return element(state, node, key);
   }
-}
-
-JsValue productionCreate(JsValue jsxFn, JsValue jsxsFn) {
-  return JS.methodRest((JsValue _self, JsValue args) => {
-    JsValue props = args[2];
-    JsValue fn = jsxFn;
-    if (JS.isArray(JS.get(props, "children"))) {
-      fn = jsxsFn;
-    }
-    JsValue key = JS.undefined();
-    if (args.length.toInt() > 3) {
-      key = args[3];
-    }
-    if (key.truthy()) {
-      return JS.call(fn, JS.undefined(), args[1], props, key);
-    }
-    return JS.call(fn, JS.undefined(), args[1], props);
-  });
-}
-
-void crashEstree(RuntimeState state, JsValue place) {
-  JsValue options = JS.object(
-    "ancestors", state.ancestors,
-    "place", place,
-    "ruleId", "mdx-estree",
-    "source", "hast-util-to-jsx-runtime"
-  );
-  JsValue message = createVFileMessage("Cannot handle MDX estrees without `createEvaluater`", options);
-  message["file"] = JS.undefined();
-  message["url"] = docs + "#cannot-handle-mdx-estrees-without-createevaluater";
-  throw message;
-}
-
-bool isIdentifierName(string name) {
-  return JS.invoke(identifierPattern, "test", name).truthy();
-}
-
-JsValue findComponentFromName(RuntimeState state, string name, bool allowExpression) {
-  if (!allowExpression || name.indexOf(".") < 0 && (!isIdentifierName(name) || name.charCodeAt(0) >= 97 && name.charCodeAt(0) <= 122)) {
-    JsValue components = state.components;
-    if (objectHasOwn(components, name)) return JS.get(components, name);
-    return name;
+  if (JS.strictEqual(type, "mdxFlowExpression") || JS.strictEqual(type, "mdxTextExpression") || JS.strictEqual(type, "mdxjsEsm")) {
+    crashEstree(state, JS.get(node, "position"));
   }
-  crashEstree(state, JS.undefined());
-  return JS.undefined();
-}
-
-JsValue parseStyle(string value) {
-  try {
-    return styleToJs(value, true);
-  } catch (JsValue cause) {
-    return JS.object();
+  if (JS.strictEqual(type, "mdxJsxFlowElement") || JS.strictEqual(type, "mdxJsxTextElement")) {
+    return mdxJsxElement(state, node, key);
   }
-}
-
-PropertyResult? createProperty(RuntimeState state, string prop, JsValue value) {
-  PropertyInfo propertyInfo = find(state.schema, prop);
-  if (JS.isNullish(value) || JS.isUndefined(value) || JS.typeOf(value) == "number" && JS.call(JS.get(Number, "isNaN"), JS.undefined(), value).truthy()) {
-    return null;
-  }
-  JsValue next = value;
-  if (JS.isArray(value)) {
-    next = stringifyList(value, propertyInfo.commaSeparated);
-  }
-  if (propertyInfo.property == "style") {
-    JsValue styleObject = next;
-    if (JS.typeOf(next) != "object") {
-      styleObject = parseStyle(JS.string(next));
-    }
-    return PropertyResult{"style", styleObject};
+  if (JS.strictEqual(type, "root")) {
+    JsValue props = object {};
+    addChildren(props, createChildren(state, node));
+    return create(Fragment, props, key);
   }
-  string key = propertyInfo.attribute;
-  if (propertyInfo.spaceSeparated) {
-    key = reactPropertyName(propertyInfo.property);
+  if (JS.strictEqual(type, "text")) {
+    return JS.get(node, "value");
   }
-  return PropertyResult{key, next};
+  return JS.undefined();
 }
 
-JsValue createElementProps(RuntimeState state, JsValue node) {
-  JsValue props = JS.object();
+JsValue element(State state, JsValue node, JsValue key) {
+  JsValue parentSchema = state.schema;
+  if (JS.strictEqual(JS.invoke(JS.get(node, "tagName"), "toLowerCase"), "svg") && JS.strictEqual(parentSchema, html)) {
+    state.schema = svg;
+  }
+  JS.invoke(state.ancestors, "push", node);
+  JsValue type = findComponentFromName(state, JS.get(node, "tagName"), false);
+  JsValue props = object {};
   JsValue alignValue = JS.undefined();
-  string tag = JS.string(JS.get(node, "tagName"));
   for (string prop in JS.get(node, "properties")) {
-    if (prop != "children" && objectHasOwn(JS.get(node, "properties"), prop)) {
-      PropertyResult? result = createProperty(state, prop, JS.get(JS.get(node, "properties"), prop));
-      if (result != null) {
-        string key = result.key;
-        JsValue value = result.value;
-        if (JS.strictEqual(key, "align") && JS.typeOf(value) == "string" && isTableCell(tag)) {
-          alignValue = value;
+    if (prop != "children" && JS.invoke(Object, "hasOwn", JS.get(node, "properties"), prop).truthy()) {
+      JsValue value = JS.get(JS.get(node, "properties"), prop);
+      PropertyInfo info = find(state.schema, prop);
+      // `value === null || value === undefined || (typeof value === 'number' && Number.isNaN(value))` skips it.
+      if (!JS.isNullish(value) && JS.strictEqual(value, value)) {
+        if (value.isArray()) {
+          // comma-separated-tokens / space-separated-tokens `stringify`
+          if (info.commaSeparated.truthy()) {
+            if (JS.strictEqual(value[value.length - 1], "")) {
+              value = JS.invoke(value, "concat", "");
+            }
+            value = JS.invoke(JS.invoke(value, "join", ", "), "trim");
+          } else {
+            value = JS.invoke(JS.invoke(value, "join", " "), "trim");
+          }
+        }
+        if (JS.strictEqual(info.property, "style")) {
+          if (JS.typeOf(value) != "object") {
+            value = styleToJs(JS.call(String, JS.undefined(), value));
+          }
+          props["style"] = value;
         } else {
-          JS.set(props, key, value);
+          JsValue name = if (info.space.truthy()) { reactName(info) } else { info.attribute };
+          if (JS.strictEqual(name, "align") && JS.typeOf(value) == "string" && (JS.strictEqual(JS.get(node, "tagName"), "td") || JS.strictEqual(JS.get(node, "tagName"), "th"))) {
+            alignValue = value;
+          } else {
+            props[name] = value;
+          }
         }
       }
     }
@@ -163,154 +103,115 @@
   if (alignValue.truthy()) {
     JsValue style = JS.get(props, "style");
     if (!style.truthy()) {
-      style = JS.object();
+      style = object {};
       props["style"] = style;
     }
     style["textAlign"] = alignValue;
   }
-  return props;
-}
-
-JsValue createJsxElementProps(RuntimeState state, JsValue node) {
-  JsValue props = JS.object();
+  JsValue children = createChildren(state, node);
+  JsValue tag = JS.get(node, "tagName");
+  if (JS.strictEqual(tag, "table") || JS.strictEqual(tag, "tbody") || JS.strictEqual(tag, "thead") || JS.strictEqual(tag, "tfoot") || JS.strictEqual(tag, "tr")) {
+    children = JS.invoke(children, "filter", JS.method1((JsValue _t, JsValue child) => {
+      return JS.typeOf(child) != "string" || !JS.invoke(whitespaceOnly, "test", child).truthy();
+    }));
+  }
+  return finish(state, node, type, props, children, parentSchema, key);
+}
+
+JsValue mdxJsxElement(State state, JsValue node, JsValue key) {
+  JsValue parentSchema = state.schema;
+  if (JS.strictEqual(JS.get(node, "name"), "svg") && JS.strictEqual(parentSchema, html)) {
+    state.schema = svg;
+  }
+  JS.invoke(state.ancestors, "push", node);
+  JsValue type = if (JS.strictEqual(JS.get(node, "name"), JS.assume(null))) { Fragment } else { findComponentFromName(state, JS.get(node, "name"), true) };
+  JsValue props = object {};
   JsValue attributes = JS.get(node, "attributes");
-  int index = 0;
-  while (index < attributes.length.toInt()) {
+  for (float index = 0; index < attributes.length; index++) {
     JsValue attribute = attributes[index];
     if (JS.strictEqual(JS.get(attribute, "type"), "mdxJsxExpressionAttribute")) {
       crashEstree(state, JS.get(node, "position"));
     } else {
+      JsValue name = JS.get(attribute, "name");
       JsValue value = JS.get(attribute, "value");
       if (value.truthy() && JS.typeOf(value) == "object") {
         crashEstree(state, JS.get(node, "position"));
-      } else if (JS.isNullish(value) && !JS.isUndefined(value)) {
-        value = true;
       }
-      JS.set(props, JS.string(JS.get(attribute, "name")), value);
+      props[name] = if (JS.strictEqual(value, JS.assume(null))) { true } else { value };
     }
-    index = index + 1;
   }
-  return props;
+  return finish(state, node, type, props, createChildren(state, node), parentSchema, key);
 }
 
-JsValue[] createChildren(RuntimeState state, JsValue node) {
-  JsValue[] children = [];
-  JsValue source = JS.get(node, "children");
-  Map<string, int> counts = new Map<string, int>();
-  int index = 0;
-  while (index < source.length.toInt()) {
-    JsValue child = source[index];
-    JsValue key = JS.undefined();
-    JsValue childType = JS.get(child, "type");
-    JsValue name = JS.undefined();
-    if (JS.strictEqual(childType, "element")) {
-      name = JS.get(child, "tagName");
-    } else if (JS.strictEqual(childType, "mdxJsxFlowElement") || JS.strictEqual(childType, "mdxJsxTextElement")) {
-      name = JS.get(child, "name");
-    }
-    if (name.truthy()) {
-      string childName = JS.string(name);
-      int? countValue = counts.get(childName);
-      int count = 0;
-      if (countValue != null) {
-        count = countValue;
-      }
-      key = childName + "-" + JS.string(count);
-      counts.set(childName, count + 1);
-    }
-    JsValue result = one(state, child, key);
-    if (!JS.isUndefined(result)) {
-      JS.push(children, result);
-    }
-    index = index + 1;
-  }
-  return children;
-}
-
-JsValue element(RuntimeState state, JsValue node, JsValue key) {
-  string parentSchema = state.schema;
-  string tag = JS.string(JS.get(node, "tagName"));
-  if (tag.toLowerCase() == "svg" && parentSchema == "html") {
-    state.schema = "svg";
-  }
-  state.ancestors.push(node);
-  JsValue type = findComponentFromName(state, tag, false);
-  JsValue props = createElementProps(state, node);
-  JsValue[] children = createChildren(state, node);
-  if (isTableElement(tag)) {
-    JsValue[] filtered = [];
-    int index = 0;
-    while (index < children.length) {
-      JsValue child = children[index];
-      if (JS.typeOf(child) != "string" || !whitespace(JS.string(child))) {
-        JS.push(filtered, child);
-      }
-      index = index + 1;
-    }
-    children = filtered;
+JsValue finish(State state, JsValue node, JsValue type, JsValue props, JsValue children, JsValue parentSchema, JsValue key) {
+  if (JS.typeOf(type) != "string" && !JS.strictEqual(type, Fragment)) {
+    props["node"] = node;
   }
-  addNode(state, props, type, node);
   addChildren(props, children);
-  state.ancestors.pop();
+  JS.invoke(state.ancestors, "pop");
   state.schema = parentSchema;
-  return JS.call(state.create, JS.undefined(), node, type, props, key);
+  return create(type, props, key);
 }
 
-JsValue root(RuntimeState state, JsValue node, JsValue key) {
-  JsValue props = JS.object();
-  addChildren(props, createChildren(state, node));
-  return JS.call(state.create, JS.undefined(), node, state.fragment, props, key);
-}
-
-JsValue mdxExpression(RuntimeState state, JsValue node) {
-  crashEstree(state, JS.get(node, "position"));
-  return JS.undefined();
-}
-
-JsValue mdxEsm(RuntimeState state, JsValue node) {
-  crashEstree(state, JS.get(node, "position"));
-  return JS.undefined();
+void addChildren(JsValue props, JsValue children) {
+  if (children.length > 0) {
+    JsValue value = if (children.length > 1) { children } else { children[0] };
+    if (value.truthy()) {
+      props["children"] = value;
+    }
+  }
 }
 
-JsValue mdxJsxElement(RuntimeState state, JsValue node, JsValue key) {
-  string parentSchema = state.schema;
-  JsValue name = JS.get(node, "name");
-  if (JS.strictEqual(name, "svg") && parentSchema == "html") {
-    state.schema = "svg";
-  }
-  state.ancestors.push(node);
-  JsValue type = state.fragment;
-  if (!JS.isNullish(name)) {
-    type = findComponentFromName(state, JS.string(name), true);
-  }
-  JsValue props = createJsxElementProps(state, node);
-  addNode(state, props, type, node);
-  addChildren(props, createChildren(state, node));
-  state.ancestors.pop();
-  state.schema = parentSchema;
-  return JS.call(state.create, JS.undefined(), node, type, props, key);
+JsValue create(JsValue type, JsValue props, JsValue key) {
+  JsValue fn = if (JS.get(props, "children").isArray()) { jsxs } else { jsx };
+  if (key.truthy()) {
+    return JS.call(fn, JS.undefined(), type, props, key);
+  }
+  return JS.call(fn, JS.undefined(), type, props);
 }
 
-JsValue one(RuntimeState state, JsValue node, JsValue key) {
-  JsValue type = JS.get(node, "type");
-  if (JS.strictEqual(type, "element")) return element(state, node, key);
-  if (JS.strictEqual(type, "mdxFlowExpression") || JS.strictEqual(type, "mdxTextExpression")) return mdxExpression(state, node);
-  if (JS.strictEqual(type, "mdxJsxFlowElement") || JS.strictEqual(type, "mdxJsxTextElement")) return mdxJsxElement(state, node, key);
-  if (JS.strictEqual(type, "mdxjsEsm")) return mdxEsm(state, node);
-  if (JS.strictEqual(type, "root")) return root(state, node, key);
-  if (JS.strictEqual(type, "text")) return JS.get(node, "value");
-  return JS.undefined();
+JsValue createChildren(State state, JsValue node) {
+  JsValue children = JS.array();
+  JsValue countsByName = JS.construct(Map);
+  for (float index = 0; index < JS.get(node, "children").length; index++) {
+    JsValue child = JS.get(node, "children")[index];
+    JsValue key = JS.undefined();
+    JsValue name = if (JS.strictEqual(JS.get(child, "type"), "element")) {
+      JS.get(child, "tagName")
+    } else {
+      if (JS.strictEqual(JS.get(child, "type"), "mdxJsxFlowElement") || JS.strictEqual(JS.get(child, "type"), "mdxJsxTextElement")) { JS.get(child, "name") } else { JS.undefined() }
+    };
+    if (name.truthy()) {
+      JsValue count = JS.or(JS.invoke(countsByName, "get", name), 0);
+      key = JS.add(JS.add(name, "-"), count);
+      JS.invoke(countsByName, "set", name, JS.add(count, 1));
+    }
+    JsValue result = one(state, child, key);
+    if (!JS.isUndefined(result)) {
+      JS.invoke(children, "push", result);
+    }
+  }
+  return children;
 }
 
-export JsValue toJsxRuntime(JsValue tree, JsValue fragment, JsValue components, JsValue jsxFn, JsValue jsxsFn) {
-  JsValue create = productionCreate(jsxFn, jsxsFn);
-  if (!components.truthy()) components = JS.object();
-  RuntimeState state = RuntimeState{fragment, [], components, create, "html"};
-  JsValue result = one(state, tree, JS.undefined());
-  if (result.truthy() && JS.typeOf(result) != "string") {
-    return result;
-  }
-  JsValue child = JS.undefined();
-  if (result.truthy()) child = result;
-  return JS.call(create, JS.undefined(), tree, state.fragment, JS.object("children", child), JS.undefined());
+JsValue findComponentFromName(State state, JsValue name, bool allowExpression) {
+  if (allowExpression && (JS.invoke(name, "includes", ".").truthy() || JS.invoke(identifierName, "test", name).truthy() && !JS.invoke(lowercaseStart, "test", name).truthy())) {
+    crashEstree(state, JS.undefined());
+  }
+  if (JS.invoke(Object, "hasOwn", state.components, name).truthy()) {
+    return state.components[name];
+  }
+  return name;
+}
+
+void crashEstree(State state, JsValue place) {
+  JsValue message = createVFileMessage("Cannot handle MDX estrees without `createEvaluater`", object {
+    ancestors: state.ancestors,
+    place: place,
+    ruleId: "mdx-estree",
+    source: "hast-util-to-jsx-runtime"
+  });
+  message["file"] = JS.undefined();
+  message["url"] = "https://github.com/syntax-tree/hast-util-to-jsx-runtime#cannot-handle-mdx-estrees-without-createevaluater";
+  throw message;
 }
diff -ruN a/src/host.lil b/src/host.lil
--- a/src/host.lil	2026-09-23 20:40:45.025690507 +0000
+++ b/src/host.lil	2026-09-23 20:40:45.364693490 +0000
@@ -1,3 +1,4 @@
+// Host globals shared by the port and its sibling graph (src/graph/** imports these externs).
 export extern JsValue Object;
 export extern JsValue Error;
 export extern JsValue TypeError;
@@ -15,118 +16,3 @@
 export extern JsValue Promise;
 export extern JsValue encodeURIComponent;
 export extern JsValue structuredClone;
-
-export JsValue NULL = JS.invoke(JSON, "parse", "null");
-export JsValue HAS_OWN = JS.get(JS.get(Object, "prototype"), "hasOwnProperty");
-export JsValue TO_STRING = JS.get(JS.get(Object, "prototype"), "toString");
-export JsValue PROPERTY_IS_ENUMERABLE = JS.get(JS.get(Object, "prototype"), "propertyIsEnumerable");
-
-export JsValue undef() {
-  return JS.undefined();
-}
-
-export bool isUndef(JsValue value) {
-  return JS.isUndefined(value);
-}
-
-export bool isNullish(JsValue value) {
-  return JS.isNullish(value);
-}
-
-export bool isFn(JsValue value) {
-  return JS.typeOf(value) == "function";
-}
-
-export bool isStr(JsValue value) {
-  return JS.typeOf(value) == "string";
-}
-
-export bool isObj(JsValue value) {
-  return !JS.isNullish(value) && JS.typeOf(value) == "object";
-}
-
-export int len(JsValue value) {
-  return value.length.toInt();
-}
-
-export JsValue[] copyArray(JsValue list) {
-  JsValue[] out = [];
-  int n = list.length.toInt();
-  int i = 0;
-  while (i < n) {
-    JS.push(out, list[i]);
-    i = i + 1;
-  }
-  return out;
-}
-
-export JsValue[] sliceArgs(JsValue list, int start) {
-  JsValue[] out = [];
-  int n = list.length.toInt();
-  int i = start;
-  while (i < n) {
-    JS.push(out, list[i]);
-    i = i + 1;
-  }
-  return out;
-}
-
-export bool objectHasOwn(JsValue obj, string key) {
-  return JS.call(HAS_OWN, obj, key).truthy();
-}
-
-export void setIndex(JsValue arr, int index, JsValue value) {
-  JS.set(arr, JS.string(index), value);
-}
-
-export bool isErrorInstance(JsValue value) {
-  if (JS.isNullish(value)) {
-    return false;
-  }
-  string t = JS.typeOf(value);
-  if (t != "object" && t != "function") {
-    return false;
-  }
-  return JS.invoke(JS.get(Error, "prototype"), "isPrototypeOf", value).truthy();
-}
-
-export bool looksLikeAValue(JsValue value) {
-  if (JS.typeOf(value) == "string") {
-    return true;
-  }
-  return isUint8Array(value);
-}
-
-export bool isUint8Array(JsValue value) {
-  if (!value.truthy()) {
-    return false;
-  }
-  if (JS.typeOf(value) != "object") {
-    return false;
-  }
-  return JS.in("byteLength", value) && JS.in("byteOffset", value);
-}
-
-export void throwError(string message) {
-  throw JS.construct(Error, message);
-}
-
-export void throwType(string message) {
-  throw JS.construct(TypeError, message);
-}
-
-export void bail(JsValue error) {
-  if (error.truthy()) {
-    throw error;
-  }
-}
-
-export void assertOk(JsValue value, string message) {
-  if (!value.truthy()) {
-    throw JS.construct(Error, message);
-  }
-}
-
-export JsValue protoOf(JsValue value) {
-  return JS.invoke(Object, "getPrototypeOf", value);
-}
diff -ruN a/src/index.lil b/src/index.lil
--- a/src/index.lil	2026-09-23 20:40:45.035690595 +0000
+++ b/src/index.lil	2026-09-23 20:40:45.364605717 +0000
@@ -1,11 +1,8 @@
-import {
-  MarkdownAsync as markdownAsyncExport,
-  MarkdownHooks as markdownHooksExport,
-  defaultUrlTransform as defaultUrlTransformExport,
-  default as markdownDefault
-} from "./lib/index.lil";
+import { Markdown, MarkdownAsync, MarkdownHooks, defaultUrlTransform } from "./lib/index.lil";
+import { Object } from "./host.lil";
 
-export JsValue MarkdownAsync = markdownAsyncExport;
-export JsValue MarkdownHooks = markdownHooksExport;
-export JsValue defaultUrlTransform = defaultUrlTransformExport;
-export JsValue default = markdownDefault;
+export { MarkdownAsync, MarkdownHooks, defaultUrlTransform };
+export JsValue default = Markdown;
+
+// Upstream's `export function Markdown` is the default export; keep its name.
+JS.invoke(Object, "defineProperty", Markdown, "name", object { value: "Markdown" });
diff -ruN a/src/lib/index.lil b/src/lib/index.lil
--- a/src/lib/index.lil	2026-09-23 20:40:45.055690771 +0000
+++ b/src/lib/index.lil	2026-09-23 20:40:45.354505823 +0000
@@ -1,186 +1,100 @@
-import { Object, PROPERTY_IS_ENUMERABLE, Reflect, RegExp, String, objectHasOwn, setIndex } from "../host.lil";
+// react-markdown@10.1.0 `lib/index.js`. Every read of `options` happens in upstream's order (the getter
+// trace is part of the behaviour: createProcessor, createFile, then post).
+import { Object, RegExp, String } from "../host.lil";
 import { unreachable } from "../devlop.lil";
 import { unified } from "../graph/unified/entry.lil";
 import { default as remarkParse } from "../graph/remark-parse/entry.lil";
 import { default as remarkRehype } from "../graph/remark-rehype/entry.lil";
 import { toJsxRuntime } from "../hast-util-to-jsx-runtime/lib/index.lil";
-import { UrlAttribute, urlAttributes } from "../urls.lil";
+import { urlAttributes } from "../urls.lil";
 import { visit } from "../unist-util-visit/lib/index.lil";
 import { VFile } from "../vfile/lib/index.lil";
 
-export extern JsValue Fragment;
-export extern JsValue jsx;
-export extern JsValue jsxs;
 export extern JsValue useState;
 export extern JsValue useEffect;
 
-string changelog = "https://github.com/remarkjs/react-markdown/blob/main/changelog.md";
 JsValue[] emptyPlugins = [];
-JsValue emptyRemarkRehypeOptions = JS.object("allowDangerousHtml", true);
+JsValue emptyRemarkRehypeOptions = object { allowDangerousHtml: true };
 JsValue safeProtocol = JS.construct(RegExp, "^(https?|ircs?|mailto|xmpp)$", "i");
 
-struct Deprecation {
-  string property;
-  string anchor;
-  string replacement;
+// Upstream's deprecation table as `from:id` or `from:id:to` rows.
+JsValue deprecations = JS.invoke("astPlugins:remove-buggy-html-in-markdown-parser allowDangerousHtml:remove-buggy-html-in-markdown-parser allowNode:replace-allownode-allowedtypes-and-disallowedtypes:allowElement allowedTypes:replace-allownode-allowedtypes-and-disallowedtypes:allowedElements className:remove-classname disallowedTypes:replace-allownode-allowedtypes-and-disallowedtypes:disallowedElements escapeHtml:remove-buggy-html-in-markdown-parser includeElementIndex:#remove-includeelementindex includeNodeIndex:change-includenodeindex-to-includeelementindex linkTarget:remove-linktarget plugins:change-plugins-to-remarkplugins:remarkPlugins rawSourcePos:#remove-rawsourcepos renderers:change-renderers-to-components:components source:change-source-to-children:children sourcePos:#remove-sourcepos transformImageUri:#add-urltransform:urlTransform transformLinkUri:#add-urltransform:urlTransform", "split", " ");
+
+export JsValue Markdown(JsValue options) {
+  JsValue processor = createProcessor(options);
+  JsValue file = createFile(options);
+  return post(JS.invoke(processor, "runSync", JS.invoke(processor, "parse", file), file), options);
 }
 
-Deprecation[] deprecations = [
-  Deprecation{"astPlugins", "remove-buggy-html-in-markdown-parser", ""},
-  Deprecation{"allowDangerousHtml", "remove-buggy-html-in-markdown-parser", ""},
-  Deprecation{"allowNode", "replace-allownode-allowedtypes-and-disallowedtypes", "allowElement"},
-  Deprecation{"allowedTypes", "replace-allownode-allowedtypes-and-disallowedtypes", "allowedElements"},
-  Deprecation{"className", "remove-classname", ""},
-  Deprecation{"disallowedTypes", "replace-allownode-allowedtypes-and-disallowedtypes", "disallowedElements"},
-  Deprecation{"escapeHtml", "remove-buggy-html-in-markdown-parser", ""},
-  Deprecation{"includeElementIndex", "#remove-includeelementindex", ""},
-  Deprecation{"includeNodeIndex", "change-includenodeindex-to-includeelementindex", ""},
-  Deprecation{"linkTarget", "remove-linktarget", ""},
-  Deprecation{"plugins", "change-plugins-to-remarkplugins", "remarkPlugins"},
-  Deprecation{"rawSourcePos", "#remove-rawsourcepos", ""},
-  Deprecation{"renderers", "change-renderers-to-components", "components"},
-  Deprecation{"source", "change-source-to-children", "children"},
-  Deprecation{"sourcePos", "#remove-sourcepos", ""},
-  Deprecation{"transformImageUri", "#add-urltransform", "urlTransform"},
-  Deprecation{"transformLinkUri", "#add-urltransform", "urlTransform"}
-];
-
-export JsValue defaultUrlTransform(JsValue raw) {
-  JsValue value = raw;
-  int colon = JS.number(JS.invoke(value, "indexOf", ":")).toInt();
-  int questionMark = JS.number(JS.invoke(value, "indexOf", "?")).toInt();
-  int numberSign = JS.number(JS.invoke(value, "indexOf", "#")).toInt();
-  int slash = JS.number(JS.invoke(value, "indexOf", "/")).toInt();
-  if (colon == -1) {
-    return value;
-  }
-  if (slash != -1 && colon > slash) {
-    return value;
-  }
-  if (questionMark != -1 && colon > questionMark) {
-    return value;
-  }
-  if (numberSign != -1 && colon > numberSign) {
-    return value;
-  }
-  if (JS.invoke(safeProtocol, "test", JS.invoke(value, "slice", 0, colon)).truthy()) {
-    return value;
-  }
-  return "";
+export async JsValue MarkdownAsync(JsValue options) {
+  JsValue processor = createProcessor(options);
+  JsValue file = createFile(options);
+  Task<JsValue> ran = JS.assume(JS.invoke(processor, "run", JS.invoke(processor, "parse", file), file));
+  return post(await ran, options);
 }
 
-void assignOwn(JsValue target, JsValue source) {
-  if (JS.isUndefined(source) || JS.isNullish(source)) {
-    return;
+export JsValue MarkdownHooks(JsValue options) {
+  JsValue processor = createProcessor(options);
+  JsValue errorState = JS.call(useState, JS.undefined(), JS.undefined());
+  JsValue setError = errorState[1];
+  JsValue treeState = JS.call(useState, JS.undefined(), JS.undefined());
+  JsValue setTree = treeState[1];
+  JS.call(useEffect, JS.undefined(), JS.method0((JsValue _e) => {
+    bool cancelled = false;
+    JsValue file = createFile(options);
+    JS.invoke(processor, "run", JS.invoke(processor, "parse", file), file, JS.method2((JsValue _s, JsValue error, JsValue tree) => {
+      if (!cancelled) {
+        JS.call(setError, JS.undefined(), error);
+        JS.call(setTree, JS.undefined(), tree);
+      }
+      return JS.undefined();
+    }));
+    return JS.method0((JsValue _c) => {
+      cancelled = true;
+      return JS.undefined();
+    });
+  }), [
+    JS.get(options, "children"),
+    JS.get(options, "rehypePlugins"),
+    JS.get(options, "remarkPlugins"),
+    JS.get(options, "remarkRehypeOptions")
+  ]);
+  if (errorState[0].truthy()) {
+    throw errorState[0];
   }
-  JsValue from = JS.call(Object, JS.undefined(), source);
-  JsValue keys = JS.invoke(Reflect, "ownKeys", from);
-  int i = 0;
-  int n = keys.length.toInt();
-  while (i < n) {
-    JsValue key = keys[i];
-    if (JS.call(PROPERTY_IS_ENUMERABLE, from, key).truthy()) {
-      JsValue descriptor = JS.object(
-        "configurable",
-        true,
-        "enumerable",
-        true,
-        "value",
-        JS.invoke(Reflect, "get", from, key),
-        "writable",
-        true
-      );
-      JS.invoke(Reflect, "defineProperty", target, key, descriptor);
-    }
-    i = i + 1;
+  JsValue tree = treeState[0];
+  if (tree.truthy()) {
+    return post(tree, options);
   }
+  return JS.get(options, "fallback");
 }
 
 JsValue createProcessor(JsValue options) {
-  JsValue rehypePlugins = JS.get(options, "rehypePlugins");
-  if (!rehypePlugins.truthy()) {
-    rehypePlugins = emptyPlugins;
-  }
-  JsValue remarkPlugins = JS.get(options, "remarkPlugins");
-  if (!remarkPlugins.truthy()) {
-    remarkPlugins = emptyPlugins;
-  }
+  JsValue rehypePlugins = JS.or(JS.get(options, "rehypePlugins"), emptyPlugins);
+  JsValue remarkPlugins = JS.or(JS.get(options, "remarkPlugins"), emptyPlugins);
   JsValue remarkRehypeOptions = emptyRemarkRehypeOptions;
-  JsValue extra = JS.get(options, "remarkRehypeOptions");
-  if (extra.truthy()) {
-    JsValue merged = JS.object();
-    assignOwn(merged, JS.get(options, "remarkRehypeOptions"));
-    assignOwn(merged, emptyRemarkRehypeOptions);
-    remarkRehypeOptions = merged;
+  if (JS.get(options, "remarkRehypeOptions").truthy()) {
+    // `{...options.remarkRehypeOptions, ...emptyRemarkRehypeOptions}`: assigning into a null-prototype object
+    // defines every key (an own `__proto__` too), as spread does; the copy then gets spread's prototype.
+    remarkRehypeOptions = JS.invoke(Object, "setPrototypeOf",
+      JS.invoke(Object, "assign", JS.invoke(Object, "create", JS.assume(null)), JS.get(options, "remarkRehypeOptions"), emptyRemarkRehypeOptions),
+      JS.get(Object, "prototype"));
   }
   JsValue processor = JS.call(unified, JS.undefined());
-  processor = JS.call(JS.get(processor, "use"), processor, remarkParse);
-  processor = JS.call(JS.get(processor, "use"), processor, remarkPlugins);
-  processor = JS.call(JS.get(processor, "use"), processor, remarkRehype, remarkRehypeOptions);
-  processor = JS.call(JS.get(processor, "use"), processor, rehypePlugins);
-  return processor;
+  return JS.invoke(JS.invoke(JS.invoke(JS.invoke(processor, "use", remarkParse), "use", remarkPlugins), "use", remarkRehype, remarkRehypeOptions), "use", rehypePlugins);
 }
 
 JsValue createFile(JsValue options) {
-  JsValue children = JS.get(options, "children");
-  if (!children.truthy()) {
-    children = "";
-  }
+  JsValue children = JS.or(JS.get(options, "children"), "");
   JsValue file = JS.construct(VFile);
   if (JS.typeOf(children) == "string") {
     file["value"] = children;
-    return file;
+  } else {
+    unreachable("Unexpected value `" + children + "` for `children` prop, expected `string`");
   }
-  JS.call(unreachable, JS.undefined(), "Unexpected value `" + JS.string(children) + "` for `children` prop, expected `string`");
   return file;
 }
 
-void checkDeprecations(JsValue options) {
-  int i = 0;
-  int n = deprecations.length;
-  while (i < n) {
-    Deprecation deprecation = deprecations[i];
-    string from = deprecation.property;
-    if (objectHasOwn(options, from)) {
-      string hint = "remove it";
-      if (deprecation.replacement.length > 0) {
-        hint = "use `" + deprecation.replacement + "` instead";
-      }
-      JS.call(unreachable, JS.undefined(),
-        "Unexpected `" +
-          from +
-          "` prop, " +
-          hint +
-          " (see <" +
-          changelog +
-          "#" +
-          deprecation.anchor +
-          "> for more info)"
-      );
-    }
-    i = i + 1;
-  }
-}
-
-void spliceRemove(JsValue kids, int index) {
-  JS.invoke(kids, "splice", index, 1);
-}
-
-void spliceUnwrap(JsValue kids, int index, JsValue nodeChildren) {
-  JsValue[] args = [];
-  JS.push(args, index);
-  JS.push(args, 1);
-  if (JS.isArray(nodeChildren)) {
-    int i = 0;
-    int n = nodeChildren.length.toInt();
-    while (i < n) {
-      JS.push(args, nodeChildren[i]);
-      i = i + 1;
-    }
-  }
-  JS.apply(JS.get(kids, "splice"), kids, args);
-}
-
 JsValue post(JsValue tree, JsValue options) {
   JsValue allowedElements = JS.get(options, "allowedElements");
   JsValue allowElement = JS.get(options, "allowElement");
@@ -188,143 +102,88 @@
   JsValue disallowedElements = JS.get(options, "disallowedElements");
   JsValue skipHtml = JS.get(options, "skipHtml");
   JsValue unwrapDisallowed = JS.get(options, "unwrapDisallowed");
-  JsValue urlTransform = JS.get(options, "urlTransform");
-  if (!urlTransform.truthy()) {
-    urlTransform = defaultUrlTransform;
+  JsValue urlTransform = JS.or(JS.get(options, "urlTransform"), defaultUrlTransform);
+
+  for (float index = 0; index < deprecations.length; index++) {
+    JsValue deprecation = JS.invoke(deprecations[index], "split", ":");
+    if (JS.invoke(Object, "hasOwn", options, deprecation[0]).truthy()) {
+      JsValue to = deprecation[2];
+      unreachable(
+        "Unexpected `" + deprecation[0] + "` prop, " +
+          (if (to.truthy()) { "use `" + to + "` instead" } else { "remove it" }) +
+          " (see <https://github.com/remarkjs/react-markdown/blob/main/changelog.md#" + deprecation[1] + "> for more info)"
+      );
+    }
   }
-  checkDeprecations(options);
+
   if (allowedElements.truthy() && disallowedElements.truthy()) {
-    JS.call(unreachable, JS.undefined(), "Unexpected combined `allowedElements` and `disallowedElements`, expected one or the other");
+    unreachable("Unexpected combined `allowedElements` and `disallowedElements`, expected one or the other");
   }
-  JsValue transform = JS.method3((JsValue _s, JsValue node, JsValue index, JsValue parent) => {
-    if (JS.strictEqual(JS.get(node, "type"), "raw") && parent.truthy() && JS.typeOf(index) == "number") {
+
+  visit(tree, JS.undefined(), (JsValue node, JsValue index, JsValue parent) => {
+    bool numeric = JS.typeOf(index) == "number";
+    if (JS.strictEqual(JS.get(node, "type"), "raw") && parent.truthy() && numeric) {
       if (skipHtml.truthy()) {
-        spliceRemove(JS.get(parent, "children"), JS.number(index).toInt());
+        JS.invoke(JS.get(parent, "children"), "splice", index, 1);
       } else {
-        JsValue text = JS.object();
-        text["type"] = "text";
-        text["value"] = JS.get(node, "value");
-        setIndex(JS.get(parent, "children"), JS.number(index).toInt(), text);
+        JsValue children = JS.get(parent, "children");
+        children[index] = object { type: "text", value: JS.get(node, "value") };
       }
       return index;
     }
+
     if (JS.strictEqual(JS.get(node, "type"), "element")) {
-      int attributeIndex = 0;
-      while (attributeIndex < urlAttributes.length) {
-        UrlAttribute attribute = urlAttributes[attributeIndex];
-        string key = attribute.property;
-        if (objectHasOwn(JS.get(node, "properties"), key)) {
+      for (string key in urlAttributes) {
+        if (JS.invoke(Object, "hasOwn", urlAttributes, key).truthy() && JS.invoke(Object, "hasOwn", JS.get(node, "properties"), key).truthy()) {
           JsValue value = JS.get(JS.get(node, "properties"), key);
-          bool applies = attribute.allElements;
-          if (!applies) {
-            string tagName = JS.string(JS.get(node, "tagName"));
-            int elementIndex = 0;
-            while (elementIndex < attribute.elements.length) {
-              if (attribute.elements[elementIndex] == tagName) {
-                applies = true;
-                break;
-              }
-              elementIndex = elementIndex + 1;
-            }
-          }
-          if (applies) {
-            string raw = "";
-            if (value.truthy()) {
-              raw = JS.string(JS.call(String, JS.undefined(), value));
-            }
+          JsValue test = JS.get(urlAttributes, key);
+          if (!test.truthy() || JS.invoke(test, "includes", JS.get(node, "tagName")).truthy()) {
             JsValue properties = JS.get(node, "properties");
-            properties[key] = JS.call(urlTransform, JS.undefined(), raw, key, node);
+            properties[key] = JS.call(urlTransform, JS.undefined(), JS.call(String, JS.undefined(), JS.or(value, "")), key, node);
           }
         }
-        attributeIndex = attributeIndex + 1;
       }
     }
+
     if (JS.strictEqual(JS.get(node, "type"), "element")) {
-      bool remove = false;
-      if (allowedElements.truthy()) {
-        remove = !JS.invoke(allowedElements, "includes", JS.get(node, "tagName")).truthy();
-      } else if (disallowedElements.truthy()) {
-        remove = JS.invoke(disallowedElements, "includes", JS.get(node, "tagName")).truthy();
-      }
-      if (!remove && allowElement.truthy() && JS.typeOf(index) == "number") {
+      JsValue remove = if (allowedElements.truthy()) {
+        !JS.invoke(allowedElements, "includes", JS.get(node, "tagName")).truthy()
+      } else {
+        JS.and(disallowedElements, JS.invoke(disallowedElements, "includes", JS.get(node, "tagName")))
+      };
+      if (!remove.truthy() && allowElement.truthy() && numeric) {
         remove = !JS.call(allowElement, JS.undefined(), node, index, parent).truthy();
       }
-      if (remove && parent.truthy() && JS.typeOf(index) == "number") {
+      if (remove.truthy() && parent.truthy() && numeric) {
+        JsValue children = JS.get(parent, "children");
         if (unwrapDisallowed.truthy() && JS.get(node, "children").truthy()) {
-          spliceUnwrap(JS.get(parent, "children"), JS.number(index).toInt(), JS.get(node, "children"));
+          JS.apply(JS.get(children, "splice"), children, JS.invoke(JS.array(index, 1), "concat", JS.get(node, "children")));
         } else {
-          spliceRemove(JS.get(parent, "children"), JS.number(index).toInt());
+          JS.invoke(children, "splice", index, 1);
         }
         return index;
       }
     }
     return JS.undefined();
   });
-  visit(tree, transform);
-  return toJsxRuntime(tree, Fragment, components, jsx, jsxs);
-}
 
-export JsValue Markdown(JsValue options) {
-  JsValue processor = createProcessor(options);
-  JsValue file = createFile(options);
-  JsValue parsed = JS.call(JS.get(processor, "parse"), processor, file);
-  JsValue tree = JS.call(JS.get(processor, "runSync"), processor, parsed, file);
-  return post(tree, options);
+  return toJsxRuntime(tree, components);
 }
 
-export async JsValue MarkdownAsync(JsValue options) {
-  JsValue processor = createProcessor(options);
-  JsValue file = createFile(options);
-  JsValue parsed = JS.call(JS.get(processor, "parse"), processor, file);
-  JsValue ran = JS.call(JS.get(processor, "run"), processor, parsed, file);
-  JsValue tree = await Task.resolve(ran);
-  return post(tree, options);
-}
-
-export JsValue MarkdownHooks(JsValue options) {
-  JsValue processor = createProcessor(options);
-  JsValue errorState = JS.call(useState, JS.undefined(), JS.undefined());
-  JsValue setError = errorState[1];
-  JsValue error = errorState[0];
-  JsValue treeState = JS.call(useState, JS.undefined(), JS.undefined());
-  JsValue setTree = treeState[1];
-  JsValue tree = treeState[0];
-  JsValue[] effectDeps = [];
-  JS.push(effectDeps, JS.get(options, "children"));
-  JS.push(effectDeps, JS.get(options, "rehypePlugins"));
-  JS.push(effectDeps, JS.get(options, "remarkPlugins"));
-  JS.push(effectDeps, JS.get(options, "remarkRehypeOptions"));
-  JS.call(useEffect, JS.undefined(), JS.method0((JsValue _e) => {
-    bool cancelled = false;
-    JsValue file = createFile(options);
-    JS.call(JS.get(processor, "run"), processor, JS.call(JS.get(processor, "parse"), processor, file), file, JS.method2((JsValue _s, JsValue nextError, JsValue nextTree) => {
-      if (!cancelled) {
-        JS.call(setError, JS.undefined(), nextError);
-        JS.call(setTree, JS.undefined(), nextTree);
-      }
-      return JS.undefined();
-    }));
-    return JS.method0((JsValue _c) => {
-      cancelled = true;
-      return JS.undefined();
-    });
-  }), effectDeps);
-  if (error.truthy()) {
-    throw error;
-  }
-  if (tree.truthy()) {
-    return post(tree, options);
+export JsValue defaultUrlTransform(JsValue value) {
+  JsValue colon = JS.invoke(value, "indexOf", ":");
+  JsValue questionMark = JS.invoke(value, "indexOf", "?");
+  JsValue numberSign = JS.invoke(value, "indexOf", "#");
+  JsValue slash = JS.invoke(value, "indexOf", "/");
+  if (
+    JS.strictEqual(colon, -1) ||
+    !JS.strictEqual(slash, -1) && JS.greaterThan(colon, slash) ||
+    !JS.strictEqual(questionMark, -1) && JS.greaterThan(colon, questionMark) ||
+    !JS.strictEqual(numberSign, -1) && JS.greaterThan(colon, numberSign) ||
+    JS.invoke(safeProtocol, "test", JS.invoke(value, "slice", 0, colon)).truthy()
+  ) {
+    return value;
   }
-  return JS.get(options, "fallback");
-}
-
-export JsValue default = Markdown;
-
-void setFunctionName(JsValue value, string name) {
-  JS.invoke(Object, "defineProperty", value, "name", JS.object("configurable", true, "value", name));
+  return "";
 }
 
-setFunctionName(Markdown, "Markdown");
-setFunctionName(MarkdownAsync, "MarkdownAsync");
-setFunctionName(MarkdownHooks, "MarkdownHooks");
-setFunctionName(defaultUrlTransform, "defaultUrlTransform");
diff -ruN a/src/property-information/lib/find.lil b/src/property-information/lib/find.lil
--- a/src/property-information/lib/find.lil	2026-09-23 20:40:45.234902415 +0000
+++ b/src/property-information/lib/find.lil	2026-09-23 20:40:45.362422997 +0000
@@ -1,173 +1,118 @@
-import { commonSchemaBlob, htmlSchemaBlob, svgSchemaBlob } from "./schema-data.lil";
+// property-information@7.2.0: the merged `html` and `svg` schemas and `find(schema, value)`, as upstream.
+// A schema is upstream's pair of tables: `normal` maps a lowercased property or attribute name to a
+// property name, `property` maps a property name to its info; `find` looks names up with `in` (inherited
+// names included, as upstream). The tables are built once from the generated rows in schema-data.lil.
+import {
+  ariaRows, commonOnRows, commonRows, htmlCommaRows, htmlOnRows, htmlRows, noSpaceRows,
+  svgCommaRows, svgDashRows, svgLowerRows, svgOnRows, svgRows
+} from "./schema-data.lil";
 
-export struct PropertyInfo {
-  string property;
-  string attribute;
-  bool spaceSeparated;
-  bool commaSeparated;
-}
+extern JsValue RegExp;
 
-Map<string, PropertyInfo> records = new Map<string, PropertyInfo>();
-bool initialized = false;
-
-string camelToDash(string value) {
-  string output = "";
-  int index = 0;
-  while (index < value.length) {
-    int code = value.charCodeAt(index);
-    if (code >= 65 && code <= 90) {
-      output = output + "-" + value.charAt(index).toLowerCase();
+export extern class PropertyInfo {
+  string property;
+  JsValue attribute;
+  JsValue space;
+  JsValue commaSeparated;
+}
+
+extern class Schema {
+  JsValue normal;
+  JsValue property;
+}
+
+export JsValue html = object { normal: object {}, property: object {} };
+export JsValue svg = object { normal: object {}, property: object {} };
+
+JsValue cap = JS.construct(RegExp, "[A-Z]", "g");
+JsValue dash = JS.construct(RegExp, "-[a-z]", "g");
+JsValue valid = JS.construct(RegExp, "^data[-\\w.:]+$", "i");
+
+JsValue kebab = JS.method1((JsValue _t, JsValue found) => JS.add("-", JS.invoke(found, "toLowerCase")));
+export JsValue camelcase = JS.method1((JsValue _t, JsValue found) => JS.invoke(JS.invoke(found, "charAt", 1), "toUpperCase"));
+
+void define(JsValue target, string property, JsValue attribute, bool space, JsValue comma) {
+  Schema schema = JS.assume(target);
+  schema.property[property] = object { attribute: attribute, property: property, space: space, commaSeparated: comma };
+  schema.normal[property.toLowerCase()] = property;
+  schema.normal[JS.invoke(attribute, "toLowerCase")] = property;
+}
+
+// A group of rows (see schema-data.lil): space-separated `name` or `name|attribute`; the property is `prefix`
+// + name, the attribute follows `rule` unless given: 0 the property, 1 its lowercase, 2 its dash-case,
+// 3 `aria-` + the lowercased name, 4 the property. Rules 3 and 4 have no space. `only` is the schema to define
+// into, or nothing for both.
+void load(string rows, int rule, JsValue only, JsValue comma, JsValue prefix) {
+  JsValue list = JS.invoke(rows, "split", " ");
+  for (float index = 0; index < list.length; index++) {
+    JsValue fields = JS.invoke(list[index], "split", "|");
+    JsValue name = fields[0];
+    string property = JS.assume(JS.add(JS.or(prefix, ""), name));
+    JsValue attribute = JS.or(fields[1], if (rule == 3) {
+      JS.add("aria-", JS.invoke(name, "toLowerCase"))
     } else {
-      output = output + value.charAt(index);
-    }
-    index = index + 1;
-  }
-  return output;
-}
-
-void addRecord(string schema, string property, string attribute, string flags, bool infoSpace) {
-  if (attribute == "~") attribute = property.toLowerCase();
-  if (attribute == "@") attribute = "aria-" + property.slice(4).toLowerCase();
-  if (attribute == "-") attribute = camelToDash(property);
-  if (attribute.length == 0) attribute = property;
-  PropertyInfo info = PropertyInfo{property, attribute, infoSpace, flags.indexOf("c") >= 0};
-  records.set(schema + "|" + property.toLowerCase(), info);
-  records.set(schema + "|" + attribute.toLowerCase(), info);
-}
-
-void addCommonRecord(string property, string attribute, string flags) {
-  bool infoSpace = flags.indexOf("s") >= 0;
-  addRecord("html", property, attribute, flags, infoSpace);
-  addRecord("svg", property, attribute, flags, infoSpace);
-}
-
-void load(string schema, string blob) {
-  int start = 0;
-  int index = 0;
-  while (index <= blob.length) {
-    if (index == blob.length || blob.charAt(index) == "\n") {
-      string line = blob.slice(start, index);
-      int first = line.indexOf("|");
-      int second = line.indexOf("|", first + 1);
-      string property = line;
-      string attribute = "";
-      string flags = "";
-      if (first >= 0) {
-        property = line.slice(0, first);
-        if (second > first) {
-          attribute = line.slice(first + 1, second);
-          flags = line.slice(second + 1);
-        } else {
-          attribute = line.slice(first + 1);
-        }
-      }
-      if (schema.length == 0) {
-        addCommonRecord(property, attribute, flags);
-      } else {
-        addRecord(schema, property, attribute, flags, true);
-      }
-      start = index + 1;
-    }
-    index = index + 1;
-  }
-}
-
-void initialize() {
-  if (!initialized) {
-    initialized = true;
-    load("", commonSchemaBlob);
-    load("html", htmlSchemaBlob);
-    load("svg", svgSchemaBlob);
-  }
-}
-
-string dashToCamel(string value) {
-  string output = "";
-  int index = 0;
-  while (index < value.length) {
-    if (value.charAt(index) == "-" && index + 1 < value.length) {
-      int code = value.charCodeAt(index + 1);
-      if (code >= 97 && code <= 122) {
-        output = output + value.charAt(index + 1).toUpperCase();
-        index = index + 2;
-        continue;
-      }
+      if (rule == 2) { JS.invoke(property, "replace", cap, kebab) } else { if (rule == 1) { property.toLowerCase() } else { property } }
+    });
+    bool space = rule < 3;
+    if (only.truthy()) {
+      define(only, property, attribute, space, comma);
+    } else {
+      define(html, property, attribute, space, comma);
+      define(svg, property, attribute, space, comma);
     }
-    output = output + value.charAt(index);
-    index = index + 1;
   }
-  return output;
 }
 
-bool validDataName(string value) {
-  if (value.length < 5 || value.slice(0, 4).toLowerCase() != "data") {
-    return false;
-  }
-  int index = 4;
-  while (index < value.length) {
-    int code = value.charCodeAt(index);
-    if (!(code == 45 || code == 46 || code == 58 || code == 95 || code >= 48 && code <= 57 || code >= 65 && code <= 90 || code >= 97 && code <= 122)) {
-      return false;
-    }
-    index = index + 1;
-  }
-  return true;
-}
-
-PropertyInfo unknownInfo(string property, string attribute) {
-  return PropertyInfo{property, attribute, false, false};
-}
-
-export PropertyInfo find(string space, string value) {
-  initialize();
-  string lower = value.toLowerCase();
-  string key = space + "|" + lower;
-  PropertyInfo? known = records.get(key);
-  if (known != null) {
-    return known;
+load(ariaRows, 3, 0, false, "aria");
+load(noSpaceRows, 4, JS.undefined(), JS.undefined(), JS.undefined());
+load(commonRows, 1, JS.undefined(), JS.undefined(), JS.undefined());
+load(commonOnRows, 1, 0, false, "on");
+load(htmlRows, 1, html, JS.undefined(), JS.undefined());
+load(htmlOnRows, 1, html, false, "on");
+load(htmlCommaRows, 1, html, true, JS.undefined());
+load(svgRows, 0, svg, JS.undefined(), JS.undefined());
+load(svgDashRows, 2, svg, JS.undefined(), JS.undefined());
+load(svgLowerRows, 1, svg, JS.undefined(), JS.undefined());
+load(svgOnRows, 1, svg, false, "on");
+load(svgCommaRows, 0, svg, true, JS.undefined());
+
+export PropertyInfo find(JsValue target, string value) {
+  Schema schema = JS.assume(target);
+  string normal = value.toLowerCase();
+  if (JS.in(normal, schema.normal)) {
+    return JS.assume(schema.property[schema.normal[normal]]);
   }
   string property = value;
-  string attribute = value;
-  if (lower.length > 4 && lower.slice(0, 4) == "data" && validDataName(value)) {
+  // Upstream first checks `normal.length > 4 && normal.slice(0, 4) === 'data'`; `valid` (ASCII only, `data`
+  // and at least one more character) implies both.
+  if (JS.invoke(valid, "test", value).truthy()) {
     if (value.charAt(4) == "-") {
-      string rest = dashToCamel(value.slice(5));
-      property = "data";
-      if (rest.length > 0) property = "data" + rest.charAt(0).toUpperCase() + rest.slice(1);
+      string rest = JS.assume(JS.invoke(value.slice(5), "replace", dash, camelcase));
+      property = "data" + rest.charAt(0).toUpperCase() + rest.slice(1);
     } else {
       string rest = value.slice(4);
-      if (rest.indexOf("-") < 0) {
-        string dashed = camelToDash(rest);
-        if (dashed.length == 0 || dashed.charAt(0) != "-") dashed = "-" + dashed;
-        attribute = "data" + dashed;
+      if (!JS.invoke(dash, "test", rest).truthy()) {
+        string dashes = JS.assume(JS.invoke(rest, "replace", cap, kebab));
+        if (dashes.charAt(0) != "-") {
+          dashes = "-" + dashes;
+        }
+        value = "data" + dashes;
       }
     }
   }
-  return unknownInfo(property, attribute);
+  // Upstream's Info/DefinedInfo: no space, not comma-separated (both read as falsy).
+  return JS.assume(object { attribute: value, property: property });
 }
 
-Map<string, string> hastToReact = new Map<string, string>();
-hastToReact.set("classId", "classID");
-hastToReact.set("dataType", "datatype");
-hastToReact.set("itemId", "itemID");
-hastToReact.set("strokeDashArray", "strokeDasharray");
-hastToReact.set("strokeDashOffset", "strokeDashoffset");
-hastToReact.set("strokeLineCap", "strokeLinecap");
-hastToReact.set("strokeLineJoin", "strokeLinejoin");
-hastToReact.set("strokeMiterLimit", "strokeMiterlimit");
-hastToReact.set("typeOf", "typeof");
-hastToReact.set("xLinkActuate", "xlinkActuate");
-hastToReact.set("xLinkArcRole", "xlinkArcrole");
-hastToReact.set("xLinkHref", "xlinkHref");
-hastToReact.set("xLinkRole", "xlinkRole");
-hastToReact.set("xLinkShow", "xlinkShow");
-hastToReact.set("xLinkTitle", "xlinkTitle");
-hastToReact.set("xLinkType", "xlinkType");
-hastToReact.set("xmlnsXLink", "xmlnsXlink");
-
-export string reactPropertyName(string property) {
-  string? mapped = hastToReact.get(property);
-  if (mapped != null) {
-    return mapped;
-  }
-  return property;
+
+// hast-to-react: React's spelling of 17 properties. Each differs from its property name only in case, and no
+// other property with a space shares that lowercase name, so the table is keyed by lowercase name.
+export JsValue hastToReact = object {};
+JsValue reactNames = JS.invoke("classID datatype itemID strokeDasharray strokeDashoffset strokeLinecap strokeLinejoin strokeMiterlimit typeof xlinkActuate xlinkArcrole xlinkHref xlinkRole xlinkShow xlinkTitle xlinkType xmlnsXlink", "split", " ");
+for (float index = 0; index < reactNames.length; index++) {
+  hastToReact[JS.invoke(reactNames[index], "toLowerCase")] = reactNames[index];
+}
+
+export JsValue reactName(PropertyInfo info) {
+  return JS.or(hastToReact[info.property.toLowerCase()], info.property);
 }
diff -ruN a/src/property-information/lib/schema-data.lil b/src/property-information/lib/schema-data.lil
--- a/src/property-information/lib/schema-data.lil	2026-09-23 20:40:45.238692382 +0000
+++ b/src/property-information/lib/schema-data.lil	2026-09-23 20:40:45.362782130 +0000
@@ -1,4 +1,14 @@
-// Generated from property-information@7.2.0 by scripts/generate-schema.mjs.
-export string commonSchemaBlob = "ariaActiveDescendant|@\nariaAtomic|@\nariaAutoComplete|@\nariaBusy|@\nariaChecked|@\nariaColCount|@\nariaColIndex|@\nariaColSpan|@\nariaControls|@\nariaCurrent|@\nariaDescribedBy|@\nariaDetails|@\nariaDisabled|@\nariaDropEffect|@\nariaErrorMessage|@\nariaExpanded|@\nariaFlowTo|@\nariaGrabbed|@\nariaHasPopup|@\nariaHidden|@\nariaInvalid|@\nariaKeyShortcuts|@\nariaLabel|@\nariaLabelledBy|@\nariaLevel|@\nariaLive|@\nariaModal|@\nariaMultiLine|@\nariaMultiSelectable|@\nariaOrientation|@\nariaOwns|@\nariaPlaceholder|@\nariaPosInSet|@\nariaPressed|@\nariaReadOnly|@\nariaRelevant|@\nariaRequired|@\nariaRoleDescription|@\nariaRowCount|@\nariaRowIndex|@\nariaRowSpan|@\nariaSelected|@\nariaSetSize|@\nariaSort|@\nariaValueMax|@\nariaValueMin|@\nariaValueNow|@\nariaValueText|@\nrole\nclassName|class|s\ncontent||s\ncrossOrigin|~|s\ndownload||s\nheight||s\nhref||s\nhrefLang|~|s\nid||s\nlang||s\nmax||s\nmedia||s\nmethod||s\nmin||s\nname||s\nonAbort|~|s\nonAfterPrint|~|s\nonBeforePrint|~|s\nonCancel|~|s\nonCanPlay|~|s\nonCanPlayThrough|~|s\nonChange|~|s\nonClick|~|s\nonClose|~|s\nonCopy|~|s\nonCueChange|~|s\nonCut|~|s\nonDblClick|~|s\nonDrag|~|s\nonDragEnd|~|s\nonDragEnter|~|s\nonDragExit|~|s\nonDragLeave|~|s\nonDragOver|~|s\nonDragStart|~|s\nonDrop|~|s\nonDurationChange|~|s\nonEmptied|~|s\nonEnded|~|s\nonError|~|s\nonFocus|~|s\nonHashChange|~|s\nonInput|~|s\nonInvalid|~|s\nonKeyDown|~|s\nonKeyPress|~|s\nonKeyUp|~|s\nonLoad|~|s\nonLoadedData|~|s\nonLoadedMetadata|~|s\nonLoadStart|~|s\nonMessage|~|s\nonMouseDown|~|s\nonMouseEnter|~|s\nonMouseLeave|~|s\nonMouseMove|~|s\nonMouseOut|~|s\nonMouseOver|~|s\nonMouseUp|~|s\nonOffline|~|s\nonOnline|~|s\nonPageHide|~|s\nonPageShow|~|s\nonPaste|~|s\nonPause|~|s\nonPlay|~|s\nonPlaying|~|s\nonPopState|~|s\nonProgress|~|s\nonRateChange|~|s\nonReset|~|s\nonResize|~|s\nonScroll|~|s\nonSeeked|~|s\nonSeeking|~|s\nonSelect|~|s\nonStalled|~|s\nonStorage|~|s\nonSubmit|~|s\nonSuspend|~|s\nonTimeUpdate|~|s\nonToggle|~|s\nonUnload|~|s\nonVolumeChange|~|s\nonWaiting|~|s\nping||s\nreferrerPolicy|~|s\nrel||s\nstyle||s\ntabIndex|~|s\ntarget||s\ntitle||s\ntype||s\nwidth||s\ncolor||s\nevent||s\nrev||s\nversion||s\nproperty||s\nxLinkActuate|xlink:actuate|s\nxLinkArcRole|xlink:arcrole|s\nxLinkHref|xlink:href|s\nxLinkRole|xlink:role|s\nxLinkShow|xlink:show|s\nxLinkTitle|xlink:title|s\nxLinkType|xlink:type|s\nxmlnsXLink|xmlns:xlink|s\nxmlns||s\nxmlBase|xml:base|s\nxmlLang|xml:lang|s\nxmlSpace|xml:space|s";
-export string htmlSchemaBlob = "abbr\naccept||c\nacceptCharset|-\naccessKey|~\naction\nallow\nallowFullScreen|~\nallowPaymentRequest|~\nallowUserMedia|~\nalpha\nalt\nas\nasync\nautoCapitalize|~\nautoComplete|~\nautoFocus|~\nautoPlay|~\nblocking\ncapture\ncharSet|~\nchecked\ncite\nclosedBy|~\ncolorSpace|~\ncols\ncolSpan|~\ncommand\ncommandFor|~\ncontentEditable|~\ncontrols\ncontrolsList|~\ncoords||c\ndata\ndateTime|~\ndecoding\ndefault\ndefer\ndir\ndirName|~\ndisabled\ndraggable\nencType|~\nenterKeyHint|~\nfetchPriority|~\nform\nformAction|~\nformEncType|~\nformMethod|~\nformNoValidate|~\nformTarget|~\nheaders\nhidden\nhigh\nhtmlFor|for\nhttpEquiv|-\nimageSizes|~\nimageSrcSet|~\ninert\ninputMode|~\nintegrity\nis\nisMap|~\nitemId|~\nitemProp|~\nitemRef|~\nitemScope|~\nitemType|~\nkind\nlabel\nlanguage\nlist\nloading\nloop\nlow\nmanifest\nmaxLength|~\nminLength|~\nmultiple\nmuted\nnonce\nnoModule|~\nnoValidate|~\nonAuxClick|~\nonBeforeMatch|~\nonBeforeToggle|~\nonBeforeUnload|~\nonBlur|~\nonContextLost|~\nonContextMenu|~\nonContextRestored|~\nonFormData|~\nonLanguageChange|~\nonLoadEnd|~\nonMessageError|~\nonRejectionHandled|~\nonScrollEnd|~\nonSecurityPolicyViolation|~\nonSlotChange|~\nonUnhandledRejection|~\nonWheel|~\nopen\noptimum\npattern\nplaceholder\nplaysInline|~\npopover\npopoverTarget|~\npopoverTargetAction|~\nposter\npreload\nreadOnly|~\nrequired\nreversed\nrows\nrowSpan|~\nsandbox\nscope\nscoped\nseamless\nselected\nshadowRootClonable|~\nshadowRootCustomElementRegistry|~\nshadowRootDelegatesFocus|~\nshadowRootMode|~\nshadowRootSerializable|~\nshape\nsize\nsizes\nslot\nspan\nspellCheck|~\nsrc\nsrcDoc|~\nsrcLang|~\nsrcSet|~\nstart\nstep\ntranslate\ntypeMustMatch|~\nuseMap|~\nvalue\nwrap\nwritingSuggestions|~\nalign\naLink|~\narchive\naxis\nbackground\nbgColor|~\nborder\nborderColor|~\nbottomMargin|~\ncellPadding|~\ncellSpacing|~\nchar\ncharOff|~\nclassId|~\nclear\ncode\ncodeBase|~\ncodeType|~\ncompact\ndeclare\nface\nframe\nframeBorder|~\nhSpace|~\nleftMargin|~\nlink\nlongDesc|~\nlowSrc|~\nmarginHeight|~\nmarginWidth|~\nnoResize|~\nnoHref|~\nnoShade|~\nnoWrap|~\nobject\nprofile\nprompt\nrightMargin|~\nrules\nscheme\nscrolling\nstandby\nsummary\ntext\ntopMargin|~\nvalueType|~\nvAlign|~\nvLink|~\nvSpace|~\nallowTransparency|~\nautoCorrect|~\nautoSave|~\ncredentialless\ndisablePictureInPicture|~\ndisableRemotePlayback|~\nexportParts|~|c\npart\nprefix\nresults\nsecurity\nunselectable";
-export string svgSchemaBlob = "about\naccentHeight|-\naccumulate\nadditive\nalignmentBaseline|-\nalphabetic\namplitude\narabicForm|-\nascent\nattributeName\nattributeType\nazimuth\nbandwidth\nbaselineShift|-\nbaseFrequency\nbaseProfile\nbbox\nbegin\nbias\nby\ncalcMode\ncapHeight|-\nclip\nclipPath|-\nclipPathUnits\nclipRule|-\ncolorInterpolation|-\ncolorInterpolationFilters|-\ncolorProfile|-\ncolorRendering|-\ncontentScriptType\ncontentStyleType\ncursor\ncx\ncy\nd\ndataType|~\ndefaultAction\ndescent\ndiffuseConstant\ndirection\ndisplay\ndur\ndivisor\ndominantBaseline|-\ndx\ndy\nedgeMode\neditable\nelevation\nenableBackground|-\nend\nexponent\nexternalResourcesRequired\nfill\nfillOpacity|-\nfillRule|-\nfilter\nfilterRes\nfilterUnits\nfloodColor|-\nfloodOpacity|-\nfocusable\nfocusHighlight\nfontFamily|-\nfontSize|-\nfontSizeAdjust|-\nfontStretch|-\nfontStyle|-\nfontVariant|-\nfontWeight|-\nformat\nfr\nfrom\nfx\nfy\ng1||c\ng2||c\nglyphName|-|c\nglyphOrientationHorizontal|-\nglyphOrientationVertical|-\nglyphRef\ngradientTransform\ngradientUnits\nhandler\nhanging\nhatchContentUnits\nhatchUnits\nhorizAdvX|-\nhorizOriginX|-\nhorizOriginY|-\nideographic\nimageRendering|-\ninitialVisibility\nin\nin2\nintercept\nk\nk1\nk2\nk3\nk4\nkernelMatrix\nkernelUnitLength\nkeyPoints\nkeySplines\nkeyTimes\nkerning\nlengthAdjust\nletterSpacing|-\nlightingColor|-\nlimitingConeAngle\nlocal\nmarkerEnd|-\nmarkerMid|-\nmarkerStart|-\nmarkerHeight\nmarkerUnits\nmarkerWidth\nmask\nmaskContentUnits\nmaskType|-\nmaskUnits\nmathematical\nmediaCharacterEncoding\nmediaContentEncodings\nmediaSize\nmediaTime\nmode\nnavDown|-\nnavDownLeft|-\nnavDownRight|-\nnavLeft|-\nnavNext|-\nnavPrev|-\nnavRight|-\nnavUp|-\nnavUpLeft|-\nnavUpRight|-\nnumOctaves\nobserver\noffset\nonActivate|~\nonBegin|~\nonEnd|~\nonFocusIn|~\nonFocusOut|~\nonMouseWheel|~\nonRepeat|~\nonShow|~\nonZoom|~\nopacity\noperator\norder\norient\norientation\norigin\noverflow\noverlay\noverlinePosition|-\noverlineThickness|-\npaintOrder|-\npanose1|panose-1\npath\npathLength\npatternContentUnits\npatternTransform\npatternUnits\nphase\npitch\nplaybackOrder|~\npointerEvents|-\npoints\npointsAtX\npointsAtY\npointsAtZ\npreserveAlpha\npreserveAspectRatio\nprimitiveUnits\npropagate\nr\nradius\nrefX\nrefY\nrenderingIntent|-\nrepeatCount\nrepeatDur\nrequiredExtensions\nrequiredFeatures\nrequiredFonts\nrequiredFormats\nresource\nrestart\nresult\nrotate\nrx\nry\nscale\nseed\nshapeRendering|-\nside\nslope\nsnapshotTime\nspecularConstant\nspecularExponent\nspreadMethod\nspacing\nstartOffset\nstdDeviation\nstemh\nstemv\nstitchTiles\nstopColor|-\nstopOpacity|-\nstrikethroughPosition|-\nstrikethroughThickness|-\nstring\nstroke\nstrokeDashArray|stroke-dasharray\nstrokeDashOffset|stroke-dashoffset\nstrokeLineCap|stroke-linecap\nstrokeLineJoin|stroke-linejoin\nstrokeMiterLimit|stroke-miterlimit\nstrokeOpacity|-\nstrokeWidth|-\nsurfaceScale\nsyncBehavior\nsyncBehaviorDefault\nsyncMaster\nsyncTolerance\nsyncToleranceDefault\nsystemLanguage\ntableValues\ntargetX\ntargetY\ntextAnchor|-\ntextDecoration|-\ntextRendering|-\ntextLength\ntimelineBegin|~\ntransformBehavior\ntypeOf|~\nto\ntransform\ntransformOrigin|-\nu1\nu2\nunderlinePosition|-\nunderlineThickness|-\nunicode\nunicodeBidi|-\nunicodeRange|-\nunitsPerEm|-\nvalues\nvAlphabetic|-\nvMathematical|-\nvectorEffect|-\nvHanging|-\nvIdeographic|-\nvertAdvY|-\nvertOriginX|-\nvertOriginY|-\nviewBox\nviewTarget\nvisibility\nwidths\nwordSpacing|-\nwritingMode|-\nx\nx1\nx2\nxChannelSelector\nxHeight|-\ny\ny1\ny2\nyChannelSelector\nz\nzoomAndPan";
+// Generated from property-information@7.2.0 by scripts/generate-schema.mjs: every property of the merged
+// `html` and `svg` schemas, as the row groups that find.lil loads (see there).
+export string ariaRows = "ActiveDescendant Atomic AutoComplete Busy Checked ColCount ColIndex ColSpan Controls Current DescribedBy Details Disabled DropEffect ErrorMessage Expanded FlowTo Grabbed HasPopup Hidden Invalid KeyShortcuts Label LabelledBy Level Live Modal MultiLine MultiSelectable Orientation Owns Placeholder PosInSet Pressed ReadOnly Relevant Required RoleDescription RowCount RowIndex RowSpan Selected SetSize Sort ValueMax ValueMin ValueNow ValueText";
+export string noSpaceRows = "role";
+export string commonRows = "className|class content crossOrigin download height href hrefLang id lang max media method min name ping referrerPolicy rel style tabIndex target title type width color event rev version property xLinkActuate|xlink:actuate xLinkArcRole|xlink:arcrole xLinkHref|xlink:href xLinkRole|xlink:role xLinkShow|xlink:show xLinkTitle|xlink:title xLinkType|xlink:type xmlnsXLink|xmlns:xlink xmlns xmlBase|xml:base xmlLang|xml:lang xmlSpace|xml:space";
+export string commonOnRows = "Abort AfterPrint BeforePrint Cancel CanPlay CanPlayThrough Change Click Close Copy CueChange Cut DblClick Drag DragEnd DragEnter DragExit DragLeave DragOver DragStart Drop DurationChange Emptied Ended Error Focus HashChange Input Invalid KeyDown KeyPress KeyUp Load LoadedData LoadedMetadata LoadStart Message MouseDown MouseEnter MouseLeave MouseMove MouseOut MouseOver MouseUp Offline Online PageHide PageShow Paste Pause Play Playing PopState Progress RateChange Reset Resize Scroll Seeked Seeking Select Stalled Storage Submit Suspend TimeUpdate Toggle Unload VolumeChange Waiting";
+export string htmlRows = "abbr acceptCharset|accept-charset accessKey action allow allowFullScreen allowPaymentRequest allowUserMedia alpha alt as async autoCapitalize autoComplete autoFocus autoPlay blocking capture charSet checked cite closedBy colorSpace cols colSpan command commandFor contentEditable controls controlsList data dateTime decoding default defer dir dirName disabled draggable encType enterKeyHint fetchPriority form formAction formEncType formMethod formNoValidate formTarget headers hidden high htmlFor|for httpEquiv|http-equiv imageSizes imageSrcSet inert inputMode integrity is isMap itemId itemProp itemRef itemScope itemType kind label language list loading loop low manifest maxLength minLength multiple muted nonce noModule noValidate open optimum pattern placeholder playsInline popover popoverTarget popoverTargetAction poster preload readOnly required reversed rows rowSpan sandbox scope scoped seamless selected shadowRootClonable shadowRootCustomElementRegistry shadowRootDelegatesFocus shadowRootMode shadowRootSerializable shape size sizes slot span spellCheck src srcDoc srcLang srcSet start step translate typeMustMatch useMap value wrap writingSuggestions align aLink archive axis background bgColor border borderColor bottomMargin cellPadding cellSpacing char charOff classId clear code codeBase codeType compact declare face frame frameBorder hSpace leftMargin link longDesc lowSrc marginHeight marginWidth noResize noHref noShade noWrap object profile prompt rightMargin rules scheme scrolling standby summary text topMargin valueType vAlign vLink vSpace allowTransparency autoCorrect autoSave credentialless disablePictureInPicture disableRemotePlayback part prefix results security unselectable";
+export string htmlOnRows = "AuxClick BeforeMatch BeforeToggle BeforeUnload Blur ContextLost ContextMenu ContextRestored FormData LanguageChange LoadEnd MessageError RejectionHandled ScrollEnd SecurityPolicyViolation SlotChange UnhandledRejection Wheel";
+export string htmlCommaRows = "accept coords exportParts";
+export string svgRows = "about accumulate additive alphabetic amplitude ascent attributeName attributeType azimuth bandwidth baseFrequency baseProfile bbox begin bias by calcMode clip clipPathUnits contentScriptType contentStyleType cursor cx cy d defaultAction descent diffuseConstant direction display dur divisor dx dy edgeMode editable elevation end exponent externalResourcesRequired fill filter filterRes filterUnits focusable focusHighlight format fr from fx fy glyphRef gradientTransform gradientUnits handler hanging hatchContentUnits hatchUnits ideographic initialVisibility in in2 intercept k k1 k2 k3 k4 kernelMatrix kernelUnitLength keyPoints keySplines keyTimes kerning lengthAdjust limitingConeAngle local markerHeight markerUnits markerWidth mask maskContentUnits maskUnits mathematical mediaCharacterEncoding mediaContentEncodings mediaSize mediaTime mode numOctaves observer offset opacity operator order orient orientation origin overflow overlay panose1|panose-1 path pathLength patternContentUnits patternTransform patternUnits phase pitch points pointsAtX pointsAtY pointsAtZ preserveAlpha preserveAspectRatio primitiveUnits propagate r radius refX refY repeatCount repeatDur requiredExtensions requiredFeatures requiredFonts requiredFormats resource restart result rotate rx ry scale seed side slope snapshotTime specularConstant specularExponent spreadMethod spacing startOffset stdDeviation stemh stemv stitchTiles string stroke strokeDashArray|stroke-dasharray strokeDashOffset|stroke-dashoffset strokeLineCap|stroke-linecap strokeLineJoin|stroke-linejoin strokeMiterLimit|stroke-miterlimit surfaceScale syncBehavior syncBehaviorDefault syncMaster syncTolerance syncToleranceDefault systemLanguage tableValues targetX targetY textLength transformBehavior to transform u1 u2 unicode values viewBox viewTarget visibility widths x x1 x2 xChannelSelector y y1 y2 yChannelSelector z zoomAndPan";
+export string svgDashRows = "accentHeight alignmentBaseline arabicForm baselineShift capHeight clipPath clipRule colorInterpolation colorInterpolationFilters colorProfile colorRendering dominantBaseline enableBackground fillOpacity fillRule floodColor floodOpacity fontFamily fontSize fontSizeAdjust fontStretch fontStyle fontVariant fontWeight glyphOrientationHorizontal glyphOrientationVertical horizAdvX horizOriginX horizOriginY imageRendering letterSpacing lightingColor markerEnd markerMid markerStart maskType navDown navDownLeft navDownRight navLeft navNext navPrev navRight navUp navUpLeft navUpRight overlinePosition overlineThickness paintOrder pointerEvents renderingIntent shapeRendering stopColor stopOpacity strikethroughPosition strikethroughThickness strokeOpacity strokeWidth textAnchor textDecoration textRendering transformOrigin underlinePosition underlineThickness unicodeBidi unicodeRange unitsPerEm vAlphabetic vMathematical vectorEffect vHanging vIdeographic vertAdvY vertOriginX vertOriginY wordSpacing writingMode xHeight";
+export string svgLowerRows = "dataType playbackOrder timelineBegin typeOf";
+export string svgOnRows = "Activate Begin End FocusIn FocusOut MouseWheel Repeat Show Zoom";
+export string svgCommaRows = "g1 g2 glyphName|glyph-name";
diff -ruN a/src/style-to-js/index.lil b/src/style-to-js/index.lil
--- a/src/style-to-js/index.lil	2026-09-23 20:40:45.233734907 +0000
+++ b/src/style-to-js/index.lil	2026-09-23 20:40:45.362959612 +0000
@@ -1,209 +1,72 @@
-import { Error } from "../host.lil";
-
-bool asciiWhitespace(int code) {
-  return code == 9 || code == 10 || code == 12 || code == 13 || code == 32;
-}
-
-string trim(string value) {
-  int start = 0;
-  int end = value.length;
-  while (start < end && asciiWhitespace(value.charCodeAt(start))) {
-    start = start + 1;
-  }
-  while (end > start && asciiWhitespace(value.charCodeAt(end - 1))) {
-    end = end - 1;
-  }
-  return value.slice(start, end);
-}
-
-bool propertyCode(int code) {
-  return code == 42 || code == 45 || code == 35 || code == 47 || code == 92 ||
-    code == 95 || code >= 48 && code <= 57 || code >= 65 && code <= 90 ||
-    code >= 97 && code <= 122;
-}
-
-void fail(string source, int line, int column, string reason) {
-  JsValue error = JS.construct(Error, source + ":" + JS.string(line) + ":" + JS.string(column) + ": " + reason);
-  error["reason"] = reason;
-  error["filename"] = source;
-  error["line"] = line;
-  error["column"] = column;
-  throw error;
-}
-
-int skipSpace(string style, int index) {
-  int n = style.length;
-  while (index < n && asciiWhitespace(style.charCodeAt(index))) {
-    index = index + 1;
-  }
-  return index;
-}
-
-int skipComment(string style, int index) {
-  if (index + 1 >= style.length || style.charAt(index) != "/" || style.charAt(index + 1) != "*") {
-    return index;
-  }
-  int cursor = index + 2;
-  while (cursor + 1 < style.length && !(style.charAt(cursor) == "*" && style.charAt(cursor + 1) == "/")) {
-    cursor = cursor + 1;
-  }
-  if (cursor + 1 >= style.length) {
-    fail("undefined", 1, index + 1, "End of comment missing");
-  }
-  return cursor + 2;
-}
-
-string removeComments(string value) {
-  string output = "";
-  int index = 0;
-  while (index < value.length) {
-    if (index + 1 < value.length && value.charAt(index) == "/" && value.charAt(index + 1) == "*") {
-      int next = skipComment(value, index);
-      index = next;
-    } else {
-      output = output + value.charAt(index);
-      index = index + 1;
-    }
-  }
-  return output;
-}
-
-bool isCustomProperty(string property) {
-  if (property.length < 3 || property.slice(0, 2) != "--") {
-    return false;
-  }
-  int index = 2;
-  while (index < property.length) {
-    int code = property.charCodeAt(index);
-    if (!(code == 45 || code == 95 || code >= 48 && code <= 57 || code >= 65 && code <= 90 || code >= 97 && code <= 122)) {
+// style-to-js@1.1.21 (with style-to-object@1.0.14 and inline-style-parser@0.2.7) as hast-util-to-jsx-runtime
+// calls it with `ignoreInvalidStyle`: `styleToJs(value, {reactCompat: true})` on a string, and `{}` when that
+// throws. The parser is upstream's: the same regular expressions, applied in the same order to the rest of the
+// input; a parse error only needs to throw.
+import { camelcase } from "../property-information/lib/find.lil";
+
+extern JsValue RegExp;
+
+JsValue propertyPattern = JS.construct(RegExp, "^(\\*?[-#/*\\\\\\w]+(\\[[0-9a-z_-]+\\])?)\\s*");
+JsValue colon = JS.construct(RegExp, "^:\\s*");
+JsValue valuePattern = JS.construct(RegExp, "^((?:'(?:\\\\'|.)*?'|\"(?:\\\\\"|.)*?\"|\\([^)]*?\\)|[^};])+)");
+JsValue semicolon = JS.construct(RegExp, "^[;\\s]*");
+JsValue comments = JS.construct(RegExp, "\\/\\*[^*]*\\*+([^/*][^*]*\\*+)*\\/", "g");
+JsValue customProperty = JS.construct(RegExp, "^--[a-zA-Z0-9_-]+$");
+JsValue msPrefix = JS.construct(RegExp, "^-ms-");
+JsValue hyphen = JS.construct(RegExp, "-[a-z]", "g");
+
+export JsValue styleToJs(JsValue style) {
+  JsValue output = object {};
+  JsValue rest = style;
+
+  // Consume `pattern` at the start of the rest; the match, or null.
+  auto eat = (JsValue pattern) => {
+    JsValue found = JS.invoke(pattern, "exec", rest);
+    if (found.truthy()) {
+      rest = JS.invoke(rest, "slice", JS.get(found[0], "length"));
+    }
+    return found;
+  };
+  // One comment at the start of the rest (an unclosed one throws); whether there was one.
+  auto comment = () => {
+    if (!JS.strictEqual(JS.invoke(rest, "charAt", 0), "/") || !JS.strictEqual(JS.invoke(rest, "charAt", 1), "*")) {
       return false;
     }
-    index = index + 1;
-  }
-  return true;
-}
-
-bool hasHyphen(string property) {
-  return property.indexOf("-") >= 0;
-}
-
-string replaceHyphens(string property) {
-  string output = "";
-  int index = 0;
-  while (index < property.length) {
-    if (index + 1 < property.length && property.charAt(index) == "-") {
-      int code = property.charCodeAt(index + 1);
-      if (code >= 97 && code <= 122) {
-        output = output + property.charAt(index + 1).toUpperCase();
-        index = index + 2;
-        continue;
-      }
-    }
-    output = output + property.charAt(index);
-    index = index + 1;
-  }
-  return output;
-}
-
-export string camelCase(string property, bool reactCompat) {
-  if (property.length == 0 || !hasHyphen(property) || isCustomProperty(property)) {
-    return property;
-  }
-  string next = property.toLowerCase();
-  if (reactCompat) {
-    if (next.slice(0, 4) == "-ms-") {
-      next = "ms-" + next.slice(4);
-    }
-  } else {
-    string[] prefixes = ["webkit", "moz", "ms", "o", "khtml"];
-    int i = 0;
-    while (i < prefixes.length) {
-      string prefix = prefixes[i];
-      string marker = "-" + prefix + "-";
-      if (next.slice(0, marker.length) == marker) {
-        next = prefix + "-" + next.slice(marker.length);
-        break;
-      }
-      i = i + 1;
-    }
-  }
-  return replaceHyphens(next);
-}
-
-void addDeclaration(JsValue output, string property, string value, bool reactCompat) {
-  property = trim(removeComments(property));
-  value = trim(removeComments(value));
-  if (property.length > 0 && value.length > 0) {
-    output[camelCase(property, reactCompat)] = value;
-  }
-}
-
-export JsValue styleToJs(string style, bool reactCompat) {
-  JsValue output = JS.object();
-  if (style.length == 0) {
-    return output;
-  }
-  int index = 0;
-  int n = style.length;
-  while (index < n) {
-    index = skipSpace(style, index);
-    while (index + 1 < n && style.charAt(index) == "/" && style.charAt(index + 1) == "*") {
-      index = skipSpace(style, skipComment(style, index));
-    }
-    if (index >= n) {
-      break;
-    }
-    int propertyStart = index;
-    if (style.charAt(index) == "*") {
-      index = index + 1;
-    }
-    while (index < n && propertyCode(style.charCodeAt(index))) {
-      index = index + 1;
-    }
-    if (index == propertyStart || index == propertyStart + 1 && style.charAt(propertyStart) == "*") {
-      break;
-    }
-    string property = style.slice(propertyStart, index);
-    index = skipSpace(style, index);
-    if (index + 1 < n && style.charAt(index) == "/" && style.charAt(index + 1) == "*") {
-      index = skipComment(style, index);
-      index = skipSpace(style, index);
-    }
-    if (index >= n || style.charAt(index) != ":") {
-      fail("undefined", 1, index + 1, "property missing ':'");
-    }
-    index = skipSpace(style, index + 1);
-    int valueStart = index;
-    string quote = "";
-    int parentheses = 0;
-    while (index < n) {
-      string character = style.charAt(index);
-      if (quote.length > 0) {
-        if (character == "\\") {
-          index = index + 2;
-          continue;
+    JsValue end = JS.invoke(rest, "indexOf", "*/", 2);
+    if (JS.strictEqual(end, -1)) {
+      throw rest;
+    }
+    rest = JS.invoke(JS.invoke(rest, "slice", JS.add(end, 2)), "trimStart");
+    return true;
+  };
+
+  try {
+    if (rest.truthy()) {
+      rest = JS.invoke(rest, "trimStart");
+      while (comment()) {}
+      JsValue property = eat(propertyPattern);
+      while (property.truthy()) {
+        comment();
+        if (!eat(colon).truthy()) {
+          throw rest;
         }
-        if (character == quote) {
-          quote = "";
+        JsValue value = eat(valuePattern);
+        JsValue name = JS.invoke(JS.invoke(property[0], "replace", comments, ""), "trim");
+        value = if (value.truthy()) { JS.invoke(JS.invoke(value[0], "replace", comments, ""), "trim") } else { "" };
+        rest = JS.invoke(rest, "trimStart");
+        eat(semicolon);
+        if (name.truthy() && value.truthy()) {
+          if (JS.invoke(name, "includes", "-").truthy() && !JS.invoke(customProperty, "test", name).truthy()) {
+            name = JS.invoke(JS.invoke(JS.invoke(name, "toLowerCase"), "replace", msPrefix, "ms-"), "replace", hyphen, camelcase);
+          }
+          output[name] = value;
         }
-      } else if (character == "'" || character == "\"") {
-        quote = character;
-      } else if (character == "(") {
-        parentheses = parentheses + 1;
-      } else if (character == ")" && parentheses > 0) {
-        parentheses = parentheses - 1;
-      } else if ((character == ";" && parentheses == 0) || character == "}") {
-        break;
+        while (comment()) {}
+        property = eat(propertyPattern);
       }
-      index = index + 1;
-    }
-    addDeclaration(output, property, style.slice(valueStart, index), reactCompat);
-    if (index < n && style.charAt(index) == "}") {
-      break;
-    }
-    if (index < n && style.charAt(index) == ";") {
-      index = index + 1;
     }
+  } catch {
+    return object {};
   }
   return output;
 }
diff -ruN a/src/unist-util-visit/lib/index.lil b/src/unist-util-visit/lib/index.lil
--- a/src/unist-util-visit/lib/index.lil	2026-09-23 20:40:45.233609222 +0000
+++ b/src/unist-util-visit/lib/index.lil	2026-09-23 20:40:45.363577180 +0000
@@ -1,42 +1,14 @@
-bool EXIT = false;
-string SKIP = "skip";
-JsValue[] emptyResult = [];
-
-JsValue toResult(JsValue value) {
-  if (JS.isArray(value)) return value;
-  if (JS.typeOf(value) == "number") return JS.array(true, value);
-  if (JS.isNullish(value) || JS.isUndefined(value)) return emptyResult;
-  return JS.array(value);
-}
-
-JsValue walk(JsValue node, JsValue visitor, JsValue[] parents) {
-  JsValue parent = JS.undefined();
-  JsValue index = JS.undefined();
-  int parentCount = parents.length;
-  if (parentCount > 0) {
-    parent = parents[parentCount - 1];
-    index = JS.invoke(JS.get(parent, "children"), "indexOf", node);
-  }
-  JsValue result = toResult(JS.call(visitor, JS.undefined(), node, index, parent));
-  if (JS.strictEqual(result[0], EXIT)) return result;
-  if (JS.in("children", node) && JS.get(node, "children").truthy() && !JS.strictEqual(result[0], SKIP)) {
-    JsValue children = JS.get(node, "children");
-    JsValue[] grandparents = parents.slice();
-    grandparents.push(node);
-    int offset = 0;
-    while (offset < children.length.toInt()) {
-      JsValue subresult = walk(children[offset], visitor, grandparents);
-      if (JS.strictEqual(subresult[0], EXIT)) return subresult;
-      if (subresult.length.toInt() > 1 && JS.typeOf(subresult[1]) == "number") {
-        offset = JS.number(subresult[1]).toInt();
-      } else {
-        offset = offset + 1;
-      }
+// unist-util-visit@5.1.0 `visit(tree, visitor)` for react-markdown's one visitor, which returns the index to
+// continue at (a number) or nothing: upstream's EXIT and SKIP never occur, and only the direct parent of a
+// node is observable. Call as `visit(tree, undefined, visitor)`.
+export JsValue visit(JsValue node, JsValue parent, func(JsValue, JsValue, JsValue)->JsValue visitor) {
+  JsValue result = visitor(node, JS.and(parent, JS.invoke(JS.get(parent, "children"), "indexOf", node)), parent);
+  if (JS.in("children", node) && JS.get(node, "children").truthy()) {
+    JsValue offset = 0;
+    while (JS.greaterThan(offset, -1) && JS.lessThan(offset, JS.get(JS.get(node, "children"), "length"))) {
+      JsValue next = visit(JS.get(node, "children")[offset], node, visitor);
+      offset = if (JS.typeOf(next) == "number") { next } else { JS.add(offset, 1) };
     }
   }
   return result;
 }
-
-export void visit(JsValue tree, JsValue visitor) {
-  walk(tree, visitor, []);
-}
diff -ruN a/src/urls.lil b/src/urls.lil
--- a/src/urls.lil	2026-09-23 20:40:45.045690683 +0000
+++ b/src/urls.lil	2026-09-23 20:40:45.368693525 +0000
@@ -1,19 +1,15 @@
-export struct UrlAttribute {
-  string property;
-  string[] elements;
-  bool allElements;
-}
-
-export UrlAttribute[] urlAttributes = [
-  UrlAttribute{"action", ["form"], false},
-  UrlAttribute{"cite", ["blockquote", "del", "ins", "q"], false},
-  UrlAttribute{"data", ["object"], false},
-  UrlAttribute{"formAction", ["button", "input"], false},
-  UrlAttribute{"href", ["a", "area", "base", "link"], false},
-  UrlAttribute{"icon", ["menuitem"], false},
-  UrlAttribute{"itemId", [], true},
-  UrlAttribute{"manifest", ["html"], false},
-  UrlAttribute{"ping", ["a", "area"], false},
-  UrlAttribute{"poster", ["video"], false},
-  UrlAttribute{"src", ["audio", "embed", "iframe", "img", "input", "script", "source", "track", "video"], false}
-];
+// html-url-attributes@3.0.1: the attributes react-markdown passes through `urlTransform`, with the elements
+// they apply to (`0`: every element; upstream's `null`).
+export JsValue urlAttributes = object {
+  action: ["form"],
+  cite: ["blockquote", "del", "ins", "q"],
+  data: ["object"],
+  formAction: ["button", "input"],
+  href: ["a", "area", "base", "link"],
+  icon: ["menuitem"],
+  itemId: 0,
+  manifest: ["html"],
+  ping: ["a", "area"],
+  poster: ["video"],
+  src: ["audio", "embed", "iframe", "img", "input", "script", "source", "track", "video"]
+};
diff -ruN a/src/vfile/lib/index.lil b/src/vfile/lib/index.lil
--- a/src/vfile/lib/index.lil	2026-09-23 20:40:45.234456149 +0000
+++ b/src/vfile/lib/index.lil	2026-09-23 20:40:45.363964596 +0000
@@ -1,12 +1,3 @@
-import {
-  VFile as SharedVFile,
-  VFileMessage as SharedVFileMessage,
-  createVFileMessage as sharedCreateVFileMessage
-} from "../../graph/unified/vfile.lil";
+import { VFile, createVFileMessage } from "../../graph/unified/vfile.lil";
 
-export JsValue VFile = SharedVFile;
-export JsValue VFileMessage = SharedVFileMessage;
-
-export JsValue createVFileMessage(string reason, JsValue options) {
-  return JS.call(sharedCreateVFileMessage, JS.undefined(), reason, options);
-}
+export { VFile, createVFileMessage };
