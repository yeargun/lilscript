# Migration progress — the dense ledger

Parent: [index](index.md). Plan: [009](009-phases.md), re-sequenced by [012](012-second-look.md).
Narrative and evidence per step: [history](history.md); frozen phase 0–2 evidence: [status](status.md).

**This file is the authoritative state**, updated in the same commit as the work it describes.
One row per landed step. Numbers are Brotli-11 from `lilscript-codec` unless marked raw; ports are
pool-built from arm-isolated binaries; "cases" is `tests/cases` (73 files) plus `~/probelil`.

Updated 2026-09-05, branch `migration/target-tree`, 111 commits ahead of `main` (`54e1948`).

---

## State in one screen

| Phase | State | Gate |
|---|---|---|
| 0 instrument | **done** (0.4 narrowed to two ports + probe + cases, by the owner) | reverting a fix turns the probe/cases red; the pool runs the matrix in 11 s |
| 1 expression tree | **done** | 44/44 artifacts identical on three canaries; twin witness with negative control |
| 2 statements, block | **done** | escapes 26 → 0; block is a type; canaries identical |
| 3 tree authoritative | **done on the emitter** | `Raw` gone, text deleted, classifiers structural; G1/G2 deletion moved to 6 |
| 4 facts delivered | **done on the node** | `IrFacts` delivered; `NodeId` required; 8-bit fact word stamped; byte-identical |
| 5 naming post-layout | **mechanism done; deletions deferred** | binds, spelling table, respell, scope tree, sound renamer, census; idiom g1 as a per-port pin (−1,168 net on six ports); default byte-identical |
| 6 fold groups | **4 printer-side retirements landed** (6.1–6.4) | each: solo residue 0, Brotli same-or-better on ports, behaviour/twin/tests/pool green |
| 7 candidate derivation | **evidence only** | L11 vs L13; emission-count profile; scoring-order pathology named — **next**, see 012 |
| 8 retire the text layer | not started | — |

**The gate** (owner, 2026-09-04): behaviour never relaxes; Brotli and compile time must be same-or-better
when the migration is *finished*, not at every step. Byte identity is evidence, never a requirement.

**The two verification ports against the pre-migration compiler `54e1948`:**

| port | `54e1948` | now (`acace54`) | Δ |
|---|---:|---:|---:|
| markedlil `marked.esm.js` | 9,470 | **9,322** | −148 |
| zodlil `zod.core.js` | 32,489 | **32,415** | −74 |

Both are below the baseline they must end at or below, with every correctness fix's cost inside
(live-12 alone was +35 on zodlil). Compile time is not yet measured end-to-end against `54e1948` on
one worker; the current profile is under *Measurement facts*.

---

## The steps

### Phase 0 — repair the instrument

| commit | step | evidence |
|---|---|---|
| `fe51558` | `[profile.release-assert]`; `portgate.mjs`; random differential seed; fleet F1/F3/F4/F5; `fleet-compare.mjs`; live-3 (config discovery) and live-4 (`charCodeAt`) fixed | posthoglil `trust: ok`; markedlil portgate 406 s, suite green, 77.7% idle folds |
| `5fc2aac` | live-7: a two-use inlined value gets a name; parameters never skipped | release gate green again (`optional_constructor_callback` compiles) |
| `76d3c4f` | live-6: `.length` ToNumber elision closed at the family's *admission*, not the option default | the search had re-proposed the wrong spelling after the default flip |
| `152b830` | live-5: two folds that deleted a live binding refused (`remove_unused_standalone_vars`, `fold_single_use_literal_bindings`) | found by `LILSCRIPT_SKIP_FOLDS` bisection in minutes |
| `bf3a89f` | first arm-isolated pool A/B (`54e1948` vs `152b830`) | 12 artifacts: 4 identical, rest −39..+46 — correctness came free; compiler digests recorded |
| `6d0a741` | 0.3b: the differential's interpreter models classes, `super`, `this`, lexical capture | finds live-8 within minutes |
| `68801c4`, `d07a529` | live-8: a phi region that duplicates a side effect is never proposed (`may_have_effects` on the tree) | 0 Brotli over 5 ports; 10 wrong candidates per compile stop being built |
| `a5dee05`, `8cb7979` | `workers.mjs check`: the case matrix on the pool; configs travel with the tests; pool driven from a worktree | 144 case-lanes in 11 s; the first run's 72 false failures were config discovery (rule: discovery is never load-bearing in a harness) |
| `aaae9a4` | fold census on the fleet (`LILSCRIPT_FOLD_REPORT=all`) | 53 of 128 folds never fire on four ports; never-active folds are ~3% of CPU |
| `80fac19` | phase 7 evidence: the effort ladder | L11 beats the shipped L13 on cnlil/posthoglil/mobxlil in bytes *and* time; markedlil prefers 13 |
| `3fe40f3` | `~/probelil`: one feature-dense library, both lanes, golden output, 22-config matrix | 4.6 s inner loop; 21/22 agree (`level15` is live-9) |
| `5b289d6`, `53a4275` | the gate revised to behaviour + end-state; this ledger created | owner directive 2026-09-04 |

### Phase 1 — the expression tree (`56fc86d` … `4c10031`)

| commit | step | evidence |
|---|---|---|
| `56fc86d`, `0081beb` | the arity table, written down then enforced (`render(..).expect` per kind) | found `NullNormalized` built with no operands, the `Member`/`Index` tag collision, an operandless `>>>0` |
| `e7fa02f`, `b9ebd20`, `697c735`, `8ed77c3`, `0570fea` | one owner for children; `code` derived by `render` for every non-leaf kind; `JsRenderOptions` replaces 57 hand-threaded copies of one flag | 0 of 72 artifacts differ per step; 1,705 tests |
| `b31c5d6` | `LILSCRIPT_TWIN=1`: every node rebuilt from its children, compared to its own text; negative control | 72/72 both lanes; a corrupted arm fails 53 of 72 |
| `4c10031` | proved on cnlil | 8/8 artifacts identical; twin clean on a 26.8 KB artifact at +2% time |

### Phase 2 — statements, functions, the module (`404ec93` … `7adb6e0`)

| commit | step | evidence |
|---|---|---|
| `404ec93` | 2a: `type JsBlock = String`, 63 signatures | pure rename |
| `292803b`, `00206f1` | 2b: the block is a type with counters; the loop-keyword census stops rescanning the artifact | first counters were byte-identical and **23% slower**; bounded: cnlil 76.2 s → 76.4 s |
| `db4ede9`, `98666ab` | IDENTICAL on cnlil, posthoglil, markedlil | 44/44 artifacts; time +0.3% / +0.4% / +0.006% |
| `76859e6` | `take_trailing_expression_statements` measured | 5.1 ms of 31.2 s (0.016%) — retracted as a superlinearity in 009 |
| `afacdc1` … `9ac8c49`, `7adb6e0` | statements become nodes (bindings, `return`/`throw`, loop control, module clause, declaration groups, `if`/`else`, loop bodies as values, fusion runs held back, terminators as facts); escapes 26 → 0; escape methods deleted | each family found a disagreement the text hid (two `return` sites, two `export` emitters, a heuristic reading the wrong buffer) |

### Phase 3 — the tree becomes authoritative (`ec19c3e` … `ac50e29`)

| commit | step | evidence |
|---|---|---|
| `ec19c3e` … `9254eeb`, `7012629`, `5622552`, `d0667b5` | the block is a statement list with the text as its cache; statement bytes arriving as nodes 61% → 100% on the probe and all cases; the state machine, module `let` list, cluster IIFEs, class sites as nodes | ports identical at every batch except `2d7a01a` (zodlil +18: one concise arrow lost, recovered in `229b728`) |
| `523ca17` | `Declarators` node; live-10 (a name declared twice in a `let` run) fixed | two cases smaller (1238 → 600); zodlil +6 |
| `7e5975e`, `3bb2322` | classifiers get structural twins under the witness, then the call sites read the list | live-11 found (`1==x?..` accepted as an assignment to `1`); **live-12** found and fixed (`if` body of `[try{..}, stmt]` spelled braceless — a wrong program on `main`, zodlil +35 as the correct program's price) |
| `f6d515a`, `29b8ed7` | `Raw` gone; the block's text deleted; `JsBlock` renders from its list | probe wall 19.8 s → 16.7 s, emit CPU 104 s → 83 s |
| `9ed41cb`, `e5c40c3`, `78aaf49` | G1 emitter side: keyword separator; braceless `if`/`else` unless the `else` would be captured; presence test as a node (`UndefinedTest`) | raw residue 0 for all three; zodlil −72 (keyword spaces); braceless: zodlil +47 / markedlil −114 |
| `1516ad0`, `ac50e29` | `braceless_control_bodies` is a knob and a scored variant; the G1/G2 "unreachable" assumption corrected | a fold that fires on other folds' output is deleted with them (phase 6) |

### Phase 4 — deliver the facts (`f609a79` … `a24ee0f`)

| commit | step | evidence |
|---|---|---|
| `f609a79` | `IrFacts` (effect summaries, finite values, array-parameter lengths) delivered per emission | byte-identical; `facts_delivered` counted |
| `46df98b` | `NodeId` required on every instruction, module-wide allocator; 18 `None` sites derive | two subsumption tests caught the first attempt |
| `ed20326` | `JsOrigin` + `JsFacts` on every node; **`PairGraph`** replaces the BFS in `safe_two_address_phi_pairs` | probe wall 16.7 s → 5.8 s, emit CPU −80%; the "+17%" before it was a code-alignment lottery, proved with an uncalled function |
| `a24ee0f` | `PURE`, `NO_THROW`, `OWNED_SLOT`, `NON_NULLISH` from oracles the tree had (`ValueKind`) | byte-identical; 1,714 tests |

### Phase 5 — naming post-layout (`bac9678` … `8d255e1`)

| commit | step | evidence |
|---|---|---|
| `bac9678` | the name-request-order trace (`LILSCRIPT_NAME_TRACE=1`, `name-trace-diff.sh`) — the gate lands before any naming moves | 759 requests per emission on the probe; self-gate 0/12 |
| `cc88070`, `e640a1c`, `61df684` | `Bind(u32)` on references, declarations, heads, loop heads, catch, module names | 0 byte / 0 trace diffs each; census `decl_bound` 53,505 / unbound 31,277 |
| `6cdbe83`, `c491f98`, `578625f` | heads as pieces; closures as nodes with kept trees (`ClosureId` per rendering); spelling as a side table with the `Respell` walk, witnessed both ways | ports byte-identical each step |
| `abbceb9` | `ScopeTree`, the sound renamer, `NameOrdering::FrequencyDesc` off by default | a closure-tree key collision (wrong program) caught and fixed before landing |
| `491a856`, `a38f42c`, `30c30a1` | conditions keep their trees; three raw sites keep their node; function/global references carry binds | probe renameable bindings 100 → 7,549 of 14,545 |
| `d84992b`, `9b54a4c`, `66a36ab`, `e9d7e5d` | `LILSCRIPT_NAME_ORDERING`, `_SEARCH`, the `rename_kept_<kind>` census, the priority-slot family | **frequency-desc pinned on the fleet: markedlil −35, zodlil +55, cnlil +77, micromarklil +591, mobxlil +22 — off** |
| `e20d700` | the port twin lane (`port-twin.sh`); the `this`/`arguments` receiver bind leak fixed | remarklil, cnlil, micromarklil clean; live-13 recorded (pre-existing) |
| `e0436a3`, `8d255e1` | `IdiomConverged` on binding identity, the collision guard, one idiom group per candidate, the `name-ordering` Cartesian axis | **g1 pinned on six ports: remarklil −811, micromarklil −391, mobxlil −37, markedlil −5, cnlil +3, zodlil +73 — net −1,168; ships as a per-port pin, default byte-identical**; as a late candidate it wins nothing (names steer layout) |

### Phase 7′ — one emission, many prints (`6ee42eb` …)

| commit | step | evidence |
|---|---|---|
| `6ee42eb` 7.0 | the reading: Terser 5.44.0, Oxc 0.147.0, Closure master — each runs its last transform before the printer and scores nothing in between; `refs/competitor-techniques.md` §I | file:line for the pipeline order, the printer's own options, and `best_of` |
| 7.1 | `ModuleTree` (block, closure trees, bind table) kept by `emit_with_tree`; `reprint(options)` is a forced `Respell` under new printer options; `LILSCRIPT_PRINT_TWIN=1` compares re-print with re-emission per candidate field | **census, 148 emissions (74 files × shipped-off, none-off):** every field with an effect is emission-dependent — `function_spelling` 40, `string_quote` 38, `conditional_expressions` 33, `comma_expressions` 30, `mutation_spelling` 28, `update_loop_layout` 27, `compact_boolean_literals` 24, `loop_spelling` 10, `elide_new_parentheses` 7, `truthy_nullable_checks` 6, `elide_call_chain_parentheses` 5 of 11, `unused_catch_binding_elision` 4, `effect_ternary` 3, `compact_generator_star` 2, `braceless_control_bodies` 1; `elide_block_terminal_semicolons` has no effect any more (6.4). Byte-identical to `acace54`; 1,717 tests |

| 7.2 | booleans are a `Bool(bool)` leaf the printer spells (`JsRenderOptions.compact_boolean_literals`); stores and fused-run members are `Assign` nodes (`Member`/`Index`/`Name` targets) instead of raw text; `statement_expression_node` feeds the fusion run | byte-identical to `cd50de7` in three lanes (one swap-canonicalisation regression caught by the none lane and fixed: `is_constant_literal` keyed on the atom root); twin: `compact_boolean_literals` 24 effects → 18 print-ok / 6 emit-dep (the six: the inline-cost shape `let a=true` vs `!0` inlined, ternary arms and array elements still text), `elide_call_chain_parentheses` 11/11 print-ok; renamer `rename_kept_raw` on the probe 16 → 6; 1,717 tests |

| 7.3 | string literals are a `Str(Lit)` leaf over a per-emission `LiteralTable` (interned: equal contents, one id — an arm merge compares nodes), spelled by the printer from `JsRenderOptions.string_quote`; the table rides the `Mangler` so the naming context's inlined constants are leaves too | byte-identical to `a32eb07` in three lanes after two parity fixes the none lane caught (the constant-operand swap must not widen to template quotes; equal strings must compare equal); twin: `string_quote` 38 effects → 24 print-ok / 14 emit-dep (call arguments, array elements and ternary arms are still text); 1,717 tests |

| 7.4 | array literals are an `Array` node over element nodes (the packed `"a,b".split(",")` spelling stays an atom, chosen by length as before) | byte-identical to `bf6d05c` in three lanes; twin: `compact_boolean_literals` 19 of 24 print-ok, `string_quote` 25 of 38; 1,717 tests |

| 7.5 | `typeof x` is a `Unary(TypeOf)` node and `Symbol(..)` a `Call` node (two raw producers gone); `[emission-options]` trace and `emission-axes.py` count the option tuples the search emits | byte-identical to `f80e669`; **the emission axes:** probe 381 emissions / 130 tuples — `string_quote` varies on 168, `identifier_alphabet` 102, `stable_local_names` 165; markedlil 277 / 141 — `string_quote` 64, the naming-policy fields (`stable_local_names` 107, `local_name_reserve` 32, `precise_cross_scope_shadowing` 22, `frequency_order_local_names` 19) ≈ 196, structural (`constructor_initializer_fusion` 72, `iife_private_callee_clusters` 66, `function_layout` 25) the minority; 1,717 tests |

| 7.6 | the first search-side re-print: `FrozenModuleTree` (a `ModuleTree` with its tables out of their `Rc`s, so it crosses threads), `emit_javascript_candidate_frozen`, `reprint_javascript_candidate` (finished by the same six memoized folds an emission gets, timed as `reprint`), and under `LILSCRIPT_REPRINT_QUOTES=1` the emission contexts keep the first tree per (context, options with `string_quote` erased) and print a later plan that differs only in the quote from it | **probe:** 381 → 279 emissions, 102 re-prints at 2.3 ms against 48 ms per emission, wall 5.39 → 5.11 s, Brotli 1,514 → 1,517 (the residue sites keep the base quote; parity waits on them); cases +1..+3 on six files, behaviour 0 wrong, probe both lanes ok. **markedlil: 1 re-print of 277** — its 64 quote variants are paired with identifier-alphabet variants (the spelling family emits `(alphabet, quote)` pairs whose `Double` base is never emitted), so the cache misses; the axes that multiply markedlil's emissions are the naming ones, which need a name-table replay, not a re-print. Off by default; 1,717 tests |

| 7.7 | the conditional return keeps its node (`ConciseNode` / `Return { value }` instead of `into_minimal` text); class construction is a `New` node (identity classes) or a `Call` node with `value_atom`/`function_atom` (positional classes); record field stores are `Assign` nodes | byte-identical to `9dba734` in three lanes; twin: `string_quote` 30 of 37 print-ok, `compact_boolean_literals` 20 of 24; the residue: `default_value` text (`b=''`), `coalesce_absent_to_null`'s `??null` wrapper (kept raw: its text precedence is `Conditional`, the node's `LogicalOr`, and `a??null||b` must stay grouped), `Set.has` arguments, the `typeof` comparison; 1,717 tests |

| 7.8 | `[emission-context]` beside the options trace, so the axes are counted per IR context; ports and pool re-verified for 7.1–7.7 | **markedlil, per context:** 277 emissions over 13 IR contexts; 17 exact duplicates (same context, same options); erasing `string_quote` alone frees 18, the identifier alphabet 17, the nine naming-policy fields 91, naming + quote **99 of 277 (36%)**; the remaining 178 are structural bases. Ports byte-identical to 6.4 (markedlil 9,322, zodlil 32,415, same digests); pool 292/292 |

| 7.9 | **the text layer, measured** (owner asked: does it still run?). Yes: 47,281 lines, 124 registered folds, 78 runs per markedlil compile (21 s of 136 s single-thread). Census with the 7.8 binary — probe + 73 cases in 3 lanes, plus markedlil: **52 folds active somewhere, 72 registered folds never fire** (boolean 18, classes 14, loops 11, declarations 10, control 7, syntax 6, copies 3, members 2, calls 2). On markedlil's single emission 29 folds rewrite, 33 activations in chain order; two of the four 6.x ports still fire there: the inner `;`→`,` join (320 sites: 6.2 did the module level only) and the `var`-list for-init (2, deliberately left). **Chain-head rule:** a fold at chain position *k* can be ported to the emitter byte-neutrally only when every fold before *k* is idle on that text — the earliest active folds are the neutral ports. Ported the two chain heads: `fold_assignment_guards` (`a=f();if(a)` → `if(a=f())`, `merge_assignment_guard` at push time, by binding) and `remove_unused_standalone_vars` (`prune_unreferenced_declarators` after `build_module`, by binding, the `ScopeTree` as the reference count); hoisted bare declarators and the if/else merge's declarator now carry their binds (`claim_remaining_declarations` returns declarators). One token still moved: the leading `let`/`var` is scored by the codec on the *pre-peephole* emission, and the changed emission flipped a tie the final text loses (+43 markedlil, search off) — **the 012 scoring pathology, reproduced** — so the terminal slot now re-decides the leading keyword on the final text (`terminal_leading_declaration_respell`, strictly non-regressing; `terminal_keyword_flipped`/`kept` counters). Prior art for G4/G5 read and recorded (refs §J) | markedlil search off: byte-identical to 7.8; `fold_assignment_guards` 1→0 active, `remove_unused_standalone_vars` 2→1 (the last one at chain position 21, on a var an earlier fold emptied). Cases: 73/73 identical in every lane (maps_sets −1 by the terminal keyword). Twin 146/146, tests 1719, probe 21/22, pool 292/292. **Ports with the search on: markedlil 9,322 → 9,344 (a different spelling family won: 2,437 token changes), zodlil 32,415 → 32,427 (dev config, no peephole: the emitter now writes the guard shape and drops 2 dead vars, raw −38, Brotli +12).** The ranking among candidates is done on pre-peephole text, so any emission-side port moves the winner even when each candidate's final text is unchanged: byte identity under the search needs 7a first, as 012 ordered |

| 7.10 | **7a built and measured.** `EmissionPeephole`: every emission passes the text peephole before anything scores it, codec-verified per emission (the folds are not monotone: the async single-use IIFE lost to the binding it replaced), under the finalist stage's own function-elision and startup-limit rules (jquerylil's `max_nesting` rejected every candidate before the guard existed). Knob `javascript.emission_peephole`, env `LILSCRIPT_EMISSION_PEEPHOLE=0|1` forwarded by the pool; the function-scope wrapper's "exported binding written from a nested scope" check made scope-aware (a local shadowing an export is not a write to it). Fleet A/B on the pool, 27 ports, knob on vs off | **markedlil, search on, local: 9,234 → 9,190; wall 136 → 178 s single-thread (peephole 78 → 322 runs); all 259 changed emissions won at the codec, 0 lost.** Cases: shipped 7517 → 7508, none 11389 → 11383, zodlike 7467 → 7475, behaviour 0 diffs, twin 146/146, tests 1719, probe 21/22, pool 292/292. **Fleet: 19 ports built in both arms; on 342,388 vs off 341,735 — on is +653 worse**: mobxlil +429, remark-gfm +173, unifiedlil +88, markedlil +42; wins posthog 9, remark-math 10, remark-breaks 13, mdast-util-to-hast 48; ten ports identical. The search made fewer scored measurements with the knob on (remark-gfm codec calls 1,722 → 2,711 of which ~1,100 are the per-emission verification, mobxlil's main compile 1,156 → 938): folded emissions collide, and exploration keys on distinct text (`seen_code`, the frontier dedup, the entropy sources), so the search explores less. **Default off** per the owner's rule; the knob is the byte-neutrality instrument for fold retirements until the search keys on plan identity (7e). jquerylil, motionlil, react-markdownlil, solidlil failed in both arms — see live-15 |
| 7.11 | third chain head: `x=x±1\|0;` and the member form are `x++`/`x--` at push time (`Update(JsUpdate)` node, `unit_counter_update`; `fold_unit_counter_updates`); the guarded-decrement rotation now carries its condition tree (the twin caught `a>0` beside `a--`) and fires only under an explicit prefix/postfix spelling, as before. Both push-time shapes gated on `IrJsOptions.text_peephole` (the compile runs the peephole): zodlil read +49 when the unit update reached its postfix variants unasked, and the option that owns the spelling (`mutation_spelling`) must keep its choice where no fold overrides it | markedlil search off byte-identical; the 8 node-shaped `x=x-1\|0` become `x--`, the 8 `x=x+1\|0` are `Raw` bindings from the phi-copy path (the residue). Cases: none identical, shipped 73/74 (probe +3), zodlike 72/74 (−3); behaviour 0; twin 146/146; tests 1719; pool 292/292. **Ports under the search: markedlil 9,344 → 9,355, zodlil 32,427 → 32,476** — the same perturbation as 7.9: with pre-peephole ranking every emitter-side shape moves the winner. Probe now 22/22: live-9's level-15 configuration takes a different candidate; the fold is unchanged, live-9 stays open |
| 7.12 | raw-site ranking on markedlil after 7.9–7.11 (`LILSCRIPT_RAW_SITES=1`, kept-name hits): the if/else merge zone (`emit_structured_path` 12977: 207 hits, 13226: 95, 12941: 53, 13309: 46), `render_intrinsic` (16837: 121, 16941: 37), the phi-edge copies (`emit_phi_edge_cached` 15250: 81 over 180 nodes), named callee clusters as text (9861: 48), `render_op` stores (15973: 48). Statement kinds pushed on markedlil's emission: 238 `Binding` with a `Raw` value, 144 `Expression Raw` | the queue for G10/G4: the merge zone first (401 hits over four sites), then the phi copies |

| 7.13 | **the if/else merge zone as nodes (G10, first three arms).** The merged single assignment (`statement_single_assignment_node`, `block_merge_conditional_assignment_nodes`: the value node and the target's bind), the merged value built as `nullish` / the condition tree / its negation / `binary(Or\|And)` / `conditional` nodes, `push_merge_declaration` and the deferred merge carrying nodes; the effect ternary and the guard compaction (`cond&&arm`, `!cond\|\|arm`) as nodes through `block_compact_arm_node`, offered only when the node spells the text the zone would have written, so the zone's remaining text rewrites keep their cases. A raw operand under `&&`/`\|\|` is grouped by the zone's old text scan rather than its declared precedence (`binary_operand`), until the raw sites are gone | markedlil search off: six redundant parentheses gone (`s=(s>=48&&s<=57)\|\|…` → `s=s>=48&&s<=57\|\|…`: the old scan read `>=` as an assignment), Brotli identical. Raw ranking on markedlil: the zone's 207 + 95 + 53 + 46 hits are gone; the head is now `render_intrinsic` (105), the phi copies (80 over 180 nodes), `render_op` stores (48), the callee clusters (46). Cases identical in every lane; twin 146/146; tests 1719; pool 292/292. **Ports: markedlil 9,355 → 9,310, zodlil 32,476 → 32,413** — both below 7.8 (9,322 / 32,415) for the first time since the chain-head ports |

| 7.14 | the intrinsic constructor `new C(a,b)` is a `New` node over its argument nodes; `HostFieldSet` is an `Assign` node over a `Member` target (the last raw store) | byte-identical everywhere: markedlil search off, cases in every lane, ports (same digests as 7.13); twin 146/146; tests 1719; pool 292/292. Raw ranking head on markedlil: the phi copies (72 hits over 180 nodes), the callee clusters (44), the `+""` coercion (37) |

| 7.15 | **the phi-edge copies as nodes (G4, the copy statements).** `PhiCopy { target, bind, source: JsExpression }`; the single copy, the declarator list, the ordered scalar copies and the tuple (`[a,b]=[x,y]`: an `Array` node over the sources, the pattern still text) keep their nodes and binds; the dependency ordering reads the sources' text as before; the swap through a temporary stays textual (it rewrites names inside sources). Loop increments now reach the push-time unit update, so `fold_index_postfix_updates`, which precedes the counter fold in the chain, sees `s++` first on one markedlil loop and `for(s=0;…;s++){…N[s]=U}` becomes `s=0;while(…){…N[s++]=U}` — **a port that makes a shape appear earlier can wake an idle earlier fold; the chain-head check must be re-run on the new text** | markedlil search off: Brotli 10011 → 10008 (that loop, plus the terminal keyword flipping `var Fd` to `let`); raw ranking: the phi site (72 hits over 180 nodes) gone; head is now the callee clusters (40), the `+""` coercion (37). Cases: shipped 7520 → 7509, none 11389 → 11395 (33_algorithms: the destructuring copy now takes 6.3's for-init absorption, 350 → 337 raw), zodlike 7464 → 7459; the other diffs are name-order shifts where the renamer now sees the copies' binds; behaviour 0 diffs; twin 146/146; tests 1719 (the copy fixtures are `PhiCopy`s); pool 292/292; probe 21/22 (live-9 visible again). **Ports: markedlil 9,310 → 9,297, zodlil 32,413 identical** |

| 7.16 | the last intrinsic text: `x===void 0` is the `UndefinedTest` node, `x==null` and `x+""` are `binary_in_order` nodes (no constant-first swap, no coercion rewrite), the `""` an interned literal that follows `string_quote` (the text always wrote `""`, even inside a single-quote candidate) | markedlil search off and every case lane byte-identical; twin 146/146; tests 1719; pool 292/292. **Raw ranking on markedlil: only the two callee-cluster bodies remain (31 + 20 kept-name hits); every other raw site is under 3.** Ports: markedlil 9,297 → 9,364 (a different winner: the consistent quote inside single-quote candidates moves the pre-peephole ranking; raw −221), zodlil 32,413 → 32,391 |

| 7.17 | string constants are interned `Str` nodes (the `Const` op), the type check `"number"==typeof x` a `binary_in_order` over a literal and a `typeof` unary (`Array.isArray` a call), the absent-to-null coalescing the `NullNormalized` node (grouped under `&&`/`\|\|` like `??`); the callee clusters' root bodies and the wrapping IIFE are closure trees behind `Closure` nodes (`render_closure_statement`), the wrapper's callee at the lowest precedence so the call spells its parentheses. The renamer's typeof round trip fixed (`typeof ` with its space) | **print twin on the cases: `string_quote` 35 same / 1 diff (was 33/5; the one left is a module-level array of string constants built as text), `elide_call_chain_parentheses` 11/11, `compact_boolean_literals` 18/4 (the 4 are literal pooling reading the spelled length)** — the two clean fields are 7b's first re-prints. Byte-identical on markedlil search off and every case lane at each step; twin 146/146; tests 1719; ports identical (markedlil 09d2b39c, zodlil 9475d724). **markedlil's raw ranking is empty above 3 hits: the emission is nodes.** Pool 290/292 caught the typeof round trip on `type_guards` in the renamer lanes |

| 7.18 | **live-15 fixed**: a raw `throw ..` expression statement becomes a `Throw` statement at the push hook, so no compaction reads it | jquerylil compiles and parses again (search off: 92,829 raw / 31,219 Brotli; the pool's search-on number follows in 7.19). Cases identical in every lane; twin 146/146; tests 1719; pool 292/292 on the typeof build |

| 7.19 | jquerylil on the pool with the live-15 fix, the search on | **82,318 raw / 28,382 Brotli, against the pre-migration baseline `54e1948` at 86,072 / 28,764: −382 (−1.3%)** — the first giant measured on the branch, and the first end-state-style number: same source, same config, the tree's emitter beats the text-era one. markedlil and zodlil identical to 7.18; pool 292/292 |

| 7.20 | **7b, first three axes as re-prints.** A plan that differs from an emitted one only in `string_quote`, `elide_call_chain_parentheses` or `compact_boolean_literals` re-prints that emission's frozen tree; both emission paths (`emit`, `emit_frozen`) share one cache keyed by the erased options per IR context. The boolean inline-or-bind decision costs the compact spelling whichever prints, so the tree is the same under both spellings. Knob `javascript.reprint_spellings` (default on), env `LILSCRIPT_REPRINT_SPELLINGS` | print twin on the cases: `string_quote` 37/1, `compact_boolean_literals` 23/1, `elide_call_chain_parentheses` 11/11 (the two residuals are one struct literal still built as text). **Honest yield on markedlil: 3 re-prints of 278 — within one IR context the search proposes almost no spelling-only siblings (0 quote/paren, 4 with the alphabet, 16 with the frequency order, 78 with all eight naming policies); 7.8's "18 quote-only" had lumped the 20 unlabelled seed emissions.** The 30% cut is 7d's, the naming policies as a renamer pass over the frozen tree. Cases: 15_strings and callable_defaults move by noise; none lane identical; behaviour 0; tests 1719; pool 292/292; probe 21/22; ports identical (09d2b39c / 9475d724) |

| 7.21 | **7d, the naming policies as a rename pass over the cached tree.** `FrozenModuleTree::rename_reprint`: the tree's own scopes, the renamer in emission order (`RenameOrder::Emission`, the bind table's order) under the plan's alphabet, then the print. The cache entry carries its base's naming key; a plan with the same key re-prints, another key re-names — for the three policies that decide only which spellings a binding may take: `identifier_alphabet`, `local_name_reserve`, `reserved_local_name_prefix`. The others proved structural on the cases: `frequency_order_local_names` orders the hoisted declarators, the shadowing flavours decide whether a value is bound or inlined (17_struct: `var point=[13,29]` vs the literal in the call), `stable_local_names` needs the source hints on the tree; without identifier mangling every policy is a plain re-print. Re-prints count as attempted emissions (the resource-count test wants the same count at any thread count). Knob `javascript.reprint_names`, env `LILSCRIPT_REPRINT_NAMES`, off until the fleet measures it | markedlil, search on: emissions 278 → 263, 16 rename re-prints in 0.25 s, Brotli identical (9,190 local; pool 09d2b39c, wall 23.7 → 23.0 s); zodlil identical. With all eight policies erased it was 219 emissions and −7, but the none lane moved by −618 (the pass renamed unmangled names) and then +19 (the structural policies), which is what fixed the field set. Cases with the knob on: none identical, shipped 7513 → 7511, zodlike 7463 → 7475 (the renamer's spellings, not the emitter's), behaviour 0; tests 1719; pool 292/292; probe 22/22 |

| 7f (interim) | **end-state measurement, first pass**, the pool's workers, same source and config, the pre-migration `54e1948` against `18df6cc` (7d, knob on) | markedlil 9,470 → **9,364** (−106); zodlil 32,489 → **32,391** (−98), wall 2.39 → 1.76 s; jquerylil 28,764 → **28,382** (−382, at `a36a6cd`). markedlil's four artifacts: walls 45.2/44.0/24.8/27.2 s → 23.0/22.2/32.6/31.0 s (the pool's contention moves these; a same-worker sequential pair is owed). Search off, single thread, this host: markedlil emit 1,427 → 257 ms and wall 2.02 → 0.78 s; jquerylil emit 621 → 541 ms, wall 6.5 → 4.6 s. jquerylil with the search on, the pool: 450 → 576 s wall with the peephole 1,102 → 1,467 CPU-s over 455 → 626 runs and emit 482 → 940 CPU-s over 901 → 914 emissions — **open**: the per-emission cost fell everywhere measured, so the search reaches heavier emission kinds or more finalists; measured locally next |

| 7.22 | the positional struct literal and the inlined class value are `Array` nodes over their field nodes (the named object forms stay text until an object-literal node exists); field defaults as literal nodes; `LILSCRIPT_ATOM_SITES=1` names the source line of every bracketed atom (`#[track_caller]` on `atom`) | **print twin on the cases: `string_quote` 38/38, `compact_boolean_literals` 24/24, `elide_call_chain_parentheses` 11/11 — 7b's three axes proved per option.** markedlil search off byte-identical; cases identical except interprocedural_finite_values in the none lane (+9: the literal's quote now follows the option); tests 1719; pool 292/292; ports identical |

| 7.23 | fleet A/B of `reprint_names` on the pool (`18df6cc`, 27 ports, 20 built in both arms) | **on 370,105 vs off 370,069: +36 over 20 ports — a wash.** remark-gfm −187 (on better), jquerylil +102, mobxlil +54, posthoglil +28, four small ports +1 to +14; twelve ports identical. Emissions with the knob on: markedlil 276 → 263, remark-gfm 544 → 501, posthoglil 1,212 → 1,083 (191 rename re-prints), micromark 59 → 53. The default stays off (the owner's rule: a flip needs a measured win); the compile-time gain is real and the size is noise, which says the three pure naming policies were never where the bytes were — the structural ones (frequency, shadowing, the source hints) are, and those need the tree to own the bind-or-inline and declarator-order decisions (7e) |

| 7.24 | **the push-time render is gone.** `push_statement_with` rendered every statement to count `for(`/`while(` (the `LoopSpelling::Auto` heuristic) and to read its `;`, and `pop_statement` rendered again; a function body was therefore rendered once per enclosing level (the gdb sampler on the no-cluster variant: the innermost emitter frames were `EmittedStatement`/`JsStatement`/`JsExpression` renders). Both are read off the shape now: `loop_keywords_in_statement` (loop heads and the leaves the tree does not own — raw nodes, closures' code, atoms — where the old count found them too) and `statement_is_terminated`; the block's `tail` needle buffer is gone. `[emission-ms]` per-emission trace under `LILSCRIPT_EMISSION_OPTIONS`; `pmp2.sh` samples a markedlil compile | markedlil search off: emit 403 → 155 ms (baseline `54e1948`: 2,446), the no-cluster variant 1,007–1,632 → 305 ms (baseline 2,340); **search on, single thread: wall 169 → 109 s, emit 130 → 66 s, output identical**. Cases identical in every lane; tests 1719. jquerylil on this host, 4 threads, before this fix: baseline 1,361 s wall / 28,649 vs `18df6cc` 1,605 s / 28,332 — emit 726 → 975 CPU-s over 901 → 815 emissions was the whole regression; measured again with this fix in 7.25 |

| 7.25 | **Three batches, one verification each (the owner's batching rule).** Batch 1: 8 folds that never fired on the cases, markedlil or the fleet deleted with their helpers and tests (drop_orphaned_class_identity_guards, fold_conditional_assigned_false_phi, fold_console_log_conditionals, fold_guarded_and_addends, fold_guarded_uninitialized_assign, fold_increment_infinite_for_bounds, fold_index_scan_for_headers, fold_predicate_reassign_same_expr; −1,321 lines, four whole-chain tests that asserted their output gone, six emitter tests of deleted text helpers gone); the inner comma join (G1) on every block; `x=E;x=x OP B` as `x=E OP B` at push time and inside sequences (G4). Batch 2: the control shapes as one structural pass over the finished tree (`shape_block`, children first): braces recomputed per branch with a joined run as one statement and `else if` (G2, 102 sites on markedlil), conditional return tails (G5, 72), continue-tail guards (G5, 50), early-exit guards at a function's or loop body's end (G5, 18), `c?x=a:x=b` as `x=c?a:b` in the conditional constructor (G4), the trailing unit update lifted into the loop head when no `continue` targets the loop (G3, 108). Batch 3: closures shaped where they are rendered, the lift through a comma tail, `x=>{return v}` as `x=>v` once the tails fold, the for-init hoist. Every port is a `StatementPolicy` flag on the peephole-configured compile, `LILSCRIPT_SKIP_PORTS=…` turns one off on a built binary, `LILSCRIPT_PORTS=…` turns on one that ships off; `[peephole-refused]` under `LILSCRIPT_PEEPHOLE_TRACE` names why a terminal fold is refused. Two findings in [012](012-second-look.md) §"Found while batching": the chain is adopted whole (on markedlil the joined text made the whole chain a loss and the emission shipped unfolded, +6, until the ports took the shapes themselves), and a print decision read from the thread-local at render time is a wrong program under the pool's re-prints (live-16, two case-lanes, bisected by the switch in one run; every shape now lives on the tree). The for-init hoist ships off: with the search on markedlil read 9,316 without it and 9,392 with it while the text fold still runs | markedlil search off: 10,008 → **10,006** (9,965 with `LILSCRIPT_PORTS=for_init`: the hoist is −41 with the search off and +76 with it on, the ranking's lottery, so the fleet-facing number decides), text rewrites 607 → 129 (98 with the hoist); jquerylil search off 31,257 → 31,111; pool, search on: markedlil 9,364 → **9,316**, zodlil 32,391 identical (it ships without the peephole; the join alone read −33 there when applied unconditionally, a knob for later); cases shipped 7,513 → 7,512, none 11,405 → 11,438, zodlike 7,463 → 7,456 (the none lane runs the peephole too; its +33 is the probe and small files, each within the rename floor); census on the cases: continue tails 190 → 0, early exits 199 → 0, trailing increments 244 → 0, return tails 170 → 0, arrow guard returns 62 → 0, assignment guards 59 → 0, loop-body braces 619 → 58, keyword spaces and ASI semicolons 57 → 0, the for-init fold still 443 (its port ships off); tests 1621 (1719 before the deletions); pool 292/292; probe 21/22 (live-9). jquerylil search on, this host, 4 threads, with 7.24's accounting fix: 1,493 s wall / 28,332 (baseline 1,361 s / 28,649), emit 746 CPU-s over 815 emissions (975 before the fix, 726 over 901 at baseline), the peephole 3,233 CPU-s over 630 runs — the text layer is two thirds of the CPU with the search on; that is the next lever |

| 7.26 | **Batch 4: the residue the probe named.** A module-level counter is written without a bind, so `W=W+1|0` is `W++` for an unbound binding and for an identifier atom too; the self-assignment chain merges through the `|0` (`x=E;x=(x+B)|0` is `x=(E+B)|0`); a lone `if`, a non-`do` loop, and a `var` stand braceless (the dangling `else` is still `block_can_absorb_else`'s check), and the port drops braces where the fold ran whatever `braceless_control_bodies` said, as the fold did; `(a+b|0)+c|0` is `a+b+c|0` for additive chains on the left operand (G6's grouped-plus rule; int32 operands sum exactly and wrap once); an arm sequence merges its chain after the twin check. The braceless rule keeps the old fallback for a loop whose body is braceless, which three emitter tests pin without the policy | markedlil search off 10,006 → **9,971**; jquerylil search off 31,111 → 31,105; pool: markedlil 9,316 → 9,325 (noise), zodlil identical; cases shipped 7,513 → **7,490** (1 smaller, 0 larger), none 11,440 → 11,409, zodlike 7,456 → 7,443; census: control braces 228 → 0, loop-body braces 58 → 0, self-assignment chains 184 → 0, unit updates 336 → 28, int32 coercions 468 → 291; tests 1621; pool 292/292; probe **22/22** (b6 read 22 of 22 — level15's wrong output is gone, cause not identified, live-9 stays listed until it is) |

| 7.27 | **Batch 5: the last search-off residue on the cases**, each a one-site shape: an unread declaration whose value is a literal is pruned (G7, `remove_unused_standalone_vars`), in closures too — when a closure's tree changes after its text was rendered, the identity re-spell prints the module again from the trees; `b=b+1` inside a sequence is `b++`. Neither reaches its site: the dead test's name safety net (a nested parameter spelled the same) keeps the module-level `let a=null`, and the closure's `let b=2` is a text the prune sees after the render. Recorded, not chased: the search-off residue is now one site per case and the text fold still runs there | cases all three lanes identical (shipped 7,490 = 7,490); markedlil search off 9,971 identical; ports identical; tests 1621; pool 292/292; probe 22/22 |

| 7.28 | **The fleet, and where the text layer runs.** A 27-port census on 7.27 (`fleet-census-sum.py`): 23 built, katexlil **fails** ("startup limits rejected every candidate" — every finalist dropped; bisected by `LILSCRIPT_SKIP_PORTS` to the comma join, open below), remarklil +480 (the chain-adoption flip: skipping any of three ports recovers ~500, so the residual folds lose on the joined text; per-fold bisect running), remark-gfm −170, rehype −67, remark-parse −69, markedlil −39, jquerylil +59, 19 ports +88 net. The early-exit and continue-tail guards now rewrite one level as the text fold did (its guard had to sit directly in the function or loop body); the recursion had nested katexlil's guard chains, and the one-level rule alone did not un-fail katexlil. `javascript.peephole_scope = "terminal"` (7.27's knob), measured: markedlil 9,325 → 9,298 with the text layer 10.2 → 1.2 CPU-s (74 → 11 runs); jquerylil 28,465 → 28,600 (+135) with wall 554 → 149 s and the text layer 1,556 → 15 CPU-s (746 → 12 runs, 716 of them the leaf preparation). `javascript.peephole_plans` caps the plans admitted to leaf preparation for the middle ground; the 27-port arm B is queued behind the bisects. Seventeen folds are idle on every built port and 10 of them on every case lane too, ready to delete once katexlil builds again (three are class folds) | cases: shipped 7,490 → 7,489, none and zodlike identical; tests 1621; pool 292/292; probe 22/22; **open: katexlil does not build, remarklil +480** |

| 7.29 | **katexlil builds again: the printer and the braceless rule read one predicate.** The printer refused to join a statement whose text began with a quote (its directive guard), the braceless rule counted the same statement as joinable, so a `for` body printed braceless with a `;` inside and its tail leaked into the `else` — every candidate failed the standards parser and the error said "startup limits". Now `statement_opens_sequence` (only a bare string-literal statement refuses to open a sequence) and `emitted_joins_next` are the one predicate both sides read; a run prints braceless only when every consecutive pair joins; a unit test pins it; the empty-finalist error names the parser's refusal. Tried and reverted in the same batch: a codec-verified terminal chain (each fold kept only when Brotli does not grow) — the folds depend on each other (remarklil produced `Cannot assign to this expression` when a preparatory fold was refused), markedlil grew 9,962 → 9,997 because locally losing folds enable later wins, jquerylil paid 116 codec calls for the same bytes. The chain is a pipeline, not a set; a fold that loses where it fires is retired, not gated: remarklil's bisect names `fold_dead_pure_identifier_assigns` (skip: 37,698 → 37,077) and `fold_self_receiver_calls` (37,250), both active on two ports, measured on the other next | katexlil (pool, search on) 64,971 → **64,878**; markedlil 9,364 → **9,299**; zodlil identical; remarklil 37,698 (open, the two folds); katexlil search off 58,645; cases shipped −1, none and zodlike identical; tests 1622; pool 292/292; probe 22/22 |

| 7.30 | **The text layer folds only what ships; fourteen folds deleted; the tail guards recurse again.** Fleet A/B on 7.29, 27 ports, both arms on one binary: `peephole_scope = "terminal"` reads **−188 over 20 ports** against `"all"` (jquerylil −56, rehype −47, posthog −36, remark-math −28; markedlil +4, mdast-util-to-hast +3, mobx +1; twelve identical) with the text layer's runs gone from the exploration (jquerylil 746 → 12 runs, 1,556 → 15 CPU-s; markedlil 74 → 11), so it is the default (refs §K: every competitor folds once, on what ships). Fourteen folds idle on every built port in two fleet censuses and on every case lane are deleted with their registrations and tests (`fleet-census-sum.py`, `delete-folds.py`): drop_pure_regex_expression_statements, drop_redundant_class_constructor_guards, fold_empty_ternary_then_comma, fold_expression_branches, fold_inverted_continue_tail_guards, fold_not_gt_zero_length, fold_same_lvalue_ternary, fold_value_binding_iife, hoist_async_arrow_method_bodies, rotate_proven_initial_true_loops and the standalone registrations of four class helpers a live composite still calls (fold_named_class_identity, fold_or_empty_object_assign, rewrite_class_ctor_identity_to_new_target, terminate_bare_prototype_before_statement); three late-cleanup passes go with them. 7.28's one-level tail guards were a wrong diagnosis (katexlil was the join predicate, 7.29) and cost bytes broadly (remark-gfm's bisect: +64 without early exits; b9 → b11 +182 there, +98 on jquerylil), so the guards recurse into the tail they make again. Also measured: fleet compile time 7.22 → 7.29, scope all, 24 ports: wall 827 → 674 s, text layer 1,766 → 1,353 CPU-s, emit 1,149 → 814, codec 581 → 469; the pool is repeatable (remarklil three runs byte-identical on one worker; one earlier 37,190 stays unexplained); `fold_self_receiver_calls` skipped on its 11 ports reads −450 (remarklil −441 — the adoption flip — rehype −47; three ports +21..+27) and `fold_dead_pure_identifier_assigns` +31 on mobxlil, both re-measured under the terminal scope before either is retired | cases (search on): shipped 7,489 → 7,501, none 11,411 → 11,461, zodlike 7,443 → 7,465 — the tiny files miss the exploratory folds (with the scope forced to `all` the batch is 7,490 → 7,489, none identical); tests 1607 (the deleted folds' tests gone); pool 292/292; probe 22/22. **Fleet, 7.29 → this, both terminal: +1,099 over 20 ports, all of it katexlil +1,004, posthog +69, unified +29, remark-math +18 — and with the fourteen folds only *skipped* by env katexlil reads its old 64,878: the deletions are byte-neutral, the work-unit ledger is not (012, fixed in 7.31).** The guard ports on the fleet: without early exits and continue tails 20 ports read +159 (remark-parse +99, remark-gfm +50, remark-rehype +49, rehype +33, jquerylil +29), so they stay. `fold_self_receiver_calls` skipped under the terminal scope: −377 over its 11 ports (remarklil −419, micromark −9; three ports +2..+27) — deleted next. **End state against `54e1948`, 20 ports, this binary: bytes +947 (jquerylil −276, rehype −260, markedlil −167, hast −122, remark-parse −116, zodlil −98; katexlil +975, remarklil +617, micromark +210, mobx +148, posthog +136), compile wall 744 → 340 s, the text layer 1,320 → 48 CPU-s, emit 958 → 880, codec 526 → 446.** And posthoglil is not deterministic since 7′ (012) |

| 7.31 | **The ledger charges proposals, not attempts; the self-receiver fold is deleted.** In the late cleanup a work unit was reserved for every (pass, candidate) attempt before the pass ran, so idle passes spent the budget the naming families needed after them, and deleting three of them (7.30) moved katexlil +1,004. The unit is now reserved when a pass yields a proposal that passes analysis and admission, just before the codec scores it; a fold's deletion no longer moves a byte by itself. `fold_self_receiver_calls` (−377 over its 11 ports when skipped) is deleted with its registration and tests | the ledger alone does not move katexlil back (25 cleanup runs instead of 7, the same 65,882): the old 64,878 was idle passes happening to starve the families after them, a budget lottery that 7e must re-derive; with the self-receiver fold gone remarklil 37,698 → 37,279 and posthog −69 (9 ports −497); tests 1607; pool 292/292; the determinism bisect runs (`det-bisect.sh`, posthoglil three times at 4 threads and once at 1) |

| 7.32 | **Conditional shapes in the constructor; return tails over text conditions.** `c?!0:!1` is `!!c` and `c?!1:!0` is `!c` (`fold_boolean_conditional_values`), `a?a:b` is `a||b` when the test is the same name read again (`fold_ident_ternary_to_or`), and `!c?a:b` as `c?b:a` (`fold_negated_conditional_arms`) is written but ships off: −32 on katexlil and −7 on markedlil, +36 on micromark with the search off, so the text fold keeps making the swap where it pays. A return tail whose `if` carries no condition tree folds over the text as a raw node, grouped when the text has a top-level `?`, `,` or assignment (micromark: 257 residue sites, mostly closures whose tests are calls, now 0). Also found: the join residue left in micromark's closures is gone; in mobx a bare `var b;` hoisted between two member stores keeps them apart (G7, the declarator belongs at the head) | search off: katexlil 58,645 → 58,592 (`ident_or` alone −44), micromark identical, markedlil identical, mobx +5; cases shipped 7,490 → 7,482, none 11,409 → 11,392, zodlike 7,443 → 7,451, behaviour 0; tests 1607; pool 292/292; probe 21/22 — level15, and b19 and b22 emit the same 3,725-byte level15 program, so the flip is the search's non-determinism reaching the harness (live-9 stays open until the bisect lands). **Pool, ten heaviest ports against `54e1948`: −19 net** (rehype −311, jquerylil −230, markedlil −167; micromark +201, mobx +144, posthog +123, katexlil +86, remarklil +70, remark-gfm +55, unified +10); against 7.30's fleet −1,450 (katexlil −889, remarklil −547) |

| 7.33 | **The search is deterministic again.** `git bisect run` (posthoglil compiled three times at 4 threads and once at 1, byte identity as the oracle) names 7.20 — the shared spelling re-print cache: a plan that differed only in a printer axis re-printed whichever sibling's tree had reached the cache first, and for some shape the re-print is not the fresh emission, so the text depended on thread order (posthoglil 5,622 / 5,677 / 5,659 / 5,650 / 5,673 at 1/2/4/8/16 threads, a 4-thread repeat differing; `54e1948` byte-stable). Now every plan under a key re-prints the *canonical* spelling's tree (`canonical_options`: the printer axes at their canonical spelling, the naming policies too under `reprint_names`); when the canonical plan has not been emitted the derived plan emits it first — a plan the search seeds anyway — and re-prints it. The twin's per-shape mismatch that made the order matter is still there to find (`LILSCRIPT_PRINT_TWIN` on posthoglil), but it no longer decides anything | posthoglil 5,647 at 4/4/4/1/8 threads, byte-identical, and two pool builds of posthoglil and remarklil byte-identical; tests 1607; pool 292/292; cases shipped +1, none −2, zodlike +2, behaviour 0; probe 21/22 — level15 now *reproducibly* wrong, so live-9 is finally traceable (the pre-batch 7.24 reads the same 21; with the search off it is correct; a late-cleanup pass inlines the counting helper into the right operand of `\|\|` and loses its increment); **pool, ten heaviest ports against `54e1948`: −130 net** (jquerylil −446, rehype −311, markedlil −168; micromark +201, mobx +189, posthog +104, katexlil +86, remark-gfm +80, remarklil +70, unified +65), against 7.32 −111 (jquerylil −216: the canonical base re-prints better trees there) |
| 7.34 | **The single-use collapse on the tree, and what a pre-scoring rewrite does to the search.** G4/G5 as tree work (`collapse_single_uses`): a binding read once, written once, spelled in no text the tree does not own, read in the statement after it, is replaced by its value at the read and dropped -- a literal anywhere, a name never written again anywhere, or any value when the read is the first thing the next statement evaluates (Terser `collapse_vars`, Oxc `substitute_single_use_symbol`, refs §J). Also: the re-print cache's accounting raced (a sibling arriving during the canonical emission emitted it again and the count depended on thread order; `compiler_resource_counts_preserve_exact_selected_javascript` failed 4/4 on b24) -- one slot per key now, held while the canonical tree is emitted, one attempt counted per request; and the gzip coalescing test asserts the property (the search never pays for coalescing) rather than the shape, since the collapse ties `if(G)G=G.x` with `H=G?G.x:G` | search off: micromark 29,421 → 29,389, mobx 21,031 → 20,997, katexlil 58,592 → 58,585, remark 39,323 → 39,332, markedlil 9,962 (0 collapses); cases shipped/none identical, zodlike −12, behaviour 0; tests 1607; pool 292/292; probe 21/22 (live-9). **Fleet A/B on one binary, collapse on against off, 20 measurable ports: +128 net** (mdast-from-markdown −66, katexlil −64, micromark −39, remark-parse −31; jquerylil +119, remarklil +103, rehype +61, remark-gfm +39; eight ties) -- the same ten-port numbers as b23→b24 to the byte, so the search is deterministic and the loss is real: a rewrite on every emission moves which plan wins, and the plan it moves to prints worse than the incumbent did (remarklil's winner lost the exclusive-closure inlining, the `void 0` drop and the regex spelling together). **Ships off** (`LILSCRIPT_PORTS=single_use_collapse`), and becomes a terminal challenger in 7.36 |
| 7.35 | **G7 on the tree: dead `void 0`, dead stores, adjacent declarations; the collapse over declarator lists.** `void 0` is a pure literal to the prune, so an unreferenced `var x=void 0` goes with its name -- katexlil's phi-copy lists (`var ZW=void 0,_W=void 0,…`, 5,924 bytes in one statement) fall to the referenced few; `var x=void 0` prints as `var x` when the store is dead (first write of the bind in program order, no closure writes it, not in a loop of its function: the census now carries the write scope, a loop depth and the first-write set), `let x=void 0` always (`fold_void_initializers_off_fresh_vars`); adjacent declarations under one keyword merge again on the finished block (`merge_block_declarations`, the push-time merge factored into `declaration_parts`) -- it fires 0 times on four ports, the push already covers it; the collapse reads a declarator's one use in the declarator after it (`var a=f(),b=a.x` → `var b=f().x`) and a list's last declarator into the next statement; the name case of the collapse is sound now (a name is only substituted when its one write is its definition -- the old check looked for a direct write in the next expression and missed a closure's) | search off, b25 → b26 (collapse on → off, plus G7): katexlil 57,799 → 57,317 (−482, raw −6,659), micromark +15, mobx +21, remark +39 (the collapse's wins reverting; void drops 7/4/23/30); cases shipped/none identical, zodlike +12 (the collapse's one case); tests 1607; pool 292/292; probe 21/22. **Pool, ten heaviest ports, b26 against the collapse-off arm: +88 net** (remark-gfm −131, katexlil −32, micromark −29; remarklil +164, unified +55, mobx +30, rehype +26) -- with the search on the terminal chain had already stripped the dead `void 0`, so the tree's gain is compile-order, not bytes; **against `54e1948`: −42 net** (jquerylil −441, rehype −285, markedlil −168, remark-gfm −51; remarklil +234, mobx +219, micromark +172, unified +120, posthog +104, katexlil +54) |
| 7.36 | **Terminal shape challengers: the off-for-cause tree shapes ship in the codec-verified slot.** A rewrite on every emission moves the search's plan choice (7.34); the text folds it replaces never did, because they ran on the finalist and were scored there. So `IrJsOptions` carries `single_use_collapse`, the policy reads it, and the terminal stage re-emits the winner of the naming and pooling families with it on (`terminal_shape_options`, one emission, the declaration ladder, admitted by `finalized_javascript_candidate_precedes` like those two); `javascript.terminal_shape_challengers` / `LILSCRIPT_TERMINAL_SHAPES` gate it, and the decisions report `terminal_shape_challengers` / `_selected` / `_incumbent_bytes` / `_best_bytes`. Also the test initializers of the selected candidate (three) | cases: shipped and zodlike identical, none 11,389 → 11,353 (three cases smaller, the challenger's first wins, behaviour same); tests 1607; pool 292/292; probe 21/22. **Pool, ten heaviest ports against 7.35: byte-identical, wall 361.2 → 360.6 s** -- with the search on the terminal ledger is spent before the shape stage is reached (`terminal_codec_probe_limit_reached` on every port), so no challenger was emitted there; 7.37 reserves its slice |
| 7.37 | **The shape family gets its own ledgers, and the fresh-emission challenger is measured and rejected.** The terminal codec ledger was spent before the shape stage on every port and the plan slots (the candidate limit less the finalists) were gone after the naming and pooling families, so 7.36 never offered a challenger with the search on. Now a slice of the codec ledger is held back for the shapes (`release_shape_reserve_once`) and the family is granted its own plan slots (`TerminalJavaScriptCandidateBudget::grant`); the options `hoist_for_initializers` and `negated_conditional_arms` join `single_use_collapse` on `IrJsOptions` so the two other off-for-cause shapes can be offered the same way. With that, markedlil offered four challengers -- and the best was 101 bytes *worse* than the incumbent, then 97 with the stage moved ahead of naming, then 71 with the ledger extended by the finishing's own cost; the collapse forced on every emission gives the identical 9,178, so the shape did nothing and the loss was the finishing a fresh emission never has (the search's tree, the rename family's spellings, the late cleanup). Also: `codec_scoring_selects_a_better_function_layout_without_raw_growth` asserts no raw growth rather than raw identity, since a challenger may now shave bytes after the layout is chosen | cases: shipped +1, zodlike −6; tests 1607; pool 292/292; probe 21/22; ten pool ports against 7.36: nine identical, markedlil +11 (the reserve moving the cleanup lottery), wall 360.6 → 364.1 s |
| 7.38 | **The challenger is a print of the finalist's tree; the ledger it exposed; a text fold that shipped invalid JavaScript.** `frozen_tree` fetches the finalist's canonical tree from the re-print cache (emitted on demand when the search never printed it) and `FrozenModuleTree::reprint_collapsed` applies `ModuleTree::collapse` -- the single-use collapse, the dead-store drop and the declaration merge over the module block and every closure body, one census over all of them -- and prints it exactly as the plan prints (the rename pass first when the plan re-spells its names). The text goes through the gate the naming and pooling families use (`score_terminal_javascript_challenger_texts`, the standards parser first). A two-stage gate compares before finishing, where the two sides are on equal terms, and finishes the one challenger that wins raw on the incumbent's finishing allowance (`extend`). markedlil: one offered and selected, 9,189 → 9,180. On the ports the collapse rarely beats the finalist's tree (unified −2, the rest identical), and the stage costs no wall time. **Found on the way:** the probe at level 15 stopped compiling -- `fold_expression_suffix_returns` (a terminal local pass, 0 activations on the cases) folded a parenless `catch{..}finally{..}` into the `return` after it, `try{..}return catch{..},n`, and the cleanup families' admission (`validate_inner`) never ran the standards parser, so it shipped; the pass is deleted with its `ExpressionSuffixReturns` variant and three keyword-space tests, and the shaped cleanup families now parse every candidate they push. Also the explain line `terminal shape challengers`, `LILSCRIPT_SHAPE_DUMP=<path>`, and the two helpers the refactor left dead | cases: shipped identical, none +16 (four cases: the deleted pass's `e;return v` → `return e,v` in the unoptimized lane -- the tree's return tails do not write that shape yet), zodlike −5; tests 1604 (three of the deleted pass's removed); pool 292/292; probe 21/22 (level 15 compiles; live-9 stays); **pool, ten heaviest ports against 7.36: nine identical, markedlil +13 (the reserve), wall 360.6 → 362.0 s; against `54e1948`: −29 net** (jquerylil −441, rehype −285, markedlil −155, remark-gfm −51; remarklil +234, mobx +219, micromark +172, unified +120, posthog +104, katexlil +54) |
| 7.39 | **All three off-for-cause shapes as prints of the finalist's tree, judged print against print.** `TreeShapes` and `ModuleTree::reshape`: the collapse (with a beta reduction of concise closures called where they are built, `reduce_immediate_call`), the for-init hoist (`hoist_for_initializers_in_block`, the push-time hoist run over a finished block) and the negated-arm swap (`swap_negated_arms`, over `rewrite_block_expressions`, a bottom-up expression rewrite walk every later shape can use) applied to the thawed tree and its closure bodies, four challengers (each alone, all three). The gate compares the shaped print with the *unshaped* print of the same tree, one codec probe: a print of the tree is not the emission byte for byte (7.33 knew; markedlil 9,471 against 9,484 before this), so the fresh emission is the wrong incumbent. Found: every concise closure reaches `closure_node` as raw text (`render_closure_body` never registers a tree for it), so the beta reduction sees no `Closure` callee -- 0 reductions everywhere; registering concise closures as trees is the prerequisite for G4/G5's IIFE folds (104 + 31 + 61 on the cases). Also: the shape stage's finishing does not count as a structural candidate, and the two-slot inlining test runs with the shape stage off | markedlil search-on 9,178 → 9,169 (one offered, selected, 9,182 → 9,169 after finishing); level 15 on the probe 3,712 (four offered, selected); cases shipped 7,483 → 7,480 (four smaller, one larger), none −3, zodlike −5; probe 21/22; pool 292/292; tests 1604; **pool, ten heaviest ports against 7.38: −22** (remark-gfm −28, markedlil −2, posthog +8, seven identical), **against `54e1948`: −51 net**; wall 362.0 → 369.7 s (+2%: four prints and, where the gate passes, a second finishing -- remarklil +5 s, mobx +3 s) |
| 7.40 | **Concise closures have trees.** `render_prepared_inlined_closure`'s deferred path (a single-block closure whose instructions all defer into its return) rendered the function node straight to text; through `render_closure_statement` now, so the closure is registered with its head pieces and its body as an expression node, `closure_node` finds it, and the value is a `Closure` node -- the collapse's census sees its reads by bind instead of as text, the rename pass can look inside, and the beta reduction of a call on it fires: with the text folds off, `(a=>a*7|0)(6)` is `6*7|0` on the four cases that had it (30 → 22, 30 → 22, 101 → 95, 297 → 295 bytes, the shape challenger selected each time) | cases shipped 7,480 → 7,483 (two larger: the closure body now takes the emitter's terminal-semicolon option and the closure shapes), none identical, zodlike −2; tests 1604; pool 292/292; **probe 22/22 -- live-9 no longer reproduces** (the level-15 text differs; the late-cleanup pass that produced it is still there, so live-9 stays listed until that pass is gone); ten pool ports byte-identical to 7.39, wall +0.6%. The fold census doubled across the board (`fold_expression_self_assignments` 69 → 150) because the shape stage's finishing runs the cleanup a second time on each case: residue is measured with `LILSCRIPT_TERMINAL_SHAPES=0` from here on |
| 7.41 | **G4/G5's last two folds as prints: the single-use function moves to its call, the classic return-only IIFE reduces.** `inline_single_use_functions`: a `function f(..){..}` declared in the module block, read exactly once and that once as the callee of a call in the module block's own statements (never below a function or class, where a call-site local could capture a name the body reads -- the text fold checked every enclosing scope for that; the tree restricts the site instead), with no closure inside and no `arguments`/`super`, becomes a function expression at the call (registered as a closure, so the print and the renamer see its tree) and the declaration goes (`fold_single_use_function_expressions`). `reduce_immediate_call` also reduces `(function(){return v})()` to `v` when `v` reads neither `this` nor `arguments` (`fold_zero_argument_return_iife`). Both in the collapse family of the shape print, judged print against print | with the text folds off: the cases reduce 10 IIFEs and move 2 functions (`live12_braced_try_in_if`, 101 → 96, same output); cases shipped 7,483 → 7,479 (two smaller), none −10, zodlike identical; tests 1604; pool 292/292; probe 22/22; **ten pool ports against 7.40: −8** (posthoglil −8, the move; nine identical), against `54e1948` −59; wall 371.8 → 369.9 s |
| 7.42 | **Phase 5.5: the folds that fire nowhere.** With the shapes off (so the finishing is counted once), the 73 cases in both lanes and all 24 fleet ports under `LILSCRIPT_FOLD_REPORT=all` name 96 folds that fire somewhere; against the 92 `session.run` registrations, nine never fire: `fold_arguments_slice_to_rest`, `fold_dead_identifier_copy_declarators`, `fold_empty_comma_operators`, `fold_nullish_index_walks`, `fold_omissible_trailing_false_args`, `fold_redundant_null_undefined_or` (and its `RedundantNullUndefinedOr` cleanup pass, `ALL` 19 → 18), `fold_undefined_defaults_into_formals`, `fold_void_prefix_updates`, `strip_stale_set_prototype_of`. Seven are gone with their tests; two stay as functions because the compiler calls them directly on the repair and canonical paths, which the report never sees (`fold_dead_identifier_copy_declarators`, `fold_redundant_null_undefined_or`), and `fold_empty_comma_operators` stays because a class fold calls it as a helper -- only their pass registrations went. `flip_false_equalities` fired on one small port and stays. Tools: `$SP/idle-folds.sh`, `$SP/delete-batch.py` (registrations, use lists, functions, tests; the cleanup-pass variant by hand) | cases: all three lanes byte-identical; tests 1594 (ten tests of the deleted folds removed); pool 292/292; probe 22/22; ten pool ports byte-identical to 7.41, wall 369.9 → 367.4 s (the peephole 52.5 → 49.6 s); text layer 30,020 → 29,827 lines |
| 7.43 | **The collapse on every emission, re-measured with closures as trees** (no code; `LILSCRIPT_PORTS=single_use_collapse` on 7.42's binary) | ten pool ports: remarklil −103, remark-gfm −73, mobx −67, jquerylil −42, rehype −7, posthog −6, markedlil and unified identical, micromark +12, **katexlil +736**; net +450, −286 without katexlil. Search-off katexlil with the collapse is +5 Brotli on −134 raw (19 collapses), so the +736 is the terminal ledger's plan lottery (7e), not the shape. Stays off by the rule; with the print challenger already on the finalist, the nine-port win is the plan choice the everywhere collapse steers to -- what 7e/7′ must make deterministic before the default can flip. Wall 367 → 379 s with it on |
| 7.44 | **7e, first measurement: katexlil's loss under the collapse is the ledger's.** `LILSCRIPT_TERMINAL_PROBES=<n>` pins the terminal codec ledger for an A/B (ahead of the configured value and the artifact scaling; no behaviour change without it). katexlil on the pool: the incumbent path is byte-identical at 256 and 384 probes (64,961); the collapse-on-every-emission path is +736 at 256 and **−68 at 384**. So the everywhere collapse wins on all ten heaviest ports once the ledger is not the binding constraint, and 7.34's +128 and 7.43's +450 were the ledger deciding katexlil, not the shape. The explain on katexlil: `terminal work 258/256 (exhausted)`, `0 shape challengers offered` (its reserve released into a ledger already spent by the naming family and the cleanup), `rename_won_sum` 1,149 and `cleanup_shaped_pushed_sum` 1,095 -- the finishing is worth ~2 KB there and gets ~250 probes for it. The re-derivation is now concrete: the ledger must be sized by what the families are worth on the artifact (the finishing's marginal bytes per probe), not by raw size alone | tests unchanged (the override is env-only) |
| 7.45 | **7e's first cut, and the collapse ships on every emission.** At level 13 and above the terminal codec ledger is the level's base (384) whatever the artifact's size; the artifact scaling (a quarter at 64 KB, a twelfth at 256 KB) stays below 13. With that, `single_use_collapse` is `port()` -- on by default, `LILSCRIPT_SKIP_PORTS=single_use_collapse` off -- since the everywhere collapse is a measured win once the ledger is not the binding constraint (7.44). `docs/configuration.md` says so | **Fleet A/B on one binary, 20 measurable ports, the two defaults against 7.42's: −660 net** (remarklil −253, mdast-from-markdown −126, remark-parse −125, remark-gfm −73, katexlil −68, rehype −61, rehype-stringify −36, mobx −29, posthog −10, remark-math −4; jquerylil +96, micromark +12, mdast-to-hast +11, markedlil +5, remark-rehype +1; five identical), **against `54e1948`: −1,096** (the day started at −436 there); CPU on emission plus codec 1,268 → 1,382 s (+9%, all of it codec probes); VERIFY_B43 |
| 7.45 | **7e's first cut: the ledger unscaled at level 13; the collapse stays off, and live-9's root cause is found.** At level 13 and above the terminal codec ledger is the level's base (384) whatever the artifact's size; the artifact scaling (a quarter at 64 KB, a twelfth at 256 KB) stays below 13; an explicit `terminal_codec_probe_limit` still wins (katexlil pins 256, jquerylil 1,536 -- which is why 7.44's env-pinned arm was not this rule: it overrode both). `docs/configuration.md` says so, the two config tests check the rule and the curve below it. The collapse on every emission was flipped on and flipped back: the fleet arm read −660 over 20 ports, but on the probe at level 15 it makes the plan win in which the emitter inlines the pure helper into a short-circuit's right operand and drops the prefix statement -- `f=X(b)\|\|(W=W+1\|0,X(b+1\|0))` becomes `f=0==(b&1)\|\|0==(b+1&1)`, the block traced *before* the collapse runs already holds the wrong text (`[collapse-block]` under `LILSCRIPT_SHAPE_TRACE`) -- **live-9's root cause, an emission-time miscompile in the phi-region rendering that the collapse only exposes**; no text fold is involved (every `LILSCRIPT_SKIP_FOLDS` leaves it wrong, `LILSCRIPT_SKIP_PORTS=single_use_collapse` makes it right). Fix that rendering, then flip the collapse. Also `LILSCRIPT_SHAPE_TRACE=1` traces the shape prints, the beta reduction's refusals, the function move, and the collapse's substitutions | ledger rule alone, ten pool ports against 7.42: **−130** (remarklil −138, markedlil +5, remark-gfm +4, seven identical), against `54e1948` −189; wall 367 → 456 s (+24%, all of it codec probes on the ports without an explicit ledger); cases: all three lanes byte-identical (the cases are under 16 KB, so the ledger is unchanged there); tests 1594; pool 292/292; probe 22/22 |
| 7.46 | **live-9 reproduced search-off; the collapse was only its detector.** `LILSCRIPT_EMISSION_DUMP=<dir>` writes every emission's text and options (and `[ir-context]`/`[ir-options]` lines name each IR context's passes and optimizer options); on the probe at level 15 with the everywhere collapse, 50 of 373 emissions carry the wrong `f=0==(b&1)\|\|0==(b+1&1)` and all of them belong to IR contexts 12 and 13 (13 a projection of 12), whatever their print options. Context 12's optimizer options against the root: `common_subexpression_elimination` off, `constant_parameter_specialization` off, inline limits 48/128/40 against 12/30/16. Search-off with those options reproduces it, collapse on or off, text folds on or off, level 13 or 15; the minimal trigger is CSE off with the aggressive limits (either alone is correct). `migration/tools/live9.toml` is the one-second reproduction. A ten-line extract of the helper and the short-circuit compiles correctly under the same options, so the probe's other uses of the counter and the helper are part of the trigger | no code path changed; the instruments are env-gated |
| 7.47 | **live-9 fixed at the root.** `LILSCRIPT_IR_DUMP=<path>` writes the first module emitted as the optimizer left it; on the failing variant `shortCircuit` still holds all five global stores and no calls, so the loss was the emitter's. In `render_expression_region_block_with_effect_count` an instruction without an output (the counter's `StoreGlobal`) is parked as a pending effect and glued as a comma prefix onto the next instruction's value in the expression cache -- and in every arm that next instruction is a `Const`, which consumers take from `inlined_values` by value, never from the cache: the prefix was written where nothing reads. The effect now waits for a value the consumers read through the cache (never a `Const`, never a value in `inlined_values`); a block that ends with it still pending is refused, as before. The failing configuration emits `f=0==(b&1)\|\|0==(W=W+1\|0,b+1&1)`, the increment inside the arm | live9.toml correct, collapse on and off; level 15 with the search and the collapse everywhere correct; **probe 22/22**; cases: shipped and none byte-identical, zodlike +3 (the probe: the arm keeps its increment); tests 1594; pool 292/292; ten pool ports byte-identical to 7.45's, wall unchanged |
| 7.48 | **The collapse on every emission, re-measured on the fleet after the fix** (no code) | 20 measurable ports, 7.47's defaults against the same binary with `LILSCRIPT_PORTS=single_use_collapse`: **+254 net** -- twelve wins (remarklil −115, mdast-from-markdown −80, mobx −77, remark-gfm −77, jquerylil −42, rehype-stringify −36, remark-parse −36, five under −10), six ties, micromark +12, **katexlil +736**. Without katexlil −482. katexlil pins `terminal_codec_probe_limit = 256` in its own config, so 7.45's ledger rule does not reach it and the plan lottery decides its artifact (7.44: −68 at 384). By the rule the default stays off; the port's ledger is the port's call (the release procedure's site measurement), and the collapse stays available as the terminal print and as `LILSCRIPT_PORTS=single_use_collapse`. **7.47's defaults against `54e1948`: −701 over 20 ports** (the day started at −436); CPU on emission plus codec 1,452 → 1,468 s with the collapse on |
| 7.49 | **7f, interim: the whole fleet on one worker, 7.47's defaults against `54e1948`** (no code) | 24 ports on one 16-core instance each, sequentially: wall from the first to the last build line **938 s → 891 s** (−5%); the sum of the ports' own walls 718 → 582 s (−19%); the text layer's CPU 1,265 → 63 s (−95%); emission CPU 874 → 862 s; codec probes 491 → 569 s (+16%, the ledger rule of 7.45); **Brotli −701 over the 20 measurable ports**. One port failed to build on the baseline binary only. The gate -- tests, no Brotli regression, compile time same or better -- holds at this point of the migration; the text layer is 95% gone from the clock and 29,827 lines on disk |
| 7.50 | **The late-cleanup ladder loses the five passes the tree carries.** `LateJavaScriptCleanupPass::ALL` is thirteen: `NegatedConditionalArms` (the shape print), `IdentTernaryToOr` (`ident_or` on every emission), `IdentityArrowIife` and `ZeroArgumentReturnIife` (the print's beta reduction) and `UnusedStandaloneVars` (the prune) are out of the codec-verified ladder; the variants and their functions stay for the canonical chain and the families that name them. Measured the long way: all eleven twins cut at once read +629 (jquerylil +496); the five control shapes restored brought jquerylil to −9; rehypelil's +106 came back only with `BooleanConditionalValues` in its early slot (18 minus it: +106; 18 minus any other: 0 or −23) -- the tree's `boolean_arms` sees construction-time literals, the text fold sees the printed `!0`/`!1`. `LILSCRIPT_CLEANUP_LADDER=Name,..` runs any ladder without a rebuild, `LILSCRIPT_CLEANUP_TWINS=1` the old eighteen (`LADDER_WITH_TWINS`); the env path reproduces 7.47 byte for byte | ten pool ports against 7.47: **+7 net** (remarklil −48, rehype −23, markedlil −6, katexlil −3; jquerylil +55, mobx +17, micromark +10, unified +5), against `54e1948` −182; **wall 458 → 447 s**; cases shipped +2, none identical, zodlike +2; tests 1594 (one expectation follows `a?a:b` leaving the ladder); pool 292/292; probe 22/22 |
| 7.51 | **The structural chain loses three twins and gets the standards parser.** The late cleanup's fixed chain (applied to each beam entry in up to four variants) drops `NegatedConditionalArms`, `BooleanConditionalValues` and `UnitCounterUpdates` -- the shape print, `boolean_arms` and `unit_updates` carry them -- each measured alone on the three heavy ports (jquerylil, rehypelil, remarklil): −18, 0, −49; `ConditionalReturnTails` stays (+17 without it). The chain's result is admitted through the standards parser now, like the shaped families and the shape prints since 7.38 (byte-identical on the three ports). `LILSCRIPT_CANONICAL_CHAIN=Name,..` keeps only the named passes in the chain for an A/B without a rebuild; with `LILSCRIPT_CLEANUP_LADDER` and `LILSCRIPT_SKIP_FOLDS` every text stage is measurable one rule at a time from the environment | ten pool ports against 7.50: **−53** (jquerylil −81, katexlil −17, markedlil −3; posthog +30, mobx +17), against `54e1948` −235; **wall 447 → 431 s** (emission CPU 838 → 794 s); cases shipped +1, none identical, zodlike +1; tests 1594; pool 292/292; probe 22/22 |
| 7.52 | **Twelve emission-chain folds with tree twins, measured one at a time; four go.** `LILSCRIPT_SKIP_FOLDS` on the three heavy ports (jquerylil, rehypelil, remarklil), each fold alone: `fold_negated_conditional_arms` 0, `fold_statement_assignments_into_first_use` 0, `fold_expression_self_assignments` 0, `merge_adjacent_declarations` −6, `fold_zero_argument_return_iife` 0 -- and the others still pay: `fold_prior_assign_into_for_init` +407, `fold_identifier_copies` +379, `fold_single_use_function_values` +142, `fold_boolean_conditional_values` +95, `fold_single_use_function_expressions` +64, `fold_identity_arrow_iife` +16, `fold_single_use_literal_bindings` +12. Deleted: the negated arms with its `NegatedConditionalArms` variant and three tests; the statement-assignment collapse with its variant and the structural chain's statement-assignment dimension (the chain has half the proposals it had); the self-assignment fold's sub-run in the `UnitCounterUpdates` arm; the declaration merge's registration. The zero-argument IIFE fold is the next tranche's first entry. What the numbers say about the ports: the for-init hoist and the identifier copies are worth 800 bytes on three ports *on the finished text* -- the tree's `for_init` shape and the collapse do the same rewrites at construction and the print offers them on the finalist, yet the text fold still finds 800 bytes; the text sees shapes the search's later stages create, which is exactly the residue 7′ must make a print of | ten pool ports against 7.51: **−28** (posthog −42, remarklil −19, markedlil −11, micromark −7; katexlil +45, jquerylil +10), against `54e1948` −263; wall 431 → 437 s; cases shipped 7,482 → 7,476 (five smaller), none +4, zodlike identical; tests 1587 (two tests of the deleted folds removed, three merge expectations now read the unmerged text the tree merges at push); pool 292/292; probe 22/22 |
| 7.53 | **Fourteen more emission-chain folds measured alone; four go.** On the three heavy ports: `fold_zero_argument_return_iife` 0, `fold_ident_ternary_to_or` 0, `fold_unit_counter_updates` 0, `fold_single_return_arrow_bodies` −14, `fold_continue_tail_guards` −26 (gone next) -- and paying: `fold_single_statement_control_braces` +465, `strip_unused_simple_declarators` +193, `fold_int32_coercions` +147, `remove_unused_standalone_vars` +119, `fold_conditional_return_tails` +77, `fold_returned_temporaries` +70, `join_adjacent_declarations` +33, `fold_top_level_adjacent_expression_statements` +11, `fold_early_exit_guards` +5. Deleted the first four with the `IdentTernaryToOr` and `ZeroArgumentReturnIife` variants (the `UnitCounterUpdates` arm keeps only the while-true bounds fold) and six tests that asserted their text; five expectations now read the tree's shape. 75 registered folds remain. **What the paying list says:** every fold that still pays has a tree twin that runs on every emission (`control_braces`, the prune, `return_tails`, `early_exits`, `comma_join`), so the bytes are shapes the *finishing* creates after emission -- the ladder, the chain, the text rename convergence -- on text the tree never sees again. That is Phase 7′'s definition, measured twice: jquerylil search-off (3.7 s, 91,543 bytes raw) fires `fold_single_statement_control_braces` 2 times, `fold_identifier_copies` 2, `fold_conditional_return_tails` 2, `strip_unused_simple_declarators` 2 -- against +465, +379, +77 and +193 with the search on. Make those three stages tree work and the residue has nothing to feed on | ten pool ports against 7.52: +24 (katexlil +40, jquerylil −14, two −1), against `54e1948` −239; wall 437 → 435 s; cases shipped −3, none +3, zodlike +1; tests 1582 (six tests of the deleted folds removed, five expectations now the tree's shape); pool 292/292; probe 22/22 |
| 7.54 | **7′, first stage: the rename convergence as a print.** `converge_local_names` re-parses the finished text to rebuild a binding resolution and reassigns each function scope's names by descending use; the tree's `Renamer` already knows every scope and bind, so `TreeShapes::converge` runs it (frequency order, over every scope) on the finalist's thawed tree before the print, alone and with the collapse, guarded on identifier mangling exactly like the search's rename re-prints (unguarded it renamed a readable build's exports: `can_mangle_public_esm_export_names`). Six shape prints now. **Measured (b52 vs b51, ten pool ports): −19** (jquerylil −15, mobxlil −13, remark-gfm +9), −258 against the baseline; the pool's wall 435 → 461 s and codec CPU 469 → 509 s are the added prints' probes. Tests 1,582; pool 292/292; lanes shipped −11, zodlike −12, none +4; probe 22/22. The text convergence still runs after the print (its retirement is the ladder's, next). |
| 7.55 | **7′, second stage: the print ladder.** The six fixed shape combinations become a ladder over the finalist's frozen tree: the unshaped print is scored once as the incumbent, then each rung adds one shape to the incumbent's set, reshapes, prints, scores, and is kept when lower -- eight rungs (`collapse`, `for_init`, `negated_arms`, `negated_equalities`, `return_tails`, `exit_guards`, `rebrace`, `converge`; `LILSCRIPT_SHAPE_LADDER=name,..` pins them), the winner finished on the incumbent's allowance as before. Four rungs are new: `!(a==b)` to `a!=b` on the tree (equality only; an ordering's inverse is not its negation under NaN); the return tails, early exits, continue tails and braces are the emitter's own `shape_block` (phase 6's port of the control folds, which the emission runs under its policy) run again post-hoc on the finished tree, plus the guard-suffix and branch forms `if(c)return a;E;return b` and `if(c){E;return a}return b` as one conditional, which `shape_block` lacked. The shapes now run under the print's installed statement policy (live-16's rule). `LILSCRIPT_LADDER_REPORT=1` prints every scored proposal of the text ladder and chain, the census of what the tree still leaves to the text. On markedlil the ladder keeps negated arms (−2), negated equalities (−3) and the braces (−1), refuses the exit guards (+10) and the rename (+50, the ±100 rename lottery measured pre-finishing while the text convergence still renames afterwards), and loses the finished gate by one byte (9,247 against 9,248). **Measured (b53 vs b52, ten pool ports): +70** (jquerylil +42, markedlil +26, mobxlil +11, remark-gfm −9): one greedy line, gated before the finishing, gives the gate one candidate where the six combinations gave it six. Tests 1,582; pool 292/292; lanes shipped +8, zodlike +7, none −4; **probe 21 of 22**: the raw configuration failed to compile -- the ladder's scorer runs admission and my `?` propagated its rejection of a converge print, and the print was right: the admission's `nested_function_end` did not take `function*`, so a module-level generator had no binding and its call sites were "unresolved" whenever another function had a local of the same name (fixed in 7.56 with a regression test; rejected prints are now dropped, not fatal). The census: after the ladder, the text passes still win on the ten ports (ConditionalReturnTails −4,743 over 251 wins, GuardReturnExpressionSuffixes −4,659, BooleanConditionalValues −4,329, NegatedEqualities −3,400, UnitCounterUpdates −1,433, SingleStatementControlBraces −874; the chain −3,412/−3,628), and with the text ladder and chain switched off by env the ten ports lose 869 (jquerylil 561) while the pool's wall drops 463 → 364 s. |
| 7.56 | **7′, third stage: the print beam inside the cleanup.** 7.55's line moved into `late_javascript_cleanup_finalists` as the cleanup's first family (`offer_print_beam`), on each finalist's cached frozen tree in the bridge call, before the letter remaps move the text away from the tree: the unshaped print enters the cleanup beam as its own member, each rung adds its shape to every member's set (`LILSCRIPT_PRINT_BEAM`, default 4 members), the print is scored, and the members join the cleanup beam where the text passes compete with them under the same codec gate; the post-finalize stage, its reserve arithmetic and `terminal_shape_ladder` are gone, the shape reserve is released before the finalize, the explain line reports the beam (`printed` lineage on `CleanupCandidate`). A finalist the search never printed gets no emission for it (`cached_frozen_tree`; three one-slot ledger tests counted the emission). `LILSCRIPT_TEXT_CONVERGE=0` switches the text rename convergence off for its A/B. **Measured (b54 vs b52, ten pool ports): −4** (katexlil −126, remark-gfm +33, posthog +32, markedlil +26, jquerylil +22), −74 against b53, −262 against the baseline; **the pool's wall 461 → 374 s and codec CPU 511 → 433 s** -- the beam is cheaper than the fixed combinations were. Tests 1,583; pool 292/292. The census after the beam: ConditionalReturnTails −2,744, BooleanConditionalValues −2,533, NegatedEqualities −1,960, GuardReturnExpressionSuffixes −1,713, SingleStatementControlBraces −799, UnitCounterUpdates −742, SameBindingStrictEquality −333, CanonicalLeafSyntax −295; the chain −974/−1,088. |
| 7.57 | **Strict equality is a node.** The `===`/`!==` intrinsics built raw text, so `!(a===b)` never flipped on the tree and was the text ladder's (NegatedEqualities, 207 wins after the beam): `JsExpressionRoot::StrictEquality { negated }` renders, negates (`negated_tree`), rebuilds, and the `negated_equalities` rung flips it; `same_binding_equality` is a new rung (`a===a` to `a==a`, the ladder's SameBindingStrictEquality). The census line now carries the first differing site of every scored proposal (`before ~> after`), the residue's own samples. **Measured (b55 vs b54, ten pool ports): −102** (jquerylil −76, posthog −33, remark −30, mobx −25, katexlil +62), −106 against b52 and −364 against the baseline; wall 374 → 380 s. Tests 1,583; pool 292/292; probe 22/22; lanes shipped +1, zodlike −7, none 0. The samples name the next gaps: the emitter builds `!` over an equality without flipping it (`!(g==null)`, `!(b===void 0)`, `!("object"!=typeof e)`), one-arm boolean conditionals are not folded (`c?true:x`, `c?false:x`), and the return-tail rung reports no change on markedlil where the text pass wins inside closure bodies. |
| 7.58 | **Three gaps the samples named.** (1) `JsExpression::unary("!", ..)` over an equality, a strict equality or an undefined test now builds the inverse comparison (`!(g==null)` is `g!=null`), so the text ladder's NegatedEqualities has nothing left; (2) `conditional()` folds one literal boolean arm under the `boolean_arms` policy (`c?!0:x` is `!!c||x`, `c?!1:x` is `!c&&x`, `c?x:!0` is `!c||x`, `c?x:!1` is `!!c&&x`; the coercion drops where the condition is boolean-valued), the ladder's BooleanConditionalValues; (3) the shape walker enters `declarator.function` bodies -- markedlil's finalist is 19 module statements, one of them hundreds of `let f=a=>{..}` declarators, so the return-tail rung had reached nothing (`[shape] reshape ..` now counts blocks, closures and guard+return pairs). The rung still refuses a suffix with a `var` declarator, a loop or an `if`, like the text fold. On markedlil the flip and the one-arm folds move the search's plan choice (a different record layout and pooling plan; +32, the lottery). **Measured (b56 vs b55, ten pool ports): +545** (jquerylil +501, rehypelil +88; katexlil −51). Bisected on b57 with the three changes as emission ports switched by env, no rebuild: the walker alone is byte-identical on all ten (the rung fires now, 6 prints scored on markedlil, none kept); the `!` flip alone **+289** (jquerylil +367, remarklil +89; mobx −128, unified −54) -- `!(x==null)` shares its bytes with the `x==null` tests around it; the one-arm folds alone **+232** (remark-gfm +138, jquerylil +129; unified −61, markedlil −30). Both emission ports are off by default (`LILSCRIPT_PORTS=negated_equalities,boolean_one_arm` turns them on); the codec-judged rungs keep the choice. Tests 1,583; pool 292/292; probe 22/22; lanes flat. |
| 7.59 | **Two more rungs and a quoted key.** `loop_bounds`: an infinite loop whose body begins with a unit update of a local and a break guard reading it once becomes the `for` head's test with a new `PrefixUpdate` node (`while(!0){x--;if(x<0)break;S}` is `for(;--x>=0;){S}`, the ladder's UnitCounterUpdates); `boolean_one_arm`: 7.58's one-arm folds as a print, rebuilt through `conditional()` under a policy with the fold on. `member_keys` (on): an index whose key is a quoted identifier prints as a member access (`o["text"]` is `o.text`, CanonicalLeafSyntax's win) -- first as a `Str`-only rule, which never fired (byte-identical on ten ports), then on any quoted leaf. `LILSCRIPT_SHAPE_RAW=1` lists the largest raw nodes; the reshape trace counts coverage (markedlil: 35 raw nodes, 700 of 33,835 bytes; micromark: 53 raw nodes, 13,149 of 94,837 bytes, 5 holding closures). **Measured (b58 vs b55, ten pool ports): +43** (jquerylil +61, remarklil +24, katexlil −39), −321 against the baseline, codec CPU 420 → 406 s. Tests 1,583; pool 292/292; probe 22/22; lanes shipped −3, zodlike +5. |
| 7.60 | **The private cluster IIFE is a closure tree.** `emit_clustered_root` flattened its helpers and the returned root into raw text (`var outer=(function(){helpers;return function..})()`), 12,787 of micromark's 94,837 bytes that no shape and no renamer could reach; it now builds the wrapper and the returned root as closure nodes, the named cluster's way. micromark's raw bytes: 13,149 → 489. Found by the coverage trace, not by a census. `LILSCRIPT_SKIP_PORTS=cluster_tree` keeps the text, for the A/B. |
| 7.61 | **Why the beam was dead on micromark, and the chain's handicap.** `LILSCRIPT_SHAPE_TRACE` at the bridge showed micromark's finalists with a cached tree and probes to spend, yet no rung: the unshaped print failed the admission ("unclassified static properties: parse") because the source calls `JSON.parse` on a constant, the emission chain's `fold_constant_json_parse` had turned that into an object literal on the text, and the direct source no longer had `.parse`; the emitter now builds the literal itself (`json_literals` port, on). With the beam alive, the second finding: the emission text costs 9,343 on markedlil where the unshaped print costs 9,528 -- every print starts behind by the emission chain's worth; the beam's members are now finished by that chain (`finish`) before they join the cleanup beam. Still ~80 behind: the finalist's text also had the search's declaration shapes (`var`) and text renames after its tree was frozen. |
| 7.62 | **The best print is a finalist.** A print cannot level the search's text finishing inside the bridge's beam, so it was cut before the remaps and convergence that would have; the beam's cheapest print is now carried as one more finalist whatever its rank (`LILSCRIPT_PRINT_FINALIST=0` off), gets the same remaps and cleanup, and the finished ranking decides -- 7.55's "finish the winner" done through the real finishing. **Measured (b62 vs b58, ten pool ports, 7.60–7.62 together): +192** (jquerylil +153, micromark +35, mobx +34, rehype +26; unified −55), −129 against the baseline; wall 377 → 419 s, codec CPU 406 → 433 s. Tests 1,584 (a `JSON.parse` regression test: the first cut took the constant's value and the generic call then spelled the register's name, jquerylil failed to build); probe 22/22; lanes shipped −1, none −24, zodlike −22; pool 291 of 292 with one shard's SSH timing out, not a case. Bisected by env next: the print finalist (`LILSCRIPT_PRINT_FINALIST=0`), the JSON literal (`json_literals`), the cluster tree (`cluster_tree`). |
| 7.63 | **The cluster tree named the renamer gap; parameters first.** 7.62's +192 bisected by env on the pool: the print finalist off +213 (remarklil loses the 21 it had bought), the JSON literal off +192, the members' finishing off +192, the cluster tree off **−16**. With the cluster owned, jquerylil ships a candidate with the tree renamer's names (`a,b,c` per scope, `Bc`/`Cc` globals, `let`) where the text path ships the convergence's (`e,t,r,n`, `var`): the search's rename re-print now renames the helpers too, wins the search's gate before the finishing, and finishes bigger. The cluster port is off by default (`LILSCRIPT_PORTS=cluster_tree`) until the tree's renamer matches the text convergence's; its first rule ported: parameters by position before the rest by descending use (`RenameScope::parameters`) -- on markedlil the converge print reads −20/−26 against the unshaped print where it read +47. The convergence's other rule, an alphabet ordered by the artifact's identifier bytes, loses on the tree (+4/+27 identifier bytes, +105 every byte; `LILSCRIPT_CONVERGE_ALPHABET`), so the plan's alphabet stays. **Measured (b64 vs b58, ten pool ports, cluster off): −16** (remarklil −20, jquerylil +4), −337 against the baseline; with the cluster on, +192 to the byte -- parameters-first did not touch the search's re-print path where that loss is made. The members' chain-finishing changed no artifact and doubled jquerylil's text-peephole CPU (13 → 30 s): off by default (`LILSCRIPT_PRINT_FINISH=1`). Tests 1,584; probe 22/22; lanes shipped +6, zodlike +4, none 0; pool 292/292. |
| 7.64 | **The declaration finishing as rungs.** The search's declaration scores spell four texts per emission: the module's leading keyword as `var` and as `let`, and each function's leading `var` list as `let` where the names are not parameters, the initializers are literals, no `var` follows and no function or class stands in the body (`top_level_declaration_variants`, `function_leading_declaration_variant`). The tree now offers both as rungs (`top_keyword`, `function_let`; `for_each_function_body` walks statement functions and declarator functions with their heads, the closure map's entries with theirs), so the print's `let` against the finalist's `var` -- one of the two finishing steps a print lacked (7.62) -- is the beam's choice. On markedlil both cost before the finishing (+42, +34) and the artifact ends +9. **Measured (b66 vs b64, ten pool ports): −15** (markedlil −8, jquerylil −4, mobx −3), −352 against the baseline; the pool's wall 413 → 381 s with the members' finishing off. Tests 1,584 with one pin moved: the effort-tier test's 55-byte artifact ships as `var m=n=>10+n|0;..` at 45, the top-keyword rung's find. Probe 22/22; lanes flat; pool 292/292. |
| 7.65 | **The chain's single-use inlining on the tree, into nested functions.** The finalist's own dump (`LILSCRIPT_SHAPE_DUMP`, one prefix per finalist) against its collapse print named the emission chain's remaining lead: the tree's `inline_single_use_functions` moved only `function f(){}` statements called from the same block, where markedlil's single-use functions are declarators (`let Rc=a=>..`) called from inside other functions. `inline_single_use_declarator_functions` (run once per reshape on the module block and the closure map's entries) moves a declarator function or a function statement read exactly once to that read, inside another function too, when no spelling the closure mentions -- its own parameters and locals excepted -- is declared by a frame on the way (`read_site`, `collect_declared_spellings`), and no arrow reading `this` moves into a function; a read inside a loop is allowed, as the chain always did (`LILSCRIPT_INLINE_LOOPS=0` refuses). With it, `x===void 0||null==x` is `x==null` at construction on a name (`null_test_of`), the chain's other unconditional spelling. On markedlil: 2,832 moves across the beam's prints, the collapse print 9,524 → 9,423 where the emission stands at 9,356, the carried print finishing 9,355 against the incumbent's 9,245 (225 behind at 7.62), the artifact 9,245 (b54: 9,247). **Measured (b67 vs b66, ten pool ports): −136** (unifiedlil −175, remark-gfm −52; rehypelil +53, mobx +15, markedlil +13, posthog +10), −488 against the baseline; the pool's wall 381 → 375 s, emit CPU 842 → 759 s. Tests 1,584; probe 22/22; lanes shipped 0, none 0, zodlike −9; pool 292/292. |
| 7.66 | **The collapse family in every function body; identifier copies; the `for` head as a tree.** The collapse family (copies, the single-use collapse, immediate calls, void initialisers, declaration merges) ran on the module block and the closure map's bodies only -- markedlil's hundreds of declarator-function bodies never saw it; it now runs in every function body below a block, innermost first (`for_each_function_body`, `collapse_family`), the for-init hoist likewise. `propagate_identifier_copies` is the text chain's identifier-copies fold on the tree: `var b=h` with `h` never written again reads `h` at every same-level read and the copy goes (`LILSCRIPT_CENSUS_WATCH=name` prints which opaque text makes a name unsafe). It found nothing on markedlil: every source spelling is unsafe, because the census marks a spelling unsafe for the whole module whenever any text the tree does not own mentions it, and `for` heads were text. `JsLoopHead::For` carries `initializer_tree: Option<JsForInit>` (an expression, or a `var` declarator list) and `update_tree` beside the texts; the three producers (`take_trailing_expression_statements`, `take_trailing_assignment_statements`, `for_update_clause`) return the node with the text, the for-init hoist and the rotation carry it, the census and the scope collector walk it, and the Respell re-renders the text from it. **Measured (b68 vs b67, ten pool ports): +200** (remark-gfm +182, remarklil +25; jquerylil −7), bisected locally on remark-gfm (deterministic: b67 10,368 three times): not the beam, not the collapse family, not the copies -- the emitter's own census, which its void-initialiser and declaration-merge policies consult at emission, now saw through `for` heads and let those policies act on names they had refused; with the heads opaque to the emitter's census (and transparent to the reshape's) remark-gfm reads 10,368 again. Tests 1,584; probe 22/22; lanes flat; pool 292/292. |
| 7.67 | **The unsafe set per scope.** The watch on markedlil's `Q` showed the census's coarseness, not one opaque holder: a local `Q` in one function, a module function `Q` called as raw text elsewhere and a `Q` in a spread all poisoned each other's scopes. The census now records each scope's declarations by spelling and its parent, collects opaque mentions with their scope, and resolves each mention after the walk to the binding it names where it stands (`declare`, `resolve`, `is_unsafe(bind, spelling)`); the consumers ask by bind. Found on the way: the emitter's census at emission needs the same `resolve` (without it every declared bind read as safe: remark-gfm +217 before the beam ran), and it keeps `for` heads opaque since its policies were measured that way. On markedlil the copies propagate where all were refused before. The remaining opaque texts to own: calls rendered as text, the spread in `[...x].length`, the max-assign idiom, concise text bodies, `for..in`/`for..of` heads, switch discriminants, module-level `DeclarationGroup` names without binds. **Measured (b69 vs b67, ten pool ports): +222** (unifiedlil +169, jquerylil +35, remarklil +16) and one pool case-lane a wrong program (`function_subsumption`, `none` lane): `triple` inlined while a raw call in another function still read it -- `head()` declared a function statement's name in the function's own scope, so the mention resolving upward never found it. Fixed in 7.68. |
| 7.68 | **Two fixes to the scoped census.** A `FunctionName` is declared in the enclosing scope as well as its own (a statement's name is the enclosing scope's; declaring it in both is the safe over-approximation), and the emitter's own census answers by spelling for every bind (`by_spelling`), as it did when its void drop and merge were measured -- the reshape's census alone answers per scope. `function_subsumption` prints 3940336 again on the `none` lane; remark-gfm holds at b67's 10,368. **Measured (b70 vs b67, ten pool ports): −37** (unifiedlil −32, jquerylil −7, markedlil +2), −525 against the baseline; wall 375 → 381 s, codec CPU 398 → 432 s (the beam scores more prints now that the collapse family reaches every body). Tests 1,584; probe 22/22; lanes flat; pool 292/292. |
| 7.69 | **Captures resolved by respelling.** The inliner's 776 refusals on markedlil were all short module names (`f`, `i`, `t`, `h`, `g`, `e`) declared again as locals of a frame on the way; the colliding locals are now respelled fresh through the bind table (`collect_declared_binds`; a declaration without a bind, or one the census sees written from opaque text, still refuses), so the move is sound and the print's renamer re-spells everything after. markedlil: 124 resolved, 356 still refused, 3,208 moves; the artifact 9,266 either way. `LILSCRIPT_SHAPE_DUMP` also writes every finished finalist (`.finished.<context>.<n>[.print].js`): on markedlil's context 20 the finished print already beats the incumbent (9,377 against 9,555). **Measured (b71 vs b70, ten pool ports): byte-identical**; tests 1,584; probe 22/22; lanes identical; pool 292/292. The trace also shows why markedlil's artifact keeps its incumbent: its finalist (context 0, the configured plan) reaches the bridge in a later finalize pass with the shared ledger at zero, so it never gets a beam. |
| 7.70 | **The beam pays its own way.** The shape reserve released into the shared ledger was spent by the first finalists, and the later finalize passes (the naming and pooling challengers, where the artifact's finalist runs on markedlil) reached the bridge with nothing left; the beam now draws on its own allowance per bridge (`print_beam_allowance`, width 2), nothing is held back from the search, and a finalist the search never printed is emitted for its tree when more than one plan is registered (the one-slot ledgers count emissions). A bare `return` arm fuses as `void 0` in the return-tail rung (the text fold's `if(!a)return;..;return` form). On markedlil every bridge now gets a beam, the artifact's finalist included (9,272; b70 9,266). **Measured (b72 vs b71, ten pool ports): +16** (remarklil +20, markedlil +13, posthog −15), −509 against the baseline; lanes shipped −32, zodlike −32; but the pool's wall 384 → 408 s: a beam at every bridge of every finalize pass. Tests 1,584; probe 22/22; pool 292/292. |
| 7.71 | **Once per context; binds on `let a,b,c;`.** The naming and pooling passes re-finalize the same contexts and the rungs are naming-independent, so the beam shapes each context once per compile (`beamed_contexts`). `JsStatement::DeclarationGroup` carries the binds beside its names where the emitter knows them (a function's locals via `LocalNames::bind_by_name`, a cluster's roots), so the renamer, the census and the collectors own them instead of keeping the spellings; the `for`-initialiser hoist's group has none. markedlil's plan inlines its declarations and emits no group, so it reads the same. **Measured (b73 vs b72, ten pool ports): +21** (posthog +15, unified +6), −488 against the baseline; the pool's wall 408 → 393 s. Tests 1,584; probe 22/22; lanes shipped −3; pool 292/292. |
| 7.72 | **Pooled literals have binds.** The census watch on markedlil found its most-read opaque name to be a pooled numeric alias: the string and number pools took a name from the module mangler without a bind, their declarators had none, and every reference was an `Atom` (276 for one alias). The pools now allocate a bind per alias (`alias_binds`), the declarators carry it, and a reference is a `Name` at both render sites (the emitter's and `LocalNames::new`'s), so the renamer and the census own the aliases. On markedlil the converge print improves by 17 on the first finalist; the artifact reads the same. **Measured (b74 vs b73, ten pool ports): byte-identical**; probe 22/22; lanes identical; pool 292/292; two test call sites of `LocalNames::new` given the map. On markedlil's finished dumps the prints now stand: context 20's print 9,380 against its incumbent's 9,553, context 30's 9,317 against 9,280 (the artifact's, 9,272 shipped), context 0's print alone at 9,364. |
| 7.73 | **Four more holders owned.** The census watch on a local named `T` listed them: `[...s].length` (a `Spread` node in an `Array` under `.length` now; markedlil 9,272 → 9,236 from this alone), `Math.min`/`Math.max` (a call node, not raw text), the export list (`JsModuleBinding` carries the bind; an export whose public name is its alias is renamable, one without an alias keeps its spelling, which is public), and `for` initialisers whose taken statements were commas themselves (flattened). The min-assign idiom `a<b\|\|(a=b)` is the text ladder's `fold_statement_or_assigns` on `if(c)x=v`: the `or_assigns` rung does it on the tree (`c&&(x=v)`, `c\|\|(x=v)` for `!c`). The traced fleet run (b74) stands the prints per finalist: ahead on unifiedlil (by 200) and posthog, within 30 on micromark's later finalists and markedlil's, 200 to 300 behind on jquerylil, remark and remark-gfm. **Measured (b75 vs b74, ten pool ports): +184** (jquerylil +226, posthog +10; markedlil −52), −304 against the baseline; tests 1,584; probe 22/22; lanes identical; pool 292/292. Three emission-side changes went in together (export binds, the min/max call node, the spread node); each got a port (`export_binds`, `minmax_call`, `spread_node`) and jquerylil is compiled under each in 7.74. |
| 7.74 | **The converge rung under the text convergence's rules.** `RenameOrder::Converge`: the module scope untouched, function and class names kept (`ScopeTree::function_names`), parameters by position, then the rest in first-occurrence order -- the canonical form under which two alpha-equivalent functions spell the same (`converge_local_names`' rule, read at 7.63 but ported only now). On markedlil the converge print reads −36 and −43 against the unshaped print (the frequency order −18 and −51; `LILSCRIPT_CONVERGE_ORDER=frequency`). The 7.73 bisect on jquerylil: export binds off +226 still, the min/max call node off +0 -- the node is the loss (the raw text gave the emitter's own folds another shape), so it is off by default (`LILSCRIPT_PORTS=minmax_call`); the spread node is markedlil's −52. **Measured (b76 vs b74, ten pool ports): −52** (markedlil), −540 against the baseline; wall flat. Tests 1,584. |
| 7.75 | **Concise bodies and records as nodes.** The renamer's kept-spelling buckets (`LILSCRIPT_TIMING=1`, `rename_kept_*`) named the holders on markedlil: 56 concise arrow bodies still text, 78 raw nodes, 24 literals. A concise arrow body is now the returned value's node (`concise_arrow_node`, `JsFunctionBody::ConciseNode`) when its minimal text is what the text form spelled, and a record literal is an `Object` node whose operands alternate key atoms and value nodes (`LILSCRIPT_SKIP_PORTS=object_node` keeps the atom). markedlil 9,236 → 9,234; `rename_kept_concise` 56 → 0. **Measured (b77 vs b76, ten pool ports): +82** (remark-gfm +83, markedlil +32; jquerylil −18, mobx −15), bisected locally: the object node is inert, the concise node is remark-gfm's +77 -- the emitter's own census saw the reads inside concise bodies and its collapse decisions moved, the `for`-head lesson again. Fixed in 7.76 by scanning concise bodies as text in the emitter's census only. Tests 1,584; probe 22/22; lanes flat; pool 292/292. |
| 7.76 | **The concise node waits, like the cluster tree.** The emitter's census scans a concise body as text when it answers by spelling (the reshape's walks the node), and the two walkers skip an `Object` node's key atoms (they are property names, not reads). Neither restored remark-gfm: its artifact under the node is another naming plan altogether (`i,t,e,r` locals, another function order) -- the search's rename re-prints re-spell the names inside concise bodies and the naming family lands elsewhere. `concise_node` is off by default (`LILSCRIPT_PORTS=concise_node`) until the naming family is the tree's; the object node stays on (inert on both). **Measured (b78 vs b76, ten pool ports): byte-identical**, −540 against the baseline; tests 1,584; probe 22/22; lanes flat; pool 292/292. |
| 7.77 | **Deleting text passes whose rung exists: measured, and refused by the artifact.** Each ladder pass with a rung in place is measured off on the ten pool ports by `LILSCRIPT_CLEANUP_LADDER` alone (no rebuild), against b78. NegatedEqualities off (rung `negated_equalities`): **+122** (posthoglil +60, katexlil +44, remark-gfm +21; jquerylil −13). SameBindingStrictEquality off (rung `same_binding_equality`): **+32** (posthoglil +45; jquerylil −11). Neither is deletable yet. The reason is the print-path standing of 7.72: the artifact is the text path's print on eight of ten ports, and a rung only reaches the artifact when a whole-artifact print wins the beam -- the text pass wins the same sites one at a time on the text path's own finished text. The text passes are deletable only once the print path owns the artifact, which is the naming family (the last text-side stage the trees do not own). Also in this row, more of the tree: **for-in/of heads own their object/iterable trees and the switch owns its discriminant tree** (`object_tree`, `iterable_tree`, `discriminant_tree`; the census, the collector, the Respell and `read_site` walk them; the emitter's own census keeps reading their text), and the for-init hoist group carries binds. **Measured (b79 vs b78, ten pool ports): byte-identical**, −540 against the baseline; tests 1,584; probe 22/22; lanes flat; pool 292/292; wall 398.8 s (b78 400.0). |
| 7.78 | **The print's deficit is the emission chain, and the chain's stage order is a spelling rule.** The finished print stood 37 behind the finished text on markedlil's artifact context; the trace says why: the unshaped print starts 214 behind its own emission, because the emission text has been through the chain's folds and the print has not. Itemised by statement diff: the single-use *literal* aliases (fifty-five regexes inlined at their one read by the text, kept pooled by the print), compound assignments (`a+=b`), the bare-first `var` order, self-assignment chains, a countdown loop the bounds rung missed because the hoist ran first, and fifteen `;}` the for-init rung left (the hoist's pops reset the block's terminator flag). Shipped: (1) the flag recomputed after the hoist; (2) `a=a OP b` spelled `a OP=b` on the statement (the chain's `compound_assignment_rewrite` runs on the statement's own region, so the expression render stays plain -- an expression-level spelling moved the plan choice on every port, +388; and the two nodes compare by spelling, since coalesced values share a name without sharing a bind); (3) bare `var` declarators first as a *render-time* spelling below the module, and inside the hoist -- a push-time reorder starved the text hoist (`fold_prior_assign_into_for_init` wants an initialised list) and remark-gfm lost every for-init, +205; the chain hoists first and reorders after, and the tree must keep that order; (4) single-use literal aliases inlined on the tree (`inline_single_use_literal_aliases`: regexes into nested functions, the rest in their own function, never a global or sticky regex, never a plain copy's value); (5) self-assignment chains fused on the tree (`fuse_self_assignment_chains`, statements and commas); (6) `loop_bounds` before `for_init` in the ladder; (7) a `guard_tails` rung, the inverse of `exit_guards` (`if(c){S}` last in a body → `if(!c)continue;S` / `return`), which first shipped a wrong program -- three statements pushed into a braceless loop body -- caught by the lanes (`05_for_control`), fixed by re-deciding the branch's braces; (8) the rung rejection trace carries the admission's reason. The beam's carry has a knob (`LILSCRIPT_PRINT_CARRY=best|collapse|both`; the least-shaped member joins the cleanup beam): under the first cut the collapse-only print finished 60 better than the beam's cheapest (the text finishing decides each shape where it pays; the beam scores before finishing), under the shipped cut the cheapest is best again on markedlil (9,208 / 9,188 / 9,188). **Measured (b81 vs b79, ten pool ports): −62** (rehypelil −95, unifiedlil −24, markedlil −17; posthoglil +38, micromarklil +17, remark-gfm +13), **−602 against the baseline**; tests 1,584; probe 22/22; lanes 72/74 identical on the shipped and zodlike lanes (the rest smaller); pool 292/292; wall 393.5 s (b79 398.8). |
| 7.79 | **After the collapse rung the print stands 15 behind its emission (markedlil; 214 before 7.78).** Four print-side steps, all in the collapse family: (1) the closure inliner's identifier scans lex the text (`identifiers_in`, `text_mentions_identifier`) -- a byte scan read a string's `\n` as an identifier `n`, and 56 closures on markedlil were refused as capturing one; (2) a raw value (a host call) is collapsible into its one read under the first-leaf rule, which keeps its evaluation point whatever the value is (closures stay the inliner's); (3) `x=E;use(x)` as an expression statement collapses like the binding form; (4) `var ..,x,..;x=E` absorbs the assignment (`fold_uninitialized_var_into_any_assign`), the compound form refused as the chain refuses it. Then a rung `expression_bodies` (an arrow's block of expression statements and a returned value as the sequence, `fold_expression_bodies`' arrow case; a sequence body keeps its parentheses in the concise render): it loses on markedlil and the beam drops it. Measured on b81 without a rebuild: `LILSCRIPT_PORTS=concise_node` **+64** (stays off), `LILSCRIPT_PRINT_CARRY=collapse` +1 and `both` +10 (`best` stays), the text reorder fold skipped −9 but not byte-identical (the render-time spelling does not reach a module-level block's `var`; the fold stays), NegatedEqualities off **+20** (was +122 on b78). **Measured: b82 vs b81 −64** (markedlil −47, posthoglil −18, eight ports byte-identical); **b83 vs b82 byte-identical**; **−666 against the baseline**; tests 1,584; probe 22/22; lanes identical (one case −2); pool 292/292; wall 394.0 s. |
| 7.80 | **Two parity rewrites that lose bytes, parked.** (1) An assignment moved into its first read where other reads remain (`fold_sequence_assignments_into_first_use`): on adjacent statements and a statement's own comma it costs the collapse member +41 on markedlil (`(e=c(e,r)).href=t` breaks the run the codec had); the text fold only touches sequences already inside parentheses, so the tree's form is the nested comma only (and the sequence bodies the `expression_bodies` rung makes). (2) `+x|0` spelled `x|0` (`IntegerNormalization` over a unary plus; ToInt32 begins with ToNumber, so always sound). Both fire a handful of times per print and **b85 vs b83 reads +42** (markedlil +47, unifiedlil −5): five dropped pluses and one move, fewer raw bytes, worse Brotli -- the noise floor, on the wrong side. Off by default behind `LILSCRIPT_PORTS=first_use,unary_plus`; b86 is b83's bytes (markedlil 9,183; lanes identical; tests 1,584; probe 22/22; pool 292/292). |
| 7.81 | **The census lexes; two more parity rewrites parked; where the print really loses.** (1) The reshape's `BindCensus::text` lexes an opaque text (the emitter's own keeps its byte scan, its policies were measured with it): a string's or a regex's contents (`tableCell`, `Aa`, `CDATA`) no longer make spellings unsafe -- 323 → 202 unsafe spellings on markedlil, 369 → 249 on remark-gfm; the unsafe *binds* stay (165 on remark-gfm: the raw holders name real variables), bytes unchanged. (2) `A?X:B?X:Y` → `(A||B)?X:Y` (`fold_common_conditional_arms`) and `x=E;if(x)` → `if(x=E)` on the print: **b88 vs b86 +45** (markedlil +47 again), parked behind `LILSCRIPT_PORTS=common_arms,assignment_guards`. (3) A traced fleet run (b83) gives the print's standing per port at the finish: ahead on unifiedlil (−135..−214) and at parity on katexlil, mixed on markedlil and posthoglil, behind by 30–110 on mobxlil and micromarklil, 138 on remarklil, 218–352 on remark-gfm, 237–443 on jquerylil; the unshaped print's gap to its emission (the chain's work) is 8 on katexlil, 66–143 on unifiedlil/markedlil, 519–807 on micromark/mobx/remark-gfm, 1,638 on jquerylil, 3,261 on remarklil. The fold census over the same run ranks what the print lacks: `conditional_return_tails` 1,402 fires, `negated_equalities` 900, `guard_return_expression_suffixes` 718, `uninitialized_var_into_any_assign` 666, `boolean_conditional_values` 636, `single_statement_control_braces` 433, `common_conditional_arms` 412, `single_use_if_assigns` 341, `prototype_tables_to_classes` (G12) 329, `int32_coercions` (G6) 297. (4) `LILSCRIPT_PRINT_BASE=chain` (the beam from a chain-shaped print, every chain rung applied unconditionally as the chain does): the base lands within 45 of the emission on markedlil, but the finished print is worse (9,264 vs 9,249; remark-gfm 10,621 vs 10,603) -- the finishing's ladder already re-applies those shapes on the print; what it cannot do is the question. On remark-gfm the finished print keeps `if(..){..;return d}a.exit(e);return b(c)` where the text has `return k(c)&&h<j?(..):(..)` -- ladder-shaped residue -- and the timing says `rename_starved` 9: the carried print is finished last, on a ledger the text finalists have spent. b89 is b86's bytes (lanes identical; tests 1,584). |
| 7.82 | **7e, the print's finishing funded: −277 on the fleet.** The carried print is finished last, on whatever the text finalists left of the candidate's ledger, and its finishing has the most to do (every ladder pass proposes on it, every remap is live). With the ledger pinned at 4,096 probes (`LILSCRIPT_TERMINAL_PROBES`) the print finished ahead on both local ports (remark-gfm 10,327 vs 10,366; markedlil 9,157 vs 9,208) -- ladder-shaped residue in the finished print was starvation, not a missing rung. Shipped: `LILSCRIPT_PRINT_FINISH_LEDGER=<n>` extends the ledger by `n` probes for the carried print's finishing alone (`TerminalCodecProbeBudget::extend`), **384 by default** (the level-13 base). **Measured on ten pool ports against b89: +384 → −277** (jquerylil −242, remark-gfm −48, unifiedlil −9; posthoglil +25), pool wall 394 → 548 s (+39%), codec CPU 435 → 572 s; **+192 → −227** (jquerylil −219, remark-gfm −30), wall 484 s (+23%); +96 buys nothing on remark-gfm. The bytes are taken at 384 under the rule that a mid-migration step may cost time; the wall is a debt to pay back before the end -- the text finalist's finishing and the print's are independent, and the two can run in parallel on their own slices of the ledger (next). Lanes: 17/18 cases smaller, none larger; tests 1,584; probe 22/22; pool 292/292. **−943 against the baseline.** |
| 7.83 | **Paying the wall back, first two thirds.** (1) The finalists' finishings are independent, so they run in parallel (`into_par_iter`), each on its own slice of the ledger (`TerminalCodecProbeBudget::slice`/`absorb`: the even share, the carried print's extended by 7.82's probes), absorbed back in order; `LILSCRIPT_PARALLEL_FINISH=0` keeps the sequential loop. Bytes identical on the ten ports; pool wall 548 → 536 s only -- the extra probes are the cost, not the serialisation. (2) The traced standings (7.81) say which beams can win: the best unfinished member's distance from its emission was 1,150–1,340 on jquerylil, which then won by 242 finished, and 2,877 on remarklil, which never did and paid 38 s. The print's finishing is funded only within `LILSCRIPT_PRINT_FINISH_GAP` (1,500) of the emission, per beam: **bytes identical, wall 536 → 499 s** (remarklil 96 → 58 s). Against b89: −277 for +105 s (jquerylil +75 s of it, paying 242 bytes); the rest of the debt is jquerylil's probes themselves. Tests 1,584; lanes identical; pool 292/292. |
| 7.84 | **The chain's canonical leaf syntax on every print: −100.** remarklil's unshaped print carried 129 long booleans and 21 `length|0` against none in its emission -- the plan asked for long booleans and the chain spelled them `!0`/`!1` on every emission regardless. The print's respell follows the plan's options, so the print's options now say compact booleans always; the raw and atom texts (an object literal held as one atom, an idiom) are lexed and their `true`/`false` words respelled (not a property name, not an object key, not a receiver; the space after a keyword dropped); text-only holders (conditions and heads without a tree, concise text bodies, class fields, case labels) the same; and `|0` is dropped from a `.length`, `.indexOf(..)` or `.lastIndexOf(..)` read (an integer already) where the tree holds the node. Expression rewrites now reach loop heads (their texts re-rendered) and concise node bodies. remarklil's unshaped print 40,338 → 40,238, its carried print 37,209 → 37,142 (still unfunded: the gap to the emission is 3,161). **Measured (b94 vs b93, ten pool ports): −100** (remark-gfm −53, unifiedlil −23, markedlil −13, jquerylil −11), wall 500 s (flat); **−1,043 against the baseline**; tests 1,584; probe 22/22; pool 292/292. |

### Phase 6 — the fold groups (`ea045ca` … `acace54`)

| commit | step | evidence |
|---|---|---|
| `ea045ca` | `fold-census.sh`, `fold-residue.py`; the first two push-time retirements reverted (33 byte diffs; the `mutation_spelling` contract) | the finding: a mid-pass fold cannot be retired byte-identically while the search scores pre-peephole text |
| `b2a789e` 6.1 | adjacent declarations merge at the join (`push_statement_with`, across `push_block`); `let f=…` arrows absorbed as declarators | cases shipped 7,512 → 7,493, zodlike 7,453 → 7,439; solo residue 0 with the search off |
| `06edd9b` 6.2 | the module-level `;`→`,` is the printer's (`JsBlock::render`, `top_level`); `Comma` node with operands; `[emission]`/`[peephole] input` traces; `fold-origin.py`; `LILSCRIPT_ONLY_FOLDS`; **live-14** fixed | ports markedlil 9,340 → 9,323, zodlil 32,464 → 32,438 (281 module `;` → 77); cases +25 (tiny artifacts prefer `;`); solo residue 0 in every lane |
| `4edeb38` 6.3 | the `for(` head takes the plain assignments written before it (G8 first item) | ports byte-identical; none lane identical; the `var`-list shape reverted (it hides a single-use literal from a later fold: +5/+8) |
| `acace54` 6.4 | the `;` before `}` and at EOF is always the printer's (`close_branch`, `into_braced_body`) | solo residue 79/134/514 → 0/0/0; markedlil 9,323 → 9,322, zodlil 32,438 → 32,415 |

---

## Live wrong programs

Fourteen found, all reproduced. **Every one was found by an instrument this migration built**
(reading, the twin, the probe matrix, the differential at classes, fold bisection, the port twin
lane) — none by the fleet's own test gate.

| # | defect | found by | state |
|---|---|---|---|
| 1 | `extern` rewritten by source spelling (105 names) | reading | **open** — wants the declared host binding (003); an owner decision (012) |
| 2 | closure `this`/`arguments` rebound by arrow spelling | reading, 061 | **open** — port fixed; the compiler fix is a fleet-rule change (004 §2) |
| 3 | `lilscript.toml` ignored for a bare relative path | reading | fixed `fe51558` |
| 4 | `charCodeAt` out of range yields `NaN` under `size-first` | reading | fixed `fe51558` |
| 5 | `preset = "none"` deletes a live binding — two folds | `SKIP_FOLDS` bisection | fixed `152b830` |
| 6 | `JS.number(x["length"])` loses `ToNumber` under `size-first` | agent | fixed `76d3c4f` at the family's admission |
| live-15 | jquerylil: "startup limits rejected every JavaScript candidate" — every candidate failed admission because the emission does not parse: `Za==bh.promise()&&throw new TypeError(..)`. A throwing host alias (`throwTypeError`, `JsHostAliasConvention::ThrowConstruct`) expands to `throw ..` text as an *expression*, and since `3bb2322` (migration 3, the structural classifiers) the guard compaction reads that expression statement as compactable. Bisected on the pool with `git bisect run` (99 commits, 7 steps): last good `7e5975e`, first bad `3bb2322`; the pre-migration baseline `54e1948` builds jquerylil at 28,764 Brotli | 7.10 fleet A/B | **fixed 7.18**: the push hook turns a raw `throw ..` expression statement into a `Throw` statement, which no compaction reads |
| 7 | `optional_constructor_callback` fails to compile | release gate | fixed `5fc2aac` |
| 8 | a phi region duplicates a side-effecting call (10 of 137 candidates wrong; the search picked one) | differential at classes | fixed `d07a529` |
| 9 | at level 15 a `\|\|`/`&&` chain loses one side-effect call | probe matrix | **open** — pre-existing; minimisation pending |
| 10 | a name declared twice in a `let` run joined the next declarator onto a bare assignment | Declarators node | fixed `523ca17` |
| 11 | `parse_single_assignment` accepted `1==x?..;` as an assignment to `1` | classifier twin | fixed `7e5975e` |
| 12 | an `if` body of `[try{..}, stmt]` spelled braceless, the statement outside the `if` — on `main` | classifier twin, zodlil diff | fixed `f6d515a`; `tests/cases/live12_braced_try_in_if.lil` |
| 13 | mobxlil fails standards-parser admission with the search off | port twin lane | pre-existing, on the owner's list |
| 14 | `fold_dead_pure_identifier_assigns` deletes a braceless loop body's carried write (an infinite loop) | probe matrix (`gzip` row) | fixed `06edd9b`; the sixth wrong-program fold |

Caught before landing, not numbered: closure trees keyed by IR function re-rendered one clone from
another's tree (5.3); the receiver parameter's bind leaked into `Name(bind,"this")` (5.4d).

---

## Instruments (what proves what)

| instrument | proves | how |
|---|---|---|
| `LILSCRIPT_TWIN=1` | every node reproduces from its children; the list equals the text at every boundary; the identity respell changes nothing; condition text equals its tree | env-gated, runs in release; negative control recorded |
| `~/probelil` + `scripts/configs.mjs` | one feature-dense program has one answer under 22 configurations | 4.6 s; found live-9, live-14; run compiled outputs under `timeout` |
| `tests/cases` in three lanes | behaviour and Brotli per step against the pinned previous binary | shipped / none / zodlike; `$SP/bin/<sha>/lilscript` |
| `workers.mjs check --lanes …` | the case matrix on the pool, four lanes | 292/292 in ~20 s |
| `lilscript-differential --random-seed` | generated programs with classes, closures, `this` | found live-8 |
| `LILSCRIPT_NAME_TRACE=1`, `name-trace-diff.sh` | naming did not move when it should not have | 0 diffs required on byte-neutral steps |
| `LILSCRIPT_FOLD_REPORT=all`, `fold-census.sh` | which folds still rewrite, per lane | deletion needs 0 everywhere |
| `LILSCRIPT_ONLY_FOLDS`, `fold-origin.py`, `fold-residue.py` | a fold's residue on the emitter's own text, split from text-derived candidates and from other folds' output | the phase 6 work list is the *emitted* column |
| `LILSCRIPT_SKIP_FOLDS` | bisects a wrong program to its fold | seven builds over 111 folds |
| `LILSCRIPT_PEEPHOLE_TRACE=1` (+ `RAYON_NUM_THREADS=1`), `LILSCRIPT_STATEMENT_TRACE=1` | per-run snapshots, every rendered candidate | parallel emissions interleave stderr — single-thread it |
| `port-twin.sh` | the witnesses on a real port with the search off and under an ordering | found the receiver bind leak |
| `LILSCRIPT_TIMING=1` | effort buckets, `emit_calls`, `lex_calls`, `facts_delivered`, `name_bound`, `rename_kept_<kind>` | counters, never wall clock |

---

## Measurement facts that steer the work

- **Where compile time is, on the current binary (markedlil, pool D16 worker, `acace54`):** wall
  29.9 s; **emit 66.7 s CPU over 269 emissions (248 ms each)**; peephole 11.6 s over 72 runs (idle
  folds 6.3 s / 9,624 calls, active 5.1 s / 2,507); codec 6.5 s over 492 calls; optimize 1.7 s;
  analyze 1.6 s; lex 1.1 s over 7,666. On posthoglil (pre-`PairGraph`) codec was 87.7 s and emit
  60.1 s over 1,246 emissions. **The emission count is the multiplier**; per-emission cost is
  naming, out-of-SSA and rendering, paid again for every spelling variant.
- **zodlil ships one emission and no peephole** (`emit_calls` 1, `peephole_calls` 0, level 8, its dev
  config): its artifact is pure emitter output, so emitter-side changes show there directly.
- **The text layer is worth 189 Brotli on markedlil** (all folds on 9,397, off 9,586); a
  semantically empty change moves Brotli by about −125..+30 per artifact; tiny artifacts prefer
  `;` over `,` by three bytes while ports prefer `,`.
- **The search scores pre-peephole text on the candidate stages and the folds normalise afterwards**:
  option-off spellings (`elide_block_terminal_semicolons`, `comma_expressions`, `mutation_spelling`)
  only ever existed for what the intermediate folds saw; a printer-side retirement is neutral exactly
  when the fold ran after everything that could have consumed its input (6.3's lesson).
- **The string-surgery candidates re-create shapes the emitter no longer writes**
  (`top_level_declaration_variants`, the function-leading respelling, pooling): the *derived* column
  of the origin census — `merge_adjacent_declarations` 146, `fold_prior_assign_into_for_init` 536,
  all derived. That is candidate derivation's residue, not the emitter's.
- **Effort is not monotone downward**: L11 is smaller and faster than the shipped L13 on three of
  four ports; the ladder wants re-deriving per port after the cost model changes.
- **Name convergence loses to frequency because frequency ordering is entropy coding** (056/059);
  one idiom group applied as the baseline of the whole search wins (−1,168 net), the same group as a
  late re-spell wins nothing, because names steer `CompressionSimilarity` layout and every
  text-scored decision downstream.
- **The identifier stream is where katexlil's gap lives (+2,113), and the port's transliteration
  is why** (047/050): function by function we already beat Terser there; the loss is collective.
- **Never-active folds are ~3% of CPU**; guarding them is safe and small. 53 of 128 never fire on
  four ports; 81 of 129 never fire on the corpus.

---

## What is open, in order

1. **Phase 7′ — one emission, many prints** ([012](012-second-look.md)). Where it stands after
   7.82: the print beam runs once per context inside each finalist's bridge cleanup on its own
   allowance, sixteen rungs, the cheapest print carried through the real finishing on a ledger
   extended by 384 probes for it (7.82, −277 on the fleet; wall +39%, a debt). The tree owns the
   chain's single-use inlining of functions and literal aliases, the self-assignment chains, the
   compound spelling and the bare-first `var` order (7.78–7.79); after the collapse rung the print
   stands within 15 of its emission on markedlil. Per port at the finish (7.81's traced run, before
   the ledger): ahead on unifiedlil, at parity on katexlil, behind on jquerylil/remark/remark-gfm
   where the chain's gap is 800–3,300. What is left, in order: (a) pay the wall back -- the text
   finalist's finishing and the print's are independent and can run in parallel on their own
   ledger slices; the text passes whose rungs exist come out when the artifact stops needing them
   (NegatedEqualities off is +20 now, +122 at b78); (b) the chain folds the print still lacks, by
   the fold census (7.81): `conditional_return_tails`, `boolean_conditional_values`,
   `uninitialized_var_into_any_assign` (done on the print), `common_conditional_arms` (parked: it
   loses at the noise floor), G12's class recovery, G6's int32 elision, `arguments` to formals,
   fresh-array pushes (needs an `Array` node); (c) the raw holders that make 165 binds unsafe on
   remark-gfm (the emitter's statement-or runs, `x=x||R.exec(y)`, `a?.b??null`, `new T(..)`, array
   literals) -- each an ownership step with the naming lottery to measure; (d) the naming on the
   tree: the converge rung costs +150–200 on markedlil, the text convergence and the letter remaps
   still finish every print. Parity rewrites that fire a handful of times per print each lost
   ~+45 on markedlil (7.80, 7.81): the fleet decides, never parity.
2. **Phase 6 under that regime**: the residue on the cases with the shapes off (7.41):
   `fold_prior_assign_into_for_init` 128, `fold_identity_arrow_iife` 108,
   `fold_negated_conditional_arms` 90 and `merge_adjacent_declarations` 72 (all four now carried
   by the shape prints or the tree's push-time merge -- what remains is the text chain's own
   adjacency, and the search's plans that the prints do not reach), `fold_expression_self_assignments`
   69 (the probe's closures), `fold_single_use_function_expressions` 61 and the collapse family
   (`fold_statement_assignments_into_first_use` 52, `fold_single_use_literal_bindings` 48,
   `fold_identifier_copies` 13 -- the prints carry them on the finalist; on the search's plans they
   are still text), `fold_int32_coercions` 33 (G6, the one untouched family), G8, the scored
   groups as `ShapeTransform`s, G12 last. The single-use collapse on every emission is measured
   and off (+128 on the fleet, 7.34).
3. **live-9 fixed (7.47)**; **live-1** and **live-2** (owner decisions), the G8 latch. The
   everywhere collapse re-measured after the fix (7.48): twelve wins, katexlil +736 on its own
   256-probe ledger, +254 net -- off by the rule until katexlil's ledger is the port's decision
   or 7e proper sizes the ledger by the finishing's value.
4. **Phase 5.5 deletions** once the tree's idiom axis matches the text pass on the fleet.
   Deleted today: `fold_expression_suffix_returns` (7.38, it shipped `try{..}return catch{..}`),
   the nine folds that fire nowhere on the cases or the fleet (7.42), five passes of the
   codec-verified ladder (7.50), three passes of the structural chain (7.51), and four
   emission-chain folds with their variants (7.52) -- every one measured alone on the port that
   lost most (`LILSCRIPT_CLEANUP_LADDER`, `LILSCRIPT_CANONICAL_CHAIN`, `LILSCRIPT_SKIP_FOLDS`).
   Thirteen ladder passes, a structural chain of six, and 79 registered folds remain. What
   still pays on the three heavy ports, on the finished text: the for-init hoist (+407), the
   identifier copies (+379), the single-use function values (+142), the boolean values (+95),
   the single-use functions (+64) -- the shapes the search's later stages create, which the
   tree writes at construction and the print offers on the finalist but the text still finds.
   That residue is 7′'s: when the print is the artifact, the finished text is a tree.
5. **End-state measurement**: interim on one worker (7.49): wall −5%, the ports' own walls
   −19%, the text layer −95% CPU, Brotli −701 over 20 ports. Repeat when 7′ lands; then the
   remaining clean ports (jquerylil, mobxlil, posthoglil, cnlil).
