import {
  dataPrivExpando as expando,
  dataPrivCache as cache,
  dataPrivGet as get,
  dataPrivSet as set,
  dataPrivAccess as access,
  dataPrivRemove as remove,
  dataPrivHasData as hasData
} from "../Data";

export { expando, cache, get, set, access, remove, hasData };
