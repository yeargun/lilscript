import { jQuery } from "../core";
import {
  scheduleTimeout,
  throwValue
} from "../js-host.lil";

jQuery["readyException"] = (JsValue error) => {
  scheduleTimeout(() => {
    throwValue(error);
    return JS.undefined();
  });
  return JS.undefined();
};

export { jQuery };
