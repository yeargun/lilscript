export pure float stringHash(string value) {
  int hash = 5381;
  int index = value.length;
  while (index > 0) {
    index -= 1;
    hash = (hash * 33) ^ value.charCodeAt(index);
  }
  float unsigned = hash;
  if (hash < 0) {
    unsigned += 4294967296.0;
  }
  return unsigned;
}
