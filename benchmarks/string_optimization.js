const product = "LilScript";
const valid = product.startsWith("Lil") && product.endsWith("Script");
const normalized = product.toLowerCase();
console.log(`${normalized}:${valid}:${product.includes("Script")}`);
console.log("repeated-long-application-label");
console.log("repeated-long-application-label");
console.log("repeated-long-application-label");
