# Artifact Admission and Public Service Evidence

This is a bounded qualification receipt, not a migration plan or milestone certification.

The first accepted [run receipt](run-2026-09-19T13-05-45.258Z/receipt.json) has SHA-256
`cd424638e1d82eba84df5d9dcbafe58455f82c375e8009d7db7b0dead67340bc`.
Compiler/build/test input digest before and after the run is
`dc3be0375ba356193b5aab674044c8b6cf021c7eb5faa9d2f689996472c09160`.
The receipt records individual commands, tool versions, compiler and scorer hashes,
test filters, delivered bytes and independent canonical codec replay.

## Verified Scope

- 154 focused Rust library tests and one CLI unit test pass on Node 24.11.1.
- Source/path service calls return qualified immutable JS. Raw/gzip/Brotli winners
  remain independent; aliases transfer one output buffer without duplicate ownership.
- Immutable records own admission/tactic provenance. Unknown required runtime cost,
  changed formation contracts, stale/foreign handles and incomplete packages reject.
- Native C/header records share the same artifact arena, remain charged through JS
  work, and transfer without copying. Native-only requests do no JS codec work.
- Original integrated module, callback/reentry/throw, public-object, value/reference
  and Marked-slice observations pass through fast and searched public service routes.
  The unchanged portable product consumer also executes as native C.
- Search requests respect effective policy/API caps, leave work for discovery and
  other attempts, and partition fact-cache storage between metadata and useful results.
  The 1 MB raw-output regression finds a real helper alternative under both forms of
  equivalent resource configuration.
- CLI semantic stdout and file output match exactly, including the reported hash.
  Canonical rescoring agrees; Node parses and executes the delivered file. Development
  mode performs no optional proposals. Unsupported defaults diagnose without fallback.
  CLI `all` produces matching JS/C hashes and both executions print the expected result.
- The structural interaction fixture independently enumerates eight helper subsets
  with three naming styles. Brotli minima: baseline 162, A 175, B 187, A+B 161,
  all three 143. Width-two bounded search reaches the combination; width one misses it.

`qualify.mjs` rebuilds incrementally and runs only the listed affected cohorts and CLI
checks. Build and execution logs are retained per run. Debug durations are diagnostic,
not release-speed or RSS qualification.

## Shared Recipes And Scoped Sessions

The subsequent [accepted receipt](run-2026-09-19T13-32-24.596Z/receipt.json), SHA-256
`3bde6d738191b03dd68163f298192c75000f71174c33375e2fe4ab8f363894b0`,
pins unchanged compiler/build/test inputs with digest
`1a63448cb7c2279a877a902109d2a3d4c4753381c961cdd07711b1e13fa1f1c0`.
It passes 743 Rust library checks (689 semantic-core, 15 service and 39 policy),
one CLI unit test, the existing CLI checks, and all original example-consumer
modes: direct, search, retained edits, advancing edits and native.

- Candidates, search states and immutable JS records share one admitted complete
  implementation descriptor. Naming variants do not copy it; it survives source,
  candidate and search disposal until the last artifact owner releases it.
- Frozen dependencies retain their producer recipes; complete resource comparison
  and hashing include those recipes. Resource format is 2 and schedule version is 7.
- Unsupported or budget-refused target preparation cannot publish a new descriptor
  cache. Successful preparation may retain it with the candidate; output scratch
  still releases exactly, and final compiler retention is zero.
- `with_checked_source` / `with_checked_path` create one scoped compilation session.
  The default semantic service and migrated example share frontend adoption,
  qualified direct/edit/search/native delivery, request budgets and phase reports.
- Recoverable package handoff errors release service-owned staged records and keep
  the caller's candidates usable. Whole-file delivery still explicitly rejects
  packages; this is not a newly implemented package-delivery API.
- Original twelve-module direct output measures 18,232 raw / 4,229 gzip / 3,863
  Brotli bytes. Independent searched winners measure 17,463 raw, 3,832 gzip and
  3,499 Brotli. These are one fixture's outcomes, not a competitor or speed claim.
- Native original consumer delivers 5,606 C bytes, executes its fixed observation,
  performs zero codec work and releases all admitted storage at finish.

Use `qualify.mjs --semantic-core` at this shared-owner promotion boundary. Ordinary
iteration retains narrower affected filters. Neither mode runs the full library
fleet. Debug build took about 68 seconds; semantic-core execution about 40 seconds.
Example direct timing now explicitly includes qualification and delivery metadata;
historical clocks with different scopes are not directly comparable.

The [retained-observation supplement](session-retained-observation/summary.json)
uses the same accepted binaries with four edits and a branch retained after edit
one. Both retained and advancing workflows preserve the branch's externally
observed value 12 while the final and original snapshots return 11. Their exact
artifact hashes agree between workflows, and every file passes the unchanged
consumer and canonical rescoring. Summary SHA-256:
`b98f27c4d066d52ccdf4faffc3dffabf91026c3892b275cdc406b7f44ffc71be`.
The qualifier now uses that odd retention point; the accepted broad run above
used an even point, whose retained/final observations both returned 11.

The [shared-recipe comparison](shared-recipe-factory-comparison/receipt.json)
executes the same independent twelve-state oracle against both preserved accepted
test binaries. All 72 observations (36 oracle and 36 search) have identical JS,
naming choices and scores. The intentional recipe-fingerprint format change is
excluded from equality. Search still makes 35 proposals, admits 36 renders and
uses 70 optional codec probes; independent winners remain 205 raw / 159 gzip /
124 Brotli bytes. Receipt SHA-256:
`dd9c1a3a904a11aeb0cfa8337ce501a15b178d79a971ced1ae8d36689a363f52`.
This is one fixture's before/after check, not a fleet no-loss or speed claim.

## Checked Rewrites And Factory Finalization

The [accepted rewrite/finalization receipt](run-2026-09-19T13-59-26.405Z/receipt.json)
has SHA-256 `1524f7b66ea50d0f91080c2c2e6abea5d73da5c4655c1b23af4af3d71147cb68`.
Its unchanged input digest is
`8cb3f1b60dfc2137037891b84b17f7779fd254da6dee927e4facf96e90a05b7f`.
It passes 760 Rust library checks (701 semantic, 20 service and 39 policy), one
CLI unit test, exact CLI delivery and all original example-consumer modes.

- One closed literal-integer binary-fold rule creates its own checked evidence
  and publishes through the existing atomic transaction. Raw annotated inputs
  do not qualify as literals. Source changes get new meaning identities; only
  checked rewrites preserve meaning, while every edit gets a fresh snapshot.
- Twelve focused rewrite tests cover fifteen integer edge cases, coercion and
  throw observations, false/stale/foreign evidence, permission vetoes, retained
  artifacts, fact invalidation, full-index agreement and resource/unwind rollback.
  A combined six-case native program executes before/after under GCC and Clang
  at O0, O2 and UBSan, using the existing native harness.
- Shared admitted lineage retains complete versioned rules and original snapshot
  stamps through JS/native artifacts and frozen dependencies. Later source edits
  mark inherited history, not replay proof for their new meaning. Canonical
  comparison excludes process-local stamps; exact candidate identity does not.
  Resource format is 3 and schedule version is 8. Exact byte-score reuse is unchanged.
- Public service metadata owns the full rule description after finalization.
  Native formation and JS formation/admission both preserve folding permission
  dependencies. Publication receipts include proving work and new lineage storage.
- Factories borrow the session to callbacks and finalize it themselves, including
  callback error and panic paths. Path source buffers drop after the compilation
  and before final reporting or resuming the original panic payload. Caller-owned
  source strings are explicitly not reported as released. Target report metadata
  survives this finalization. This establishes ordering, not frontend admission.

Debug incremental test build took about 21 seconds; the semantic cohort took
about 41 seconds. These are qualification costs, not compiler-speed measurements.

## Source Buffers, Parser Arenas And Verification

The [accepted frontend-owner receipt](run-2026-09-19T14-47-06.382Z/receipt.json)
has SHA-256 `1804a4c053da8d245595748b3aa15782a4440850343fdd349b0527cf6dd086f2`.
Its unchanged input digest is
`884bba2f08f13805e1349c24d9b8057b0b027e1a44350042a48d31f448f5d359`.
All 842 Rust library checks pass: 707 semantic, 28 service, 55 parser, 13 module
and 39 policy. One CLI unit test, exact delivered-byte/canonical-codec checks,
unsupported-option diagnostics and original direct/search/edit/native consumers
also pass. Schedule version is 9; resource identity format remains 3.

- Source reads admit buffer growth, including old/new capacity overlap, through
  the existing module loader. Actual String capacities remain charged through
  frontend, adoption and backend work until the factory drops source storage.
- Source, discovery and main multi-file parser arenas admit backing growth before
  allocation. The same grammar uses a fallible private facade; legacy public
  parser contracts remain intact. The owned multi-program list runs element
  destructors before arena release, including retained source identities.
- Semantic service rejects nonzero `for_of_specialize_family` before parsing or
  I/O. Its legacy AST expansion generates callable names and is not a harmless
  option to ignore. The semantic route already rejects the underlying loop;
  legacy compiler/parser expansion behavior is unchanged.
- One full semantic verifier admits its actual scratch, traversal and comparison
  work. Fixed edits reuse the same unit validator without unrelated body scans.
  Typed allocation refusals survive conversion and public service error reporting.
- Tests cover malformed/UTF8/import diagnostics, partial growth, deadlines,
  syntax/resource refusal, same-ledger releases, successful and unsuccessful
  verification peaks, unchanged parents and callback error/panic cleanup.
  Render work includes shared buffer allocation/movement tariffs, not only
  target-code generation. Logical accounting is not process RSS.

The [delivery comparison](frontend-delivery-comparison/receipt.json) verifies all
15 delivered JS/C files are byte-identical to the accepted 760-check checkpoint,
including independent codec winners and retained/advancing edit outputs. Both
checkpoints independently execute original consumers and replay canonical scores.
This is fixture output neutrality, not a fleet no-loss or compiler-speed claim.
Debug test build took about 54 seconds; the semantic cohort about 41 seconds.

Remaining frontend exclusions are lexer/template heap, pre-adoption source-identity
allocation, checker and semantic-conversion construction, module metadata and
diagnostics. Multi-file discovery still full-parses each source before the service
parses it again; checking happens once. The discarded discovery AST is not a second
semantic authority, but duplicate parsing remains an efficiency/ownership gap.

## Token Storage And Semantic Construction

The [accepted 897-check receipt](run-2026-09-19T15-15-53.507Z/receipt.json) has
SHA-256 `d1aa63ebaa3d2b54147154b86cc0ef5e3d9fb1e6dcb6bc85f7513856ba869ee3`.
Compiler/build/test inputs stay unchanged at digest
`c8e2321a2441ce13d7f696a9172aac26f7b98e8c455c401fe6fb1c0d5af6e124`.
Schedule identity is 10; complete resource identity remains format 3.

- All 897 focused library checks pass: 716 semantic, 32 service, 39 policy,
  13 lexer, 58 parser, 13 module, 13 literal and 13 allocation-owner checks.
  One CLI unit test, exact CLI delivery/rescoring/execution, original direct,
  searched, retained/advancing edit and native consumers also pass.
- The same lexer admits token Vec growth before allocation, including old/new
  capacity overlap. Nested parser fragments hold simultaneous token charges;
  lexical, syntax, resource and unwind paths free buffers before releasing them.
- The existing Lower admits graph tables, units and temporary operand/capture/type
  storage. Shared string decoding admits UTF16 scratch and UTF8 output overlap;
  independent Node code-unit checks cover surrogate and template-cooking cases.
  Raw-name misses do not rescan the string table after their initial lookup.
- Prepared construction partitions charges exactly into the existing Pending
  publication owner. It cannot top up a previously unadmitted graph. Only a
  measured unused Type-clone capacity allowance reconciles downward. Publication
  preserves payload addresses and admits only new headers and the reverse index.
- Deadline, work, memory, full-store and index refusal preserve existing parents.
  Store construction returns the original ledger on ordinary Result failure;
  the factory drops prepared graphs and path sources before releasing charges.
  This does not claim recoverable allocator OOM or process-level limits.
- The [delivery comparison](lower-delivery-comparison/receipt.json), SHA-256
  `b55d5c06fb8a4a81b75801d12ca83220f1ce87da7f016a90a77a7ed7d31f3812`,
  checks actual bytes: all 15 delivered JS/C files equal the 842-check checkpoint.

The single incremental test build took 79.2 seconds, semantic checks 47.7 seconds,
and CLI/example linking 3.8 seconds. These debug durations are diagnostics, not
release-speed comparisons. The qualifier now continues all focused filters with
the same binary after a filter failure, then refuses promotion if any failed.

Preserved compiler/scorer/example/test binaries are under
`/tmp/lilscript-lower-admission-baseline-20260919/`; their hashes match this receipt.
At that checkpoint, checker storage, template scanner buffers, initial
source-identity allocation, module metadata, diagnostics, reporting and
whole-process RSS remained excluded, and multi-file discovery still parsed twice.
No complete milestone was closed.

## Joint String Pool Oracle

The same accepted run contains a [finite manual oracle](run-2026-09-19T15-15-53.507Z/string-group-oracle.json),
SHA-256 `9c287d1734a32f2dde1413d43b482e967ee7ae94109dd187e60c5d0df6105c17`.
Six existing recipes times three naming styles produce 18 outputs through the
common artifact owner. Independent codec replay and Node execution verify each
delivery, including UTF16 values, host-call order, throws and exported name/arity.
The automatic inventory is inspected, not executed: its four opportunities are
singleton groups, while manual publication can prove one jointly shared group.

Each cell is the minimum across the same three naming styles, not unrestricted
naming or global optimization:

| Recipe | Raw | Gzip | Brotli |
|---|---:|---:|---:|
| Computed | 147 | 117 | 85 |
| Literal substitution | 139 | 114 | 85 |
| Pool first definition | 151 | 128 | 102 |
| Pool second definition | 151 | 129 | 101 |
| Separate singleton pools | 155 | 123 | 91 |
| One joint pool | 119 | 115 | 98 |

Joint pooling is an unreachable automatic raw-size choice, but loses Brotli by
13 bytes against computed/literal output. Literal substitution wins gzip.
Neither tested pooling combination demonstrates a Brotli valley. Production
discovery/scheduling is unchanged; no default pooling priority follows from this
fixture. Recipe, policy, exact bytes/hashes, scores, observations and work counters
are recorded. Frontend inspection, receipt storage and independent measurement
are outside those compiler counters; this is not a library or speed claim.

## Checker Fixed Tables

The [accepted 1,036-check receipt](run-2026-09-19T15-34-11.534Z/receipt.json) has
SHA-256 `53888122458e0198873b0854a848d03c1598d7bb532023026d86d19a6984c7b7`.
Compiler/build/test inputs remain unchanged at digest
`5a53a17673368ef3ab133158cb9378506fc59f1d54f9e6f8218ee5a273a8c985`.
Schedule identity is 11; resource identity remains format 3.

- All 1,036 library checks pass: 717 semantic-core, 138 checker, 32 service,
  39 policy, 13 lexer, 58 parser, 13 module, 13 literal and 13 allocation-owner.
  CLI units/delivery and all original direct/search/edit/native consumers pass.
- The existing checker pre-admits fixed expression-type/source-info arrays and
  the outer module-facts vector. It keeps one Analyzer/declaration authority.
  Its callback runs Lower on the same budget while the model is live, then
  drops checker storage before its scratch charge. Public inspection APIs keep
  their existing signatures and diagnostics.
- Ten owner tests cover exact charges, partial/indexed refusal, retained callback
  transfer, parent preservation, caller errors/panics, shared declarations,
  malformed-graph diagnostic precedence and independent Node observations.
- The [delivery comparison](checker-delivery-comparison/receipt.json), SHA-256
  `c06f03c8fc1912d728bb57cdefa8e9ee756b77352a170f0a11f1ee88ff2f0dbc`,
  checks actual bytes: all 15 JS/C files equal the 897-check checkpoint.

Incremental debug build took 71.6 seconds, checker tests 0.2 seconds,
semantic-core tests 54.7 seconds and CLI/example linking 3.5 seconds. These
are diagnostic durations, not release-speed comparisons. Preserved binaries
are under `/tmp/lilscript-checker-admission-baseline-20260919/`.
Nested type payloads, maps, declaration/interface storage, graph validation and
schedule, traversal scratch, diagnostics and comprehensive checker work remain
excluded. Multi-file discovery still parses twice at this accepted source pin.
The subsequent parse-once work is not qualified by this receipt.

## Source Name Neighborhood Oracle

The same run contains a [24-output oracle](run-2026-09-19T15-34-11.534Z/naming-neighborhood-oracle.json),
SHA-256 `147fee6eba3d4e2a2757e9c6f303b1a0baa31153fe3255f3f1f062c1db835c47`.
Two existing structural recipes each prepare once, discover eligible binding
identities independently, and try four override masks with three existing styles.
All outputs use common qualification and independent codec/Node replay. Node
observations cover public name/arity, host-call order, throws and UTF16 values.
Test-only callable inspection uses the existing JS parser to map parameter names;
that text is not compiler input or semantic proof.

Each cell is a raw/gzip/Brotli minimum across the three styles:

| Source-name Overrides | Computed | Literal |
|---|---|---|
| None | 150 / 117 / 84 | 142 / 114 / 82 |
| status | 165 / 119 / 88 | 157 / 117 / 83 |
| message | 168 / 119 / 88 | 160 / 118 / 86 |
| Both | 183 / 122 / 90 | 175 / 120 / 87 |

Per structure, six override trials produce five distinct byte streams absent
from its three no-override style outputs. None improves any codec's winner.
No naming/structure valley is established; this does not rule out other inputs.
Optional work totals 6,482,106 units, including 6,446,024 codec units. Frontend
inspection, test receipt ownership and independent replay are outside these
counters. No production heuristic changed and no default expansion, library
coverage or speed claim follows from this negative result.

## Parse Once Module Discovery

The [accepted 1,065-check receipt](run-2026-09-19T15-55-00.967Z/receipt.json) has
SHA-256 `32243eee8a6548c09cfea59945debbbf40ba5618d0fae1f4b4a96b85a4d12648`.
Inputs remain unchanged at digest
`1fca56536bbfcb2152924d5ee9e94f905ef84b02be5c7d18c0eee7f1d2fd5635`.
Schedule identity is 12; resource identity remains format 3.

- All 1,065 focused library checks pass: 717 semantic-core, 139 checker,
  33 service, 39 policy, 13 lexer, 58 parser, 25 module, 13 literal,
  13 allocation-owner and 15 selected legacy module/cycle/foreign/split checks.
  CLI units, original consumers and exact CLI delivery also pass.
- One generic loader keeps the existing resolver, canonical IDs, import collector
  and traversal. Public wrappers retain default String-owned graphs; the private
  factory path borrows stable source backing and retains discovery programs for
  checking. Generic defaults do not promise unchanged inference for every
  unconstrained external struct literal.
- Source backing and syntax have separate owners. A shared helper pre-admits bump
  growth for both. Temporary read Strings overlap charged stable source storage,
  then drop before their charges. Parsing runs outside short ledger callbacks.
  The factory drops checker, owned programs, syntax backing and graph metadata
  before backend work. Source backing survives until compilation finalization,
  including client error/panic and prepared/adoption refusal paths.
- Actual parser counters prove one parse per canonical source and no live parser
  owner at the backend callback. Tests cover cycles, repeated canonical imports,
  static/foreign/nested dynamic discovery, original diagnostics, retained source
  pointers/identities, partial refusal and source-store unwind settlement.
- The [delivery comparison](parse-once-delivery-comparison/receipt.json), SHA-256
  `c4c9e8c9949f42b0614923786fb309be0db12357e90dbe6256b96a3573f3f186`,
  checks all 15 JS/C files against the 1,036-check checkpoint; all are identical.

The debug test build took 127.2 seconds, semantic-core tests 55.8 seconds,
and CLI/example linking 7.5 seconds. Source-pinned binaries are preserved under
`/tmp/lilscript-parse-once-baseline-20260919/`. No complete milestone is closed.
At that checkpoint, template scanner buffers, initial source identity, remaining
checker allocations, module metadata, diagnostics and whole-process resource
limits remained uncovered.

### Paired Frontend Diagnostic

The [30-trial diagnostic](parse-once-debug-comparison/receipt.json), SHA-256
`ed500f5a0499baea8f81e5a069aafc2ac8c8847fa44bbe3dfc95e9cc813f1827`,
uses the two qualified debug compilers with five alternating pairs per unchanged
fixture. Node, scripts, fixture/configuration and both accepted source manifests
are pinned; input digest is
`a1a0bb60a4f0d585e6550fd2675b85386dba9139f9a44663fe5d08e5768e287a`.
All exact stdout and module manifests match. Every run uses development semantic
JS-module delivery, including the fixture named native-entry.

| Fixture | Modules | Discovery + Parse Median, ms | Total Median, ms | Source Backing, bytes |
|---|---:|---|---|---|
| entry | 12 | 8.55 -> 4.72 | 137.05 -> 135.18 | 38,871 -> 48,768 |
| native-entry | 2 | 1.12 -> 0.71 | 14.89 -> 13.74 | 1,028 -> 1,408 |
| factory | 6 | 2.25 -> 1.51 | 18.72 -> 20.91 | 1,985 -> 3,840 |

Discovery-plus-parse medians improve in this small diagnostic, while overall
factory compilation is slower. Source backing and whole-compilation logical
retained peaks increase by 9,897 / 380 / 1,855 bytes respectively. Frontend logical
work rises by 16,935 / 392 / 208 tariff units; these counters are not elapsed time.
The shared eight-CPU host was not isolated (starting load average 5.45), executable
hashing can warm caches, and five debug pairs do not establish release speed or
fleet behavior. Separate new discovery/parse timings are not invented; the public
service and example report the combined phase explicitly. Raw trials remain
available, including slower observations.

## Repeated Ownership Comparison

The [repeated retained/advance receipt](retained-advance-debug-comparison/receipt.json)
has SHA-256 `30c22e90d17f8bb88b8551b5d6ba225ce7d72912a5c27ec704ff89211d88a801`.
Its input digest is
`13bad2506bfbd4ccb9c91b93f5fdebaeedf9c91832b47059668b0f12c280ea97`;
the [unchanged runner summary](retained-advance-debug-comparison/paired/summary.json)
has SHA-256 `502d10794b07a3fd528ca4ef9091c71cf133c210bd7f92091a13671841dce75b`.
One bounded invocation uses the preserved 1,065-check debug driver/scorer, five
alternating pairs, sixteen edits and a retained branch at edit one. All ten runs,
160 edits, fifty selected-artifact executions and fifty independent canonical
artifact replays qualify. Exact paired bytes, old baseline and retained branch
observations match. Every trial remains recorded; there were no retries.

| Median or Deterministic Counter | Retain History | Advance Current |
|---|---:|---:|
| Post-first-fork edit plus release time, ms | 2.050 | 1.421 |
| Copied semantic payload, all edits, bytes | 8,128 | 1,016 |
| Copied semantic payload after first fork, bytes | 7,620 | 508 |
| Reused edited units after first fork | 0 | 14 |
| Whole-driver peak RSS, KiB | 18,980 | 19,036 |
| Whole-driver wall time, seconds | 0.55 | 0.60 |

The median paired advance/retain edit-time ratio is 0.590 (range 0.526-0.854;
geometric mean 0.635). This differs from the ratio of separate arm medians.
The measured edit interval excludes initial compilation, rendering, codecs and
host observations. Advancing still retains the initial baseline and first branch;
it does not discard all history. Payload-copy counters are not total allocation
or RSS. Whole-driver time and RSS do not improve in these samples. This is a
shared-host debug ownership comparison, not fresh recomputation, release speed,
full-library performance or a confidence interval. No new driver was introduced.

## Public Factory Integration

The [focused integration receipt](run-2026-09-19T16-13-23.731Z/receipt.json), SHA-256
`d75730228d1ad58920a7fe72c4053b3207962d1c8f0f3e9923dc2a0824e270db`,
passes 89 affected Rust checks, CLI checks and the original direct/search/edit/native
consumers. Input digest is
`754b8b3cb6ffe876a598c1a51e3c739573d643606a7d7b595fbf4d81ada5e1d7`;
before/after inputs match. This test-only slice does not rerun or replace the
previous 1,065-check production checkpoint. Compiler, scorer and example binaries
are byte-identical to that checkpoint; only the library test binary changes.
All five binaries are preserved in
`/tmp/lilscript-public-integration-baseline-20260919/`.
The test build took 40.1 seconds, the integrated JS cohort 27.8 seconds and the
native callback cohort 4.0 seconds. These are debug verification timings.

The [combined JS witness](run-2026-09-19T16-13-23.731Z/public-combined-oracle.json)
has SHA-256 `ed5201c360fbca4b8c6bada249222df31cb612f667e37a40912c68391bd8f0f0`.
The public path factory parses the unchanged twelve-module fixture once, seals
its zero-proposal baseline, then qualifies two existing manually selected recipes
in three naming styles. Each combines record scalarization, helper inlining,
shared string data, caller/callee product layouts and field-call transport.
Selected candidate handles are disposed before scoring; immutable records retain
complete recipe details. All six outputs pass original observations and independent
raw/gzip/Brotli replay after qualified terminal handoff. Each required family veto
rejects the corresponding artifact. The compiler ledger returns to zero.

| Recipe | Naming | Raw | Gzip | Brotli |
|---|---|---:|---:|---:|
| shared-caller-callee-fields | Global | 18,125 | 4,243 | 3,876 |
| shared-caller-callee-fields | Scoped | 17,956 | 4,036 | 3,686 |
| shared-caller-callee-fields | Source | 19,010 | 4,317 | 3,939 |
| shared-caller-callee-fields-inline | Global | 18,090 | 4,234 | 3,866 |
| shared-caller-callee-fields-inline | Scoped | 17,918 | 4,031 | 3,695 |
| shared-caller-callee-fields-inline | Source | 18,956 | 4,303 | 3,911 |

The service baseline is 18,232/4,229/3,863 bytes. Among the six combined outputs,
extra scoped helper inlining wins raw/gzip but loses nine Brotli bytes. This is
manual finite qualification, not an automatic search or optimum claim. Optional
work is 2,807,714 logical units and the admitted retained peak is 8,416,010 bytes;
independent runtime/codec replay and test metadata are outside those counters.

The [native public-factory witness](run-2026-09-19T16-13-23.731Z/native-public-factory.json)
has SHA-256 `8b34eff08be926555cd4a5e06a448f7fa2a6603f16f9c1ebf6ffb1dc1c2045eb`.
Original callback factory and reentry fixtures pass source/path factory delivery,
common C/header admission and zero-copy qualified handoff. Seven host bindings,
ABI/callback versions, receipt invalidation and exact file-capacity release are
checked. Factory finalization and a zero ledger precede execution. Each fixture
passes eight separate-translation-unit GCC/Clang O0/O2/UBSan/ASan-LSan profiles
and the existing JS host oracle. Native requests perform no JS codec work.
C/header sizes are 18,386/8,101 and 20,849/8,101 bytes, not executable-size claims.
Runtime evidence at admission remains explicitly unknown, not inferred from
later observations.

The [delivery comparison](public-integration-delivery-comparison/receipt.json),
SHA-256 `4017725be66715b282d8093b4b30ad4abf45662f76d5fa0373383d7efa2fc012`,
finds all fifteen original-consumer JS/C files unchanged. General public value
struct adapters, complete frontend accounting, language/library coverage and
milestone prerequisites remain open. No production policy or heuristic changes.

## Multi-Unit Atomic Batches

The [63-check focused receipt](run-2026-09-19T16-31-37.931Z/receipt.json), SHA-256
`8bae37388c19b65f10ecfc99184192f264be3f9e2033f7ccd2d0cdca59c859a5`,
qualifies three new tests plus nearby edit/publication/index/facts/rewrite tests.
Input digest is `06e5d55144f107d4f4acbad5896021211bc986e2ad69b8eecb7678b400994d8c`;
before/after inputs match. Only the test owner and qualification runner differ
from the public-integration compiler-input manifest; production code is unchanged.
The preserved library test binary in `/tmp/lilscript-multi-unit-baseline-20260919/`
has SHA-256 `6c35b04eb852de3a061ab9e92f0a491bab9d96b5ae12845e5f63060f54ef7560`.

The same existing transaction now has executable witnesses for two-unit batches:

- Fork/shared advance copies both edited units; unique advance reuses both.
  An unchanged unit keeps its cached facts. Full rebuilt indexes, including call
  mappings, match incremental indexes; Node observes both changed results.
- A semantically invalid second replacement is rejected after both swaps.
  Fork and unique-advance rollback preserve both revisions, backing addresses,
  index contents, original output and retained charges. A valid retry succeeds.
- Calibrated work/memory refusal and existing Applied/Prepared unwind and
  Prepared-deadline hooks publish nothing and release temporary owners. The
  memory bound uses final retained growth minus one, not an exact peak claim
  or proof that memory refusal occurs after mutation. Explicit hooks cover
  late rollback. Old artifacts still execute after checkpoint disposal.

Every owner finishes with a zero retained ledger. These are inspection/artifact
lifetime and source-edit tests, not new public-factory admission or codec claims.
No optimizer, transaction implementation or language contract changed.

The runner now accepts repeated `--test-filter=<Rust fragment>` arguments as a
tests-only mode, mutually exclusive with the existing integration cohorts. It
builds once, rejects empty matches, continues collecting filter failures and
always records final input stability. It does not rebuild CLI/example binaries or
rerun unrelated codec/library sweeps. Four invalid argument forms were rejected
in a separate syntax/argument diagnostic; current control flow was independently
reviewed. First incremental compilation took 38.1 seconds. The corrected filter
invocation reused that binary (Cargo 0.36 seconds); its 63 checks took 1.89 seconds.
This is verification overhead, not compiler performance.

## Release Integration Checkpoint

The [release integration receipt](run-2026-09-19T16-36-23.430Z/receipt.json), SHA-256
`020c288f5b0e6bafc016e12cbb6d5a7a7ab53b9b28a32444c584c9781abbf533`,
passes the same 89 affected Rust checks, CLI tests and original direct/search/edit/
native consumers. Input digest is
`2e140666dd1cb5fd2f0cf82d0256cc0ed15e630a1defaaf4eca549c887e1e2f3`;
before/after inputs match. No production compiler source changed for this build.
This does not claim that the full 1,065-check debug cohort ran in release mode.

The existing qualifier now accepts `--profile=debug|release` (default debug).
Both Cargo commands receive the selected profile. Receipts capture effective
artifact profiles and features; release requires optimization level 3 with debug
assertions and overflow checks disabled, as configured in Cargo.toml. Inherited
compiler/profile overrides are recorded and rejected for this release witness.
Two Cargo jobs limit concurrent build pressure. The control-flow diff was
independently reviewed; this is not a second qualification framework.

Five verified binaries are preserved under
`/tmp/lilscript-public-integration-release-baseline-20260919/`:

| Binary | SHA-256 |
|---|---|
| Compiler | `3d8e450590247c53f928a7bc2eceb1ac50b60dfb21464c6629a55e8a813ea00e` |
| Codec | `d55c6f33119cb11153f21145f45058ef3ed188c04554cc198af242f4b12e1ef9` |
| Integrated driver | `e61d3d9fc6ab5265b1a876667ed16cac02cce44a42d325cc7a491bf5d6f414d1` |
| Library tests | `08370cb8e46c1c027e50f450d0e9b646fa7593a2981ea1fe36715c950d4498ff` |
| CLI tests | `8331a7d937d5e4f4cf26de839daf38487f5289d08c51b187d60144acecf8a834` |

The [delivery comparison](release-delivery-comparison/receipt.json), SHA-256
`684a550fa3b614834105f1d4a2f29283aa2a9040e688f7b0fff28b89481fd6b4`,
finds all fifteen original-consumer JS/C files identical to the debug integration
checkpoint. The entire six-output combined-JS oracle also has the same SHA-256
`ed5201c360fbca4b8c6bada249222df31cb612f667e37a40912c68391bd8f0f0`.
Both native C/header payload pairs are byte-identical; their receipt differs
because execution/build metadata differs. Its SHA-256 is
`cd77926f6a83bdc74293d0d0582926b396db32d852b0a99c0b8aac68db71e771`.

The first release test build took 372.4 seconds; CLI/example linking took 9.7
seconds. Integrated JS and native callback cohorts took 14.8 and 2.9 seconds.
These are build/verification observations on a shared host, not paired compiler
performance or a speed gate. Reuse these source-pinned binaries for bounded
library baseline work; original library tests must qualify their actual outputs
separately. Default library routing remains legacy and all milestone limits remain.

## Template Storage And Target Reuse

The [512-check affected debug receipt](run-2026-09-19T17-23-39.205Z/receipt.json)
has SHA-256 `c73f54c55c7ad906a6820568f9745e9bdf63359651c498383a2e8dbf99619a3e`.
Compiler/build/test inputs remain stable at digest
`227482f94832b3aae2943cc3badf214a9583eebfa80d3f1db3394c1a1dac21a8`.
Schedule identity is 13; resource format remains 3. The 27 recorded filters cover
source identity, lexer/parser/modules/checker, service/policy, source conversion,
publication, search, artifact ownership, integrated original JS consumers and
affected target naming/literal/printing contracts. No full suite, CLI/example
rebuild, dedicated native callback matrix, release build or library-fleet run was requested.

- SourceIdentity uses an opaque inline stamp and node count instead of an Arc.
  Clone/equality/debug behavior and cross-thread freshness are tested. Both old
  shared-allocation allowances are removed; typed owner capacity includes the
  changed inline size (8 to 16 bytes on this target). This removes an allocation,
  not a guaranteed reduction in arena peaks or every module-vector capacity.
- The existing lexical owner admits template frames, interpolation vectors,
  exact boxed-copy overlap and template-table growth. One generated tokenizer
  and one scanner preserve both public Logos and wrapper behavior. Tests cover
  fixed tokens, spans/extras, nested templates, syntax/resource/deadline refusal,
  unwind and overlapping parent/child parser storage. Ordinary Logos traversal,
  parser recursion/lookahead, diagnostics and remaining checker/metadata owners
  are still explicitly outside complete admission.
- Search retains one immutable direct target through mandatory and optional
  naming, with fresh per-domain naming budgets. Baseline naming storage drops
  before sealing; the reused target stays charged until continuation ends, then
  drops before structural discovery. No persistent target cache, budget-domain
  switch or generated-JS rescan is introduced. Naming-only continuation proves
  one successful formation and two prepared naming bases for three styles.
- Seven new search tests cover prior exact artifacts, codec replay and public
  name/arity/results; literal-only continuation; zero optional work; Optional
  work refusal; observer panic; distinct structural children; and ordinary public
  service handoff. Existing memory-refusal, staged-scoring, independent winners
  and structural-loss interaction tests also pass. Test lifetime observers wrap
  private callback-owned values without modifying public model/output types.

The [prior executable reference](target-reuse-before/identity.json) pins retained
logs, its accepted parent and both disclosed executions. The compared Global and
Scoped answer artifacts remain 74 raw / 77 gzip / 61 Brotli bytes; Source naming
remains 79 / 84 / 72. These are unchanged finite-fixture results, not new size wins.
The new binary is preserved at
`/tmp/lilscript-template-target-baseline-20260919/lilscript-c737e884cfca95f9`, SHA-256
`a78bd936cb375bc079f6c0993a37f7795c05b8917822a3b1b3174d6be9ccfaba`.
The corrected debug build took 83.1 seconds and selected execution 49.0 seconds.
This is mechanism/correctness evidence on a shared host, not measured Marked or
release speed. Prior release evidence remains tied to its unchanged older inputs.

## Checker Signature And Scratch Ownership

The [243-check affected debug receipt](run-2026-09-19T17-50-43.603Z/receipt.json)
passes with stable inputs. Receipt SHA-256:
`aef29ac6c83fca826c4a2db8f3c15ee37c48177a9c441fcac8727229a2b86bdc`;
compiler-input digest:
`0cd3bf95d760ef6440072743bf8c7e8ba3201aa15d9782b055bafe1d41852fee`.

The existing checker's default-metadata predicate now serves both finalization
and stripping. Stripping checks borrowed signatures before calling
`Arc::make_mut`: default-free signatures remain shared, changed ancestors detach,
and unaffected siblings retain their original backing. Five focused tests cover
all existing type containers, ordinary/generic signatures, return-only nested
defaults, pending/resolved metadata, source arity and idempotence. Existing
finalization cases remain in the executed cohort.

The original module checker drops its interface-resolution graph and local
preflight maps after installing interfaces and scopes. Completed body scopes no
longer return to the pending-scope vector. Shared declarations, initialization
state, facts and unprocessed module scopes retain their existing owners. The
existing cyclic live-import/reexport case additionally lowers and executes the
checked graph in Node, observing `2\n2\n`.

The cohort contains 145 semantic checks, 25 module checks, 33 public-service
checks, eight source-lowering checks, two module-publication checks, four module
JS checks, eight integrated JS checks, ten callable-contract checks, six
parameter-contract checks, and two focused legacy default-lowering checks.
All 243 names are distinct. No assertions were weakened. Independent production
review found no correctness issue. The initial build and all filters passed;
there was no failed build/test retry in this slice.

One debug library build took 40.5 seconds and selected execution 39.6 seconds on
the shared host. This is not a controlled timing comparison. The qualified
test executable is preserved at
`/tmp/lilscript-checker-ownership-baseline-20260919/lilscript-c737e884cfca95f9`,
SHA-256 `0c66aa666d25b0298182e91aace2193e883d3e5111da41b950987270e9dadfc0`.
CLI/example and release binaries were not rebuilt; dedicated native callback
matrix, full-suite and library-fleet qualification were not rerun. Schedule
identity remains 13 and resource format remains 3: this reduces unadmitted
checker ownership, without changing the charged work schedule or adding an
allocation-accounting claim. Checker maps, nested types, interface storage and
remaining frontend work still need admission. No measured whole-library speed,
RSS or output-size improvement is claimed.

## Module Interface And Schedule Admission

The [298-check affected debug receipt](run-2026-09-19T18-07-51.312Z/receipt.json)
passes with stable inputs. Receipt SHA-256:
`850f9a43296f7c30752dfc78cb6f3333a20d1f013555dcd6017e20f8a1a478ef`;
compiler-input digest:
`4283fc3c5b11c85cb312c136a12f5c12d781b00b04e0958c57afc4fb3140f8c0`.

Public inspection and admitted module checking now enter the same private
`analyze_modules_in`. The admitted callback's existing allocation scope starts
before graph validation. The shared iterative static scheduler charges its
returned order to that scope; traversal state and iterator-stack storage drop
in the scheduler's existing child scope. Ownership, dependency and order checks
charge actual work. No second graph algorithm, semantic model or ledger exists.

The checker pre-admits the interface vector and each module's dependency,
import and export vectors from their exact source counts. Imports count
specifiers, not declarations. The existing alias resolver writes those rows
through the same budget, without growing the preallocated buffers. Dense facts,
interfaces and order remain charged through conversion; actual checked storage
drops before scope release. The public unmetered wrapper preserves its semantic
diagnostic API, while admitted failures keep their typed resource errors and
owning module identities.

Three new tests and revised exact-cap tests prove:

- Exact live/peak capacities and canonical identities, with a nonzero root,
  repeated dependencies and a side-effect-only import.
- Eight pre-allocation refusal boundaries, including scheduler scratch and
  later-module interface backing, without releasing older parent reservations.
- Seven work cutoffs with precise module attribution, including partially
  constructed interfaces and a refused final exported row.
- Callback error, panic and deadline cleanup, preserving a real parent string
  until its own release; invalid-root/dependency/order diagnostic parity.

The cohort contains 148 semantic, 25 module, 33 service, 31 policy, eight policy
evidence, eight source lowering, five module contract, two module publication,
four module JS, eight integrated JS, four formation-budget, nine native callback
and thirteen allocation-owner tests. All 298 test names are distinct. One build
and all filters passed without retries. Existing JS/native observations remain
passing; CLI/example delivery comparisons, release and the fleet were not rerun.

The debug library build took 40.5 seconds and selected execution 42.2 seconds
on the shared host. No timing improvement is claimed. The executable is preserved
at `/tmp/lilscript-module-interface-baseline-20260919/lilscript-c737e884cfca95f9`,
SHA-256 `8574db6d251cbcf5883809c781da2a17d82c9a7eaea23b042903a49039a6ae54`.
Schedule identity is now **14** because admitted work/peak accounting changed;
resource format remains 3. Larger reported memory is newly covered actual
storage, not a relaxed cap. Nested types, hash/ordered maps, declarations,
alias/local/scope scratch, diagnostics and comprehensive checker work remain
unadmitted. This is not whole-frontend/RSS admission, a size/speed win or a new
prerequisite for milestone 006.

## Canonical Binding Type Ownership

[Accepted affected-test receipt](run-2026-09-19T18-30-06.962Z/receipt.json):
**393 distinct Rust library tests passed**, including seven new regressions.
Receipt SHA-256:
`248b065b241bcb6c6b4ad9643d311ddc5d8325f403060b8e84248276e5d1877d`.
The 970-file input manifest is unchanged before/after, digest
`af7b1fdd4172e4f5f3600d4f386be582a6e10fea50d78b149f14dc3bb6e30ce7`.

The existing private binding-span table now stores canonical symbol identities
for value bindings, detached parameters, equal foreign declarations and module
import aliases. `binding_type` borrows the type already owned by that symbol;
ordinary identifier-use spans still have no binding entry. Type-only nominal
entries and the existing class-merge entry retain inline types. Declaration
types are moved once rather than cloned into both owners, and default
finalization no longer traverses the removed value-binding copies.

Tests verify actual nested Box/Vec addresses, binding versus use spans, nominal
exports, finalized defaults, cloned-model independence after the original drops,
four aliases across a reexport chain and distinct foreign-parameter identities.
The new entry and complete map-row layouts are no larger than their previous
Type-based equivalents on this build. Existing foreign-conflict, cyclic-module,
interpreter, lowering, JS and native observations remain passing.

The thirteen filters execute 155 semantic, sixteen lowering-fragment, 26
interpreter, five direct JS, 118 structured JS, 33 service, eight source lowering,
five module contract, two module publication, four module JS, eight integrated
JS, four formation-budget and nine native callback tests. The lowering fragment
includes one structured-lowering worklist test; actual names are all distinct.
One debug library build took 74.8 seconds; selected tests took 51.9 seconds.
There were no failed attempts or retries. CLI/examples, delivery comparisons,
release and library/profile fleets were not rerun.

The executable is preserved at
`/tmp/lilscript-binding-type-baseline-20260919/lilscript-c737e884cfca95f9`, SHA-256
`2eb34507512cbce5db40a2724d4d512f5a58c4ed7ca93e4e1415aa3a9f7a7f52`.
Schedule 14 and resource format 3 are unchanged. This removes duplicate
ownership, but does not newly admit checker maps or the remaining nested type
payloads, and establishes no whole-library speed, RSS or compression benefit.
The preserved release used for Probe remains a distinct older source identity.

## Lexical Work Ownership

[Accepted affected-test receipt](run-2026-09-19T18-42-49.317Z/receipt.json):
**246 distinct Rust library tests passed**, including four new regressions.
Receipt SHA-256:
`33ddb85b1e78ee0ef3e1d57bd145b3b64f546b2e7642176283978f705cbc2adc`.
The 970-file input manifest remains unchanged before/after, digest
`9514c49ed06f38a097e320f02a2f5a25aa88b078c148b9bd15b274a485a29db0`.

The existing lexer prepays the complete input-byte Analysis tariff for every
admitted lexical stream. Moving the source factory's old pre-parse tariff
avoids charging it twice; path-based and nested-fragment lexing now pay their
own tariff. File reading, stable-source copying, path metadata hashing, checker
entry and conversion charges remain separate and unchanged. Template scanning
retains its additional detailed byte work. This is logical accounting, not an
exact CPU-operation count.

A zero-work checkpoint follows every raw Logos call, including ordinary
tokens, malformed input and skipped-trivia EOF. It can reject an expired
deadline when the call returns, but cannot preempt an individual Logos call.
The ordinary inspection lexer and public Logos interface retain their grammar
and unmetered behavior. No scanner, prescan, counter or resource owner is added.

Three new lexer tests verify exact UTF-8/trivia tariffs, pre-token-allocation
refusal for long literals and malformed input, unchanged borrowed token/error
results, and post-call deadline cleanup while a real token vector stays live.
One new parser test refuses a nested leaf's input bytes without changing either
parent stream or arena/peak storage. Existing tests now check exact child/leaf
work and retain the exhaustive below-budget template refusal loop. The source
service calibration removes only its relocated pre-parse charge. No memory
ceiling increases or assertion relaxations were needed.

Fourteen filters execute 22 lexer, 61 parser, 25 module, 33 service, 31 policy,
eight policy-evidence, fifteen checker-admission, eight source-lowering, five
module-contract, four module-JS, eight integrated-JS, four formation-budget,
nine native-callback and thirteen allocation-owner checks. One build and all
tests passed without retries. Debug build/test durations were 34.5/42.4 seconds
on the shared host, not a speed comparison. CLI/examples, dedicated delivery
comparisons, release and the library fleet were not rerun.

The binary is preserved at
`/tmp/lilscript-lexer-work-baseline-20260919/lilscript-c737e884cfca95f9`, SHA-256
`7276b9fef34d967297e09f0a942698d02684d3ff9d94a6b71702056f2c7143c7`.
Schedule **15** identifies newly admitted path/nested lexical work and earlier
refusal; resource format remains 3. Parser recursion/lookahead, remaining
checker payloads/traversal and metadata/diagnostics remain incomplete. Source
receipt hashing is not newly admitted. No hard deadline, whole-frontend, RSS,
speed or compression benefit is claimed.

## Parser Lookahead Work

[Accepted affected-test receipt](run-2026-09-19T19-06-28.962Z/receipt.json):
**264 distinct Rust library tests passed**, including six new regressions.
Receipt SHA-256:
`1d407039e3df68ff2c66643360f59029265147b54d96a316affbd856838360d3`.
All 970 compiler inputs remain unchanged before/after, digest
`99b9f69cb9057dd4e417a7b008383b123461a379eb9033a325c88c153428581b`.
The preceding 246-check receipt is the prerequisite; before-edit files are at
`/tmp/lilscript-parser-lookahead-before-20260919/`.

One private token-probe helper charges the existing parser admission owner
before each arrow or type/reference lookahead inspection, including existing
EOF probes. The four private decision helpers propagate typed resource
refusals; malformed lookahead still returns an ordinary mismatch. Original
EOF short-circuits remain intact. Public inspection without admission keeps
the same grammar. No cache, counter, prescan, speculative AST, independent
ledger or new depth threshold is introduced.

Six tests cover exact token/EOF tariffs, every lower work cutoff across fourteen
recursive type fixtures, final probes in seventeen arrow/binding/reference
decisions, deadline refusal with live parent template storage, and structured
AST/error parity. Actual nested grouping demonstrates `n * (n + 3)` repeated
arrow probes and refusal before the innermost syntax is constructed. Cursor,
token/arena pointers, retained/peak memory and lifetime-ordered cleanup stay
asserted; no old assertion or memory cap was relaxed.

The same fourteen affected cohorts as the lexical checkpoint pass, with parser
coverage increasing from 61 to 67. Four formatter and eight lint checks also
pass because they share the public parser. One incremental build took 33.8
seconds and selected tests 41.6 seconds on the shared host, with no retries.
These are diagnostics, not a performance comparison. CLI/examples, dedicated
delivery comparisons, release and the library fleet were not rerun.

The 413,234,248-byte test binary is preserved at
`/tmp/lilscript-parser-lookahead-baseline-20260919/lilscript-c737e884cfca95f9`,
SHA-256 `30afaa6131f07770d880baa7c9fd973d92f43eff2c50248e330e12ab8114629b`.
Schedule **16** identifies the coherent work-accounting change; resource format
remains 3. This admits repeated scans; it does not eliminate them or bound
stack depth. Comprehensive traversal/recursion, escaping diagnostic storage,
remaining checker payloads and metadata remain outside the accounting claim.
No hard-preemption, whole-frontend, RSS, speed or compression win is claimed.

## Canonical Class Member Ownership

[Accepted affected-test receipt](run-2026-09-19T19-26-36.403Z/receipt.json):
**409 distinct Rust library tests passed**, including five new regressions.
Receipt SHA-256:
`3fe0dae73a567a8bae77239a579818798584ca92c35c63dc229abdee3761ffd6`.
All 971 compiler inputs remain unchanged before/after, digest
`90ed4899e1dac470a6066bdc4869a5729abcf210737c720e2fa1bd98354be56a`.
The preceding 264-check receipt is the prerequisite; before-edit source is at
`/tmp/lilscript-class-ownership-before-20260919/semantic.rs`.

The existing checker now keeps one field map and one method map per class,
instead of duplicate declared/effective maps. Definitions finish before the
sole hierarchy pass; its existing completed set distinguishes resolved classes.
Resolution borrows base metadata, constructs required inherited substitutions,
validates collisions in the original order, then moves the class's own payloads
into its effective maps. Whole-class scratch clones are removed. Default
finalization visits the same effective members without duplicate declared-map
visits. No new semantic authority, cache, ledger or resolution pass is added.

Five tests check nested Box, generic-parameter Vec and callable Arc identities;
merged object member order and nominal identities; three-level generic
substitution and declaring slots; own/inherited/object/extern defaults; and
seven exact hierarchy-error cases with unchanged own-member storage. Necessary
inherited instantiated payloads remain independent. Existing partial nominal
slot updates before a later error are preserved, not newly promised as API.

All semantic (160), lowering (16), interpreter (26), legacy JS (5), structured
JS (118), public service (33), source conversion (8), module contract (5),
module publication (2), module JS (4), integrated JS (8), formation budget (4)
and native callback (9) checks pass. Eleven selected existing compiler/IR-JS/
native tests additionally cover public constructor identity, inheritance,
syntax floors, host member names, public layouts/defaults and native boundaries.
The 22 filters are non-overlapping; no failures, ignored tests or retries occur.

One incremental build took 48.0 seconds and selected tests 55.4 seconds on the
shared host. These are diagnostics, not a speed comparison. CLI/examples,
dedicated delivery comparisons, release and the library fleet were not rerun.
The 413,311,528-byte test binary is preserved at
`/tmp/lilscript-class-ownership-baseline-20260919/lilscript-c737e884cfca95f9`,
SHA-256 `ac954a3d3f092beaac8bd70fb15f530db0ec3fa36187eee52f8c424282ee0499`.
Schedule **16** and resource format 3 are unchanged: this removes redundant
ownership but does not add allocation admission. Remaining checker payloads,
maps, traversal and diagnostics still need accounting. No measured RSS, library
speed, compressed-size improvement or whole-milestone qualification is claimed.

## Unsafe Assignment Sink Retirement

The unchanged Mdast source-built attempt exposed wrong retained values in
generated JS. The same optional token-sink implementation was present in that
preserved release and current sources. Independent review also found unsafe
movement across reads/coercion, shorthand-object rewriting and arrow-binding
rewriting; its caller already documented an unresolved Katex miscompile.

[Red receipt](run-2026-09-19T19-43-24.565Z/receipt.json), SHA-256
`9015da23b61650eb4aedfb29f112dc927bcb978018fc9de0f26752e8f6b618c1`,
records one passing and five failing new tests against the unchanged owner.
All 972 inputs stayed stable, digest
`ff83569af4948094cc010d4eb812ec6051d5d61560df29e632b5f6d29607e7a1`.
Original JS observations pass; both declaration shaping and the general
optimizer expose failures. This is an intentional regression witness, not a
failed build or an unchanged library retry.

The optional sink, its two exclusive helpers and both production invocations
are removed. The existing implicit-declaration pass remains at the same
pipeline position. Other sequence/statement-first-use rewrites are unchanged.
Three private implementation-shape tests are retired with the deleted rule;
their source is preserved at `/tmp/lilscript-assignment-sink-before-20260919/`.
The six replacement tests retain the old five runtime programs' behavior and
assert independent fixed observations through both production entrypoints.
No replacement token heuristic, new owner or package-specific exception is added.

[Green receipt](run-2026-09-19T19-45-46.277Z/receipt.json), SHA-256
`e86609dd04ce096a2809bf2d290fe00b8b741797bf1fbd01085431c47d2fb242`,
records **611 distinct passing tests**: 600 JS peephole and eleven affected
compiler tests. One pre-existing manual artifact diagnostic remains ignored;
there are no failures. All six new tests pass without changing their reviewed
source SHA-256 `103832ffbbca82c71efaaa45f247fbc07b53ac187aa92a2a7866372a56cdecab`.
All 972 current inputs match both manifests, digest
`e5a33cae693ff58a818bfc395a55bc6d958b6bf21c0679ae82e39f86311a9554`.
The green build took 31.0 seconds and selected execution 5.1 seconds; these are
shared-host diagnostics, not a compiler performance comparison.

The 413,313,176-byte green test binary is preserved at
`/tmp/lilscript-assignment-sink-retirement-baseline-20260919/lilscript-c737e884cfca95f9`,
SHA-256 `203525ac979def653df7c96c67b9bf97a02de8ffb84e083ace01d6a2a0998ea9`.
The failed Mdast receipt remains failed. This checkpoint alone establishes no
whole-library repair, release qualification, output-size stability or gain;
only a newly qualified source build can test that boundary again. Schedule 16
is unchanged; no resource-accounting behavior is added by this retirement.

## Post-Retirement Release Checkpoint

[Release receipt](run-2026-09-19T19-48-31.223Z/receipt.json), SHA-256
`8918faac11122e916d8c7decbdfe03d71f3b873c6dc990691a2709fe498e1a39`,
passes **89 public-integration library tests and one CLI unit test**, plus the
original direct/search/edit/native example consumers and CLI delivery checks.
All 972 compiler inputs match the preceding 611-check debug checkpoint and
both release manifests, digest
`e5a33cae693ff58a818bfc395a55bc6d958b6bf21c0679ae82e39f86311a9554`.
The five producer/test artifacts use opt-level 3, no debug info, disabled debug
assertions and overflow checks, and no feature/profile overrides.

[Cached focused receipt](run-2026-09-19T19-56-07.064Z/receipt.json), SHA-256
`c9fdb6e0672d0c97d89b4dd60b16e6c6a4cf2ad9d409dbb19d8f3abd18eee94b`,
additionally passes the six assignment-sink and five class-ownership tests on
the identical release test binary. Cargo reports the artifact fresh; no second
compilation occurs. Both input manifests remain identical. Neither release
cohort has failures or ignored tests.

Independent review checks actual test identities, all five binary pins and
the complete current input set. All fifteen compared JS/C delivery files are
byte-identical to the earlier 16:36 release checkpoint. The fourteen JS
artifacts retain their independent expected observations and canonical
raw/gzip/Brotli scores; native output matches its original oracle and final
retention is zero. Six public combined-recipe JS rows and two public native
rows pass. CLI stdout/file deliveries match, codec replay agrees at 75 raw /
85 gzip / 79 Brotli bytes, development mode makes no optional proposals, and
unsupported input remains an explicit diagnostic.

All five binaries are preserved at
`/tmp/lilscript-assignment-sink-retirement-release-20260919/`:

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| Compiler | 18,000,040 | `e0c4cb31f2bf306512767d296e8411f58652e52f12ad158a4d7733acc60712bd` |
| Codec | 2,618,064 | `2c9683d9b80746472f687dad7758075cdb9519032ecd3e5c48ec93e81bb50094` |
| Integrated example | 8,907,640 | `87fc388b1a9810a93bc1f10b348f8eb83e7dec6c930b6409abd0726282254e1f` |
| Library tests | 39,485,112 | `29659ada78cee37d3e52aef2d5ec4a49d08d2fd81fdaa70b300acb9577957dfd` |
| CLI tests | 1,758,832 | `8331a7d937d5e4f4cf26de839daf38487f5289d08c51b187d60144acecf8a834` |

The test build took 359.2 seconds; building the CLI/codec/example took 10.2
seconds. These are shared-host build diagnostics, not compiler-performance
measurements. Schedule 16 and resource format 3 remain current. This release
qualifies a parent for a separately bounded changed-source Mdast replay; it
does not by itself repair that library, establish competitive compression or
close a migration milestone. No full library/profile sweep was rerun.

The subsequent [Mdast source-built replay](../2026-09-19-mdast-to-hast-baseline/README.md)
passes both original compilations, type checking, all 152 frozen identities and
the separate package dry-run on this parent. Every previously failing identity
passes. Exact loaded/scored artifacts, all 121 retained outputs and complete
inputs are independently verified. This closes the bounded original-boundary
remediation, not remaining delivery/semantic-backend qualification. The earlier
failed receipt stays failed; its incorrect output is not a compression baseline.

## Constructor Metadata Ownership

[Affected-test receipt](run-2026-09-19T20-16-16.663Z/receipt.json), SHA-256
`9370386fee70dd4ea2b2d738e98e2607c13299eacea7945378c0558c15cdfa91`,
records **298 distinct passing Rust library tests**, with no failures or ignored
tests. All 973 inputs match before/after, digest
`51f23bccba5fc6faeb63739f7116ffdec3a53cff098210a39d0c32111eb29666`.
Before-edit `semantic.rs` remains at
`/tmp/lilscript-constructor-ownership-before-20260919/semantic.rs`.
All 972 pins from the accepted 19:48 release were checked before editing.

Constructor validation, the two `super` metadata lookups and `new` checking no
longer clone whole `ClassInfo` values. These paths borrow class/base metadata;
`new` retains only type-parameter names and the existing shared constructor
signature across mutable argument checking. Required inherited substitutions
remain. Constructor bindings consume parameter types already resolved before
default checking, and constructing the `this` type borrows parameter names.
Validation, source-resolution publication and diagnostic order are preserved.

Five new tests cover repeated inferred/explicit/contextual construction, nested
unrelated member types, three-level generic inheritance, explicit and omitted
`super` defaults, implicit constructors and twenty exact diagnostic/span cases.
The frozen test source SHA-256 is
`2ff9312ecf0b8581d8c956c2e6d37542a288787d69d1d7f8aa4e674952a75d1a`.
Removal of temporary whole-class copies is a source-reviewed invariant, not a
claim inferred from unchanged canonical allocation addresses.

The 23 non-overlapping filters cover semantic checking (165), lowering (16),
interpreter (26), legacy JS (5), public service (33), source conversion (8),
module contract (5), module publication (2), module JS (4), integrated JS (8),
formation budget (4), native callbacks (9) and thirteen selected compiler/IR-JS/
native checks. Existing runtime observations include public constructor identity,
inheritance, defaults and an imported constructor in a module cycle. Unrelated
structured-target formatting tests and library/profile sweeps were not rerun.

One incremental build took 41.4 seconds; affected execution took 55.0 seconds.
These are shared-host build/test diagnostics, not a performance comparison.
The 413,419,192-byte debug test binary is preserved at
`/tmp/lilscript-constructor-ownership-baseline-20260919/lilscript-c737e884cfca95f9`,
SHA-256 `0d5b0e0fcb003907e09bbf17cde0c3f78d0285646542ad88b51d810588263eb5`.
It uses opt-level 0, debug info 2, enabled debug assertions/overflow checks and
no feature/profile overrides. No retry or second build occurred.

Independent review re-enumerates the full current input set, verifies both
binary copies and all 298 identities, and confirms that the only changes from
the release prerequisite are `semantic.rs` and the new focused test file.

Schedule 16 and resource format 3 are unchanged. No new semantic owner, helper,
cache or resource ledger was introduced. This removes redundant ownership and
resolution work; remaining checker allocation admission, release/fleet coverage,
measured speed/RSS and competitive compression remain separate requirements.

## Canonical Declaration Admission

The [348-check debug receipt](run-2026-09-19T21-17-15.408Z/receipt.json),
SHA-256 `c99b9df51c2eacdf8ba12886ad63ea252c4e13308f914a47952f6a61c94979ec`,
qualifies the existing checker's four canonical vector backings: symbols,
symbol-module ownership, struct records and nominal-member records. Input
digest is `b368f233b2f4e8ba730618796a08861d097aff4fe55abcbd952b88319a67c74b`.
The C2 prerequisite's 973 inputs were independently re-enumerated before edits;
before-edit files remain in `/tmp/lilscript-declaration-admission-before-20260919/`.

`Analyzer` borrows the callback's existing `AllocationBudget`. All four vector
writers admit initial backing, growth overlap and publication through this same
owner. The paired symbol vectors reserve both capacities and both publication
operations before publishing either row or installing scope/source bindings.
A refused second reservation can retain the first grown capacity until cleanup;
logical rows remain unchanged. This is atomic publication, not capacity rollback.
No second checker, resource ledger, refusal side channel or guessed map tariff
is introduced. Module analysis preserves typed resource errors and canonical
module attribution. Unmetered public APIs retain their existing semantic errors.

Six new tests cover first/later growth, paired memory/work/deadline refusals,
unpublished scopes/source bindings, struct/member admission, detached symbols,
shared-module ownership and callback error/unwind cleanup. Expected capacities,
work and overlapping peaks are derived independently from element sizes and
known construction order. Existing exact-byte tests include the additional live
backing without relaxing caps. Nested payload pointers survive vector growth;
parent-owned storage survives child failure. Ordinary diagnostic/span assertions
and checked-model parity remain intact.

One `cargo test --lib --no-run` build and 25 non-overlapping filters pass:
semantic checking 171; the preceding C2 consumer cohort 133; policy 31; allocator
13. All 348 identities pass, with no retry or ignored test. Build time is 90.3
seconds and execution 68.7 seconds, shared-host diagnostics rather than compiler
performance measurements. No CLI, release build, library/profile sweep or
competitive codec qualification ran.

The 414,025,760-byte debug binary is preserved at
`/tmp/lilscript-declaration-admission-baseline-20260919/lilscript-c737e884cfca95f9`,
SHA-256 `3198fe57313de379efe30b5c7b9f03daf24cf67fa70645154f6e89a0606ec245`.
Its profile is opt-level 0, debug info 2, debug assertions/overflow checks enabled,
with no feature/profile overrides. Schedule 17 records the newly admitted work;
resource format 3 and the structural search algorithm are unchanged.

Independent review re-enumerates all 974 current input pins against both complete
manifests, verifies all 348 distinct identities and all six new tests, and checks
the preserved/current binary hashes. Exactly six existing input files change and
one test file is added relative to C2. All 30 commands finish normally without
timeout or forced pipe closure.

This admits vector backing, not nested `Type`/signature/default payloads,
declaration maps, Analyzer scope/continuation storage, diagnostics, debug
identifier-consistency scratch or comprehensive checker traversal. Those gaps,
the older release's distinct input identity and whole-migration prerequisites
remain explicit. No speed, RSS or compressed-size improvement is claimed.

## Rest Capture Reduction

The retained jQuery consumer failure motivates a generic regression, not a
package-specific repair. `compiler_rest_capture_tests.rs` checks a rest-bound
method with a nested mutable argument alias and a distinguishable enclosing
parameter. Two calls must preserve their own argument identities and the outer
value. One variant retains readers after clearing the alias and observes the
first invocation during the second; another only clears the alias after its
nested read. Dynamic indexing comes from an external host function, preventing
the fixture from exercising only fixed-index formal specialization. The host
also verifies exact callback/index-call counts against independent expectations.

The [first 10-check receipt](run-2026-09-19T21-29-13.790Z/receipt.json),
SHA-256 `52aa9ee69c62c38b23c1badbe40d41b64bc619fb055811e4272d718bbcfc777d`,
passes two initially fixed-index witnesses and eight nearby existing adapter,
capture and nested-name regressions. Its test source is preserved at
`/tmp/lilscript-rest-capture-before-20260919/static-capture-tests.rs`.
That first reduction did not establish dynamic-argument or unobserved-clear
behavior, so the fixture was refined rather than treating it as a diagnosis.

The [refined two-check receipt](run-2026-09-19T21-31-59.442Z/receipt.json),
SHA-256 `976301c525f05fb4466ef8505a29eb689d4a7d999e20b2b6edfa46d9971eaee2`,
passes both stronger fixtures across three emitter settings and two configured
pipeline settings: six emitter executions plus four pipeline executions. The
emitter settings cover unmangled ordinary functions, defaults and nested name
reuse; the configured pipeline uses effort 15, identifier mangling and search
Off/Production with the default Brotli objective. Both independent JS reference
implementations pass the same observations. These are ten generated variants,
not ten asserted distinct byte streams or size qualification.

The final test source SHA-256 is
`aec83b0715f3cc1a014e7512aa5811a44b8a8ffe6edc5e59b3ba2b2eecf5dcce`.
Only that test file changes between the two attempts. Each uses one incremental
build; the second reruns only the two changed tests, not the unchanged neighbors
or library fleet. Build times are 35.2 and 32.8 seconds; test times are 1.9 and
1.5 seconds. These are diagnostic host observations, not compiler speed claims.

The [read-only receipt audit](rest-capture-audit.json) re-enumerates all 975
current inputs, verifies both before/after manifests, exact test identities,
command completion, archived initial test source and the final preserved binary.
Current input digest is
`504cd39f6d3aff54c89c79f7408e6cd4810e4230656b9c32fa0f2d2ec9b7a813`.
Relative to C3, only `compiler.rs` test registration and the new test file change.
No production implementation, schedule, library source/configuration or assertion
is altered. The 414,235,016-byte debug binary is preserved at
`/tmp/lilscript-rest-capture-baseline-20260919/lilscript-c737e884cfca95f9`,
SHA-256 `8f2cc1d61241e7343aa6c3ddfb157f9e0a0c789ccde9c0a528954611ef8cfc50`.

**The generic reductions pass; the original jQuery consumer still fails.**
The historical artifact's responsible pass and full current-source behavior
remain unproven. A qualified full-source build still requires the recorded
configuration/delivery prerequisites. Do not infer a fixed library, a current
compiler defect, or license for a speculative capture/mangling patch from this
bounded result. No unchanged failing consumer was rerun.

## Analyzer Storage Admission

The [258-check final receipt](run-2026-09-19T22-57-57.506Z/receipt.json),
SHA-256 `eeb54c085c768a7cf20331eed5e48ed044a55ea0c56f4db46efccdbc0a371276`,
qualifies the six existing Analyzer vector backings: lexical scopes, narrowings,
type-parameter scopes, return contexts, constructor classes and generator contexts.
The same callback budget admits allocation, old/new growth overlap and row
publication. Both scope capacities and publication operations are admitted before
either logical scope is pushed. Partial reservation may retain capacity until
cleanup, without publishing half a scope pair.

Analyzer construction now propagates typed allocation errors, including canonical
module attribution. Each Analyzer drops its own backing before releasing its
charges, including partial construction and unwinding. Shared declaration backing
stays live across module passes. No new ledger, checker, allocation journal, map
tariff or persistent counter is introduced. The public resource report now states
the admitted declaration/context backing and remaining gaps separately.

Four new tests cover exact initial/growing capacities and work, paired-scope
memory/work/deadline refusal, partial callable frames, nested contexts and cleanup
after an expired deadline or panic. Existing exact-cap tests account for the
additional overlapping backing without relaxing limits; callback live-byte
expectations exclude already-dropped Analyzer storage. Module and public-consumer
tests retain diagnostics, behavior and parent-storage assertions.

The [first attempt](run-2026-09-19T22-54-09.513Z/receipt.json) built once and ran
354 distinct tests: 352 passed and two new fixtures failed parsing because they
spelled a constructor `Box()` instead of `init()`. That failed receipt is retained.
The corrected fixture also expects the actual seven declarations: the class,
`this` and five arrow locals. The only other changes before the final run update
the resource-report text and its exact assertion; Analyzer implementation is
unchanged between attempts. The final incremental build reruns six affected
cohorts: semantic 175, service 33, integrated JS 8, native closure 9, policy 31 and
captured-argument regressions 2. All 258 pass, none ignored. The other initial
cohorts were not rerun and are not described as final-input qualification.

Build times were 84.3 and 30.4 seconds; test execution was 50.2 and 34.1 seconds.
These are shared-host diagnostics, not compiler performance measurements. Each
command keeps the existing 600-second/64-MiB output bound; no timeout, forced pipe
closure, cap increase, full-fleet sweep or unchanged retry occurred.

The [root read-only audit](analyzer-storage-audit.json) checks all 976 input pins,
both stable manifests, exact test identities, before-edit snapshots and preserved
binary. It is not independent agent review. Prerequisite digest is
`504cd39f6d3aff54c89c79f7408e6cd4810e4230656b9c32fa0f2d2ec9b7a813`;
final digest is
`d2862e88e4e41815230e90268edc47b4f2c85da516d446a5988e2fb520a2f43b`.
Nine existing source/test files change and one test file is added. Before-edit
files and the initially failing test source remain in
`/tmp/lilscript-analyzer-storage-before-20260919/`. The 414,297,880-byte debug
binary is preserved at
`/tmp/lilscript-analyzer-storage-baseline-20260919/lilscript-c737e884cfca95f9`,
SHA-256 `3ce4fa2b83c405dbde96bfda2d0f20920dfa49e64130436d3ff53976ce269fdb`.

Schedule 18 records the admitted work; resource format 3 and search algorithms
are unchanged. Maps, nested type/signature/default payloads, alias/local-resolution
and binary-expression continuation scratch, diagnostics, debug consistency
scratch and comprehensive traversal/native stack accounting remain outside this
slice. There is no release/fleet, RSS, speed or compressed-size claim.

## Binary Continuation Admission

The [277-check receipt](run-2026-09-19T23-42-16.634Z/receipt.json), SHA-256
`040684ed2ddf57f3feb30e0d013cfca8a17b9eb3cb4c4926e81d961333d5dff9`,
qualifies admission of the existing binary-expression checker's temporary
continuation vector. The same callback budget admits exact backing and
simultaneous old/new growth storage. Each expression visit and continuation
probe, including the terminal empty probe, charges Analysis work. Existing
vector allocation, movement and pushes retain the shared Render tariff.

The traversal, leaf checker, contextual RHS expectations and diagnostic order
are unchanged. Success and ordinary failure drop the actual vector before
releasing its charge. A failure after opening a logical RHS scope restores the
original lexical/narrowing depth. Reentrant binary checking inside another
operand admits both live worklists. Panic unwinding drops the vector before
the existing callback scope rolls back remaining charges; this is not a
guarantee that a private Analyzer can resume after catching an internal panic.
No extra ledger, persistent counter, cache or depth cap is introduced.

Six new tests cover capacity-4 first allocation and 4-to-8-to-16 growth,
left/right tree directions, exact work by kind, first/later memory refusal,
every work cutoff for arithmetic and nested logical fixtures, nested worklist
overlap and subsequent reuse, diagnostic/deadline cleanup and an injected
private-storage panic while a continuation remains live. Existing deep-chain,
nullish contextual typing and real narrowing regressions remain unchanged.
The module-interface work test now subtracts the independently derived nine
units for its single binary expression; no resource cap is relaxed.

One incremental build takes 41.7 seconds; the eight non-overlapping test cohorts
take 27.8 seconds: semantic 181, service 33, checked-source 8, module contract 5,
integrated JS 8, native closure 9, policy 31 and captured-argument regressions 2.
All pass, with zero ignored tests or failed attempts. The existing 600-second
and 64-MiB command bounds are unchanged; no timeout, forced pipe closure,
full-fleet run, library rebuild or release qualification occurs. Times are
shared-host diagnostics, not compiler-performance measurements.

The [root read-only audit](binary-continuation-audit.json) verifies all 977
compiler/build inputs, stable before/after manifests, exact test identities,
five before-edit snapshots and the preserved binary. It is not independent
agent review. Prerequisite digest is
`d2862e88e4e41815230e90268edc47b4f2c85da516d446a5988e2fb520a2f43b`;
final digest is
`3b739ba7e605fe838937bcee96f73f9dd656a457793b6b0c50e9bb7a3d7d8350`.
Five existing source/test files change and one test file is added. Before-edit
files are in `/tmp/lilscript-binary-admission-before-20260919/`. The
414,705,656-byte debug binary is preserved at
`/tmp/lilscript-binary-admission-baseline-20260919/lilscript-c737e884cfca95f9`,
SHA-256 `d7efa3057db26f7972c9698b163b94770f2d994f1d4f9e705a743a2dd0c35f7a`.

Schedule 19 records the added accounting; resource format 3 and structural
search behavior are unchanged. Nested type/signature/default payloads,
separate condition-narrowing worklists/maps, alias/local-resolution scratch,
diagnostics, debug consistency scratch and comprehensive traversal/native
stack accounting remain outside this slice. There is no RSS, speed or
compressed-size claim.

## Condition Narrowing Admission

The [284-check receipt](run-2026-09-19T23-54-40.319Z/receipt.json), SHA-256
`70828fd8a797b065fce38b9eafb109ac3bdc30c9f8968c7772872952eb972388`,
qualifies the existing condition-narrowing traversal's pending and answer vector
backing. The existing callback budget admits both first allocations, old/new
growth overlap and the shared allocation/movement/push tariff. A fast-path leaf
query charges one Analysis unit; composed guards charge each traversal probe,
including the terminal empty probe. Returned map payloads remain separate.

The algorithm and branch-map operations are unchanged. Normal success, typed
refusal and semantic errors drop both buffers before releasing their charges.
Panic unwinding drops actual storage before the enclosing callback scope rolls
back; resumable Analyzer recovery is not promised. No new ledger, cache, hard
depth cap, map tariff or duplicated checker is introduced.

Seven new tests cover exact byte/work expectations for leaf, negation and both
Boolean tree directions; all four first/growing buffer allocation boundaries;
every work cutoff in a composed fixture; independent expected branch maps;
assignment invalidation, shadowing and active scopes; diagnostic/deadline
cleanup; an injected private-binding panic with both worklists live; and exact
repeated-prefix work. The prior binary-worklist test adds exactly three leaf
query units to its logical fixture's Analysis expectation. Caps and original
behavior expectations are not relaxed.

The same source guard returns different narrowing maps after scope narrowing,
assignment and shadowing. An expression-only result cache would be unsound.
The constructed all-true tree witness checks these Analysis totals, including
binary traversal and narrowing queries, but excluding Render work:

| Operators | Left-associated | Right-nested |
|---|---:|---:|
| 1 | 7 | 7 |
| 4 | 43 | 22 |
| 16 | 457 | 82 |
| 64 | 6,433 | 322 |

This establishes the existing repeated-prefix cost, not a timing/RSS improvement,
real-library speed prediction or permission to reassociate source expressions.
Map merging, nested types and leaf-resolution costs are not comprehensive work
tariffs. Any later optimization must preserve context and diagnostic order.

One incremental build takes 36.5 seconds; the eight non-overlapping test cohorts
take 27.9 seconds: semantic 188, service 33, checked-source 8, module contract 5,
integrated JS 8, native closure 9, policy 31 and captured-argument regressions 2.
All pass with zero ignored tests or failed build/test attempts. Existing
600-second/64-MiB command bounds are unchanged. No timeout, forced pipe closure,
full-fleet run, library rebuild or release qualification occurs. Times are
shared-host diagnostics, not compiler-performance measurements.

The [root read-only audit](narrowing-traversal-audit.json) verifies all 978 input
pins, stable manifests, exact test identities, five before-edit files and the
preserved binary. It is not independent agent review. Prerequisite digest is
`3b739ba7e605fe838937bcee96f73f9dd656a457793b6b0c50e9bb7a3d7d8350`;
final digest is
`e483ca9178e1abdb33d63418ceefc43e39ff76056423b67c59879dd7806055ce`.
Five existing source/test files change and one test file is added. Before-edit
files are at `/tmp/lilscript-narrowing-admission-before-20260919/`. The
414,785,392-byte debug binary is preserved at
`/tmp/lilscript-narrowing-admission-baseline-20260919/lilscript-c737e884cfca95f9`,
SHA-256 `7df4e1dde6914195aa34fb369db1f1a9f2a9e7a4ae9860f1798531e0ea4cdbbb`.

Schedule 20 records the additional work; resource format 3 and structural search
are unchanged. Hash/ordered maps and merge operations, nested type/signature/
default payloads, alias/local-resolution scratch, diagnostics, debug consistency
scratch and comprehensive traversal/native-stack coverage remain separate.
No compressed-size improvement or full migration completion is claimed.

## Sparse Narrowing Inputs

The [289-check receipt](run-2026-09-20T00-09-32.566Z/receipt.json), SHA-256
`06762f10049771ac8b158a29d8a64277b1d1c5c39734cbc20470b89d67c7d364`,
qualifies a bounded improvement to the existing binary checker. Continuations
carry a syntax-only narrowing input and true/false projection. Empty narrowing
branches are omitted from subsequent queries, without skipping expression
checking, runtime calls or source evaluation. Retained guards resolve names,
types and narrowing in the current scope; no resolved result is cached.

Projection does not discard diagnostic obligations: even a guard whose true
and false maps are both unused is still queried. A single shared leaf classifier
recognizes type checks and null comparisons for both full queries and input
construction. When both branches contain relevant guards, the existing complete
subtree is retained; unary negation is conservatively opaque. No source
reassociation, persistent index, second semantic owner or general linear-time
claim is introduced. Statement-level full queries remain unchanged.

Five new tests cover 2,560 shape/context comparisons against full traversal,
active scopes, assignment invalidation and shadowing; unknown and missing-fact
diagnostics despite discarded projections; all work cutoffs in a sparse guard
fixture; exact sparse-query work; and separate JS script/module observations.
The runtime fixture independently expects false/true/true/false and exactly two
effectful calls. A post-statement nullable access still fails. The existing
binary-work test removes exactly three now-skipped leaf queries, and the earlier
guard-free work witness now asserts the new linear counts. Historical receipts
and before-edit test sources preserve the previous expectations.

| Guard-free operators | Prior left | Prior right | Current, either direction |
|---|---:|---:|---:|
| 1 | 7 | 7 | 6 |
| 4 | 43 | 22 | 18 |
| 16 | 457 | 82 | 66 |
| 64 | 6,433 | 322 | 258 |

These are exact Analysis units, not total work or elapsed time. Additional
current tests with 1/4/16/64 trailing true operands verify `5N + 6` units for one
nullable guard and `9N + 15` for two adjacent guards. Sparse prefixes avoid
repeated empty traversal; dense guard trees may still repeat substantial work.

The [read-only layout receipt](narrowing-input-layout.json) checks both preserved
debug binaries using GDB's type information, without executing either target.
`BinaryContinuation` grows from 64 to 80 bytes; the input itself is 16 bytes.
The larger frame applies to nonlogical binary trees too, and is covered by the
existing exact-capacity admission tests. It can increase scratch pressure or
cause earlier memory refusal. Fewer probes do not imply universally smaller
memory, lower RSS or faster compilation. No pointer tagging or guessed layout
tariff hides this tradeoff.

One incremental build takes 74.7 seconds; eight non-overlapping cohorts take
27.8 seconds: semantic 193, service 33, checked-source 8, module contract 5,
integrated JS 8, native closure 9, policy 31 and captured-argument regressions 2.
All pass with zero ignored tests, failures or retries. Existing 600-second/
64-MiB bounds are unchanged; no full fleet, library rebuild or release run is
included. Times are shared-host diagnostics, not compiler-speed measurements.

The [root read-only audit](narrowing-input-audit.json) verifies all 979 inputs,
stable manifests, exact test identities, six before-edit snapshots, layout pins
and the preserved binary. It is not independent agent review. Prerequisite
digest is `e483ca9178e1abdb33d63418ceefc43e39ff76056423b67c59879dd7806055ce`;
final digest is
`269288a54694ef126db4e16bba350254c0a7cc2629a6c6641cd5cf672511295a`.
Six existing source/test files change and one test file is added. Before-edit
files remain in `/tmp/lilscript-narrowing-input-before-20260920/`. The
415,309,016-byte debug binary is preserved at
`/tmp/lilscript-narrowing-input-baseline-20260920/lilscript-c737e884cfca95f9`,
SHA-256 `577d715df53a175d22376ba098d65c5e445a62070d2b3033550973c896d5e8d4`.

Schedule 21 records changed work/layout behavior; resource format 3 and target
representation/search algorithms are unchanged. Maps, nested types, remaining
frontend ownership and whole-library qualification are still open. This is not
a measured release-speed, RSS or compressed-size gain.

## Frontend Release Checkpoint

003-R1 qualifies C7's unchanged 979 compiler inputs, digest
`269288a54694ef126db4e16bba350254c0a7cc2629a6c6641cd5cf672511295a`,
through the existing release/public-integration owner. No compiler or test source
changed for this checkpoint. Schedule 21 and resource format 3 remain current.

The [main receipt](run-2026-09-20T00-18-53.146Z/receipt.json), SHA-256
`e6301eff8cf5d57d39313452af9e6d1babb09894406e3bf43e6c967b7c5a116a`,
passes 89 public library tests, one CLI unit test, original direct/search/paired/native
consumers, exact CLI delivery/scoring/execution and the two expected refusals.
Release profiles use optimization level 3, no debug assertions/overflow checks
and no inherited compiler/profile overrides. The test build took 350.154 seconds;
delivery binaries took another 9.506 seconds. These are build times, not compiler
performance measurements.

The [cached affected-cohort receipt](run-2026-09-20T00-25-48.616Z/receipt.json),
SHA-256 `00d1cb34ed8d44fc9273948ce3a6b78606bb0128770ccef194bbffc79e40714d`,
passes semantic (193), source conversion (8), module contracts (5), rest capture
(2) and retired assignment-sink (6) checks. Cargo reports `fresh=true`; the test
binary is identical to the public run. Its 0.102-second build command performs
no compilation. Together the ten cohorts execute 303 distinct library tests in
18.786 seconds, with no failure, ignore or test retry; the CLI unit is separate.

Fourteen original JS deliveries pass authored independent observations and
canonical codec replay. The [comparison](frontend-release-delivery-20260920/receipt.json)
finds these and one native C delivery byte-identical to the accepted September 19
19:48 release. The native example compiles and executes; the separate public
native factory checks retain their GCC/Clang O0/O2/UBSan/ASan-LSan executions.
Final retained ledgers are zero. The CLI example remains 75 raw / 85 gzip / 79
Brotli bytes. Its Brotli-only compiler request deliberately reports gzip as null;
85 is the independent canonical replay, not an internally requested score.

The [root read-only audit](frontend-release-audit-20260920.json), SHA-256
`9addb531c581328f38a81723c88208b4b3db8f9662269d573cbfb751c7752ad9`,
checks all inputs, 227 retained evidence files, actual test identities, release
profiles, consumer hashes/observations and all five preserved binaries. It also
records an initial audit-probe error that assumed an unrequested gzip score was
present; only that audit assumption changed, with no qualification rerun.
No independent agent review is claimed.

Five binaries are preserved at `/tmp/lilscript-frontend-release-20260920/`:

| Binary | Bytes | SHA-256 |
| --- | ---: | --- |
| `lilscript` | 17856808 | `066d7d344694ee126c136a53b478750ea8daff7971a12961e729bad20f1539f7` |
| `lilscript-codec` | 2600976 | `fd21fda3d4e38ebb223e42b91fec9763aebaf5de187158bbc2397b1acc24a2f5` |
| `semantic-integrated` | 8883376 | `8c09a93c0507e1ce878d90730a97ec648d7e177fee4ae17d3380ffd722b36470` |
| `lilscript-118f13b3560da7bd` (library tests) | 39879536 | `c0066b199eca5446f966d26256d22b875fac2178e9d4823d4cd86ba210897b0c` |
| `lilscript-0f1288c2f91c3043` (CLI tests) | 1758832 | `8331a7d937d5e4f4cf26de839daf38487f5289d08c51b187d60144acecf8a834` |

This is a scoped release parent for subsequent selected library checks, not a
full fleet, competitor, speed/RSS or size-win qualification. Remaining frontend
accounting and migration prerequisites are unchanged; no milestone is closed.

## Resolved Type Ownership

003-C8 removes nine nested `Type::clone` calls from seven built-in argument
construction sites in the existing checker. Explicit Map/Set construction and
Map/Set/Task/Generator/Record annotations now move freshly resolved arguments
into their final boxes. Checked arity makes the consuming vector-to-array
conversion exact. Nominal struct/class parameter lists are borrowed from their
canonical metadata instead of allocating two temporary copies. Contextual
Map/Set inference still clones borrowed expected types where ownership requires
it; no type interning, cache, alternate resolver or additional owner is added.

The previous nested annotation path cloned an already resolved subtree again
at each enclosing built-in. This change removes that source of repeated copying,
not all type allocations or recursive traversal. Arity checks, left-to-right
argument resolution, key validation, shadowing and diagnostic precedence remain
unchanged. Schedule 21 and resource format 3 are unchanged because no admitted
tariff or policy changes. Full nested-type accounting remains open.

The [initial receipt](run-2026-09-20T01-13-34.821Z/receipt.json) passes **256 of
260** selected checks after a 38.048-second debug test build. All four new tests
fail on fixture assumptions: nested generic closers require spaces in the
existing grammar, and named-type spans end at the final argument rather than
including `>`. Production code is not changed in response. The failed receipt,
initial test source and binary remain preserved.

The [test-only correction](run-2026-09-20T01-15-31.176Z/receipt.json), SHA-256
`237c8223c42b2184a61c883effd0bd6fc72c092188f18d27ca76499dfde42d92`,
passes all four changed tests after one 13.580-second incremental build. Only
those four rerun; 256 source-identical passes remain reusable. The **260 distinct
accepted checks** comprise 197 checker tests, 33 public service tests, eight
source-conversion tests, five module-contract tests, eight JS integration tests
and nine native closure tests. None is ignored. The four new tests include
fifteen nested type/depth combinations, explicit/contextual collections,
canonical nominal metadata with Task/Generator shadowing, and twenty exact
diagnostic/span cases. These are behavior checks, not allocation or speed
measurements.

The [root audit](type-resolution-audit.json), SHA-256
`8a90a8948fdc92c5e22f0bdb77c9360a4a40abaa3c1534b0c1ad78b99b11775d`,
checks both stable manifests, forty output files and both preserved binaries.
Only the new test file differs between attempts. Relative to the prerequisite
979-input release checkpoint, only `src/semantic.rs` changes and the new test
file is added. Current **980-input** digest is
`ef50031f3894c1771480f02496c58d5672a5bc7997229b1fdd7e3eb36b0b68f6`.
The original source is preserved under
`/tmp/lilscript-type-resolution-before-20260920/`; final source/tests and the
415,206,584-byte debug test binary are under
`/tmp/lilscript-type-resolution-baseline-20260920/`. Binary SHA-256 is
`fff400653482e7a32b0a67d9e40409b63995b704552442dbfdda477cfb14c372`.
No independent agent review is claimed. No library/fleet or release rebuild,
measured speed/RSS, compressed-size win or complete migration claim follows.
The preceding release parent remains qualified only for its earlier inputs.

## Failed Iterations

- `run-2026-09-19T22-54-09.513Z`: 352 of 354 checks passed; two new Analyzer
  fixtures failed parsing due to constructor spelling. Corrected fixtures and
  resource-report assertions pass the affected 258-check rerun documented above.
  The failed receipt and initial fixture source remain preserved.

- `run-2026-09-19T17-22-14.526Z`: the first build found two existing internal
  search tests still calling the renamed evaluator. Their callers now use the
  structural evaluator directly; assertions and fixtures are unchanged. No tests
  ran from this failed build. Inputs were stable, and the failure is retained.

- `run-2026-09-19T16-30-40.643Z`: all selected nonempty cohorts passed, including
  the new multi-unit tests, but `semantic_program::uses_update::` matched zero
  tests. The runner rejected that result and retained the failed receipt. The
  corrected command uses `semantic_program::uses::update::`; no source changed
  between this invocation and the accepted 63-check receipt.

- `run-2026-09-19T16-11-12.065Z`: a test-only target-selector change missed its
  document-consumer child test. The build rejected its old Program argument.
  That lookup now uses the public semantic view after adoption, like the other
  callers; no production API or observation was changed.
- `run-2026-09-19T15-54-22.590Z`: the new optional module-boundary filter list
  was appended before its declaration. The qualifier stopped before invoking
  Cargo. The ordering fix retains the same 15 selected legacy module tests;
  this failed wrapper run provides no compiler qualification.
- `run-2026-09-19T12-47-49.810Z`: 117 focused Rust checks passed; CLI qualification
  stopped at a test regex expecting the source-conversion diagnostic, while the path
  checker correctly reported its earlier unsupported-default diagnostic.
- `run-2026-09-19T13-00-26.075Z`: compile failure showed that `parse_modules` tied
  source lifetime unnecessarily to AST arena lifetime. Separating those lifetimes
  permits checker/AST/arena release before backend work without copying source text.
- `run-2026-09-19T13-01-54.236Z`: ten service tests passed; the small-budget structural
  test exposed per-attempt work reservations consuming the entire effective budget.
  The corrected bounded fraction preserves room for baseline/discovery/other attempts.
- `run-2026-09-19T13-21-06.153Z`: scoped convenience wrappers required explicit
  closures for Rust's higher-ranked lifetime inference.
- `run-2026-09-19T13-21-58.712Z`: service/policy/admission checks passed, but old
  scoped-output tests expected the candidate's new descriptor cache to disappear.
  A one-time existing-binary semantic cohort found the same assumption in other
  renderer helpers. Success-only baselines now account for the legitimate cache;
  cold refusal, exact scratch cleanup, budget caps and final-zero checks remain.
  Review also moved descriptor installation after successful target preparation
  and fixed reusable-session handoff cleanup. Original reference source/oracle
  bytes and their historic hash remain pinned in a frozen test snapshot.
- `run-2026-09-19T13-55-30.113Z`: build passed; a new service test requested Optional
  work before baseline sealing. Its explicit pre-search rule/edit workflow now
  uses Baseline without weakening the ledger. An existing-binary semantic
  diagnostic passed 700/701 checks; the remaining new test expected a wrapped
  forbidden-tactic error instead of the existing direct variant. The corrected
  assertion still requires rejection and reports the actual error on failure.
- `run-2026-09-19T14-30-03.302Z`: the source-buffer/parser/verifier build and
  service/policy/parser/module cohorts pass; semantic checks pass 704/707.
  One assertion assumed the shared allocation helper's Render tariff meant
  target rendering only. Two memory caps were calibrated from invalid input
  that exits before successful verification's later workspace. Corrections
  independently measure successful publication while preserving exact-cap,
  below-cap refusal, unchanged-parent and final-zero assertions. This failed
  run does not qualify compiler source; the subsequent 842-check receipt verifies
  the corrected assertions and coherent main multi-file parser integration.

The failures remain recorded. The final tests retain the original fixture and require
successful structural exploration; they do not merely accept a surviving baseline.

## Limits

Default public routing remains legacy; the semantic route is explicit. Source-buffer,
parser-arena/token, checker fixed-table, construction and verifier admission is partial frontend coverage, not a whole-compiler
memory/time bound. Reports enumerate the uncovered phases. Native byte
counts describe C/header delivery, not linked executable size. Runtime estimates are
not runtime measurements. Complete descriptors survive handoff, but a recipe replay
engine is not implemented. Factory finalization holds source charges through actual
release; remaining checker and other frontend owners still need admission.
Full language/delivery coverage, original library
baseline builds, competitive matrix and release-speed/resource gates remain open.
No compiler-design contract changed and no complete migration milestone is closed.
