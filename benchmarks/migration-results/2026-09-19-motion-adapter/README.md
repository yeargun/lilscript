# Motion Caller Evidence

## Original Node Selection

All **11 original non-browser Node identities pass** in the recorded maintained
workspace's unchanged four-entry npm-test selection. Source and distribution
come from `/tmp/motionlil-cost-audit-20260911/current`, not the narrower sibling
checkout. The [discovery receipt](existing-dist/receipt.json) has SHA-256
`13478e56418f011009b0b74e39fd92065f4516d22294fe4a2550eb34909d3b77`.

The recorded workspace has no resolvable installed dependencies. Discovery uses
an isolated byte-identical copy at
`/tmp/lilscript-motion-node-discovery-20260919-Zr235m/motionlil`, with an explicit
symlink to `/home/azureuser/motionlil/node_modules`. Esbuild 0.25.9, Motion 13.1.0,
Terser 5.43.1, TypeScript 5.9.2 and Vite 6.4.3 match the recorded direct versions
and lockfile. This recovered environment is not the historical installed closure.
Playwright is absent; the four original browser entries remain unexecuted.

The [frozen inventory](../../libraries/motionlil.required-tests.json) preserves
all eleven identities, type/browser/site/pack obligations, and the distinction
between the default and full public entry. The existing
[inventory consumer](inventory-check.json) accepts this one-row selection while
retaining its coverage gaps. No compiler, installation or codec ran. Original
tests still run Terser, esbuild and Vite; their transformed outputs are not
independently scored compiler deliveries.

Eleven passive loads identify the original default barrel, its six feature
modules, root CJS, and full/mini/debug ESM files. Root CJS observes export names
and one numeric result. Full export presence does not establish behavior of
every export. Original caller tests additionally require retained callbacks and
unsubscribe, public prototype/shared-method identity, constructor/method arity,
unchanged control accessors and completion-promise identity.

The [root read-only audit](audit.json) verifies 24 discovery outputs, eight input
pins, 469 source files, 491 files including distribution, and all 1,761 recovered
dependency entries (1,518 file paths, 78,414,745 bytes). Original, copied, before,
after and current trees agree. There is no independent agent review. Discovery
took 6.1 seconds under a 90-second process-group bound, without timeout or forced
pipe closure; that is telemetry, not a performance qualification.

## Geometry Supplement

Five new independent observations pass against both installed upstream Motion
and the retained full candidate: **10 checks**, separate from the original eleven.
The [accepted receipt](geometry-r1/receipt.json) has SHA-256
`462522c111e9cbb28d4fb26f2569062e19115abe15e70df832e3bcec5181706c`.

| Observation | Independent expectation |
| --- | --- |
| Copy into a distinct box | Destination leaves retain identity; source leaves are not substituted |
| Overlapping source and destination | The second copy sees values written by the first, not an earlier snapshot |
| Later getter throws | Earlier `min = 5` remains visible; `max = 2` stays unchanged; original error identity and read/write order survive |
| Setter replaces the next leaf | The next copy writes the replacement leaf, leaving the old leaf unchanged |
| Both axes alias one object | Both deltas apply sequentially, producing `{min:8,max:12}` on the same object |

The candidate is the exact 194,166-byte `dist/full.js` from the parent discovery,
SHA-256 `b4a3328d68d3514804647a58248fa800522ebb767cc212fdda130b4c19c280a2`.
Passive records observe it and the separate upstream ESM entry. Complete parent
source/dependency and tool pins are checked before and after the supplement.
These observations constrain a compatible public adapter: snapshot-in/bulk-copy-out
is not equivalent for these inputs. They do not choose D2 or the portable
language's overlapping-reference policy, nor prove arbitrary proxy behavior.
The [feature mapping](public-features.json) distinguishes executed expectations
from the inspected by-value-shaped mutators and projection callers.

The [first setup receipt](geometry/receipt.json) remains failed: the shared Node
owner rejected an evidence directory inside the supplied input workspace before
running any tests. The corrected invocation uses the isolated library workspace
and a separate output directory. Test source and assertions are byte-identical.
The accepted run has 14 outputs, the failed setup eight; both pass the receipt
audit. No unchanged library retry or compiler repair occurred. The supplement
took 2.0 seconds under a 30-second inner / 60-second outer bound, not a speed claim.

## Remaining Scope

Original type checking, all browser entries, site/pack, installed-package and
global/UMD behavior remain required. Source build inspection finds nine concurrent
compiler invocations, selected through `MOTIONLIL_LILSCRIPT_BIN`, followed by
compatibility facades, esbuild and Terser. The current two-invocation qualifier
does not fit unchanged. Neither retained-distribution result supplies fresh
compiler provenance, semantic-backend support, competitive size or whole-library
qualification. This is evidence storage, not a second migration plan.
