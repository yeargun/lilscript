# Remark-Rehype Original Boundary Discovery

The single existing-dist discovery completed with the original type prerequisite
and all 21 distinct Node identities passing: 18 test nodes (including the official
parent) and three suite containers. No cases failed or were skipped. No compiler,
installation, library source/config/assertion change, or source-built replay ran.

- [Discovery receipt](existing-dist/receipt.json):
  `c71c89a9b873eab0ce53eb88be75a4d9edf449eb0d541bf9dfee9f566d98eb8a`.
- [Original case report](existing-dist/node/report.json):
  `08298e4edb05cfaedcf65c1eff6fbf96b3462ffef6a81e07391bcf29dae6ef3a`.
- [External supervisor](discovery-supervisor.json) ended with status 0, no timeout
  or signal, in 5.459 seconds. This shared-host observation is not a performance
  qualification. Its process-group deadline was 90 seconds with a one-second
  kill grace; the original type prerequisite had a 60-second ceiling.
- [Frozen inventory](../../libraries/remark-rehypelil.required-tests.json) retains
  every actual identity and the original four entry files. The discovery itself
  deliberately used an empty required inventory, so its qualification remains
  `unverified`; it is evidence for freezing cases, not compiler qualification.

The original command is `npm run test:types && node --test test/*.test.mjs
test/official/test.js`. The shared Node owner preserved the prerequisite ordering
with the installed TypeScript 5.5.4 `bin/tsc` and `lib/tsc.js`, the generated/source
declarations, and original type assertions. Offline npm used isolated empty user
and global configuration files; no package installation ran.

The 21 retained output files and 11 input pins were rehashed after completion.
Before, after, and current source trees agree: 35 source-only files and 41 files
including distribution artifacts. Complete installed closures also agree:
project dependencies contain 1,338 entries / 1,182 file paths / 58,633,333 bytes;
global npm contains 2,643 entries / 2,106 file paths / 11,734,603 bytes. These counts
include symlink destinations. The [before snapshot](existing-dist/before.json)
keeps file identity separate from each nested manifest fingerprint.

Four passive module-load records bind the actual executed/imported bytes:

| Artifact | Bytes | SHA-256 | Loads |
| --- | ---: | --- | ---: |
| `dist/remark-rehype.esm.js` | 14,163 | `32efb099f561ef40d5fb63cff9e5cdc0bc609bb2a2642e909406eda47ced7f96` | 3 |
| `dist/remark-rehype.closed.js` | 15,090 | `8519ba7d9910b3904d09be43b3fd502148ee1a588e1be44b2883ae13b451f503` | 1 |

Candidate ESM behavior is exercised directly and through the original unified
mutate/bridge consumers. Upstream `mdast-util-to-hast` supplies only two numeric
footnote-helper reference functions; it does not replace the candidate plugin.
The closed test checks import and callable-export shape, not tree conversion.
The site test reads existing files and imports its configured ESM callable; it
does not build the site or execute a browser. CJS/UMD behavior, installed-package
delivery, `check:pack`, and fresh `check:site` remain separate requirements.

The later separately authorized [source-built boundary](../2026-09-19-remark-rehype-baseline/README.md)
passed the original type/Node selection, package dry-run and codec replay using
the preserved release. Its evidence is distinct from this discovery. This
discovery provides no compressed-size, competitive, release, or current
semantic-backend library-support claim. This README is post-run narration, not a
frozen discovery input.
