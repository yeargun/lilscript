# Original Prerequisite Admission

001-Q3 removes the shared source qualifier's hardcoded `test:types` name.
The existing owner now consumes the exact original npm script declaration
from the frozen inventory and its accepted discovery report. This does not
broaden the two-build/five-JS-output recipe or change any library script.

Both the accepted receipt and inventory must pin the actual report. Original
case identities must match and all must pass. The one original prerequisite
must use the pinned npm executable with exactly `run` and its script name,
without optional flags. Its positive timeout stays within the existing ceiling.
Successful execution, complete supervision and exact executable/input identities
before, after and at final observation are mandatory. These checks run before
source copying, portgate or any producer invocation.

## Focused Verification

The [initial receipt](receipt.json) passes ten focused tests. A subsequent
[entrypoint receipt](entrypoint/receipt.json), SHA-256
`fc5ad375de2633c8d4357f649213c5024c5c28490ba19fc2de95fab9500e8dc9`,
passes all eleven after one integration test is appended; production code is
identical between runs. There are no failures, skipped tests or cancellations.
The final cohort finishes in 4.348 seconds under the unchanged 120-second
process-group bound. This is test duration, not compiler performance evidence.

Six new tests cover exact original script names, substituted report pins,
changed/omitted/optional commands, failed or incompletely supervised execution,
and all three before/after/final input sets. The actual archived Mdast,
Remark-Rehype and GFM discovery reports pass the same owner without executing
their suites. Four entrypoint probes use a synthetic pinned parent and producer
sentinels: report-pin, declaration and execution failures stop at the intended
gate; valid GFM prerequisite evidence reaches a deliberately mismatched case
count. All four retain failed receipts and invoke no producer. These are refusal
tests, not synthetic compiler qualification or a GFM source-built pass.

The [root audit](audit.json), SHA-256
`ed71f89ac533866147a5ee4318f2a8f241a3a0530c549e99667c927045cd4cf5`,
checks both receipts, all 25 final input pins, 14 recorded outputs, the
inventory-pinned dependency snapshot and all 46 original GFM fixture pins.
All 979 compiler inputs and `docs/compiler-design.md` remain unchanged.
Before-edit tool sources remain in
`/tmp/lilscript-original-prerequisite-before-20260920/`; both tested source
versions are preserved here. No independent agent review is claimed.

## Configuration History

[Read-only Git evidence](configuration-history.json) corrects the earlier
description of GFM's two fields as obsolete current-branch options:

| Field | Introducing Commit | Historical Behavior |
| --- | --- | --- |
| `terminal_cleanup_chain` | `79f4decfcc78e4dd6dcbc2d1c4572033084ceb14` | Optional extra generated-JS peephole work on finalists |
| `wide_single_use_collapse` | `f471f1fe3d6f56b1089c6179bd36b36c60a7b0f9` | Broader per-definition single-use collapse in emission |

Both introducing commits belong to `migration/target-tree`, not current HEAD's
ancestry. Neither field exists in the current deny-unknown-fields schema.
Their historical measurements are not fresh correctness or compression evidence.
The first is not a proven alias for current `ParsedPeephole`; the second requires
checked def-use, effects and evaluation-order legality. Neither warrants copying
the sibling compiler, restoring a generated-text semantic owner or forcing an
optional representation. Their legal intents belong to the existing common
family/edit/search design and need explicit configuration compatibility.

GFM's original source-built boundary remains unverified. No configuration was
dropped, renamed or translated; no compiler/codec ran. This change makes no
size, speed, broader delivery or milestone-completion claim. The only active
implementation coordinator remains [the migration plan](../../../docs/migration/index.md).
