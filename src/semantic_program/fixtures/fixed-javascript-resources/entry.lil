import { Point, score } from "./producer";

Point state=Point{0,0};

export void reset(int x,int y) {
    state=Point{x,y};
}

export int run(int x,int y) {
    Point local=Point{x,y};
    state=local;
    Point saved=local;
    return score(saved);
}

export int currentX() {
    return state.x;
}
