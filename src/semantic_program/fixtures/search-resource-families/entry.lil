import { Point, score } from "../fixed-javascript-resources/producer";

int fold(Point value) {
    return value.x * 10 + value.y;
}

export int run(int x,int y) {
    Point local=Point{x,y};
    Point saved=local;
    local.x+=1;
    return score(saved)+fold(local);
}
